# BUILD QUEUE — ideas to TEST, in fire order

**Magnus, 2026-08-11 (s31), standing directive:** *"you need to be constantly
putting experiments to test, there should be a queue with ideas to build, the
researcher will be responsible to make sure there are ideas to build."*

**OWNERSHIP.** RESEARCH keeps this stocked — it is their deliverable that this
file is never empty. BUILDER fires from the top and never idles waiting for
analysis. **An empty queue is a research failure, not a builder pause.**

**THE CLOCK (corrected 2026-08-13, lane-structure review).** Rated cadence is
**72 matches/day at HEAD** (20.0-minute pairing gaps, 40/40 recent; the old
"~420 at ~84/day" was measured 646→749 across the 08-10 cadence change and
averaged two regimes). No hard end date is on record — "over in a week"
(Magnus, 08-11) ⇒ roughly **300–390 rated matches left**. Convergence cost is
UNVALIDATED in both directions: "~100 matches" has no derivation on record,
and "~30 suffices" fits v104's tape but is contradicted by v116 (+32 in its
first 15, −57 in its next 27). What is certain: **displacement before a
ship's own gate arms buys zero rated information** — `SHIP_SIT_MIN_K: 8` in
PROGRAMME.md now binds. A ship converges in the BACKGROUND, so an unshipped
plank is a certain zero and every idle hour on the slot is spent.

**ADMISSION — an item is queueable only with all four:**
1. the CHANGE, named to the constant or branch
2. the MECHANISM METRIC only that change can move
3. the FIXTURE it is measured on, and whether that fixture can resolve it
4. why it is worth a slot **now** (`tools/target_value.py` band if it is a live leg)

**+ HOT-TURN RIDER (s36, from #44's cut — a standing constraint, not a
one-time reading): any item whose change touches per-turn work on the
raid/doctrine hot path carries one line in its GREP stamp — "hot-turn cost:
adds / neutral / reduces, vs the ~1,200µs GRAND margin" (v125's worst 900-area
turn runs at median 8,748µs of 10,000, in a 55µs-tight cluster, and
`get_cpu_time_elapsed()` reads ZERO locally, so no local test catches a
regression — the stamp at stocking is the only cheap gate).**

---

## ⭐⭐⭐ FIRE ORDER — RE-RANKED s36 2026-08-13 (research, on Magnus's question "do we need to re-rank now that maps lean toward bigger areas?" — answer: YES, scoped)

**THE GEOMETRY FACT DRIVING IT:** the rotated pool is 10 maps ≤676 + 5 maps at
900 — **a uniform draw puts ~33% of all games on a size class where our kill
does not fire and (per the amended `R1000_DEFEAT_AREA_MAX: 676`) an r1000 win
now SCORES.** Observed rated draw today: 8/40 = 20% (small n). Two rules and a
rider, then the ranking:
* **MECHANISMS DO NOT RE-RANK — SIZES DO.** Every row's mechanism is a code
  fact, geometry-independent. What changed is each row's PAYOFF WEIGHT
  (2/3 of games keep the kill currency; 1/3 move to the tiebreak currency).
* **EVERY kill-round bar and KILL_SPEED read is now CONDITIONAL ON AREA ≤676.**
  A bar pooled across classes mixes a 192-median surface with a
  does-not-terminate surface and reads as Simpson's paradox.
* ⚠ **RIDER ON EVERY SIZE NUMBER IN THIS FILE: it was measured on the RETIRED
  pool** (8 battery maps, 4 now gone; largest old map 625). Directions survive;
  magnitudes re-read before any verdict cites them.

⛔ **SUPERSEDED IN PART, same day ~11:3xZ (@a6b2df0): `R1000_IS_DEFEAT` is UNCONDITIONAL again** — the exemption's basis was the pathfinding defect v125 fixed; kills now fire on every class. **What survives of this block: the area-CONDITIONAL kill-round bars (as measurement), the stale-geometry rider, and the mechanism/size distinction. What dies: "r1000 scores on 900s" and #36's win-path framing (row re-scoped in place). Ranking impact: tier 0 (MAP_CODES) SHIPPED as v125; #36 drops from slot 1 to mid-order as re-scoped; #34 rises to top (the wincond read now guards a defeat-condition everywhere).**

**RANKING (top first):**
0. *(precedes the queue)* the builder's in-flight **MAP_CODES livelock fix** —
   mechanical loss on ~1/3 of draws outranks every row here.
1. **#36** 900-area win-path branch — we forfeit the scoring currency on ~1/3
   of games while banking thousands of Ti (today: 5/5 r1000 losses).
2. **#34** `wincond` backfill — the kill/tiebreak split IS the currency split
   now, and 63% of unrated files cannot be read for it.
3. **#38** kidnap/crash at 900 (after #17's local dose) · **#37** belt tap —
   the two offensive planks whose payoff CONCENTRATES on big maps.
4. **#39** as re-based (targeting prior; decode half done, table live).
5. Kill-lane rows (**#2, #21, #22, #23, #30, #33**) keep their s34 relative
   order for the ≤676 two-thirds — demoted by nothing except the rider above.
6. **#35** map name for unrated — rises slightly (area CLASS is decidable from
   mw×mh today; full name still matters for per-map books).

## ⭐⭐⭐ FIRE ORDER — RE-RANKED s34 2026-08-12 19:2xZ (research, on Magnus's instruction)

**THIS BLOCK SUPERSEDES POSITIONAL ORDER IN THE SECTIONS BELOW.** Rows keep their
numbers and their evidence; this is the order to fire them in. **Ranked on today's
measurements, not on the order things were queued.**

⛔ **THE RANKING RULE, stated so it can be argued with: no two rows here share a
currency.** A seat gap is in Elo, a forward-turret gap is in rounds, a threat term
is in builder-deaths/game. **I have NOT converted them to a common unit — that
would be false precision.** The order is: **(1) multipliers that make everything
else measurable, (2) the largest prize with a named lever and a stated size,
(3) the programme's own brief, (4) priced-but-modest, (5) conditional.**

### TIER 0 — MULTIPLIERS. Not planks. Do them first because everything after is worth more once they land.
1. ~~**`#18` `arena.py` must persist per-game rows.**~~ **DONE — `b4f56fa`, 2026-08-12 06:46:53 +0200, `arena.py:264`. The debt is PAID.** ⛔ **This entry outlived its row by a day and was still ranked Tier 0 item 1 when the repaired `cores_idle` picker started reading this block — so the fixed alarm's FIRST output was the one item we knew was done.** Removed by research s35. **The lesson is in the staleness note below the `#18` row, not here.**
2. **`#33` ablate `LOKI_GUNAXIS_PENALTY` (NEW s34).** Local, no rated cost, and it **decides whether a flag we SHIPPED in v114 does anything.** It gates `#30` and `#31a`, both of which may be addressing a population the incumbent already banks. **One battery answers what 5,000 archived games cannot.**

### TIER 1 — THE LARGEST PRIZE WITH A NAMED LEVER
3. **`#8` seat-relative scan order.** A **7.6pp self-play seat gap**, confirmed twice by two independent methods 0.2pp apart, maps explaining only ~1.8pp. **The ONLY row on this board carrying an Elo estimate (~+7-14).** Mechanical, changes an algorithm not a constant, no strategic risk. **If half of every game is played at a self-inflicted disadvantage, this outranks every strategic idea here.**
4. **`#23` — the FORWARD TIMING lever, NOT the cap raise.** Measured s34: our forward arm opens at **r33 against the field's r25**, in **78% of games against 87%**, at **2.28/game against 4.54**. Largest measured structural gap we have, with a named gate (`LOKI_FWD_MIN_HARV`/`ti_floor`, `raid.py:407-412`). ⛔ **The cap raise is DEAD (binds in ≤12.92% of games) and the pooled turret figure INVERTS the sign — do not re-derive either.**

### TIER 2 — THE PROGRAMME'S OWN BRIEF. Loki is the line; these are the line.
5. **`#17` does the crash weapon actually fire.** **Magnus asked for this directly.** The displacement channel is measured dead (Δ −0.080pp, CI crosses zero) and **all 9,372 archived throws are `EXILE` — the border-throw mechanism has never been tested at all.** Local, both-ways, no submission.
6. **`#5` crash induction at scale.** Highest ceiling on the board, approved class, already built, never scored on the currency. **Gated behind `#17`: do not scale a weapon before confirming it fires.**
7. **`#13` ambush the rebuild.** **22.6% of our turret-destructions produce a rebuild on the EXACT tile within 25 rounds** (33.8% within d²≤2, 42.5% within d²≤8), off 15,958 events.

### TIER 3 — PRICED, BUILDABLE, HONESTLY MODEST
8. **`#28` `LAUNCHER_RESERVE` dose.** Two independent surfaces converge (bank median 54 Ti vs a ~120-160 Ti gate → 5.7-12.4% of games can afford one; the decode says 3 of 55 build one). One constant.
9. **`#14` the idle builder gets a destination.** **25.76% of our builder-rounds before the core dies are idle AND free** — both cooldowns 0, `run()` executed, nothing chosen — and it is the SAME in wins, so not a losing-position artefact.
10. **`#30` sentinel threat term** and **`#31a` GUNBLOCK** — **effectively TIED and both modest: 0.32 vs 0.29 forward builder deaths/game addressable (~25 vs ~23 Ti/game).** Both gated behind `#33`. ⚠ **`#31a`'s "BEST EVIDENCED, BUILD FIRST" label is withdrawn** — it rested on the stale 92%.

### TIER 4 — CONDITIONAL, SYMPTOMATIC, OR CHEAP-BUT-OFF-CURRENCY
11. `#16` non-strike surcharge (the only self-limiting defence mechanism in the field literature) · 12. `#26` conveyor density (cheapest graft from the #1 team, but its channel is economy, which is INSTRUMENTAL under `R1000_IS_DEFEAT`) · 13. `#10` blind their gun with their own body · 14. `#19` `NOISE_ON` (only bites batteries that pin it) · 15. `#9` EXILE-FIRST · 16. `#6` best-fit sentinel repeat · 17. `#7` ore-barrier carve-out (ranked last deliberately — enemy economy is instrumental).

### ⭐⭐ CONSOLIDATION — `#21`, `#22` AND `#23` ARE ONE GAP, NOT THREE
**Do not build them separately.** `#21` (gunner count 1.86 vs 10.77) and `#22` (turrets after r150: us 1.95 vs 3.28-4.75) are **facets of the same deficit `#23` measures directly: the FORWARD arm, which opens late, in fewer games, at half the count. The home arm is fine — we are first at r7 against the field's r8 and present in 96% of games against 81%.** ⇒ **`#23`'s timing lever reads out first; re-measure `#21`/`#22` after it, because both may be downstream.**

### WHAT IS NOT ON THIS LIST AND WHY
**`#20` demoted** (title claim was false) · **`#12` blocked** on a control · **`#29` SALT demoted to the LEAF payoff** (trunk case closed at both ends: chain-head tile occupied by median r8, and the near-core chain has EIGHT entries not one) · **`#24` gated** · **`#25`/`#27` are NOTES, not planks.**

## FIRING NOW
| # | plank | change | mechanism metric | status |
|---|---|---|---|---|
| 1 | **LOKI-27 ferry-first** | ferry request outranks home exile | INSERT:EXILE ratio | ⭐ **SHIPPED 13:14Z** — v112 LIVE, verified on the `Active bot:` line. Ends v104's 29 h 25 m hold. Rollback: `fcode submission activate 104`. |

## NEXT UP — ready to build

### ⭐⭐ STOCKED 2026-08-15 (non-lane instruments session, on Magnus's framing: *"A 55 is not the goal, that just gives us an indication the arm might be on to something. When we unlock the real valuable piece it should show 60+"*). ONE row. GREP run against the live incumbent `_v223sealrepair` BEFORE counting.

| # | plank | GREP | mechanism metric | fixture | why now |
|---|---|---|---|---|---|
| **71** | **FUND THE COLLAR BEFORE THE KILL WINDOW, NOT AFTER IT. `SURGE_MIN_RND = 300` WHILE OUR WINNING ARMS KILL AT MEDIAN 226–259 — the eco→pressure conversion fires past the decision.** The screen says the wins live in `eco.py` and the winners win by NOT DYING rather than by killing sooner — so the bank is being converted into survival, and the one branch that converts it into PRESSURE is gated past the window where our games now end. Plank: make the surge gate KILL-CONDITIONAL (fire on siege/contact state or on a bank-vs-round curve) instead of a fixed r300, so surplus Ti becomes hands **inside** the 200–300 band where the decision is actually landing. | **GREP: PASS vs `_v223sealrepair`.** `SURGE_TI_FLOOR = 1500`, **`SURGE_MIN_RND = 300`**, `SURGE_EXTRA = 5`, `SURGE_ECO_CAP = 24` (`doctrine.py:402-405`) — the gate is an **absolute round constant**, with no contact, siege, bank-curve or opponent-state term. `ECO_CAP = 18` (`doctrine.py:30`) likewise absolute. `eco.py` carries **13** `get_current_round()` branches and only **2** references to the enemy core/kill at all — **the economy is essentially blind to the kill.** ⚠ **AND THE r300 GATE IS DELIBERATE, NOT AN OVERSIGHT — its reasoning is on the record at `doctrine.py:388-401`:** *"by round 300 every class of game is either decided (rushes), in its siege endgame … or an economy race, and only the last both reaches the Ti floor and profits from more hands."* **This row does not claim that was wrong when written; it claims the DISTRIBUTION MOVED UNDER IT.** `_v69bc` already measured the naive version (**−13 pts**: raising the standing target inflates the +20%/builder scale from the opening) — that failure mode is why the plank must stay *strictly surplus, strictly conditional*, and must NOT touch the opening curve. | **median kill round (TREAT vs CTRL) as the PRIMARY dial, not win share** — the whole thesis is that the gain converts to a FASTER kill. Secondary: banked-Ti-at-end (must go DOWN), builders alive at r200–300 (must go UP), and share of wins by `core_destroyed` vs `tiebreak` (tiebreak share must NOT rise from its current 6.1–7.5%). ⛔ A win-share gain with a FLAT or RISING kill round does not clear this row — that is the incumbent behaviour it is trying to beat. | local corefill screen vs `_v223sealrepair` at n=5,400 (the screen band is ±1.32pp, so a real 60% arm is unmissable), then a pre-registered live unrated leg before any ship claim | **THE SCREEN SAYS ECO IS THE ONLY SUBSYSTEM WITH A HIT RATE.** Per-plank footprint vs v140 against each plank's solo screen: **`bodyaware` = 105 lines of `eco.py` and NOTHING ELSE → 53.70% (n=10,800)**; `maxstack` (eco 116) → 52.41%. Every raid-heavy plank screens at or below 50 — `spawnlock` 49.70, `sentshell` 48.75, `bodyblock` 47.26. **1 of 1 pure-eco planks wins; 0 of 4 pure-raid planks do.** Meanwhile the leaders top out at 55.24% and all kill LATER than control (`MIX280mix4` TREAT 236 / CTRL 209; +20–47 on every leader except `AWRLNCH` 205/208 and `BODYAWR` 205/207, which are FLAT). ⇒ **the 55% wall looks like an attrition ceiling: longer games trend to coinflips.** 60% overall needs **61.4% of DECIDED games** vs 56.3% today (~93.5% of games end in a kill) — a +5.1pp shift in the kill race, which no amount of extra survival buys. ⭐ **START FROM `AWRLNCH`** (`_v267awrlnch` = `bodyaware` + `homeearly`, 53.95% at n=5,399 with a FLAT kill round) — it is the one arm already winning without slowing the kill, it is hand-built with no `stack.py` stamp, and **it appears in ZERO combinations.** ⚠ **RELATIONSHIP TO `#36`:** that row is the same eco→pressure thesis scoped to 900-AREA maps and framed on the idle bank; **this one is map-independent and names a specific mechanism (the round gate) with a measured mistiming.** Build whichever, but do not double-count them as independent evidence. ⛔ **NOT A VERDICT — no lane owns this and none was typed.** It is a prioritisation claim from a local screen; per the standing rule a road opens or closes on live-game evidence only. ⛔⛔ **AMENDED s45 2026-08-16 ~08:3xZ (research, as queue owner) — TWO OF THIS ROW'S THREE LEGS ARE RETRACTED. THE PLANK SURVIVES; ITS RATIONALE MOSTLY DOES NOT. ADD-ONLY; nothing above is rewritten.** ⛔ **LEG 1 RETRACTED — "the winners win by NOT DYING rather than by killing sooner" and "all leaders kill LATER than control (`MIX280mix4` 236/209, +20–47)".** Those are **kill-win-CONDITIONED** medians and the treatment moves the denominator: an arm converting marginal LOSSES into WINS adds precisely the hard slow games to its own numerator (`corr(denominator asymmetry, conditioned timing) = −0.79` across winning arms; `NULL114`, at −0.5% asymmetry, shows no bias). **On the UNBIASED ITT RMST₃₀₀ — now `PROGRAMME.md`'s adopted estimator — the leaders are FLAT and the eco arms are significantly FASTER:** `MIX280mix4` **−0.87** [−3.18,+1.43] · `MIX281mix4` **−0.29** · `MIX284mix3` **+0.21** · `TRIO` **−0.34** · **`BODYAWR` −6.84** [−8.61,−5.08] · **`AWRLNCH` −6.43** [−8.88,−3.99]; controls `NULL114` +0.42 flat, `NEG114` +33.27 slower, `MAPCODE` −60.81 faster. ⇒ **NOBODY IS BUYING WIN SHARE BY LENGTHENING GAMES. The 55-class is kill-neutral and the eco planks kill EARLIER.** Record + four-case control matrix: `docs/research/RMST-ESTIMATOR-2026-08-16.md`. ⛔ **LEG 2 RETRACTED — "the 55% wall is an attrition ceiling: longer games trend to coinflips."** It rested on LEG 1, **and its second support is independently dead:** the *"they convert four times better late"* table in `raid.py` is **PRE-map-rotation**; re-derived post-rotation, **r200–300 is ~parity (0.97–1.05, convention-sensitive) and r300+ has gone 0.43 → 0.82** (`docs/research/KILL-HAZARD-REDERIVED-2026-08-16.md`). **Both supports gone ⇒ the attrition-ceiling explanation of the 55% wall is UNSUPPORTED. The wall is real; this row no longer offers a mechanism for it.** ✅ **LEG 3 SURVIVES INTACT AND IS NOW THE ROW'S WHOLE CASE — and it is immune by construction: the MISTIMING is a statement about ABSOLUTE ROUNDS, not a T-vs-C contrast, so no collider touches it.** `SURGE_MIN_RND = 300` (`doctrine.py:402-405`) fires **after** the median kill lands — **and it does so for the INCUMBENT too (v140 median kill 209), not merely for the leaders**, which is a stronger claim than the row originally made. The `eco.py` blindness stands unchanged (13 `get_current_round()` branches, 2 references to the enemy core). ⭐⭐ **PRIMARY METRIC REPLACED — MANDATORY FOR ANY ARM OFF THIS ROW: the dial is `ITT RMST₃₀₀`, NOT "median kill round (TREAT vs CTRL)".** The original primary IS the statistic that produced LEG 1's artefact, so an arm scored on it would inherit the same bias. **The disqualifier is re-cut accordingly: it read "a win-share gain with a FLAT or RISING kill round does not clear this row" — on the unbiased estimator FLAT is what a legitimately kill-neutral arm reads, so it becomes: a win-share gain with a SIGNIFICANTLY POSITIVE RMST₃₀₀ does not clear; FLAT clears on win share alone; NEGATIVE is this row's own thesis landing.** ⭐ **"START FROM `AWRLNCH`" IS STRENGTHENED WITH ITS REASON CHANGED:** it was named *"the one arm already winning without slowing the kill"* off a flat conditioned median — **on RMST it is not flat, it is 6.43 rounds FASTER, and `BODYAWR` is faster still at 6.84.** ⚠ **But the builder's arm for `LEG-fieldcal-2026-08-16` is `BODYAWR`, not `AWRLNCH`, on attribution grounds**: `AWRLNCH` = `bodyaware` + `homeearly`, and `homeearly`/`rnd1` measured null solo (pooled 51.26%, [50.33, 52.19]), so its second plank adds attribution noise rather than signal. **Read "start from AWRLNCH" as superseded for LEG purposes; it stands for COMBINATION work.** ⚠ **THE ARITHMETIC-OF-60 CLAUSE IS UNAFFECTED** (60% overall needs 61.4% of decided games vs 56.3% today) — it never depended on the timing comparison.

⛔⛔ **AMENDED 2026-08-15 ~20:5xZ, SAME SESSION, AFTER READING `raid.py`'s OWN HEADER. THE OBSERVATION STANDS; MY IMPLICATION FROM IT WAS WRONG AND IS WITHDRAWN.** The row originally read as *"eco is the only subsystem with a hit rate ⇒ dig further in eco."* **`raid.py` is `LOKI-1 raid layer — THE COLLAR`, and its docstring already priced this exact trap:** *"TIME IS THEIR ASSET: any design that merely lengthens games moves us into the window where they convert four times better."* Its measured per-window kill hazard vs opponents ≥1550: **ours 15.1 / 5.9 / 7.7 / 9.8 %** over r0-150 / 150-200 / 200-300 / 300+, **theirs 9.8 / 5.9 / 12.5 / 40.9 %** — ratio 1.54 early, **0.62 by r200-300**. And: *"We START kills fine — 29.8% of games produce one, median r148 — and then fail to CLOSE about 70% of them."*
⇒ **THE CORRECTED READING, and it is less comfortable than the original: our "winning" arms are executing the documented LOSING pattern.** They win more AND kill LATER (leaders 226–259 vs control 209), i.e. they buy win share by lengthening games toward the window where the opponent converts **four times better**. **That is the 55% ceiling — a mechanism, not a coincidence.** Eco planks improve SURVIVAL; the bottleneck is CLOSING; those are different modules and different jobs.
⇒ **THE 60 LIVES IN THE COLLAR — which is exactly where FIVE planks have already failed** (spawnlock 49.70, sentshell 48.75, collarseal 47.71, bodyblock 47.26, catapult open). Harder than "dig in eco", and it is what the evidence and the module's own thesis both say. **Do not read the eco hit-rate as a map to the prize; read it as an explanation of the ceiling.**
⭐ **AND IT MAKES THIS ROW'S MECHANISM CONCRETE RATHER THAN VAGUE.** The collar is CHEAP: a barrier is **3 Ti for 30 HP**, bot-impassable, breaking one costs 15 pecks at 2 Ti = **a 10:1 exchange in our favour**, and **four diagonal raiders seal all eight core-adjacent seats — sealed, the defender's heal rate is ZERO and every point of damage becomes permanent** (`raid.py` header). Meanwhile the only bank→pressure conversion is `SURGE` at **r300**, and the kill lands at **174–236**. ⇒ **The plank is an ECO→RAID HANDOFF: spend surplus bank on COLLAR MATERIAL inside the kill window, instead of on hands after it.** Not "more economy", not "more survival" — *fund the seal earlier*. The primary metric (median kill round) was already right and is now justified by the module that owns the job. |

### ⭐ STOCKED s36 2026-08-13 (research, on the builder's relayed Magnus ask: "core experiments on the new maps — what paths would be interesting"). Four rows, greps run BEFORE counting, all vs the live incumbent.

| # | plank | GREP | mechanism metric | fixture | why now |
|---|---|---|---|---|---|
| **36** | **THE 900-AREA ECO-AS-KILL-ENABLER — ⭐ RE-SCOPED s36 ~11:3xZ on the R1000 reversion (@a6b2df0, Magnus: "offensive team"): the ~4,800 Ti we bank idle on big maps buys KILL PRESSURE (raiders, turrets, ammo, sustained assault), NEVER delivery-for-tiebreak — a tiebreak win scores −10 everywhere again** | **GREP: PASS vs `_v187saltidle_f`** — `mw`/`mh` are read only for geometry bookkeeping (`main.py:158,311`, `raid.py:883` — border clamps, angle seeds); **NO cap, gate or doctrine branch keys on map area** (`ECO_CAP=18` `doctrine.py:30`, `SURGE_ECO_CAP=24` `:405`, both absolute integers) and **no tiebreak-mode branch exists anywhere in the tree**. ⚠ RE-SCOPED: the branch condition survives (area>676 ⇒ the absolute caps starve the bank into idleness — a CODE FACT unchanged by the currency reversion), but the SPEND changes: convert the idle bank into kill capacity (builder count, forward turrets, ammo bank), not delivery. The old win-path/delivery framing is DEAD with the reverted currency. **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | median kill round + kill share on 900-area games (v125 baseline now exists: panel medians 153-167), with banked-Ti-at-end as the mechanism dial (target: the 4,805-Ti idle bank goes DOWN because it was spent on pressure) | local battery on the five 30x30s vs starter + pool opponents (mechanism); PANEL-CAL-1's 900-draws + the rated tape (currency) | **Post-v125 the kill FIRES on 900s (panel: zero tiebreak wins) — so the residual question is whether the idle bank converted to pressure kills FASTER/more reliably. Lower urgency than at stocking (the livelock fix took the crisis out), kept because the bank-idleness code fact stands and every r1000 game is once again a defeat that eco-pressure could have closed.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `ECO_CAP=18` (doctrine.py:30) and `SURGE_ECO_CAP=24` (:405) confirmed byte-identical (doctrine.py<1619 is unchanged v218mapfix→v223sealrepair outside one unrelated edit at :1228 — see #52/#53). main.py:158,311/raid.py:883 carry unchanged (0 diff in either file). No map-area branch or tiebreak-mode branch added anywhere in the 239 changed doctrine.py/eco.py lines (which are the SEAL_TI_FLOOR value change + a new L4 trunk-repair block, both orthogonal to eco caps). Premise fully intact. | ⚠ **CLOSED-BY-LEG ANNOTATION (SOFT LINK), added s40 2026-08-14 ~15:5xZ.** The index flags this row as feeding `_v213ecoraid`'s closed leg by DOCUMENT lineage rather than by its own text — **weaker than `#42`'s link and stated as such.** The relevant fact either way: **the eco family's local screens (53-55) never survived a live surface (rc8.5 pooled 46%, v135 era 4/25, MC −1, MD 0), and its last pre-registered road closed on live matched evidence ~11:49Z.** ⇒ **this row's SPEND thesis (convert the idle 900-area bank into kill pressure) is NOT refuted — it was never the thing tested — but any arm that spends the bank THROUGH the eco set inherits that record.** ⛔ **AND A SEPARATE DEFECT IN THIS ROW, found today: its title says "900-AREA" and the segment term as used elsewhere covers FIVE 400-area maps. Whoever builds this must state whether it means ACTUAL area ≥900 (drakkarfjord, glacierkeep, midgard, ragnarok, valkyrie — five maps) or the new-pool label. They are different sets and the row currently reads as if they were one.**
| **37** | **TAP THE BELT — the OFFENSIVE half of the siphon: plant OUR conveyor beside THEIR harvester and wire it home. The engine splits their output with us 50/50, team-blind.** *(Provenance: engine-MEASURED at the primary, not inference — instrumented probe in `doctrine.py:860-886`: 49/49 stacks to the only acceptor, then 99/98 strict alternation over 800 rounds; wild corpus: 4.41% of OUR mined stacks banked by enemies through exactly this shape. Transferability: it is our own engine, exact.)* | **GREP: PASS vs `_v187saltidle_f`** — the SIPHON block is DEFENSIVE ONLY: WIRE arm wires OUR orphan harvesters (`SIPHON_WIRE_ON` `doctrine.py:899`), DENY arm pecks enemy taps on OUR harvesters (`SIPHON_DENY_ON` `:915`). **No code path plants a conveyor adjacent to an ENEMY harvester; the offensive arm is ABSENT.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | stacks banked by US from enemy-built harvesters per game (decode: delivery attribution off `flow`/`econ`) + tap-conveyor survival rounds (their DENY latency — a play-the-players read for free) | local dose vs pool bots first (does the tap get built and survive; measures THEIR deny response — note our own tree ships DENY, so self-play is the WORST case, the same asymmetry as the crash guard); **live unrated for the currency read** | ⛔ **RE-BASED s36 ~11:5xZ on the R1000 reversion (missed in the first sweep, caught during the team-lazy admission pass): "the tiebreak now SCORES" is DEAD. What survives: game_share still pays for tiebreak wins on the LADDER, so the tap is INSURANCE for games that reach r1000 despite doctrine (~6% historically) — real but small. DEMOTED accordingly; the team-lazy profile independently rules a belt-cut leg off-currency vs that opponent (their doc, habit 3). Build only if a cheap rider on another leg. ⛔ CLASS CAVEAT s36 (second confirmation): belt-CUTTING is DEAD vs HEALING teams — Leviathan heals belts in place (127 heals vs our 79 cuts in one game); lazy replaces-never-repairs. ⭐⭐ REPRICED s36 ~12:4xZ (builder + Magnus independently rediscovering the round-robin the same hour): vs CORE-TANK teams the tap's payment is DENIAL, not credit — the 4 HP/Ti heal wall is income-funded, and a tap halves a harvester's output WITHOUT destroying anything, so heal-in-place defence NEVER TRIGGERS (nothing to repair). Belt-cut dies to healers; belt-TAP is invisible to them. Composes with #42: sentinel damage that loses to 4.78 heal/round wins when the heal budget halves. PROMOTED back to live as #42's economic half vs the core-tank class. ⛔ ONE ENGINE CONSTRAINT THE DESIGN MUST CARRY (the tree's own measurement, doctrine.py:896): a lone stub conveyor is a DEAD END — accepts exactly ONE stack, then blocks forever. Sustained 50% denial therefore requires a FUNCTIONING ROUTE HOME (long on exactly the GRAND maps where their sprawl maximizes access) or a keep-accepting sink; a bare stub denies 10 Ti once and stops. The wire cost is the plank's real price and the prereg must budget it.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `SIPHON_WIRE_ON=True` (doctrine.py:899) and the dead-end-stub comment (:896) confirmed byte-identical. `SIPHON_DENY_ON` actually sits at doctrine.py:913 (the row's own :915 is a pre-existing 2-line imprecision, not introduced by this carry — doctrine.py<1619 is byte-for-byte identical across this version bump). Still WIRE(defensive)/DENY(defensive) only — no offensive plant-beside-enemy-harvester path added. Premise fully intact. |
| **38** | **KIDNAP/CRASH AT 900 SCALE — the approved class, aimed where it pays most: 1000-round games, enemy builders hundreds of tiles from home for hundreds of rounds** | **GREP: PASS vs `_v187saltidle_f`** — EXILE (enemy pickup d²≤2, throw to the site FARTHEST from our core, `raid.py:900-932`) + FERRY shipped; **NO border-tile selector, no map-area keying**; launcher build sits behind `LAUNCHER_RESERVE=80` (`doctrine.py:965` — `#28`'s starvation question applies unchanged). Cross-refs: `#17` (the LOCAL both-ways dose check remains the precondition), `#5` (scale mechanics), `#24` (we are the top table's launcher singularity — nobody defends against this). **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | enemy unexplained unit removals per game on 900-area games (`crash_census` method: field base 2,451 removals/1,855 games) + throws landed on border tiles | `#17`'s LOCAL both-ways dose FIRST (unchanged precondition — do not skip to live), then live 900-area draws | **On maps where the kill does not fire, games run 1000 rounds — a permanently destroyed unit compounds ~5x longer than in a r200 kill game, and it moves BOTH tiebreak keys (harvesters alive, collected throughput). Same mechanism as the approved class, new trigger geometry — per the class ruling, no new organiser question needed.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `LAUNCHER_RESERVE=80` (doctrine.py:965) confirmed byte-identical. raid.py:900-932 EXILE mechanism carries unchanged (0 diff). No border-tile selector or map-area keying added. Premise fully intact. |
| **39** | **THE OPENING BOOK OF THE NEW POOL — per (team, map) modal first-harvester tiles off live replays → one 3-Ti barrier each as pre-emption. Harvesters build ONLY on ore; a barrier on the modal tile taxes their key-1 currency from round ~10.** *(Provenance: determinism DOCUMENTED for one family — Ouroboros v8 build queue diverges at r3 keyed on our opening signature, spitball 2026-08-08 + the steering note; INFERENCE for the wider field — and the table itself measures who is deterministic before a single game is spent on them.)* | **GREP: PASS vs `_v187saltidle_f`** — barriers are used ONLY for seat SEAL (`LOKI_BARRIER_SEAL_ON`, `raid.py:267-277`) and as a generic build option (`main.py:507,740`); **no ore-tile denial, no opening-keyed pre-placement, nothing reads enemy build history.** ⛔ **CORRECTED BY ITS AUTHOR MINUTES AFTER STOCKING (Q9 discipline — the original objection re-run against the row):** `builds.tsv` is TURRET-ONLY (launcher/gunner/sentinel — verified on the file, 200k rows sampled, zero harvester rows). **The real surface is `events.tsv`: (`BUILD`, `harvester`) rows with rnd/x/y exist league-wide (35,737 in the first 500k rows).** One cut, but off events, not builds. ⚠ **MAP ATTRIBUTION, CORRECTED TWICE (s36, builder's archiver read supersedes my staleness framing): `league_games.tsv` was RETIRED 2026-08-11 (documented in corpus_sanity — no producer, no consumer) and `replay_archiver` NEVER had a league pass — so there is NO live league-wide per-game map surface, and none silently broke.** **PATH CHOSEN (research, s36): build the book from OUR OWN decoded games** — the (MAP, SEAT) convergence control justifies it: the field lands on map-geometry tiles, so our opponents' halves of our own games sample the same modal tiles, and the table thickens with every match we play. A league per-game producer is queue-able later if a per-TEAM book ever earns its cost; `mw`x`mh` ambiguity (5-way on 30x30/20x20) only bites league-wide files anyway — our own games carry map names via `ladder_games` (rated) and area class via `events` (unrated). **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | their first-harvester DELAY in rounds vs their own modal baseline (decoded, per team per map) + our spend (3 Ti + 1% scale per barrier) | research builds the modal-tile table from live replays (ZERO games spent); local probe vs teams the table calls deterministic; live leg for currency | **The pool rotated TODAY — every team's opening book on 10 new maps is being learned right now and will re-converge to determinism within days. A table built THIS WEEK is first-mover; the same table a week late is archaeology. This is `play-the-players` in its cheapest form.** ⭐⭐ **MEASURED s36, SAME SESSION — THE BOOK EXISTS AND IS (MAP, SEAT), NOT (TEAM, MAP): enemy-only first-harvester modal tiles off our rated games since 08-12 (side-mapping a/b→0/1 fixed after the author caught his own filter never matching): fjordgate seat-1 16/18 (89%) on (3,7) · nordkap seat-0 12/14 (86%) on (9,2) · fjordgate seat-0 79% · antler seat-0 75% · drumlin/archipelago 40-62%. CONTROL: our OWN book has the same modal tiles — the convergence is MAP GEOMETRY (best ore per seat), field-wide, which is why one table covers everyone.** ⛔ **AND THE PLANK AS FIRST WRITTEN IS UNREACHABLE: the modal tile stands beside THEIR core at median r4 — no builder of ours can be adjacent by then. The mechanism is therefore NOT r4 pre-emption. It is (i) a TARGETING PRIOR — raiders route to a 75-89%-confident eco tile with zero search, worth most on 900-area maps where vision is small against the map — and (ii) denial of their LATER expansion tiles (2nd/3rd harvester, mid-game, reachable), for which the same decode yields the table. Mechanism metric re-based: rounds from our first raider crossing midline to first enemy-harvester kill, vs incumbent baseline.** New-pool cells are n=1 still; the table refills daily from the same cut. **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — raid.py:267-277/main.py:507,740 carry unchanged (0 diff in either file); barriers still SEAL-only, no ore-tile denial or opening-keyed pre-placement added. |
| **40** | **PRE-SEAL OUR OWN SIEGE RING vs point-blank-sentinel killers (team lazy habit 1) — ⚠ DEFENSIVE: carries `DEFENCE_ADMISSION_BAR: kill_round_non_regression`** *(Provenance: OPP-team-lazy-profile-2026-08-13.md, HP-verified 20/20 — 12/12 of their core-shooting turret tiles at d²≤5 from our core; they rebuild on IDENTICAL tiles and never heal turrets; their only barrier-counter is 2dmg/2Ti pecks vs 30HP + our 1Ti/4HP heal. Documented, single opponent, structural habit.)* | **GREP: vs `_v197mapcode`** — home defence is TURRET-only (single home-gunner defender, `main.py:320`, `SLOT_HOME_GUN` `doctrine.py:938`); the ONLY barrier-seal in the tree targets the ENEMY core's seats (`raid.py:267-277`, `seatkeys`); **no home-ring barrier logic exists.** | median round of first enemy turret-build at d²≤8 from OUR core (profile baseline: their builder reaches d²≤8 then builds 1-6 rounds later) + kill-round NON-REGRESSION (the bar) | local battery vs a point-blank-siege fixture + live unrated (the class of opponent is identifiable in the field) | **Our core dies in 46.3% of games and this opponent class kills ONLY through that ring; 3 Ti/tile pre-seal by r40 vs their measured +9-round defensive-build latency. Admission bar attached at stocking, not discovered later. ⭐ DUAL-PURPOSE s36 (Leviathan autopsy): a SECOND class wants the same tiles — Leviathan's last-mile capture (cut our core-adjacent conveyor, barrier that tile NEXT ROUND; g4 held 14/21 of our spawn tiles for 750 rounds) is denied by the same pre-seal. Two confirmed opponent classes, one 3-Ti answer — the case strengthened materially. ⭐⭐ AND NOW MEASURED FIELD-WIDE, s36 tri-arm decode: the chronic-camp precondition (enemy turret d²≤64 of our core sustained ≥100 rnds WITH our belt cut, `tools/camp_detect.py`) FIRED IN 9 OF 25 CONTROL GAMES, across THREE of the five opponents tested (Leviathan 4, Juusto 3, LingLing40 2; Coreflood and team lazy zero) — **camping our core ring is not a two-opponent quirk; it recurs across a majority of opponents sampled, though NOT universally**, and it is the precondition of the income lock the tri-arm just confirmed. The pre-seal denies the tiles that behaviour needs.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `SLOT_HOME_GUN=7` (doctrine.py:938) confirmed byte-identical. main.py:320/raid.py:267-277 carry unchanged (0 diff in either file). No home-ring barrier logic added by the v140 diff. Premise fully intact. |
| **41** | **FORWARD-SENTINEL SITING AT d²14-32 + BARRIER THE ADJACENT TILES (team lazy habit 2; composes with the #23 family)** *(Provenance: same profile — 30/31 of our forward-turret deaths involve gunner fire, 0 sentinel; counter-gunner built NEXT DOOR (d²=1) in 31/52 pairs; our forward turret median life 10 rounds; their new-build latency +3..+29 rounds.)* | **GREP: vs `_v197mapcode`** — `_try_forward_sentinel` sites within d²≤50 of target (`raid.py:668`) with **no minimum distance and no adjacent-tile denial**; nothing barriers around our own turrets anywhere in the tree. | forward-sentinel median lifetime (baseline 10 rounds) + shots-landed-per-sentinel | local battery for mechanism; live for lifetime (their reactive CB is the treatment surface) | **A sentinel at d²14-32 outranges the reactive counter-gunner (r²=13, obstacle-blocked) BY CONSTRUCTION; a 3-Ti barrier on the next-door tile forces their measured +3..+29-round rebuild latency instead of the d²=1 insta-counter. Cheap, offensive, composes with forward placement.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A/B** — raid.py:668 carries unchanged (0 diff). The row's ‘nothing barriers around our own turrets anywhere in the tree’ claim re-checked against the full 239-line doctrine.py/eco.py diff: the only changes are the SEAL_TI_FLOOR value (12→0, the ENEMY-core seat seal — unrelated to own-turret protection, see #52/#53) and a new eco.py L4 trunk-repair block (relays conveyors, not barriers). No own-turret barrier logic added. Premise fully intact. |
| **42** | **⭐⭐ VOLUME-NOT-SEQUENCE — UPGRADED s36 ~12:3xZ from diagnosis to THE TOP OFFENSIVE PLANK on the Leviathan autopsy: confirmed vs TWO core-tank opponents with closed arithmetic (their heal 4 HP/Ti vs our best damage 1.8 dmg/Ti = a 2.2× Ti deficit — ONE sentinel structurally cannot kill a core-tank; net core damage in our four Leviathan losses: 0/0/14/0 HP of 2000; the one game with TWO simultaneous sentinels is the one we won, g1 second-landed r208 → core dead r357). The diagnosis half below is DONE and feeds the design; launcher-evicts-healer now confirmed vs BOTH core-tanks (healers park on the ring both times).** *(Provenance: profile M2g4 — WE built 21 forward sentinels SEQUENTIALLY, never 2 firing simultaneously; their core-tank out-heals any single sentinel (9 healers, 803/870 heals on core) — 12 HP/round beats it, 6 does not.)* | **GREP: vs `_v197mapcode`** — `LOKI_FWD_GUN_CAP = 3` *alive at once* (`doctrine.py:1237`) so the CAP is not the ceiling; candidate mechanisms in-tree: one-raider build seriality, `LOKI_FWD_TI_FLOOR=40` between builds (`doctrine.py:1264-1265`), replacement-only-on-death. **Which one binds is not determinable by grep — that IS the row.** | max simultaneous live forward sentinels per game (decodable from events BUILD/DEATH intervals — no new games needed for the diagnosis half) | ✅ **DOSE BAR MET LIVE s36 ~17:0xZ — THE DIAGNOSIS IS CLOSED AND THE BINDER IS NAMED: TRAVEL SERIALITY, NOT REPLACE-ON-DEATH AND NOT THE TI-FLOOR.** Arm C (TWORAID, second raid seat) vs the v125 control on 5 pinned opponents x 5 pinned maps, opponent-version pinned (`--match`), decoded by research: **max simultaneous siege sentinels UP in 4 of 5 cells — Coreflood 3v2 (mean 2.4v1.4) · LEVIATHAN 5v2 · team lazy 3v3 (mean 1.8v1.4) · LingLing40 2v1 · Juusto 1v2. The Leviathan max of 5 is the largest overlap measured anywhere in the corpus**, and it lands against the core-tank class whose out-heal arithmetic (4 HP/Ti vs 1.8 dmg/Ti) is what made volume the plank. FWDFLOOR8's morning acquittal removes the TI-floor candidate; the s36 corpus cut had already refuted replace-on-death (297/348 consecutive builds independent of predecessor death). ⇒ **the row's QUESTION is answered; what remains is the SHIP decision on TWORAID/COMBO, which is the builder's and is gated on singles' finals.** W/L descriptive only at n=25/arm (A 12 · B 9 · C 13). Prior diagnosis half, kept for provenance: ** (a) **"never 2 at once" is NOT universal — max simultaneous SIEGE sentinels (d²_enemy≤32 at build): 1 in 90/173 games (52%), 2 in 54, 3+ in 29.** M2g4 was the modal case, not the rule. (b) **The builder's replace-on-death prior is REFUTED as the binding mechanism: 297 of 348 consecutive forward builds start INDEPENDENT of the predecessor's death (only 51 within ±6 rounds of it).** Remaining candidates: TI-floor pacing or single-raider travel seriality — a local battery discriminates. (c) win split by max-overlap: 1→67%, 2→87%, 3→81%, 4+→7/8 — **INFERENCE-CONFOUNDED, labeled: overlap is partly an EFFECT of games going well (survival), not a cause; do not cite as the plank's payoff.** Fix half: local battery; volume-vs-core-tank: live | **Against core-tank opponents a single sentinel can NEVER kill (measured out-heal), so this defect/limit caps us at zero vs a whole opponent class. Diagnosis first: if seriality is deliberate doctrine, the row becomes a design question for the builder, not a bug. Composes: the profile's launcher-evict (their healers park on the core 8-ring for consecutive rounds; one 0-ammo throw evicts one — no team check) is the cheap second dial on the same out-heal arithmetic: 12 HP/round beats 9 healers, but 6 HP/round beats 7.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `LOKI_FWD_GUN_CAP=3` (doctrine.py:1237) and `LOKI_FWD_TI_FLOOR=40`/`LOKI_FWD_MIN_HARV=2` (:1264-1265) confirmed byte-identical. Premise (seriality is the binder, not the cap or the floor) fully intact. | ⛔ **CLOSED-BY-LEG ANNOTATION, added s40 2026-08-14 ~15:5xZ (from the CLOSED-BY-LEG INDEX, which exists because `#47` recruited a fire order on half its evidence).** This row's own text names TWORAID/COMBO, and **those feed `_v213ecoraid`, whose PINNED LIVE MATCHED LEG closed at net 0 (MD 5/20 vs MB 5/20, same cells/maps/anchors, `ourver` and `oppver` exact per cell, sourceMatch pointers confirming the pin) — coordination.md 2026-08-14 ~11:49Z.** The builder's verdict there: ***"the ECORAID/APPRLAUNCH map-gated road — the eco family's LAST pre-registered road — closes on LIVE MATCHED evidence. The family's local screens (53-55) NEVER SURVIVED ANY LIVE SURFACE: rc8.5 pooled 46%, v135 era 4/25, MC −1, MD 0."*** ⇒ **THIS ROW'S DIAGNOSIS HALF IS UNAFFECTED — the core-tank arithmetic (their 4 HP/Ti heal vs our 1.8 dmg/Ti, the 2.2× Ti deficit, one-sentinel-cannot-kill-a-core-tank) is a MEASUREMENT and stands.** What is affected is the ECO-family DELIVERY of it: **any arm that reaches this plank through the eco set is reaching through a family with four consecutive live failures, and its prereg must say so and argue the difference.** ⚠ **Not a kill: the simultaneity mechanism is not the eco mechanism, and nothing live has tested TWO-SENTINELS-AT-ONCE. But the row could previously be read as unfired, and it cannot now.**
| **43** | **BARRIER-IN-BASE CRASH CONFIRMATION — two-arm unrated leg (approved class if it confirms; cross-refs #38/#17)** *(Provenance: profile fact 8 — 6/6 no-damage builder removals, ALL theirs, ALL in the one game we planted barriers inside their base, 5/6 at r81-111 at d²1-5 from their core; one spawned r98, vanished r99, never moved. Crash vs `self_destruct` undecidable on the wire.)* | **GREP: vs `_v197mapcode`** — we build barriers in enemy base only via the seat-seal (`raid.py:267-277`); nothing TARGETS barrier placement to induce pathing exceptions; the EXILE/FERRY launcher code (`raid.py:900-957`) is the only other displacement source. | no-damage enemy unit removals per game, barrier-arm vs no-barrier-arm | **two-arm unrated leg** (same opponent class, barriers-on vs barriers-off) — the profile's own suggested design | **If confirmed this is a THIRD crash trigger (approved class, new geometry: their own pathing vs our barriers) that costs 3 Ti a tile and fires while we siege anyway. If refuted (self_destruct), one leg closes it forever.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — raid.py:267-277,900-957 carry unchanged (0 diff); seat-seal and EXILE/FERRY displacement code unaffected by the v140 diff. **⭐ RE-READ s47 2026-08-16 vs the LIVE CONTROL `_v223sealrepair` (research, on the side lane's `GREP:`-scope audit; every anchor below re-verified here by hand, `ZZQXNOTATOKEN` control = 0/0/0/0 across all four files):** ⛔⛔ **STALENESS FINDING THAT INVALIDATES THIS ROW'S LEG DESIGN AS WRITTEN — the POSITIVE half of the grep is now FALSE.** The cell asserts *"we build barriers in enemy base **only** via the seat-seal (`raid.py:267-277`)"*. **There are now THREE barrier build sites in `raid.py`, and two were added after this row's stamp:** `raid.py:277` (seat-seal, the one the row knows about), **`raid.py:500` (`# (1) SALT THE CORPSE`, `raid.py:485`)**, and **`raid.py:558` (salt-beside-belt, gated by `_salt_beside_belt`)**. ⛔ **CONSEQUENCE FOR THE FIXTURE, AND IT IS THE WHOLE POINT OF THE ROW: the proposed two-arm "barriers-on vs barriers-off" leg CANNOT be built by toggling the seat-seal alone. The two SALT paths would keep planting barriers in the "barriers-off" arm and CONTAMINATE THE CONTROL** — the leg would read a null it could not have avoided. ✅ **Row stands and the question is unchanged; the ARM DEFINITION must now silence all three sites, and any prereg written off the old cell would have fired a broken control.** ⭐ **This is the cheapest kind of save: a precondition read that costs minutes and would have cost a whole leg.** |
| **44** | **⚠ SELF-AUDIT: v125 RUNS AT 87.6% OF THE TLE CEILING ON 30x30s — measure the margin BEFORE stacking more per-round work on GRAND maps** *(Provenance: profile fact 7 — our execmax 8,757µs of 10,000 on 900-area replays; team lazy runs at 6,701. A CPU-timeout turn silently skips; enough of them is a lost game with no traceback.)* | **GREP: vs `_v197mapcode`** — `_cpu_exhausted` guards exist (`raid. **GREP-PATH: corpus/econ.tsv** *(s46: this row has NO BOT TREE BY DESIGN — its subject is an instrument. Path verified present on disk 2026-08-16. Checked: `cpu_max_us` — the row's own text names platform-replay `execTimeUs` as the ONLY honest surface, because `get_cpu_time_elapsed()` reads 0 under local `fcode run`.)*py:196,670`, `main.py:442`) **but `doctrine.py:1072` records `get_cpu_time_elapsed()` READS 0 UNDER LOCAL `fcode run`** — local batteries cannot see this; the only honest surface is platform-replay `execTimeUs`, which `econ.tsv` already aggregates (`cpu_max_us`). | distribution of per-turn execmax on 900-area platform games, by round band (the corpus cut costs zero games) | ⭐ **CUT DONE s36 (zero games, v123/v125 since 08-12):** **v125 on 900-area runs its worst turn at median 8,748µs — 87% of ceiling — in a TIGHT cluster (p95 8,803, max 8,804, n=31): a SYSTEMATIC hot turn on essentially every 900 game, not a tail event.** v123's 900 median was 2,963 — **the MAPCODE fix bought correctness and spent the CPU** (a livelocked bot does less work). ≤676 classes sit at 3,300-6,900. **Actual TLE interrupts so far: ZERO** (`tled=0` across the population). Margin arithmetic: ~1,200µs (12%) before the hard 10,000 on the median GRAND game. Monitor graduation (builder's suggestion, endorsed): watch current-version 900-area `cpu_max_us`; the cluster's tightness makes a small threshold meaningful. ⭐ **MECHANISM RESOLVED s36 (builder, verified by research at the code): the cluster IS our own guard — `CPU_BUDGET_US = 8000` (`doctrine.py:1076`) + ~750-800µs overshoot between 64-step BFS checks (`eco.py:431`). Risk re-read: TLE margin is held BY DESIGN, not by luck; a 9,200+ reading means the guard coarsened or an unguarded path grew — unambiguous signal either way. The residual finding is a QUALITY tax (builders truncate BFS mid-path many turns/game on GRAND maps), deferred below. Standing design rule (builder's): new GRAND planks compete for the 8ms and carry their own guard checks.** | **Every plank the queue adds work to (siting logic, volume, book lookups) spends from a budget nobody has measured; 12.4% margin on the biggest maps is thin, and the local-reads-zero note means NO local test would catch a regression. One cut prices the whole risk.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `CPU_BUDGET_US=8000` (doctrine.py:1076) and the ‘reads 0 locally’ comment (:1072) confirmed byte-identical. The 64-step BFS check is now eco.py:434 (was :431, +3 shift from the new LOKI-L4 docstring lines), content byte-identical. `_cpu_exhausted` guard sites raid.py:196,670/main.py:442 unchanged (0 diff). ⚠ NEW SINCE v139: eco.py's new `_l4_repair` path calls `self._cpu_exhausted(ct)` once per builder-turn before scanning, on top of the existing per-turn work — an ADDITIONAL hot-path check consistent with this row's own HOT-TURN RIDER. The 87.6%-of-ceiling margin this row measured has NOT been re-read post-L4 and should be, since L4 adds real per-turn work (nested 4-direction adjacency scans) before its own exhausted-CPU check can fire. |
| **45** | **KILL THE BUILDER, NOT THE LADDER — vs point-blank creepers the turret is a renewable resource and the builder is not** *(Provenance: OPP-lingling40-profile + OPP-team-lazy-profile, both 2026-08-13, and a research recount of the tri-arm's 75 pinned games. LingLing40 v40: **80 siege turrets, median d²=5 from OUR core, 45 at d²≤5, 14 at d²≤2, 58 of 80 gunners**, walking a ladder tile-by-tile (r89→r112 in one game) with **rebuild latency 1-2 rounds**. team lazy v222 independently: **48 siege turrets, median d²=5, 31 at ≤5**. Two teams, one shape, map-controlled.)* | **GREP: vs `_v197mapcode`** — our raider attack priority `pr=3` (`raid.py:616`) targets the TURRET; nothing targets the enemy BUILDER adjacent to a turret it is feeding, and `LOKI_GUNAXIS_PENALTY` (`doctrine.py:1533`) makes us AVOID the axis rather than walk it to the source. | enemy builder deaths within d²≤8 of their own siege turret, per game · and siege-turret REBUILD latency (baseline 1-2 rnds) | local battery vs a point-blank fixture; live cells C1/C4 of CAL-3 are both ladder teams | **Rebuild latency 1-2 rounds means every turret we kill is refunded before our next action — we are paying 2 Ti/swing into a renewable resource. The builder feeding it has 40 HP, no armour, and stands adjacent. Composes with #40 (deny the tiles) and with the launcher (evict rather than kill).** ⭐ **ITERATION 1 READ OUT s37 2026-08-13 (`docs/prereg/DOSE-feeder45-2026-08-13.md`): mechanism WORKS, exposure-starved — dose bar NOT MET (FEEDER45 in 1/16; 6-7 kills-shots in the games where a sentinel had the pair in range; off-branch 0/4 with plants present).** Cause (a) confirmed by construction: `get_gunner_target` offers no target choice, so feeder-first only governs SENTINELS, rarely sited at the creep arrival. **Two engine facts banked: builder melee CANNOT target enemy builder bots (`can_fire=False` on adjacent enemy builder, every occurrence; predicate-level — ungated `fire()` untested per the side lane's scope rider), and class attrs don't share across units.** ⇒ the only anti-feeder tools are TURRET FIRE and LAUNCHER EVICTION. **ITERATION 2 FORK, research prioritisation (s37): (i) launcher eviction FIRST.** Grounds: 0 ammo vs 30 ammo/feeder-kill for the sentinel path against a 1-2-round rebuild economy; pickup d²≤2 is HOME-turf siting (creepers come to us at median d²=5) while (ii) inherits the open forward-siting problem (#41 blocked, #30 blocked/demoted); and the lever is constant-sized (iteration mill). ⛔ **BUT TWO GATES, NOT ONE — ablating only `LAUNCHER_MIN_RND=160` (doctrine.py:1536) can null while `LAUNCHER_RESERVE=80` (doctrine.py:965, #28) still starves the build: the Leviathan autopsy shows the bank PINNED AT 12 Ti in exactly the sieged games (under-latch, main.py:242), where cost+80 is unreachable. Iteration 2's dose must log the gate arithmetic per round (bank vs cost+reserve vs round-gate) so a no-fire attributes to a named gate, not to the pair.** Mechanism metric for eviction ≠ builder deaths: feeder-ABSENCE rounds near their siege turret / rebuild latency stretched off its 1-2 baseline (eviction displaces, it does not kill — the EXILE displacement signal (+0.265pp, #5) is prioritising-only and measures vanish-undamaged, a different quantity; do not cite it as the mechanism). **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.** **⭐ BISONS-v8 NAMED CUSTOMER s39 (BOOK-bisons-v8-2026-08-14.md): their entire damage output walks to our core as an unescorted, melee-less builder at r25-33 on an opponent-blind script — and they have built ZERO launchers in 892 archived games, so a home launcher is uncontested. The most predictable kidnap/kill target on the ladder; they are ~38 pts below us and closing at +11-18/match.** **⭐ 0033 = SECOND NAMED CUSTOMER s39 (BOOK-0033-2026-08-14.md): 0 launchers in 2,307 archived games (opponents built 751 in the same files); their damage requires an unescorted melee-less builder standing in our spawn ring from median r43 (p10 r13) on a script opponent-blind to build #14 — same kidnap geometry as the Bisons, arriving earlier.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `LOKI_GUNAXIS_PENALTY=8` (doctrine.py:1533), `LAUNCHER_MIN_RND=160` (:1536) and `LAUNCHER_RESERVE=80` (:965) all confirmed byte-identical. raid.py:616/main.py:242 carry unchanged (0 diff). Two-gate arithmetic (MIN_RND + RESERVE) fully intact. |
| **47** | ⭐ **CONDITIONAL SIEGE LAUNCHER (#28 × #45-iter3) — build ONE launcher on APPROACH DETECTION, not standing and not at siege time** *(Provenance: DOSE-siegelaunch45 iter-2 falsifier with clean attribution — with BOTH gates waived, EVICT45 0/16 and every refusal `gate=eco`, bank 1-44 Ti vs cost 52-61 through the siege window; the Leviathan under-latch bank-pin reproduced on the local fixture. Detection PROVEN: SIEGE45 fired 15/16.)* **⛔ THE EVIDENCE CORRECTION THIS ROW EXISTS TO CARRY: unconditional pre-build is SCREEN-OPPOSED, not screen-backed.** The launcher family finals (all n=5408, era arms vs `_v146gunaxis` control, self-play, between-shard): **LAUNCH0 52.77±1.33 — and LAUNCH0 is the NO-launcher arm (`_v161launch0`, LAUNCHER_CAP=0, 0 throws in its own positive control)** · LAUNCHLATE160 51.42 · LAUNCHLATE80 50.74 · BOTH0 (launcher PAID, mechanisms off) 46.43 · LAUNCH2 44.67 · LAUNCH3 43.73. ⇒ **launcher premium = LAUNCH0−BOTH0 = +6.34pp at identical mechanism; earlier = monotonically worse.** The surviving design is CONDITIONAL: trigger the build when enemy builders APPROACH (creep precedes plants; LingLing40's ladder starts r89 — the bank is still healthy pre-drain), cap 1, waive MIN_RND/RESERVE only inside the triggered state. | **GREP: vs `_v197mapcode`** — no approach-triggered launcher build exists; the gates are `LAUNCHER_MIN_RND=160` (doctrine.py:1536), `LAUNCHER_RESERVE=80` (doctrine.py:965), the one-way latch + `SLOT_HARVESTERS<1` (main.py:593-638); siege detection exists only in the `_v200siegelaunch` prototype. | feed-interruption (feeder-absence rounds near their siege turret · rebuild latency stretched off the 1-2 baseline) **+ the per-round gate log (bank vs cost+reserve vs trigger round) so a no-fire attributes to a NAMED gate** | `_probe_creeper` dose per the DOSE-siegelaunch45 bar pattern → corefill non-regression vs `_v197mapcode` (**the unsieged-games premium is the named hazard this bar screens for**) → pinned live leg vs CAL-3 C1/C4 with its own prereg | **Why now: iter-2's falsifier fired with clean attribution TODAY; detection is proven; and the 2×2 names the exact hazard (−6.34pp standing premium) an unconditional design walks into. Fallback if approach-detection triggers too late to beat the bank drain: the spend-freeze fork (touches the under-latch, main.py:242 — deeper, #36's territory).** Evidence: screens documented (finals on disk, era + self-play caveats inline), dose local, engine facts from iter-1/2. ⭐ **UPDATED s37 same-day (iter-3 dose, `docs/prereg/DOSE-launch0evict45-2026-08-13.md` — the arm itself is retired on the screen correction, but its dose data transfers): THE MECHANISM HALF IS DEMONSTRATED. With a launcher pre-existing: 205 evictions, ALL r<160, 0 ammo — the road iter-2 proved eco-shut is OPEN when the launcher exists. Best case midgard 993003: 18 evictions and the ladder NEVER FORMED (0 plants) — feed-interruption in its strongest form. So iteration 4 tests ONLY the TRIGGER + the PREMIUM: dose bars = (a) triggered games: launcher exists BEFORE first plant; (b) unsieged games: launcher count 0 (the premium hazard bar); mechanism carry-over needs no re-proof.** Two design fixes from iter-3, both pre-registered into any iter-4 prereg: **(1) COLLIDER — do NOT condition validity on CREEP45 plants: the treatment SUPPRESSES plants (iter-3's own bar excluded the mechanism's best game); condition on enemy-builder APPROACH (the exposure), not on the outcome the mechanism prevents — **operationalized (side-lane sharpening, s37): the validity denominator is games with ≥1 enemy builder observed in OUR half of the map, a pre-treatment quantity readable from the same replays whichever arm runs.** ⭐⭐ **ITERATION 4 DOSE MET, s37 same-day (`docs/prereg/DOSE-apprlaunch47-2026-08-13.md`, verified at the doc): approach detection buys the launcher at a HEALTHY bank — replay-decoded builds r48/r52/r52/r55 (+1 second at r81), vs iter-2's bank-dead 1-44 Ti window. Frostgate: 6/8 games at 41-152 evictions/game, two games where the ladder NEVER FORMED. Midgard sharpens the siting stratum: build 4/8 but CONVERT 1/4 — a home-sited launcher's pickup d²≤2 does not reach the 900-area feeder ring. ⇒ REMAINING LADDER: (1) APPRLAUNCH corefill screen = bar (b), the unsieged premium — queued seed 228000 with a D26 replication rule declared at queueing; (2) 900-area SITING is the next design lever after the screen and BEFORE the live leg (the big-map stratum is a siting problem, per the row's own pre-registered classification); (3) pinned live leg vs CAL-3 C1/C4 — builder's prereg + research window coordination, only if the screen reads inside-band.** | (2) MAP STRATUM — frostgate 6/8 games evicted, midgard 0/7 WITH the launcher existing: siting-vs-creep-path differs by map; read the strata separately and treat a midgard zero as a siting question, not a mechanism null.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `LAUNCHER_MIN_RND=160` (doctrine.py:1536) and `LAUNCHER_RESERVE=80` (:965) confirmed byte-identical. main.py:593-638 latch/gate carries unchanged (0 diff). No approach-triggered launcher build added. Premise fully intact. | ⛔⛔ **STOP — LIVE ROAD CLOSED, ADDED s40 2026-08-14 ~15:4xZ. THIS ROW WAS ABLE TO RECRUIT A LIVE LEG ON HALF ITS OWN EVIDENCE AND VERY NEARLY DID.** Everything above is the SCREEN evidence. **The LIVE evidence, which this row did not carry until now: `_v207apprlaunch` IS THIS ROW'S ARM — its doctrine block opens *"LOKI-APPR (QUEUE #47 = #28 x #45-iter3): build ONE launcher on APPROACH"*, citing the row by number — and it ALREADY HAD ITS PINNED LIVE LEG. `MC` read 8/20 against `MA` (v125, same cells, same maps, same anchors) at 9/20 = NET −1, against a PRE-COMMITTED +4 bar (coordination.md 2026-08-14T08:07:23Z, side-lane-verified scores, `ourver`=136 on all five, oppvers = anchors exactly). The builder's verdict at the time: *"the 52.67 pooled local screen (n=10,800) and this live matched null are the same story as ECORAID's: THE FAMILY'S LOCAL EDGE DOES NOT SURVIVE CONTACT WITH UPWARD OPPONENTS… the plank's non-map-gated ship path was already gated on live n≥50; this closes the map-gated alternative. Road closed on LIVE evidence (point 6 compliant)."*** ⇒ **THE TWO SCREENS SOMETIMES CITED AS "#47 PASSED TWICE" ARE APPRLAUNCH's OWN (52.67 pooled n=10,800; APPRLAUNCH2 52.39 replicating) — they are the local edge the live leg refuted, NOT independent support.** ⇒ **NO LIVE LEG OFF THIS ROW WITHOUT A NAMED DESIGN CHANGE AND AN ARGUMENT FOR WHY IT SURVIVES WHERE `MC` DID NOT.** ⚠ **HOW THIS NEARLY COST A WINDOW, recorded so the shape is recognisable: on 2026-08-14 ~15:3xZ this row was ranked to the TOP of the live fire order — by me, reading it cold — on the strength of "screen-passed twice", with the unrated pipe free for the evening. The hold came from going to the primary to write the prereg, not from the row. **A row that lists its screens exhaustively and omits its live null is not neutral; it actively recruits.** Durable fix beyond this row: a queue row's evidence block must carry BOTH dimensions — ANSWERED-BY-SHARD and CLOSED-BY-LEG.** ⭐ **INHERITANCE FROM `#58`'s REFUTED LEG, s40 2026-08-14: THE PLANT IS NOW LIVE-VALIDATED AND YOU DO NOT HAVE TO REBUILD IT.** EVICT58's pinned leg measured **1.240 launchers built/game against v140's 0.341 (3.6×, 31 across 9 of 25 games)** — so the **approach trigger, cap-1 live census, Ti floor and conditional siting all demonstrably FIRE in live games.** `#58` died on PICKUP OPPORTUNITY (0.04 evictions/game vs a >1.0 bar) because 0033's builders never enter the d²≤2 envelope — **a FORWARD-siting problem against a back-sitting opponent, which is precisely the failure mode `#47`'s HOME siting does not have.** ⇒ **`#47`'s remaining question is narrowed to whether HOME-side pickup opportunity exists (do the opponents whose builders DO come forward — Jython, Focalground, LingLing40 at 22-33 throws/game against us — present targets to a home launcher), and the plant half of its build is already paid for.** ⚠ **This does NOT reopen `#47`'s own live road: `MC` read net −1 and that stands. It changes what a NEW design would have to argue and what it would have to build.**
| **48** | ⭐⭐ **PARKED-RAIDER TERMINAL IDLE — an arrived raider whose whole action ladder has closed still holds the foothold and beats the heartbeat, parking half the workforce on a dead square** *(Provenance: Magnus live observation tonight + builder's opus autopsy of match 4b039a9c — 0 core hits in 4 losses with ~half the workforce parked from r~50; numbers re-derivable from replay_archive/4b039a9c-*_game_*.replay26. Prevalence probe: 22 of our last 110 archived games have a raider parked ≥100 rounds at d²≤4 of the enemy core, 8 opponents.)* | **GREP: vs `_v197mapcode`, anchors verified s37** — arrived raider makes no move attempt past `raid.py:194` (acts only via `_raid_act`); `LOKI_QUIET_ON = True` (`doctrine.py:1488`) kills all builder melee; `LOKI_SALT_CUT_MAX = 40` (`doctrine.py:1585`) exhausts the peck budget (bots observed at exactly 40 lifetime attacks then permanent silence); seal/heal are conditional — **yet the bot still beats `SLOT_RAID_LIVE` (`doctrine.py:1188`) every round, so the team treats the dead foothold as a live raid.** No terminal-idle detection exists anywhere in the tree. | parked-raider rate (bot-rounds parked-and-action-silent at d²≤4 of enemy core / total raider rounds) **by opponent, map, ourver** — the full-width corpus cut is COMMISSIONED s37 (research subagent, detector definition pinned to the autopsy's: ≥100 consecutive rounds, d²≤4, zero actions) | corpus cut first (sizes the prize and names where it binds); then EACH reopening rung is its own small mill arm: (a) QUIET ablation, (b) salt-budget refresh, (c) terminal-idle seat release (stop beating `SLOT_RAID_LIVE` when the ladder is closed — frees the seat AND un-parks the workforce) | **Why now: found live tonight, mechanism code-confirmed same evening, and the 22/110 prevalence says it is not a one-match story. The three rungs are separable constants/flags — mill-sized. Terminal-idle seat release is the only rung that needs no behaviour re-design, just honesty about a closed ladder.** ⭐⭐ **FULL-WIDTH CUT LANDED s37 same-evening (opus agent; detector `scratchpad/parked_raider.py --selftest`, cut `scratchpad/parked_raider_cut.tsv`, 7,203 games = FULL population, 0 errors; controls driven both verdicts incl. seat-sensitivity; aggregates spot-reproduced by research from the TSV).** Headlines, denominators inline: **(1) prevalence 1,981/7,203 = 27.5%** (mixed rated+unrated BY DESIGN — prevalence, not win rate; ladder-only 740/3,240 = 22.8%). **(2) REGIME BREAK at ~v99→v102: ≤v94 runs 0-17% of games and 0.5-2.4% of bot-rounds; v102+ runs 22-49% and 5-19% — the park is a ~v102-era REGRESSION, so diffing what shipped there is the cheapest rung-selector.** (3) **41% of every bot-round we spend at d²≤4 of the enemy core is a ≥100-round silent park** (814,242/1,986,370), 56.5% in the last 110 games; **v125: 10.68% of ALL builder-bot-rounds, median park onset r47.** (4) ⚠ **the 22/110 figure is NOT reproduced under the pinned definition: last-110 = 54/110 = 49.1%, ~2× the relayed figure** (nearest variants: ≥2 parked bots 27/110, max_park≥300 15/110 — the original likely used a stricter cut; the problem is TWICE as prevalent as first stated). (5) Opponent gradient (INFERENCE marked): camp/heal-heavy teams park us — Leviathan 53.5% (246/460), I Stone 55.6%, Landers 44.5% vs The Bisons 8.2%, Lunds 12.5% — consistent with the out-heal autopsy shape (raider arrives, ladder closes, nothing left to do). (6) ⚠ Instrument limits that matter for rung choice: the detector CANNOT separate chose-to-idle from TLE-every-round (**ties to #44: v125 at 87.6% of TLE ceiling on 30×30 — some park-share may be CPU, and the regime break could partly track CPU growth**); destroy-actions are proxied conservatively (misses parks, never invents them); thrown bots split runs (floor, not ceiling). ⭐ **MILL RESULTS s37 same-evening (builder, DOSE-idlepeck-quiet0-48 + amendments): (a) WIDENED PECK (any adjacent enemy building) REFUTED on its own kill-round clause — fires 12/12 (22-462 pecks) but median +44.5 rounds SLOWER, the peck-economics rider's prediction landing exactly (2 Ti/peck diverted from the ammo pipeline). DEAD, never screened.** (b) NARROW IDLEPECK (core-only) is exposure-limited in the fixture, not dead: local fire 1/12 because the camper's own healers hold the core-adjacent seats; the one firing game ran 101 pecks / −28 rounds paired. Fixture-shopping stopped after two honest attempts (disclosed). **Resolving read = pinned CAMP-CLASS live leg** (wire metric: builder-attacks-ON-CORE, by-construction zero in v125 — side lane's scoped filter), slotted in the fire order after rc8.3's read. (c) `_v208` harm screen + QUIET0 doctrine screen filling (n~86/82 at report). (d) Seat-release is the next build per the standing ranking — orthogonal, not raced. ⭐ **DETECTOR v2 s37 (`scratchpad/parked_raider_v2.py/.tsv`, same 7,203-game population, 0 join failures, research spot-reproduced the totals): THE CPU CONFOUND IS CLOSED — TLE = 0 of ALL 814,242 parked bot-rounds** (no tled, none ≥9,000µs; parked rounds run at median ~200µs). **Every park is chose-to-idle. #48 is decoupled from #44: the v99→v102 regime break is BEHAVIOUR, not CPU growth.** Honest instrument limit carried: execTimeUs cannot discriminate idle-from-active on our own code (actively-acting bot-rounds in the reference games were also 97.8% <1,000µs), so v2 answers ONLY "TLE vs not" — which is the question that mattered; and the ~8.3ms "navigating raider" reference from the incident relay did not reproduce and has no findable source (flagged, not asserted). **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `LOKI_QUIET_ON=True` (doctrine.py:1488), `LOKI_SALT_CUT_MAX=40` (:1585) and `SLOT_RAID_LIVE=15` (:1188) all confirmed byte-identical (this row's own citations were already the correct post-shift numbers). raid.py:194 carries unchanged (0 diff). No terminal-idle detection added anywhere in the 239 changed lines. Premise fully intact. |
| **51** | ⭐ **AIM THE THROW LOOP — the launcher is already a persistent workforce-denial engine by accident; point it at the builder that matters** *(Provenance: rc8.4 O2 g1 (replay 483b5bcd game 1), a SURPRISE banked before explanation per directive point 4 — our launcher ran 259 EXILE throws r47-998 at ~3.7-round cadence, 0 ammo, all game. ⚠ RESEARCH CORRECTION at verification (throws.tsv): NOT one victim — SERIAL victims, ≥3 enemy builder ids (89/84/66 throws each). Stronger, not weaker: the engine denies whichever builder comes. We lost that game anyway (r1000), which is exactly why AIMING is the question.)* | **GREP: vs `_v197mapcode`, verified s37/38** — `raid.py:909-931`: the exile picker takes ANY adjacent enemy builder (first in `get_nearby_entities` order) and throws to the site farthest from OUR core. **No role selection (feeder/repair-bot preference), no victim persistence, no denial-value scoring of the throw destination — the loop is EMERGENT, not designed.** Approved class (kidnap), in a form nobody designed. | denial-rounds per victim class: enemy builder-rounds spent walking back (from throw landing to re-entering d²≤2 of their work site), split by victim role (feeder-adjacent-to-turret vs repair vs harvest/pave) — decodable from throws.tsv (`d2_before/d2_after`, `bot`, `life`) + events | replay decode first (the 483b5bcd loop prices the unaimed baseline free); then a local dose vs `_probe_creeper` (feeders exist by construction); the aimed change is constant-to-small (score victims by adjacency-to-enemy-turret in the picker) | **Why now: banked as a surprise from a live leg tonight; the mechanism is already shipped and firing 259 times/game when geometry allows; #47's approach-launcher work composes (same tool, home-turf pickup); and the failure mode is visible in the source — an unaimed loop can spend the whole game denying a HARVEST bot while the feeder rebuilds the siege.** ⭐ **BASELINE PRICED s39 2026-08-14 (replay decode, agent-verified at primary: 259 EXILE / 89·84·66·20 per victim / loss by ti_collected vs Leviathan v67):** denial rate is **100.0%** (0 victim actions in all 259 inter-throw intervals; 5-round rail: 4-tile throw vs 4-step walk-back), `border=0` throughout (never the crash channel). Victims = 3 pure approach bots + 1 siege-builder (built the r46 forward sentinel, thrown from r47). **BUT marginal denial value in that game was ZERO: all 8 enemy builders — thrown or not — idled from r47** (economy complete by r42; they out-collected us 14,630:4,270 on the tiebreak key). **The measured gap is GEOMETRY + AMMO, not victim choice:** modal throw pad (28,17) sat d²=16 from our own sentinel 162 — which faced 180° AWAY — and our ammo was 24 for r100-900 (3 sentinel shots/kill = 30); victims took 0 HP over 259 displacements. **⇒ the aimed change is: pick the pad ON a friendly sentinel ray + the ammo conversion as co-requisite** (not adjacency-to-enemy-turret as first drafted). Q4 association (INFERENCE, us-archive, length-matched): ≥20-throw loops in FAST games co-occur with 67.0% wins (n=100) vs 50.5% control; loops persisting to r1000 with 39.1% (n=69) — **the loop that never terminates is the bad half**; a live leg decides, per point 6. EXILE rows carry hardcoded `-1/0/0/0` in life/core_atk/any_atk/reached — never filter EXILE on those columns. **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — raid.py:909-931 carries unchanged (0 diff); the exile picker still takes any adjacent enemy builder with no role selection or destination scoring. |
| **52** | ⭐ **COLLAR MEDIC — staff the 4:1 heal exchange we already own (the camper trick, reversed; deepening row for the barrier-seal asset, −10pp ablation-validated)** *(Provenance: Magnus's deepening directive s38 — "take what we do right and do it even better"; the ablation ledger marks barrier-seal −10pp + home-turrets −6.7pp as our biggest asset with ZERO deepening rows. The camp class beats us with 4 HP/Ti heals; our own barriers enjoy the same arithmetic against their pecks and nobody staffs it.)* | **GREP: vs `_v197mapcode`, verified s38** — the exchange EXISTS but is unstaffed: `_heal_adjacent` (`eco.py:322-340`, "1 Ti for +4 HP against an enemy peck's 2 Ti for 2 dmg — eight to one") heals any adjacent damaged friendly building, **but only opportunistically — no dispatch, no positioning: a builder must already happen to stand beside the pecked barrier.** Seal machinery: `LOKI_BARRIER_SEAL_ON=True`, `LOKI_SEAL_TI_FLOOR=12` (`doctrine.py:1227-1228`). **LEAK NAMED: a pecked seal barrier (30 HP, 15 pecks) dies unattended whenever no builder is adjacent — the 4:1 exchange goes unclaimed exactly when it pays.** ⛔ **RATIO CORRECTED 2026-08-14 (research s42), closing an open item flagged at the side lane's s40 wrap and left unfixed by two lanes.** This row said **8:1** in five places, inherited from `eco.py`'s own source comment ("eight to one"), and the comment is WRONG. The arithmetic: we pay **1 Ti for +4 HP**, so undoing their 2 damage costs us **0.5 Ti**, against the **2 Ti** they spent to deal it — **4:1, not 8:1.** The exchange is still strongly in our favour and the row's premise is untouched; **the number was simply double the truth and it was propagating.** ⚠ **The `eco.py:325-343` source comment is STILL WRONG and is the builder's to fix** — it is the origin, so leaving it live re-seeds this error into the next row that greps for it. | seal-barrier survival under attack (breach rate; heals delivered per enemy peck within the barrier's life) — decodable locally; live from events.tsv barrier DEATH + heal counts | **COUPLING CLASS: COUPLED, INCUMBENT-ABSENT** — the payoff requires enemy melee pecks at our barriers, which v125 NEVER does (QUIET_ON) ⇒ **the self-play screen reads HARM ONLY; value read = lazy/creeper-class fixture (peck behaviour present) or the pinned live leg.** Stated per the refinement — this is the library's use case. | **Why now: Magnus's directive; the asset is the biggest uncovered one; the change is mill-sized (a dispatch/stay condition on a builder already near home); and the arithmetic is already in our own source comment — we wrote the exchange into a comment (as "eight to one" — WRONG, see below) and never staffed it.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED, WITH A FLAG** — `_heal_adjacent` now eco.py:325-343 (was :322-340, +3 shift), body byte-identical, still opportunistic-only (no dispatch). ⛔ `LOKI_SEAL_TI_FLOOR` (doctrine.py:1227-1228) is NO LONGER 12 — IT IS NOW 0. v140 ships as ‘mapfix + sealfloor0 + l4repair’ (HANDOVER.md:1-2); `sealfloor0` IS this constant, changed 12→0 and shipped. This row's own premise (unstaffed heal dispatch) is UNAFFECTED — the floor gates spend-before-pecking/sealing, not heal targeting — but the cited value is stale, and the change means seals can now be pecked/sealed down to a zero bank. See #53: the floor-sweep half of THAT row's premise is now FALSE. | ⭐⭐ **PROMOTED s40 2026-08-14 ~17:3xZ ON MAGNUS'S TWICE-PUSHED ASK — AND THE PROMOTION IS BECAUSE THIS ROW'S STATED BLOCKER IS WRONG ABOUT THE FIELD.** The row says the value read is COUPLED/INCUMBENT-ABSENT and needs *"a lazy/creeper-class fixture (peck behaviour present) or the pinned live leg"*, which has read as *"this needs a special opponent"* and is why it never moved. **MEASURED (`build_agg` `metric=='batk'`, `ourver`≥125, archived): THE FIELD PECKS OUR BUILDINGS 85.5 TIMES PER GAME ON AVERAGE, AND 19 OF 20 OPPONENTS WITH n≥15 GAMES ARE NON-ZERO.** Top of the table: **Coreflood 196.9/game · Big O 192.9 · Jython 138.9 · Juusto 129.4 · lingling_40h 109.1 · 0033 101.8 · team lazy 96.0.** The single exception is **Leviathan at ZERO** — and they are 1997.8 and barely pair us. ⇒ **THE ROW IS RIGHT ABOUT SELF-PLAY AND WRONG ABOUT THE POPULATION: `LOKI_QUIET_ON = True` silences OUR melee so a self-play screen genuinely reads harm-only, but the LIVE customer is not a niche class — IT IS ESSENTIALLY EVERY OPPONENT, EVERY GAME, ~85 TIMES.** ⇒ **the blocker is a FIXTURE CHOICE, not a rarity: screen against any peck-capable probe (`_probe_creeper`) and the value channel is present by construction.** ⭐ **AND THE ARITHMETIC MAGNUS FRAMED AS BANKRUPTCY NOW HAS A DENOMINATOR: our own source comment prices the exchange at 4:1 in our favour (1 Ti for +4 HP against their 2 Ti for 2 dmg: undoing their 2 dmg costs us 0.5 Ti against the 2 Ti they spent) — unstaffed, we decline that trade ~85 times a game.** ⚠ **What is NOT established: that `batk` lands on the BARRIERS this plank heals — the metric counts builder attacks on any building of ours, and `BOOK-0033` shows at least one opponent aims them at the BELT. ⇒ the first cut for whoever builds this is batk-by-target, and the row must not claim barrier-specific volume until that lands.**
| **53** | **SEAL TIMING/GEOMETRY SWEEP — the −10pp asset has never had its constants swept** *(Same provenance: deepening directive; barrier-seal validated by ablation, parameters never explored.)* | **GREP: vs `_v197mapcode`** — `LOKI_BARRIER_SEAL_ON=True`, `LOKI_SEAL_TI_FLOOR=12` (doctrine.py:1227-1228); seal targets = free seats on the spawn ring via `seatkeys` (`raid.py:589-597` — seat conveyor kills convert to permanent 3-Ti seals). **No timing constant (seal-by-round), no geometry variant (which seats first), no floor sweep has ever run — the asset ships at its birth constants.** | seal uptime (seat-rounds sealed / seat-rounds available) · breach rate · kill-round non-regression (defensive rider) | corefill sweep arms (one constant each, mill-sized) — **COUPLING CLASS: SELF-KNOWLEDGE/field-universal (seat geometry is ours; spawn-ring pressure is universal) ⇒ screen-trustworthy per the refinement — the nice property of deepening rows.** | **Why now: the directive; screen-trustworthy class means cheap local resolution; ranked BELOW #52 (no named leak yet — this is exploration of a validated asset, #52 is a named leak in the same asset).** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED — ⛔⛔ HEADLINE FINDING, ROW'S OWN PREMISE IS NOW FALSE, FLAGGED NOT REWRITTEN.** `LOKI_SEAL_TI_FLOOR` (doctrine.py:1227-1228) is NO LONGER 12 — it is 0 in the live incumbent. v140 ships as `_v223sealrepair` = ‘mapfix + SEALFLOOR0 + l4repair’ (HANDOVER.md:1-2, shipped 2026-08-14 11:37Z) — SEALFLOOR0 IS this exact constant, and it has already been swept and shipped (HANDOVER's SEALFLOOR0/SEALFLOOR0R legs). This row's central claim — ‘no floor sweep has ever run — the asset ships at its birth constants’ — is FALSE for the floor third: a floor sweep DID run and its result (0) is now live. What SURVIVES: no timing constant (seal-by-round) and no geometry variant (which seats first) have been swept — those two thirds are still open. raid.py:589-597 seatkeys carry unchanged (0 diff). → RESEARCH ACTION NEEDED: re-scope this row to timing/geometry only and drop the floor clause, or determine whether 0 was the swept optimum (in which case this row's floor question is ANSWERED, not just stale) — not resolved here, flagged per the carry's own instruction not to quietly rewrite a falsified premise. **⭐ RE-SCOPED s40 2026-08-14 ~13:2xZ (research, answering that action item — and the answer is NEITHER of the two offered options).** Read at the tape: **the floor was explored DOWNWARD ONLY.** `SEALFLOOR0` ran, is at FINAL 54.47@4239, and shipped inside v140; **`SEALFLOOR24` — the upward arm — was ALLOCATION-CANCELLED, not futility-dropped** (s38 arm retro, coordination.md:46626, where allocation cancels are typed separately from futility gates precisely so this distinction survives). ⇒ **0 is not "the swept optimum"; it is the only non-baseline value ever measured.** The floor clause therefore does NOT die — it NARROWS, and its shape changes: the open question is no longer "has the floor been swept" (it has, one-sided) but **"is the optimum at or below 0, or did we ship the bottom of a curve whose top was never sampled?"** — 0 vs 12 vs 24 with 12 as the historical baseline, and the 24 arm is the one that was never run. **Row scope after this re-scoping: (i) floor UPWARD arm (24, the cancelled one) — now the cheapest of the three since 0-vs-12 is already priced by the ship; (ii) seal TIMING (seal-by-round) — never swept; (iii) seal GEOMETRY (which seats first) — never swept. The "ships at its birth constants" phrasing is retired as false.** |
| **54** | ⭐⭐ **STATUS: BLOCKED — DETECT-AND-REPICK IS ROAD-CLOSED (built twice, screened twice, dropped: OSCLOCK **48.53** final, OSCLOCK2 **46.49**). ⛔ **AND THIS ANNOTATION RETRACTS MY OWN `QUEUE-ECONOMICS-SWEEP` RECOMMENDATION** to re-screen it on the 900-area segment at "+2-4pp": both dead arms' rows **on exactly that segment** read **47.50 and 45.68**, and OSCLOCK2's CI there **excludes 50 from above**. The segment I proposed as the rescue is where the evidence is *worst*. **Any successor must change NAVIGATION or DESTINATION, never DETECTION** — and the census that supplies its dose is currently MISSING (see below). — THE NAV LIMIT CYCLE — a two-tile oscillation is invisible to its own stuck counter, and in one decoded game it consumed the ENTIRE workforce** *(Provenance: s39 #51 decode side-finding, then a dedicated trace (scripts `six_bot_oscillation.py`, scratchpad): in 483b5bcd g1 (midgard, rc8.4=v130), ALL 11 of our builders ended in a permanent two-tile oscillation — dwell exactly 1 round, 100% round-parity-locked (807/961/970/626/629/606-round windows, zero exceptions for 5 of 6 traced), 5 bots mutually in phase + 1 anti-phase (phase = onset parity), staggered onsets r30-398 after monotone transit, 3 of 6 with EMPTY terrain both sides — congestion excluded. Six had ZERO lifetime actions; ~4,050+ builder-rounds and 180 Ti wasted in that game.)* | **GREP: vs `_v197mapcode` — MECHANISM NAMED IN SOURCE (eco.py IDENTICAL between rc8.4 and the incumbent, verified by diff):** (1) `_nav` (eco.py:735-747): when the BFS step fails, the fallback tries +90°/−90°/**`desired.opposite()`** — a backstep counts as a successful move, and `self.stuck += 1` fires ONLY when all four moves fail; (2) the only exits from a bad target — `p == self.tgt` or `stuck >= 5` (eco.py:1055) — therefore NEVER trigger while the bot is moving in a loop; (3) `_bfs_direction` (eco.py:646) builds its blocked set from `get_nearby_entities` — POSITION-DEPENDENT (one step changes what's visible, so the plan can flip per-round) — and it ignores CONVEYOR/SPLITTER/BUILDER tiles the engine actually refuses, so `can_move` vetoes steps BFS believes in. No oscillation detector exists anywhere in the tree. | corpus-wide prevalence first: oscillation-locked builder-rounds / total builder-rounds (the trace method generalizes: ≥50-round two-tile lock per bot per game), by ourver/map/opponent — sizes the prize before any fix | prevalence cut (free, the detector exists in scratchpad); then the fix is mill-sized with variants: (a) last-K-positions oscillation detector → force repick + ban the target (mirrors the existing teleport detector at raid.py:174), (b) count fallback/backstep moves as fractional stuck, (c) direction hysteresis in `_bfs_direction` | **Why now: 11/11 builders in a real decoded game, mechanism named at exact lines, code identical in the SHIPPED tree (v125), and it composes with #48 (parked raiders) and #14 (idle destination) — this may be the mechanism UNDER both. n=1 game so far — the prevalence cut says whether this is a midgard-shaped edge or the default failure mode of every idle walk.** ⭐⭐ **PREVALENCE LANDED s39 2026-08-14 (sonnet agent, ALL 1,160 archived v125 games — 100% coverage, 0 parse errors; positive control 11/11 incl. all six known ids, negative control 1/6; headline re-derived by research from the census JSONL exactly): THE LOCK IS THE DEFAULT FAILURE MODE, NOT AN EDGE. v125 STRICT: 183,489/1,584,948 = 11.58% of ALL builder-rounds spent in permanent locks; 552/1,160 (47.6%) of games have ≥1 locked builder (median lock onset r68); 39.8% of the 962 locked bots never acted in their lives. SOFT (any ≥50-round window): 19.29%. MAP SIZE GRADIENT — the 30×30 maps own the top of the table: midgard 35.6% of builder-rounds locked · ragnarok 14.1% · valkyrie 12.8% · drakkarfjord 11.0% (small maps 3-8%; the 64 "UNKNOWN" games are the glacierkeep layout under pre-MAPFIX terrain, verified by dims+cores (14,2)/(14,26), at 6.25%) — direct support for the #36 hypothesis that 900-area underperformance is partly THIS defect (long walks = more nav exposure; INFERENCE). r1000 games run 27.0% locked (n=43). Threshold sensitivity: min_span 100 → 27.6% of games / 9.69% of rounds (rounds metric robust). Caveats: pools 860 unrated + 300 ladder (same code both surfaces); comparison strip v130/133/134 is n=20/arm — ballpark only. RESEARCH RANKING VIEW: an 11.6% workforce tax on the incumbent with a named mechanism and mill-sized fixes outranks every current deepening row — builder decides. Fix arms are the builder's; census scripts in scratchpad (nav_limit_cycle_census.py). **BASE-RATE NOTE (s39, pre-empting a misread): the census figures are REAL-OPPONENT games (midgard 35.6% locked builder-rounds) while the builder's arm-1 dose fixture is SELF-PLAY (control 5.37% on midgard n=12) — same mechanism, different base rate; lock exposure is partly opponent-induced (what blocks/redirects our walks), so self-play under-doses it. NOT a discrepancy; the regression read is the treatment/control RATIO within each surface, and the live confirmation is the ladder census re-run on the fixed tree post-ship.** **ARM 1 BUILT+DOSED+QUEUED same hour (builder): `_v220osclock` = v125 + last-K parity detector (K=8) feeding the EXISTING repick machinery; dose 1.55% vs 5.37% control, games-with-lock 1/12 vs 4/12; declared gap: strict alternation misses dwell-2/period-4 cycles (arm 1b follows if the screen pays); SCREEN-osclock prereg committed, n=5400.** **CARRIED s39 → `_v218mapfix` (v139) — WITH A SUBSTANTIVE NOTE: the ship's 2 changed lines are exactly the valkyrie/glacierkeep grids that `_bfs_direction` reads via `map_walls`, so the census's valkyrie (12.8%) and pre-patch-glacierkeep (6.25%) cells were measured under the OLD grids; the mechanism (stuck-counter blindness) is grid-independent and stands, but per-map rates on those two maps may shift under v139 — re-read them from post-ship games before citing.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — eco.py identical between v139 and v140 at these sites: `_nav` fallback now eco.py:898-910 (was :735-747, +163 shift), `stuck>=5` exit now eco.py:1226 (was :1055, +171 shift), `_bfs_direction` now eco.py:809 (was :646, +163 shift) — all content byte-identical. raid.py:174 teleport detector unchanged (0 diff). This row's own s39 note flagged the valkyrie/glacierkeep grid change as map-rate-relevant; that grid data is untouched by the v139→v140 diff (which touches only doctrine.py SEAL_TI_FLOOR + the new eco.py L4-repair block, neither of which is `map_walls`/grid data), so no further map-rate caveat is added here. Mechanism (stuck-counter blind to 2-tile oscillation) fully intact and, notably, is DISTINCT from the new L4 belt-repair mechanism — L4 repairs missing conveyor LINKS, it does not touch builder navigation/pathing at all. |
| **50** | **PAVE THE WALK-OUT — lay the belt WHILE walking to harvester #1, so the first stack delivers at zero latency** *(Provenance: Magnus live observation — some teams' openings have the belt ready when the harvester lands. Engine trade priced in the ask: act/move exclusivity halves the paver's walk, so it RELOCATES belt-rounds from post- to pre-harvester rather than creating time; payoff channel is FIRST-DELIVERY round — collected = delivery to core — which feeds the rush bank (r22 sentinel recipe).)* | **GREP: vs `_v197mapcode`, verified s37** ⭐⭐ **UPGRADED 2026-08-16 ~13:4xZ ON MAGNUS'S MARKER (2403b27b g2, kladde v119 vs our v140 — a LEG CELL, MECHANISM-ONLY READING, no arm contrast read or implied). HIS WORDS: "they move once and build twice, which is genius."** ⛔ **THIS RE-CLASSES THE ROW: #50 WAS AN *ORDERING* CHANGE (terminus-first). IT IS NOW A *CADENCE* CHANGE, AND THE CADENCE IS THE BIGGER TERM.** **The engine mechanism: one action per round + orthogonal-adjacent build means a builder STANDING BETWEEN TWO BUILDABLE TILES PLACES BOTH ON CONSECUTIVE ROUNDS WITH ZERO REPOSITIONING.** ⇒ **a stop yields 2 STRUCTURES PER MOVE against our 1-per-move walk-build cadence — their belt goes down in ~HALF the builder-rounds, and the harvester lands into a LIVE LINE as a SIDE EFFECT of the same walk (the wire-first property comes free).** **Observed card at r7: kladde 3H / 9c / 10 collected vs our 1H / 0c / 0.** ⭐ **PRECONDITION, FREE AND CHECKABLE ON THE WIRE BEFORE ANY ARM IS WRITTEN — the same sizing discipline that re-shaped #78 and killed #80's premise: kladde's build events should show CONSECUTIVE-ROUND BUILDS FROM IDENTICAL BUILDER POSITIONS. A corpus signature cut confirms whether the pattern is SYSTEMATIC or was one lucky opening.** ⇒ **if confirmed, the arm spec becomes: WALK THE PLANNED LINE STOPPING EVERY OTHER TILE, BUILDING BEHIND + AHEAD AT EACH STOP.** ⚠ **If NOT confirmed, this stays an ordering row and Magnus's marker was a single-game artefact — which is the cheaper thing to learn first.** **Composes with `#78` (both are the belt-construction layer): #78 changes WHERE the line goes, #50 changes HOW FAST it is laid. Neither subsumes the other and they should be screened separately before any composite.** — we ship harvester-first ALWAYS: our own pave-trail machinery exists (`PAVE_TRAIL_ON`, `eco.py:756/:794`) but the pave is gated on `ct.read_store(SLOT_HARVESTERS) >= 1` (`eco.py:773`) — **paving is disabled exactly for harvester #1, the one the observed teams pave for.** | per-team OPENING SIGNATURE (first-conveyor round vs first-harvester round ordering, from `builds.tsv` — free) → rounds-to-first-delivery and first-N00-banked-Ti (**needs per-round collected state — `tools/bank_trace.py` is the candidate instrument; `flow.tsv`/`econ.tsv` are band-aggregated and cannot give the round**) | archive cut FIRST (no games): does the belt-en-route signature correlate with faster first delivery / faster banked Ti across the field? If yes, arms are (a) relax the pave gate for the walk-out, (b) two-builder split (fast scout + core-end paver) — each mill-sized. Cost-scale wrinkle (+1%/conveyor earlier) prices itself in the screen. | **Why now: observed live tonight; and the payoff channel (first-delivery round) is the same bank that buys the r22 sentinel opening — a latency win here compounds into the kill recipe, not into tiebreak economy.** ⭐⭐ **SIGNATURE CUT RUN s37 (research, `events.tsv` BUILD rows — NOT `builds.tsv`, which carries turrets only; surface correction made during the cut): WE ARE THE OUTLIER. OpenSverige: 99.7% harvester-first (n=7,169 sides). The field: pave-first is near-universal at the top — SmartFridge 100.0% (n=3,260), kladde chatte tville 99.6% (n=1,784), diverge/Focalground 100.0%, Besvikomat 99.4%, Hugging Farce 99.8% — median belt lead 5-6 rounds.** Not a strict rating gradient (team lazy 99.8% and Jython 93.8% harvester-first like us; Leviathan mixed 47/40), so pave-first is not sufficient for strength — but the tactic Magnus observed is the FIELD NORM and we never do it. ⛔→⭐ **RE-SPECIFIED s37 same-evening (builder dose falsifier + research position cut): THE PAVE PREMISE IS DEAD, THE ROW IS NOW TERMINUS-FIRST.** The dose killed the walk-out arm (`_v211` dead, screen slot withdrawn pre-start): **harvester #1 lands r4-8 on near-adjacent ore — there is no walk to pave.** The position discriminator then settled what the field actually does: **top teams' first conveyor lands at median d²=2 from their OWN CORE at round 1-4, harvester at r6-7** (SmartFridge n=3,260 conv-rnd 1/harv-rnd 7, kladde n=1,776 same shape, Besvikomat, LingLing40 d²=1) — **CORE-SIDE TERMINUS-FIRST, confirmed on the wire.** One variant: Hugging Farce wires the HARVESTER site instead (98.0% first conveyor at d²≤2 of harvester #1). Field-wide: 20,540 wire-first sides, median d²(conv,harv)=9. **Mechanism (builder, geometric): an acceptor tile for site bp is never orthogonally adjacent to the position that builds bp, so wire-after always costs a repositioning (our measured conveyor-after-harvester gap: 2-3 rounds, 8/8 local games) — wire-first reorders the same rounds and saves 1-2 otherwise-wasted stacks per harvester. Scope: `_has_acceptor` counts the CORE, so core-adjacent harvester sites waste nothing — the fix applies to NON-core-adjacent sites only.** Arm shape: approach re-sequencing (terminus two tiles out, walk OVER it — builders traverse conveyor tiles — then place the harvester). **Preferred implementation if built (builder, s37): the HARVESTER-SIDE variant (Hugging Farce's) — one tile out, not a core-side chain; geometrically the simpler resequencing.** **GO/NO-GO for the arm: the `bank_trace` first-delivery latency read prices the 1-2 stacks in Ti before we pay the build complexity — if the latency saving is under a stack's worth of bank at r22 (the sentinel recipe's clock), the arm is not built.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `PAVE_TRAIL_ON` gate now eco.py:919/957 (was :756/794, +163 shift) and the `SLOT_HARVESTERS>=1` gate now eco.py:936 (was :773, +163 shift) — all content byte-identical. Premise (pave disabled exactly for harvester #1) fully intact; unaffected by the new L4-repair code (a separately-gated eco.py mechanism). ⭐⭐⭐ **PRECONDITION RUN AND RETURNED s47 2026-08-16 ~14:55Z (research, opus agent over `corpus/events.tsv`). VERDICT: CONFIRM — MAGNUS'S MARKER WAS SIGNAL, NOT NOISE. But the arm spec in this row is WRONG in two independent ways, and the second one is a CODE fact I verified by hand here.** ⛔⛔ **FIRST, AGAINST MY OWN INSTRUMENT: THE METHOD I BRIEFED WOULD HAVE KILLED THIS ROW WITH A FALSE NULL.** I specified consecutive-round build pairs at d²∈{2,4} against a permutation null. **Run as briefed it returns whole-field 11.1% observed vs 9.7% null — lift 1.14, a clean, quotable, WRONG null**, and it ranks Erebus at d²=1 **31.7%** (a geometric impossibility from one stop) — **pure multi-builder artefact.** ⇒ **THE NULL MODEL WAS NOT UNDERPOWERED, IT WAS MEASURING A DIFFERENT THING. The confound had to be RETIRED, not corrected for.** The agent did that by replaying `moveBuilderBot {id,to}` to track **every builder's position at every point in the stream** and attribute each build to the unique adjacent friendly builder. **Controls all fired: attribution positive control `miss = 0 of 253,081` builds across 18 teams; geometric negative control `d²=1 = 0.00%` of 2,291 pairs; single-living-builder positive control 94.2% d²∈{2,4}.** ⭐ **THE MEASUREMENT — a builder BUILT at round t; what does it do at t+1? (r0-40, 400 games/team, exhaustive outcomes, no null needed):** **lingling_40h 10.8% BUILD (82.5% of games) · kladde 9.7% (79.2% ±5.4pp) · Jython 7.2% · Erebus 6.1% · 0033 4.1% · HTTP 418 3.0% · US 2.2% (29.5% ±6.1pp) · gsxWins 1.3% · The Bisons 1.0% · farming_200s 0.5% (6.1%).** **kladde vs us is non-overlapping by ~44pp on the games-with-≥1 measure.** Opening-concentrated (kladde 11.2% r0-20 → 4.5% r80-140). **NOT a build-volume proxy: r(builds/game, BUILD→BUILD%) = +0.219, n=18, p≈0.38.** Sample is league-wide — **90.5% of kladde's rows are against teams other than us.** ⛔ **CORRECTION 1 TO THE ARM SPEC — IT IS A MINORITY CADENCE, NOT A DEFAULT.** Even for kladde, **70.8% of post-build decisions are MOVE** and only 9.7% are a second build. ⇒ **the row's spec ("walk the planned line STOPPING EVERY OTHER TILE, building behind + ahead at each stop") would run the cadence ~10x more aggressively than ANY observed team, including the one Magnus watched.** That may still be right, but it is an **untested extrapolation, not a copy of kladde**, and a prereg must say so in those words. ⭐⭐ **THE DEFENSIBLE, EVIDENCE-BACKED ARM IS NARROWER AND CHEAPER — AND IT LANDS ON THE PROGRAMME'S OWN DECIDING FACT: WHEN PLACING A HARVESTER, PLACE ITS OUTBOUND CONVEYOR FROM THE SAME STOP.** Kind-pair inside a 2-build stop: **lingling_40h conveyor+harvester 98% (near-ritual) · kladde conveyor+conveyor 47%, conveyor+harvester 35% · US barrier+barrier 39%, harvester+harvester 21%, conveyor+harvester 7%.** Shape: **lingling 68% d²=4 and kladde 54% d²=4 (opposite pair = behind+ahead on a STRAIGHT LINE, exactly Magnus's reading) vs US 22% d²=4 / 78% d²=2 — when we double-build we TURN A CORNER, we do not pave a line.** ⇒ **they spend the double-build on a harvester PLUS its route home in one stop, so the harvester never exists without a route — and `CLAUDE.md` is explicit that `titanium_collected` counts DELIVERY TO THE CORE, so a harvester with no route home is worth ZERO. We spend ours on BARRIERS.** ⛔⛔ **CORRECTION 2, AND IT IS A CODE FACT THIS ROW'S OWN GREP GETS WRONG — VERIFIED BY HAND vs the live control `_v223sealrepair`.** The cell says our pave *"is gated on `ct.read_store(SLOT_HARVESTERS) >= 1` (`eco.py:773`) — paving is disabled exactly for harvester #1."* **THE TRUTH IS BIGGER: `PAVE_TRAIL_ON = False` (`doctrine.py:528`), and the ENTIRE pave block — `eco.py:920-954`, INCLUDING the `SLOT_HARVESTERS >= 1` gate now at `eco.py:936` — sits inside `if PAVE_TRAIL_ON:` at `eco.py:919`.** ⇒ **PAVING IS DISABLED ENTIRELY, for every harvester, always. The harvester gate is UNREACHABLE DEAD CODE, and `doctrine.py:1646` states the flag is False in writing.** ⭐⭐⭐ **AND THE STRUCTURAL POINT THAT DECIDES THE WHOLE ARM: TURNING THE FLAG BACK ON WOULD NOT PRODUCE KLADDE'S BEHAVIOUR.** Read the mechanism — `eco.py:955-960` builds on `pave_prev`, **the tile the builder just LEFT, while moving** (`if ct.can_move(d): ct.move(d)` then `self.pave_prev = p0`). ⇒ **our machinery is a WALK-AND-PAVE: one structure per MOVE — precisely the 1-per-move cadence the winning teams do NOT use.** ⇒ **the arm is NOT `PAVE_TRAIL_ON = True`; it is a DIFFERENT MECHANISM (stop, build two adjacent tiles from one position, then move).** ⚠ **And the flag was turned off for a priced reason that the arm inherits: `eco.py:657` records it measured at 38.20 conveyors/game — which under `#78`'s currency is +1% cost scale per conveyor FOREVER. ⇒ any pave arm must carry a SCALE budget, not just a delivery metric.** ⇒ **ROW RE-SCOPED: `#50` is CONFIRMED as a real, systematic, ~44pp field gap, and its buildable form is the one-stop harvester+outbound-conveyor placement — NOT a stop-every-other-tile walk, and NOT re-enabling `PAVE_TRAIL_ON`.** ⭐ **THREE FREE LEADS THE CUT WAS NOT LOOKING FOR, banked rather than dropped:** (1) **d²=0 pairs exist** — a builder destroys its own building and rebuilds a DIFFERENT type on the same tile next round (kladde 0.45%, farming_200s 7.7%); `destroy` is free and un-cooldowned so this is legal and cheap, **and nobody here has written it down.** (2) ⭐ **Jython: 11.3% of post-build actions are the builder being THROWN** (vs 0.0% kladde/Erebus/lingling, 0.2% us) — **they launcher-ferry their own builders immediately after building.** A play-the-players lead independent of this row. (3) **kladde IDLEs 18.7% after a build, nearly double the field**, while still leading the double-build table — their cadence is build → (71% move, 19% idle, 10% build), **not a tight loop.** |
| **49** | **ORE-BARRIER DENIAL, DEFENCE SIDE — does an enemy barrier on our ore BIND, or is it 3-Ti theatre?** *(Provenance: Magnus live observation — Jython barriering our harvesting spots on icefloe, "seems like a tactic some players use." Offensive twin is #7, which now has field evidence for the demand side.)* | **GREP: vs `_v197mapcode`** — no code reads enemy barriers on ore: the harvester planner scores ore tiles by availability, so a barriered tile silently drops out of the candidate set rather than being contested (no destroy/attack path targets an enemy barrier on ore; `LOKI_QUIET_ON = True` silences builder melee that could clear a 30-HP barrier for 30-45 Ti of pecks — 15 pecks × 2 dmg... 2 Ti each = uneconomic vs 3-Ti barrier; the economical clearer is a turret or the #47 launcher evicting the BUILDER before it plants). | enemy barrier-built-on-ore events per game (builds.tsv has builder+type+pos; ore positions from the map files for the named pool) **× our end-game harvester count and titanium_collected in those games vs matched games without the denial** — the cut answers BIND vs theatre; per-opponent prevalence names who does it | corpus cut (no games needed); if it binds, the counter is already in flight — #47 evicts exactly this target class (enemy builder loitering in our half) and #40 pre-seals tiles | **Why now: observed live tonight; the cut is free; and the answer routes design — if it binds, #7 (our offensive twin) inherits demand-side evidence and a priority bump (noted on the row s37); if theatre, we bank the negative and stop worrying about it.** ⭐⭐ **CUT LANDED s39 2026-08-14 (opus agent, 3,430 archived rated games, validators driven both ways incl. harvester-100%-on-ore with discriminating complement; relayed on tape) — THE ANSWER IS BOTH, SPLIT BY LEVEL: (a) IT BINDS THE TILE** — a barriered our-side ore tile receives our harvester at 0.28x the same-game control rate when barriered r≤60 (per-game paired p=1.5e-27), the bind decays to ~1.0x past r150, 85.8% of the 1,929 barriers stand to game end (clearing costs ~30 Ti/15 turns vs their 3 Ti), and the bias direction of the test is conservative. **(b) ~~IT DOES NOT BIND THE ECONOMY at observed doses~~ ⛔⛔ DOWNGRADED TO *UNRESOLVED* s40 2026-08-14 ON RE-DERIVATION (econ v2, sonnet agent, pipeline rebuilt from this row because no script survived) — AND NOT FOR THE REASON THE econ EMBARGO PREDICTED.** The re-run reproduced finding **(a)** as its positive control at full strength (**0.106× receipt rate, n=389 vs 1,853, z=−15.78, per-game paired t=−21.98 over 199 games** — stricter causal definition than the banked 0.28×, same direction and significance class), so the instrument is trusted. **On (b) it could not reproduce +19 on EITHER table.** Within-opponent, r≤60 treatment / r0-150 outcomes, DEFF 1.366: **econ v2 ti_collected −54.9 [−236.2, +126.4] · OLD table −35.0 [−211.2, +141.2] · clean-era ≤v123 −45.6 [−265.5, +174.3]** (harvesters +0.72, identical on both tables). ⭐ **THE DECIDING FACT IS A SENSITIVITY SWEEP NOBODY RAN THE FIRST TIME: walking the per-opponent minimum-n matching threshold 1→3→5→10 moves the estimate −55→−69→−19→**+36** on v2 and −35→−42→−0.6→**+61** on the old table — THE SIGN FLIPS TWICE INSIDE A DEFENSIBLE RANGE**, because 353 treated games spread thinly over 24 opponents leaves several cells at n_treat=1. **Clean-era differs from full by only ~9 Ti ⇒ THE econ CORRUPTION WAS NOT THE DOMINANT NOISE SOURCE; OPPONENT-STRATIFICATION VARIANCE IS**, and that was invisible in a bare *"+19"* published with no CI and no sensitivity check. ⇒ **The row's (b) claim is WITHDRAWN as a finding. `#7` does NOT inherit a clean negative on the economy question.** Resolving it needs either far more treated-arm n or a **pre-registered fixed high-volume-opponent set** (team lazy / I Stone / Focalground). Scripts: scratchpad `ore49.py`. **(a), (c) and (d) are UNAFFECTED — they never touched `econ.tsv`.** Original text follows for the record: **(b) IT DOES NOT BIND THE ECONOMY at observed doses** — median coverage is 21% of our ore; within-opponent exposure-matched (r≤60 treatment, r0-150 outcomes), harvesters +0.8 and ti_collected +19 (both wrong-sign/null for a bind). **(c) The −20pp within-opponent win-rate association is a MARKER, not the barrier** (INFERENCE, stated with collider caveat): conditioning on our harvester deaths by r150 (D 1.8 vs N 0.6, 3x) collapses it to +0.8pp n-weighted — the RAID that kills the harvester costs the game; the barrier tags the raid. **(d) The dose that could bind is EARLY+TOTAL** — 17.3% of denial games have ≥50% coverage (8 at 100%); Jython's 10/10-tile icefloe version of it (v121/v128, 57-108 barriers) exists only in unrated-era archives, rated coverage n=5. Prevalence: 19.8% of our rated games; team lazy 80%, I Stone 70%, Focalground 55%, Jython 40% (version-gated: v33 none, v119+ yes). **ROW'S QUESTION ANSWERED — residual value routed to #7 (demand-side evidence + prescription) and #47 (evict the planter). Decoder facts banked: builds.tsv is TURRETS-ONLY (no barrier/harvester kinds — events.tsv is the build surface); replays carry their own map buffer (18 ladder map names have no maps/ file, so name-keyed ore joins silently drop them).** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A/B** — the row's `LOKI_QUIET_ON=True` reference confirmed byte-identical (doctrine.py:1488); the rest of the row is a corpus cut over `builds.tsv`/`events.tsv`, not a live-tree claim. Premise fully intact. **⭐ RE-READ s47 2026-08-16 vs the LIVE CONTROL `_v223sealrepair` (research, on the side lane's `GREP:`-scope audit; every anchor below re-verified here by hand, `ZZQXNOTATOKEN` control = 0/0/0/0 across all four files):** ⚠ **NEAR-MISS — the cell offers "a turret" as the UNBUILT remedy and it is PARTLY SHIPPED.** `main.py:740` ranks `EntityType.BARRIER: 7` inside the live turret attack-priority dict, in the enemy-target scan at `main.py:726-741` (team-checked via `ct.get_team(bid) != self.team`, gated on `ct.can_fire(t)`). ⇒ **an existing turret with a line on an enemy barrier ALREADY shoots it — last in priority, but wired.** ✅ **Row stands; the plank is the SITING/PRIORITY question (is 7-of-8 the right rank for a barrier that is denying an ore seat?), not "teach turrets to shoot barriers".** |
| **55** | ⭐⭐ **EVERY BAR WE QUOTE IN GAMES IS TOO NARROW — CARRY THE MEASURED DESIGN EFFECT INTO WHATEVER COMPUTES OUR INTERVALS** *(Provenance: research s40, surfaced while fixing a CAL-7 resolution error the side lane flagged. Not an assumption — measured on our own record by comparing the observed variance of match scores against the binomial.)* | **GREP: vs `_v223sealrepair` (v140) — N/A TO THE BOT TREE; this is  **GREP-PATH: tools/panel_read.py** *(s46: this row has NO BOT TREE BY DESIGN — its subject is an instrument. Path verified present on disk 2026-08-16. Checked: where no `deff`/`icc`/`cluster` term appears and every printed bar is a plain games-level interval.)*a TOOLS row and the grep is against our instruments.** Checked: no `deff`, `design effect`, `icc`, or `cluster` appears in `tools/panel_read.py`, and every bar this repo prints is a plain two-proportion or binomial interval over GAMES. `CLAUDE.md` itself quotes an *"MDE of ~39pp"* and a *"12pp same-bot swing"* on 25-game windows, both games-level. ⛔ **PARTIAL-NOVELTY CORRECTION, side-lane flag, applied before this row was relied on: the CONSTANT IS NOT NEW, only the tool is.** `PREREG-PANEL-CAL1-v123-field-2026-08-13.md:94-96` (A1.2, one day before this row) already named the clustering unit and MEASURED it — *"empirical per-match sd 8.565 against the binomial model's 7.111 (~20% understatement)"*, i.e. **implied DEFF ≈ 1.45** — and mandated match-level clustering as that panel's primary readout with game-level CIs labeled ANTI-CONSERVATIVE. **It never left the CAL family and no tool implements it.** ⛔⛔ **LIVE HAZARD NAMED BY THE s39 AUDIT (`docs/workflow-analysis/AUDIT-2026-08-14-s39.md` item 4) AND FIXED HERE BEFORE ANY BUILD: THIS ROW AS ORIGINALLY WORDED WOULD HAVE WIDENED LOCAL BARS BY 24-35% FOR CORRELATION THAT IS NOT THERE.** Local corefill/arena screens are a **balanced-by-construction** fixture and were newly MEASURED at **pair-weighted DEFF = 0.98 (ρ = −0.020) across 124 shards** — naive local bars are correct and marginally conservative. **⇒ THIS ROW IS PLATFORM-FIXTURE ONLY (rated ladder + unrated challenges). It must NOT be applied to local batteries.** *(Outliers: strong map-interaction arms reach ~1.25 — still leaves SEALFLOOR0 untouched.)* **And note WHY this correction was needed: local's exemption was previously scope-by-fiat in this row's own text, which my own scope PROCEDURE forbids — the exemption is now a measured fact with its n.** ⇒ **THREE INDEPENDENT MEASUREMENTS NOW CONVERGE — 1.45 implied (08-13, CAL-1), 1.434 df-corrected (08-14, this row), and a side-lane replication — which is what makes the constant trustworthy, and it retires "nobody knew" as the framing.** What remains true and is the whole justification for the row: **no TOOL applies it, so every bar printed outside the CAL family still treats the 5 games of a match as 5 independent trials.** | **MEASURED, both fixtures, from 5-game matches — AND IT SPLITS INTO TWO CONSTANTS, WHICH IS THE PART THAT MAKES THIS BUILDABLE RATHER THAN A SLOGAN:** | | **POOLED** (between+within) | **WITHIN-OPPONENT** (df-corrected) | |---|---|---| | RATED (965 matches, 59 opponents n≥2, share 0.5123) | **1.529** (ICC ρ=0.132) | **1.366** | | UNRATED (947 matches, 43 opponents n≥2, share 0.4809) | **1.833** (ρ=0.208) | **1.434** | **SCOPE RULE — a single repo-wide multiplier OVERCORRECTS every stratified bar and must not be built:** a pooled share over a MATCHMADE record (the ladder picks the mix, so the mix genuinely IS sampling variance) takes the pooled constant; a stratified or per-cell bar (we chose the cells, so the mix is fixed by design and must not be charged as noise) takes the within-opponent constant. Metric for the change: every printed interval carries `DEFF_u/n_u + DEFF_r/n_r` instead of `1/n_u + 1/n_r`, **with the constant selected by whether the comparison is stratified.** ⚠ Estimator note, banked because two lanes got two answers: the within-opponent figure REQUIRES the degrees-of-freedom correction (K estimated opponent means removed from N observations); without it the side lane's independent recomputation returned 1.282, and `1.282 × 960/(960−59) = 1.366` recovers this row's number exactly. **The uncorrected form is biased LOW.** | no games needed — the constants are measured and the change is one function. Re-derivation is a 15-line script over `ladder_games.tsv` and `meta_join.tsv` (variance of match scores vs 5p(1−p)); **re-measure the constants monthly rather than hardcoding them forever, since ρ is a property of the current pool.** | ⛔ **NOT A DUPLICATE OF `tools/effective_n.py` OR `tools/mde.py` — DIFFERENT MECHANISM, DIFFERENT SURFACE, checked before this row was counted.** `effective_n.py` measures SEED DEGENERACY in LOCAL batteries (a deterministic bot replaying the same game under different `--seed`s); `mde.py` measures the kill-speed noise floor from a LOCAL null. **Neither touches PLATFORM games, and neither knows that platform games arrive in 5-game matches.** This row is the platform-side gap both of them leave open, and the three are complementary: seed degeneracy shrinks local n, match clustering shrinks live n. | **Why now: it is a one-function change that touches EVERY verdict this project types, the correction always makes a bar HARDER to clear (so it can turn a positive into an unresolved but never the reverse — the exposed class is claims that cleared a bar NARROWLY), and CAL-7's own P1 band is already known to be narrower than its design can resolve because of it.** ⚠ **The unrated fixture clusters HARDER than the rated one (1.833 vs 1.529) — and the unrated fixture is the one we call `FIXTURE_OF_RECORD` and fire in 5-game blocks by choice.** |
| **56** | **`target_value` PRICES OFF A CACHED RATING AND IS SILENTLY STALE ON EXACTLY THE TEAMS WORTH TARGETING — ⭐ SCOPE TIGHTENED s43: THIS IS THE *OPPONENT* HALF ONLY, PLUS A SECOND, DIFFERENT DEFECT ON THE *OUR-RATING* HALF** *(Provenance: research s40 boot — the band print omitted Juusto entirely because it carries them ~65 points below their live rating; raised independently by the builder in the same hour.)* | **GREP: vs `_v223sealrepair` (v140) — TOOLS row.** `tools/target_va **GREP-PATH: tools/target_value.py** *(s46: this row has NO BOT TREE BY DESIGN — its subject is an instrument. Path verified present on disk 2026-08-16. Checked: the `--band` print carrying ONE global freshness line for the whole table rather than per team.)*lue.py --band` prints `RATINGS from corpus/league_matches.tsv — newest observation <ts>` — **ONE global freshness line for the whole table, not per team.** Measured at boot 2026-08-14 12:53Z: the band print puts Juusto at ~1815 while `league_matches`'s newest non-null `ratingABefore` for them (2026-08-14T12:32Z) reads **1883.3**. **A team's row can be hours stale while the global line says 0.3h**, because the global line reports the newest row in the FILE, not the newest row FOR THAT TEAM. **⭐ s43 — TWO CORRECTIONS TO THIS ROW, one narrowing and one WIDENING it.** (a) **NARROWED: OUR OWN rating is NOT cached and never was.** `tools/target_value.py:281-288` shells `fcode status` and parses the `Rating:` line; verified live 2026-08-14T20:45Z (`Rating: 1775`, `Active bot: v140`) against the tool's own band print of 1775. The title's *"prices off a cached rating"* reads as covering both halves and covers only the opponent half. *(Flagged by the side lane at boot, then withdrawn by them on reading the current tree — the comment at :272-277 credits THEIR OWN lane with having got this fixed.)* (b) ⭐ **WIDENED — A SECOND, LIVE DEFECT ON THE HALF JUST CLEARED: THE FALLBACK IS SILENT.** The live read sits in `try/except Exception: pass`, which leaves `ours` at the **cached corpus value** and returns normally. `CLAUDE.md` records that during the 07:1x outage **`fcode status` EXITS 0 while printing `Error: True`** — so a degraded response simply has no `Rating:` line, the parse loop matches nothing, and the function returns a stale rating **with nothing on screen to distrust.** This is the repo's own alarm-that-cannot-tell-it-is-blind shape, sitting in the single number every gap in every prereg is measured from. **Fix is one line and mirrors what the row already demands for opponents: record which path supplied the number and print it — `ours=1775 (live 20:45Z)` vs `ours=1663 (CACHED — live read failed)`. A healthy read and a blind read are currently byte-identical.** *(Found by the side lane s43 while withdrawing (a).)* | per-opponent row age (minutes between that team's newest observation and now) and the rating delta it hides; success = no admissible team priced more than one pairing cycle stale, or its age printed beside it | no games; a cut over `league_matches.tsv` grouping by team is the whole instrument. **Positive control available and required: Juusto must come out ≥60 points different under the fix, and a slow-moving team (e.g. one with ≤2 versions this week) must come out unchanged — the check has to produce both verdicts.** | **Why now: `target_value --band` is in EVERY lane's boot sequence precisely so nobody sizes a prereg against an assumption, and it is wrong in our FAVOUR (understating a target's rating overstates nothing, but it DROPS reachable teams out of the printed band entirely — Juusto at 1883 is +136 on us and pays ≈ +21.4 for a 5-0, the TOP of the band, and it was not printed at all). A gate that silently omits the best target is worse than one that prints a stale number, because there is nothing on screen to distrust.** ⚠ **Stale-on-fast-risers is not a uniform error: it is largest exactly on the high-churn teams (Juusto ran 11 versions in under 3 days), i.e. on the teams whose rating is moving because they are actively improving.** |
| ~~**57**~~ | ⛔ **DEAD — ANSWERED BY ITS OWN AUDIT, s40 2026-08-14 ~14:0xZ, ~30 MINUTES AFTER STOCKING. DO NOT RE-QUEUE.** The row asked whether v140's dark throw programme is a REGRESSION or a CONSEQUENCE. **CONSEQUENCE, and the audit also killed the arm the builder proposed off it.** `corpus/version_trees.tsv`: **v116 = "Loki v5" = `bots/_v169launchlate160`** — v116 IS the LATE160 arm, so the suppression is a deliberate, screen-backed ship (LATE160 51.42 > LATE80 50.74, s37 n=5408/arm), working exactly as designed. **THE DENOMINATOR THAT SETTLED IT — share of games in which we build ANY launcher: v104 92.5% · v114 94.0% · v115 93.3% → v116 5.8% → v125 11.6% · v139 7.8% · v140 8.2% (7 of 85 games).** ⇒ **The proposed `LAUNCHOFF` arm is INERT IN 92% OF ITS OWN POPULATION**, and the sweep's `LAUNCH0−BOTH0 = 6.34pp` was measured removing launchers from a bot that built them in ~93% of games — at 8.2% coverage the same mechanism scales to ≈**0.56pp**, below screen resolution. **Not admitted.** ⛔ **AND IT CORRECTS THIS ROW'S OWN HEADLINE, which said v140 "pays the launcher premium and throws nothing" ≈ `EXILE0` (47.02): FALSE AT POPULATION LEVEL — in 92% of games v140 is already effectively `LAUNCH0`, the arm that WON the sweep at 52.77.** The error was reading per-game rate averages that hid a coverage collapse; **a rate whose denominator includes the games where the mechanism is absent is not a rate.** Residue, recorded and deliberately NOT queued: in the 7 v140 games that do have a launcher, 17 launchers produced 1 throw against v104's ~5.7/launcher — **n=7 games is not a finding**, and settling it needs per-round positions that `events.tsv` (BUILD/DEATH only) cannot supply. **The live lever for launcher work remains `#47` (conditional siege launcher).** |
| **58** | ⭐⭐ **THE FORWARD EVICTION LAUNCHER — station one at THEIR core in the kill window and throw their heal/repair staff off it. OFFENCE.** *(Provenance: MAGNUS, live field observation this session — he watched an opponent base-camp a launcher near OUR core and evict our builders as they walked up to heal. Routed by the builder; admitted here after the grep.)* | **GREP: vs `_v223sealrepair` (v140) — WE HAVE NO FORWARD LAUNCHER, BY EXPLICIT DESIGN.** `main.py:615-617` is literally *"One Launcher, near home. ~70% of all launcher activity in the field is defensive disposal and **ours is ~97% defensive** — so this is bought as home defence first and as the raid ferry second."* Siting is core-adjacent (`SLOT_LAUNCHER` latch at `:628-638`, released only when the CORE can no longer see one). ⛔ **NOT A DUPLICATE OF `#47`, and the distinction is the whole row: `#47` is the HOME-defence conditional — build one in OUR base on APPROACH detection and evict the builders feeding THEIR forward siege turret. This is the mirror: OUR launcher at THEIR core, evicting the staff that heals and rebuilds THEIR ring and turrets during OUR kill window.** Different location, different trigger, different target. | enemy heal/repair actions within d²≤8 of their own core during our siege · rebuild latency of their ring barriers and forward turrets after we kill one (the `#3`/NESTSHOT removal work supplies the baseline) · **and the kill-round non-regression rider, because this is a launcher and launchers are priced** | **⚠ THE HAZARD IS PRICED AND IT IS THE REASON THIS ROW MUST INHERIT `#47`'s DISCIPLINE, NOT BYPASS IT: the s37 family sweep (n=5408/arm) reads LAUNCH0 52.77 · LATE160 51.42 · LATE80 50.74 · BOTH0 46.43 ⇒ standing/early launcher premium −6.34pp, earlier monotonically worse.** ⇒ **CONDITIONAL, SIEGE-PHASE, CAP 1, gates waived only inside the triggered state** — the cell that sweep never measured, because every arm in it was unconditional. Local dose vs `_probe_creeper` first (their heal staff exists by construction), then the pinned live leg. | **Why now: Magnus watched it work, and the field supplies a measured proof-of-concept that this is not theory — three opponents run near-core eviction against US at scale (`throws.tsv`, `ourver`≥125: Jython 32.9 throws of our builders per game over 125 games · Focalground 24.7 over 55 · LingLing40 22.1 over 85; 8,274 total). We are the ones being farmed by a plank we already own the parts for.** ⚠ **And the honest asymmetry: those three do it to us DEFENSIVELY, at their own base. Nobody in the archive does it OFFENSIVELY at an enemy core — so the field proves the mechanism, not this application of it.** | ⛔⛔ **REFUTED AS DESIGNED — LIVE LEG, s40 2026-08-14 17:04Z. DO NOT REBUILD THIS APPLICATION.** Pinned leg vs `0033` (5 matches / 25 games, pin id `353429e5`, `oppver`=v57 on all five, `ourver`=v144 on all five, PIN ALARM CLEAN). **P1 registered at >1.0 evictions/game; MEASURED 0.04 (ONE eviction in 25 games) — a 25× miss.** ⭐ **AND THE ATTRIBUTION SPLITS, WHICH IS WHAT THE LEG BOUGHT: the PLANT fired and the THROW did not.** Launchers built/game **1.240 (31 across 9 of 25 games) against v140's 0.341 — 3.6×**. ⇒ **the binding constraint is PICKUP OPPORTUNITY, not planting: 0033's builders never enter the forward launcher's d²≤2 envelope — which follows from `BOOK-0033`'s own measurement that their core-kill is 100% TURRET FIRE, `batk_core` = 0 in 246/246 archived games in EVERY era. They do not send builders forward, so a forward launcher near their core has nothing to pick up.** ⛔ **WHAT IS *NOT* REFUTED, AND A READER MUST NOT COLLAPSE IT: THE EVICTION MECHANISM ITSELF IS UNTOUCHED — three opponents run it against US at 22-33 throws/game (Jython 32.9 · Focalground 24.7 · LingLing40 22.1). What died is a FORWARD launcher against a BACK-SITTING TURRET bot. THE FAILURE IS OPPONENT-SHAPED.** A HOME-side eviction launcher (`#47`'s territory, vs opponents whose builders DO come forward) is a different question and **this leg says nothing about it.** ⭐⭐ **AND THE ASSET THAT SURVIVES — RECORDED SO NOBODY REBUILDS IT: the 3.6× plant rate LIVE-VALIDATES the conditional-launcher INFRASTRUCTURE (approach trigger, cap-1 live census, Ti floor, forward siting). Any future launcher plank inherits a WORKING PLANT for free.** *(Leg cost for the record: one rate window, 36 seconds of prototype exposure, ZERO rated leakage.)*
| **59** | **DON'T GET FARMED — the enemy launcher's PICKUP ENVELOPE is unguarded in our walk path** *(Same provenance: Magnus's live observation, defensive half.)* | **GREP: vs `_v223sealrepair` (v140) — THE TILE IS BLOCKED, THE ENVELOPE IS NOT.** `_bfs_direction` (`eco.py:809-832`) adds an enemy LAUNCHER to `blocked` — **but only its OWN tile**, which merely stops us walking through a building. **Pickup is d²≤2, i.e. the 8 surrounding tiles, and every one of them is free to route through.** A penalty exists but in the wrong layer: `LOKI_EXILE_PENALTY = 24` (`doctrine.py:1286`) is scored in the RAID station picker (`raid.py:809`) only — **the eco/heal walk-home path has nothing.** And `LOKI_QUIET_ON = True` (`doctrine.py:1488` — *"no builder melee: no core peck, no siphon hit, no counterbattery"*) means our builder cannot even hit the 30-HP launcher it is standing next to. | ⛔ **LENGTH-CONTROLLED OR IT MEASURES NOTHING: evictions per OUR-builder-round in r0-150** (not per game), plus rounds-lost-to-walk-back. | archive-only first — the cut below must find a cost before any arm is built. | ⛔⛔ **WHY THIS IS RANKED BELOW `#58` — I RAN THE COST CUT AND IT DOES NOT SHOW A COST.** Volume is large and real (8,274 throws of our builders at `ourver`≥125). But: **(i) 60.1% of them land AFTER r150 and the median is r209 — past our median kill (174) AND our median death (187), i.e. mostly in games already decided**; **(ii) within-opponent dose→outcome signs DISAGREE — Jython +12.0pp (more evictions, MORE wins), LingLing40 −8.0pp, Focalground −14.2pp, Coreflood −1.7pp — and dose is confounded with GAME LENGTH (each low-dose half has median 0 evictions/game, i.e. short games)**; **(iii) era-rated we BEAT both named users (Jython 56.7%, LingLing40 52.0%).** ⇒ **This is a measured ANNOYANCE, not a shown leak, and it is admitted as an archive cut rather than as an arm.** The row earns a build only if the length-controlled r0-150 metric finds a cost that the raw dose hides. **Stated this way because the raw 33-throws-per-game number is exactly the kind of figure that recruits a build on its size alone.**  **⭐ CROSS-CITED 2026-08-15: #70 (ZERO IDLE-AND-FREE) DELIBERATELY SENDS IDLE BUILDERS TOWARD THEIR CORE, i.e. INTO the very envelope this row says we do not guard. The two are ONE DESIGN — #70's destination scorer needs #59's envelope cost, and #59's fix is what makes #70's destination (b) safe to site. Neither row cited the other until the side lane priced the collision.** |
| **60** | ⭐⭐⭐ **REVIVED s45 2026-08-16 ON NEW EVIDENCE AND A DIFFERENT CURRENCY — the s45 block at the end of this row is the live case; everything between here and it is the ORIGINAL WITHDRAWAL, KEPT VERBATIM and NOT rewritten. It was WITHDRAWN — CLOSED BY ITS OWN OWNER (research) ~10 MINUTES AFTER STOCKING AND NEVER ANNOTATED UNTIL s43. `coordination.md:48128-48160`: launcher = **2.0% of a 308.2% scale contribution at 8.2% coverage**, converted to a rider on another row. ⛔ **AND ITS REGISTERED MECHANISM METRIC IS INERT BY CONSTRUCTION** — it reads `get_scale_percent()` at r50/r100/r150 while `LAUNCHER_MIN_RND = 160`, i.e. the dial cannot move in EITHER arm (Obligation 13). **This row kept recruiting screens for a day after its own owner killed it; it is the canonical instance of the stocking-side staleness defect the builder found in s43.** — RENT, DON'T OWN — WE WROTE THE MECHANISM INTO OUR OWN SOURCE AS A COMMENT AND HAVE NEVER ONCE CALLED `destroy()`. The launcher premium our own sweep priced at −6.34pp is the PERMANENT scale contribution of a launcher we KEEP; a demolished one leaves none of it.** *(Provenance: BOOK-juusto-2026-08-14 — 403 launchers in 110 games, **402 demolished at an age of exactly 2 rounds** (p25=median=p75=2), 3-round cadence, +132 Elo in 11.7 h. This is the one half of the Juusto import our own evidence does not oppose.)* | ⛔ **GREP: vs `_v223sealrepair` (v140) — `ct.destroy(` AND `ct.can_destroy(` APPEAR ZERO TIMES IN THE ENTIRE LIVE TREE** (all four modules; every one of the 11 textual "destroy" hits is a COMMENT). **And the comment that matters is `main.py:256`: *"dies (destroying an entity removes its contribution), so surplus bank…"* — WE WROTE THE MECHANISM DOWN AND NEVER ACTED ON IT.** *(Same shape as `#52`, where we wrote the heal arithmetic into a comment (mis-stated there as 8:1) and never staffed it.)* Our launcher DEATHs are ENEMY kills, not demolitions: v104 329/2125 · v114 59/333 · v125 76/371 · **v140 1/17**. **ENGINE FACT, SOURCE-READ NOT INFERRED:** `get_scale_percent@0x11fb8` is team-keyed and *"its contribution removed when that team's building is destroyed"* (`docs/research/engine-guard-matrix-exploit-hunt-2026-08-10.md:154-159`, disassembly of the shipped `.so`) — **rules-level, so admissible without a live test per CLAUDE.md's own carve-out.** `destroy` is free, no cooldown, unlimited per turn, allied buildings on an orthogonally adjacent tile only. | **`get_scale_percent()` at r50/r100/r150 vs control** (the direct dial — the whole claim is that scale stops climbing) · cost of the Nth build of an untouched category (conveyor is the cheap witness: +1% steps are visible) · launcher builds/game as the dose | local screen — **SELF-KNOWLEDGE coupling class (the scale factor is OURS and moves whatever the opponent does), so it is screen-trustworthy**, which is rare for a launcher plank — then a live leg | ⭐ **WHY THIS IS NOT THE ARM THE s37 SWEEP REJECTED, and the distinction is the row: that sweep (n=5408/arm) measured launchers we KEEP. Its −6.34pp premium IS `LAUNCH0 − BOTH0` = the standing scale contribution. A launcher demolished two rounds after use pays the Ti and NONE of the permanent inflation, so it is not a variant of the rejected arm — it is the arm that avoids the cost the sweep measured.** ⚠ **AND THE LIMIT, STATED SO NOBODY OVERSELLS IT: the TITANIUM IS NOT REFUNDED — only the scale contribution. Renting costs 20×scale Ti per cycle, ~73+ Ti/game at Juusto's 3.66 rate, and that is real money in the r0-50 window where our kill is bought. The arm must show the scale saving beats the Ti burn, and the honest first read is the SCALE CURVE, not the win rate.** **Why now: measured on a live opponent today at 402/403; the mechanism is engine-confirmed; the change is a demolish step after use; and it generalises past launchers — every building whose job is finished is currently taxing every subsequent build we make, forever.** ⭐⭐⭐ **REVIVED s45 2026-08-16 ~10:2xZ (research, as queue owner). THE WITHDRAWAL WAS CORRECT ON ITS CURRENCY AND THE CURRENCY WAS THE WRONG ONE. This row was closed on SCALE (launcher = 2.0% of a 308.2% scale contribution at 8.2% coverage). The demolish step is not a scale play — IT IS A TRANSPORT PLAY, and the evidence is the team that just went #1.** ⛔ **MEASURED ON `Jython` (rating 2134, #1, was BELOW us three days ago), decoded from 165+145 archived replays and re-derived by me from `corpus/events.tsv` directly:** they run a **DISPOSABLE LAUNCHER LADDER** — build a launcher one tile ahead, get thrown ~5 tiles, build the next, **and demolish the spent one**. **Median launcher LIFETIME: `Jython` v149 = 1 round, v137 = 5 rounds. OURS: v140 = 42 rounds, v152 = 96 rounds.** Builds/deaths per game: Jython v149 **5.66 built / 3.81 destroyed**; us v140 **2.23 built / 0.37 destroyed**. ⇒ **they treat a launcher as a single-use rung; we treat it as a permanent emplacement.** ⭐⭐ **AND THE PAYOFF IS ARRIVAL ROUND, WHICH IS THE CURRENCY THIS ROW SHOULD HAVE BEEN PITCHED ON ALL ALONG: first build within `d2_enemy <= 8` of the enemy core — `Jython` v137 **r7 (92% of games)** and v149 **r7 (100% of games)**, against **US v140 r28 (94%)** and **US v152 r32 (95%)**. FOUR TIMES FASTER TO THE ENEMY CORE, and it is the fastest in the league (field median r30).** ⛔ **CRUCIALLY IT IS NOT WHAT THEY CHANGED — the ladder is present at BOTH v137 and v149, unchanged across a +306-rating day. It is their long-standing delivery system, and the v146/v149 jump changed the PAYLOAD (gunner→sentinel swap, forward sentinel arriving r95→r28 in 44%→89% of games) ON TOP of a transport we do not have.** **GREP: `ct.destroy(` and `can_destroy` occur ZERO times in `bots/_v223sealrepair` — we have never demolished one of our own buildings, so the ladder is not merely unused, it is UNIMPLEMENTABLE in the current tree.** And `main.py:613` gates every launcher build behind `LAUNCHER_MIN_RND = 160`, so a ladder that must start at r1 is **doubly** blocked. **MECHANISM METRIC: median round of our first build within `d2_enemy <= 8` of the enemy core (currently r28–32; the bar is a fall, and r7 is the demonstrated floor). Secondary: launcher builds and destroys per game.** **FIXTURE: local corefill solo screen vs `_v223sealrepair` at n=5,400 (house bar, single arm, no leaderboard-of-maxima), then `LEG-fieldcal`'s pinned scheduler for the field read.** **WHY NOW: the #1 team's transport is measured, our own is 4x slower, the enabling call does not appear in our source even once, and Magnus has asked for SOLO arms that can combine past 60.** ⚠ **HONEST LIMIT, INHERITED FROM THE DECODE: that the demolition is FOR scale is INFERRED, not measured; and no damage attribution exists, so which entity lands their kill is unknown. This row is pitched on ARRIVAL, which is measured, not on the scale story that closed it the first time.** |
| **61** | ⚠️ **TWO OF THREE LANES HAVE NO RECORDED DECISION SURFACE — SO `audit_trigger`'s DENOMINATORS ARE STRUCTURALLY BIASED AGAINST THEM, AND THE ALARM THAT FIRED TODAY CANNOT BE EVALUATED** *(Provenance: `audit_trigger` fired 2/6 at 2026-08-14 14:3xZ — `cross-lane analysis` 4.60, trajectory 2.12 → 3.83 → 4.60 across one session with the numerator RISING 17→23 and the denominator FALLING 8→6→5. Row admitted by the lane the alarm indicts.)* | **GREP: vs the instruments, not the bot tree.** **GREP-PATH: tools/audit_trigger.py** *(s45: token per `docs/research/SPEC-queue-grep-path-2026-08-16.md` — inert prose until the tool change lands; this row has no bot tree BY DESIGN and `queue_check`'s UNNAMED warning is a false positive on it.)* **RE-CHECKED 2026-08-16 (research, queue-grep audit): `tools/audit_trigger.py` confirmed present on disk; the current bot incumbent (see PROGRAMME.md INCUMBENT field) is NOT the relevant tree — this row's subject is an analysis instrument, not bot behaviour, so a bot-tree grep would misreport what was checked. Cited read/function still exist, line numbers drifted (`:59`→`:83`, `:221-238`→`:251+`) after s45 edits — mechanism unchanged, not stale. GREP-PATH exemption stands; the fix is still the tool-side change routed to BUILDER, unmade.* `audit_trigger.py:59` reads **`results.tsv` and nothing else**; `cross_lane_analysis()` (`:221-238`) counts `.md` files added under `docs/` against **the same builder-tape decision set** (`verdict+keep+discard+refuted+gate+baseline+ship`). **Its own docstring already names the defect — *"a decision is still only recorded in one place"* — and labels the 4.0 threshold *"deliberately loose… NOT calibrated on a confirmed incident… so nobody quotes it as a p-value."*** `results.tsv`'s live vocabulary is 20 status tokens, all builder-typed (verdict 68 · keep 60 · baseline 25 · discard 20 · ship 8 · gate 6 · refuted 6 …). **CHECKED: no research or side-lane decision surface exists anywhere.** Queue admissions and kills, rulings the builder acts on, road closures, prereg certifications, retractions — **all of them live only as prose on `coordination.md` and are uncountable by construction.** *(Same session, unrecorded by the denominator: six queue rows admitted or killed, three rulings the builder executed, four retractions, a book banked, two method rules, and a tool built.)* | the two ratios recomputed with an all-lane decision set; success = **the denominator counts decisions that were actually made, whoever made them** | no games — a tooling change plus a row-type vocabulary the other two lanes can write. ⛔ **POSITIVE CONTROL REQUIRED IN BOTH DIRECTIONS, because this row proposes moving a number that currently indicts its own author: a window with many lane-decisions and few documents MUST read LOW, and a window with many documents and few decisions of ANY lane MUST still TRIP.** A fix that can only ever lower the ratio is not a fix, it is a silencer — and this row is exactly where that temptation lives. | **Why now: the alarm fired today and NEITHER reading can be defended. "Analysis is outpacing decisions" cannot be confirmed, because two thirds of the decisions are invisible; and "the denominator can't see me" cannot be trusted either, because it is the precise shape of a rationalisation offered by the lane with the motive. An alarm that cannot be evaluated is worse than one that is wrong — a wrong alarm gets corrected, an unevaluable one gets argued about every time it fires.** ⚠ **NOT A REQUEST TO SILENCE IT: the audit-session prescription stands on Magnus's call regardless of this row, and the prior art found a 19%-power battery nobody else caught.** | ⭐⭐ **PARTLY ANSWERED AND PARTLY SUPERSEDED WITHIN THE HOUR — THE AUDIT RAN AND BOTH TRIPPED ROWS WERE INSTRUMENT DEFECTS. THE FIRE WAS SPURIOUS; WITH BOTH REPAIRED THE DAY READS 0/6** (`docs/workflow-analysis/AUDIT-2026-08-14-s39.md`). **My denominator diagnosis was right and INCOMPLETE, and the half I missed was mine:** the numerator counts **8 `PREREG-*` files living in `docs/research/`** because the mandated-prose exclusion is DIRECTORY-prefix-based and misses them — **so my own preregs, which are the most mandated prose this lane writes, were inflating the row that indicts this lane.** The denominator half was real (builder had typed zero of ~10 decisions onto a 22.1h-stale `results.tsv`, with no freshness refusal) and is repaired + backfilled. Repairs shipped: filename-based exclusion, a freshness gate, and the builder types its gates. ⇒ **THE ROW'S REMAINING SCOPE IS NARROWER: research/side-lane decisions still have no recorded surface, which is a real gap, but it is NO LONGER EVIDENCED BY A FIRING — the firing is explained.** Keep it stocked at reduced priority; **the two-directional positive control is now MORE important, not less, because the alarm it would modify is currently reading correctly.** ⚠ **And the lesson I owe out loud: I offered *"the denominator can't see me"* as an assessment and flagged it as the shape of a rationalisation. It WAS partly right — and the audit found a second defect pointing the OTHER way that I never considered, in my own output. Being suspicious of your own excuse is not the same as finding the error.**
| **62** | ⭐⭐ **TINY-MAP ECONOMY FLOOR — OUR OPENING IS MAP-BLIND: WE SPAWN EXACTLY 5 BUILDERS BEFORE THE FIRST HARVESTER WHETHER THE MAP IS 100 TILES OR 900** *(Provenance: `BOOK-worst-maps-2026-08-14.md` (opus, Magnus's worst-maps directive). Admitted after independent verification — see the metric column.)* | ⛔ **GREP: vs `_v223sealrepair` (v140) — THE OPENING HAS NO MAP TERM AT ALL.** ⭐⭐ **RE-SCOPED 2026-08-16 ~12:5xZ (research, as queue owner) ON MAGNUS'S DIRECT IDEA — "why always 5 builders; can we spawn fewer and scale with Ti income?" THIS ROW NOW OWNS BOTH TERMS: OPENING SIZE = f(MAP, INCOME), replacing the constant. Not stocked as a new row — #62 already owned "the opening is map-blind", and splitting the two inputs would create two rows testing ONE function.** **ADDED GREP FACTS: `doctrine.py:28 MAX_BUILDERS = 5` is an ABSOLUTE FITTED CONSTANT. ⭐ AND THE ASYMMETRY IS THE FINDING: the SCALE-UP path above 5 ALREADY EXISTS and is INCOME-GATED (`doctrine.py:97`, delivered-Ti-per-round) — NOTHING STARTS BELOW 5. The machinery to make the opening income-responsive is already written; it simply cannot open lower than the constant.** **CURRENCY IS SCALE, as with #78: five openers = +100% BEFORE THE FIRST TURRET, so the r22 rush recipe pays DOUBLE PRICES for its hardware.** ⚠⚠ **THREE PRIORS THE ROW MUST CARRY, AND THE THIRD CORRECTS THE IDEA'S OWN STATED REASON:** (i) **9 of 10 losses in the fitting sample never spawned a SIXTH — the floor was shown to matter UPWARD; DOWNWARD is unknown, not shown safe.** (ii) **`_v69bc` read −13pts, but that was for RAISING the standing target — the direction is UNTESTED downward and the negative must not be quoted as if it were.** (iii) ⛔ **OUR MEASURED EARLY IDLE IS LOW — 3.31% at r10.** Magnus's *"standing around"* is **mostly the LATE SEALED class, which is `#74`'s territory, not the opening's.** ⇒ **THE CASE FOR FEWER OPENERS IS PRICE, NOT IDLENESS — and a row built on the idleness reason would be testing something the data says is not there.** The spawn path (`main.py:285-302`) sorts candidate tiles by a salted key and spawns; **the only gates before the first harvester are `SLOT_HARVESTERS`-keyed (`main.py:202`, `:548`, `:639`) and the eco caps are ABSOLUTE INTEGERS — `ECO_CAP = 18` (`doctrine.py:30`), `SURGE_ECO_CAP = 24` (`:405`).** `mw`/`mh` are read only for geometry bookkeeping (border clamps, angle seeds), which `#36`'s grep established and this row inherits. **No cap, gate or branch anywhere in the opening keys on map area, so the builder count CANNOT vary with map size — and it does not.** | ⭐ **INDEPENDENTLY VERIFIED BY RESEARCH BEFORE ADMISSION, and the measured form is stronger than the book's headline: MEDIAN BUILDERS BEFORE THE FIRST HARVESTER = 5.0 ON EVERY AREA CLASS — tiny <300 (n=252), 400 (n=574), mid 500-676 (n=351), 900 (n=611) — while the first-harvester ROUND does adapt sensibly (r5 tiny → r8 on 900).** ⇒ **the harvester TIMING responds to distance and the builder COUNT does not: a constant where a map-aware bot would vary.** Dial: builders-before-first-harvester as a function of area, and first-delivery round on tiny maps. | local screen on the tiny set, then a live leg. **15a PRIMARY SEGMENT: TINY MAPS (area < 300 — fjordgate 100, antler 252, moonrise 168). EXPECTED DIRECTION: POSITIVE on-segment, ~ZERO off-segment.** ⚠ **The direction is inherited from a mechanism, not from a sweep hit, so this is a genuine prediction rather than a replication test — unlike the segment-sweep candidates.** | **Why now: Magnus's direct directive ("improvements at our worst maps would be a significant upgrade"); fjordgate (38.5%) and antler (36.7%) are two of the five worst cells; and the two easy outs are BOTH closed with evidence — the maps are NOT upset-prone (league favourite-win rate 60.5-60.8% on antler/fjordgate against a 60.2% average) and the losses are NOT opponent-concentrated (terrain-general, 9-11 distinct opponents per map).** ⇒ **a mechanism must exist in our play, and this is one, named in our own source.** |
| **63** | ⭐⭐ **LONG-APPROACH ARRIVAL — OUR BUILDERS LOCK AT 3.3× THEIRS ON MIDGARD, THE FIRST US-VS-THEM LOCK COMPARISON EVER RUN** *(Provenance: `BOOK-worst-maps-2026-08-14.md`. The `#54` nav census has always measured OUR lock rate against OUR OWN maps; this is the first time it has been measured against the SAME OPPONENTS ON THE SAME MAPS.)* | **GREP: vs `_v223sealrepair` (v140) — inherits `#54`'s source read, unchanged in this tree:** `_nav`'s fallback counts a BACKSTEP as a successful move (`eco.py:898-910`), `self.stuck` increments ONLY when all four moves fail, and the escape hatches (`p == self.tgt`, `stuck >= 5`, `eco.py:1226`) therefore **can never fire inside an oscillation** — the home-lock report's own reason for recommending against a third detect-and-repick leg. **No oscillation detector exists in the shipped tree.** | lock-rate RATIO ours:theirs per map (the new comparative form), and builder-rounds recovered; `#54`'s census gives the absolute baseline (11.58% of our builder-rounds, midgard 35.6%) | archive-first (the census script is recoverable and must reproduce `#54`'s headline before its output is read), then a screen. **15a PRIMARY SEGMENT: {midgard, ragnarok} — LONG-APPROACH, and explicitly NOT "900-area": valkyrie and glacierkeep read 77%/73% and are among our BEST cells despite being the same size.** **EXPECTED DIRECTION: POSITIVE on-segment.** | **Why now: it is the first comparative lock evidence — a 3.3× ratio against the same opponents on the same terrain is a claim about US that no absolute rate could make.** ⛔ **AND THE HAZARD IS ALREADY PRICED: two detect-and-repick arms (OSCLOCK, OSCLOCK2) have died, and the home-lock report gives the source reason they could never have worked. ⇒ THIS ROW IS NOT A THIRD DETECTOR. Any arm off it must change the NAVIGATION or the DESTINATION, not the detection — and its prereg must say which, or it is the same dead arm renamed.** ⭐⭐ **MEASURED s45 2026-08-16 ~06:0xZ (research, opus sweep + independent re-derivation of its headline) — `docs/research/FORWARD-ARRIVAL-BASELINE-2026-08-16.md`. THE ROW IS CONFIRMED ON A CHANNEL IT DID NOT USE, AND ITS SEGMENT IS HALF WRONG.** ⛔ **SEGMENT CORRECTION, and it is the actionable half: `ragnarok` IS NULL — paired +5.80 rounds, p=0.33.** The `15a PRIMARY SEGMENT` is declared as **{midgard, ragnarok}**; only midgard carries an effect. ⇒ **an arm scoped to the stated segment DILUTES ITS OWN DOSE BY ~50%. The segment is midgard alone.** ✅ **CONFIRMED, via a statistic the row never measured:** the row's 3.3× is a **LOCK RATE**; **ARRIVAL TIME had never been compared us-vs-them.** Measured now, paired within game (D² recovered from core-death geometry, threshold `d2_enemy ≤ D²/9`, `join.tsv.our_team`): **midgard paired mean +22.14 rounds / median +19.5** (n=59 of 78 paired, 20 opponents, opponent-clustered sign-flip **p=0.0176**), and the **era-matched per-opponent DiD is +31.31 ± 13.44, 12 of 14 opponents** — so it is not an opponent-mix artefact. A same-type analogue also reproduces the row's own ratio: **failure-to-arrive 19.2% vs 5.1% = 3.75× (McNemar exact p=0.0192).** ⭐ **AND A BOUND THE ROW DID NOT HAVE, which is what keeps this from becoming a general nav rewrite: POOLED ACROSS ALL 25 MAPS WE ARE THE *FASTER* SIDE — paired mean −16.71 rounds (0.67×).** The defect is local to midgard, not a property of our navigation. ⚠ **SECOND-WORST IS `heart` (mean +15.92, n=93) BUT ITS MEDIAN IS ONLY +3.0 against midgard's +19.5** ⇒ **heart is a thin tail of catastrophic games, midgard is a systematic lag. Different mechanisms; only midgard's is the one this row describes.** ⛔ **COST, AND IT IS THE GATE: midgard is 6.78% of the new pool and the map is NOT SELECTABLE** ⇒ 48 midgard games for 80% power = **~710 games ≈ 142 matches ≈ 9.5 h of window.** A live leg on this row is a nine-hour instrument for one map. ⛔ **AND WHAT CANNOT BE MEASURED AT ALL, so nobody re-tries it: THERE IS NO STALL/LOCK RATE ON THIS CORPUS** — `corpus/events.tsv` carries only `BUILD` and `DEATH` rows, no per-round positions. **The 3.75× ↔ 3.3× correspondence is INFERRED, not measured**, and closing that gap needs a DECODER change, not a better query. ⛔ **PROVENANCE WARNING ON THIS AMENDMENT ITSELF: the sweep that produced it was briefed on a PARAPHRASE of this row's title taken from the TRUNCATED `SessionStart` boot line (`…3.3× THEIRS O`, cutting off before `N MIDGARD`), so it was sent to test a map-AREA axis THIS ROW ALREADY REJECTS in its own `15a` clause. The area cut duly returned a null (+1.77 rounds, p=0.7551) and {valkyrie, glacierkeep} duly ran the other way (−11.21, p=0.0365) — i.e. an opus agent re-derived this row's own text. Retracted in `docs/coordination.md` s45. ⇒ NEVER QUOTE A QUEUE ROW FROM THE BOOT DISPLAY; the truncation point is where the scope lives. Run `queue_check.py --next`.** **⭐ RE-READ s47 2026-08-16 vs the LIVE CONTROL `_v223sealrepair` (research, on the side lane's `GREP:`-scope audit; every anchor below re-verified here by hand, `ZZQXNOTATOKEN` control = 0/0/0/0 across all four files):** ⚠ **NEAR-MISS — the right PATTERN is shipped for the WRONG UNIT.** `rot_prev_dir` is a live A→B→A oscillation guard: `main.py:117` init, `main.py:791` `if want == self.rot_prev_dir`, set at `main.py:850` and `main.py:867`, with `doctrine.py:757` describing it as *"the A->B->A edge itself"*. ⛔ **But it guards GUNNER ROTATION, not builder movement — it is nothing to do with long-approach arrival.** ⇒ **the row's claim (builders lock at 3.3× on long approaches) is UNTOUCHED and stands.** ✅ **What changes for the builder: there is a proven in-tree template for the anti-oscillation term, so this is a PORT rather than a design — cite `main.py:791` in the prereg and do not re-invent it.** |
| **64** | ⭐⭐ **SPAWNPOCKET — NEVER PUT A BUILDER IN A CELL IT CANNOT LEAVE** *(Provenance: `HOME-LOCK-MECHANISM-2026-08-14.md` §5.1 — five candidate mechanisms refuted WITH controls, this is the residue. Prereg: `docs/research/PREREG-SPAWNPOCKET-2026-08-14.md`.)* | **GREP: vs `_v223sealrepair` (v140).** **(a)** the core's spawn-tile chooser (`main.py`, the `cands.sort(key=…)` before the `can_spawn` loop) sorts by a SALTED KEY ONLY — **nothing in it knows whether a candidate tile sits in a sealed region**, so a builder can be spawned into a pocket it cannot leave. **(b)** the `readable` pave branch of `_move` (`eco.py:934-954`) lays a conveyor with **no check on whether it removes an adjacent friendly builder's last exit** — we wall our own bots in. ⛔ **AND THE CLASS IS PROVABLY OUTSIDE `_bfs_direction`, which is the finding: OSCLOCK could not have fixed it BY CONSTRUCTION — you cannot free a bot from a sealed pocket, so those bots sat in OSCLOCK's treatment arm as PURE DILUTION.** That is a second, independent reason the two dead detect-and-repick arms read as they did. | per-half dose (pocket spawns on valkyrie → ~0; pave-refusals > 0 and pave-sealed bots → ~0 on midgard/fjordgate) · class-P bot count per map · locked builder-rounds against `#54`'s 11.58% baseline · **kill-round non-regression as an EXCLUSION** | local screen on the PRIMARY segment, `GATE-1000 < 51` then ≥52.0 @ 2,700 (±1.9pp at local DEFF 0.98) | ⛔ **SEGMENT REGISTERED DIFFERENTLY FROM ITS SOURCE, DELIBERATELY: the report proposes one primary of {valkyrie, midgard, fjordgate}; I register PRIMARY = {midgard, fjordgate} with the PAVE half as the primary mechanism, and valkyrie DESCRIPTIVE ONLY.** The two halves act on **disjoint map sets with opposite standing** — (a) is valkyrie-only and **valkyrie is one of our BEST cells (80.0%)**, (b) is midgard+fjordgate, **two of our five WORST (38.2%, 38.5%)**. **A pooled read would let a move on a map we already win four-in-five carry a segment whose whole value proposition is the worst-map deficit.** `ragnarok` excluded (2 pocket bots / 84 games) — **and note my own "lock-heavy" vocabulary would have made exactly that dilution: the segment term must come from the MECHANISM, not from a geometry label.** **Why now: Magnus's worst-maps directive, and this is the second mechanism-backed candidate under it after `#62`.** |
| **65** | ⭐ **PAIRED PANEL — GIVE EVERY SCREEN'S LOSING ARM A FIELD READ THE LADDER STRUCTURALLY CANNOT SUPPLY** *(Provenance: `docs/research/SCREEN-PREDICTIVE-VALIDITY-2026-08-14.md`, s42. Commissioned to put evidence behind a non-transitivity objection; found something worse.)* | **GREP: NOT A BOT CHANGE — this is a MEASUREMENT PROTOCOL, so the grep runs against the RECORDING PATH, and it found two live gaps.** **GREP-PATH: results.tsv** *(s45: token per `docs/research/SPEC-queue-grep-path-2026-08-16.md` — inert until the tool change lands; no bot tree BY DESIGN.)* **RE-CHECKED 2026-08-16 (research, queue-grep audit): `results.tsv` confirmed present on disk. The current bot incumbent (see PROGRAMME.md INCUMBENT field) is NOT the relevant tree — this row's subject is a measurement-recording protocol, not bot behaviour, and a bot-tree grep would misreport what was checked. GREP-PATH exemption stands; the tool-side fix routed to BUILDER is still unmade.* **(a)** `results.tsv` keys screens by **local tree name only** — neither arm's PLATFORM version is stored, so no retroactive join is buildable however much field data accumulates. **(b)** `corpus/version_trees.tsv` covers **only our own ships**: x3r0's **v134, v141, v142, v143, v145 have NO entry**, which is why the validity join had to be rebuilt by grepping `docs/coordination.md`. ⇒ **the cheapest half of this row — RECORD BOTH ARMS' PLATFORM VERSIONS PER SCREEN — is a one-field change that ships INDEPENDENTLY of the panel and unblocks every future validity read.** | **concordance rate between screen verdict and matched-panel field verdict, as a SLOPE not a sign** — a screen share is a falsifiable Elo prediction, `400·log10(p/(1−p))` (the 51.0 bar predicts **+7.0 Elo**, smaller than one rated match's swing). Recording that number per screen turns concordance from 1 bit/pair into a regression, informative from **~10 pairs instead of ~30**. | **pinned six-opponent unrated panel, BOTH arms, ~150 games/arm ≈ 2 h of free window per arm** (unrated is free; the cap is 5 matches/20 min). At ~3 screens/day that is **~40 paired observations in a fortnight.** | ⛔ **WHY NOW — THE SHIP CRITERION RESTS ON m = 1, MEASURED.** Only **four** version-pairs in all history have both an informative local screen and rated games on both sides: 3 concordant, 1 discordant (the screen said x3r0's v134 beat our v125; the field said the opposite), two-sided binomial **p = 0.625 — consistent with ZERO predictive validity and with PERFECT validity alike.** Exactly one pair (v140 vs v139) is statistically resolved at all, and the screen was right on it. ⛔ **AND THE FALLBACK IS GONE TOO: the rated tape cannot rank our own versions at the tenures we grant.** Across the 17 versions with ≥15 rated matches, between-version variance in Elo/match is indistinguishable from sampling noise — **ANOVA F(16,612) = 0.840**, within-version SD 8.04, between-version point estimate 0.000. ⚠ **RESTATED AS AN EXCLUSION per `CLAUDE.md`'s direction clause (a fail-to-reject must not be banked as a null): 95% UPPER BOUND on between-version SD = 1.31 Elo/match ⇒ up to ~23.5 Elo over v140's 18-match tenure.** That is **not** "our versions are all the same" — it is a band the size of the effects we ship for, which is exactly why a resolving fixture is worth building. **The ladder cannot supply it by construction: the slot holds ONE bot, so the losing arm's tenure is whatever survived until displacement (0, 2, 2 and 8 matches in the four cases), and lengthening tenure prices at ~3.5 days of frozen slot per pair.** **Does NOT retire the screen — point 6: a corpus statistic prioritises a road, it does not close one. It is written BESIDE it.** |
| **66** | ⭐⭐ **MULTI-BUILDER BELT COLLABORATION — SPLIT THE LONG HAUL SO THE BELT CONNECTS IN 1/k THE TIME** *(Provenance: **MAGNUS, 2026-08-14** — "two or more builders collaborate on a harvester+conveyor setup: one building from the core, one walking to the harvester, building it, then laying conveyors until they connect"; he asked which variations to explore. Assessment + prior art from the builder; admitted by research.)* | ⛔ **GREP: vs `_v223sealrepair` (v140) — TWO FINDINGS, and the second is a COST the row must pay.** **(a) NO ROUTE COORDINATION EXISTS.** The comms store carries enemy-core, raid-live, forward-gun, harvester count, eco-ready, ferry id/round, launcher, threat, siege — **no segment assignment, no "who is paving what", no shared route state.** Belt-laying is per-builder and solo: `_wire_on_build` (`eco.py:504`), `_wire_tick` (`:530`), `_nav`/`_move` pave branch (`:898`/`:912`). ⛔ **(b) THE COMMS STORE IS FULL — ALL 16 SLOTS ARE ALLOCATED** (`doctrine.py:931-961` plus the reassignments at `:1184-1187`: FWD_GUN=8, FERRY_ID=10, FERRY_RND=11, RAID_N=12). **`STORE_SIZE` is 16 and there is no free slot**, so any crew/segment channel must EVICT a slot or BIT-PACK into one. A named cost that belongs in the design, not discovered at build time. | **time-to-first-delivery on the long-haul segment** (round the belt first connects end-to-end) · ⛔ **DIAL CORRECTED s43 (builder, from the `#66(d)` code read) — THE ORIGINAL DIAL WAS INVERTED ON ITS OWN PRIMARY SEGMENT.** ragnarok reads **41.9 conveyors/game, the HIGHEST of any cell** — because a high conveyor count *with abandoned chains* **IS the defect's signature, not its absence.** ⇒ **the honest dial is CONNECTED-CHAIN COUNT and FIRST-DELIVERY ROUND**, never raw conveyors/game. *(Original, now void: conveyors-per-game on fjordgate as the dose dial)* (currently **5.9/game there vs 33.4 off-cell — the belt is effectively never built**) · **arrival round on midgard/ragnarok** (r89, 12.5% arrive) · ⛔ **kill-round non-regression as an EXCLUSION** per `DEFENCE_ADMISSION_BAR` — economy is instrumental, the payoff channel is the r22-sentinel bank via first-delivery round, and a fail-to-exclude must be restated as an exclusion before banking | **local screen on the PRIMARY segment {midgard, ragnarok, fjordgate}**, which is **14.4-14.6% of pairings in the NEW POOL** (against 1.6-6.5% all-time — the 9.1x/2.2x misprice finding), so this is real money rather than a tail cell | ⭐ **VARIATIONS — ⛔ RE-RANKED 2026-08-14: `#66a` RESOLVED AND (d) IS NOW PRIMARY, NOT A GATED OPTION.** **`#66a` ANSWER: STALL, NOT DISCARD.** Probe (`bots/_probe_beltstall`) confirms **lossless back-pressure** — a disconnected chain queues **one stack per tile**, holds them **~984 rounds unchanged**, and on connection every stack shifts while the harvester releases a fresh one **IMMEDIATELY, off-schedule**: missed emissions were **QUEUED, NOT DESTROYED**. Connected control shows continuous flow, so the instrument sees movement. ⇒ **(d) HARVESTER-END-FIRST + BACKFILL STRICTLY DOMINATES for the long haul: emission starts the moment the harvester lands and NOTHING is wasted while the crews build toward each other.** The other three become fallbacks for the case (d) cannot cover:  **(a)** meet-in-middle two-crew (Magnus's form) · **(b)** leapfrog same-lane, both crews core-side — **avoids sending a lone builder on the exact walk that locks at 3.3x on midgard; couples with `#54`'s nav limit cycle** · **(c)** N-segment split-pave · **(d)** harvester-end-first + backfill, IF a disconnected chain banks stacks for burst delivery at connection. ✅ **The API bound held and the probe answered the only open half.** One stack per conveyor (`get_stored_resource_id` returns ONE id), so a chain of N holds N stacks — **and saturation STALLS the harvester rather than discarding, so the 10N Ti ceiling is a BUFFER, not a loss cap.** ⇒ **the remaining design constraint is the COMMS-SLOT COST (all 16 allocated), not the economics.** **Why now: Magnus's directive, and it lands on the segment the pool change just made 9.1x more valuable.** ⚠ **INTERACTION OWED (s40 rule — whichever prereg lands second owes the line): shares {midgard, fjordgate} with SPAWNPOCKET and {midgard, ragnarok} with `#63`.** **`#50` is NOT this row** — its walk-out pave died because harvester #1 lands r4-8 on near-adjacent ore with no walk to pave; **nothing to collaborate on at d²<=2. This row is the LONG-HAUL case only.** |
| **67** | ⭐⭐ **WIRE `_hunt_turret` — WE SPECIFIED ANTI-TURRET-CREEP IN 90 LINES WITH SIX TUNED CONSTANTS AND NEVER WROTE THE FUNCTION** *(Provenance: `docs/research/BOOK-http418-v103-2026-08-14.md`, s42 — commissioned as an opponent book, returned a defect in OUR OWN tree.)* | ⛔ **GREP: vs `_v223sealrepair` (v140) — VERIFIED BY THIS LANE, not relayed, and it is worse than "unwired".** a grep for `def _hunt` and for `_hunt_turret(` returns **NOTHING**: no definition, no call site, anywhere in the tree. The **only** three occurrences are `doctrine.py:160, :231, :771` and **all three are COMMENTS — two in the PRESENT TENSE**: *"the principle `_hunt_turret` is already built on"*, *"`_duel_safe` correctly refuses the melee duel, `_hunt_turret` therefore…"*. All **six** constants (`HUNT_DESIGNATE_DSQ`=8, `HUNT_MIN_HEALERS`=2, `HUNT_FINISH_HP`, `HUNT_FIRE_TI`=2, `HUNT_DEFER_BASE`, `HUNT_DEFER_SPREAD`) are **defined in `doctrine.py` and read ZERO times in `main.py`/`eco.py`/`raid.py`.** ⚠ **Builder still owes `git log -S "_hunt_turret"` to distinguish NEVER-WIRED from REMOVED-ON-EVIDENCE** — a plank removed for a measured reason must not be re-added as if new. | **harvesters BUILT per game** (the measured discriminator: **8.13/g on our wins vs 5.36/g on our losses**, while harvesters LOST is FLAT at 2.43 vs 2.54 — **they suppress our EXPANSION, they do not kill what we built**) · **early entity count** (moves our win rate **+15 to +19pp at every stratum**, computed independently of harvesters) · **PLACEBO: their harvester loss rate (1%) MUST NOT MOVE** · ⛔ **`DEFENCE_ADMISSION_BAR` kill-round non-regression at r198, as an EXCLUSION** | **pinned unrated legs — we already hold 26 matches / 126 games vs their v103 to pin against** (6 rated + 20 unrated). Treatment leg ⇒ **PIN** (per the pinning design rule) | ⭐ **Why now: the archetype is NAMED in our own doctrine essay** (*"Ouroboros… their whole plan is a creeping Gunner picket"*) **and the counter is recorded as measured** (*"volume: several builders peck concurrently, 348 hits, we win that map"*) — **we wrote down the enemy, the answer, and the constants, and shipped none of it.** HTTP 418 is **in band at +21 gap, a 5-0 pays +16.97**, and their picket is **5.21 gunners/game against a 2.20 field baseline**, planted at d²≈41 of OUR core, **5.07 near-core builds/game in 84% of games from median round 40**. Downstream chain measured end to end: our median `ti_collected` **1650 on wins → 455 on losses**, ammo **78 vs their 256**, our sentinel shots **20-22/band vs their 72-85**, our core dies **r191**. ⚠ **NOT a defence plank in disguise — the metric is our own BUILD-OUT, and the kill-round bar is carried as an exclusion precisely so it cannot become one.** |
| **68** | ⭐⭐ **OPPONENT-VERSION QUALITY FROM THE FIELD — READ THEIR STRENGTH OFF THE LEAGUE, NOT OFF OUR OWN GAMES. IT DISSOLVES THE COLLINEARITY THAT `CLAUDE.md` CALLS UNRESOLVABLE, AT ZERO GAMES.** *(Provenance: research s43, 2026-08-14. Born from a RETRACTION: I published that "NO retrospective cut can EVER separate our-regression from their-improvement, for ANY opponent" and it was false within six minutes. The agent-decoded Bisons book found the third body.)* | ⛔ **GREP: NOT A BOT CHANGE — an ANALYSIS instrument, so the grep runs against the analysis path, and it found the gap.** **GREP-PATH: tools/target_value.py** *(s45: token per `docs/research/SPEC-queue-grep-path-2026-08-16.md` — inert until the tool change lands; no bot tree BY DESIGN.)* **RE-CHECKED 2026-08-16 (research, queue-grep audit): `tools/target_value.py` confirmed present on disk; `corpus/league_matches.tsv` fields cited still present. The current bot incumbent (see PROGRAMME.md INCUMBENT field) is NOT the relevant tree — this row's subject is an analysis instrument, not bot behaviour, and a bot-tree grep would misreport what was checked. GREP-PATH exemption stands; the tool-side fix routed to BUILDER is still unmade.* `tools/target_value.py` reads opponent RATINGS from `corpus/league_matches.tsv` but **nothing anywhere computes an opponent's performance PER THEIR VERSION**. `corpus/league_matches.tsv` already carries every field required (`teamAVersion`, `teamBVersion`, `scoreA/B`, `ratingABefore/Before`, `createdAt`) and **no tool consumes them jointly**. `CLAUDE.md`'s opponent-version bullet says only that we can **PIN** theirs and **READ** theirs; it does not say we can **PRICE** theirs. **The capability has been on disk for the whole project and has never been run.** | **match-level mean(S − E) per opponent version, our matches EXCLUDED**, with `E = 1/(1+10^(gap/400))` on `ratingBefore` and `S = games/total`. **Success = the instrument returns OPPOSITE verdicts on two teams from the same run** (it already has: The Bisons v7→v8 **+32.5pp**, Juusto v7→v11/13 **flat, ~+6→+7pp, all CIs overlapping**). **Validated:** 5,000-draw label permutation (null centred −0.11pp, band ±10.2pp, **p<0.0002**) and a negative control on an adjacent version pair (v6−v5 = −4.1pp, **inside** the band). | **NO GAMES — the entire instrument is a cut over `league_matches.tsv`, already synced.** Cost is one tool. **Fixture-free by construction, which is the point: it is the only question-answering surface we own that consumes no window and no rating.** | ⭐ **Why now — IT HAS ALREADY PAID FOR ITSELF TWICE IN TEN MINUTES, IN OPPOSITE DIRECTIONS.** **(1) It KILLED a leg I had ordered:** The Bisons' v7 was a **field-wide collapse** (−15.2pp vs field, n=61 matches) that **recovered to +17.2pp at v8** — our celebrated **82.2% vs their v7 was us catching a broken bot**, our swing +33.7pp against the field's +32.5pp, and **we have never been above ~50% against a functioning Bisons.** Mechanism visible in their build: **sentinels by r60 median 3.0 → 1.0 → 4.0**, costing them **~284 Elo in 20 hours.** **(2) It CONFIRMED the other leg** by eliminating the boring explanation: Juusto's field strength is flat across the boundary while we fell **52.00% → 25.33% (p=0.0002)**, leaving only *our-side-specific* or *a targeted counter*. ⚠ **MECHANISM-OCCURRENCE (fifth clause) — the limit is structural and must travel with the row: a counter aimed specifically at US is INVISIBLE to this instrument by construction**, because it would not move their field numbers. **It removes the boring explanation; it can never confirm the interesting one.** ⇒ **it PRIORITISES a pinned leg, it does not replace one** (point 6). ⛔ **SECOND LIMIT, ADDED s43 AFTER STOCKING AND IT IS THE LOAD-BEARING ONE: `mean(S−E)` IS CENTRED AT ZERO BY CONSTRUCTION.** League-wide control over **46,318 match-sides: +0.328pp ± 0.119** — the Elo system drives S−E → 0 for every converged rating. ⇒ **the instrument measures RATING-RELATIVE SURPRISE, not absolute strength, and it can only see a version change in the window BEFORE the rating catches up.** A team reading ~0 is **correctly rated, not weak**, and **a flat reading is NOT "they did not change" — it is "they made no UNPRICED change".** The Bisons v7→v8 was detectable precisely because it was large and fast enough to outrun the rating. ⚠ **Third limit: opponents below ~4 matches on a version are not reportable** (Bisons v9 = 2 matches at time of writing), so it is blind to exactly the freshest ship — **the pinning capability covers that half.** |
| **69** | ⭐⭐⭐ **THE CONJUNCTION — SPAWN-RING SIEGE × BELT ATTRITION IS THE LOSS MODE OF OUR TWO WORST MATCHUPS, AND SINGLE-FACTOR PLANKS PREDICT NOTHING** *(Provenance: research s43 agent decode of `0033`, control opponent `team lazy` on every cut, 2026-08-14. Ordered as the #1 fire-order cell then deliberately answered from the 241 games already in hand rather than with a window.)* | ⛔ **GREP: vs `_v223sealrepair` (v140) — and it lands on a CALL SITE, not doctrine, so it is a behaviour and not a spec.** The belt-repair path `_l4_repair` EXISTS and runs (its own docstring counts "DEAD HEADS" off 15 replays), and `_wire_tick` (`eco.py:530`) **overwrites `link_queue` after `SIPHON_WIRE_RNDS = 12` rounds** — ~6 tiles at ~2 rounds/tile, while **14/16 midgard and 12/14 ragnarok routable chains exceed 6 tiles.** ⇒ **we already rebuild the belt; the defect is that the rebuild is ABANDONED mid-lay under exactly the sustained attrition this row measures.** ⛔ **NOTHING in the tree reads enemy builds inside our own spawn ring**: no d²≤8 enemy-barrier census, no spawn-tile contest logic — the core's spawn chooser sorts by a salted key only (per `#64`). **The siege half is UNDEFENDED and UNMEASURED in the shipped bot.** | ⭐ **THE CONJUNCTION, because neither half is a metric on its own** — cross-opponent Spearman over 17 opponents: **spawn-ring-siege rate −0.79 · conveyor attrition −0.72 · forward-build ratio r<100 −0.83**, against **their melee attacks −0.03** and **their mid-game conveyors −0.01**. Primary dose: **our conveyor deaths per 100 rounds of exposure (0033 4.10, Leviathan 3.02, floor: The Bisons 0.34, Focalground 0.01 — a 12–400× range)** and **the SLOPE r100→r200 (0033 4.55 vs lazy 1.85 / field 2.22 — against everyone else the curve FLATTENS after r100; against 0033 it stays LINEAR to r200)**. Payoff channel: our **r100–250 build-out**, which is where the whole divergence sits (sentinel 0.71 vs 2.02 lazy; builder spawns 0.39 vs 0.93) and our **Ti at r150, 620 vs their 930 — 310 behind, 2× the field gap and 5× the control's.** ⛔ **PLACEBO, MANDATORY: `team lazy` must NOT move — it sieges us in 67% of games and we beat it 61–71%.** ⛔ **`DEFENCE_ADMISSION_BAR` kill-round non-regression carried as an EXCLUSION** (our core dies r146–165 in a narrow r125–200 window; **we are NOT rushed** — dead by r100 is 0–1.9%, *below* the field's 5.4%). | **pinned unrated legs — we already hold 42 unrated matches / 206 games vs their v54–v58 plus 7 rated to pin against.** Treatment leg ⇒ **PIN** (design rule). ⚠ **And run `#68` on them FIRST**: 0033's hole is stable at 19.7–40% across six of their own versions, so #68 will not explain it away — **that check is what promotes this from "a bad cell" to "a mechanism", and it is free.** | ⭐ **Why now — IT NAMES BOTH OF OUR TWO WORST MATCHUPS AT ONCE, AND IT SURVIVED FOUR NULLS THAT EACH HAD A CONTROL.** 0033 is **in band at +64, a 5-0 pays +18.93**, the top-value reachable target; ⛔ **LEVIATHAN AS A SECOND INSTANCE IS RETRACTED s43 — BY THIS ROW'S OWN AUTHOR, WITHIN THE HOUR, USING THE RULE HE HAD JUST PUBLISHED.** The row cited *"Leviathan sits at the same conjunction (95% siege, 3.02 attrition) and is our worst cell at 15.9%"* — **and 15.9% is a RAW WIN SHARE, which is exactly the baseline-not-S−E error `#68` was amended to stop 20 minutes earlier.** Leviathan is rated **1982, +207 above us, OUTSIDE `BAND_HI`**. At that gap the expectation is **23.3%**; we score 15.9%, so **S−E = −8.9pp [−21.5, +3.6] since 08-12 (14 m) and −2.7pp [−9.3, +3.9] all-time (55 m) — NEITHER EXCLUDES ZERO.** ⇒ **we lose to Leviathan about as much as anyone 207 points weaker should. They are a STRONG team, not a conjunction instance.** **#69 therefore rests on ONE instance (`0033`), not two, and the "mechanism rather than opponent quirk" promotion is NOT YET EARNED** — it needs a second IN-BAND cell whose S−E excludes zero. **Candidate to test: `LingLing40`** (in band, pays +16.84, −15.9pp on 8 matches, does not exclude zero). ⚠ **The 0033 evidence is UNAFFECTED: its S−E since 08-12 is −17.9pp [−34.8, −1.0] and DOES exclude zero.** **`titanium_collected` is NOT the channel and `R1000_IS_DEFEAT` is not at risk — rated games vs 0033 are core_destroyed 35 of 35 (100%), and r1000 is RARER here (0.0%/1.5%) than anywhere.** Economy is instrumental exactly as the programme says: the 310 Ti deficit at r150 buys the mid-game sentinels we never build. ⛔ **DEAD ENDS ALREADY CLOSED WITH CONTROLS, do not re-spend them: titanium siphon (median leak 0.00% for us, them, control AND field), builder-melee VOLUME (ρ=−0.03; Coreflood/Hugging Farce/Focalground all run 59–60 attacks/game and we beat them 65/86/83% — attacks matter only WHERE they land), their conveyor economy (ρ=−0.01), map size (0033 skews small, but the effect is intact restricted to area≥400), and launcher kidnap either direction (ZERO launchers built by 0033 or the control in any game).** ⚠ **LIMIT THAT TRAVELS WITH THE ROW: attribution is INFERENCE, not measurement** — `events.tsv` carries BUILD/DEATH only, no attack event, so *"their builders ate our belt"* is circumstantial (converging: their melee 63–74/game vs 20–27 elsewhere; our conveyor deaths cluster 3.6 tiles from OUR core; our own `destroy()` cannot drive it since zero-attack matchups floor at 0.34/0.01). ⚠ **And the siege dose is SATURATED within 0033** (83 of 120 games ≥3 ring barriers by r150), so **the evidence is ACROSS-opponent only** — a within-opponent dose-response does not exist and **reverses on the 7-match ladder cut.** **⭐ RE-READ s47 2026-08-16 vs the LIVE CONTROL `_v223sealrepair` (research, on the side lane's `GREP:`-scope audit; every anchor below re-verified here by hand, `ZZQXNOTATOKEN` control = 0/0/0/0 across all four files):** ⚠ **NEAR-MISS, and it sharpens the belt-attrition half.** `eco.py:267-272` `_free_seats` DOES read seat occupancy — `if ct.get_tile_builder_bot_id(s) is not None: continue` — so occupancy is not invisible. ⛔ **But the read is TEAM-BLIND (no `get_team` call on the returned id) and the seat is SILENTLY DROPPED.** ⇒ **an ENEMY builder parked on our heal seat and one of OUR OWN builders standing there are byte-identical to this function, and both simply remove the seat from the list.** ⭐ **That is the contested-seat mechanism this row is about, already half-instrumented: occupancy is READ, never ATTRIBUTED and never CONTESTED.** ✅ **Plank sharpens to: attribute the occupier's team at `eco.py:271`, and contest rather than yield when it is theirs.** |
| **70** | ⭐⭐⭐ **ZERO IDLE-AND-FREE — AN IDLE BUILDER MUST ALWAYS EMIT A VERB, AND THE TERMINAL FALLBACK IS A FREE ONE: BODYBLOCK** *(Provenance: **MAGNUS, 2026-08-15** — "I want us to get as close to 0% idle and free builders as possible. If they cant heal or build or peck, they can still bodyblock and be annoying." Evidence re-measured by research the same hour off `scratchpad/idle_split_s31.py`, 8,338 archived games / 894k builder-rounds, cooldowns READ off SetActionCooldown/SetMoveCooldown, not inferred.)* | **GREP: PASS, three sites checked in `_v223sealrepair` (v140, the incumbent).** (1) `main.py:797 _idle_rotate` is GUNNER-only, guarded at `:801` by `if turret_type != EntityType.GUNNER: return` — **no builder-side idle handler exists**; grep for `SLOT_IDLE` / `idle.*destination` / `idle.*broadcast` across `main.py`,`eco.py`,`raid.py`,`doctrine.py` = **0 hits** (carried from #14, re-verified today). (2) `eco.py:228 _eco_spendable` gates EVERY paid verb on `ti >= cost` (+`SIEGE_HEAL_RESERVE_TI`=16 under siege, `doctrine.py:437`) — **so build/heal/attack are bank-gated and a MOVE is not.** (3) No bodyblock/denial-move exists anywhere: the only body-in-the-lane row is **#10, which is a LAUNCHER plank** (throw a KIDNAPPED ENEMY builder into their gunner's lane), not our own idle builder. **Different mechanism, and #10's physics fact is the gift: a gunner's shot is a straight line that does NOT pierce, so one body blanks it.** | share of builder-rounds in bucket A (idle AND free), measured at THREE windows so the decay curve is the metric, not a single number: `[0,10]` · `[40,60]` · `[T-20,T]`. Secondary: mean idle headcount at T-1 (now 1.44 LOSS / 1.62 WIN). | `scratchpad/idle_split_s31.py` recomputes all three windows from the archive at zero cost and IS the mechanism metric; then a local screen on the arm; then a live leg. **The instrument already carries its own controls (see below), so the fixture is not the risk.** | ⭐ **THE PRIZE IS BIGGER THAN #14 SAID AND THE SHAPE IS DIFFERENT: IDLE-AND-FREE IS NOT A CONSTANT, IT IS A DECAY CURVE.** #14 banks 25.76% as a single figure measured ONLY in `[T-20,T]`. Re-run across three windows (n=8,338 games): **r10 `[0,10]` = 3.31% LOSS / 3.12% WIN · r60 `[40,60]` = 17.77% / 16.86% · core-death `[T-20,T]` = 29.01% / 29.44%.** ⇒ **the bot starts near Magnus's target and decays to ~29% — it does not begin broken, it RUNS OUT OF THINGS TO DO.** ⭐⭐ **AND THERE IS NO ENGINE EXCUSE: bucket D (ON COOLDOWN) is `0.00%` at ALL THREE WINDOWS, both arms.** Every SetActionCooldown/SetMoveCooldown ever written for a builder is `1`, and placement is `(0,0)` — a cooldown set to 1 decrements at end of round, so **a builder may act or move EVERY round and 0% is a genuinely reachable ceiling, not an asymptote.** **WHY THE FALLBACK MUST BE A MOVE, which is Magnus's point and the arithmetic backs it:** global titanium at T-1 is **median 26 LOSS / 53 WIN, q1 12** — against `_eco_spendable`'s `cost + 16` under siege, so at the first quartile build/heal/attack are ALL unaffordable while **a move costs 0 Ti and is never bank-gated.** ⚠ **AND ONE VERB IS ALREADY ENGINE-BARRED: builder melee CANNOT target an enemy builder bot** (`can_fire=False` on an adjacent enemy builder, every occurrence — banked in #45), so "peck" only ever means an enemy BUILDING. **That removes a fallback people will assume exists.** **NAMED DESTINATIONS, so "be annoying" is a spec and not a mood — in cost order:** (a) **stand in an enemy gunner's fire line** (#10's non-piercing fact, applied to our own body, 0 Ti and 0 ammo); (b) **stand on their core's spawn-adjacent tiles** — spawn-tile denial is one of the three roads `CLAUDE.md` records as never balance-changed and still open; (c) **stand on the ore tile they want a harvester on**; (d) **stand in the belt corridor** so their builder must path around. **COMPOSES WITH / SUPERSEDES:** absorbs #14 arm A (destination) and gives it a FREE terminal fallback #14 never had — ⚠ **#14 arm B (RECALL) still carries `kill_round_non_regression` and is NOT revived by this row.** **⛔ AND #54 MAY BE THE MECHANISM UNDER IT:** 11.58% of ALL v125 builder-rounds sit in permanent two-tile nav locks and 39.8% of locked bots never acted in their lives — **a bot oscillating in a lock is emitting a MOVE, so it scores as ACTIVE here and is INVISIBLE to bucket A.** ⇒ **a verb-emission target can be met by a bot that is still useless. The arm must be scored on bucket A AND on #54's lock rate together, or it will Goodhart itself.** **⛔ AMENDED 2026-08-15T07:5xZ — TWO HAZARDS THIS ROW DID NOT CARRY, BOTH RAISED BY PEERS, BOTH VERIFIED BY ME AT THE ENGINE PRIMARY.** **(H1) OUR OWN BODY BLOCKS OUR OWN BUILDERS — passability is TEAM-BLIND.** `Tile::is_bot_passable@0x2eabc` **returns false for ANY tile holding a live builder bot** (`engine-guard-matrix-exploit-hunt-2026-08-10.md:171`, disassembly, not inference). ⇒ **destinations (c) their ore tile and (d) their belt corridor are the two most likely to be mis-sited into OUR half, where a bodyblock is self-harm** — it walls our own belt-layers out of their own corridor. **The rule must site destinations in THEIR half and say so.** *(builder)* ⭐ **AND H1 NEEDS NO LIVE TEST — IT IS A RULES-LEVEL IMPOSSIBILITY, NOT A BEHAVIOURAL INFERENCE** *(side lane)*: the disassembly carries **no team qualifier and no premise about how any bot chooses to act**, so it falls under D12's carve-out and closes on the engine alone. ⇒ **the hazard is SETTLED; only the destination SCORING needs measuring.** ⚠ **Cite the address, never the gloss:** `CLAUDE.md:71` describes `is_tile_passable` as *"A friendly builder bot could stand there"* — **consistent** with the engine but it invites exactly the team-scoped misreading, and the disassembly is what settles it. **(H2) ⭐ DESTINATION (b) PUTS OUR BUILDER INSIDE AN ENEMY LAUNCHER'S PICKUP ENVELOPE BY CONSTRUCTION.** Pickup is **d²≤2, no team check, no vision guard, 0 ammo** (`CLAUDE.md`, engine-read). Standing on their core's spawn-adjacent tiles IS that envelope. **⇒ #70 AND #59 ARE ONE DESIGN, NOT TWO ROWS, AND NEITHER CITED THE OTHER:** #59 (`DON'T GET FARMED`, Magnus's own defensive observation) already records that `_bfs_direction` (`eco.py:809-832`) blocks an enemy launcher's **OWN TILE ONLY** and never its envelope. ⇒ **the bodyblock destination scorer must treat `d²≤2` of an enemy launcher as a COST, which is the fix #59 already specifies and is blocked on. Build them together or #70 walks idle builders into the farm #59 exists to stop.** ⚠ **AND PRICE THE TRADE RATHER THAN AVOIDING IT, because it is asymmetric IN OUR FAVOUR: their throw costs 0 ammo and one cooldown; our thrown builder is a POSITION-ONLY mutation and SURVIVES.** ⇒ **the cost is the walk-back, not the unit** — a bodyblock beside their core may well be worth being thrown for, and the prereg must model that as a priced trade, not an unmodelled hazard. *(side lane)* **⭐ AND ONE SHARPENING ADOPTED FROM THE BUILDER: the #54 lock rate is a FALSIFIER, not a companion column.** If lock rate RISES while bucket A falls, the plank is **refuted regardless of what bucket A does** — that is strictly stronger than scoring them side by side and it closes the Goodhart by construction rather than by inspection. **WHAT DEFERS (Magnus's row jumps, and this is what it jumps over):** #67 `_hunt_turret` and #69 the conjunction are the two rows I would otherwise have put next; both stay ranked and neither is withdrawn. |
| ~~**46**~~ | ⛔ **DEAD — REFUTED BY ITS OWN GATING CUT, s36, ~45 minutes after stocking. DO NOT RE-QUEUE.** The premise was *"Juusto's siege may be decorative — 44 turrets, only 15 ever fire at our core, 11,939 ammo banked"*. **The gating cut (shots at ANY target) RAN and refutes it: Juusto's turrets fire 846 shots across 15 games = 15.7 shots/turret — the same rate as Coreflood (15.8), and ABOVE Leviathan (5.3).** They are not decorative; they shoot our RAIDERS instead of our core, which is a different fact and not a plank. Field context from the same cut: LingLing40 27.4 shots/turret, team lazy 26.9. ⛔⛔ **AND MY "BLOCKED ON A DECODER CHANGE" NOTE 45 MINUTES AGO WAS ALSO WRONG, IN A WAY WORTH MORE THAN THE ROW: `corpus_sanity.py:90` reads `"econ.tsv shots": replay_econ.py:109 pass -- USE build_agg.tsv metric=='shot'`. I quoted that entry as proof the column was dead and did not read its second clause, which names the working surface. I read half a documented entry and reported the half that agreed with me.** *(The decoder fix stands on its own merits — see the tail — but it was never the blocker.)* |

 # | plank | change | mechanism metric | why now |
|---|---|---|---|---|
| ~~2a~~ | ~~**Idle-builder defence**~~ | **WITHDRAWN — WE ALREADY SHIP IT.** See below. |
| 2 | **KILL THE SENTINEL FROM OFF ITS AXIS — `rotate()` IS GUNNER-ONLY** | raider adjacent to an enemy SENTINEL, on a tile off its facing line, attacks it to death instead of routing around it | **GREP: PASS** — `raid.py:366` already attacks turrets (priority 3/7) so the bare plank is SHIPPED; `raid.py:501-566` + `doctrine.py:1515 LOKI_GUNAXIS_PENALTY=8` is **avoidance only** and `raid.py:518` builds the axis set from `EntityType.GUNNER` alone — **no path prefers an off-axis tile in order to ATTACK, and sentinels are never modelled as shooters.** Metric: enemy sentinels destroyed by builder attack per game (now ~0) **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: attack priority `pr=3` for GUNNER/SENTINEL confirmed at `raid.py:616`; the avoidance gun-axis set is built only from `EntityType.GUNNER` at `raid.py:768-774`, consuming `LOKI_GUNAXIS_PENALTY=8` (`doctrine.py:1515`) at `raid.py:816` — lines shifted (was :366/:501-566/:518) but content and premise unchanged: no off-axis-to-attack preference, sentinels never modelled as shooters.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **The intervention `#2` was missing. On-axis attack is a LOSING trade by rules arithmetic (we lose by 1 round vs a gunner, 11 vs a sentinel); off-axis against a sentinel is FREE, because a sentinel cannot turn to face the attacker.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `LOKI_GUNAXIS_PENALTY=8` confirmed byte-identical; true current location is doctrine.py:1533 (the row's inline :1515 is the pre-MAP_CODES-shift number, never individually updated — value/content unchanged, only the pointer was already stale before this carry). raid.py:616,768-774,816 carry unchanged (0 diff). Premise fully intact. |

**⭐⭐ `#2` RE-SPECIFIED s35 (research). It was admitted with `change` = *"not yet
specified — intervention deliberately NOT named"* and NO `GREP:`, so
`queue_check` refused to count it while `cores_idle.next_queue_item()` — which
reads positional order — handed it to a builder as the top of the queue. Side
lane flagged the split; this row is the fix. The duplicate row number `2` is also
resolved (the withdrawn idle-builder row is now `2a`).**

**`GREP:` (run against the SHIPPED tree `bots/_v169launchlate160`, which is live
as v116, BEFORE counting this row):**
* **We ALREADY attack enemy turrets** — `raid.py:366`, priority **3 of 7** in the
  raider's adjacency attack scan (`CORE 0 · seat conveyor 1 · LAUNCHER 2 ·
  GUNNER/SENTINEL 3 · HARVESTER 4 · conveyor 5`). So *"destroy enemy turrets"* as
  a bare instruction is **a feature we ship**, and the cheapest null in this repo
  would have been a leg that tested it.
* **What we do NOT ship is any axis awareness on the OFFENSIVE side.** The
  `gun_axis` machinery (`raid.py:501-566`, `LOKI_GUNAXIS_PENALTY = 8` at
  `doctrine.py:1515`) is **pure avoidance** — it penalises *standing* on a ray —
  and `raid.py:518` builds that set from `EntityType.GUNNER` **only**. **No code
  path prefers an off-axis tile in order to ATTACK.** Sentinels appear in the
  scorer as targets, never as shooters (same blindness `#30` names defensively).

**⭐ THE ARITHMETIC THAT NAMES THE INTERVENTION** *(rules-level, from the entity
table: builder 40 HP and 2 Ti → 2 dmg adjacent, 1 action/round; gunner 25 HP dmg
7 reload 1; sentinel 40 HP dmg 18 reload 2)*:

| duel, ON the turret's firing axis | rounds we need | rounds until our builder dies | verdict |
|---|---|---|---|
| builder vs **GUNNER** | 13 (26 Ti) | 6 hits × 2 = **12** | **WE LOSE by 1 round** |
| builder vs **SENTINEL** | 20 (40 Ti) | 3 hits × 3 = **9** | **WE LOSE by 11 rounds** |

⇒ **This is why the intervention was never nameable: head-on, attacking a turret
is a losing trade in BOTH cases, and no amount of prioritisation fixes it.**

**⭐⭐ AND THE ASYMMETRY THAT MAKES IT FREE: `rotate()` IS GUNNER-ONLY**
(`CLAUDE.md:124`, *"Gunner-only; 10 Ti, sets action cooldown to 1"*). A gunner
answers an off-axis attacker for 10 Ti and one cooldown. **A SENTINEL CANNOT
ANSWER AT ALL** — it is a facing turret with a single-tile-wide line shot
(`CLAUDE.md:38`) and no rotate in the API. **A builder orthogonally adjacent to a
sentinel but off its facing line kills it in 20 rounds for 40 Ti while taking
ZERO damage from it.** And because turret facing may be any of the 8 directions
while builders stand only on the 4 orthogonal neighbours, **a diagonally-facing
sentinel cannot hit ANY adjacent tile.**

**WHY NOW:** `#32` measures **our sentinels dying at 71% against theirs at 34% —
the largest measured gap on the board.** This is the same asymmetry read from the
other end: if a fixed-facing sentinel is nearly free to kill from off-axis, the
gap is a *behaviour* difference, not a durability one, and this row is the
offensive half of it.

**FIXTURE THAT CAN RESOLVE IT:** local self-play on the `_v169launchlate160`
contrast (the calibrated one — null reads 49.56%, known-bad reads 40.53% at
n=5408), mechanism metric = **enemy sentinels destroyed by builder attack per
game**, which is currently ~0 and so is a clean dose check: if the arm does not
move it off zero, the leg is uninterpretable and must not be read (`B5`).

## ⭐⭐⭐ FALSIFIER CLEARED ON THE ENGINE BINARY, s35 2026-08-13 (research)

**The row was admitted resting on `CLAUDE.md`, which this repo records as
known-wrong in places. It no longer does. Both premises are CONFIRMED on
`fcode_engine.cpython-313-darwin.so`, two toolchains intersected
(`objdump -d` Apple/LLVM and `otool -tV -arch arm64`, instruction-for-instruction
agreement over both decisive windows), with strings and jump tables read as RAW
BYTES rather than trusted from disassembler annotation.**

**PREMISE 1 — `rotate()` IS GUNNER-ONLY. CONFIRMED at THREE independent
enforcement layers**, each demanding the raw entity tag `0x8000000000000006`
(GUNNER = 6, SENTINEL = 7, LAUNCHER = 8, established three separate ways):

| layer | address | sentinel outcome |
|---|---|---|
| `__pymethod_can_rotate__` | check `0xb2c8`–`0xb2dc` | returns **False** |
| `__pymethod_rotate__` | check `0x8418`–`0x842c` | **GameError `"Unit is not a gunner"`** |
| `Game::rotate_gunner` | check `0x267d4`–`0x267dc` | Rust panic `"rotate_gunner called on non-gunner"` |

⇒ **A SENTINEL'S FACING IS IMMUTABLE FOR ITS ENTIRE LIFE.** `rotate_gunner` has
**exactly one call site in the whole binary** (`0x85cc`) and the symbol table
contains **no other direction or facing mutator**. Direction is set at build time
and never changes.

**PREMISE 2 — THE SENTINEL RAY CANNOT REACH AN OFF-AXIS ADJACENT TILE.
CONFIRMED.** `turret_target_valid_from@0x276f8`, sentinel branch at
`0x27764`–`0x27800`, is a pure ray walk: true **iff**
`target == turret + k·(dx,dy)` exactly, for `k ≥ 1`, bounded by `k²·|step|² ≤ 32`.
The unit-step tables at `0x9e5f8`/`0x9e618` were read as raw bytes and are the
exact 8-compass vectors. **The branch contains NO `GameMap::tile` call at all** —
obstacle-ignoring confirmed by absence, and **the absence is meaningful because
the GUNNER branch in the same function does call `gunner_attack_line` then
`GameMap::tile@0x2f0b0` per tile to block.** `attackable_tiles_from@0x27130` runs
the byte-identical predicate, so `get_attackable_tiles()` agrees — no wider
hidden pattern. `fire_sentinel@0x26230` damages **exactly one** entity, no splash
(`mov w2, #0x12` = 18 → `apply_damage@0x22048`).

⇒ **EXACT GEOMETRY: a sentinel at (x,y) facing NORTH covers precisely
(x,y−1)…(x,y−5). The other three orthogonal neighbours — (x−1,y), (x+1,y),
(x,y+1) — are NOT in the set and CANNOT be made so.** Cardinal facing reaches 5
tiles, diagonal facing 4.

**PREMISE 3 — BUILDER ATTACK. CONFIRMED:** adjacency is `|dx|+|dy| == 1` exactly
(`can_fire@0x16280`); damage 2 (`mov w3,#0x2` at `0x5be8`); cost 2 Ti
(`0x5bd0`–`0x5bd8`); **action cooldown incremented by 1 after attacking, so with
the end-of-round decrement it is exactly ONE ATTACK PER ROUND — 20 attacks = 20
rounds.** And `damage_building_tile` has **no team compare**, so an enemy
sentinel is a legal target.

**THE CONTROL, run because an absence had to be meaningful:** `can_destroy@0x3bc0`
loads the building's team byte and compares it to the caller's on its **first
three instructions** — the method surfaces a team check instantly where one
exists, and surfaced type checks in four separate functions. **So "no tile lookup
in the sentinel branch" is a real absence, not a method failure.**

## ⚠ THREE CAVEATS THAT SURVIVE — none touches the two premises
1. **LOCAL-ONLY.** This is the local `.so`; the platform may ship a different
   build. Every address above is a file offset in this binary.
2. **⭐ THE PRICING IS PER-SENTINEL, NOT PER-POSITION.** The result says nothing
   about a SECOND turret covering that tile. **A gunner (r²=13, and it CAN
   rotate for 10 Ti) or another sentinel on-axis is the real threat model**, and
   20 stationary rounds is a long time to be wrong about that. The prereg must
   score the tile against ALL enemy turrets, not just the target.
3. Our builder must gate on `can_fire()` every turn — see the by-product below.

## ⭐ TWO BY-PRODUCTS OF THE ENGINE READ — both checked against our own tree, both CLEAN
* **`fire()` subtracts 2 Ti UNCONDITIONALLY with no re-check.** The affordability
  guard lives in `can_fire` only (`cmp w8,#0x2` at `0x16650`). **Calling `fire()`
  ungated below 2 Ti drives team titanium NEGATIVE silently rather than raising.**
  **Audited s35: all 8 `fire()` call sites in `_v178salt` and all 7 in
  `_v169launchlate160` are gated on `can_fire()`. WE ARE NOT EXPOSED.**
* **⭐⭐ A LOKI LEAD, RECORDED NOT CLAIMED: an opponent that calls `rotate()` on a
  SENTINEL without catching the GameError DESTROYS ITS OWN UNIT PERMANENTLY**
  (`0x1ac5c` → `Game::destroy_entity`, prior art). **We cannot force it** — rotate
  is self-only — so this is not a weapon today. It becomes one only if a
  displacement trick can make a bot's own facing logic fire on the wrong unit
  type. **Not queued as a plank; logged so the next launcher-kidnap design knows
  the sentinel case exists.** (Our own rotate calls are all gated on
  `can_rotate()` — verified same session.)

**⛔ #2 WITHDRAWN — THE CHEAPEST NULL IN THIS REPO IS A LEG THAT TESTS A FEATURE
WE ALREADY SHIP, AND THIS WAS ONE.** Research's idle/active split gave the number
that unblocked it AND the warning that killed it: **bucket B ("active, home") is
92% HEAL** — 65,673 heal-rounds against 1,532 build and 3,980 attack in the 20
rounds before our core dies. **grep of the shipped tree confirms it**:
`eco.py:312 _heal_core`, `eco.py:322 _heal_adjacent`, `eco.py:125 heal_seats`
(tiles that exist ONLY to heal the core), multi-healer convergence at `:16-17`,
and `main.py:160-188 SLOT_UNDER` — an under-attack flag that already retargets
`ammo_target` and `ti_floor`. **The 2.44 "active home" builders are our own
heal line showing up in the data.** Two minutes of grep, one window saved.

**AND THREE MEASURED REASONS IT WOULD NOT HAVE PAID ANYWAY:**
* **Idleness carries NO loss-specific information**: idle share **25.76% in
  losses vs 27.59% in WINS**, flat straight through the core's destruction. What
  differs is WHERE activity sits, not how much there is.
* **Median bank at T-1 is 28 Ti.** At 2 Ti per 2 damage a redirected builder
  cannot do much — a cap independent of headcount.
* The genuinely expensive bucket (forward AND mid-action) is **0.18
  builders/game**, so the kill-budget objection was small — **my "admissible by
  construction" was still wrong, but the number landed in my favour.**

**⭐ TWO ENGINE FACTS ESTABLISHED AS BY-PRODUCTS, both correcting standing belief:**
**a builder bot's action cooldown is NEVER >0 at decision time** (only the value
`1` is ever written, 93.9% of one builder's consecutive actions are exactly 1
round apart) ⇒ **"builders act every other round" is FALSE.** And **99.75% of idle
rounds carry a `BotOutput`** — the engine ran `run()` and it emitted nothing —
with `tled` at **0.00% for us vs 1.52% on the field**, so that zero is real and
not a dead column.

**⛔ AND MY OWN "FORWARD TIMING" ENTRY FROM 20 MINUTES AGO IS ALSO REFUTED, BY THE
ARM THAT PROPOSED IT.** *"We go forward late into a matured turret field"* fails:
enemies build the SAME turrets against us as against the top tier EARLY
(**r0-59 ratio 1.04, r60-179 ratio 1.00**); divergence is entirely late, which is
**DOWNSTREAM of clearing, not upstream of it**. **The field is the same size when
we get there.**

⇒ **What differs from the first minute is DESTRUCTION: cumulatively the enemy
builds 1.19x more turrets against us while we destroy 0.88x as many.** That is
why #2 above is now *destroy enemy turrets* — and it is OFFENSE, so it needs no
defence-admission bar at all.

⚠ **Caveat carried from the source:** deaths-per-build *within* a band is a flow
ratio, not a survival share (a turret built in one band can die in the next).
**The cumulative totals are the clean comparison.**

| ~~3~~ | ~~**Arrive without traversing**~~ | **WITHDRAWN 2026-08-11 — its premise is refuted.** See below. |
| 3 | **CLEAR MORE ENEMY TURRETS** *(was "we go forward late" — refuted; then over-sized — corrected)* | **GREP: PARTIAL PASS — we ALREADY destroy 3.01 enemy turrets/side-game and our turret priority already ranks SENTINEL 1 / GUNNER 2.** **This is therefore MORE of an existing behaviour, not a new one — likely a targeting/priority change, and it may fall under the KNOB BUDGET.** **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: home-turret build order still tries SENTINEL before GUNNER — `choices = (EntityType.SENTINEL, ct.get_sentinel_cost()), (EntityType.GUNNER, ct.get_gunner_cost())` at `main.py:556-558`, iterated in that order at `main.py:560`. Premise HOLDS.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | enemy turret deaths per side-game; standing enemy turrets per forward builder-round (term `A`) | archive-measurable now | **SIZED HONESTLY AFTER THE SIDE LANE'S SIGN OBJECTION, WHICH WAS CORRECT.** ⛔ **Destroying a turret PROVOKES a replacement: P(enemy turret build within 25 rnds) is 65.0% after we destroy one vs a 32.9% matched baseline — +32.1pp, and TOP sees the same (+29.5pp).** ⇒ **each destruction nets only −0.68 standing turrets, not −1.** ⇒ **My sentence "the late build divergence is downstream of failing to clear" HAD THE SIGN WRONG and is withdrawn — clearing MORE causes MORE of their builds, so under-clearing cannot explain their late build excess. That excess is UNEXPLAINED again.** **What survives: at TOP's per-build clearing rate (53.0% vs our 39.2%) we would make +1.06 extra destructions ⇒ −0.72 net standing, taking our standing stock 4.67 → 3.95 and the ratio to TOP 1.54× → 1.30× — closing 39% of the standing-stock gap and 15% of our own stock.** **Real, on-programme, but roughly a THIRD of what the earlier row implied.** ⚠ deaths-per-build within a band is a flow ratio, not a survival share. **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.** **⭐ BISONS-v8 DEMAND EVIDENCE s39 (BOOK-bisons-v8-2026-08-14.md): the field's winning counter to the ladder's fastest climber IS this row — defenders who remove their forward nest win (66% removal in their losses, field) while we remove 15-17% in wins and losses alike (20 decoded h2h games). Their nest: 4.6 fwd sentinels/game at d²4-10, arriving r30-34, opponent-blind script until r30. Out-racing lost its math (their median kill r71 vs our 133-176).** ⭐⭐ **MECHANISM DECODED s39 (damage-fingerprint ledgers, 12 dead turrets, 0 unresolved events; retraction rider: an earlier in-session claim that our first-contact WIN featured removal was INVERTED — the removal game was a fjordgate LOSS; the win was a pure race): REMOVAL THAT WORKS IS SENTINEL FIRE — 74.7% of removal damage, kills in median 2-4 rounds by a SINGLE sentinel, zero gunner-fired kills observed anywhere; builder pecks are 75% of events but 25% of damage (19-156 rounds/kill — too slow vs a r71 killer). In our fjordgate game the removers were two HOME sentinels whose envelope covered the plant zone — SHIPPED machinery; the gap is COVERAGE+PRIORITY over the nest ring (d²≤10 around our core) vs the inward follow-up plant (their 2 later gunners at d²5-8 went unengaged and killed us). **⚠ corrected s39: SENTINELS CANNOT RE-AIM — \`rotate()\` is gunner-only, facing fixed at build — so the follow-up-wave answer resolves to the builder's ladder: (a) target priority within the existing envelope [arm 1, building], (b) build-time facing/siting for ring coverage [#41], (c) reactive second-sentinel placement only if a+b fall short.** ⇒ the arm is a targeting/coverage knob, not new capability. Composes with #30 (station scorer never scores our sentinel as an anti-turret shooter) and #41 (siting).** **⭐ 0033 REPLICATION s39 (BOOK-0033-2026-08-14.md): the removal discriminator holds on a SECOND top shape — 51% fwd-turret removal in 0033's field losses vs our 14% — BUT their shape is GUNNER CREEP (d²6-16, r50-150, behind a 4.3-barrier screen in our spawn ring) not sentinel mass, so the arm's priority set must include short-range gunners in our ring AND the barrier screen (we remove 8-14% of it), not only the r²=32 sentinel band. One arm, three shapes, if and only if the target class is wide enough.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — main.py:556-560 carries unchanged (0 diff); SENTINEL-before-GUNNER build order re-verified byte-identical. |
**⛔ #3 REPLACED. THE ORIGINAL PREMISE ("the traverse is what kills us") IS
REFUTED BY RESEARCH'S OWN ATTRIBUTION CUT (13,771 replays, 0 parse failures,
anchor reproduced: US 2.90 vs 2.92, TOP 0.87 vs 0.84).**

**AND THE "UNEXPLAINED ~2.3x" THAT MOTIVATED THE WHOLE ROAD NEVER EXISTED — it
was an arithmetic error (terms MULTIPLY, they were treated as ADDING), corrected
by its own author.** The gap decomposes exactly:

| term | factor | log-share |
|---|---:|---:|
| **A. enemy turrets EXIST near us** (per fwd builder-round) | **1.77x** | **47.3%** |
| B. share of them IN RANGE of our tile | 1.57x | 37.0% |
| C. damage per turret-in-range-round | 1.05x | 4.3% |
| D. damage absorbed per death | 1.15x | 11.4% |
| **product** | **3.36x** | = the anchor |

**B = 1.57x independently reproduces `FORWARD-HAZARD`'s ray-based 1.53x on a
different instrument.** So *"exposure explains 1.53x, leaving 2.3x unaccounted"*
was never a residual.

**⛔ AND TRANSIT-vs-STATION KILLS THE LAUNCHER-DELIVERY CASE SPECIFICALLY:**
our forward builders die **2-9 rounds AFTER they stop moving** — 40.6% of forward
deaths in 11.5% of forward rounds, at **4x mean hazard**. **MOVING IS BELOW-AVERAGE
HAZARD (0.90x). Parked 30+ rounds is the SAFEST state in the game (0.19x).**
⇒ **Launcher delivery removes the traverse — the LEAST hazardous state, 44.6% of
deaths — and drops the bot straight into the PEAK-hazard window.** Ceiling ~45%
of forward deaths with **no hazard-per-round discount**. The saving is purely
"fewer forward rounds per build", **the same lever LOKI-25 pulled.**

**⭐ THE GENERATIVE FINDING, AND IT IS `A`:** opponent-matched, we see **3.94
enemy turrets alive per forward round vs TOP's 2.19**, and the opponent-matched
gap is **2.96x** — opponent selection is only ~10% of it. The alive-ratio runs
**1.14x at r0-59 -> 2.25x at r500-999**, and **30.5% of our forward rounds sit in
r500-999 against TOP's 16.8%.** **We are forward LATE, into a turret field that
has matured.**

**TWO STORIES KILLED OUTRIGHT, so nobody re-queues them:** *"we fail to disengage
or heal"* is **dead** (moved-after-first-damage 76.74% vs 76.81%; ever-healed
14.34% vs 13.96%). And **we do not crash** — no-damage removals 0.00% US vs 1.02%
TOP. **Our movement-state mix is FAVOURABLE**; standardised to TOP's mix our rate
RISES 2.90 -> 4.11 and the gap WIDENS to 4.74x.

⚠ **Carried caveat, flagged by the agent against itself:** *"moved last round"* is
partly a RESPONSE to danger, so the transit/station boundary is blurred in both
directions. **The bucket shape is robust; the causal reading of it is not.**

## ⭐ UNBLOCKED — BUILD THESE WITHOUT WAITING FOR RESEARCH

| **33** | ⭐⭐ **DOES `LOKI_GUNAXIS_PENALTY` DO ANYTHING? — ablate the flag we SHIPPED in v114 and have never tested** | **GREP: PASS.** `LOKI_GUNAXIS_PENALTY = 8` (`doctrine.py`), consumed at `raid.py:508-527` where `gun_axis` is built from `get_attackable_tiles_from` and used ONLY to push raider stations off an enemy gunner's ray. Present in all three shipped trees (`_v146gunaxis` v114, `_x3r0_v115`, `_v169launchlate160` v116). **Never ablated.** **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: flag PRESENT AND CONSUMED — `doctrine.py:1515` `LOKI_GUNAXIS_PENALTY = 8`, applied `raid.py:816`. Ablation question stands.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **gunner-covered forward builder deaths per game** (shipped baseline **0.60/game**; sentinel-only **0.32/game** as the negative control that must NOT move) | local both-arms battery, `LOKI_GUNAXIS_PENALTY` 8 vs 0, zero rated exposure | ⭐ **CHEAPEST DECISION-UNBLOCKER ON THE BOARD, and it gates two other rows.** The flag shipped in v114 on the strength of *"92% of our forward builder deaths are enemy gunners"* — **a figure that is real for the era it was written in (all-time cut reproduces 92.0%) and STALE for the tree we ship (any-gunner 64.8%, any-sentinel 68.4%).** ⛔ **The archive CANNOT attribute the flag and the adjacent-version contrast runs BACKWARDS: v112 (pre) 0.43 gunner-covered forward deaths/game and 60.0% share vs v114 (first with it) 0.59 and 66.9%** — and v114 differs from v112 by more than this flag, so the comparison is confounded either way. ⇒ **one local ablation answers what 5,000 archived games cannot**, and it decides whether `#30` and `#31a` are addressing a population the incumbent already banks. **Resolution declared: at a 0.60/game baseline a real avoidance effect should move gunner-covered deaths by ≥0.15/game; anything smaller is inside the noise of a battery we would pay for.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `LOKI_GUNAXIS_PENALTY=8` confirmed byte-identical; true current location doctrine.py:1533 (row's inline :1515 is the same pre-shift-number staleness as #2/#22-#24, value unchanged). raid.py:508-527,816 carry unchanged (0 diff). Ablation question stands, never run. |

| **34** | ⭐⭐ **BACKFILL `wincond` ONTO A FULL-COVERAGE SURFACE — EVERY UNRATED LEG WE HAVE EVER READ IS BLIND TO HOW IT WON** | **GREP: PASS.** `wincond` **already exists and is fully computed**  **GREP-PATH: corpus/throws.tsv** *(s46: this row has NO BOT TREE BY DESIGN — its subject is an instrument. Path verified present on disk 2026-08-16. Checked: the surface that HAS `wincond` (col 21, 445,775 rows, zero blanks) and covers only 37.2% of files — the row is about moving it, not computing it.)*— `corpus/throws.tsv` col 21, **445,775 rows, ZERO blanks**, real domain (`core_destroyed` 50.6% · `titanium_collected` 49.1% · `titanium_stored` 0.2% · `harvesters` 0.1% · `coinflip` 0.0%). ⛔ **But `throws.tsv` covers 11,675 distinct files against `events`/`econ`'s 31,347 — 37.2%** — because it only has rows for games containing a **launcher throw**. `builds.tsv` carries `winner` (col 11, 30,567 files) but **no `wincond`**; `events.tsv` carries **neither**. **RE-CHECKED s36 2026-08-13: not a bot-tree claim; incumbent `_v187saltidle_f` unaffected. `corpus/throws.tsv` col 21 `wincond` still present, now 454,459 rows / 0 blanks / 11,967 distinct files vs `events.tsv`'s 32,455 (36.9% coverage, corpus grown since but same shape); `builds.tsv` col 11 `winner` present (now 31,740 files) with no `wincond` column; `events.tsv` has neither. Premise HOLDS.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | unrated games with a readable win condition: **37.2% → ~97%** | none needed — the decoder already emits the field; this is a column, not an extraction | ⛔ **`R1000_IS_DEFEAT` IS THE PROGRAMME'S CENTRAL CURRENCY AND WE CANNOT READ IT ON AN UNRATED LEG.** `wincond` is literally how a core kill is told from a tiebreak win, and **the 37% we can read is a THROW-SELECTED subpopulation — not random, and weighted toward us, since `#24` records we are the launcher singularity of the top table.** Cheapest decision-unblocker on the board after `#33`. **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — concerns `corpus/throws.tsv`/`events.tsv`/`builds.tsv` coverage, not the bots/ tree; unaffected by the incumbent move by construction. |
| **35** | **PER-GAME MAP NAME FOR UNRATED GAMES — the SALT leg was decided on a comparison nobody could see was map-confounded** | **GREP: PASS.** `ladder_games.tsv` is the **only** surface with a r **GREP-PATH: tools/corpus/ladder_meta.py** *(s46: this row has NO BOT TREE BY DESIGN — its subject is an instrument. Path verified present on disk 2026-08-16. Checked: the ingest that makes `corpus/ladder_games.tsv` the only map-bearing surface and rated-only BY CONSTRUCTION.)*eal `map` column and is **rated-only by construction** (`tools/corpus/ladder_meta.py` walks `fcode match list --mine --type ladder`). `meta_join.tsv`'s 24 columns contain no map. `events`/`builds`/`econ`/`flow`/`throws` carry only `mw`/`mh`. **RE-CHECKED s36 2026-08-13: not a bot-tree claim; incumbent `_v187saltidle_f` unaffected. `corpus/ladder_games.tsv` still has a `map` column (col 8); `corpus/meta_join.tsv` still 24 columns, no `map`, only `mw`/`mh` (cols 10-11); `events.tsv`/`builds.tsv` confirmed `mw`/`mh` only, no `map`. Premise HOLDS.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | matched-cell yield on an unrated A/B leg (SALT: **9 of 37** cells matched on `(opponent, mw×mh)`) | validated proxy exists — `mw`×`mh` resolves **15 maps → 11 distinct pairs**, 7 unique, **4 pairs ambiguous between exactly 2 maps** (26×26 snowflake/archipelago · 25×25 hive/drumlin · 16×16 jackpot/lighthouse · 28×20 eider/heart) | ⭐ **THIS GAP PRODUCED A PUBLISHED WRONG n — in the LIVE 25-GAME LEG (`LOKI-50`, 2026-08-12T18:3xZ), NOT in the local battery.** ⛔ **DISAMBIGUATED s35 AFTER THE BUILDER ASKED WHICH: `scratchpad/overnight/SALT.tsv` carries `map` NATIVELY (col 4) and is map-BALANCED — independently recounted off the raw rows: 5,408 data rows, 8 maps × exactly 676 each, seats 2,704/2,704, T 3,299 = 61.00%, cond 5,052 `core_destroyed` + 356 `tiebreak`. THIS GAP DOES NOT REACH IT AND THE v122 SHIP IS UNAFFECTED.** The defect is in the LIVE leg: **v116/v117/v118 shared ONE identical 25-slot map draw while v119 played a different map in 25 of 25 slots**, so its "pooled n=33 kills" was **25 map-slots replicated three times** and z=+2.63 fell to **+1.99** when clustered. ⚠ **AND THE DIRECTION IS NOT SIMPLY "WEAKENS THE CASE AGAINST SALT":** the n inflation weakens it, but the **map DRAW ran in SALT's favour** (its mix predicts 155.6 rounds vs baseline 163.1), so standardising **strengthens** the effect to z=+3.55. The conditional finding survives every powered correction; what rescues SALT is the **choice of statistic** (unconditional log-rank z=−0.22, p=0.83), which is independent of this gap. **Real extraction work, unlike `#34`** — needs either `fcode match info` on unrated match ids or a wall-layout fingerprint in `tools/corpus/replay_events.py`. **Neither exists today.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — concerns `corpus/ladder_games.tsv`/`meta_join.tsv` map columns, not the bots/ tree; unaffected by the incumbent move by construction. |

**⭐ `#34` AND `#35` STOCKED s35 (research) — AND DELIBERATELY SPLIT INTO TWO ROWS.**
They arrived as one claim of mine (*"no decoded surface carries map name or win
condition for unrated games"*). **The side lane checked it before it became a
queue item and it SPLIT: the map half holds in full, the win-condition half was
WRONG — `wincond` exists and is fully populated.** ⇒ **As a single row this would
have been priced at the map half's cost and the cheap half would have ridden
along unbuilt.** `#34` is a one-column backfill that reaches the programme's
central currency; `#35` is genuine extraction work.
**⚠ One correction to the routed diagnosis, verified off the headers:** the fix
spec named *"`builds.tsv` and `events.tsv` already carry a `winner` column"* —
**`events.tsv` does not** (its 11 columns are `file, ev, rnd, team, kind, x, y,
d2_own, d2_enemy, mw, mh`). **`builds.tsv` is the cheap target**; `events.tsv`
needs `winner` AND `wincond`, i.e. two columns, though it has the best coverage
(31,347 files).

## ⛔⛔ THE `GREP:` GATE VALIDATES THAT A CHECK **RAN**, NOT THAT ITS RESULT IS **STILL TRUE** *(research s35, on the side lane's finding — and `#18` is the proof)*

**`#18`'s grep was HONEST WHEN WRITTEN. The fix landed afterwards, and the row
passed the gate every single time from then on.** A row whose premise was true at
admission and false today **passes forever**. `#18` was caught by a one-off sweep
somebody happened to commission — **1 FAIL out of 19 rows. That is a sweep, not a
gate.** And it was caught only after the builder's repaired `cores_idle` picker
started reading the fire-order block and **handed out the one item we knew was
done as its very first output.**

**⭐ THE TRIGGER IS ALREADY IMPLIED BY `PROGRAMME.md` AND NOBODY IS USING IT.**
The gate is denominated in *"what was checked in the **INCUMBENT**"* ⇒ **when the
incumbent moves, every `GREP:` in this file is potentially stale BY DEFINITION.**
**THE INCUMBENT MOVED TODAY: v116 → v122 at 04:45:54Z.**

**⇒ CONVENTION ADOPTED, EFFECTIVE NOW: every `GREP:` NAMES THE TREE IT WAS
CHECKED AGAINST.** `#17` and `#5` already do (`_v148ferryfirst`), so the field
half-exists and is simply never read. New rows carry it; existing rows acquire it
on next touch.

**⇒ SPEC ROUTED TO THE BUILDER (tooling, not mine to write):** have
`queue_check.py` parse the tree named in each `GREP:` and compare it to
`PROGRAMME.md`'s `INCUMBENT:` field, printing
`GREP STALE — checked against <old>, incumbent is <new>`. **It need not re-run
anything — surfacing the mismatch is the whole gap.** A row with no named tree
should print `GREP TREE UNNAMED` rather than pass silently.

⚠ **SCOPE, stated so this is not over-read: NO CLAIM IS MADE THAT OTHER ROWS ARE
STALE.** 18 of 19 held against `_v169launchlate160` when swept at ~04:5xZ. **The
claim is that NOTHING WOULD TELL US IF THEY BECAME STALE** — and today's
incumbent move is exactly the event that should have forced a re-check.
⚠ **AND THE SWEEP THAT FOUND `#18` IS ITSELF ALREADY STALE BY ITS OWN RULE:** it
ran against `_v169launchlate160`, which **stopped being the incumbent while it was
being published.** ⛔ **`PROGRAMME.md:8` still reads `INCUMBENT: bots/_v169launchlate160`
against a live `Active bot: v122` — that field needs updating by whoever owns the
ship, and until it does, the spec above has nothing correct to compare against.**

**Stocked by research s31 after auditing this file and finding ZERO buildable
items: #1 shipped, #2 blocked on a number I owe, #3 gated on #4, and #4 is a
research cut rather than a plank. Every blocker was mine.** These three depend on
**nothing I am running.**

| # | plank | change | mechanism metric | fixture — can it resolve? | why now |
|---|---|---|---|---|---|
| **17** | ⭐⭐ **DOES THE CRASH WEAPON ACTUALLY FIRE? — a LOCAL both-ways drive, no submission, no rated cost** *(Magnus asked for it directly, s32)* | **GREP: PASS** — `bots/_v148ferryfirst/raid.py` has **zero** `border`/`edge` references; the incumbent ships INSERT and EXILE only. **The weapon is already BUILT and unshipped: `bots/_v131loki14/raid.py:618` carries a two-arm tag, `"B" = border arm (the crash bid)`, `"I" = interior`, behind `LOKI14_KIDNAP_ON`.** **Nothing to write — this is a RUN, not a build.** **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: launcher code PRESENT — EXILE (enemy pickup d²≤2, throw to site farthest from our core, `raid.py:900-932`) + FERRY (`raid.py:934-957`). No explicit border-tile targeting in the tree; whether the exile throw lands crash-inducing positions is exactly this row's LOCAL dose check. Question stands.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **enemy builder-bot removals with NO preceding damage event**, per `tools/kidnap_fate.py` (already built, selftested both ways). Locally also the **engine's own traceback on stderr** — see the fixture cell | ⭐ **LOCAL IS THE RIGHT INSTRUMENT HERE, WHICH IS UNUSUAL AND WORTH SAYING WHY.** The standing rule is *prototypes go at live teams, not our own probes*, because probes lie about EFFICACY. **This leg does not measure efficacy — it measures whether the WEAPON FIRES, and for that we own a purpose-built vulnerable target AND a purpose-built immune one:** `bots/_probe_oov_raw` (*"UNGUARDED out-of-vision query… if the raise escapes `run()` the unit should stop appearing forever"*) and `bots/_probe_oov_guard` (*"same far-tile query, wrapped. Control arm."*). **⭐ AND `print()`/tracebacks ARE VISIBLE LOCALLY while the platform strips stdout in 30,664 of 30,664 `BotOutput` events — so a local run can SEE the victim's traceback, which no live leg can ever do.** **THREE CELLS, and it is the drive-both-ways that makes it a test:** (a) `_v131loki14` border arm **ON** vs `_probe_oov_raw` ⇒ **MUST produce crashes**; (b) same vs `_probe_oov_guard` ⇒ **MUST produce zero**; (c) border arm **OFF** vs `_probe_oov_raw` ⇒ **MUST produce zero**, isolating the throw from the query. **If (a) is silent the weapon does not work and the whole road closes for the price of a local batch.** | **BECAUSE THE ARCHIVE JUST CLOSED THE OTHER CHANNEL AND THIS IS THE ONLY ONE LEFT.** `tools/kidnap_fate.py` over all 5,398 of our games (43,211 enemy builder lives, 9,372 thrown) reads **THROWN 0.134% vs matched CONTROL 0.214%, Δ −0.080pp, CI [−0.257, +0.097]** ⇒ **the DISPLACEMENT channel is dead**, and **all 9,372 archived throws are `EXILE`, so the border-throw mechanism has never been tested at all** — 314 throws in LOKI-14 and no rating read. ⛔ **RANK IT ABOVE the live crash leg and BELOW #16/#8: it is cheap, it needs no submission and no rated exposure, and it can KILL the road outright — which is worth more than a live leg that could only weakly confirm it.** ⚠ **This CANNOT tell us what share of the real field is vulnerable — we patched this bug in `eco.py` and most teams have not, but "most" is unmeasured. That question needs the live leg, and only if (a) fires.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — raid.py facts (EXILE/FERRY shipped, no border-tile selector) carry unchanged (0 diff); unaffected by the v140 diff (SEAL_TI_FLOOR value change + new eco.py L4-repair block, both outside raid.py). Question stands. **⭐ RE-READ s47 2026-08-16 vs the LIVE CONTROL `_v223sealrepair` (research, on the side lane's `GREP:`-scope audit; every anchor below re-verified here by hand, `ZZQXNOTATOKEN` control = 0/0/0/0 across all four files):** ⚠ **POINTER STALE, CLAIM INTACT — a STALENESS failure, which a scope audit does not see by design.** The cell's grep names `bots/_v148ferryfirst`, **three incumbents back**. Re-run against the live control: the raid.py facts (EXILE/FERRY shipped, no border-tile selector) carry unchanged. ✅ **Question stands exactly as written; only the anchor was out of date.** ⇒ **pointer updated to `_v223sealrepair`.** |
| **16** | ⭐⭐ **THE NON-STRIKE SURCHARGE — make the defence bar a RUNTIME GATE instead of a post-hoc bar** *(sweep 24; stocked by research s32)* | **GREP: PARTIAL PASS, AND THE GREP RESHAPED THE PLANK RATHER THAN KILLING IT.** ⛔ **The sweep's proposed hook is NOT BUILDABLE AS WRITTEN: it asks for a new store slot `SLOT_STRIKE_ALIVE` and THE STORE IS FULL** — all 16 slots bound (`doctrine.py:931-961`, remapped `:1166-1170`); **FREE SLOTS: NONE**, verified by enumerating every `SLOT_* = <int>` and taking last-write-wins. **But both halves it needs ALREADY EXIST separately and have never been connected:** (i) **the surcharge SHAPE is shipped** — `eco.py:225 _eco_spendable()` already returns `ti >= cost + SIEGE_HEAL_RESERVE_TI` when `SLOT_UNDER != 0` and `round >= HUNT_MIN_RND(120)`, i.e. a conditional reserve on top of cost, which is arithmetically a surcharge; (ii) **the strike-alive SIGNAL is shipped** — `SLOT_RAID_LIVE` (slot 15) is a **heartbeat**, written `rnd + 1` by each reporting raider (`raid.py:142,155`), so `read_store(SLOT_RAID_LIVE) >= rnd` **is** "a raider reported last round". ⇒ **CHANGE: add one condition to `_eco_spendable` — while `rnd < 250` AND the `SLOT_RAID_LIVE` heartbeat is fresh, require `ti >= cost + NON_STRIKE_SURCHARGE`.** **ONE new constant. ZERO new slots. ZERO new units. ZERO scale delta.** `_eco_spendable` is already the single chokepoint for non-strike spend — 4 call sites, all eco: conveyor (`eco.py:543,774`), harvester (`:944`), launcher (`main.py:608`). **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: store slots `doctrine.py:931-961`/`:1166-1170` reproduce exactly, all 16 (`SLOT_ROLE_N`..`SLOT_SIEGE`=0-15) bound, no free slot. `_eco_spendable` still `eco.py:225`, conditional `SIEGE_HEAL_RESERVE_TI` reserve unchanged. `SLOT_RAID_LIVE` heartbeat confirmed at `raid.py:112,142,155` and `doctrine.py:1170`. Call sites reproduce for conveyor (`eco.py:543,774`) and harvester (`:944`); launcher site shifted to `main.py:641` (was `:608`). Premise HOLDS.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **median kill round** (the bar itself) + **game share**; mechanism channel = **eco Ti spent before r250 while a raider is live**, which only this change can move | **unrated legs vs live teams.** ⚠ **NOT our own probes: sweep 24's arm B is exactly why** — see the "why now" cell | ⭐ **THE ONLY MECHANISM IN 166,155 WORDS OF FIELD LITERATURE THAT MAKES DEFENCE SELF-LIMITING BY ARITHMETIC RATHER THAN BY A REVIEWER CHECKING A METRIC AFTERWARDS.** Source `confused`, BC2020, verified piecewise 1 hit/fragment: *"we implemented a rush cost: if it's still before round 250 and the rush isn't over (our rush miner signals that the rush is over when our design school is destroyed) then everything non-rush gets an additional rush cost of 250"*. **Note the source terminates on an EVENT, not a round number** — round 250 is only an outer bound — and `SLOT_RAID_LIVE` is already exactly that event signal. **AND IT IS THE STRUCTURAL ANSWER TO A HARD NEGATIVE: nobody in this field has EVER measured that a defensive addition did not slow their attack** (27 duration phrasings → **0 hits** in 166,155 words, against `defense` 236 in 31 files; re-verified independently by research, not relayed). ⇒ **`DEFENCE_ADMISSION_BAR: kill_round_non_regression` has no precedent to copy — a runtime gate is how you stop needing the measurement you cannot borrow.** ⚠ **The tactic file's `WHAT WOULD KILL IT` was written under `PLAY_DEFENCE: never` and must be re-read before building — that caveat is now the design.** ⚠ **Pick `NON_STRIKE_SURCHARGE` BEFORE the leg and pre-register it**; the source's 250 is on their cost scale, not ours, and a knob chosen after seeing the kill round is not a gate. **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — all 16 store slots (doctrine.py:931-961, remap block ending at :1188) confirmed byte-identical; still ZERO free slots. `_eco_spendable` now eco.py:228 (was :225, +3 shift), body byte-identical; call sites now eco.py:546/937 (were :543/774, +3/+163 shifts), unchanged. main.py:641 unchanged (0 diff). `SLOT_RAID_LIVE` heartbeat confirmed at its true location doctrine.py:1188 (the row's own :1170 pointer was already pre-shift-stale by 18 before this carry — same family as #2/#22-#24/#33). Premise (surcharge shape shipped, strike-alive signal shipped, never connected) fully intact. |
| **5** | ⭐ **CRASH INDUCTION AT SCALE** | **GREP: PASS** — the live tree (`_v148ferryfirst/raid.py`) does INSERT (ferry our own) and EXILE (eject an enemy builder off a station); **throwing an enemy builder to a MAP-BORDER tile to trigger their own off-map query is NOT shipped.**  **already built** — `bots/_v131loki14`: launcher kidnaps an **enemy** builder (`can_launch` has no team check, no vision guard) and throws it to a legal **map-border** tile, where their own code queries an off-map neighbour, raises, and **the engine permanently destroys that unit for the match** **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: same launcher facts as #17 — EXILE + FERRY shipped, no border-targeting selector. Row's own STATUS markers unchanged.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **enemy builder-bot removals with NO preceding damage event** — read ENGINE-SIDE off the wire via `tools/crash_census.py`. **NOT our own throw count**, which measures what we did rather than what landed | **live unrated. YES** — the dose is already demonstrated at **314 kidnaps** in the LOKI-14 leg, and the outcome is an entity-removal event, not a log line | **Highest ceiling on the board, APPROVED CLASS (Magnus asked the organisers), already built, and never scored on the currency.** Field baseline: **2,451 unexplained unit removals by opponents across 1,855 games, against 0 by us** — most teams do not guard what we patched in `eco.py`. **⛔ Its only prior failure was an INSTRUMENT failure, not a negative: the leg planned to read its arm tag from `print()`, and the platform strips stdout (0 of 30,664 `BotOutput` events). That read-out no longer has to be attempted that way.** **⛔⛔ AMENDED s32 2026-08-11T19:3xZ (research) — THIS ROW AND `docs/research/CRASH-INDUCTION-league-wide-2026-08-11.md` CITE THE SAME 314 KIDNAPS AS EVIDENCE FOR OPPOSITE CONCLUSIONS, AND THE QUEUE NEVER SAW THE DOC (it has ZERO inbound references anywhere in the repo).** The row says the dose landed, so only the instrument failed. **The research doc says the dose landed AND STILL DID NOT MOVE RATING** — §4 verbatim: *"Do not spend a window on crash induction as a CURRENCY plank. Even perfect induction buys the condition two 1980-rated teams already live in. **LOKI-14 delivered 314 kidnaps and this is why it never showed up in rating.**"* **The measurement behind it: `r(rating, crashes suffered per game) = −0.029` over n=67 teams. Two teams in the TOP SEVEN crash 2.3×/game and rate 1978–1988; two of the weakest on the board crash ZERO. Surviving your own exceptions is not what separates 1690 from 2000.** ⇒ **"HIGHEST CEILING ON THE BOARD" IS NOT SUPPORTED AND IS WITHDRAWN AS A RANKING CLAIM.** ⚠ **BUT NOT CLOSED, AND THE DOC ITSELF SAYS SO (§5):** it is an archive statistic with a **BEHAVIOURAL premise** (how opponents perform *while* crashing), so **under D12 it goes to the BOTTOM of the queue, never off it** — and **no leg has ever aimed crash induction at a live opponent and read the result.** ⇒ **Keep it queued, re-ranked BELOW #16 and #8; if it is fired, the pre-registration must predict a RATING effect and not merely a dose, because the dose is the one thing already demonstrated.** **⭐⭐ RESOLVED IN PART s32 2026-08-11T20:0xZ, FOR ZERO RATED MATCHES — `tools/kidnap_fate.py`, ALL 5,398 OF OUR ARCHIVED GAMES, 43,211 enemy builder-bot lives, 9,372 of them thrown by us.** The victim-fate column had been the sentinel `-1` in all 36,275 archived enemy throws because `replay_throws.py:134` only tracks `kind == "INSERT"` — it follows our own ferried bots and never follows the enemy bots we eject. Read now, matched on **(file, distance-to-their-core bucket, 100-round band)** so map, phase and position are held constant and only *"was it thrown"* varies: **THROWN 5/3,724 = 0.134% · CONTROL 10/4,668 = 0.214% · Δ −0.080pp, 95% CI [−0.257, +0.097]pp.** ⇒ **THROWING AN ENEMY BUILDER DOES NOT MAKE IT VANISH UNDAMAGED. We can exclude any elevation above +0.097pp — an upper bound of 1.45× the control rate — and the point estimate is NEGATIVE.** **⛔⛔ RETRACTED 2026-08-11T20:2xZ — THAT CLOSURE WAS COMPUTED ON A BIASED ESTIMATOR AND CORRECTING IT REVERSES THE SIGN. Flagged by the SIDE LANE; confirmed by me against my own result.** **IMMORTAL-TIME BIAS: a victim must be ALIVE at round R to be thrown, and "alive" includes "has not yet been removed undamaged", WHICH IS THE OUTCOME.** The thrown arm was therefore conditioned on not having crashed while controls were clocked from BIRTH and were not — **the thrown group was depleted of crash-prone bots BY CONSTRUCTION, in exactly the direction of the finding.** Compounding it, the outcome was a LIFETIME property, so a victim's whole pre-throw life sat inside the outcome window. **The old selftest could not catch this: every fixture row had identical timing in both arms, so exposure and selection were equal by construction.** **RE-RUN WITH RISK-SET MATCHING** (controls = enemy builders alive and not-yet-removed at R; outcome = removed-undamaged strictly AFTER R, same clock both arms), all 5,398 games: **THROWN 17/3,844 = 0.442% · CONTROL 13/7,341 = 0.177% · Δ +0.265pp, 95% CI [+0.034, +0.496], z = 2.25, ratio 2.50×.** ⇒ **THROWN ENEMY BUILDERS VANISH UNDAMAGED AT ~2.5× THE RATE OF MATCHED CONTROLS. The displacement channel is NOT closed — it is WEAKLY POSITIVE.** ⚠ **AND IT IS NOT CONFIRMED EITHER: 17 and 13 events, z = 2.25, an interval that nearly touches zero, and `no_damage_removal` still conflates an uncaught exception with `self_destruct()`.** **Treat as a PRIORITISING signal, not a result. The new selftest drives the bias both ways — naive reads −50.00pp on a true-zero fixture, risk-set reads +0.00pp.** **AND IT BOUNDS THE WHOLE ROAD: only 360 of 13,743 enemy-builder removals (2.62%) are no-damage AT ALL**, so even perfect induction plays for 2.6% of their builder deaths. ⛔ **SCOPE, AND IT IS THE HALF THAT SURVIVES: all 9,372 archived throws are `EXILE`. This does NOT test the BORDER-THROW crash mechanism of `bots/_v131loki14` — that channel has 314 throws, no rating read, and remains the ONLY case for a live leg.** A prereg for it must predict a rating effect, name the border-tile dose, and cannot cite the displacement evidence, which now runs the other way. **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — raid.py facts (EXILE/FERRY shipped, no border-tile targeting) carry unchanged (0 diff); unaffected by the v140 diff. |
| **6** | **BEST-FIT SENTINEL PLACEMENT — one repeat** | re-run the placement arm **with the 10 ms TLE enabled**, after cutting its per-turn cost | forward sentinel siting quality **and** `get_cpu_time_elapsed()` per turret turn | local self-play, TLE **on**. **YES at n=4,096** (MDE 3.1pp) | **The dead-list entry invites exactly this**: it won 6/6 with the CPU limit disabled and lost 5/6 with it on, at **n=12**. That is a **CPU-cost regression, not a placement refutation** — the road is open and only this implementation died. Cheap, and it does not consume a knob slot because it changes an algorithm, not a constant. |
| **7** | **ORE-BARRIER CARVE-OUT** | **GREP: PASS** — `raid.py:276` builds barriers only to SEAL A FREE SEAT on the spawn ring; **barriering an ORE tile a forward gun already covers is NOT shipped** (and the barrier machinery existing lowers the build cost).  barrier (3 Ti) an ore tile that a forward gun **already covers** **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: `raid.py:589 def _raid_peck` (line shifted from `:276`) still only seals seat-ring tiles via `seatkeys`; no site checks ore-tile status against forward-gun coverage. Premise HOLDS.** ⭐ **RE-CHECKED s46 2026-08-16 vs the CURRENT incumbent `bots/_v223sealrepair` — the GREP was TWO incumbents stale (`_v187saltidle_f` → `_v197mapcode` → now `_v223sealrepair`). PREMISE HOLDS, and this time the COMPLEMENT WAS DRIVEN rather than the absence merely observed: `build_barrier` call sites = 3 (`raid.py:277` seat-ring seal, `:500` salt-the-corpse, `:558` an empty tile adjacent to a LIVE BELT via `_salt_beside_belt()`), **none ore-gated**; `ORE_TITANIUM` occurs **0 times in `raid.py`** and 6 times in `eco.py` only; and **NO FILE IN THE TREE CONTAINS BOTH `ORE_TITANIUM` AND `build_barrier`** — so the bot reads ore, and that reading is nowhere wired to barrier placement. ⇒ **ore-barriering is UNSHIPPED AS A MECHANISM, not merely unobserved at one call site — which is the difference between a plank and a second null.** *(Probe originated by the SIDE LANE while closing `SCREEN-beltsever`; re-derived here independently across the whole tree rather than within ±8 lines of a call site. Same verdict, wider basis.)* ⭐ **THIS ROW NOW ABSORBS THE `SCREEN-beltsever` RESIDUAL:** beltsever's clause A — cut an adjacent enemy conveyor and barrier the corpse — is **ALREADY SHIPPED** at `raid.py:424 _salt_turn` (96.5% of all our builder attacks land on a conveyor), so the screen as locked would have spent part of 10,800 games measuring the incumbent against itself. **What survived it is ore-barriering-with-no-kill, which is THIS row. No duplicate stocked; the residual has a home.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | rounds-to-first-enemy-harvester on covered ore tiles; enemy builder-turns spent clearing | live unrated. **PARTIAL** — the metric is readable, but see the caveat | Never measured, and **both primaries explicitly preserved this carve-out** while killing the parent. The price refutation that buried it was computed under the **retired** currency and is void. Clearing a 3 Ti barrier costs them **~30 Ti and ~15 builder-turns** — a tempo weapon nobody priced as one. **⚠ RANKED LAST DELIBERATELY: its channel is the enemy's economy, which is instrumental under `R1000_IS_DEFEAT`. Build it only if 5 and 6 are both in flight.** ⭐ **FIELD EVIDENCE FOR THE DEMAND SIDE, s37 2026-08-13: Magnus observed Jython running this tactic AGAINST US (barriering our harvesting spots on icefloe) — a live opponent prices the tempo weapon we never built. #49's defence-side cut will say whether it BINDS; if it does, this row's "ranked last" no longer follows from its evidence and the rank should be revisited (flag, not a re-rank — the R1000 instrumental-channel caveat still applies).** ⭐ **#49's CUT LANDED s39 2026-08-14 — the flagged condition PARTIALLY FIRED, evidence now on this row:** the tile-level bind is real and cheap (0.28x harvester-placement suppression when planted r≤60; 85.8% of barriers never cleared; ~30 Ti for THEM to undo OUR 3 Ti — the exchange runs our way on offence too, INFERENCE: transfer assumes their planner, like ours, routes around rather than clears). Economy-level bind needs EARLY+TOTAL coverage (the 21%-coverage median dose does nothing to r150 harvest/ti; the ≥50%-coverage tail is unmeasured at game level). **Prescription shape from the data: 3-5 barriers, r≤60, on their harvester-critical tiles — NOT spam (+1% scale each; a 92-barrier game observed at +92%). And we RETIRED this behaviour ourselves: ourver 76 did it in 26/40 games, v125 in 1/300 — the incumbent grep above remains accurate.** Rank unchanged pending the R1000 instrumental-channel caveat; the flag is now priced. **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — raid.py:589 `_raid_peck` carries unchanged (0 diff); still only seals seat-ring tiles via seatkeys, no ore/forward-gun-coverage check. |

| **8** | ⭐⭐ **SEAT-RELATIVE SCAN ORDER** — *found by READING the tree, not by measuring* | **GREP: PASS — the grep IS the finding** (`doctrine.py:26` fixed absolute `CARDINALS`, 20+ first-match-wins scans, `eco.py:640` fixed absolute sweep).  `doctrine.py:26` — `CARDINALS = [NORTH, EAST, SOUTH, WEST]`, a **fixed ABSOLUTE order**, consumed by **20+ first-match-wins scans** across `main.py` and `eco.py` (`:480 :551 :617`, `eco :330 :391 :433 :465 :511 :682 :812 :919 :975 :1029` …). Also `eco.py:640` sweeps `self.ang += 0.65` rad from a **fixed absolute angle**. ⇒ **"toward the enemy" is a different cardinal for each seat, so identical code takes a different branch depending on which side we spawn.** Fix: order the scan by `cardinal_direction_to(enemy_core)` — same code, seat-relative. **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: `doctrine.py:26` still `CARDINALS = [Direction.NORTH, Direction.EAST, Direction.SOUTH, Direction.WEST]`; all cited scan sites reproduce exactly — `main.py:492,563,650` and `eco.py:330,391,433,465,511,682,812,919,975,1029` (plus more, e.g. `:588-591,703-711,743-744`) — and `eco.py:640` still `self.ang = (self.ang + 0.65) % (2 * math.pi)` from a fixed absolute angle. Premise HOLDS, line numbers unchanged.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** ⭐⭐ **GREP: run vs `_v223sealrepair` (the current incumbent) — s46 2026-08-16T09:50Z (`date -u`), run here, not relayed.** Seat canonicalisation is ABSENT from the incumbent: `SEATCANON` / `seat_flip` / `_seat_fold` = **0 hits in all four files**. The hunt's TOP-RANKED site is PRESENT and unfixed: `bots/_v223sealrepair/eco.py:868` `side = 1 if (self.idx & 1) else -1`. ⛔⭐ **AND IT IS UNFIXED IN BOTH FIX RUNGS TOO** — `bots/_v450seatspawn/eco.py:868` and `bots/_v455seatfull/eco.py:916` carry that line **verbatim**, because `docs/research/EQUIVARIANCE-SWEEP-2026-08-16.md` filed entity-id parity under *"not determinable statically"* ~19 minutes AFTER the hunt had MEASURED it 240/240. **The two documents also number their sites differently** (the sweep's "site 1" is `main.py:289`; the hunt's "#1" is `eco.py:868`), so "sweep sites 1-5+7" reads as full coverage and is not. ✅ **COMPLEMENT DRIVEN — the grep is not blind:** the same pattern DOES fire on real fixes, returning `LOKI_SEATCANON_FULL` branches at `_v455seatfull/main.py:289` and `eco.py:178, 417, 455, 809`. ⇒ the 0 on the incumbent is a measured ABSENCE, not a failed search. | **the seat gap on byte-identical arms** — currently **A 53.91% vs B 46.34%, z = 3.54** ⭐⭐ **RE-FRAMED s46 2026-08-16T09:52Z (`date -u`, research lane, read-only) — "THE GAP FALLS TO ZERO" IS NECESSARY AND NOT SUFFICIENT, BECAUSE EQUIVARIANCE IS DIRECTION-FREE.** A seat-canonical rewrite forces the two seats onto ONE behaviour; **it does not choose WHICH**. In byte-identical self-play `A% + B% ≡ 100`, so a perfectly equivariant tree reads exactly 50/50 **by construction** whether it standardised on the strong seat's behaviour or the weak one. ⇒ **this row's VALUE line (+1.48pp) silently assumes the fix LIFTS the weak seat to the strong one, which equivariance does not deliver.** ⛔ **THE BOARD NOW CARRIES THE WARNING CASE.** `SEATFULL` (`_v455seatfull`, sweep sites 1-5+7, one toggle) drove the T-vs-C seat differential to **+0.09pp, 95% CI [−5.76, +5.95], n=1,119** — gap gone — while its **share against this same control reads 48.53% [45.60, 51.45]**: *the metric was met and the bot did not get better*. Contrast `SEATSPAWN` (`_v450seatspawn`, `main.py:289` alone, still running): differential **+6.70pp [+1.54, +11.87], n=1,432**, share 51.82% [49.23, 54.40]. ⚠ **DIRECTIONAL ONLY, AND SAID SO OUT LOUD: the two differentials are NOT distinguishable from each other (contrast +6.47pp, 95% CI [−1.41, +14.35], includes zero), and SEATFULL's stop was a `TREND-FLOOR@1000` OPERATIONAL CANCELLATION that kills a truly-neutral arm 73.65% of the time. Rows are KEPT. Nothing here is a verdict and no exclusion claim is licensed.** ⇒ **ADD A SECOND, DIRECTIONAL BAR: the fixed tree must BEAT the unfixed tree head-to-head, not merely read 50/50 against itself.** ⛔ **AND THE ENABLING MEASUREMENT IS NAMED IN THIS ROW BUT HAS NEVER BEEN QUEUED.** A T-vs-C seat differential is `2(a_T + a_C)` — **one number BOTH bots share** (hunt §6.2/§8.6) — so it cannot separate *"SEATFULL is seat-neutral"* from *"SEATFULL over-corrected into an equal-and-opposite bias"* without `a_C`, the incumbent's own seat term. **Verified 2026-08-16T09:51Z: `scratchpad/corefill_work.txt` contains ZERO shards with treatment == control == `_v223sealrepair`, and no `NULL*` shard on the incumbent exists on disk.** The hunt calls it *"the cheapest missing measurement in this document"* and *"free, one shard"*; this row's FIXTURE cell already flags it as the design gap. **It is a pure-throughput ask that nobody has entered.** | **the 4,096-game byte-identical null, WHICH ALREADY EXISTS AND IS POWERED.** Re-run it with seat-relative ordering: **if the gap closes, confirmed.** ⛔ **THIS RIDER SAID "pin `NOISE_ON = False` in the measured copies" AND IT WAS WRONG FOR THIS PLANK — CORRECTED s32, and it is now a POINTER rather than a copy.** The authority is `PLAN-overnight-2026-08-11.md` control 3 (*"LEAVE IT TRUE… pinning it would measure a bot we do not run"*) and `PREREG-loki29…` Amendment 5. **Two reasons, both measured rather than argued:** (1) the seat-gap CONTROL was measured with `NOISE_ON = True`, so pinning only the treatment compares across a difference that is not the treatment; (2) **pinning is not a free fix — with it False, antler returned the IDENTICAL game across 6 distinct seeds and 10 runs**, so that map's ~676 games collapse to ONE distinct game while the row count still reads 676. Engine seed-sensitivity is **map-dependent and zero on at least one battery map.** *(D14: this rider and the plan gave opposite instructions on one question with nothing to make them meet. A copy cannot be stale-checked, only contradicted — hence a pointer.)* | **A measured 7.6pp self-play seat gap, confirmed twice** (research's algebraic `s = 54.126%` off the nine-arm screen; the side lane's `53.91%` off byte-identical arms — two populations, two methods, ~0.2pp apart). **Maps account for ~1.8pp of it (third-party same-8-maps 51.76%, n.s.); the residual is OURS.** If half of every game is played at a self-inflicted disadvantage, closing it is worth **~+1–2pp ≈ +7–14 Elo from a MECHANISM, not a knob.** **⛔ NOT YET ESTABLISHED: I have shown the ORDER IS ABSOLUTE and that a seat gap EXISTS; I have NOT shown any specific site changes the outcome. The null re-run is the test, and it can come out the other way.** *(Ladder cannot settle it: our rated win rate by our seat is +1.80pp ±3.20pp on `ladder_games` — MDE 4.58pp, and detecting 2pp needs ~9,800 games/seat against the 1,885 we have. **The ladder check CANNOT resolve the hypothesis it was proposed to test.**)* **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `CARDINALS` fixed order at doctrine.py:26 confirmed byte-identical (doctrine.py<1619 is byte-for-byte unchanged v218mapfix→v223sealrepair outside the single unrelated SEAL_TI_FLOOR edit at :1228, see #52/#53). All cited eco.py scan sites reproduce at shifted line numbers (+3 for old<578, +163 for 585-962, +171 for 969+, all from the new LOKI-L4 block): eco.py 330→333, 391→394, 433→436, 465→468, 511→514, 640→803, 682→845, 812→975, 919→1082, 975→1146, 1029→1200 — every one still `for d/step in CARDINALS:` or the fixed-angle sweep, content byte-identical. main.py:492,563,650 carry unchanged (0 diff). Seat-relative scan-order premise fully intact. ⛔⭐⭐ **AMENDED s45 2026-08-16 ~10:2xZ (research, as queue owner) — THIS ROW'S DECLARED FIXTURE WAS ALREADY RUN AND IS NULL, THE EFFECT IT WAS AIMED AT IS REAL AND LARGER THAN ANYONE THOUGHT, AND THE CARRIER IS A DIFFERENT LINE.** ⛔ **THE FIXTURE RAN AND NOBODY RECORDED IT: `SR1NULL`/`SR2NULL` vs `SRNULL0`, 2026-08-11 — CARDINALS share **−3.00pp, consistent with ZERO.** ⇒ the scan-order hypothesis as stated is NOT CONFIRMED at that n and must not be re-queued in that form.** *(Wording corrected 2026-08-16 ~11:3xZ: this read "REFUTED", which is an EXCLUSION claim made from an interval described in the same sentence as "consistent with ZERO" — i.e. an interval that by construction does not exclude. The row does not record the CI, so no exclusion is supportable from it. What IS supportable: the fixture ran, the point estimate is −3.00pp, and it gives no support to the hypothesis. "Do not re-queue" survives on the point estimate and the cost; "refuted" does not.)* ✅ **BUT THE PHENOMENON IS REAL AND IT IS BIG: on BYTE-IDENTICAL self-play (9 identity shards) seat A beats seat B by **+6.28pp, z=+16.24, n=66,572**, seats balanced 33,287/33,285 (re-derived by me; the agent's 15-map cut reads +5.92pp, n=13,402).** **It FLIPS BY MAP — glacierkeep +46.35, heart +17.02, nordkap +16.79 … yulerune −0.15, atoll −12.99, archipelago −16.36, valkyrie −26.31 — and a MAP-DEPENDENT seat effect between two copies of the SAME BOT cannot be the engine or the harness. It can only be our own map-keyed logic.** Only 4% of the between-map variance is seed noise (true sd 15.9pp); within-cell seed-split χ²/df 1.15 vs between-shard 8.02. ⭐⭐ **THE SHARPEST FORM: THE FAVOURED POLE IS A COMPASS DIRECTION. North core wins 58.39% on the 5 N–S maps (n=4,468, z=+11.2); axis NS-ness vs gap r=+0.583, permutation P=0.019 — and the alphabetical-map-name control that FIRED on the `bodyaware` map-property hunt (r=+0.525 there) stays NULL here at −0.216. Same control, same 15 maps, opposite verdict.** ⭐ **THE MEASURED CARRIER IS NOT THIS ROW'S LINE: `main.py:289` sorts the spawn ring on `(x*17 + y*31 + …) % 97` — the one component measured to carry gap, **+4.84pp [+0.98, +8.70]**. A hash of ABSOLUTE COORDINATES is not rotation-equivariant, so the two seats receive different spawn orders by construction.** Two further sites, hypotheses only: **`eco.py:868` `side = 1 if (self.idx & 1) else -1`** (BFS handedness; **entity-id parity is SEAT-LOCKED, measured 240/240 on builders #1 and #2 across 120 replays ⇒ the seats run mirror-image pathfinders every game**) and **`eco.py:97 nearest_cardinal`** (non-equivariant under rot180; governs every conveyor facing). ⛔ **CAUSATION IS NOT ESTABLISHED — site #1's own directional prediction (larger gap on rot180 maps than mirror maps) came out BACKWARDS, P=0.296.** ⛔⛔ **AND THE GUARD CANNOT SEE IT: the s43 SEAT_RELATIVE selftest passes 11/11 but tests `_v151seatrel`, asserts `CARDINALS` ONLY, and asserts CANONICALISATION rather than OUTCOME. A fix must add an outcome-level assertion or the selftest will keep passing over this.** **MECHANISM METRIC: the seat gap on a byte-identical identity shard (currently +6.28pp; the bar is that it falls to zero). FIXTURE: an identity shard of the FIXED tree vs itself — note NO identity null exists for the incumbent today, which is the design gap. VALUE: +1.48pp global upper bound per the hunt, ~3.1pp on my own rougher estimate (not re-derived, and the two have not been reconciled) — so ~58 at best, NOT a route to 60 on its own.** ⭐ **WHY IT STILL RANKS: it is a BUG FIX, not a new mechanism — free, general, and it must land BEFORE the `(map,seat)`-conditional result (CV 59.19%) can be banked, because part of that lift is arm selection routing around this very defect. Banking both double-counts.** |

| **9** | **EXILE-FIRST — the deliberate inverse of what just shipped** | invert LOKI-27: launcher prioritises grabbing an **ENEMY** builder over ferrying our own | INSERT:EXILE ratio (same metric, opposite prediction) | live unrated, **or** the local null at n=4,096 | **The tactics library's strongest cross-league inversion, and we just shipped the other direction.** Sweep 12: **BC2020's Delivery Drone has our Launcher's EXACT verb signature**, and that field **converged on grabbing the enemy's unit and never on ferrying their own forward.** LOKI-27 went the opposite way on our own reasoning. **One of the two is wrong and the pair is a clean A/B on a shipped baseline** — the cheapest possible way to find out, and it costs no new mechanism. |
| **10** | **BLIND THEIR GUN WITH THEIR OWN BODY** | **GREP: PASS** — `eco.py:pave_blocked_by_ore` concerns OUR paving, not enemy fire lines; **body-blocking an enemy gunner's lane is NOT shipped.**  launcher throws a kidnapped **enemy** builder onto the tile their own gunner is aiming through **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: `eco.py:180 def pave_blocked_by_ore` present, still governs paving only (`eco.py:189-192 pave_blocked`); no gunner-lane body-block logic found in `raid.py`/`main.py`/`eco.py` (grep for "kidnap"/"blind"/"blank"/"body.*block" returns nothing). Premise HOLDS.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | enemy gunner shots-on-target per game (their turret blanked) | live unrated — **⚠ needs a gunner-line decoder; `tools/loki9_facing.py` exists but computes EXACT-RAY collinearity, a different predicate from `ALIGNED_DEG=45` tolerance. Pick one and say which.** | Sourced (`tactics/blind-their-gun-with-their-own-body.md`, `the-blockade-blanks-your-own-guns.md`). **A gunner's shot is a straight line that does NOT pierce** (unlike a sentinel), so one body in the lane blanks it — **and it costs them a unit AND their turret's output from one throw, 0 ammo.** Ranked below 5 because the decoder question is unsettled. **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `pave_blocked_by_ore`/`pave_blocked` now eco.py:183/192-195 (was :180/189-192, +3 shift from the new LOKI-L4 docstring lines), content unchanged, still paving-only. No gunner-lane body-block logic added anywhere in the 239 changed lines (SEAL_TI_FLOOR value + new L4 trunk-repair code, both unrelated to fire-lane blocking). |
| ~~11~~ | ~~**ATTRIBUTE THE 46,858 CHANGED ENGINE BYTES**~~ | **✅ DONE 2026-08-11 — CLOSED, DO NOT RE-QUEUE.** Builder completed full **address-paired attribution: 47,404 / 47,404 bytes accounted for, no residue, and ZERO game-rules functions edited.** ⇒ **The 2.3.7 engine changes touch nothing our exploits rest on**, and the guard matrix stands on instruction-level identity rather than on symbol-name identity. **Nobody need look again.** *(Queued by the side lane, cleared by the builder within the hour — the fastest item on this board.)* |

**⭐ THE STOCKING RULE — MAGNUS, 2026-08-11 (s31), DIRECT: *"please make sure
there always is new items on the list for the builder to build and test, if the
queue runs empty we go stale, that is not acceptable."***

> **FLOOR: this file must hold ≥ 3 UNBLOCKED items at ALL times.**
> **Unblocked = the builder can start it today without waiting on a research
> number.** An item gated on a running cut is **not** unblocked and must not be
> counted toward the floor.
>
> **Dropping below 3 is a RESEARCH ALARM, not a builder pause.** Mechanised:
> **`.venv/bin/python tools/queue_check.py`** counts unblocked rows and exits
> non-zero below the floor. **Research runs it at boot, after every item is
> consumed, and at wrap.**
>
> **⚠ THE FAILURE THIS CLOSES IS THE ONE THAT ALREADY HAPPENED TODAY:** this file
> was audited at 15:2xZ with **1 shipped, 2 "next up" and 1 blocked — and ZERO
> buildable**, because #2 was blocked on a number research owed, #3 was gated on
> #4, and #4 was a research cut rather than a plank. **Every blocker was research's
> own.** A queue can read full and be empty.

| **12** | **THE POST-STOP WINDOW** — ⚠ **STATUS: BLOCKED — DEMOTED THE SAME SESSION; DO NOT BUILD BEFORE THE CONTROL BELOW RUNS** | **GREP: PASS** — nothing keys on "just stopped moving". | forward builder deaths 2–9 rounds after last move, **split by whether the unit ACTED** | needs a replay pass combining movement state with verbs — `fwd_death_attrib_s31.py` + `idle_split_s31.py` have both halves | **The effect is real and it is relatively worse for us: 3–9 rounds after stopping we die at 11.63/1k forward rounds against TOP's 2.23 — a 5.2× gap where our pooled gap is 3.36×, so the window carries ~1.55× its share of our disadvantage.** **⛔ BUT THE CONFOUND IS STRUCTURAL, NOT HYPOTHETICAL, AND OUR OWN CODE FORCES IT:** the builder measured that `_raid_act` returning True **RETURNS FROM THE TURN**, so a builder that ACTS does not MOVE that round — **"just stopped moving" in our bot largely MEANS "just acted".** ⇒ **the hazard may belong to the ACT (stationary, adjacent to a target, deliberately exposed) rather than to the transition — and you cannot build without stopping, so if it is the act, NOTHING CAN REACT TO IT and this is not a plank.** **DISCRIMINATOR (the polarity move, exactly as used to kill the ring candidate): do builders that STOP AND DO NOT ACT carry the same 4× hazard?** Idle-stopped safe ⇒ #12 is dead. Idle-stopped equally deadly ⇒ #12 is strong. ⚠ shape matches TOP (their peak is 2.58× vs our 4.00× of own-group mean); **magnitude does not, which is the only reason relative value survives at all.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.** |
| **13** | ⭐⭐ **AMBUSH THE REBUILD** — after we destroy an enemy turret, their builder must come back to that tile | **GREP: PASS** — we keep no memory of a tile we destroyed and never revisit one. **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: NO post-destroy camp/ambush behaviour in the tree (`rebuild` appears only in prose comments — `raid.py:441`, `doctrine.py:422/511/984`). Not shipped; row stands.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **enemy builder deaths within d²≤2 of a tile we destroyed a turret on, within 25 rounds** | archive gives the base rate now; a leg for the effect | **⛔ CORRECTED — MY FIRST ROW QUOTED THE WRONG QUANTITY. "65.0% → about two-thirds of the time" is P(they build a turret ANYWHERE), not P(they build AT THE TILE).** Side lane caught it; those are different numbers and only the second is an ambush. **MEASURED, 15,958 of our turret-destructions:** **22.6% produce a rebuild EXACTLY on the destroyed tile within 25 rounds · 33.8% within d²≤2 · 42.5% within d²≤8.** Conditional on a rebuild happening at all: **35.2% land on the rubble, 52.6% within d²≤2.** ⇒ **the real figure is ~1 destruction in 4.4 producing a same-tile return, not two in three** — smaller, still a predicted arrival at a known tile, which this game almost never offers. *(And our opponents rebuild in place far more than the top tier's do — 22.6% vs 13.0% on-tile — so this is worth MORE to us than to them.)* **⛔ THE DESIGN FORK, AND ONLY ONE BRANCH IS CHEAP (side lane): NAME THE KILLING INSTRUMENT.** If the rebuilder is killed by **builder melee**, the ambush pays exactly the cost that just killed counterbattery — `_raid_act` returning True RETURNS FROM THE TURN, so every attack round is a spent move round (`_v150cbturret` measured **45.2%, core kills 0.82×**). If it is killed by a **turret already covering the rubble tile**, it is genuinely free. **The tile numbers above decide both questions at once: a pre-placed turret only works if the rebuild lands on or near the destroyed tile, which is the 22.6–33.8% band.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — doctrine.py:422,511,984 confirmed byte-identical; raid.py:441 carries unchanged (0 diff). No post-destroy ambush/camp logic added by the SEAL_TI_FLOOR or L4-repair changes — premise stands. |
| **14** | **THE IDLE BUILDER GETS A DESTINATION, NOT A RECALL** | **GREP: PASS** — `_idle_rotate` (`main.py:764`) is a GUNNER facing re-aim; **no builder-side idle destination or broadcast exists.** **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: `_idle_rotate` now at `main.py:797`, guarded by `if turret_type != EntityType.GUNNER: return` at `:801` — still GUNNER-only; grep for `SLOT_IDLE`/"idle.*destination"/"idle.*broadcast" across `main.py`,`eco.py`,`raid.py`,`doctrine.py` returns 0 hits. Premise HOLDS.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | share of builder-rounds in bucket A (idle and free) | archive-measurable now (`idle_split_s31.py` computes it) | **MEASURED TODAY: 25.76% of our builder-rounds in the 20 before our core dies are IDLE AND FREE — both cooldowns 0, no verb emitted, and 99.75% of those rounds carry a `BotOutput`, so the engine ran `run()` and it chose nothing.** ~1.19 builders idle at any moment, and **idle share is the SAME in wins (27.59%), so this is not a losing-position artefact — it is what our bot always does.** Sourced from tactics sweep 23: the field's fix is **broadcast a destination**, never shorten the deployment (The Kragle: *"a robot-count lead means nothing if half of your robots are derping around"*). ⚠ lower ceiling than 12 and 13 — an idle round is not necessarily a wasted one. **⭐ AMENDED s32 2026-08-11T18:1xZ (research) — THIS ITEM'S TITLE ASSERTS A CONTRAST WHOSE SECOND HALF RESTED ON A RETIRED RULE, SO IT IS NOW A TWO-ARM TEST RATHER THAN A ONE-ARM BUILD.** *"NOT a recall"* was not an independent finding: sweep 23's `every-withdrawal-trigger-in-the-field-is-a-resupply-trigger.md:46` reached *"there is no rule-level reason for our forward builder to go home, EVER"* on **four legs — three rules-facts (our builder carries no consumable: global titanium, no ammo, healed by an adjacent builder) and one PROGRAMME leg, verbatim: *"`PLAY_DEFENCE: never` removes the one remaining motive. Preserving the unit is not a reason on this programme."*** **`PROGRAMME.md:19-20` retired that field the same day** (`PLAY_DEFENCE: not_at_the_kill_s_expense` + `DEFENCE_ADMISSION_BAR: kill_round_non_regression`). ⇒ **the three rules-legs still stand and still say no RESUPPLY trigger transfers — but "preserve the unit" is a live withdrawal motive again.** **BUILD BOTH ARMS OFF THE SAME IDLE DETECTOR, since the detector is the expensive part and it is shared:** **(A) DESTINATION** — idle builder is broadcast a forward target; **(B) RECALL** — idle builder withdraws to preserve itself. **⛔ ARM B CARRIES A SECOND BAR OR IT IS NOT ADMISSIBLE: `kill_round_non_regression`. If MEDIAN KILL ROUND RISES it is off-programme whatever it does to win rate** — and recall is the archetype of the plank that buys survival with tempo, so this is the bar doing real work rather than a formality. **The discrimination is the value: A and B predict OPPOSITE signs on median kill round from the SAME 25.76% idle pool, so one run scores both.** ⚠ **Do NOT read this as evidence FOR recall** — a voided closure reason returns a road to the queue, it does not revive it (D12). Companion caveat: `three-teams-deleted-the-withdrawal-and-it-won.md:83`, whose dismissal of *"preserve the raider"* fell to the same retirement and whose own BUILDER HOOK falsifier should be re-run with the kill-round check attached. **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — main.py:797/801 (`_idle_rotate`) carries unchanged (0 diff); still GUNNER-only, no builder-side idle destination/broadcast added. |

| ~~**15**~~ | ✅ **STATUS: ANSWERED — s33 2026-08-12, CLOSED, DO NOT RE-QUEUE. `docs/research/EFFECTIVE-N-of-our-battery-history-2026-08-12.md`, instrument `tools/effective_n.py`. NO GAMES RUN; measured from rows already on disk.** **CONTROLLED CONTRAST at matched 3 seeds/cell: `NOISE_ON=True` = 2 degenerate cells in 29,296 (0.007%); `NOISE_ON=False` = 68.8–80.6% across five legs, four opponents, eight maps, both seats, both arms — so s32's D12 boundary is CLOSED.** ⛔ **BUT THE PROJECTION BELOW WAS WRONG ABOUT SCALE: realised overstatement on the legs we actually ran is ~2.2× (384 rows → 175 distinct games), not 1–2 orders of magnitude. The damage scales with `seeds_per_cell`, NOT rows — our `NOISE_ON=False` legs were protected only by being SMALL (2–3 seeds/cell). ⛔ **THIS ROW ORIGINALLY PROJECTED "effective n ≈ 180 of 5,408 (~30×)" AT 338 SEEDS/CELL AND THAT PROJECTION IS RETRACTED — it is NOT IDENTIFIED.** The estimator is censored above at the seed budget (censoring a known-good shard to k=3 "predicts" 112.7× where the truth is 1.4×), and the arithmetic mixed two populations' cell counts. **Honest interval: effective n ∈ [22, 5408]. Settled by ONE shard — the same battery config with `NOISE_ON=False` at 338 seeds/cell.** *(Retraction and instrument fix: `b6f716e`; full amendment in the doc's own AMENDED block.)* **UNAFFECTED and still checked: we escaped it tonight because all 17 shards ran `NOISE_ON=True` (verified at `doctrine.py:474`, not assumed) — that is a read fact and it holds whatever the magnitude turns out to be.**** **Nothing is re-scored: 2.2× widens intervals, it does not flip signs.** → spawned #18 and #19. *Original entry kept below for provenance.* | **GREP: PASS — the grep IS the finding.** **63 of the 256 bot trees that define `NOISE_ON` carry `NOISE_ON = False`** — the entire `_det*` family (`_det_v115dodge`, `_det_v134loki17`, `_detNLoff`, `_detHG`, `_detMAGon`…) and the `_abl_*` ablation arms, **both sides pinned**, which is exactly the configuration measured below.  Run the seed-sensitivity probe on the HISTORICAL pairs, on the maps they actually used | **distinct outcomes per (bot-pair, map) across k distinct `--seed` values.** Effective n is the count of DISTINCT games, not the row count | local, ~6 games per (pair, map) cell — **trivially cheap, and it needs no new tooling** | **MEASURED s32 on `_v148ferryfirst` vs `_v148null`, both pinned `NOISE_ON = False`, 6 distinct seeds: antler 1 distinct outcome · atoll 1 · meander 1 · hive 2. THREE OF FOUR MAPS AT ZERO SEED SENSITIVITY.** ⇒ a pinned-both-sides battery's effective n is closer to its number of **(map, bot-pair) CELLS** than to its row count: **a 1,024-game 8-map battery could carry ~8–16 distinct outcomes rather than 1,024** — a one-to-two-order-of-magnitude overstatement of precision, **invisible in every denominator we print, on batteries `gate.py` PASSED because they complied with its own prescription** (*"flip NOISE_ON to False in this COPY"*). ⛔ **NOT A CLOSURE AND THE BOUNDARY IS THE POINT (D12): four maps, ONE bot pair. Determinism is a property of (map, bots)**, so this does NOT yet establish that the `_det*`/`_abl_*` pairs were degenerate on the maps they ran — a different pair could be seed-sensitive where ours is not. **It is a pattern rather than an anomaly, and the check is one probe per historical pair.** ⚠ **And `tools/det.py` — the authority `gate.py` cites for "ALL sides must be deterministic" — ALREADY CARRIES THIS WARNING** (`det.py:135-142`: under `NOISE_ON=False` *"the seed drives nothing that is still switched on"*, prints `LOW REPLICATION`, exits 2). **The two files contradict each other and the gate cites the one that refutes it.** ⭐⭐ **AMENDED s33 — BOTH SETTINGS OF `NOISE_ON` DEFEAT SEED CONTROL, IN OPPOSITE DIRECTIONS, AND ONLY ONE HALF WAS WRITTEN DOWN.** `det.py:135-142` records that under `NOISE_ON=False` *"the seed drives nothing that is still switched on"*. **Nobody recorded the mirror: under `NOISE_ON=True` the seed drives nothing EITHER.** `_v146gunaxis/main.py:276` is `random.Random().randrange(97)` — **no argument, so it seeds from the OS. Nothing the harness passes reaches it; `--seed` has NEVER controlled our spawn ordering.** Verified: no seeded `Random` anywhere in the tree.
```
NOISE_ON=False : seed drives nothing -> games DEGENERATE   (68.8-80.6% identical cells, measured s33)
NOISE_ON=True  : RNG is UNSEEDED         -> games are UNREPRODUCIBLE
```
⇒ **The property that makes a 5,408-row battery honest is the SAME property that makes a 6-game "paired, same-seed" local contrast NOT PAIRED.** Demonstrated by the builder: the same bot, same six seeds, same maps, run twice, produced **4 gunners against 17**. **A control that varies fourfold against itself cannot reject anything** — which is why a local dose check exists to prove an arm DOES SOMETHING, never to reject it. **Large batteries: use `NOISE_ON=True` and trust the n. Small paired contrasts: they are not paired, whatever the seed column says.** *(Side lane joined these; the two halves had been sitting in separate documents all day.)* |
| **18** | ~~⭐⭐ **`arena.py` MUST PERSIST PER-GAME ROWS**~~ | **STATUS: SHIPPED — PREMISE NO LONGER TRUE, closed by research s35 2026-08-13T05:0xZ.** `tools/arena.py:264` calls `write_rows(results, args)`; the writer is defined at `:118` and has a `--no-rows` selftest at `:181`. **Landed in `b4f56fa`, `2026-08-12 06:46:53 +0200`** — *"arena.py: persist per-game rows BY DEFAULT"* — **which PREDATES this row's own text.** ⛔ **The row's grep cites `main()` at `:94-186`; `main()` now runs to `:264`, so the line numbers it was verified against no longer exist.** Caught by a one-off sweep, NOT by the gate — see the staleness note under this table. **Original grep, kept for the record:** `tools/arena.py:47-88` **builds a per-game result dict** (winner/seat/map/seed/bot_a/bot_b/condition/crashes/collected — as rich as `NULL114.tsv`) and **`main()` (:94-186) never calls `open()`/`json.dump()`/any writer**; it prints aggregates and discards `results`. Corroborated independently by `docs/legs/LEG-loki17-battery-2026-08-10.md:55`: *"`tools/arena.py` does not retain replays — it reports win rates and discards the games."* `mech_battery.py:181-183` is the ONLY runner that persists rows. | **whether a completed battery leaves per-game rows on disk** — binary, checked by running one small battery and looking for the file | none needed; it is a writer, not an experiment. Verify by running the existing `--selftest`-scale battery and diffing the output dir | ⛔ **Because we are STILL GENERATING UNAUDITABLE LEGS RIGHT NOW.** #15 could only be answered for the `mech_battery` subset; **the `_abl_c0..c4` legs have NO per-game rows anywhere and never did** (`git log --all --diff-filter=D` shows none was committed and deleted), so their effective n is **permanently unknowable**. Every hour `arena.py` runs without this, more history becomes unauditable. **Cheapest item on this queue by a wide margin.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.** |
| **19** | **A BATTERY WHOSE n IS QUOTED MUST NOT PIN `NOISE_ON = False` ON BOTH SIDES** | **GREP: PASS.** `gate.py` prescribes *"flip `NOISE_ON` to False in  **GREP-PATH: tools/gate.py** *(s46: this row has NO BOT TREE BY DESIGN — its subject is an instrument. Path verified present on disk 2026-08-16. Checked: the prescription that pins `NOISE_ON = False` on both sides (`:107`); `det.py:135-142` is the cited authority it contradicts.)*this COPY"*; **63 of 256 bot trees carry `NOISE_ON = False`** as a result. Its own cited authority `det.py:135-142` prints `LOW REPLICATION` and exits 2 under exactly that configuration. **RE-CHECKED s36 2026-08-13: not a bot-tree claim; incumbent `_v187saltidle_f` unaffected. `tools/gate.py:107` still reads "NOISE_ON = True -- flip it to False in this COPY"; `tools/det.py:156` still prints `** LOW REPLICATION: seeds are collapsing...` and `:216` still `sys.exit(2)`. Premise HOLDS.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **fully-degenerate cells per battery, via `tools/effective_n.py`** — the gate should print **effective n**, not row count | the instrument exists and is selftested both ways; the fixture is any battery's own output rows | **MEASURED s33: 68.8–80.6% of cells degenerate under `NOISE_ON=False` vs 0.007% under `NOISE_ON=True` (29,296 control cells).** Determinism belongs in the REPRODUCIBILITY check (`det.py`'s job), not in the MEASUREMENT run. ⚠ **Lower priority than #18: this only bites batteries that pin it, and tonight's 17 shards did not — but the prescription is still live in the gate, so the next person to follow it inherits an overstatement of UNKNOWN SIZE at 338 seeds/cell.** ⛔ **THIS ROW ORIGINALLY SAID "~30×" AND THAT NUMBER IS RETRACTED — it was my projection from k=3 data and it is not identified (the estimator is censored above at the seed budget: censoring a known-good shard to k=3 "predicts" 112.7× where the truth is 1.4×). The honest interval is effective n ∈ [22, 5408]. Side lane caught the propagation into this row within the hour.** **#19's substance does not depend on it:** the 68.8–80.6%-vs-0.007% degeneracy contrast is measured at MATCHED k and stands alone. **What would settle the magnitude is one shard — the same battery config with `NOISE_ON=False` at 338 seeds/cell — which is cheap and is the natural first thing #19 buys.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — concerns `tools/gate.py`/`tools/det.py`, not the bots/ tree; unaffected by the incumbent move by construction. |
| **20** | **THE HARVESTER TARGET — ⛔ DEMOTED FROM ⭐⭐ THE SAME DAY. THE TITLE CLAIM WAS FALSE.** *(was "we run the SMALLEST economy of the top group")* | **GREP: PASS.** `ECO_CAP = 18` (`doctrine.py:30`), gate at `eco.py:945` `harv < self._eco_cap(ct)`, `_eco_cap()` at `:235-241` returns 18 (or `SURGE_ECO_CAP = 24`). **We average 7.78 harvesters/game — the cap is NOT binding at 43% of its value, so something else is the limit and it is not named anywhere.** Candidate already documented at `doctrine.py:32-38`: builders dying freezes harvester count (measured: "harvesters frozen at 10 for 559 rounds, 12,314 Ti unspent"). **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: `ECO_CAP = 18` still at `doctrine.py:30`, gate `harv < self._eco_cap(ct)` still at `eco.py:945`, `_eco_cap()` still `eco.py:235-241` returning `ECO_CAP` or `SURGE_ECO_CAP = 24` (`doctrine.py:405`); the builder-respawn candidate comment reproduces verbatim at `doctrine.py:32-38`. All lines exact, zero shift. Premise HOLDS.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **harvesters built per game, and harvesters ALIVE at r150/r300** | archive answers the base rate today (`corpus/events.tsv`, 5,618 our games vs 490–785 theirs); a local battery answers the intervention | **MEASURED s33 vs the three teams 277–344 pts ABOVE us: harvesters/game Pivot 12.99 · ph 11.15 · O(1) 7.95 · US 7.78 — we are LAST.** ph collects **6,481 Ti/game against our 4,077 (+59%)**. Economy is instrumental under `R1000_IS_DEFEAT`, but it buys the kill, and we are buying least. ⛔⛔ **THE POPULATION WAS THE LOWER HALF OF THE TOP SIX AND THE CLAIM INVERTS ON THE FULL SET.** Side lane caught that this row measured only `ph`/`Pivot`/`O(1)` — ranked 6/7/8 — while `sporks` (2185), `Lorem Ipsum` (2021) and `Clankers` (2014) sit ABOVE all three and were absent. Harvesters/game across all six teams above us: **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `ECO_CAP=18` (doctrine.py:30), `SURGE_ECO_CAP=24` (:405) and the builder-respawn comment (:32-38) all confirmed byte-identical. `_eco_cap()` now eco.py:238-244 (was :235-241, +3 shift), gate `harv < self._eco_cap(ct)` now eco.py:1108 (was :945, +163 shift) — body unchanged. Premise (cap not binding, harvester count frozen by builder deaths) fully intact. |
```
Pivot 12.99 · sporks 12.11 · ph 11.15 · O(1) 7.95 · US 7.78 · Clankers 6.14 · Lorem Ipsum 5.53
```
⇒ **THREE are above us, one is level, and TWO are BELOW us — including the teams rated 2014 and 2021.** *"We run the smallest economy of the top group"* is **false**: we are 5th of 7, not last. **The top BRACKETS us on economy rather than exceeding it, which is #25 again on a second dimension.** ⚠ **measured against six teams; the top does not converge — see #25.**
**WHAT SURVIVES, and it is much weaker than the row first claimed:** our OWN internal curve peaks at 9–10 harvesters against our 7.78. That is a us-only, observational, non-significant signal (z=+0.92 on core-kill) for a ~2-harvester move. **No cross-team evidence supports it, and two top teams run less economy than we do.** ⇒ **Build #23 first.**
**⭐ THE 11+ REVERSAL SURVIVED ITS DISCRIMINATING TEST.** Side lane proposed the mirror confound: 11+ games might be ones where the assault had already failed, so we economised INSTEAD of attacking — which would make the ceiling an artefact. **That story predicts FEWER forward units in the 11+ bucket. We observe more:**
```
harv by r150   games   fwd turrets by r150   fwd share   win%
    7-8         437          1.59              34.7%      49.2%
    9-10        295          1.80              36.0%      54.2%
    11+         334          1.90              41.1%      42.2%
   11+ vs 9-10 forward turrets: +0.09, z=+0.64
```
⇒ **The 11+ bucket invested at least as much forward, so "economised instead of attacking" is NOT what produces the reversal.** That specific confound is ruled out; others (map ore availability) are not. ⚠ **And the sizing caution stands regardless: with 5 buckets and the peak chosen post hoc, and both trend tests null (z=+0.92, z=+1.51), 9–10 is a HINT not a target. The robust clause is the third one — Pivot's 13 is past where our own data turns — and it is the only one that changes a decision.** ⛔⛔ **AMENDED SAME DAY — THE INSTRUMENTAL LINK IS NOW MEASURED AND IT IS WEAK AND NON-MONOTONE. THE TARGET IS ~10, NOT PIVOT'S 13.** The row originally asserted "economy buys the kill" with a citation. Side lane flagged that the link was unmeasured and cheap to test; it was. **Conditioned on harvesters built by r150 in 2,760 of OUR games that survived to r150, then measured FORWARD** (the duration confound they named — long games accumulate harvesters — is removed by conditioning on the state at r150 and scoring only what follows):
```
harv by r150   games   win%   core-kill%
     0-4        985    43.4%     35.4%
     5-6        709    48.4%     35.4%
     7-8        437    49.2%     38.4%
     9-10       295    54.2%     39.3%   <- PEAK
    11+         334    42.2%     33.5%   <- REVERSES
above vs below mean (6.3):  core-kill +1.7pp z=+0.92 · win rate +2.9pp z=+1.51  — NEITHER SIGNIFICANT
```
⇒ **Three consequences. (1) The economy→kill link is NOT established on our chassis — z=+0.92 on the currency that matters. (2) There is a plausible optimum around 9–10 and we sit at 7.78, so the room is REAL but it is ~2 harvesters, not ~5. (3) ⛔ COPYING PIVOT WOULD OVERSHOOT: their 12.99 is past the point where our own data reverses, and a leg sized to their number would be aimed beyond our own peak.** ⚠ Observational and us-only; the r150 conditioning removes duration but not "games we were already winning let us build more". **A leg must therefore move the harvester TARGET to ~10 and read core-kill rate, not `titanium_collected` — otherwise a null cannot separate "economy does not convert" from "we never established that it converts on our chassis".** |
| **21** | ⭐ **THE GUNNER COUNT — we build 1.86/game against Pivot's 10.77, and our stated reason does not survive their data** | **GREP: PARTIAL — the constraint is REAL but NARROWER than it reads.** `raid.py:386 _try_forward_sentinel` hard-codes SENTINEL with an explicit rationale: *"SENTINEL, not Gunner, and not as a preference: the collar blocks LOS, so a Gunner built to shoot the Core would be shooting our own barriers."* **That is sound for a turret aimed THROUGH our own collar.** ⛔ **But Pivot builds 5.72 barriers/game against our 6.17 — essentially the same — AND 10.77 gunners. So barriers and gunners coexist; the binding constraint is OUR COLLAR GEOMETRY, not the existence of barriers.** **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: `_try_forward_sentinel` now at `raid.py:636` (was `:386`), same SENTINEL-only rationale docstring verbatim at `:639-641`; `can_fire_from` call now at `raid.py:683` (was `:433`). Premise HOLDS, lines shifted.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **gunners built per game; share of gunner builds whose firing line is clear of friendly barriers at build time** | local battery; `can_fire_from` is the exact predicate and is already used at `raid.py:433` | **Pivot 10.77 · O(1) 7.02 · ph 5.79 · US 1.86 — a 3–6x gap, the largest in the set.** ⚠ **The plank is ADDITIVE, not a swap: keep the collar sentinel, ADD gunners where LOS is already clear.** A straight sentinel→gunner substitution is the thing `raid.py:389` correctly forbids, and proposing it would be the leg this row exists to prevent. **Gunner is 20 Ti to sentinel's 30 at the same +20% scale, so the same titanium buys ~1.5x the guns.** ⛔ **AMENDED — MY `GREP: PARTIAL` NAMED ONE SITE AND THE PROVENANCE IS SPLIT. Measured over 5,618 of our games (`corpus/builds.tsv`):** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — raid.py:636,639-641,683 carry unchanged (0 diff); `_try_forward_sentinel`'s SENTINEL-only rationale still verbatim. |
```
sentinel FORWARD 11,072 (35.0%)  med rnd 150   sentinel HOME 7,914 (25.0%)  med rnd  82
gunner   HOME    4,629 (14.6%)  med rnd 104   gunner   FORWARD 2,515 ( 8.0%)  med rnd  91
```
⇒ **65% of our gunners are built HOME (median d²_enemy 200 — deep in our own half), on a path `_try_forward_sentinel` never touches.** And **our FORWARD slot is 81% sentinel** (11,072 vs 2,515), which is that function doing exactly what its name says. ⇒ **Two different levers, and #21 conflated them: (a) FORWARD substitution — the `raid.py:433` site is the right place for it, and `_v163gunadd` shows the clear-line branch rarely fires there; (b) gunner COUNT — the bigger lever is the HOME path, which no proposed arm has touched.** ⭐ **And the real gap is (a): our forward gunners run ~0.45/game against Pivot's ~8.6 (10.77 × 79.6%) — roughly 19x, far larger than the 3-6x this row claimed on pooled counts.** *(Builder's `_v163gunadd` FAILED its gate — 5 gunners vs 7, 24 sentinels vs 15, paired, n=4. Not queued.)* |
| **22** | **WE STOP BUILDING TURRETS AFTER r150 AND THEY DO NOT** | **GREP: PASS — no explicit late-game turret gate found; the fall-off is emergent, which is why it needs measuring before it is changed.** `LOKI_FWD_GUN_CAP = 3` (`doctrine.py:1219`) caps forward guns ALIVE, not built, and `doctrine.py:1334` records it as deliberately left alone. **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: `LOKI_FWD_GUN_CAP = 3` confirmed exactly at `doctrine.py:1219`, live-count gate `ct.read_store(SLOT_FWD_GUN) >= LOKI_FWD_GUN_CAP` at `raid.py:654`, comment at `doctrine.py:1334` reproduces verbatim. Premise HOLDS, lines exact.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **turrets built per game in r150-200 / r200-300 / r300+** | archive gives the base rate now; a battery gives the effect | **MEASURED s33, turrets/game by band — r0-150 we are COMPETITIVE (3.68 vs 3.24–4.77) and then we stop: r150+ US 1.95 · Pivot 3.83 · O(1) 3.28 · ph 4.75.** ⇒ **the gap is not our opening, it is that our mid-game turret production halts.** ⚠ **Lowest priority of the three: this is a SYMPTOM row and may be entirely downstream of #20 (no titanium to spend). Do #20 first and re-measure before building anything for this.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED, WITH A CORRECTION** — `LOKI_FWD_GUN_CAP=3` and its ‘left alone’ comment are byte-identical but sit at doctrine.py:1237/:1352, NOT the row's own cited :1219/:1334 — those are the PRE-MAP_CODES-shift (`_v187saltidle_f`) numbers that were never individually updated when the s36 carry note said ‘>1153 shift +18’ (1219+18=1237, 1334+18=1352 — confirms the shift, corrects the pointer; value/content unchanged). raid.py:654 unchanged (0 diff). Premise (emergent fall-off, cap governs alive-not-built) fully intact. |
| **23** | ⭐⭐⭐ **FORWARD PLACEMENT — the ONLY thing all six top teams agree on, and we are the outlier** | **GREP: PARTIAL PASS — we DO place forward and it is CAPPED.** `LOKI_FWD_GUN_CAP = 3` (`doctrine.py:1219`) caps forward guns ALIVE, and `doctrine.py:1334` records the cap as deliberately left alone ("3 is already the specialists' number"). `raid.py:386 _try_forward_sentinel` is the builder. **So this is a CAP RAISE on an existing mechanism, not a new behaviour.** **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: forward-sentinel machinery SHIPPED — `LOKI_FWD_SENTINEL_ON=True`, `LOKI_FWD_GUN_CAP=3` (`doctrine.py:1218-1219`), `_try_forward_sentinel` `raid.py:284`; gates `LOKI_FWD_TI_FLOOR=40`, `LOKI_FWD_MIN_HARV=2` (`doctrine.py:1246-1247`). The cap-ablation halves checked in `_v143cap6`/`_v144cap12` remain unshipped.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **share of turret builds placed FORWARD** (verified definition: `d2_enemy < d2_own`, 100% clean on 40,000 rows) | archive gives the base rate now across 6 top teams and 5,618 of our games; a local battery gives the effect | **MEASURED s33. FORWARD share: Pivot 79.6% · O(1) 75.1% · sporks 64.3% · Lorem Ipsum 62.2% · Clankers 57.8% · US 35.9%.** ⭐ **Five of the six teams above us sit at 57-80% and we sit at 36 — and this is the ONLY dimension on which they agree.** They disagree wildly on everything else (see #25). ⚠ **ph is the counter-example at 38.4%, near ours, and is rated 1995 — so forward placement is not necessary to win, only very common among winners.** ⭐⭐ **s34 — THE GAP DECOMPOSED, AND THE AGGREGATE WAS HIDING IT.** Over **2,884 of our games**, first-turret round and turrets/game, split by the row's own verified FORWARD definition (`d2_enemy < d2_own`): **HOME — us first at r7 in 96% of games, 3.32/game; them r8 in 81%, 4.52/game (we are AHEAD on timing, behind on count). FORWARD — us first at r33 in 78% of games, 2.28/game; them r25 in 87%, 4.54/game.** ⇒ **the deficit is ENTIRELY forward and it is three deficits at once: 8 ROUNDS LATER, in 9pp FEWER games, at HALF the count.** ⛔ **AND THE POOLED FIGURE INVERTS THE SIGN: across all turrets we look EARLIER than the field (first turret median r7 vs r10), because our prompt HOME turret masks a forward arm that arrives 8 rounds late.** Anyone quoting an all-turret timing number will conclude we are ahead. **Eight rounds against a median kill of ~174 and a median death of ~187 — a thirteen-round race.** *(Raised by the side lane off a live rated 0-5 loss where their first turret landed r3-r22 against our 0/1/2/1/0 forward sentinels; the single match generalises on the FORWARD axis and NOT on the first-turret axis, which is why the decomposition was worth running.)* ⛔⛔ **AMENDED s34 — THE PRECONDITION WAS MEASURED BEFORE ANY GAMES WERE SPENT, AT THE BUILDER'S REQUEST, AND IT RE-SPECIFIES THIS ROW. A CAP RAISE IS THE WRONG LEVER.** Population: our games after the LOKI-2b live-cap ship (`b5a6068`, 2026-08-09T15:36+02:00) — **1,308 games / 4,224 forward-sentinel builds**, forward = our SENTINEL at `d2_enemy <= 50` (= `LOKI2B_CENSUS_DSQ`), alive-concurrency by pairing BUILD/DEATH on tile. **(1) THE CAP BINDS IN ONLY ~13% OF GAMES.** Max forward sentinels alive at once: 0→6.6% · 1→52.0% · 2→28.7% · 3→8.5% · 4→3.0% · 5+→1.4%. **Ever 3 alive at once: 169/1308 = 12.92%** *(corrected s34 after the side lane's bounded check: the original 12.77% used a within-round sort that put DEATH before BUILD, stranding a phantom on the 219 same-round build-and-die tiles. Re-run with same-tile BUILD-before-its-own-DEATH: **12.92%, and IDENTICAL under both cross-tile orderings**, so the figure is insensitive to tie-breaking. **Pairing is now proven sound, not asserted: per (game,tile) events must alternate B,D,B,D since a tile holds one building — 0 violations after the fix, and 0 (game,tile) pairs where DEATHs exceed BUILDs.** ⭐ **And the bound that needs no pairing at all: only 35.55% of games ever see ≥3 forward-sentinel BUILD events, a hard ceiling on ever-3-alive — so the pairing carries roughly two-thirds of this claim and was worth proving.** ⭐ **STATE IT AS A SOFT UPPER BOUND, NOT A POINT ESTIMATE.** The alternation invariant rules out every failure that would DEFLATE concurrency (missing/spurious BUILD, duplicated/mis-ordered DEATH), **but it cannot detect a DEATH THAT WAS NEVER EMITTED** — `B,D,B,(absent D)` is a valid alternation reading as *still alive*, indistinguishable from a building that genuinely survived. **So the entire residual error is in the INFLATING direction: true binding is AT MOST 12.92%, the dilution is AT LEAST 8×, and the re-specification is safe against the one mode the invariant cannot close.**)* (v114 17.5% n=200 · v115 10.7% n=75 · v116 13.3% n=45). ⛔ **The all-time figure is 7.74% (n=2,766) and it is the WRONG denominator** — pre-LOKI-2b ran the rubble counter and is a different bot. ⇒ raising 3→6 can move at most ~13% of games: **~8x dilution, so this row needs a pre-declared resolution like #24's or it buys a guaranteed inside-band null.** **(2) ✅ HELD FOR ~40 MINUTES, THEN CONFIRMED BY EXECUTION — UN-HELD. The premise was checked on the engine and it holds.** Builder's `bots/_probe_nearby_default` (`0686fdc`), 9 games × 3 maps × 3 seeds: **`len(get_nearby_buildings())` == `len(get_nearby_buildings(vision_radius_sq))` in 100% of informative rows, `DEFAULT-WIDER-THAN-VISION` fired ZERO times**, `dist_sq=2` reads strictly lower (so the column is not constant), and **the control that makes the match meaningful: the engine end-card showed the opponent holding 22 buildings while our core read D=6** — an unbounded default counts those. Decision rule incl. a SKIP outcome written before the run; a first probe-vs-probe run refused to score. ⇒ **`get_nearby_buildings()` defaults to the CALLER's vision radius; the census really is a d²≤50 band read by a builder that sees r²=20.** *(Kept below: the history of the hold, because it is the reason the claim is now trustworthy.)* **THE HOLD, AS WRITTEN: this claim rests on an unverified premise.** The side lane caught it; the author had asserted it.** The premise is that **`get_nearby_buildings()` with no `dist_sq` defaults to VISION RADIUS** — obtained by **reading across from the neighbouring `get_nearby_tiles` row in CLAUDE.md**, which carries "(default: vision radius)" while the `get_nearby_buildings` row does not. `official-docs.md:454,1508` states no default. **If the default is unbounded, the census is complete and every number in this paragraph is 0.** ⭐ **BEST AVAILABLE EVIDENCE, and it is better than the doc but is not execution:** the SHIPPED package's own type contract — `site-packages/fcode/_types.py:363-365`, `py.typed` present, a different artefact from `official-docs.md` — carries it on the method's **own** docstring line: *"Return ids of all buildings within dist_sq **(defaults to vision radius)**"*, for all four `get_nearby_*` methods; and the engine binary carries the guard string **`dist_sq exceeds vision radius`**, so vision is a hard ceiling on the explicit path. The `.so` carries **no docstrings** and exposes only `run_game`, so it cannot be introspected. ⇒ **ONE BUILDER-OWNED PROBE SETTLES IT:** compare `len(ct.get_nearby_buildings())` against `len(ct.get_nearby_buildings(ct.get_vision_radius_sq()))` and a map-wide count. **Nothing may be built on the leak until it reads out.** ⛔ **AND THE AUTHOR'S OWN EMPIRICAL PROOF IS WITHDRAWN AS PROOF, confound found by the author:** 320 RATED games with `ourver` ∈ {114,115,116} (all trees verified CAP=3 + live cap ON; rated-only excludes the `_v143cap6`/`_v144cap12` prototypes) show **16 games (5.0%) reaching 4+ alive — 28 builds with 3+ already alive**, which a complete census forbids — **but see (2b), a second uncapped producer explains them without any census defect.** THE HELD CLAIM, if the premise holds: **THE CAP IS NOT A TEAM CAP, IT IS A PER-RAIDER LOCAL CENSUS.** `_live_fwd_guns` (`raid.py:464`) counts with `ct.get_nearby_buildings()` and **no `dist_sq`**, so it defaults to the caller's vision — **r²=20 for a builder bot** — while the band it must count is d²≤50 around the enemy core, a disc ~14 tiles across censused by a unit seeing ~4.5. **35.6% of already-alive forward sentinels are invisible to the raider placing the next one** (sensitivity for the ±1-tile raider-vs-build-tile proxy: **24.8–50.2%**), and **71 of 95 over-cap builds (74.7%) had a census that could not see 3.** ⇒ **the gate already leaks in the direction this plank pushes, so a null here could be the leak rather than the plank.** Loose-bound control (d²≤72, since corpus `d2_enemy` uses the core's single `pos` at `replay_events.py:65` while the bot takes `min` over `core_tiles`, i.e. the strict filter UNDERCOUNTS): every figure moves the same way — **15.06% ever-3-alive, 83% blind, 45.4% invisible.** **(2b) ⭐⭐ NEW, CERTAIN, AND INDEPENDENT OF THE CONTESTED PREMISE — THE CAP GOVERNS ONE OF TWO SENTINEL PRODUCERS.** `grep 'build_sentinel('` returns **two** call sites: `raid.py:439` (forward, capped) and **`main.py:574` — the HOME-DEFENCE turret path, which never reads `LOKI_FWD_GUN_CAP` and increments `SLOT_HOME_GUN`, not `SLOT_FWD_GUN`.** And `_live_fwd_guns` counts by **position, team and entity type** — not by which store built it — so **a home-defence sentinel landing inside the d²≤50 band is COUNTED BY the cap while being UNCAPPED BY it.** That path builds adjacent to a threatened unit and our raiders operate deep in enemy territory, so it is not hypothetical. ⇒ **"Raise the cap" was never a whole lever, whatever the census turns out to do.** ⚠ **The two paths CANNOT be separated from replays:** the forward path needs `bp` within d²≤32 of a core tile, but corpus `d2_enemy` uses the core's single `pos` (`replay_events.py:65`) while the bot takes `min` over `core_tiles`, so corpus d² ≥ bot d² and **nothing inside the band can be proven home-path by geometry.** Separation needs an instrumented local run, not the archive. **(3) THE `None` FALLBACK IS DEAD CODE W.R.T. THE BUILD DECISION** — blind is `d2 > CENSUS_DSQ*2` = 100 from the enemy core, the downstream hard reject at `raid.py:415` is `d2 > 50`, and blind is a strict subset of rejected, so a blind raider returns False on either path. Rules-level (a subset relation between two literal constants), no games owed. **(4) ⚠ SEPARATE CONSUMER, FLAGGED NOT SIZED:** `main.py:202` reads `SLOT_FWD_GUN` as `fwd_guns` and `weapons = home_guns + fwd_guns` sizes the endgame ammo conversion; since `raid.py:444` writes the store as `(live census)+1` it **inherits the vision undercount — the store is below the true forward count in 17.0% of games** at the last forward build (by 1 in 12.9%, 2 in 2.8%, 3+ in 1.3%). Adjacent to the ZEROAMMO/LATE160AMMO ship decision. **No claim that the under-conversion binds — that is unmeasured.** ⇒ **NEW LEVER: either fix the census to a real team count, or go at `LOKI_FWD_MIN_HARV`/`ti_floor` (`raid.py:407-412`).** ⛔⛔ **AND THE OBVIOUS FIX — "just pass a wider `dist_sq`" — IS A LIVE HAZARD, NOT MERELY INEFFECTIVE. IT RAISES; IT DOES NOT CLIP.** Engine-established by disassembly, already in this repo at `docs/research/engine-source-crash-and-launcher-2026-08-10.md:46-50` (verified at the primary, s34): all four `get_nearby_*` **raise `"dist_sq exceeds vision radius"`** when passed an explicit `dist_sq` larger than the **CALLER's** radius (`0x4d30` in `get_nearby_tiles @ 0x4b0c`; the other three funnel through it). **Two distinct failure modes depending on where it is written:** *(a)* **inside `_live_fwd_guns`, whose whole body is wrapped in `except Exception: return None` (`raid.py:472-473`)** — `get_nearby_buildings(50)` from a builder bot at r²=20 raises, **is swallowed, and returns `None` on EVERY call: the census becomes PERMANENTLY BLIND and falls through to the monotone store.** It would read as *"the fix didn't help"* while having silently replaced a partial census with a zero one — **a null in the direction that looks like a dead plank.** *(b)* **anywhere without that guard: an uncaught raise out of `run()` permanently destroys the unit for the match.** A launcher may legally pass 26; a builder may not. ⇒ **the fix must be a per-round STORE write from a unit that can legally see the band — and `SLOT_FWD_GUN` cannot be it unchanged, since `raid.py:444` writes it from the same undercounting census.** *(Found by the side lane, who also retracted half their own flag: the engine passage establishes the API is vision-BOUND, so claim 2 does not collapse; what remains unestablished is only the `dist_sq=None` default.)* If the cap dose is still run, pair it with the census fix or treatment and leak are confounded.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A/B MIXED, RE-VERIFIED** — all raid.py/main.py facts (raid.py:284,439,444,464,472-473,415,407-412,636,683,685,689; main.py:202,578,586) carry unchanged (0 diff in either file). doctrine.py: `LOKI_FWD_TI_FLOOR=40`/`LOKI_FWD_MIN_HARV=2` confirmed exactly at :1264-1265 (matches the row's own citation). `LOKI_FWD_GUN_CAP=3` and its ‘left alone’ comment sit at :1237/:1352, not the row's inline :1219/:1218-1219/:1334 (same pre-shift-number staleness as #22, already stale before this carry, value/content unchanged). Both `_try_forward_sentinel` sites (call raid.py:284, def raid.py:636) and both `build_sentinel(` sites (raid.py:689 forward, main.py:586 home) confirmed present and distinct. Premise (forward arm late/rare/thin, two uncapped producers) fully intact. |
| **24** | **THE LAUNCHER SINGULARITY — we are the only team in the top tier that builds them at all** | **GREP: PASS.** `main.py:581 _try_build_launcher`, gated by `SLOT_LAUNCHER = 6` and `LAUNCHER_RESERVE = 80` (`doctrine.py:937,965`), with `LOKI6_LAUNCHER_RELEASE = True` allowing a rebuild after death. **A single knob, already isolated.** **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: `_try_build_launcher` now at `main.py:593` (was `:581`); `SLOT_LAUNCHER = 6` and `LAUNCHER_RESERVE = 80` confirmed exactly at `doctrine.py:937,965`; `LOKI6_LAUNCHER_RELEASE = True` confirmed at `doctrine.py:1441`. EXILE sort-farthest-from-core and FERRY sort-toward-dest both confirmed in `_launcher_turn` (now `raid.py:875`), at approx `:927` and `:940` (were `:679`/`:703`) — `LAUNCHER_CAP` itself is NOT present in this tree (it is an ablation-arm knob, not a shipped constant, so its absence here does not affect the row's premise). Premise HOLDS.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **launchers built per game, and titanium spent on them as a share of turret spend** | archive now; a local `LAUNCHER_RESERVE`-off arm is a one-constant battery | **MEASURED s33, launchers/game: sporks 0.00 · Lorem Ipsum 0.00 · Pivot 0.00 · O(1) 0.00 · Clankers 0.09 · ph 0.87 · US 1.17.** **Five of the six build essentially none; we build the most.** ⛔ **THIS IS NOT A RECOMMENDATION TO DROP THEM — the launcher IS the kidnap weapon and the LOKI line is the programme.** It is a COST question: 1.17/game at 20 Ti and +10% scale each, and today's own archive work put the crash channel at 1 event on 110 border landings across every admissible target (≤4.2% upper bound). **The honest framing: we pay a premium nobody else pays, for a channel we have just measured as weak at the field level. A local both-arms battery prices it for zero rated exposure.** ⛔⛔ **AMENDED — MY FRAMING WAS HALF WRONG AND WOULD HAVE MISATTRIBUTED THE ABLATION.** This row called the launcher *"the kidnap weapon"*. **On the v114 chassis it is ALSO the insertion system**, and `_launcher_turn`'s own docstring says so: *"Launcher: exile intruders first, then ferry raiders forward."* Verified at the call sites, not taken: **`raid.py:679` sorts sites FARTHEST from our core (EXILE); `raid.py:703` iterates `friendly_bots` toward `dest` (FERRY/INSERT). `LAUNCHER_CAP = 0` kills BOTH.** And the ferry is not incidental — the same docstring records that removing its lifetime cap and round cutoff is what stopped the insertion pipeline being *"dead code from r180"*, **i.e. it is LOKI-27, which we SHIPPED as v112.** ⇒ **`LAUNCH0` removes EXILE and INSERT together and they pull OPPOSITE ways: EXILE is the LOKI weapon (crash channel priced at ≤4.2% on admissible targets today, leaving displacement); INSERT is assault DELIVERY, how the kill lands inside `KILL_WINDOW_RND: 250`.** **A positive result cannot attribute between them, and the two implied actions are "retire the kidnap line" and "retire our fastest route to their core".** The misreading is pre-loaded by this row's own wording. ⇒ **`LAUNCH0` PRICES the premium; it does NOT ATTRIBUTE it. Attribution needs a second arm gating ONLY `raid.py:679`, with the same outcome-verified control (exile throws → 0, insert throws unchanged).** *(Side lane. The arm itself is well built — its author already caught that `LAUNCHER_CAP = 0` leaves the cap test always-true, an ablation that would not have ablated.)* ⭐ **PRE-REGISTERED RESOLVABILITY — WRITTEN BEFORE THE SHARDS LAND, so a null cannot be re-read afterwards.** The 2x2 is now complete: `EXILE0` (exile off, ferry on) · `FERRY0` (ferry off, exile on) · `BOTH0` (both off, **launcher STILL BUILT AND PAID**) · `LAUNCH0` (no launcher at all). ⛔ **The third factor neither lane named at first: `LAUNCH0` also never PAYS for the launcher — ~20 Ti and +10pp global scale at ~1.17/game — so it differs from the others in MECHANISM *and* PRICE.** ⇒ **`BOTH0` vs `LAUNCH0` is THE PAIR THAT MATTERS: identical mechanism, differing only in whether the launcher is bought. That contrast IS the premium, measured rather than inferred.** ⛔ **BUT IT IS A DIFFERENCE OF TWO NOISY ESTIMATES — the arms share a control BOT but not control GAMES, so nothing cancels:** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED, WITH A CORRECTION** — `SLOT_LAUNCHER=6` (doctrine.py:937) and `LAUNCHER_RESERVE=80` (:965) confirmed exactly, byte-identical. `LOKI6_LAUNCHER_RELEASE=True` is NOT at the row's cited :1441 (unrelated comment text there now) — true location doctrine.py:1459 (1441+18, same pre-MAP_CODES-shift pointer staleness as #2/#22/#23/#33/#44, value/content unchanged). main.py/raid.py facts (main.py:593, raid.py:679,703,875) carry unchanged (0 diff). Premise (launcher singularity, EXILE/FERRY dual-use) fully intact. |
```
per-arm SE 0.68pp -> band ±1.33pp     BETWEEN-arm difference SE 0.96pp -> 95% half-width ±1.88pp
to resolve a between-arm difference at 80% power:  1.0pp -> n>=39,200/arm · 1.9pp -> 10,859 · 3.0pp -> 4,356
```
⇒ **At n=5,408/arm the premium is resolvable ONLY if it exceeds ~1.9pp of game share. A NULL THEREFORE MEANS "the premium is under 2pp", NOT "the launcher is free"** — and that sentence sits here in advance precisely so it cannot be chosen after the numbers. **Same ±1.9pp caution on `FERRY0` vs `BOTH0`.** **And naming this pair IN ADVANCE is what makes it ONE test rather than the ten pairwise comparisons five arms admit.** *(Raised by the side lane; arithmetic verified independently.)* |
| **25** | **STATUS: NOTE — ⭐ THE TOP DOES NOT CONVERGE — there is no single build to copy, and that is the finding** | **GREP: N/A — this is a scoping row, not a plank. It exists so a successor does not read #20-24 as one coherent "top-team build".** | n/a | n/a | **MEASURED s33. The three highest-rated teams run three INCOMPATIBLE strategies: `sporks` (2185, 75.2% win) is an ECONOMY bot — 111.22 conveyors/game (2.4x ours), 12.11 harvesters, and FEWER turrets than us (5.68 vs 8.13); `Lorem Ipsum` (2021) is a GUNNER RUSH — 11.64 gunners (6.3x ours), only 5.75 builder bots (44% of ours), fastest median kill at 196; `Clankers` (2014) is a BARRIER bot — 20.27 barriers/game, 3.3x ours.** ⇒ **Copying "what the top does" is incoherent as a plan. The only cross-cutting signals are FORWARD placement (#23, 5 of 6) and the launcher singularity (#24, 6 of 6).** Everything else is style, and three different styles are all beating us. **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.** |
| **26** | ⭐⭐⭐ **CONVEYOR DENSITY — the cheapest graft from the #1 team, and it is on the path key 1 actually counts** | **GREP: TODO BEFORE BUILD — this row is admitted on the archive cut; the incumbent grep on conveyor/route logic in `eco.py` has NOT been run and must be before it is built.** *(Declared rather than stamped: #20 was corrected three times today for claims made ahead of their checks.)* | **conveyors built per game; conveyors per harvester; and `titanium_collected` per harvester — the DELIVERY ratio, not the emission count** | archive gives the base rate across 7 teams now; a local battery gives the effect | **MAGNUS'S FRAMING (s33): the top does not converge, so the criterion is what GRAFTS ONTO OUR BUILD. Measured — builder count is the chassis, and it splits the top six cleanly:** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.** |
| **28** | ⭐⭐ **`LAUNCHER_RESERVE = 80` IS THE GATE THAT STARVES THE LAUNCHER — measured against our actual bank, and it converges with the builder's replay decode** | **GREP: PASS, and it corrects a wrong lever already passed between lanes.** `main.py:629` gates the build on `_eco_spendable(ct, ct.get_launcher_cost() + LAUNCHER_RESERVE)`; `eco.py:225-233` shows `_eco_spendable` is plainly `ti >= cost` (plus `SIEGE_HEAL_RESERVE_TI` when `SLOT_UNDER` and `rnd >= HUNT_MIN_RND`). `LAUNCHER_RESERVE = 80` (`doctrine.py:965`). ⛔ **`SLOT_LAUNCHER` is a comms-store INDEX, not a cap** — it was named as a dose candidate in cross-lane traffic and is not one; the store is full (16/16 assigned), so a sweep on it would corrupt an occupied slot. The other two gates at `main.py:614-627` are a one-way latch (needs an ablation, not a sweep) and `SLOT_HARVESTERS < 1`. **`LAUNCHER_RESERVE` is the only one of the three with a dose.** **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: the gate call now at `main.py:641` (was `:629`, `_eco_spendable(ct, ct.get_launcher_cost() + LAUNCHER_RESERVE)`); `eco.py:225-233 _eco_spendable` body reproduces exactly; `LAUNCHER_RESERVE = 80` confirmed exactly at `doctrine.py:965`; the one-way `SLOT_LAUNCHER`/`LOKI6_LAUNCHER_RELEASE` latch and `SLOT_HARVESTERS < 1` gate both confirmed in `_try_build_launcher` (`main.py:593-638`). Premise HOLDS, one line-range shifted.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **launchers built per game** (builder's decode) **× share of games whose bank ever clears `floor(scale*20) + RESERVE` after `LAUNCHER_MIN_RND = 160`** (archive) | archive prices it now; a `LAUNCHER_RESERVE` dose sweep is a one-constant local battery, zero rated exposure | **MEASURED s34 on 325 RATED games, `ourver` ∈ {114,115,116}.** Our bank at the end of the r150-200 band: **median 54 Ti**, and it clears the gate's ~120-160 Ti threshold (launcher cost at a typical 2-4× scale, +80) in only **12.4% / 5.7%** of games. ⭐⭐ **TWO INDEPENDENT MEASUREMENTS CONVERGE: the builder's live decode has us building a launcher in 3 of 55 games (5.5%); the bank cut says only 5.7-12.4% of games can AFFORD one at the moment the gate opens.** ⇒ **the launcher is not deferred by `LAUNCHER_MIN_RND`, it is priced out by `LAUNCHER_RESERVE` against a bank that never gets there.** ⚠ **THE ONE HEDGE, AND IT RUNS AGAINST ME: `econ.tsv:ti_end` is a SNAPSHOT at band end, not the max over the band**, and the gate may fire at any round inside it — so the true share of games that EVER clear the threshold is **higher** than these figures, and this is a LOWER bound on affordability. The convergence with 5.5% is striking but both numbers are small-n. **Resolution to declare before the sweep: with ~1.17 launchers/game today, a dose that does not move launchers-per-game by ≥1 is uninterpretable.** ⚠ **CONTRARY SCREEN EVIDENCE ADDED s37 2026-08-13 (research, from the finals on disk): `LAUNCHRES0` 48.63±1.33 and `LAUNCHRES20` 48.95±1.33 (both n=5408) — reserve ablation alone screened NEGATIVE in the era arms.** The premise (the reserve prices the launcher out) stands as a MEASUREMENT; the implied fix (lower it) now has a two-sided record: more launcher affordability bought MORE of the −6.34pp standing premium (see #47's 2×2). ⇒ **a bare reserve sweep is no longer the natural dose — the conditional design (#47) supersedes it; keep this row for the affordability arithmetic it carries.** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS B, RE-VERIFIED** — `_eco_spendable` body now eco.py:228-236 (was :225-233, +3 shift), byte-identical; `LAUNCHER_RESERVE=80` (doctrine.py:965) confirmed exactly, byte-identical. main.py:641 gate call unchanged (0 diff). Premise (RESERVE is the binding gate, bank never clears it) fully intact. | ⛔⛔ **CORRECTED s40 2026-08-14 (research, while re-specifying #57): THIS ROW'S GATE ENUMERATION MISSED THE BINDING GATE, ONE LINE ABOVE THE ONES IT LISTED.** The row names the three gates at this build site as `LAUNCHER_RESERVE`, a one-way latch and `SLOT_HARVESTERS < 1` at `main.py:614-627`. **There is a fourth, at `main.py:613`: `if ct.get_current_round() < LAUNCHER_MIN_RND: return`, with `LAUNCHER_MIN_RND = 160` (`doctrine.py:1536`, comment 'LOKI-42: do not build a launcher before this round. Swept, not guessed').** ⇒ **`LAUNCHER_RESERVE` CANNOT BIND BEFORE ROUND 160, BECAUSE NO LAUNCHER BUILD IS EVEN ATTEMPTED BEFORE ROUND 160.** A bank-reserve sweep therefore cannot recover the r6-r18 window at all, and the measured collapse of our launcher fleet (builds/game v115 1.171 → **v116 0.142**, still 0.200 at v140) sits in a window this row's lever does not reach. **This row remains valid for the post-r160 fleet; the pre-r160 question is #57's.** ⛔ **AND A CORRECTION TO MY OWN CORRECTION, same session, ~15 min later: I wrote the paragraph above without reading this row's EXISTING s37 annotation, which already carried the contrary RES0/RES20 evidence and already recorded that `#47` supersedes a bare reserve sweep. The round-gate observation stands as a FACT (`main.py:613` is real and `#28`'s enumeration did omit it); the LEVER it implied does not — s37's family sweep reads earlier launchers monotonically worse. Annotating a row while skipping its annotations is the failure this row now documents twice.** ⭐⭐ **RESOLVED-ON-MECHANISM s45 2026-08-16 ~04:4xZ (research) — THE TI GATE IS NULL ON THE CURRENT CHASSIS, AND THE ARM THAT SAYS SO WAS BUILT BY ACCIDENT.** The overnight local board carries a clean one-constant contrast against the CURRENT control `_v223sealrepair` (v140), created when a stranded fleet row was re-queued rather than by design: **`RND1SOLO` (`_v259rnd1`) 51.29% n=5,401 ±1.33 vs `HOMEMAX` (`_v255homemax`) 51.23% n=5,401 ±1.33.** I diffed the two trees directly: **they differ by EXACTLY ONE substantive line — `doctrine.py: LAUNCHER_RESERVE = 80 → 0` — with `main.py`, `raid.py` and `eco.py` byte-identical.** ⇒ **observed effect of lifting the TI reserve entirely: 0.06pp ± 1.87pp. NULL.** ⚠ **THE BAND IS THE TWO-INDEPENDENT-SAMPLE FORM AND MUST STAY THAT WAY.** The session that queued these rows placed them adjacently for *machine conditions* and stated explicitly that **this is NOT a paired design**; the shards carry different seed bases (720000 / 724000), so no games are shared and nothing cancels. **±1.87pp is computed unpaired (local DEFF 0.98); a paired reading would quote a narrower band this design does not earn.** Flagged independently by the side lane the same morning. Positive control on the same board: `NULL5400` 49.94% (harness unbiased); the fixture detects a known-bad arm (`NEG114` 36.32%). ⛔ **STATE THE CONDITIONALITY, because this is a 2-arm contrast and not a main effect: both arms carry `LOKI_HOMEEARLY_RND = 1`, so this measures the reserve GIVEN the home-launcher round gate is already open.** That is the favourable case for the row's own premise — if the reserve were starving a launcher the bank could otherwise afford, the round gate being open is exactly when it would show — **so a null here is stronger evidence against the lever than a null at the shipped `LOKI_HOMEEARLY_RND`, not weaker.** ⇒ **the row's MEASUREMENT survives intact (the bank genuinely does not clear the threshold — median 54 Ti, 5.7–12.4% of games); the LEVER is now dead on two independent chassis** (s37's `LAUNCHRES0` 48.63 / `LAUNCHRES20` 48.95 on the era arms, NEGATIVE; s45's 0.06pp ± 1.87pp on v140, NULL). **Do not spend another shard on a bare reserve sweep.** ⚠ **AND THE SAME PAIR PRICES THE ROUND GATE, which this row does not own:** because the reserve plank is null the two arms estimate the same quantity, so **pooled they read 51.26%, n=10,802, ±0.93 → [50.33, 52.19], EXCLUDING 50** — `LOKI_HOMEEARLY_RND = 1` is a REAL but small **+1.26pp**, against `bodyaware`'s +3.70pp (`BODYAWR` 53.70%, n=10,801, ±0.94), and **`bodyaware` beats it by −2.44pp ± 1.32pp, significant.** ⛔ **`TRIO` (bodyaware+rnd1+spawnlock) does NOT beat `bodyaware` alone: +0.55pp ± 1.59pp at n=5,731.** ⇒ the launcher-gate family is a third of one plank and does not stack. **Backing: `docs/research/RATED-LADDER-POWER-2026-08-16.md` and the coordination note of the same session; verdict sentence is the builder's, not banked here.**
| **30** | ⭐⭐ **THE STATION SCORER MODELS THE SENTINEL AS A TARGET, NEVER AS A THREAT — and it is the threat the tree's own comment calls unavoidable** | **GREP: PASS, verified at the primary (not relayed).** `raid.py:500-527`, the raid-station threat scan, handles exactly two enemy kinds: `LAUNCHER` → `threats` (exile risk, `:505`) and `GUNNER` → `gun_axis` via `get_attackable_tiles_from` (LOKI-25, `:518`). **`SENTINEL` has no branch.** ⚠ **The precise claim, because a naive grep gets it wrong: sentinels ARE handled elsewhere — `raid.py:366` gives `GUNNER`/`SENTINEL` a shared attack priority `pr=3`. So we model the sentinel as something to KILL and never as something to AVOID.** And the site's own comment argues the asymmetry is backwards: a gunner's shot *"IS BLOCKED by obstacles and reaches only r²=13; a sentinel's ignores obstacles"* (r²=32) — *"we are dying almost entirely to the AVOIDABLE one."* **RE-CHECKED s36 2026-08-13 vs `_v187saltidle_f`: the threat scan is now `raid.py:749-777` (was `:500-527`) — `LAUNCHER` still branches to `threats` (`:754-756`), `GUNNER` still the only turret feeding `gun_axis` via `get_attackable_tiles_from` (`:768-774`), no `SENTINEL` branch. Shared attack `pr=3` for GUNNER/SENTINEL confirmed at `raid.py:616` (was `:366`). Premise HOLDS.** **CARRIED s36 → `_v197mapcode` (v125): ship diff verified ADD-ONLY — +18 doctrine.py lines at :1154-1171 (MAP_CODES pool entries, exact-key filter; behaviour on known maps unchanged by construction), zero other file changes. All stamped facts carry over; doctrine.py refs >1153 shift +18 (spot-checked: LOKI_GUNAXIS_PENALTY now :1533, LOKI_FWD_GUN_CAP now :1237).** | **share of our forward builder deaths reachable ONLY by an enemy sentinel** (**34.9% on the SHIPPED trees v114/115/116 — 0.32 deaths/game absolute; the 7.6% that stood here was an ALL-TIME figure and is superseded, see the status cell**) **and raider station-rounds standing on a sentinel firing line** | archive gives the base rate now (below); a local battery gives the effect — zero rated exposure | ⭐ **PRECONDITION MEASURED s34 BEFORE QUEUEING, and it SIZES the row rather than motivating it.** Our forward builder-bot deaths (`d2_enemy <= 60`), **n=8,690 across 2,865 of our games**, classified by which enemy turret was ALIVE AND IN RANGE at the death round: **gunner-only 76.9% · both 15.1% · sentinel-only 7.6% · neither 0.4%.** ⇒ **in a gunner's range 92.0%, in a sentinel's range 22.7%.** ⭐⭐ **THE 92.0% REPRODUCES THE TREE'S OWN LOKI-25 COMMENT ("92% of our FORWARD builder deaths are enemy gunners") TO THE DECIMAL, INDEPENDENTLY — an in-tree claim nobody had re-derived, now confirmed.** ⛔⛔ **THAT SIZING WAS WRONG BY 4.6x AND THE ERROR IS MINE: I SIZED IT ON A POPULATION DOMINATED BY BOTS WE NO LONGER SHIP.** The all-time cut pools v80/v90/v91-era trees that died 6-8 forward builders per game against the shipped trees' 0.93 — **so it describes the dead bot, which is exactly what this repo's ERA RIDER exists to prevent, and I invoked that rider for `#29`'s sample (D42) and then failed to apply it here one hour later.** **RE-CUT ON THE SHIPPED TREES (v114+v115+v116, 325 games, 301 forward deaths): gunner-only 31.2% · both 33.6% · SENTINEL-ONLY 34.9% · neither 0.3% ⇒ ANY sentinel coverage 68.4%, any gunner coverage 64.8%.** ⇒ **a sentinel term now addresses 34.9% newly and 68.4% at the ceiling — not 7.6%/22.7%.** ⛔⛔ **AND THE "4.6x" WAS ITSELF WRONG — IT IS A RATIO OF SHARES, WHILE THE PAYOFF IS SHARE x RATE. The side lane argued their own row down and they are right.** The era re-cut that fixed the share must also be applied to the DENOMINATOR IT SITS ON: the shipped trees lose **0.93 forward builders/game** where the all-time pool loses **3.03** and the v80/v90/v91 era lost ~7. **A share can quadruple while the thing it is a share OF collapses.** **ON THE QUANTITY THE PLANK PAYS IN — forward builder deaths prevented per game: shipped 0.93 x 34.9% = 0.32/game addressable (ceiling 0.93 x 68.4% = 0.63/game), against all-time-pooled 3.03 x 7.6% = 0.23/game.** ⇒ **1.4x the pooled figure, and 0.61x the era subset that dominates the pool — NOT 4.6x.** **THE HONEST SIZE IS THE ABSOLUTE: ~0.32 forward builder deaths/game, ~25 Ti/game equivalent at ~78 Ti a builder — the same order as the ~12 Ti barrier surcharge the side lane priced as SMALL on `#31`, and it gets the same standard. `#30` IS A REAL BUT MODEST PLANK.** ⚠ **THE ONE ARGUMENT THAT COULD RESTORE IT IS NOT MEASURED AND MUST NOT BE SMUGGLED IN: whether a forward builder death COSTS MORE NOW than it did then.** At 0.93/game we lose few, possibly at decisive moments, and our median kill r174 against median death r187 is a thirteen-round race in which one surviving raider may be worth more than one of six was in a bot that lost six. **That is a VALUE case, not a COUNT case, and it needs its own number before the row leans on it.** ⭐⭐ **AND THE CONSEQUENCE IS BIGGER THAN THIS ROW: THE TREE'S OWN LOKI-25 JUSTIFICATION IS STALE.** `raid.py:508-527` says *"we are dying almost entirely to the AVOIDABLE one"* on the strength of 92% gunner attribution — **true of the era it was written in (my all-time cut reproduces it at 92.0%) and NO LONGER TRUE OF THE BOT WE SHIP, where sentinels cover MORE forward deaths than gunners do (68.4% vs 64.8%).** *(Consistent with LOKI-25 working: suppressing gunner deaths mechanically raises the sentinel share. The archive CANNOT attribute that — see below — but either way the residual is what a plank must target.)* **(Superseded: 7.6% / 22.7%.)** **DECLARE BEFORE BUILDING: with a 34.9-68.4% treated population — and, per D46, the unit this row concludes in: ~0.32 forward builder deaths/game addressable, 0.63 ceiling — the leg needs a resolution stated up front or it buys an inside-band null** — the same arithmetic that re-specified `#23` and demoted `#29`. ⛔ **AND DO NOT LET THE ROW CLAIM MORE THAN THIS.** The finding is *"our scorer models one of two turret threats and not the more dangerous one"* — **a CODE FACT.** *"This loses us games"* is NOT measured: the observation that raised it is one rated 0-5 loss to LingLing40 where they planted a sentinel at `(9,3)` on **r6**, d²=16 from our core, on the only crossing of an 8-tile corridor on moonrise — **one opponent, one match, and they may simply be strong.** *(Raised by the side lane off a replay Magnus asked about; grep re-verified and precondition measured by research.)* **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.****CARRIED s40 → `_v223sealrepair` (v140): CLASS A** — raid.py facts (threat-scan SENTINEL branch absent, shared attack pr=3 for GUNNER/SENTINEL) carry unchanged (0 diff). **⭐ RE-READ s47 2026-08-16 vs the LIVE CONTROL `_v223sealrepair` (research, on the side lane's `GREP:`-scope audit; every anchor below re-verified here by hand, `ZZQXNOTATOKEN` control = 0/0/0/0 across all four files):** ⚠ **NEAR-MISS — the row survives the LETTER of its claim and a tree read finds a materially relevant omitted fact.** The title says the scorer models the sentinel *"never as a threat"*. **Threat-blindness is FALSE as stated: `main.py:181` and `main.py:386` BOTH read an enemy `SENTINEL` (with `GUNNER`) at `d²≤64` into the threat slots** — identical predicate `if (et in (EntityType.GUNNER, EntityType.SENTINEL) and d <= 64)`. ⇒ **the sentinel IS seen as a threat on the station path; what is genuinely absent is AVOIDANCE — nothing acts on the reading to keep a station off the sentinel's line.** ✅ **The plank survives and gets SHARPER: build the avoidance term, do not build the detection — it is already there and a builder who writes it will find a duplicate.** |
| **27** | **STATUS: NOTE — ⚠ THE DESTROY-TO-PRUNE-SCALE FAMILY. Read this before proposing a fourth.** | **GREP: N/A — provenance row, not a plank.** | n/a | n/a | **Three planks, one mechanism (destroy our own entities to refund their contribution to the global additive cost scale), and the first two died on MAGNITUDE rather than mechanism:** **CARRIED s39 → `_v218mapfix` (v139): ship diff = 2 map-table lines only (valkyrie/glacierkeep grids, side-lane verified); all stamped facts carry over unchanged.** |
| **72** | ⭐⭐ **THE COMMITTED OPENING WAS RETIRED ON A FIXTURE THAT IS NOT ALLOWED TO RETIRE ROADS — AND THE TEAM THAT JUST WENT #1 SHIPPED EXACTLY IT.** One flag, `LOKI2_RUSH_ON`, already written and already wired: inside a bounded r60 window it lifts three gates — harvester prerequisite `LOKI_FWD_MIN_HARV` 2→0, bank floor `LOKI_FWD_TI_FLOOR` 40→8, and the opening roster from seat (0) to `LOKI2_RUSH_SEATS = (0, 1)`. Past r60 the bot is LOKI-1 exactly, so the blast radius is bounded by construction. | **GREP: PASS vs `_v223sealrepair`, and it found the closure as well as the code.** `LOKI2_RUSH_ON = False` at `doctrine.py:1409`; live call sites at `main.py:336` (`LOKI2_RUSH_ON and n in LOKI2_RUSH_SEATS`) and `raid.py:656` (`rush = LOKI2_RUSH_ON and ct.get_current_round() < LOKI2_RUSH_RND`); constants `LOKI2_RUSH_RND = 60`, `LOKI2_RUSH_MIN_HARV = 0`, `LOKI2_RUSH_TI_FLOOR = 8`, `LOKI2_RUSH_SEATS = (0,1)` at `:1409-1413`. ⛔ **AND THE FLAG IS OFF ON REPLICATED ADVERSE EVIDENCE, recorded verbatim at `doctrine.py:1400-1408` — this row is not proposed in ignorance of it:** *2026-08-09, paired deterministic, 360 games, 0 tracebacks, gate CLEARED 12/12* — **orizon `core_kill_share` ALL −15.6pp sign p=0.0201, SHORT −35.4pp p=0.0005; cad ALL −18.9pp p=0.0033, SHORT −22.9pp p=0.0192; LONG band null in both.** The author predicted the opposite and the harm landed exactly where benefit was predicted. **That is strong evidence and this row does not wave it away.** | **turrets planted by r22** (the doctrine block's own specialist bar — *the sub-r80 recipe over 190 games is THREE TURRETS PLANTED BY r22*), and **median round of our first forward sentinel within `d2_enemy <= 8` of the enemy core**. Secondary: median kill round, and `core_kill_share` **on the SHORT band specifically**, because that is where the 2026-08-09 harm was concentrated and it is the cell that must be watched. | **local solo screen n=5,400 vs `_v223sealrepair` (house bar, single arm, no leaderboard-of-maxima) → THEN a live unrated leg.** ⭐ **The live half is the whole point and it is newly possible: `FIXTURE_OF_RECORD: live_unrated`, and Magnus opened unrated on 2026-08-16.** | ⭐⭐ **WHY NOW — THREE REASONS, AND THE FIRST IS A PROGRAMME RULE, NOT AN OPINION.** **(1) THE CLOSURE USED A FIXTURE THAT CANNOT CLOSE ROADS.** `orizon` and `cad` are **our own probes**, and `CLAUDE.md` rule 6 is explicit: *Arena batteries, corpus statistics, source reads and engine probes may PRIORITISE a road. They may not RETIRE one.* The same file records that our probes lie in a known direction — five share a `best_core or best_any` short-circuit, and **zero of our forward turrets died in 480 arena games against 46.9% on the ladder** — and this plank's entire subject is *forward turrets planted early*, i.e. **the exact behaviour the probe fixture is documented to mis-measure.** **(2) IT PREDATES THE MAP ROTATION AND THE HARM WAS MAP-CONDITIONAL.** The measurement is 2026-08-09; **the pool turned over on 2026-08-13 — 18 maps retired, 10 introduced, 15 carried** (`KILL-HAZARD-REDERIVED-2026-08-16.md` §1). The harm was **concentrated on the SHORT band and null on LONG**, so the closure rests on a stratum whose membership has since changed by more than half. **(3) THE #1 TEAM JUST DID IT AND GAINED 306 RATING IN A DAY.** `Jython` (2134, #1, below us three days ago): **first sentinel adjacent to the enemy core median r95 → r28, present in 44.2% → 89.0% of games**, across the v146/v149 boundary that took them 1778 → 2084 (`JYTHON-JUMP-DECODE-2026-08-16.md`). **Our own doctrine block already names this shape and the specialists that run it** (Banminary r52 planting r17 at d²=18, Big O r63 planting r14) **and says of us: *We have the mechanism and we under-commit it.*** | ⚠⚠ **THE COUNTERS, IN FULL, BECAUSE THIS ROW EXISTS TO REOPEN A CLOSED DOOR AND MUST NOT DO SO CHEAPLY.** **(a)** The adverse result is **replicated across two probes at p<0.05 on 360 paired games with the gate cleared 12/12** — by this repo's usual standards that is a good measurement, and what is in question is the fixture's **authority**, not its execution. **(b)** `QUEUE #27` separately observes that on MOONRISE *games 1-4 had their 2nd harvester by r6-r14 and STILL built 0/1/2/1 forward — so the gate is NOT the match-wide problem*, which is direct evidence that lifting the harvester prerequisite may not move the behaviour at all. **(c)** The doctrine block's own mechanism story (*on short maps the tempo was already there, so it pays the economy and receives nothing*) is labelled **UNTESTED by its own author**, so neither the harm's cause nor its transferability to the current pool is established. **(d)** `Jython`'s r95→r28 is **CORRELATED with their jump, not attributed to it** — the same boundary also halved their builders and cut gunners 77%. ⇒ **THE HONEST FRAME: this is a REOPEN-AND-RE-MEASURE row, not a ship-it row. If the local screen reproduces the 2026-08-09 harm on the CURRENT pool, the road closes properly and for the right reason — which is a result worth having either way.** ⛔⭐ **AND THE LICENCE IS NARROWER THAN THE ROW'S OWN REASON (3) MAKES IT LOOK — READ THIS BEFORE CITING THIS ROW.** **Rule 6 governs OPENING a road as well as CLOSING one, and it must be applied symmetrically or this row commits the fault it was written to correct.** *"The #1 team just shipped it"* is **live-field evidence about THEIR bot, not about ours.** ⇒ **THE REOPEN IS LICENSED BY THE INVALIDITY OF THE CLOSURE, NOT BY THE COMPETITOR'S RESULT; THE PLANK STILL OWES ITS OWN LIVE READ.** Those are two different claims and this row is entitled only to the first. **The sibling rule is `CLAUDE.md`'s us-sample rider: our experience of an opponent is not the field's, and an opponent's result is not ours** — and the very probe-fixture asymmetry that voids the 2026-08-09 closure (our probes mis-measure forward-turret survival) equally forbids inferring OUR behaviour from THEIRS. **`Jython`'s r95→r28 is a reason to LOOK, never a reason to BELIEVE.** *(Precision supplied by the side lane at audit; adopted verbatim in substance because the row would otherwise let a later reader quote reason (3) as the evidence base.)* |
| **73** | ⛔ **BELT-CUT REPAIR — AN ENEMY BARRIER ON A CORE DELIVERY SEAT IS A PERMANENT, UNANSWERED CUT, AND IT WAS USED ON US TODAY** *(Provenance: builder's opus agent off Magnus's replay-marker session, 2026-08-16; corroborated by the same day's rated decode, `docs/research/RATED-DAY-DECODE-2026-08-16.md`, in which 0033 v57 is one of the three teams that took 10/45 games off us.)* | **GREP: run vs `_v223sealrepair` (the current incumbent) — RE-RUN BY THE RESEARCH LANE, not banked from the relay. TWO INDEPENDENT GATES CONFIRMED, and they compose into a hole neither one names.** **(a)** `eco.py:626 _l4_repair` — the repair path — is gated at `eco.py:687` on `if not ct.is_tile_empty(g)`, and `is_tile_empty` is *"no building and not a wall"*, so **an enemy BARRIER on the seat can never qualify as repairable.** **(b)** `doctrine.py:1545-1552` — the LOKI-SALT melee carve-out — exempts an enemy **CONVEYOR or SPLITTER** from `LOKI_QUIET_ON` by name, with the reason stated in the comment (*"20 HP = ten pecks… a target where 2 damage a round finishes, which is exactly the property the Core lacks"*). **BARRIER appears in that block only as OUR OWN salt barrier (clause b), never as a target.** ⇒ **the repair path cannot see it and the melee path is not allowed to hit it. A 30 HP / 3 Ti object permanently severs a delivery column.** ⚠ **NOTE THE IRONY AND CHECK IT BEFORE BUILDING: the mechanism is OUR OWN SALT WEAPON pointed back at us — clause (b) exists because a bare cut gets repaired in ~4 rounds, and it works on them for the same reason it works on us.** | **stacks delivered per delivery-seat column, treated-vs-control, on games where an enemy barrier lands on a seat**; success = the barriered column stops reading ~0 for the rest of the game. Secondary: rounds-from-barrier-placement to first delivery on that column. **Field instance to replicate against: `e5d311e9` g2, barrier at r33, ONE stack delivered on that column all game — a LOSS.** | local screen vs the v140 control at standard n, then the head-to-head; **no live window needed** | ⛔ **Why now: it is being EXPLOITED AGAINST US, TODAY, by one of the three teams that account for −72.2 Elo of our rated day.** Cost to them is **3 Ti**; cost to us is **a delivery column for the remaining ~970 rounds.** **Nothing in the tree answers it — this is not a tuning gap, it is an unhandled case.** | ⚠⚠ **DEFENCE CLASS — THIS ROW CARRIES THE `DEFENCE_ADMISSION_BAR` AND MUST NOT BE ADMITTED WITHOUT IT.** Per `PROGRAMME.md`, the primary is **ITT RMST₃₀₀** and the plank is off-programme if it pushes kills past r300. **A repair behaviour that spends builder-turns is exactly the shape that buys survival at the kill's expense**, so the RMST₃₀₀ non-regression is not a formality here. **Register it in the prereg or the row is not startable.** |
| **74** | ⭐ **SEALED-PECK CARVE-OUT — `LOKI_QUIET_ON` SILENCES MELEE TO PROTECT THE BUILDER'S MOVE, AND A SEALED BUILDER HAS NO MOVE TO PROTECT** *(Provenance: builder's opus agent, 2026-08-16, off the replay-marker session.)* | **GREP: vs `bots/_v223sealrepair` — RE-RUN HERE. `doctrine.py:1488 LOKI_QUIET_ON = True` — *"no builder melee: no core peck, no siphon hit, no counterbattery"*. Its stated justification, in the tree's own comment at `doctrine.py:1545-1552`: *"acting and moving are mutually exclusive for a builder and ARRIVAL is the scarce quantity, so pecking a 500 HP Core for 2 damage a round was buying nothing at the price of a step."*** ⭐ **THE PREMISE IS AN OPPORTUNITY COST, AND IT IS ZERO FOR A BUILDER WHOSE FOUR NEIGHBOURS ARE ALL BLOCKED. The carve-out that exists (`CONVEYOR`/`SPLITTER`) is keyed on the TARGET's HP; this one would be keyed on the ATTACKER having nothing else to do — a different axis, and one nothing in the tree tests.** **Not shipped: no site conditions melee on the builder's own mobility.** | **parked-builder attack rate: attacks per parked builder-round, currently measured at 0 across 54,429 parked builder-rounds over 205 games.** ⭐ **A NEAR-PERFECT FALSIFIER BY CONSTRUCTION — the metric must go 0 → >0 or the change did not fire at all, and that is checkable before any win-path question is asked.** Guard metric: win path and RMST₃₀₀ unchanged. **Worked instance: `e5d311e9` g1, a builder idle 110 rounds adjacent to a 500 HP core = 220 damage forgone, free.** | local screen vs the v140 control at standard n | **Why now: the gate is one boolean and the forgone damage is already measured at scale (54,429 parked rounds, 0 attacks — the population is enormous and the current value is exactly zero).** ⛔ **AND IT IS THE CHEAPEST KIND OF CHANGE THERE IS: it cannot cost a step, because it only fires when no step exists.** | ⚠ **NOT DEFENCE — offensive, so no RMST₃₀₀ admission bar. But register the guard anyway: a sealed builder that starts pecking may be sealed BECAUSE it is somewhere it should be leaving, and 2 dmg/round is not a reason to stop wanting out. The falsifier must not be allowed to pass by making stuck builders comfortable.** |
| **75** | **ORPHAN-BELT FACING FIX — WE POP A PLANNED LINK WHENEVER THE TILE IS OCCUPIED, WITHOUT ASKING WHICH WAY THE OCCUPANT FACES** *(Provenance: builder's opus agent, 2026-08-16. Eco correctness, low ceiling, near-zero risk — stocked at that rank, not oversold.)* | **GREP: vs `bots/_v223sealrepair` — RE-RUN HERE AND IT IS TIGHTER THAN RELAYED.** `eco.py:545 _build_next_link`, lines **:556-558**: `occupied = ct.get_tile_building_id(tile) is not None` → `self.link_queue.pop(0); continue`. **The occupant's FACING is never read, so a conveyor pointing the wrong way satisfies the same test as a correct one and the link is silently abandoned.** ⭐ **COMPLEMENT DRIVEN — this is NOT an API-availability problem: `ct.get_direction(bid)` is already called TWICE IN THIS SAME FILE (`eco.py:619`, `eco.py:719`).** ⇒ **the tree knows how to ask and does not ask here.** *(Relay said `v152 eco.py:850-855` / `v140 eco.py:557`; the def is at `:545` and the pop at `:556-558` — same defect, exact anchors corrected.)* | **share of belt tiles that never carry a stack** — census reads **5.9%**, ≈ **2 dead conveyors/game**; success = that share falls with `titanium_collected` non-decreasing. ⚠ **STATE THE CURRENCY HONESTLY: `titanium_collected` is OFF-CURRENCY under `R1000_IS_DEFEAT` — economy buys the kill and never scores. This is admitted as a CORRECTNESS FIX whose channel is wasted builder-turns and wasted Ti, NOT as a win-path plank.** | local screen vs the v140 control at standard n; the census cut is free and already exists | **Why now: it is a one-line facing check against a defect with a measured population (5.9% of belt tiles), and the two-builder collision that causes it is more likely the more builders we run.** ⚠ **Ranked LAST of the three deliberately — lowest ceiling, and it competes for the same screen slots as #73 and #74, both of which have larger measured stakes.** | **⛔ ADMISSION CAVEAT: at ~2 dead conveyors/game the effect may be BELOW the local screen's resolution. Before firing, compute the expected game-share delta and check it against the fixture's MDE — if the screen cannot resolve it, this ships as a reviewed correctness patch on inspection, NOT as a screened arm, and must not consume a screen slot to produce an unresolvable null.** |
| **76** | ⭐⭐ **THE FORWARD-SENTINEL PLANT PATH HAS NO ATTRITION MEMORY — AGAINST A DEFENCE THAT HOLDS, IT REPLANTS ON THE SAME TILE UNTIL THE GAME ENDS. ONE RATED GAME PUT **50 SENTINELS ON TILE (16,1)**.** *(Provenance: research arm, 2026-08-16, `docs/research/KLADDE-CONVERSION-FAILURE-2026-08-16.md`, off the kladde 0/5 flagged in `RATED-DAY-DECODE-2026-08-16.md` §6.1.)* **PLANK:** give `_try_forward_sentinel` a per-tile attrition memory — refuse a build site that has already consumed K of our sentinels (or ban it for N rounds), and fall through to the next raid action if no site survives. **⛔ NOT A DUPLICATE OF `#30` OR `#32`, and the distinction is the whole reason this row exists:** both of those propose a **PREDICTIVE** threat model (compute the union of visible enemy rays and avoid it). This is an **EMPIRICAL** one — *the tile already ate three of ours* — which cannot be wrong about what happened, needs no ray computation inside the 10 ms budget, and survives the failure modes `#32` itself lists as untested (target priority, healers). **They compose; this is the cheap half.** | **GREP: PASS vs `bots/_v223sealrepair` — run here at the primary, not relayed.** `raid.py:636 _try_forward_sentinel` gates on exactly five things — `LOKI_FWD_SENTINEL_ON`, the live cap, `SLOT_HARVESTERS >= LOKI_FWD_MIN_HARV`, `resources >= cost + LOKI_FWD_TI_FLOOR`, and `min(d² to a core tile) <= 50` — then loops cardinals × core tiles and builds at the first `can_fire_from` + `can_build_sentinel` hit (`raid.py:672-691`). **No memory of previous plants, no loss counter, no per-tile record anywhere in the path.** ⭐ **AND THE ONE BRAKE THAT EXISTED WAS DELIBERATELY REMOVED.** `LOKI2B_LIVE_CAP_ON = True` (`doctrine.py:1257`) replaced the monotone `SLOT_FWD_GUN` counter with `_live_fwd_guns()` — a census of sentinels **ALIVE** within `LOKI2B_CENSUS_DSQ = 50` of their core, capped at `LOKI_FWD_GUN_CAP = 3` (`doctrine.py:1237`). **That fix was right — `doctrine.py:1239-1252` documents the old counter locking the arm out permanently after three deaths — and it created the exact opposite failure: a cap on SIMULTANEOUSLY-ALIVE turrets, applied to turrets that die in a median 6 rounds, frees itself every 6 rounds forever. The measured replant gap is 9 rounds — the cap plus one build cycle.** The doctrine block anticipated the lockout failure and not the runaway one. ⭐ **THE PATTERN IS ALREADY IN THE TREE, TWENTY LINES AWAY:** `self.raid_ban` (`main.py:87`; written `raid.py:179,223`; read `raid.py:784`) is a tile-keyed dict with round expiry, used for raid STATIONS and never for the build SITE. ⚠ **Two feasibility constraints the builder must price:** raid state is **per unit** (`main.py:95,101`) and our raiders die at 72–80% in this matchup, so an instance dict partly dies with its owner; and **all 16 store slots are allocated** (`doctrine.py:931-961` plus the LOKI re-pointings at `:1184-1188`), so a team-wide memory needs a repack. `pack_pos` (`eco.py:87`) already packs a tile into an int. | **PRIMARY MECHANISM DIAL: replant share = 1 − (distinct forward-sentinel tiles / forward-sentinel builds), per game.** Incumbent, rated r300+ games: **73% vs kladde** (187 plants on 50 tiles, median 9 rounds apart) against **19–38%** vs everyone else at 64–74 round gaps. Secondary: **forward-sentinel builds per 100 rounds** (3.55 vs kladde, 0.70–1.72 vs the other ten opponents of 2026-08-16) and **Ti spent on forward sentinels as a share of lifetime income** (~4,000–4,500 of ~6,200 in the icefloe game). ⛔ **DISQUALIFIER, registered in advance: the arm must NOT reduce forward-sentinel plants in games where the plant WORKS.** In the 3 kladde games we won the incumbent used 5.3 plants at 31% replants; median across all 115 rated games of 2026-08-16 is **2 plants/game** and p75 is 3, so any brake calibrated above ~6 should be invisible in the median game. **A drop in the timely-kill rate (core kill by r300) is an immediate fail** — this row is a waste-removal plank and must not cost a kill. | Local corefill screen vs `_v223sealrepair` for **non-regression only** — ⚠ **and state why: our own control bot is not a holding sentinel-wall defender, so the local fixture will barely express the mechanism** (self-play blindness, `docs/builder-arm-retro.md`). **A local NULL is therefore expected and is NOT evidence against the plank; a local NEGATIVE is disqualifying.** The mechanism read needs a **pinned live unrated leg against kladde v119** — ⛔ **which is BLOCKED until `LEG-fieldcal-2026-08-16` closes, because kladde is one of its ten panel cells.** Cheap interim instrument, already written and free: the replant-share cut off `corpus/events.tsv` re-run on the next rated day. | **WHY NOW — it is STRUCTURAL, not tuning, and it is measured across 191 games and fourteen bot versions.** Rated lifetime vs kladde **27/80 = 0.338**; unrated pre-clock2 **36/111 = 0.324** across **six of their versions (v80–v97) and eight of ours (v85–v143)**; the pooled unrated contrast is **0.324 (n=111) vs 0.485 (n=1,910), hw95 = 0.115, EXCLUDES ZERO** at within-opponent/pooled unrated DEFF. **All six mechanism signatures replicate out-of-sample** — replant share 61% vs 45%, our HP lost/100r 547 vs 236, their HP lost/100r 92 vs 163, exchange 5.9:1 vs 1.4:1, builder deaths 69% vs 23% (theirs 14% vs 23%). ⇒ **not a v119 counter, not a today artefact: a persistent matchup property that has survived seventeen of their ships and eight of ours.** The size of the waste: in the flagged rated 0/5 (`05:52Z`, all five losses at r322–r746) we lost **88 entities per game and they lost 1.4**; our alive-unit count sat at **7–8 from r50 to r700** while theirs went **8 → 26**. **Our forward sentinel is our ONLY kill vector — `batk_core` is 0 for us in 400 of 400 archived rated games — and against kladde 99% of them die in a median 6 rounds.** ⚠ **THE HONEST COUNTER-EVIDENCE, and it is why the brake must be per-TILE and not a per-game budget.** Simulated on the archive: a **global lifetime budget** of K=10 plants would have bound in 7/115 games on 08-16 (we won 0) but in 6/360 on 08-15 **of which we WON 4 (0.67)** — a blind budget clips wins. A **per-tile cap of 3** binds in 11/115 (08-16, won 3 = 0.27) and 5/90 v152 games (08-15, won 3 = 0.60); pooled over both v152 days it binds in 16 games we won 6 of (0.375) against a ~0.53 baseline. **Below-average games, but the 08-15 arm is not clean and this row does not pretend it is.** ⚠ **AND ONE OF MY OWN HYPOTHESES DIED HERE, which strengthens `#32` rather than this row:** I predicted the plant sites sat in a vision blind band (builder r²=20 < sentinel r²=32). **Refuted — 95% of our kladde plants have a live enemy sentinel at d² ≤ 20, i.e. VISIBLE.** The information `#32` wants to read is available; we simply never read it. |
| **77** | ⭐⭐ **COMPOSE THE CONFIRMED, DON'T MIX THE UNTESTED — THE BOARD SAYS BLIND CONJUNCTION RAISES THE MEAN AND *SHRINKS* THE TAIL, WHICH IS THE OPPOSITE OF WHAT REACHING A BAR NEEDS** *(Provenance: `docs/research/SHIP-BAR-REACHABILITY-2026-08-16.md` (59 arms, HEAD 97669852) x the BODYAWR KEEP verdict typed 08:53:48Z. Requested by the builder; written here because the reachability cut is what makes it falsifiable rather than a preference for tidiness.)* | **GREP: run vs `_v223sealrepair` (the current incumbent) — ALL THREE COMPONENTS CONFIRMED ABSENT, run here, not relayed.** `BODYAWARE`/`bodyaware`/`body_aware`: **0 hits** in the incumbent tree (present at `bots/_v242bodyaware/eco.py:816,830,841,843,856` as `# BODYAWARE (#63)`, i.e. a treatment vs this control, not shipped). Per-tile plant ban (`plant_count`/`sentban`): **0 hits** in `raid.py`; `bots/_v330sentban` exists on disk and is being built now, so #76's mechanism is NOT in the control. Seat fix (`SEAT_FIRST`/`SEATSPAWN`/`seat_asym`): **0 hits**. ⇒ **three genuinely-absent changes; no part of this is a leg testing what we already ship.** | ⭐ **THE DISCRIMINATING METRIC IS NOT THE MIX'S SHARE — IT IS WHETHER *CONFIRMED-COMPONENT* COMPOSITION BEHAVES DIFFERENTLY FROM THE BLIND-MIX POPULATION.** Board fact to beat: blind conjunctions raise mu **+3.26pp** and **SHRINK tau 2.20 -> 2.00**; 2-plank composition is additive and TRIO realises **79.9%** of the sum. **PRE-REGISTER THE ADDITIVE PREDICTION AND SCORE AGAINST IT, NOT AGAINST 50** — components' confirmed stock (bodyaware **+3.70**, screen-measured 53.70 [52.76, 54.64] at n=10,801, plus whatever #76 reads) times the 79.9% realisation is A NUMBER, written down before the screen. ⛔⛔ **SEAT COMPONENT DROPPED 2026-08-16 ~10:0xZ — OWNER'S CALL (research), on a side-lane pre-lock flag. THE COMPOSITE IS TWO COMPONENTS: `bodyaware` + `#76`.** **Reason 1, and it is the row's own title: the only available seat number is `results.tsv:412 seatfull-autostop-1000, 48.48% [45.54, 51.41], n=1,116, type CANCELLATION`. Under-powered, CI spanning 50, and TYPED A CANCELLATION — and this repo's rule is that a cancellation is NOT a verdict.** A component whose contribution is unknown-sign is BY DEFINITION not confirmed, so including it contradicts *"compose the CONFIRMED, don't mix the untested"* in the row's own heading. **Reason 2: the objective is not yet chosen.** Research's 09:5xZ amendment to `SHIP-BAR-REACHABILITY-2026-08-16.md` establishes that **"remove the asymmetry" and "capture the better seat's behaviour" are DIFFERENT OBJECTIVES WITH DIFFERENT SIGNS**, and only the first is built — equivariance is direction-free, and `A%+B% ≡ 100` makes a zero differential arithmetically automatic (`SEATFULL`: differential +0.09pp [−5.76,+5.95] while reading 48.53%, i.e. **both seats equalised downward**). **A prereg cannot name "the seat fix" until the objective is named.** **Reason 3, why the alternative resolutions were declined: (1) "give it a completed read at proper n" is BLOCKED — there is NO incumbent identity null (zero shards with treatment == control == `_v223sealrepair`), so a T−C differential is `2(a_T+a_C)` and CANNOT separate "neutral" from "over-corrected" however large n gets. (3) "register it UNKNOWN-SIGN" preserves the arm but destroys the row's discriminating power exactly where it is needed: a composite landing BELOW prediction could not be told apart from the seat arm SUBTRACTING** — the side lane named this hazard themselves when offering the option. ✅ **PATH BACK, so this is a deferral and not a closure: re-add the seat component when (i) an incumbent identity-null shard exists, and (ii) a DIRECTIONAL seat variant — one that captures the better seat rather than equalising — has a completed, verdict-grade read. Both are tracked in `#8`.** ⛔⛔ **WITHDRAWAL CONDITION — REGISTERED 2026-08-16 ~10:1xZ, BEFORE `#76` READ, WHICH IS THE ENTIRE POINT OF REGISTERING IT** *(research as owner + side lane as certifier, `0072281d`)*: **#77 requires AT LEAST TWO components each carrying a VERDICT-GRADE read. If `#76` does not produce one, #77 WITHDRAWS ITSELF. It does not proceed as a single-component row, because a one-component composite is not a composite.** **VERDICT-GRADE IS DEFINED SO NOBODY HAS TO JUDGE IT LATER: a `results.tsv` row typed `keep` or `refuted` AT ITS REGISTERED n. A cancellation, an auto-stop, a futility trip, or any read whose CI spans the bar is NOT verdict-grade** — the same rule already applied to BODYBLK, to SEATFULL, and to this row's own dropped seat component. ⭐⭐ **AND THE HALF THAT KEEPS THIS HONEST: WITHDRAW ON GRADE, NEVER ON SIGN.** **If `#76` reads verdict-grade and NEGATIVE, #77 SURVIVES.** A confirmed-positive composed with a confirmed-negative is a legitimate composite: the additive prediction simply carries a negative term, and *"does the composite beat its own prediction"* remains a real question with a computable answer. ⛔ **Withdrawing on a negative would be SELECTION ON THE OUTCOME — it would let this row survive only when its components cooperate, which is exactly the bias the row exists to test against.** ⇒ **THE TRIGGER IS ONE QUESTION: *can an additive prediction be computed from MEASURED contributions?* A verdict-grade negative answers YES.** ⚠ **Why registered now and not at lock: if `#76` reads WELL but lands as a CANCELLATION, the number will look usable, the row will look ready, and the alternative will be losing the slot. That is precisely the moment a rule stops being followable — so it is written while nobody knows the answer.** ⛔⛔ **POLICY COLLISION, FOUND 2026-08-16 ~10:3xZ BEFORE IT FIRED — THIS ROW NEEDS TWO EXPLICIT ANNOTATIONS OR IT WILL BE CANCELLED FOR LANDING ON TARGET.** `COMBO-BAR@2700` (new, Magnus-approved) auto-cancels any COMPOSED tree reading **< 55.0** at the 2,700 mark. **#77 IS A COMPOSED TREE. Its registered success criterion is NOT a level — it is *beats its own ADDITIVE PREDICTION*.** Computed: `prediction = 50 + 0.799 × (3.70 + d76)`, so **if `#76` reads +1.0pp the prediction is 53.76; +2.0pp → 54.55 — BOTH BELOW THE 55.0 BAR.** ⇒ **the row would be auto-cancelled at 2,700 while sitting EXACTLY on its registered prediction, and a cancellation is not verdict-grade, so its question would go UNANSWERED rather than answered negatively.** ⭐ **The bar is a PROSPECTING filter (*is this combo worth pursuing*); this row is a registered MECHANISM test (*does confirmed-component composition differ from blind mixing*). Different purposes — the filter should not apply.** ⇒ **ANNOTATION 1 REQUESTED: exempt #77 from `COMBO-BAR`, or the row cannot be run as designed.** ⛔ **ANNOTATION 2 — THE MDE, WHICH IS THE HARDER PROBLEM AND MUST BE REGISTERED BEFORE THE LOCK: the additive prediction is ITSELF ESTIMATED, so the test is composite-vs-prediction and needs the variance of the DIFFERENCE, not the composite's own CI.** With bodyaware banked at n=10,801 (SE 0.481) and the others at n=2,700 (SE 0.962): **prediction SE 0.86pp · composite SE 0.96pp · difference SE 1.29pp ⇒ MDE ±2.53pp.** At full n=5,400 throughout: **difference SE 0.95pp ⇒ MDE ±1.87pp.** ⚠ **TRIO realises 79.9% of additive — a ~20% shortfall — so the super-additive effect being hunted is plausibly only a few pp, and ±2.53pp MAY NOT RESOLVE IT.** ⇒ **ANNOTATION 2 REQUESTED: run #77 at CONFIRMATION-GRADE n=5,400, and register the MDE explicitly. A prospecting-n composite that cannot resolve its own registered effect is the underpowered-leg failure this repo has already banked twice.** ✅ **BOTH ANNOTATIONS DELIVERED 2026-08-16 ~10:4xZ (builder).** **(1)** The exemption is a REGISTERED CHANNEL, not a promise: a `BARS.tsv` row carrying the literal token `COMBO-BAR-EXEMPT` beside its prereg citation is skipped, **and the skip PRINTS as its own clause — typed, auditable, never silent.** Selftest drives it both ways: the exempt cell CONTINUEs with the exemption named; **its tokenless twin at identical numbers still STOPs.** **(2)** #77 runs at **confirmation-grade n=5,400** and **the prereg registers the DIFFERENCE-MDE (±1.87pp)** — so the underpowered-leg failure cannot be stumbled into. ⚠ **ONE RESIDUAL I AM REGISTERING RATHER THAN LEAVING TO USE: A TOKEN THAT SKIPS A GUARD IS THE SILENCER SHAPE, AND THIS ONE IS CURRENTLY UNVERIFIED.** The token is paired with a prereg citation by CONVENTION; nothing checks that the cited prereg EXISTS or that it registers a non-level criterion. ⭐ **The fix already exists in this repo, built this morning: `queue_check`'s `GREP-PATH` exemption VERIFIES the named path is present and fires `BROKEN-EXEMPTION` — LOUDER than no exemption — when it is not, on the principle that claiming exemption against something that does not exist is strictly worse than claiming nothing.** ⇒ **`COMBO-BAR-EXEMPT` should resolve its prereg citation the same way.** **Both verdicts are drivable at build time: a token citing a real prereg CONTINUEs; a token citing a missing one must be LOUDER than the plain stop.** Routed to the builder, not blocking #77. ⛔⛔ **RULING 2026-08-16 ~10:5xZ — ASKING FOR n=5,400 WAS CORRECT AND INSUFFICIENT: IT FIXES THE WRONG BINDING CONSTRAINT. Caught by the side lane, and MY OWN REGISTERED NUMBERS CONTAINED THE PROOF — I computed the MDE and never compared it to the PREDICTED EFFECT SIZE.** Re-derived here; all figures reproduce. **The prediction ALREADY carries the 79.9% haircut, so super-additivity is the gap between 79.9% and actual realisation: `d76=+2.0` at full additivity = `+1.15pp`; at 90% realisation = `+0.58pp`; `d76=+1.0` at full additivity = `+0.94pp`. THE EFFECT THIS ROW HUNTS IS ~1pp.** Against it: **MDE ±1.87pp at n=5,400 · ±1.42pp at 10,800 · ±1.14pp at 21,600 — only the last resolves it, and barely.** ⇒ **SUPER-ADDITIVITY IS NOT ANSWERABLE AT ANY n THIS PROJECT WILL SPEND.** ✅ **RE-SCOPED PRIMARY (owner's call — the side lane's option 2, COMPUTED rather than adopted on assertion): *DOES THE COMPOSITE BEAT ITS BEST SINGLE COMPONENT* (`bodyaware` 53.70, banked n=10,801)? MDE ±2.11pp at n=2,700 · ±1.63pp at n=5,400 · ±1.33pp at 10,800.** ⭐⭐ **AND ITS RESOLVABILITY IS CONDITIONAL ON `#76` — KNOWABLE BEFORE FIRING — SO IT IS REGISTERED AS A GO/NO-GO INSTEAD OF DISCOVERED AFTER: if `#76` reads verdict-grade at ≥ +2.0pp, the re-scoped effect is ≥ +2.00pp and n=5,400 RESOLVES IT (2.00 > 1.63). If `#76` reads below ~+2.0pp, the effect is ≤ +1.0pp, NO feasible n resolves it, and the row runs DESCRIPTIVE ONLY — registered UNDERPOWERED-BY-DESIGN, no exclusion claim.** ⛔ **NOT CLAIMED UNDER EITHER BRANCH: super-additivity. The row can say whether composition BEATS ITS BEST PART; it cannot say whether composition is ADDITIVE or SUPER-additive, and any such sentence in its readout is out of scope BY REGISTRATION.** ⚠ **Inadmissible — and the failure this repo has banked twice — is running at 5,400 against the ORIGINAL question and reporting the null as informative. The row's own registered numbers indict that plan, which is the good news: the prereg was written detailed enough to check itself.** ⛔⛔ **CORRECTION TO THE RE-SCOPE, 2026-08-16 ~11:0xZ — THE GO THRESHOLD I JUST REGISTERED ASSUMED THE ANSWER TO THE ROW'S OWN QUESTION. Caught by the side lane. THE FIX INHERITED THE AMBIGUITY OF THE THING IT CORRECTED.** I set GO at `d76 ≥ +2.0pp` on the identity `effect = d76`, **which holds ONLY at 100% realisation — and 79.9% is the board figure this row is built on.** Re-derived (`effect = realisation × (3.70 + d76) − 3.70`): **`d76=+2.0` → +2.00pp full-additive but +0.85pp at 79.9% · `d76=+2.5` → +1.25pp · `d76=+3.0` → +1.65pp.** Against **MDE ±1.63pp at n=5,400** ⇒ **my GO branch would have FIRED at d76=+2.0 with an expected effect of +0.85pp against ±1.63 — spending 5,400 games on a question it cannot resolve, immediately after being re-scoped to prevent exactly that.** ⭐ **A GO/NO-GO THAT ASSUMES THE ANSWER IS NOT A GO/NO-GO.** ✅ **CORRECTED THRESHOLD, WITH ITS ASSUMED REALISATION REGISTERED SO NOBODY RE-DERIVES WHICH NUMBER IS MEANT: GO REQUIRES `d76 ≥ +3.0pp`, COMPUTED AT THE BOARD-MEASURED 79.9% REALISATION** (exact crossing +2.97pp at n=5,400; +2.60pp at n=10,800). **Powering at the optimistic realisation would make an underpowered branch look fireable, so the threshold is set at the PESSIMISTIC one — if the effect is only visible under the assumption we are testing, we cannot tell a null from a blind spot.** ⚠⚠ **AND THE HONEST CONSEQUENCE, REGISTERED NOW SO IT IS NOT A DISAPPOINTMENT LATER: NO-GO IS THE MODAL OUTCOME. A COMFORTABLE read (effect ≥ 1.5 × MDE) needs `d76 ≥ +4.0pp` at n=5,400 — and `bodyaware`, THE BEST CONFIRMED PLANK THIS PROJECT HAS EVER PRODUCED, IS +3.70pp.** ⇒ **this row most likely runs DESCRIPTIVE-ONLY, and that is the branch working rather than failing: it declines to spend games it cannot learn from.** ⭐ **The branch structure is unchanged; only the number on the condition moved, and it moved in the direction that spends less.** **Success = the composite BEATS its own additive prediction (super-additive) — the tail actually fattens. A failure that still teaches = it lands ON the prediction, confirming composition is bookkeeping and closing this road.** | local screen vs the fixed v140 control at standard n (±1.31pp at p=0.6, sufficient to resolve a super-additive claim of the size at issue); then the head-to-head gate, **still UNMEASURED** and the half of the pipeline nobody has read | ⛔ **Why now: the confirmed single-plank stock is HARVESTED.** Only **2 of 26** single planks have a CI above 50 (bodyaware, rnd1 +1.61); additive stock **+5.71pp** against a leader already at **55.24**; and **0 of 59 arms have EVER measured ≥60, or even ≥58.** ⇒ **more single planks is not a route to the bar, and Magnus has named 70 as the real target.** **The components are being built ANYWAY** (#76 in flight as `_v330sentban`, seat rungs queued), **so the composite costs one screen slot and no new mechanism work.** | ⚠⚠ **THE PRIOR IS AGAINST THIS ROW AND IT IS STOCKED ANYWAY — THE PREREG MUST SAY SO OUT LOUD.** The same cut that motivates it predicts composition moves the CLOUD, not the TAIL, so the modal outcome is *lands on the additive prediction*. **Admitted because it is the ONE composition hypothesis the board has not already refuted** (every prior MIX was blind; this one is confirmed-component) **and because a clean on-prediction null CLOSES the road rather than leaving it a standing hope.** ⛔ **NOT a licence for another blind MIX sweep — the blind version is already refuted and re-running it would be the cheapest null on the board.** ✅ **INPUT FIGURE RESOLVED 2026-08-16 (builder's call, as verdict owner): the additive prediction is built from the KEEP SCREEN's 53.70 [52.76, 54.64] at n=10,801 — NOT the reachability cut's +4.10.** Larger n, and it is the figure that cleared a REGISTERED bar on the tape, so the prediction inherits an auditable input. ⚠⚠ **SECOND PRE-LOCK ITEM, RAISED BY THE SIDE LANE AND OPEN — A FIXTURE-NOISE QUESTION, NOT A DISCREPANCY.** The additive arithmetic assumes each component's read and the composite's read sit on a fixture with the SAME NOISE STRUCTURE. **The seat component may break that, and it is the one place with a visible mechanism: if the +6.28pp seat asymmetry is real on BYTE-IDENTICAL self-play, the current screen carries a SEAT TERM IN ITS VARIANCE, and a fix that removes that term does not only shift the mean — it changes the screen's own DISPERSION.** The composite would then be measured on a QUIETER fixture than its components were, and 'composite vs additive prediction' would compare two numbers whose half-widths were built on different noise. **Three resolutions and NOBODY KNOWS WHICH:** (1) the screen already balances seats per arm, the term cancels in aggregate, one line says so; (2) it does not cancel, so the seat fix's own read must be taken on the POST-FIX fixture — in which case bodyaware's +3.70, measured PRE-FIX, is not additively commensurable with it; (3) something unforeseen. ⇒ **REQUIRED PREREG CLAUSE: state whether the seat fix changes the screen's noise structure, and if so, WHICH FIXTURE EACH COMPONENT'S CONTRIBUTION IS DENOMINATED IN.** ⛔ **Same species as the input-figure item: if the prediction's inputs come from two fixtures, the bar is unfalsifiable by construction for a SECOND reason.** |
| **78** | ⭐⭐ **BELT TOPOLOGY IS PLANNED PER-HARVESTER, NEVER GLOBALLY — `_link_path` SEEDS ONLY THE CORE RING, SO EVERY HARVESTER ROUTES HOME ALONE AND TRUNKS ARE NEVER JOINED ON PURPOSE. THE CURRENCY IS SCALE, NOT TITANIUM.** *(Provenance: MAGNUS'S OWN REPLAY MARKERS, match `59f4d2bc` g3, our v140 vs HTTP 418 — (9,6) r20 "a conveyor here would have saved us 2 conveyor and build rounds for a builder"; (2,8)-(2,10) r33 "would have saved 2 conveyors", a 3-tile vertical shortcut against the L-route actually built. ⚠ HTTP 418 is a LEG-fieldcal panel cell: BELT-TOPOLOGY OBSERVATION ONLY, no arm contrast read or implied.)* | **GREP: run vs `_v223sealrepair` (the current incumbent) — I READ THE PLANNER RATHER THAN INFERRING FROM THE SCREENSHOT, AND IT SETTLES WHICH OF THE TWO CANDIDATE MECHANISMS IS LIVE.** ⭐ **MECHANISM (a) CONFIRMED, DECISIVELY: `eco.py:391 _link_path` builds `raw_goals` from `core_tiles(self.core)` × `CARDINALS` AND NOTHING ELSE — the goal set IS the core ring. Existing friendly conveyors/splitters appear only in the BLOCKED calculation, where they are deliberately NOT blocked (`:415-419`, an enemy belt IS blocked, ours is not) — so a route may pass THROUGH an existing belt but can never TERMINATE at one.** ⇒ **THE PLANNER HAS NO CONCEPT OF JOINING A TRUNK. Every harvester solves its own shortest path to the ring, independently, forever.** ⭐ **AND THIS RESOLVES THE BUILDER'S OPEN QUESTION: the one merge their forensics saw (the (11,10) West-facing junction) is an ACCIDENTAL OVERLAP of two independent routes through a non-blocking tile, NOT a planned merge. Partial-merge hypothesis: DEAD.** **MECHANISM (b) ALSO UNSHIPPED AND SEPARATELY BLOCKED: `ct.destroy(`/`can_destroy(` = ZERO call sites in the whole tree (excluding `self_destruct`) — confirming #60's fact. We never demolish, so belts laid before a later trunk existed can never retro-merge.** | ⭐ **THE METRIC IS SCALE, AND SAYING SO IS THE POINT: every conveyor is +1% on the cost of EVERY SUBSEQUENT BUILD, FOREVER.** Primary = **conveyors built per game** and **`get_scale_percent()` at a fixed round (r150 and r250)**, treatment vs control; success = **fewer conveyors for equal-or-better delivery, hence a LOWER scale factor at the kill window.** ⛔ **NOT `titanium_collected` — that is off-currency under `R1000_IS_DEFEAT`. The channel is that a lower scale makes the TURRETS AND LAUNCHERS THAT DO THE KILLING CHEAPER**, which is the #60 revival's own arithmetic and the only reason this is a kill-path row rather than an eco-tidiness row. Secondary: total belt tiles, and the (a)-specific counter — **planned merges per game, currently ZERO BY CONSTRUCTION.** | local screen vs the fixed v140 control at standard n; the topology census (conveyors/game, scale at r150/r250) is a FREE corpus cut on `builds.tsv` that should be run FIRST to size the prize before a tree is written | ⛔ **Why now: Magnus marked it himself with two concrete instances, and the code read says the defect is STRUCTURAL rather than a tuning miss — there is no merge logic to tune.** And the prize is denominated in the currency that is currently binding: **we are 4.76pp below the ship bar with the confirmed single-plank stock harvested, so a plank whose channel is CHEAPER KILL HARDWARE is exactly the shape the reachability cut says we need.** | ✅⛔ **CENSUS RETURNED 2026-08-16 ~13:1xZ — PRIZE SIZED, AND IT CORRECTS THIS ROW'S OWN CURRENCY CLAIM.** *(`docs/research/BELT-TOPOLOGY-CENSUS-2026-08-16.md`, `tools/belt_merge_prize.py`; population = every `join.tsv` row with `ourver==140`, **360 games, 0 dropped**, ladder-only, seat agreeing with `meta_join` 4,185/4,185.)* **PRIZE: greedy online counterfactual on real build order and real geometry saves 8.91 conveyors/game (calibrated 8.07) = 24.9% of the belt.** ⭐ **Offline Steiner bound 9.07 — GREEDY CAPTURES 98% OF THE OPTIMUM, so there is NO cleverer version worth building and the row must not grow one.** Big maps 11.34 vs small 1.48. **AT THE KILL WINDOW: r150 −7.22pp scale, 78.2 Ti freed (median 50.5) ⇒ 1.25 extra gunners mean / 0.90 median; 47% of games buy a whole gunner, 29% a whole sentinel.** ⛔⛔ **AND HERE IS THE CORRECTION TO MY OWN FRAMING, WHICH I WROTE EMPHATICALLY AND WAS MOSTLY WRONG ABOUT: I registered the currency as *"SCALE, NOT TITANIUM"*. THE CENSUS SAYS ONLY ~13 OF THE 78 Ti IS THE SCALE CHANNEL — the other ~65 is simply NOT PAYING FOR 7.2 CONVEYORS AT 9 Ti EACH (a conveyor costs 9 Ti at our modelled 304% scale, not 3).** ⇒ **the dominant channel is DIRECT TITANIUM NOT SPENT; scale is the minority term at ~17%.** ✅ **The row SURVIVES — freed Ti buys the same kill hardware and we demonstrably do not bank it (`ti_end` median 44 Ti at r150) — but the MECHANISM I insisted on was wrong in proportion, and a prereg quoting the scale channel as the reason would be measuring the small half.** ⛔ **COUNTER-COST THE CENSUS FOUND AND THE ROW DID NOT ANTICIPATE — IT CHANGES THE FALSIFIER: a conveyor moves 1 stack/round and a harvester emits 1 per 4, so >4 lines through a tile SATURATES. Merging takes games over that threshold from 76/360 to 126/360** and cuts distinct belt entries 3.3→2.3. ⇒ **MERGING BUYS CHEAPNESS AND SELLS THROUGHPUT.** ⭐ **THE BUILD REQUIRES A SATURATION GUARD, AND THE FALSIFIER MUST BE DELIVERY / KILL-ROUND — NOT CONVEYOR COUNT. A screen scored on conveyors saved would pass a change that starves the core.** ⚠ **VERDICT: REAL BUT MARGINAL — ~1 extra turret at the kill window (band 0.8–1.3), and in UNDER HALF of games individually. Not a decisive negative; it clears in the mean.** Out-of-sample `v104` (n=510) saves 5.33 ⇒ **the prize is ~1.7× POOL-DEPENDENT.** ⇒ **BUILD IT AS A ONE-FUNCTION GOAL-SET EDIT PLUS THE GUARD, DOSED ON BIG MAPS. Anything larger is not paid for by this number.** ⛔⛔ **UNCONVERTED — REGISTERED AT THE ARTEFACT 2026-08-16 ~13:2xZ ON A SIDE-LANE FLAG, SO WHOEVER PICKS THIS UP KNOWS THE D27 CHECK HAS NOT BEEN PERFORMED RATHER THAN DISCOVERING IT AT PREREG TIME: THIS ROW'S PRIZE IS PRICED IN TURRETS AND COST-SCALE; D27'S BAR IS DENOMINATED IN GAME SHARE. DIFFERENT UNITS — the row CANNOT be checked against an MDE until someone converts, and nobody has.** ⚠ **The gap's SIZE is stated rather than guessed: one extra gunner against a tree building 1.86 gunners/game (`#21`) is a +54% TURRET COUNT — materially worth buying, or noise, AND NOBODY HAS A BASIS TO SAY WHICH.** That makes the conversion a real analysis, not an arithmetic step. ⭐⭐ **AND THE CONVERSION QUESTION IS ANSWERED: IT NEEDS GAMES, BUT THE GAMES ARE ALREADY QUEUED.** **(1) A CORPUS CUT CANNOT SUPPLY IT.** Turret count and winning are confounded IN BOTH DIRECTIONS — winners have more Ti to spend on turrets, losers may build them defensively. **An observational association is not the causal quantity an MDE needs, and no amount of corpus substitutes for it.** **(2) A CAUSAL READ EXISTS AND IS ALREADY A ONE-FLAG QUEUED ARM: `#23`'s `LOKI_FWD_GUN_CAP = 3` (`doctrine.py:1237` — ⚠ the line has DRIFTED from the `:1219` recorded in `#23`), a SOFT MONOTONE cap on forward turrets alive.** Raising it is a **direct dose of "more forward turrets"**, and its screen reads **GAME SHARE**. ⇒ **the conversion costs ONE ALREADY-TAGGED ONE-FLAG BUILD, not a new instrument.** ⭐ **(3) AND IT IS A CONSERVATIVE BOUND, WHICH IS WHAT MAKES IT USABLE RATHER THAN MERELY ADJACENT: the cap arm buys extra turrets by SPENDING MORE Ti, displacing whatever that Ti would otherwise have bought. `#78` buys them with Ti it SAVED and gives up NOTHING.** ⇒ **the cap arm's share-per-turret UNDERSTATES `#78`'s. If `#23` reads POSITIVE, `#78`'s conversion is AT LEAST that. If `#23` reads FLAT OR NEGATIVE, `#78`'s prize does not convert and the row should not be built — a decisive negative for the price of a flag flip.** ⛔⛔ **TWO CORRECTIONS TO THE CONVERSION ROUTE, 2026-08-16 ~13:4xZ, BOTH FROM THE SIDE LANE AND BOTH TO TEXT I WROTE MINUTES EARLIER. The bound argument itself is CHECKED AND SOUND** (`Δ_cap = V(turrets) − V(displaced)`, `V(displaced) ≥ 0` ⇒ `V(turrets) ≥ Δ_cap`, erring safe; mild assumption named — the marginal displaced build must not be NET-HARMFUL). **(C1) THE ARM DOSES SENTINELS, NOT GUNNERS — AND I QUOTED THE LINE THAT SAYS SO.** `doctrine.py:1237` reads `LOKI_FWD_GUN_CAP = 3   # forward SENTINELS alive at once (soft, monotone)`. ⛔ **Despite the NAME it caps SENTINELS, and I pasted the comment while writing "more forward turrets" and wiring it to a GUNNER headline.** Gunner vs sentinel is 20 vs 30 Ti, dmg 7 vs 18, r²13 vs 32, 4 vs 10 ammo/shot — **converting the gunner headline through a sentinel dose OVERSTATES.** ✅ **THE FIX IS ALREADY IN THE CENSUS: the conversion runs on the SENTINEL row — 0.83 mean / 0.60 median, 29% of games buy a whole one — NOT the 1.25/0.90/47% GUNNER headline a prereg would naturally reach for.** **(C2) A SOFT CAP MAKES "FLAT" AMBIGUOUS, AND MY BRANCH WAS WRITTEN AS DECISIVE.** Confirmed soft and monotone at `raid.py:654` (`>= LOKI_FWD_GUN_CAP` ⇒ decline to build): **raising it PERMITS more, never FORCES more.** ⇒ **my registered branch — *"#23 flat or negative ⇒ #78's prize does not convert and the row should not be built"* — IS UNSOUND AS WRITTEN**, because a flat read is ambiguous between *"sentinels do not pay"* and *"the cap was never binding."* ⭐ **CORRECTED BRANCH: a flat read is decisive against `#78` ONLY WITH A MANIPULATION CHECK — did the arm actually build MORE forward sentinels than control, read off the arm's own tape? A flat read WITHOUT a confirmed dose is an INERT FLAG, NOT A NULL.** ⚠ **AND THIS IS THE DOSE-vs-MECHANISM DISTINCTION I REGISTERED ONE ROW OVER ON `#76` (*"K=2 is a DOSE, not the mechanism; a K=2 null does not refute attrition memory"*) — needed again here, and I did not carry it across.** It bit only because I wrote the branch as DECISIVE; a softer wording would not have required it. ⇒ ⛔ **RANK CONSEQUENCE UNCHANGED BY EITHER CORRECTION: DO NOT BUILD `#78` BEFORE `#23` READS. Its real cost is the cap arm PLUS its own arm — and now also the manipulation check, which is free off the same tape.** **CONTROLS, all able to fail: C1 geometry 360/360 · base-arm calibration predicts 93.7% of observed belt tiles TILE-FOR-TILE · C2 single-harvester games 0/5 show a saving (the model cannot invent one) · C3b mirror case 6→4 proves it CAN find one · C4 shuffled tie-break 10.1–10.9 vs deterministic 9.65, so our tie-break is CONSERVATIVE · 61/360 games return exactly zero.** | ⚠ **ADMITTED AS MECHANISM (a) ONLY — Steiner-ise new lines into existing trunks by seeding `raw_goals` with our own live belt acceptors. (b) CONSOLIDATE-AFTER IS A DISTINCT PLANK AND IS BLOCKED ON #60'S DEMOLITION CALL; it is NOT stocked here, because stocking a row blocked on another row is how a queue inflates.** ⛔ **AND ONE HAZARD THE ROW MUST CARRY: seeding trunk tiles as goals makes routes SHARE tiles, so a single cut severs MORE harvesters at once. That is the `#73` belt-cut exposure multiplied, and the screen must read delivery under attack, not only in clean games.** ⚠ **Adjacent rows checked and none covers this: #75 is the facing bug (dead segments), #50 is terminus-first (latency ordering), #66 is the multi-builder haul split (labour), #60 is demolition (the enabler for (b)). Global topology is unowned.** |
| **79** | ⭐⭐ **THE PLANT-AND-GUARD INFILTRATOR — ONE BUILDER, PERMANENTLY UNKILLABLE BY ANY TEAM THAT BUILDS NO TURRET, OBSERVED WINNING A GAME. AND WE OWN THE ONLY CHEAP COUNTER IN THE LEAGUE.** *(Provenance: builder's decode of the I Stone / Dino study game, 2026-08-16. Dino's builder walked 23 tiles at r3, planted a sentinel, sat DORMANT 304 ROUNDS, then spent r332-774 as a standing saboteur — **16 of I Stone's 29 conveyor deaths from that one unit**.)* | **GREP: run vs `_v223sealrepair` (the current incumbent) — NOTHING LIKE IT IS SHIPPED, IN EITHER DIRECTION.** `infiltrat`/`plant-and-guard`/`saboteur`: **0 occurrences in QUEUE.md and no analogue in the tree** — our forward units are raiders and station-holders that ACT continuously; **we have no unit whose plan is *arrive, plant, and wait*.** ⭐ **THE ENGINE FACT THAT MAKES IT WORK, and it is a guard asymmetry of exactly the class the standing brief says to hunt: `attack()` targets THE BUILDING on an adjacent tile, and a BUILDER BOT IS NOT A BUILDING** (units = core/builder/gunner/sentinel/launcher; buildings = everything except builder bots). ⇒ **an enemy builder standing in your half is IMMUNE TO BUILDER MELEE ENTIRELY. Only a TURRET can remove it — and I Stone built ZERO turrets, so the infiltrator was untouchable for 774 rounds by construction, not by luck.** | **TWO DIRECTIONS AND THE ROW REGISTERS BOTH SEPARATELY.** OFFENCE: enemy conveyor deaths caused by our single infiltrator per game, and rounds-alive of that unit. DEFENCE: enemy-builder rounds spent inside our half unremoved, currently **unbounded against a turretless build**. ⭐ **AND OUR COUNTER IS THE APPROVED CLASS: the LAUNCHER KIDNAP — `can_launch` has no team check and no vision guard, pickup d²≤2, throw 1≤d²≤26, 0 ammo. We can EVICT what I Stone could not touch at all.** Success on defence = enemy-infiltrator dwell time in our half falls; success on offence = conveyor kills per infiltrator-game. | local screen vs the fixed v140 control at standard n for the offensive half; the defensive half needs an opponent that FIELDS one, so it reads off the corpus first (how many teams run a lone dormant forward builder?) before any leg is sized | ⛔ **Why now: this is a LIVE TEAM winning an economy with a mechanism we neither run nor defend against, decoded off a real game rather than theorised.** And it sits squarely in the standing brief — **a guard present on one path and absent from its neighbour is where exploits live**, and `attack()`-cannot-target-builders is exactly that shape. ⭐ **The counter is ALREADY IN OUR TREE (the launcher) and is the thing the league does not have**, so the defensive half is cheap. | ⚠ **HONEST LIMITS: n=1 GAME, and the immunity is CONDITIONAL — it holds only against a turretless opponent, which I Stone happened to be. Against any turret-building team the infiltrator dies normally, so the offensive half is opponent-selected and must be priced per-opponent from the corpus BEFORE a leg.** ⛔ **And it composes with `#73`/`#59` rather than duplicating them: `#73` is a barrier on OUR delivery seat (a building we cannot repair), this is a UNIT in our half we cannot kill. Different object, different counter, same victim.** **⭐ RE-READ s47 2026-08-16 vs the LIVE CONTROL `_v223sealrepair` (research, on the side lane's `GREP:`-scope audit; every anchor below re-verified here by hand, `ZZQXNOTATOKEN` control = 0/0/0/0 across all four files):** ⛔⛔ **THIS ROW'S GREP IS BROKEN AND THE ROW IS RE-PRICED, NOT WITHDRAWN.** The cell claims *"nothing like it is shipped, in either direction… no analogue in the tree"*. **The QUEUE.md half is TRUE** (all 11 `saboteur` hits sit on this row's own line). **THE TREE HALF IS FALSE.** A detector-and-chase is SHIPPED AND WIRED: `doctrine.py:118` `# SABOTEUR INTERCEPTION` documents the exact incident this row describes (The Flotte Experience 1745 — one enemy builder walked our economy r16→r150+, never contested), `doctrine.py:126` `INTRUDER_CORE_DSQ = 20`, `main.py:457 _nearest_home_intruder`, `main.py:476 _home_defend`, called at `main.py:434-436` and `main.py:677`. ⇒ **a builder taking this row as written would build a first-of-its-kind interceptor that already exists.** ⚠ **AND ONE CORRECTION TO THE AUDIT THAT FOUND IT, because it makes the row weaker not stronger: the unowned band is NOT "beyond d²≤36" — `doctrine.py:121-122` says the melee recall reaches `d²≤20`, and `:124-125` say in writing that **the interceptor owns the band beyond it**. BOTH bands are covered. There is no radius gap to extend into.** ⭐⭐ **WHAT SURVIVES, AND IT IS AN ENGINE FACT RATHER THAN A CODE READ, SO IT IS THE DURABLE HALF: OUR CHASE CANNOT KILL. A builder bot's attack deals 2 dmg *to the BUILDING on an orthogonally adjacent tile* (`CLAUDE.md` API), and a builder bot IS NOT A BUILDING (units = core + builder bots + turrets; buildings = everything except builder bots). ⇒ builder-vs-builder melee is IMPOSSIBLE BY THE RULES, so `_home_defend` can harass, body-block and out-position an intruder but can NEVER remove one — and `main.py:517` `LOKI_QUIET_ON` returns False on the counterbattery melee path, silencing even that.** ⇒ **"only a turret can remove it" STANDS, and it is now proven on the ruleset rather than asserted.** ⇒ **RE-SCOPED PLANK: not a detector (shipped), not a radius extension (no gap) — the plank is (a) the OUR-SIDE plant-and-guard unit, which genuinely does not exist in either direction, and (b) a TURRET-BACKED removal for the intruder the shipped chase provably cannot kill.** **Mechanism metric and fixture unchanged; the change is what a builder writes.** ⛔⛔ **SELF-CORRECTION s47, SAME SESSION — MY OWN AMENDMENT ABOVE WAS OVERSTATED AND I AM FIXING IT IN PLACE RATHER THAN LEAVING IT TO A SUCCESSOR.** I wrote *"BOTH bands are covered. There is no radius gap to extend into."* **THAT IS TOO STRONG.** The side lane's radius was wrong (it is `d²≤20`, not `d²≤36`) and I was right about that — **but I then dismissed the gap entirely, and there IS one, at a boundary neither of us had read.** ⭐ **`main.py:469` — the detector carries a HARD CAP: `if self.core.distance_squared(ep) > 36: continue`.** ⇒ the true coverage map is **`d²≤20` recall + `role_n==4` defender · `20 < d² ≤ 36` `_nearest_home_intruder`/`_home_defend` · `d² > 36` NOTHING.** ⛔ **And `doctrine.py:124-125` promises the interceptor owns *"the band beyond it"* with NO UPPER BOUND — so the DOCTRINE describes an unbounded band and the IMPLEMENTATION stops at 36. The prose and the code disagree, and the prose is the one that got quoted.** *(Also verified, and it is why the prose drifted: `def _intercept` = **0 in all four files** — positive control `def _home_defend` = 1 — while `doctrine.py:130` and `:142` both reference `_intercept`, `:142` explicitly as **the teammate lineage's**. The doctrine is partly describing FOREIGN code. Side lane's find, verified here.)* ⭐⭐ **AND THE MEASUREMENT THAT MAKES THIS DECISION-GRADE RATHER THAN A CODE-READING QUIBBLE — the cap excludes ROUGHLY HALF THE ECONOMY IT EXISTS TO PROTECT.** Cut over `corpus/events.tsv`, all `BUILD` events, `d2_own` = distance from the BUILDER'S OWN core: **HARVESTERS: 374,860 of 675,260 = 55.5% are built at `d²_own > 36`. CONVEYORS: 1,993,761 of 4,336,449 = 46.0%. Economy total: 2,368,729 of 5,012,556 = 47.3%.** ⇒ **the intruder detector is blind to the half of the belt-and-harvester network that lies outside `d²=36` — and 55.5% of harvesters, which is PRECISELY what the Flotte Experience intruder destroyed in the incident `doctrine.py:118` cites as the reason this piece exists.** ⚠ **DENOMINATOR: ALL TEAMS, 5.01M build events across the archive** — this is a fact about where economy GENERALLY sits, not an us-only cut; **our own share is UNMEASURED and a successor should not quote this as ours.** ⇒ **#79 RE-PRICED AGAIN, and it is now STRONGER than my first pass allowed: the plank is THREE things, not two — (a) the our-side plant-and-guard unit (absent in both directions), (b) turret-backed removal for what the chase provably cannot kill (the engine fact, unchanged and still the durable half), and (c) EXTEND DETECTION TO WHERE THE ECONOMY ACTUALLY IS, which is now MEASURED at 55.5% of harvesters beyond the current cap rather than asserted.** ⚠ **AND THE LESSON I AM BANKING ON MYSELF: I corrected a peer's wrong radius and then inherited their CONCLUSION — "no gap" — without reading the scan that sets the bound. Refuting a claim's EVIDENCE is not the same as refuting the CLAIM, and I published as if it were.** |
| **80** | ⭐⭐ **BANK-TRIGGERED OFFENSE SURGE — WE ALREADY HAVE A SURGE AND IT CANNOT HELP THE KILL BY CONSTRUCTION: IT UNLOCKS AT r300, WHICH IS THE ROUND THE PROGRAMME SAYS THE KILL SHOULD ALREADY HAVE LANDED BY.** *(Provenance: MAGNUS, verbatim intent — "there should be some point in the game where we have enough Ti to increase the offensiveness on the other player even if we're cut down by pecking and barriers.")* | **GREP: run vs `_v223sealrepair` (the current incumbent) — THE MACHINERY EXISTS AND IS THE WRONG SHAPE TWICE OVER, both confirmed at source.** `doctrine.py:402-405`: `SURGE_TI_FLOOR = 1500`, **`SURGE_MIN_RND = 300`**, `SURGE_ECO_CAP = 24`. ⭐⭐ **DEFECT 1 — THE ROUND GATE IS PAST THE BAR: `DEFENCE_ADMISSION_BAR` binds at r300 and our median kill is 174. A surge that cannot fire before r300 is structurally incapable of buying a TIMELY kill; it can only ever fund a game we are already losing on the programme's own clock.** **DEFECT 2 — IT BUYS THE WRONG THING: the ONLY consumer is `eco.py:240-243`, which returns `SURGE_ECO_CAP` — it raises the ECO HAND CAP. It buys HARVESTER LABOUR, not kill hardware.** ⛔⛔ **GREP CORRECTED 2026-08-16 ~13:3xZ BY MY OWN PRECONDITION AGENT — I SCOPED THE SEARCH TO `raid.py` AND THE ANSWER WAS IN `main.py`. My claim of "ZERO bank-triggered spend" IS FALSE: `main.py:263-268` ALREADY buys extra builder seats on the bank at `LOKI_SURPLUS_TI = 260` and `LOKI_RICH_TI = 700` (`doctrine.py:1197,1199`).** ⇒ **bank-triggered spend EXISTS; what does not exist is bank-triggered OFFENSIVE spend — the existing triggers buy BODIES, like the r300 surge buys eco hands.** ⭐ **Measured fire rates: `LOKI_SURPLUS_TI` 15.9%, `LOKI_RICH_TI` 1.4% — `LOKI_RICH_TI` IS EFFECTIVELY DEAD CODE.** *(The original claim, now void, read: ZERO bank-triggered offensive spend anywhere in `raid.py` — no site conditions forward sentinels, ammo conversion, or raider spawns on the bank. Magnus's version is unshipped in both of its terms.** | **timely-kill rate (core-kill by r300 over ALL games — the `DEFENCE_ADMISSION_BAR` primary) and ITT RMST₃₀₀**, treatment vs control; plus the mechanism counter — **surge firings per game before r300, currently ZERO BY CONSTRUCTION.** ⚠ **The mechanism metric must be read FIRST: if the trigger never fires, a flat primary says nothing about the plank** *(the soft-knob lesson from `#78`/`#76`, carried across deliberately this time)*. | local screen vs the fixed v140 control; **but see the PRECONDITION below — a free corpus cut decides whether this row is live BEFORE any tree is written** | ⛔ **Why now: Magnus asked for it, and the code read makes the case sharper than the request did — we are not missing a surge, we are holding one that unlocks after the kill window closes and then spends on farming.** | ⭐⭐ **PRECONDITION — GO/NO-GO, REGISTERED BEFORE THE CUT RUNS, AND IT CAN KILL THE ROW FOR THE PRICE OF A QUERY: DOES THE BANK ACTUALLY PILE UP IN HARASSED GAMES?** **Two measured pulls are in genuine tension.** ⛔ AGAINST: the belt census just read **`ti_end` MEDIAN 44 Ti at r150 on clean v140 games** — a thin bank, and a 1500 trigger would essentially never fire. ✅ FOR: **harassment stalls SPEND (contested build sites, cut belts) while passive income and existing harvesters keep paying** — so harassed games may be exactly where surplus accumulates, **which is Magnus's scenario verbatim.** ⇒ **ONE CORPUS CUT: banked-Ti trajectory in games with measured enemy pecking/barrier pressure vs clean games. IF harassed banks are fat, the row is live AND THE TRIGGER THRESHOLD FALLS OUT OF THE SAME CUT. IF thin everywhere, THE ROW DIES and no tree is written.** ✅⛔ **PRECONDITION RAN — VERDICT: NO-GO ON THE PREMISE, GO ON A RE-PRICED ROW.** *(`docs/research/BANK-UNDER-HARASSMENT-2026-08-16.md` + `tools/bank_under_harassment.py`; population = `join.tsv` rows with `ourver` in {140,152} and a replay present — **590 games, 118 matches, 19 opponents, 0 dropped**. Harassment defined from FOUR DONE-TO-US counters decoded per round; our own conveyor deaths REJECTED as confounded by our free `destroy()` reroutes, and anything outcome-shaped rejected as a COLLIDER.)* ⛔ **MAGNUS'S PREMISE IS REFUTED: HARASSED GAMES HOLD LESS, NOT MORE, AT EVERY ROUND r50–r200.** Peak bank r101-300 **HIGH 105 Ti vs LOW 152**; ever ≥260 Ti **9.0% vs 15.8%**. **As an EXCLUSION per the direction clause: harassment cannot raise the 260-Ti fire rate by more than +1.7pp.** ⭐ **THE MECHANISM IS WORTH MORE THAN THE NULL — THERE IS NO SURPLUS BECAUSE A SWEEP ALREADY EXISTS.** r20→r150 ledger: builds/spawns **61.2%** of income · **`convert_ammo` 24.3%** · heals 8.3% · attacks 7.0% · **Δbank −0.8%**. **Even in 29,184 zero-build 25-round windows the bank gains a median +1 Ti.** ⭐⭐ **RE-PRICED ROW IS LIVE AND ITS THRESHOLD FALLS OUT OF THE SAME CUT: `SURGE_TI_FLOOR = 1500` fires 0 of 590 in r21-300 · 700 fires 1.4% · 260 fires 15.9% · 200 Ti fires 25.6%, median first crossing r67, 18.5% BEFORE r150 — three gunners at ~304% scale.** ⚠ **Only 10.3% hold 200 for ten straight rounds ⇒ THE TRIGGER MUST BE EDGE-BASED, NOT LEVEL-SUSTAINED.** ⭐ **POSITIVE CONTROL — the instrument is NOT blind and the finding is HISTORICAL: the same cut reads ≥1500 Ti in 38.8% of `v90` games and 33.7% of `v80`, against 0.6% of `v140`. THE 1500 CONSTANT OUTLIVED ITS DISTRIBUTION — fitted to a bot that banked, carried by one that does not.** Negative control: md5-parity split moves the median peak 6 Ti / 3.2pp against harassment's 47 Ti / 6.8pp. Discrimination: HIGH tertile belt 15 vs 39 at r150, win rate 42.4% vs 63.8% ⇒ **the index measures real damage, so the null is about the BANK, not the instrument.** ⚠ **SURPRISE, flagged before explaining: at r250 the split FLIPS (HIGH 67 vs LOW 30) — the ONLY cell supporting the proposer. n=63, survivorship-selected, PAST the r300 bar ⇒ reported, NOT banked.** ⛔ **TAPE GAP FOR THE NEXT PERSON: `econ.tsv`'s `ti_end` exists at FOUR band boundaries only (r150/200/300/end). There is NO banked-Ti column at r50, r100 or r250 anywhere in `corpus/`, same for `batk`. The new decoder cross-validates 40/40 EXACT against `econ.tsv` where the tape does have it.** ⚠ **Adjacency stated so it is not re-litigated: `#36` is eco-as-kill-enabler on 900-maps (carries a refuted marker); `#71` is collar funding (gated). This generalises both — ANY map, trigger = SURPLUS WHILE THE KILL IS UNLANDED — and neither owns it.** ⛔⛔ **PRECONDITION RUN AND RETURNED s47 2026-08-16 (research, opus agent over `corpus/`; the CODE claims below re-verified by hand in this lane, the BANK-TRACE figures relayed with the agent's own controls — the distinction is stated so a successor knows which is which).** ⭐⭐ **THE VERDICT KILLS THIS ROW'S PROPOSED FIX: THE ROUND GATE IS A RED HERRING.** **0 of 1,243 v140 game-sides EVER hold 1500 Ti before r250** (exact per-round series, not a band snapshot). ⇒ **moving `SURGE_MIN_RND` from 300 to 250 changes the fire rate by EXACTLY 0.00pp**, and to 200 by 0.00pp; dropping it to 150 buys at most **+0.16pp**. ⇒ ⛔ **DEFECT 1 AS WRITTEN IS TRUE BUT INERT — the gate is past the bar AND fixing it changes nothing. `SURGE_TI_FLOOR` IS THE SOLE BINDING CONSTRAINT.** **Joint shipped predicate `ti≥1500 ∧ rnd≥300` fires in 15/1,243 = 1.21% of v140 games** (5.68% of the 21.2% that reach r300 at all), and among games that ever hold ≥1500 the median holds it for **320 rounds** — **these are r1000 stall games where money piles up because nothing is being bought, not games with a spendable war chest.** As EXCLUSIONS with DEFF applied (restated per the direction clause first): v140 rated fire rate **≤1.51%**, unrated **≤2.55%**; crosses-1500-before-r250 rated **≤1.27%**, unrated **≤0.62%**. ⭐ **AND THE MEDIAN v140 GAME NEVER EXCEEDS ITS OPENING ENDOWMENT AT ANY ROUND: median peak bank = 470 = 500 start − 30 for the first builder, and it is 470 through p95.** ⭐⭐ **SURPRISE 1, VERIFIED BY HAND IN THIS LANE AND WORSE THAN DEFECT 2 STATES: `SURGE_EXTRA = 5` IS DEAD CODE.** `grep -rn SURGE_EXTRA bots/_v223sealrepair/*.py` → **exactly ONE hit, the definition at `doctrine.py:404`.** Positive control `SURGE_ECO_CAP` → definition `doctrine.py:405` **plus a consumer at `eco.py:243`**. **It was LIVE in the monolithic tree: `bots/_v115dodge/main.py:1804` `spawn_budget += SURGE_EXTRA` — five extra builder BODIES, the closest thing to kill hardware in the whole surge.** ⇒ **the modular refactor kept the eco-cap half and DROPPED THE BODY HALF.** The surge does not merely PREFER eco labour — **its non-eco half no longer exists**, while `doctrine.py:663-672` still documents `spawn_budget` as *"spawn_cap plus at most REPLACEMENT_MAX(8) plus the surge"*, **a comment describing a consumer that is gone.** ⭐ **SURPRISE 2, AND IT RETRACTS THE CORRECTION MADE TO THIS ROW EARLIER TODAY: `LOKI_SURPLUS_TI = 260` IS NOT A SURPLUS TRIGGER, IT IS AN OPENING-ENDOWMENT TRIGGER.** The game STARTS at 470, so **100.0% of v140/v152 games satisfy `ti ≥ 260` at some round** — trivially, at r0. **The bank first falls below 260 at median r8** (p10 r5, p90 r13; never-falls 0/1,613), and harvester #1 lands r4-8, so the conjunct `harv≥1 ∧ ti≥260` resolves in a **~0-5 round window at the very start and then almost never again** (median share of rounds ≥260: **4.7%**). ⇒ **the "bank-triggered spend" this row was corrected to acknowledge is SPENDING THE STARTING ENDOWMENT, not accumulated surplus.** ⇒ **MAGNUS'S ASK IS UNSHIPPED IN ALL THREE OF ITS TERMS — surplus, mid-game, offensive — not two.** `LOKI_RICH_TI = 700` is deader still (1.7% rated / 3.5% unrated) because 700 sits ABOVE the 470 opening bank and so has no free trigger. ⚠ **FIELD CONTEXT, AND IT CLOSES A STORY BEFORE ANYONE TELLS IT: NOBODY BANKS 1500 IN THE KILL WINDOW.** Every strong team's median peak bank is also ≈ its opening endowment (Erebus 454, The Bisons 438, farming_200s 430, gsxWins 450, 0033 470, kladde 470, lingling_40h 460). Best before-r250 cell in the field is lingling_40h at **2.98%**. **Erebus — a top team — banks LESS than we do (0.20% ever ≥1500 vs our 1.21%).** ⇒ **there is no "their economy is fat and ours is thin" reading available here.** ⭐ **SURPRISE 3, UNEXPLAINED AND WRITTEN DOWN ANYWAY: a large economy REGIME CHANGE in our own history.** Ever-≥1500 by version: **v80 30.9% · v90 35.6% → v104 2.3% → v125 0.44% → v140 1.21% → v154 0.50%.** ⇒ **`SURGE_TI_FLOOR = 1500` was fitted to a bot that BANKED, and is carried by one that does not.** **That is the durable finding: the constant is a fossil of a different economy.** ⇒ **ROW RE-PRICED: do NOT move the round gate. The buildable questions are (a) move `SURGE_TI_FLOOR` to an EDGE-TRIGGERED threshold near 200 (the harassment doc's menu: 200 Ti fires 25.6% at median r67, and MUST be edge-based since only 10.3% sustain it 10 rounds), and (b) RESTORE A KILL-HARDWARE CONSUMER, since the only one that ever existed was deleted in the refactor.** ⚠ **Mechanism metric unchanged and still must be read FIRST: with the shipped constants the trigger fires in 1.21% of games, so a flat primary would say nothing about the plank.** |
| **81** | ⭐⭐⭐ **0033 IS THE ONLY LOSING SEGMENT WE HAVE THAT SURVIVES A FAMILY-WISE CORRECTION — AND IT IS SIMULTANEOUSLY OUR MOST-PLAYED OPPONENT AND A TOP RATING PAYER. PLAY THE PLAYERS, AIMED AT THE ONE CELL THAT IS ACTUALLY ESTABLISHED.** *(Provenance: builder's rated-tape segment cut relayed 2026-08-16 ~15:0xZ on Magnus's question "what segments should/could we improve"; RE-DERIVED HERE from `corpus/ladder_games.tsv` rather than banked — every figure below is mine, not the relay's.)* | **GREP: run vs the live control `_v223sealrepair`, TREE-SCOPED across all four files, with a `ZZQXNOTATOKEN` control at 0/0/0/0.** **THERE IS NO OPPONENT-ADAPTIVE MACHINERY OF ANY KIND: `main.py` has ZERO occurrences of `opponent|enemy_style|fingerprint|adapt`** (doctrine 10 / eco 4 / raid 5 are prose uses of "enemy" and adjacent words, not adaptation). ⛔ **AND THE ENGINE CONSTRAINT THAT SHAPES THE WHOLE ROW: the `Controller` EXPOSES NO OPPONENT IDENTITY — there is no API to ask who we are playing.** ⇒ **any plank from this study must either (a) work unconditionally, or (b) key off a BEHAVIOURAL fingerprint sensed in-game, which does not exist in the tree and would be new machinery.** **A study that ends in "do X against 0033" is UNBUILDABLE BY CONSTRUCTION and must not be written that way.** | **our game share vs 0033, and the LOSS-MODE mix** (core died / r1000 tiebreak / kill-by-r300 rate), treatment vs the pooled baseline | **archive-only characterisation first** (their modal opening per map off `corpus/`, our loss modes in the 110 games), THEN a pinned unrated leg — `fcode match unrated <team> --match <past_match_id>` pins THEIR submission, and this is a TREATMENT fixture so it MUST be pinned (`SPEC-opponent-pinning-2026-08-13.md`) | ⭐⭐ **WHY NOW, AND WHY THIS ROW RATHER THAN THE MAP CLUSTER — I RAN THE MULTIPLE-COMPARISONS CORRECTION AND IT INVERTS THE BUILDER'S PROPOSED PRIORITY.** Over the **last 1500 rated games (2026-08-12T10:52 → 2026-08-16T14:32)**, pooled baseline **50.7%**: **0033 = 28.2% over 110 games / 22 matches.** ⚠ **DEFF enumerated per `CLAUDE.md`'s PROCEDURE, not looked up: within an opponent stratum the OPPONENT cluster is degenerate by construction and the MATCH cluster is LIVE (verified: games per match = exactly 5, min=max=median), ⇒ within-opponent DEFF = 1.366.** 95% CI **[18.4, 38.0]**. ⭐ **With 21 opponent cells tested, Bonferroni z=3.04 gives [13.0, 43.4] — STILL EXCLUDES the 50.7% pooled baseline. 0033 is one of only TWO cells (with `team lazy` at 74.0%, on the winning side) that survive family-wise correction out of 21.** ⇒ **it is the single most robust adverse segment in the entire tape.** **And it is the most-played (110 games, 22 matches) and a top payer (+16.49 for a 5-0 at our 1735).** ⛔ **CONTRAST, and this is why the map-autopsy row is NOT stocked beside it: of 15 map cells, only midgard survives the same correction and it survives NARROWLY (family-wise upper bound 50.0 against a 50.7 pooled baseline) — see `#82`.** ⭐ **BUILDER NOTE ADOPTED s47 (relayed ~15:1xZ), and it is a scoping obligation ON THIS ROW rather than a nicety: a 0033 study MUST STATE UP FRONT WHICH OF ITS TWO BUILDABLE PRODUCTS IT AIMS AT**, because they are different work with different costs. **(a) AN UNCONDITIONAL PLANK** — something learned from 0033 that we ship for every opponent, which is the cheap road and the one this row defaults to; **(b) BEHAVIOURAL FINGERPRINTING** — sensing in-game that we are facing a 0033-like opponent, which is NEW MACHINERY that does not exist in the tree (`main.py` has ZERO `opponent|fingerprint|adapt`) **and therefore needs its OWN row rather than riding in on this one.** ⇒ **THIS ROW IS SCOPED TO (a). Anything reaching for (b) is out of scope and must be stocked separately.** *(Groundwork already exists for the archive half: `#39` carries per-(team,map) modal openings.)* |
| **82** | ⭐ **MIDGARD ALONE IS OUR ONE ESTABLISHED BAD MAP — AND I KILLED ITS MOST OBVIOUS MECHANISM FOR FREE BEFORE STOCKING THE ROW, SO NOBODY SPENDS A SESSION RE-DERIVING IT.** *(Provenance: builder's segment cut relayed ~15:0xZ proposed a midgard/fjordgate/frostgate cluster autopsy; RE-DERIVED and NARROWED here.)* | **GREP: run vs `_v223sealrepair`, TREE-SCOPED, `ZZQXNOTATOKEN` control 0/0/0/0.** Map-conditional machinery EXISTS and is substantial: `doctrine.py:1110 MAP_CODES` + `:1142 EXTRA_MAP_CODES` (exact packed terrain), `eco.py:56 known_map_for`, consumed at `main.py:165` and `main.py:360`. The absolute-coordinate constant the builder named is real: **`eco.py:375 if p.distance_squared(self.core) > 64`.** | **our game share on midgard**, and any midgard-specific kill-by-r300 rate, vs the pooled baseline | archive-only autopsy of the 87 midgard games in the last-1500 window | ⛔⛔ **MECHANISM `known_map_for` MIS-RESOLUTION: REFUTED. Checked locally, offline, for free — and it is a NEGATIVE that closes a road.** `doctrine.py:1157-1159` warns of **TWO COLLISION PAIRS sharing dims+anchors — midgard/ragnarok `(30,30,2,2,26,26)` and frostgate/yulerune `(20,20,2,9,16,9)`** — resolved only by sensed terrain, and `eco.py:74-84` falls back to a blind `candidates[0]` on `ct is None` or on any exception. ⭐ **The hypothesis was that a mis-resolved pair hands `_plan_siege` a CONFIDENTLY WRONG wall map — strictly worse than the `None` fallback — and that midgard (our worst) and yulerune (our best) BOTH sitting in collision pairs was the tell.** **I decoded every packed terrain grid and measured, for all FOUR collision keys, at BOTH spawn anchors, at BOTH core `r²=36` and builder `r²=20` vision: the minimum number of DIFFERING tiles inside the sensed patch is 3, and it is non-zero in 16 of 16 cases.** ⇒ **the correct candidate scores 0 mismatches and the wrong one ≥3, so `min()` resolves CORRECTLY every time. And both live call sites PASS `ct` (`main.py:165`, `:360`), so the blind-pick branch is never taken in production.** ⇒ **MIS-IDENTIFICATION IS DEAD. Do not re-open it without new evidence.** ⚠ **AND THE HONEST BOUND ON THE ROW ITSELF, WHICH IS WHY IT IS STOCKED SMALL: the builder's three-map cluster does NOT survive scrutiny.** Over the last 1500 rated games (pooled **50.7%**, DEFF **1.07** — **enumerated, not looked up: the MATCH cluster DIES on a per-map cut, VERIFIED at 0 of 1500 `(match,map)` cells holding >1 game, while the OPPONENT cluster survives**): **midgard 34.5% (n=87) [24.2, 44.8]; fjordgate 41.1% (n=95) [30.8, 51.3] — INCLUDES 50; frostgate 44.2% (n=77) [32.7, 55.6] — INCLUDES 50.** ⇒ **two of the three proposed maps are not distinguishable from parity even UNCORRECTED.** With 15 map cells tested, Bonferroni z=2.94 leaves **midgard at [19.0, 50.0]** — surviving, but with its upper bound landing **exactly on 50.0** against a 50.7 baseline. ⛔ **Per `CLAUDE.md`'s narrowly-cleared-bar clause, that is the EXPOSED class, and it is stated here rather than in a footnote.** ⇒ **the three-map cluster autopsy is NOT stocked; midgard alone is, and it ranks BELOW `#81`.** |
| **83** | ⭐⭐⭐ **THE ENEMY BARRIER IN OUR GUNNER'S LINE IS A *ROTATION SUPPRESSOR*, NOT AN AMMO SINK — AND THE TARGET-LADDER FIX THE STUDY IMPLIES IS INERT BY CONSTRUCTION. A 3-Ti ROCK PINS A 20-Ti TURRET'S FACING FOR THE REST OF THE GAME BECAUSE OUR OWN CODE TREATS "I HAVE A TARGET" AS "I AM USEFULLY EMPLOYED".** *(Provenance: builder's `REPLAY-STUDY-0033-2026-08-16.md` piece 1-defensive, 180 games — 28% of 0033's barriers sit in our gunners' fire lines; we sink **11.9% of ALL our ammo and 34.1% of gunner shots** into 3-Ti rocks; sentinel control runs the right way at 6.6 vs 1.6 mean shots eaten. RELAYED, and the MECHANISM below is mine and verified in the tree.)* | ⛔⛔ **GREP: run vs the live control `_v223sealrepair`, TREE-SCOPED, `ZZQXNOTATOKEN` control 0/0/0/0 — AND IT KILLS THE OBVIOUS FIX BEFORE ANYONE BUILDS IT.** **THE TARGET LADDER ALREADY EXISTS AND BARRIER IS ALREADY LAST: `main.py:735-741` ranks `CORE 0 · SENTINEL 1 · GUNNER 2 · BUILDER_BOT 3 · LAUNCHER 4 · HARVESTER 5 · CONVEYOR/SPLITTER 6 · **BARRIER 7** ` of 8.** ⇒ **re-ranking barriers lower buys NOTHING.** ⭐ **AND THE REASON IS GEOMETRY, NOT RANKING: a gunner's shot is BLOCKED BY OBSTACLES (`CLAUDE.md`; the sentinel's is the one that pierces — which is exactly why the study's sentinel control ran the other way). So `ct.can_fire(t)` is True for AT MOST the nearest occupied tile in a gunner's line, the loop at `:725-743` finds ONE candidate, and THE LADDER IS INERT FOR GUNNERS BY CONSTRUCTION.** The block's own comment at `:718` says it was written for sentinels: *"Sentinels pierce intervening units, so scan the whole line."* ⭐⭐⭐ **THE ACTUAL DEFECT, AND IT IS A SUPPRESSION RATHER THAN A LEAK: `ROTATE_DISCIPLINE_ON = True` (`doctrine.py:728`) and `_idle_rotate` IS LIVE — but it is called at `main.py:757`, reachable ONLY if BOTH fire paths failed to return (`:746`, `:753`). ⇒ A GUNNER THAT CAN SHOOT A BARRIER FIRES, RETURNS, AND NEVER REACHES THE ROTATION BRANCH.** ⇒ **the rock does not merely eat 4 ammo a shot — it PINS THE FACING INDEFINITELY by keeping the gunner "busy", suppressing the very rotation that would free it. `rotate()` is gunner-only (10 Ti + cooldown 1), so the escape exists and is unreachable.** **Nothing anywhere vetoes a barrier target: the only two `BARRIER` references in `main.py` are the two priority ladders (`:507`, `:740`), both rankings, NEITHER a veto.** | **gunner shots landing on BARRIER as a share of gunner shots (34.1% baseline), ammo share into barriers (11.9% baseline), and — the mechanism counter that distinguishes the two fixes — MEAN ROUNDS A GUNNER HOLDS ONE FACING** | local screen vs the v140 control, then a pinned unrated leg vs 0033 (`--match <past_match_id>`; TREATMENT fixture ⇒ MUST pin, `SPEC-opponent-pinning-2026-08-13.md`) | ⭐ **Why now: this is the largest measured resource hemorrhage on the board (11.9% of ALL ammo) and it is UNCONDITIONAL — no opponent identity needed, so it clears `#81`'s buildability constraint. ⇒ TWO CANDIDATE FIXES AND THEY MUST BE SCREENED SEPARATELY: (a) HOLD FIRE when the only fireable target is a BARRIER (saves ammo, but a pinned gunner still contributes nothing), and (b) FALL THROUGH TO `_idle_rotate` when the only fireable target is a BARRIER (unpins the facing — strictly larger, and it reuses machinery already shipped and already on).** ⚠ **(a) alone would show as an ammo saving with no combat gain and would read as a weak positive; (b) is the one the mechanism argues for. Screening them together would confound them.** ⚠ **AND THE SELF-AUDIT RIDER: `#10` (blind-their-gun-with-their-own-body) is the same geometry aimed the other way — check it before building, they may share a site.** |
| **84** | ⭐⭐ **THE OFFENSIVE BARRIER PLUG — SPEND 3 Ti TO BLIND A 20-Ti GUNNER, AT A MEASURED ~7:1 EXCHANGE. 0033 DOES THIS TO US 180 GAMES RUNNING AND WE HAVE NEVER DONE IT ONCE.** *(Provenance: builder's `REPLAY-STUDY-0033-2026-08-16.md` piece 1-offensive. The DEFENSIVE half is `#83`; this is the same geometry aimed outward and it is a DIFFERENT BUILD.)* | **GREP: run vs `_v223sealrepair`, TREE-SCOPED, `ZZQXNOTATOKEN` control 0/0/0/0. GENUINELY UNSHIPPED — and I checked the near-miss rather than the token.** We have exactly **three** barrier build sites, all in `raid.py`: `:277` (seat-seal), `:500` (`# (1) SALT THE CORPSE`), `:558` (salt-beside-belt). **NONE reads an enemy turret's facing or line when siting.** The hypothetical-turret predicate `can_fire_from` EXISTS in the tree but is used **only** in the forward-SENTINEL siting path (`raid.py:683`) — i.e. **to place OUR turret so it can shoot, never to place a BARRIER so THEIRS cannot.** `ct.get_direction(bid)` is read at `raid.py:771` on an enemy turret, so **reading their facing is already a solved problem in this tree** — the plug is a NEW CONSUMER of machinery we already have, not new machinery. | **enemy gunner shots eaten by our planted barriers per game** (mechanism counter, currently ZERO by construction) and **enemy turret effective uptime**; primary = timely-kill rate by r300 | pinned unrated leg vs 0033 and a second turret-heavy opponent (TREATMENT ⇒ MUST pin) | ⭐⭐ **Why now: this is squarely the LOKI brief — a sequence of individually legal, documented calls whose combined effect the opponent's code cannot survive — and it is the cheapest exchange on the board at 3 Ti against 20 Ti plus the 4-ammo-per-shot they waste on the rock.** ⚠ **AND IT IS NOT A NEW CLASS AND NEEDS NO ORGANISER QUESTION: it is ordinary building on legal tiles, not exception-induction.** ⛔ **PRECONDITION, FREE, RUN IT BEFORE ANY TREE: `#83` establishes that a plugged gunner NEVER ROTATES — but that is OUR code. Whether THEIR gunners rotate off a plug decides the whole payoff, and it is measurable from the archive (mean rounds an enemy gunner holds one facing, plugged vs unplugged). If 0033's gunners rotate away, the exchange collapses from permanent to one-round.** ⚠ **The 7:1 figure is RELAYED from the study and I have NOT re-derived it.** |
| **85** | ⭐⭐ **WE CHEW A CORE THAT OUT-HEALS US 8:1 — 13.9% OF OUR BUILDER ACTIONS FOR 24 dmg/GAME AGAINST +208/GAME OF HEALING, WHILE 0033'S BUILDERS ATTACK OUR CORE 0 TIMES IN 19,198 ACTIONS. THE FIX IS ONE LINE IN A LADDER WE ALREADY SHIP.** *(Provenance: builder's `REPLAY-STUDY-0033-2026-08-16.md` piece 2. RELAYED — the 24/208/19,198 figures are the study's and I have NOT re-derived them; the CODE fact below is mine.)* | ⭐ **GREP: run vs `_v223sealrepair`, TREE-SCOPED, `ZZQXNOTATOKEN` control 0/0/0/0. THE BEHAVIOUR IS SHIPPED AND DELIBERATE, NOT AN OVERSIGHT — which is what makes it a one-line plank rather than a feature build.** `main.py:503-508` is the BUILDER ATTACK ladder: `GUNNER 0 · SENTINEL 0 · **CORE 1** · HARVESTER 2 · LAUNCHER 3 · CONVEYOR/SPLITTER 4 · BARRIER 5`, default 6. ⇒ **the core is ranked SECOND, above every economic target, so a builder adjacent to both an enemy core and an enemy harvester chews the core BY DESIGN.** | **share of builder actions spent attacking (13.9% baseline) and their reallocation**; primary = timely-kill rate by r300 and RMST₃₀₀ | local screen vs the v140 control — **this is a pure reallocation with no new machinery, so a local battery can resolve it and no platform window is needed** | ⭐ **Why now: it is the cheapest change on the board (a ladder re-rank in a ladder we already ship) and it is UNCONDITIONAL, clearing `#81`'s buildability constraint.** ⛔ **BUT THE OBVIOUS FORM IS PROBABLY WRONG AND THE ROW SAYS SO BEFORE ANYONE BUILDS IT: demoting `CORE` outright would ALSO disarm the finishing blow, and `R1000_IS_DEFEAT` means the kill is the ONLY thing that scores. A builder chewing a core at r30 is waste; the same action at r170 with the core at 40 HP is the win condition.** ⇒ **the plank is a CONDITIONAL demotion — core stays top-tier only when it is actually killable (HP below a threshold, or our damage rate exceeding the heal rate) and drops below the economic targets otherwise.** ⚠ **A flat demotion is a DIFFERENT and more dangerous arm and must not be screened as if it were this one.** ⚠ **AND THE FIGURE THAT DECIDES THE THRESHOLD IS NOT IN HAND: +208/game healing is a per-GAME average, not a rate at the moment of contact. The threshold needs the instantaneous heal rate, which is a free archive read and should precede the tree.** |
```
E-22.3   LOKI-2 destroy/scale-prune  REFUTED  median 2 prunes vs 300-590% scale
LOKI-43  LAUNCHRENT                  WITHDRAWN ~12pp of scale, ~24 Ti - below the band (s33, measured 14x short)
LOKI-48  IDLE CULL                   LIVE      96pp of scale, 6.8 culls/game - 8x LOKI-43
```
⇒ **The magnitude is the whole question in this family, and LOKI-48 is a DIFFERENT ORDER: 318% → 222% end scale makes every subsequent build ~30% cheaper, and builders are 123pp of our 377% scale — more than every turret combined.** ⛔ **The family history is the ARGUMENT here, not the hazard: without it a reader's first instinct is *"didn't we refute this twice?"*** ⚠ **AND THE PROCESS FAILURE IS RECORDED AGAINST THIS LANE:** LOKI-43's withdrawal (s33) said *"the two now cite each other so nobody re-derives this"* — **and six hours later a third instance arrived citing neither.** A pairwise cross-reference does not survive a third member; **a FAMILY needs a named row, which is what this is.** *(Side lane caught it. D14.)* |
```
team          rating  builders  vs ours   conveyor  conv/harv
sporks         2185     12.02    0.93x     111.22       9.2   SAME CHASSIS
Lorem Ipsum    2021      5.75    0.44x      41.80       7.6   half our hands
Clankers       2014      8.17    0.63x      57.58       9.4   half our hands
ph             1995     11.30    0.87x      48.82       4.4   SAME CHASSIS
Pivot          1976     12.91    0.99x      45.36       3.5   SAME CHASSIS
O(1)           1947     12.44    0.96x      90.67      11.4   SAME CHASSIS
US             1655     12.99    1.00x      46.81       6.0
```
⭐ **FOUR of the six run OUR builder chassis; Lorem Ipsum's gunner rush runs 44% of our hands and Clankers 63% — those are CHASSIS CHANGES, not grafts, and their planks should not be copied onto a 13-builder bot.** ⇒ **Among the four compatible teams, the two with the most conveyors are the #1 team (sporks, 111.22, 2.4x ours) and O(1) (90.67, 1.9x).** **And conveyor is the cheapest entity in the game — 3 Ti, +1% scale — against a harvester's 20 Ti and +5%.** ⛔ **The reason this is not just "more stuff": `CLAUDE.md` establishes that `titanium_collected` counts DELIVERY TO THE CORE, not emission — "a harvester with no route home is worth zero on it, forever". So conveyor density is on the CONVERSION path, and #20 measured harvester COUNT while never measuring whether ours are CONNECTED.** ⚠ **Not clean across all seven: Pivot (3.5) and ph (4.4) win with fewer conveyors per harvester than us. So this is ANOTHER bracketing, and the argument rests on chassis-match plus cost, not on a universal trend — see #25.** |

**CHECKED AND CLOSED, s31 — not a plank, recorded so nobody re-opens it.** `CLAUDE.md`
flags that **`can_fire` returns TRUE at 0 ammo** and that firing then RAISES, which
would permanently destroy our own turret. **We are guarded:** `main.py:116` wraps
`_dispatch` in a blanket `try/except Exception` with a comment naming this exact
hazard. So the unguarded `ct.fire(best)` at `raid.py:382` — which sits *outside* its
local `try` — **costs that turret one turn, not the unit.** Residual cost is a
silently wasted turret turn, and `econ.tsv.ammo_end` (median 24–48) says we rarely
sit at 0. **Not worth a slot.**


### ⭐ #29 — SALT THE CORPSE: BARRIER THE TILE A DEAD CONVEYOR VACATED *(Magnus, s34)*

**Magnus, watching a replay:** *"if we kill a conveyor, why dont we build a barrier on it? If it's on the ring that would be annoying for them, If it's in a tight spot they cant build around the barrier they have to kill it."*

**1. THE CHANGE, named to the branch.** `raid.py:267-279` is the ONLY `build_barrier` call site in the entire tree, and it is gated `if (t.x, t.y) not in seatkeys: continue` — **we have never barriered anything except the enemy core's 8 seats.** The change is a second admissible tile class in that same loop: an orthogonally adjacent EMPTY tile that a known enemy conveyor occupied. New constant `LOKI_SALT_ON`, off = byte-identical.

**2. WHY IT IS THE COLLAR'S OWN ARITHMETIC, POINTED AT A BETTER TARGET.** `doctrine.py:1200-1206` already prices this exact exchange for the ring: *"a barrier is 3 Ti / 30 HP; breaking one costs 15 builder pecks at 2 Ti each = 30 Ti. The exchange is 10:1 in our favour and every round they spend pecking is a round they are not healing."* **We spend ~11 Ti and ONE builder action; they spend 30 Ti and FIFTEEN builder-turns** — and builder-turns are the scarce good on this line, since acting and moving are mutually exclusive.

**3. ⭐ IT IS WHAT MAKES A CUT STICK, AND THAT IS THE REAL CASE.** Measured s34 (21,587-replay field control): **the field REPAIRS 40.5% of cut conveyors at a median latency of 4 rounds.** ⇒ **a bare cut is worth little because they rebuild it in four rounds. The barrier converts a temporary cut into a persistent one.** This upgrades the conveyor-melee carve-out from a tempo poke to a structural denial.

**4. MECHANISM METRIC.** Enemy conveyor tiles denied at r+10 after a cut (i.e. cut tiles carrying OUR barrier and no enemy conveyor); and their repair latency on salted vs unsalted cuts. Both readable from `events.tsv` BUILD/DEATH by tile.

**5. ⛔ THE PRECONDITION, AND IT BLOCKS THE PLANK AS WRITTEN.** *"If we kill a conveyor"* — **we almost never do.** `batk` is **0 in 420 of 420 rated v112-v116 games** (`LOKI_QUIET_ON = True`), and we inflict **2.1%** trunk damage against the field's **15.0%**. ⇒ **#29 is DOWNSTREAM of the conveyor-melee carve-out and they should be ONE package**, not two legs. The sequencing is naturally clean: a builder may only build on an orthogonally adjacent tile, and a raider that just pecked a conveyor down **is** adjacent — kill, then salt, same unit, consecutive actions.

**6. ⚠ THE VALUE IS ENTIRELY CONDITIONAL ON LOCAL TOPOLOGY — Magnus named the discriminator himself.** In a corridor they must clear the barrier (30 Ti + 15 turns). **In open ground they lay a conveyor one tile over and our 11 Ti bought nothing.** ⇒ **MEASURE FIRST, FREE, OFF THE ARCHIVE: what share of enemy trunk tiles have <=2 free orthogonal neighbours?** That number decides whether this is a weapon or a donation, and it is a research cut, not a battery.

**7. HONEST COSTS.** +1% global scale per barrier, permanent until destroyed — the cheapest increment in the game, but our scale is already ~377% and research measured that scale is exactly what prices our launcher out (`LAUNCHER_RESERVE` needs 120-160 Ti against a median r150-200 bank of 54). And killing their conveyor first REFUNDS them 1% scale (engine fact: destroying enemy buildings LOWERS their scale). Both small; both real.

**⭐ 9. AMENDED SAME SESSION — THE PRE-EMPTIVE VARIANT IS NOT BLOCKED AT ALL, AND IT IS THE STRONGER FORM.** Point 5 blocks this behind the melee carve-out because it assumed we must KILL the conveyor first. **We do not.** `build_barrier` needs only an orthogonally adjacent EMPTY tile and ~11 Ti — **no attack, no `can_fire`, and therefore NO dependency on `LOKI_QUIET_ON`.** Barriering a tile their chain has not reached yet denies it just as hard as barriering a corpse, and our raiders are already walking through their half to reach the ring. ⇒ **`SALT-EMPTY` is buildable TODAY on the shipped chassis; `SALT-CORPSE` is the version that waits on the carve-out.** Split them. ⛔⛔ **MEASURED s34 AND THE PRE-EMPTIVE CASE DOES NOT SURVIVE — THE WINDOW IS SHUT.** The side lane's discriminator: points 9 and 10 target **disjoint tile sets** — point 10's chain-head tile is *occupied by their conveyor*, and a barrier needs an **EMPTY** tile (`raid.py:267`), so the trunk payoff belongs to the BLOCKED `SALT-CORPSE`. The bridge between them was the pre-emptive claim that the tile is empty *early*. **It is not, by the time we can reach it.** Field-wide, both teams, every decoded replay, **44,995 team-sides**: the chain-head tile (`d2_own <= 2` of their own core) is occupied at **median r9** (p25 r4 · p75 r16 · p90 r32) and is **already occupied before r22 in 83.2% of team-sides.** Loosening the tile definition worsens it (`<=5`: **93.9%** occupied pre-r22; `<=8`: 94.8%). **Sensitivity, because "our raiders arrive ~r22" is our own measure:** window open in **47.9%** at r10 · **28.0%** at r15 · **16.8%** at r22 · **11.2%** at r30. ⇒ **the pre-emptive variant inherits the trunk payoff in ~1 game in 6 and does not rescue itself under any plausible arrival.** ⇒ **`SALT-EMPTY` MUST BE SIZED ON THE LEAF PAYOFF — *"costs them one harvester"* — not the trunk payoff that motivates the row.** Against the `_v171late160ammo` contrast's ±1.33pp band that is the LOKI-43 arithmetic: **declare, before any games, what leaf-sized effect would even be visible and whether it is reachable.** ⚠ **Caveat against my own cut: this is FIELD-WIDE, not our admissible targets.** ⛔ **AND THE TILE SET ABOVE IS CONTAMINATED — CORRECTED SAME SESSION.** `d2_own` **cannot** identify the 8 collar seats: from a 2×2 core's anchor, d² ∈ {1,2,4,5} also contains **footprint-interior tiles and the 4 diagonal CORNERS**. Proof it was wrong: the naive filter reported team-sides using **17 distinct "seats" when only 8 exist.** ⇒ recovered the core anchor by **trilateration** from (x, y, d2_own) triples and classified the seats exactly (**5,621 anchors uniquely solved of 6,000 team-sides in file order; no side now exceeds 8, which is the instrument's own check**). **CLEAN FIGURES, and they move the same way.** ⛔ **TWO SAMPLING CONTROLS RUN AT THE SIDE LANE'S REQUEST, AND THE SECOND ONE BIT.** *(a) SOLVABILITY BIAS:* an anchor is recoverable only from the triples a side's own conveyors provide, so the dropped set could be enriched for conveyor-poor sides — **confirmed on count (unsolved median 3 conveyors vs solved 28) but NOT on timing (first conveyor median r7 vs r6).** The drop is 4.0% of sides, so the worst case — every dropped side never occupying a seat pre-r22 — bounds it: **"occupied before r22" ∈ [85.7%, 89.3%]**, i.e. the reported figure is an UPPER bound and window-open a LOWER bound. Cannot move the conclusion. *(b) ⛔ ERA SAMPLE — REAL, AND FIXED:* the first 6,000 team-sides **in file order spanned only 2026-08-07 → 08-09, the first two days of a six-day archive.** Re-run on a **stride sample (every 8th of all 52,798 sides, span 2026-08-07T10:28 → 08-12T12:57, i.e. the whole archive)**: **first collar seat occupied median r8 (p25 r3 · p75 r13), occupied before r22 in 91.9%, window open 41.2% at r10 · 18.9% at r15 · 8.1% at r22 · 4.8% at r30** — every figure moves the SAME way, so the era prefix was mildly conservative. **Seat multiplicity on the spread sample: exactly one seat 9.0%, 2+ seats 89.3%. Max seats seen = 8 = the structural maximum, the instrument's own check.** *(Superseded prefix-sample figures: 89.3% pre-r22, window 10.7% at r22, seats 10.1%/87.5%.)* ⭐⭐ **AND THE FAR END OF THE TRUNK CASE IS CLOSED STRUCTURALLY, WHICH DOMINATES THE TIMING: point 10 assumes the near-core chain has a UNIQUE entry tile. It has EIGHT** (`doctrine.py:1196-1199`, our own collar's justification: the 8 orthogonal seats are *"the only tiles a conveyor can deliver into it from"*). **One barrier denies 1 of 8; they deliver into another. Sealing all eight IS the collar we already ship.** Out-degree 1 is a property of each conveyor, **not of the approach** — eight distinct 1-wide paths can arrive. **And the behavioural half is measured, not assumed: 87.5% of team-sides use 2+ distinct collar seats over a game and only 10.1% ever use exactly one.** ⇒ **the trunk payoff does not exist at either end — 8-wide near the core, and genuinely 1-wide only mid-chain where the upstream harvester count is the LEAF payoff.** ⇒ **A per-opponent timing group-by is NOT worth running: the 8-seat structure is opponent-independent, so no timing result can reopen the trunk case.** *(Structural point: side lane. Timing and seat-multiplicity: research.)* **⚠ AND THE LEAF PAYOFF IS ITSELF AN UPPER BOUND, stated before the null rather than after it (side lane, against their own contribution): the 8-seat argument is engine/doctrine-level and premise-free, but it does NOT establish that a MID-CHAIN barrier severs either — that needs the opponent not to reroute around it, which is behavioural and UNMEASURED.** Likewise **the 9.0% who use exactly one seat is an upper bound on "sides where one barrier could sever"** — a side that happened to use one seat may have used another had it been blocked. **Capability, on both sides of the ledger.** **WHAT SURVIVES AND STAYS QUEUED: the LEAF plank, honestly sized** — one barrier mid-chain denying the harvesters upstream of that point for as long as it stands. ⚠ **And the `_v171late160ammo` contrast has NO NEG CELL, so a null on it cannot currently be read as "the plank does nothing."**

**⭐ 10. AND THE TARGET CHOICE IS WORTH MORE THAN THE MECHANISM.** A conveyor chain has **out-degree 1** (engine fact, `CLAUDE.md`), so it is a PATH, not a network. ⇒ **a barrier mid-chain silently zeroes the delivery of every harvester UPSTREAM of it for as long as it stands** — they keep emitting into a dead end, which is exactly the L4 failure we measured on ourselves this session. **So which tile you salt dominates the payoff: salt near the CHAIN HEAD (the conveyor nearest their core) and one barrier severs the whole trunk; salt a leaf and you cost them one harvester.** The chain head is trivially computable (their conveyor minimising `d2` to their core). ⚠ **And note the overlap this exposes: the COLLAR IS ALREADY A DELIVERY CUT** — `doctrine.py:1196-1199` says the 8 seats are the only tiles a conveyor can deliver into the core from. **Salt-the-corpse is the collar extended down the line, not a new weapon** — which is an argument FOR it (the mechanism is already validated) and a warning that the two may not compose additively.

**8. NOTE ON "ON THE RING".** For the ENEMY CORE's ring this is the collar and we already ship it. The new ground is **trunk** tiles. The mirror case — them denying OUR delivery seats — is the L4 problem from the same session.


### ⭐⭐ ROW #30, LONG FORM — THE STATION SCORER IS BLIND TO ENEMY SENTINELS *(side lane, s34; verified independently by the builder in the shipped tree)*

**⚠ THIS IS THE LONG FORM OF TABLE ROW `#30`, NOT A SECOND ITEM.** Found by `queue_check`'s new unique-number assertion on its first live run — the two write-ups shared a number and a reference to *"#30"* was ambiguous between them. **The TABLE ROW is authoritative for the GREP and the sizing; this block is authoritative for the argument.** Two corrections landed on the table row after this block was written and are NOT reflected below: **(1)** *"`SENTINEL` → nothing"* is too strong — **sentinels ARE handled at `raid.py:366`, sharing attack priority `pr=3` with gunners; the true finding is scoped to the STATION SCAN, i.e. we model the sentinel as something to KILL and never as something to AVOID**; **(2)** the sizing is **34.9% newly / 68.4% ceiling = ~0.32 forward builder deaths/game on the SHIPPED trees**, not the all-time 7.6%/22.7% this block predates.

**⇒ CONVENTION, so the assertion stays meaningful rather than evaded: a heading that DEFINES a row writes `#N —`; a heading that ELABORATES an existing row writes `ROW #N, LONG FORM —`.**

**1. THE CHANGE, named to the branch.** `raid.py:500-527`, the raid-station scan, handles exactly **two** enemy entity types: `LAUNCHER` → `threats`, `GUNNER` → `gun_axis` (LOKI-25, the plank v114 shipped on). **`SENTINEL` → nothing.** Verified by me on `_v169launchlate160`: `grep -oE "EntityType\.[A-Z_]+"` over the scorer returns `GUNNER` ×2 and `LAUNCHER` ×1, **and no SENTINEL at all.** One call site. The change is a third term, same penalty machinery as `LOKI_GUNAXIS_PENALTY`.

**2. ⭐ THE IRONY IS IN OUR OWN COMMENT, AND IT IS THE WHOLE CASE.** `raid.py:508-517` says gunners are *"the AVOIDABLE one"* precisely because *"A gunner's shot is a straight line that IS BLOCKED by obstacles and reaches only r²=13; **a sentinel's ignores obstacles**."* ⇒ **We built avoidance for the threat we could also defeat with a 3 Ti barrier, and none for the threat that can ONLY be avoided — which additionally has 2.5× the range (r²=32 vs 13).**

**3. MECHANISM METRIC.** Share of our raid-station rounds spent on a live enemy sentinel's line; and forward-builder deaths attributable to sentinels vs gunners. `get_attackable_tiles_from` is the exact predicate and is already called at `raid.py:522` for gunners — **the expensive half is built and pointed at the wrong turret.**

**⛔ 4a. AMENDED — MY `3.04 sentinels/game` WAS POOLED, side lane was right to flag it, AND SPLITTING IT REVERSES THEIR CONCLUSION.** Split on `d2_enemy < d2_own` over the same 100 opponent-matched games:

| arm | OUR fwd sent/g | OUR home sent/g | fwd share | **THEIR fwd sent/g** |
|---|---|---|---|---|
| v116 | **1.68** | 1.36 | 55% | **0.84** |
| v117 | 2.20 | 2.64 | 45% | 0.60 |
| v118 | 2.08 | 1.92 | 52% | 0.80 |
| v119 | 1.84 | 1.88 | 49% | 0.48 |

**The load-bearing figure is 1.68 forward/game, not 3.04** — the flag was correct and the pooled number should not be quoted again.
⭐ **BUT SIDE LANE'S "SCREENABLE BUT UNDERSTATED" IS BACKWARDS FOR THIS PANEL.** The quantity that matters is **how many ENEMY forward sentinel rays our raiders must cross.** Against these five live opponents that is **0.48-0.84/game**. In a LOCAL battery the opponent is `_v169launchlate160`, which builds **1.68**. ⇒ **local presents our raiders with 2-3x MORE enemy forward sentinels than this live panel does. The local fixture OVERSTATES this plank's opportunity — the opposite direction from SALT.**
⇒ **Read it accordingly: a local NULL is damning (the term did nothing even at 2-3x the exposure); a local POSITIVE is inflated and must be re-read live before it ships.**
⚠ **AND THE TWO POPULATIONS ARE NOT THE SAME FIELD, which is why this is not a refutation of `#23`:** `#23`'s 35.9%-vs-57-80% forward share is measured over 5,618 of our games against the six TOP teams; this is 100 games against **five specific opponents**, and LingLing40 — the team whose r6 corridor sentinel started this row — **is not in it.** Both numbers are true about their own population. **Neither licenses a sentence about "the field" unqualified.**

**4. FIXTURE.** Local battery against `_v169launchlate160` resolves it: unlike SALT, this plank pays off a behaviour our own control bot HAS (it builds sentinels, 3.04/game) — so it passes the s34 self-play-blindness test in `docs/builder-arm-retro.md`. **Screenable locally.**

**5. WHY NOW.** Found in `95e14c55`, a **rated 0-5 loss by the LIVE HOLDER v116 to LingLing40 v29** that had **zero rows in `corpus/events.tsv`** — invisible to all three lanes until side lane decoded it off `replay_archive/` after Magnus asked about a replay. Their first turret is up **r3-r22 in every one of the five games**. On moonrise their sentinel at `(9,3)` on **r6** sat on the only corridor crossing and our scorer could not see it.

**⚠ 6. WHAT IS NOT CLAIMED, and side lane said so first.** That this term would have won that match (untested), and that it generalises past LingLing40 (**one match, one opponent — they may simply be strong**). ⛔ **And the MOONRISE sub-story is separately scoped: there, 3 sentinels went WEST of our own core at d²_enemy 104/130/173 against range 32, and the cause was `LOKI_FWD_MIN_HARV = 2` against a 2nd harvester that landed r65, with `LOKI2_RUSH_ON = False` disabling the waiver written for exactly that window. Games 1-4 had their 2nd harvester by r6-r14 and STILL built 0/1/2/1 forward — so the gate is NOT the match-wide problem.**


### ⭐⭐ #31 — THE BARRIER FAMILY *(Magnus, s34: "a tool in the toolbox, a trick our Loki needs to use efficiently")*

**⛔ FAMILY ROW, per D30 — every new barrier plank is checked against THIS row before it is queued separately.** Members: `#29` SALT (corpse-salt, **mechanism confirmed / plank failed the kill bar**), `#31a` GUNBLOCK, `#31b` SCREEN, `#31c` VOLUME. Pairwise cross-references do not survive a third member; this is the named row.

**THE FIELD MEASUREMENT THAT DEFINES THE GAP** (`events.tsv`, top-10 teams, placement by `d2_own`/`d2_enemy`):

| team | barriers/g | forward | on their RING (d²≤12) | median d² to their core | median build round |
|---|---|---|---|---|---|
| **us** | 4.2 | **98%** | **99% of fwd** | **4** | **r65** |
| Pantheon | 15.1 | 87% | 66% | 5 | r76 |
| Clankers | 18.2 | 60% | 62% | 5 | r160 |
| **ph** | 11.0 | 96% | **39%** | **17** | r148 |
| **not adgato** | 9.4 | 77% | **29%** | **25** | r101 |
| Bean counters | 17.6 | 52% | **4%** | **52** | r118 |
| sporks (rank 1) | 3.0 | 83% | 43% | 17 | r174 |

⇒ **THREE DISTINCT DOCTRINES EXIST AND WE RUN EXACTLY ONE:** the **collar** (ring, d²≈5 — us, Pantheon, Pivot, Clankers), the **approach screen** (d²≈17–25 — ph, not adgato, sporks), the **mid-map wall** (d²≈52 — Bean counters). **We are the purest collar in the field (99%) and the earliest builder (r65 against the field's r101–174).**

**⛔ AND THE BUDGET IS UNSPENT, NOT UNAVAILABLE — this is the finding that makes the family worth a programme.** My first hypothesis was that their barriers are cheaper. **It is FALSE and measured false:** their end scale is equal or higher (Clankers 335%, Pantheon 406%, ph 400% vs our 313%), so a barrier costs everyone **7–9 Ti**. **Clankers collects the SAME median titanium we do — 880 — and spends 110 Ti/game on barriers against our 34.** Same economy, different spending. At 8 Ti a barrier against ~2.5 Ti/round of passive income alone, **we are not constrained; we simply do not buy them.**

**#31a GUNBLOCK — barrier an enemy gunner's firing ray. BEST EVIDENCED, BUILD FIRST.**
`raid.py:500-527` already computes `gun_axis` via `get_attackable_tiles_from`; **LOKI-25 uses it only to make raiders STEP OFF.** The expensive half exists and is pointed at avoidance only. **92% of our forward builder deaths are enemy gunners** (42.8% league-wide) and our own comment calls the gunner *"the AVOIDABLE one"* because its shot — unlike a sentinel's — **is blocked by obstacles**. ⛔⛔ **THE 92% IS STALE AND THIS ROW'S "BEST EVIDENCED, BUILD FIRST" RESTS ON IT. RE-CUT s34 ON THE SHIPPED TREES (v114+v115+v116, 325 games, 301 forward deaths): gunner-only 31.2% · both 33.6% · sentinel-only 34.9% · neither 0.3% ⇒ any-gunner 64.8%, ANY-SENTINEL 68.4%.** The 92% is reproducible (my all-time cut hits **92.0%**, matching the tree's comment to the decimal) **but it pools v80/v90/v91-era trees that lost 6-8 forward builders per game against the shipped 0.93 — it describes a bot we do not ship.** ⇒ **in the bot we actually run, sentinels cover MORE forward deaths than gunners do, and the gunner-ONLY share GUNBLOCK uniquely addresses is 31.2%, not 92%.** *(Consistent with LOKI-25 working — suppressing gunner deaths mechanically raises the sentinel share — but the archive CANNOT attribute it: the adjacent-version contrast runs BACKWARDS (v112 pre-LOKI-25 0.43 gunner-covered forward deaths/game and 60.0% share vs v114 post 0.59 and 66.9%), and v114 differs from v112 by more than this flag. Attribution needs a local `LOKI_GUNAXIS_PENALTY = 0` ablation.)* ⇒ **RE-RANK: "build first" is no longer supported by the evidence cited.** On the shipped tree and in ABSOLUTE terms the two rows are **effectively TIED — `#31a` 0.93 x 31.2% = 0.29 forward builder deaths/game addressable, `#30` 0.93 x 34.9% = 0.32/game** (~23 vs ~25 Ti/game). **The rate collapse hits both equally, so this is a like-for-like comparison and neither is large.** ⇒ **both are gated behind `#33`'s ablation, which is cheaper than either and tells us whether the avoidance flag already banks this population.** Magnus's arithmetic: healing is 0.25 Ti/HP but costs an ACTION EVERY ROUND; a barrier is 0.37 Ti/HP for **one action** — **7.5× better per builder-action**, and actions are this line's scarce good.
⭐ **AND IT DOES NOT INHERIT WHAT KILLED SALT.** `#29` failed on kill round (179 vs pooled 129, p=0.008) **because pecking a conveyor costs ~10 raider actions. A barrier placement costs ONE.** That distinction is the family's central hypothesis and the thing each member must be judged on.

**#31b SCREEN — forward barriers at d²13–32**, matching ph/not adgato. The one doctrine two teams above us run and we do not.

**#31c VOLUME — spend the unspent budget.** Weakest hypothesis, cheapest build. ⚠ **And volume is NOT obviously good: sporks tops the ladder at 2181 on 3.02 barriers/game.** Do not queue this ahead of a/b.

**⚠ THE COST THAT BINDS THE WHOLE FAMILY: every barrier is +1% global scale, permanently.** Our scale already prices the launcher out (`LAUNCHER_RESERVE` needs 120–160 Ti against a median r150–200 bank of **54**). **Any volume increase must be priced against that, not just against titanium.**

**⚠ FIXTURE WARNING, per the s34 self-play rule in `docs/builder-arm-retro.md`:** GUNBLOCK pays off **enemy forward GUNNERS**, and our own control bot builds few. **Local under-represents the rays this arm exists to block — local is a DOSE check, not an efficacy read.**


### ⭐⭐⭐ #32 — OUR SENTINELS DIE AT 71% AND THEIRS AT 34%. THIS IS THE LARGEST MEASURED GAP WE HAVE. *(Magnus, s34, from watching one match)*

**MEASURED on the v121 leg, 25 live games, 5 opponents, BUILD/DEATH paired on tile:**

| | built | destroyed | median lifetime | **sentinel-rounds** |
|---|---|---|---|---|
| **us** | **107** | **76 = 71%** | **12 rounds** | **5,143** |
| them | 68 | 23 = 34% | **34 rounds** | **6,618** |

⇒ **WE BUILD 57% MORE SENTINELS AND GET 22% FEWER SENTINEL-ROUNDS.** A sentinel is 18 dmg on a 2-round reload (~9 HP/round), so sentinel-rounds is the DPS-carrying quantity: **theirs delivers ~2x the SENTINEL-ROUNDS PER BUILD** (48.1 vs 97.3), **for the same 30 Ti and the same +20% scale** — and **2.8x the median LIFETIME** (12 vs 34 rounds). ⚠ **THOSE ARE DIFFERENT ESTIMATORS AND I FIRST QUOTED THE WRONG ONE:** sentinel-rounds is a SUM and medians do not compose into sums, so my original *'~3x the damage per build'* was the LIFETIME ratio wearing the DAMAGE label, overstating the per-build gap by ~40-50%. The mean is closer than the median because survivors run to game end and pull it up (31 of ours and 45 of theirs never died). **Median describes a sentinel's typical LIFE; mean-per-build describes DAMAGE DELIVERED.** Caught by the side lane, verified by research off my own table.

**⛔ AND THE THREE EXPLANATIONS I REACHED FOR FIRST ARE ALL DEAD, EACH KILLED BY A DIFFERENT CHECK:**
* **NOT count** — we build MORE (107 vs 68).
* **NOT timing** — ours land EARLIER (median r16 vs r23 in the 0033 match).
* **NOT range or siting.** I proposed that our median sentinel sat at `d²=33`, outside the r²=32 range. **Magnus refuted it from the replay — our sentinel DID fire on their core, and it was well placed.** The corpus measures `d2_enemy` to the core's single stored position while the bot takes the min over four core tiles, so the corpus figure over-estimates. **I had flagged that caveat myself and then leant on the number anyway.**

⇒ **IT IS SURVIVAL.** The turret is fine; it does not live long enough to matter. ⚠ **AND 'ONLY' IS MORE THAN THE CHECKS LICENCE — I wrote it and it is withdrawn.** The evidence kills COUNT, TIMING and SITING. **AMMO STARVATION, HEALING (theirs, not ours) and ENEMY TARGET-PRIORITY are all UNTESTED and each would produce the same signature.** The healing hypothesis in particular is not answerable on this corpus: `events.tsv` has no heal verb.

**⭐ AND THE MECHANISM IS ALREADY MEASURED FROM AN EARLIER LEG — this REPRODUCES it exactly:** s33's 75-game decode found *"41% of our sentinels destroyed, median life 12 rounds, and 100% of the destroyed sat under ENEMY TURRET COVERAGE against 64% of survivors."* **The median-12 figure reproduces to the round on a completely different leg.** ⇒ **We plant sentinels under their guns.**

**THE PLANK, and it re-points an existing one rather than inventing anything.** `#30` is *"the station scorer is blind to enemy sentinels"* — but that scorer decides **where RAIDERS STAND** (`raid.py:500-527`). **The real gap is where our SENTINELS ARE PLANTED** (`raid.py:386 _try_forward_sentinel`), which consults **no threat model at all** — it checks `can_fire_from` for offence and never asks whether the site is covered by an enemy turret. **`get_attackable_tiles_from` is already called at `raid.py:522` for gunners; the change is to run it over enemy turrets and refuse a build site inside the union of their rays**, preferring a covered-free tile that still satisfies `can_fire_from`.

**MECHANISM METRIC:** sentinel median lifetime and destroyed-share, ours vs theirs, on the same leg. **Both are already instrumented by the BUILD/DEATH tile pairing used above — no new tooling.**

**WHY IT OUTRANKS EVERYTHING ELSE QUEUED:** it is a **3x** effect on the unit that does the killing, it needs no new entity and no extra titanium, and **the failure is not that we do too little — it is that we pay for 107 sentinels and receive the output of ~37.** ⚠ **And the obvious cheap counter-hypothesis must be tested first: are theirs surviving because they HEAL them?** `events.tsv` has no heal verb, so that is not answerable on this corpus and needs a live decode or a probe.

## BLOCKED / NEEDS A NUMBER FIRST
| # | plank | blocker |
|---|---|---|
| 4 | ~~forward-death attribution~~ **DONE s31 — AND IT DISSOLVED ITS OWN PREMISE.** ⛔ *"tile exposure explains only 1.53x of a 3.47x gap; ~2.3x is unaccounted"* **WAS A RESEARCH ARITHMETIC ERROR: the terms MULTIPLY, they do not add.** `A` turrets exist 1.77x x `B` in-range 1.57x x `C` damage/turret-round 1.05x x `D` damage absorbed/death 1.15x = **3.36x, the anchor exactly.** There was never a residual. **`B`=1.57x independently reproduces the ray-based 1.53x on a different instrument.** ⇒ **The whole gap is that we take 2.92x the damage per forward builder-round, and `A` alone is 83% of what I called unexplained.** **`A` is our forward TIMING, not their build:** opponent-matched 3.94 vs 2.19 turrets alive, gap 2.96x, and **30.5% of our forward rounds sit in r500-999 against TOP's 16.8%.** `docs/research/FORWARD-DEATH-ATTRIBUTION-2026-08-11.md` |

## DEFERRED s36 — priced and parked, revisit trigger named
- **CPU-PRESSURE ROAD has a live target (Leviathan autopsy): they TLE'd 519/1/86/1473/2 truncated turns across five games (p99 ~10,4xx µs); we had 0.** An 1800-band team drowning in CPU on GRAND maps is field evidence the road (six-roads: CPU denial) has customers. ⛔ **MECHANISM TESTED s36 AND THE OBVIOUS ONE FAILS — the road stays shut.** Over the tri-arm's 75 pinned games (research, verified at the primary): **corr(OUR builds, THEIR TLE'd turns) = −0.130 on 900-area (n=30) and +0.084 on small (n=45) — i.e. ~zero. We do not drive their CPU load by putting more of our stuff on the map.** ⚠ Observational, not an intervention (our build counts were not manipulated; range-restricted), so it PRIORITISES rather than closes. **And TLE is overwhelmingly a TEAM property, not a game property: Leviathan 12,539 TLE'd turns across 15 games · Coreflood 382 (5 of 6 900-area games, 0 of 9 small) · LingLing40 3 · Juusto 0 · team lazy 0.** ⇒ **A row needs a lever we control that is NOT entity count. Until someone names one, this stays a lead — and the fact that the obvious lever is measured dead is worth more than the lead was.**
- **BFS budget efficiency on 900s (path caching/reuse so the 8ms buys more)** — builder's candidate off #44's resolution. **DEFERRED at pricing: the quality tax is real (mid-BFS truncation many turns/game) but shows NO observable cost today — 900-area kill medians 153-167 ≈ small maps, 12/14 panel + 3/3 rated won.** Revisit iff 900-area kill rounds lag small-map medians by >20 rounds on a ≥25-game pool, or idle-builder share rises on 900s. *(Why-now fails while v8 wins there; a cache rewrite is not free and spends from the same 8ms it saves.)*

## DEAD THIS SESSION — do not re-queue without new evidence
* **cap6** (`LOKI_FWD_GUN_CAP` 3->6) — **NO INFORMATION, and it is a knob-turn
  under the bar, so it does not consume a cycle.** ⛔ **REASON CORRECTED
  2026-08-11 (side lane): I first wrote "INERT BY CONSTRUCTION — we build 1.6-1.9
  forward sentinels/game so a cap of 3 never binds." THAT ARGUMENT IS WEAKER THAN
  IT READS AND I OVERSTATED IT TWICE.** (a) Those levels come from **the dose
  check itself**, not an independent source, so it is not a structural argument at
  all — it is the same measurement wearing a different hat. (b) **A MEAN CANNOT
  SHOW THAT A CAP ON SIMULTANEOUS-ALIVE NEVER BINDS** — a tail mechanism produces
  exactly the small mean shift observed. **The deciding number — the fraction of
  games reaching 3 simultaneous forward sentinels — was never computed.** The
  harness's own verdict line said `NO-INFORMATION back to the pool, NOT demoted`
  and I wrote it onto a DEAD list anyway, one hour after building the
  NO-INFORMATION branch to stop exactly that.
* **best-fit sentinel placement** — **CPU-COST REGRESSION**: with the 10 ms limit
  disabled it wins 6/6; with it on it loses 5/6. **n=12, on THIS laptop's timing
  rather than the platform's, and ⛔ THE COUNTS HAVE NO COMMITTED ARTEFACT — they
  exist in a session scrollback only. Do not re-quote them without a committed
  log.** This is the one row where machine load genuinely bites, because it is the
  only measurement that toggles `--tle`. **Worth one repeat before the ROAD is
  closed rather than just this implementation.**
* **gunner-axis / LOKI-25** — died s30 on a resolved mechanism falsifier
  (deaths -24%, presence -23%, ratio flat -2.3%). ROAD open, implementation dead.
* **forward-efficiency 880-game screen** — DROPPED for this horizon. Its protected
  denominator needs 700-900 games/arm and at 440 misses LOKI-25's own magnitude
  (3.21 vs a 3.278 threshold). Also: dwell is 17% of the gap, not 54% — the
  headline was a 120-game sampling artefact.

---

## ⛔ READ BEFORE TRUSTING ANY LOCAL NUMBER IN THIS FILE
**Our bot reseeds an UNSEEDED RNG every game** — and note the blast radius is
NARROWER than I first published: `h2h.sh` and `dose.py` run treatment and control
as **the two sides of ONE `fcode run` process**, so any load is common-mode with
no channel to favour an arm. **The 8x1024 screen, the 4,096 null and the
platform-side ferry-first leg all SURVIVE**; only best-fit's `--tle` toggle is
genuinely load-exposed. My first published cause (wall-clock CPU guard) was WRONG
— `doctrine.py:1072-1075` in our own tree says `get_cpu_time_elapsed()` reads 0
under local `fcode run`. **Exchangeable i.i.d. salt leaves pooled estimates
unbiased; the load story would have meant systematic bias. Opposite worlds.** `bots/_v130loki13/main.py:276`:
`self.spawn_salt = random.Random().randrange(97) if NOISE_ON else 0`, with
`NOISE_ON = True`. **`--seed` never controlled it.** Identical seed/map/bots gave
kill turns 109, 118, 227, 302, 527, 118.

⇒ **"SEED-MATCHED PAIRING" IS AN ILLUSION IN EVERY LOCAL BATTERY WE RUN**, including
s30's 8x1024 screens, the 4,096-game null, and `dose.py`. The noise is unbiased so
POOLED estimates stand; PAIRED designs buy nothing they claim to buy.

**`gate.py` HAS WARNED ABOUT THIS ALL ALONG** — *"paired fixtures do not pair
against a bot that reseeds"* — **and `h2h.sh` and `dose.py` both bypass gate.py,
which the standing rule calls the sole entry to a battery.** The check existed;
the tools that needed it skipped it. **Fix — AND IT DEPENDS ON THE DESIGN. ⛔ THIS LINE PREVIOUSLY READ
"pin `NOISE_ON = False` in the measured COPIES" WITH NO DESIGN QUALIFIER, three
lines under its own text saying pooled estimates stand — and it is the GENERATOR
that emitted the wrong rider onto item #8 above** (that rider was a verbatim copy
of this sentence). Fixing the symptom and leaving the source would have put the
same rider on the next plank:
* **PAIRED / seed-matched design** → pin `NOISE_ON = False` in the measured
  copies, **on BOTH sides** — pinning one arm is itself a non-treatment difference.
* **POOLED estimate — which is every battery we actually run** (`h2h.sh`,
  `dose.py` and `overnight.sh` all put both arms inside ONE `fcode run`, so the
  pairing is WITHIN-GAME and automatic, and noise inflates the band rather than
  biasing the estimate) → **LEAVE IT TRUE** and fire under
  `gate.py --pooled-not-paired "<reason>"`.
  **⛔ Pinning here is NOT free, measured s32: with `NOISE_ON = False` the engine
  returned ONE distinct outcome across 6 seeds on antler, atoll AND meander —
  3 of 4 maps — so a shard's effective n collapses toward its (map, bot-pair)
  CELL COUNT while every denominator still prints in full.** See QUEUE #15.
* **And `tools/det.py:135-142` — the file `gate.py` cites as its authority for
  "ALL sides must be deterministic" — already carries this warning, prints
  `LOW REPLICATION`, and exits 2.** The prescription and its own cited authority
  contradict each other.
