# NO PRE-REGISTRATION EXISTS FOR LOKI-14 YET. THIS TREE HAS NOT FIRED A LEG.

**This file previously contained LOKI-8's pre-registration**, carried over
verbatim when this tree was copied from `bots/_v124loki8`. It has been emptied
deliberately.

**Why, and it is the reason this matters more than the tidiness suggests:** the
disclosure that the inherited prereg was LOKI-8's lived in `DESIGN.md` — the
file a HUMAN reads — while the stale bars lived in `PREREG.md`, the file a
SCRIPT reads. A successor grepping `bots/*/PREREG.md` would have found LOKI-8's
bars and LOKI-8's fixture filed under the LOKI-14 directory and had no way to
know. That is the "the correction lives in the index, but the file itself still
reads wrong" shape, in miniature.

**LOKI-14's leg pre-registration, when it is written, goes in
`docs/prereg/`** with both clocks, like every other leg on this programme — not
here. This file is a pointer and must stay one.

**What this tree IS:** launcher kidnap as crash-induction, a DESTINATION-ONLY
change to behaviour v102 already had (it already threw adjacent enemy builders;
this changes where they land). Split-throw design — half to map-border tiles,
half to interior, the interior arm being a within-leg placebo that must read
~0. See `DESIGN.md` in this directory and
`docs/research/engine-source-crash-and-launcher-2026-08-10.md`.

**IT MUST NOT FIRE A LEG UNTIL MAGNUS HAS PUT THE ORGANISERS' NORMS QUESTION.**
Crash-induction is held on norms, not on evidence.
