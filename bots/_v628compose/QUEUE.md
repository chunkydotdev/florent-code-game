
### ⭐ STOCKED s57 2026-08-22T18:23:16Z (research arm) — **TWO rows, both BUILDER HANDOFFS from the s56 wrap ("inherited, unadmitted, named so they are not dropped"), now through the admission gate. GREPs run THIS SESSION against `bots/_v628compose` — the head per the 14:18:59Z adoption clause + the 16:58:38Z byte-fix (Q18 discipline: keyed on the adoption clause, not traffic). Game context: in-game Florent Code League.**

| # | plank | GREP | mechanism metric | fixture | why now |
|---|---|---|---|---|---|
| **125** | **V629 MINUS-ONE DECOMPOSITION — which of the three composed defence planks is buying the negative center? Admitted WITH A BINDING PRECONDITION: does not fire at current defence economics.** CHANGE: three minus-one arms off `bots/_v629reaim` (= head + exactly three plank families, see GREP): −CAP (drop `SK_REAIM_CAP_LIVE`), −REAIM (drop `SK_DOOR_REAIM`+subflags), −SWEEP (drop `SK_ARMED_SWEEP`+`_MARGIN`), each vs head-dup control. This is POWERED-V629's own registered line (f), deferred by the builder's readout. | **GREP (s57, vs `bots/_v628compose`):** full flag-assignment diff arm-vs-head enumerated by `comm` over `^SK_* = ` lines — arm adds EXACTLY `SK_REAIM_CAP_LIVE=True`, `SK_DOOR_REAIM=True` (+`_CAP=2`,`_FLOOR=40`,`_PRI=5`), `SK_ARMED_SWEEP=True` (+`_MARGIN=2`); **head-only diff EMPTY — pure addition, NO weld** (the search could have returned a weld or a value change and returned neither; sixth-weld un-weld already carried inside `SK_REAIM_CAP_LIVE` per its own comment sk_maps.py:1878). Rotation instrument EXISTS and is validated: `scratchpad/s56_def_rotations.py` (the (c) decodes hit the exact seventh-rotation round 3/3). Hot-turn: flag removals only, reduces. | per-arm rotation churn/game (composite measured 3.85→2.95, ≈9 Ti/game) + the seventh-rotation decode attributing WHICH plank's refusals move · primary candidate−dup game share per grid · timely-checkmate non-regression guard (RELIGHT's failure mode). | local powered two-grid per the two-opponent rule (G1 Mjolnir seeds / G2 `_v542wave`), duplicate control — the registration structure already exists in POWERED-V629 (f). | **Why now, honestly: NOT now — the composite priced −0.56 [−4.80,+3.68] / −2.22 [−5.93,+1.48] with both null pairs covering 0, and the builder's arithmetic BINDS: decomposing a ±1pp null at the same power buys three smaller nulls. PRECONDITION (either): (a) an economy plank materially raises converts/contact-game (drip study: converts are a SHOT counter, 15 shots/contact-game vs fixture 52.5 — the cap's 9 Ti/game saving is priced against demand that does not currently exist), or (b) a composition candidate wants ONE of the three pieces — then fire ONLY that piece's minus-one. Stocked so the banked pieces (readout's own words: "candidate composition pieces if the defence economics change") survive the seam with their fire condition attached.** Source: POWERED-V629 readout + cert (tail 17:34-17:36Z). |
| **126** | **V631 BELT-FLOW WATCHDOG — Magnus design seed (s56 17:59:33Z, verbatim on the tail): give the CORE a delivery-flow sensor, because every belt sensor we own is DEATH-driven and the straw/prank/body-block class steals flow WITHOUT deaths.** CHANGE: core-turn delivery accounting (balance delta minus deterministic passive; deliveries land as +10 quanta at end-of-round distribution) + pipeline watch (`get_stored_resource` over trunk tiles inside core vision r²=36; stacks move 1 tile/round so terminal-empty-while-upstream-full LOCALIZES the break, detection delay ≈ break distance — Magnus's own intuition, stated in the seed) + publish rounds-since-delivery + visible-transit count for keeper/denier consumers. | **GREP (s57, vs `bots/_v628compose`):** `get_stored_resource` call sites in head = **ZERO** (function-name enumeration over all .py — the search returns sites when they exist; it returned none); no rounds-since-delivery/flow/income-stall accounting (4-pattern sweep, zero hits); existing words are NOT this sensor (`SK_SLOT_DRIP`/`SK_SLOT_STALL` are SIEGE-ENGINEER forward-turret words, sk_roles.py:6650/6701; `SK_SLOT_BELT` is a belt-map word). **⛔ THE CONSTRAINT THE GREP FOUND: ALL 16 STORE SLOTS ARE ALLOCATED** — sk_maps.py:2385 marks slot 15 (`SK_SLOT_COREFIRE`, writer: CORE) "THE LAST FREE SLOT" and it is taken. The seed's "publish to a store slot" therefore means BIT-PACKING an existing word: natural candidate is spare bits of `SK_SLOT_COREFIRE` (same writer — no same-round write collision); `SK_SLOT_STALL` has bits but a DIFFERENT writer (collision must be ruled out first). A bit-budget audit is part of the build, not an afterthought. Hot-turn: adds work on the CORE turn only (bounded trunk scan), raid hot path untouched — "adds (core), neutral (raid)" pending measurement. | detection latency in rounds from an INDUCED flow-break to the published flag, as a function of break distance (the seed's own prediction: delay ≈ in-transit drain ≈ break distance) · false-positive rate in break-free control games (must read ~0 — the control that can fail) · localization: does the flagged tile match the induced break tile. | local deterministic games with SCRIPTED breaks at known round+tile (belt cut / straw-style adjacent tap / body-block), plus the break-free control — instrumentation row: no game-share claim, validation per the standing instrument rule (each guard driven to the other verdict). | Why now: Magnus direct seed (jumps the queue per standing rule; matched against existing rows — none covers flow sensing, see GREP); the drip study measured the income cliff LEADING the convert stall by median 18 rounds in 75% of games (n=55) with no sensor naming round or tile; this is the enabling sensor for any economy-defence verb and the named consumer of the analysis-side delivery ledger (s56_drip_lib, exact 250/250). Priority per the builder's own handoff: v631-class instrumentation BEHIND the v630 tube-guard. Source: tail 17:59:33Z seed + drip study `STUDY-drip-collapse-decomposition-2026-08-22.md`. |
