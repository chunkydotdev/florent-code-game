2026-08-10T14:59:24	v104	k=32	rating=1664	net5=-31.0	peak=1698	drawdown=-34.0	armed=True	RULE=SLOT FREE	sprt_fast=OK	sprt_slow=OK	net_act=+49.0

THE SLOT RULE HAS FREED THE SLOT: v104 rolling last-5 net -31.0 (<= -21) after 32 matches.
This is a STOP-LOSS and a WAKE, not an evaluation of the bot — it
permits a swap, it does not order one.
ROLLBACK: .venv/bin/fcode submission activate <rollback version>
See HANDOVER.md for the current rollback target.

--- BUILDER DISPOSITION AT s27 WRAP, 2026-08-10 ~15:0x CEST ---
RULED: HOLD. This firing is the slot rule's PERMISSION branch, not a verdict.
The pre-committed rollback rule is a CONJUNCTION:  net5 <= -21  AND  net_act < 0.
  net5     = -31.0   -> condition met
  net_act  = +49.0   (rating 1664 vs the 1615 activation baseline) -> NOT met
So the conjunction is FALSE and v104 stays live. It is 49 points ABOVE the
rating it was handed.
ROLL BACK ONLY IF THE RATING DROPS BELOW 1615 WHILE net5 STAYS <= -21:
    .venv/bin/fcode submission activate 102     # VERSION INT, THEN VERIFY
This file is left in place deliberately so the firing stays visible to whoever
boots next. Do not clear it without re-reading the conjunction.
