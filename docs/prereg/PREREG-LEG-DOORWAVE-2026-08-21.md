# DRAFT PREREG — `DOORWAVE`: does re-enabling `_door_turret_turn` (the `#96` flag flip, `FS_V515_DOOR_OFF = False`) remove or delay the FORWARD TURRET that is the whole kill channel of two live opponents? — a pinned TWO-OPPONENT × TWO-TREATMENT unrated MECHANISM/DOSE leg

## ⭐ RATIFIED — LOCKED BY THE BUILDER (s53, 2026-08-21). Every ⟨RATIFY⟩ tag below is resolved either inline (marked RATIFIED) or in §RATIFICATION at the end of this file.

* **This file is a DRAFT.** It is not locked, it registers nothing, and no leg
  may be fired against it in this state. **The BUILDER lane ratifies every
  judgment line, runs `tools/prereg_check.py`, and types the lock commit.**
  Until that commit exists there is no clock 1 and therefore no pre-registration.
* **Drafted by a FRESH agent with NO inherited session context** beyond the five
  files named under `PROVENANCE`. This agent read no result tape, no
  `HANDOVER.md`, and no s53 session message other than the commission file. It
  did open three anchors the commission points at by name and nothing else:
  `PROGRAMME.md:600-670` (to confirm the `DEFENCE_ADMISSION_BAR` estimator form),
  `git show 13c2f5465 -- docs/coordination.md` (the KLADDEDOSE **amended v2**
  readout the commission cites), and `tools/prereg_check.py` (to satisfy its
  token vocabulary). **Every number that came from the commission rather than
  from this agent's own shell is labelled (relayed) and the builder re-verifies
  it at lock.**
* **Every line marked ⟨RATIFY⟩ is the builder's to set, change or delete.** The
  drafter proposes; the lane decides.
* **What this agent wrote:** this file, and nothing else. No commit, no bot edit,
  no `QUEUE.md` row, no `BARS.tsv` row, no challenge, no match, no `fcode` call.

**PROVENANCE:** `scratchpad/s53_doorwave_prereg_commission.md` ·
`docs/research/PREREG-amendments-and-lock-obligations-2026-08-09.md` ·
`docs/prereg/PREREG-LEG-KLADDEDOSE-2026-08-21.md` ·
`docs/research/REPLAY-STUDY-farming200s-v19-2026-08-21.md` ·
`docs/research/REPLAY-STUDY-notadgato-v25-rush-2026-08-21.md`

**STATUS: DRAFT, written BEFORE the lock commit exists, BEFORE any submit,
BEFORE any challenge is issued, and BEFORE any game of this leg is created.**
Drafting-shell wall clock **`2026-08-21T13:36:23Z`** (`date -u`, same shell);
repo HEAD at draft **`8a5a7b6101f4dd950e31bf4ead7cd9c22587908b`**
(2026-08-21 15:35:26 +0200).

**LOCK, clock 2 (platform leg).** Per the obligations doc's 16:3x addendum the
second clock is the **first accepted match's platform `createdAt`**, quoted
verbatim beside the lock commit's git author time. *(The 2026-08-17T07:24:55Z
local-shard addendum does NOT apply — this leg produces no shard tape.)*
⚠ **This leg has a THIRD clock the builder must also record, and it is not
optional: the `date -u` of every SUBMIT and every ROLLBACK** (§FIRE PLAN). Two
clocks certify the lock; the submit/rollback pair is what makes the rated-leak
claim checkable afterwards.

---

## ⛔ WHAT THIS LEG CAN AND CANNOT BUY — STATED FIRST, BECAUSE IT SETS THE DESIGN

**THIS IS A MECHANISM/DOSE LEG. IT IS NOT A CURRENCY READ, AND NO SENTENCE IN
ITS READOUT MAY BE DENOMINATED IN GAME SHARE.** At 25 games per arm-cell and the
unrated within-opponent DEFF of 1.434, a game-share number carries a ±23.5pp
half-width — it cannot separate a rout from a rout in the other direction.
`CLAUDE.md`: *a 25-game window is a DOSE AND MECHANISM probe; a currency read
requires pooling windows.*

⇒ **What this leg buys is COUNTABLE, not estimated:** against two live opponents
whose ENTIRE measured core-kill channel is a forward turret planted inside
`FS_DOOR_DSQ = 40` of our own core, **does the disabled door verb, once
re-enabled, actually FIRE — and if it fires, does the turret die or the kill get
delayed?**

**WHY TWO ARMS ARE MANDATORY AND A RE-ENABLE-ONLY LEG WOULD BE UNINTERPRETABLE.**
The off-switch that this leg proposes to flip was not a guess: it measured
**+36pp on `_v468kladturbo` and +18.9pp on `_v488beltbreak2`**
(`REPLAY-STUDY-farming200s…:418-419`, §7 RANK 1), i.e. **turning the verb OFF
was a large positive on two independent grids**. Its mechanism guess — *"door
pecks spend home actions and 2 Ti/peck out of a bank the earlier turret has
already tightened"* (`doctrine.py:3118-3131`, quoted at
`REPLAY-STUDY-farming200s…:563-564`) — **is opponent-independent and could bite
here too.** The study's own words: ***"The leg must therefore be a real two-arm
test, not a re-enable"*** and ***"Two arms or nothing"***
(`REPLAY-STUDY-farming200s…:565, 687`). A treatment-only arm compared against a
banked rated baseline would confound the flag with the fixture, the map pool and
the opponent's own release schedule.

**WHAT IS ACTUALLY IN DISPUTE, stated so the leg is not read as a re-litigation
of the off-switch:** the off-switch's evidence base is **entirely our own bots**
— the doctrine block says so verbatim: *"every game in both was played against
`_v488beltbreak2`, our own bot, so 'the field plants door turrets and we should
ignore them' is NOT what was measured"*. Under `CLAUDE.md` point 6 that is a
**hypothesis, not a refutation**, and under consequence 3 our own probes *"lie in
a known direction"*. **These two opponents are the first LIVE cases measured
whose entire kill channel sits inside the verb's own trigger band.**

⚠ **ENGINE-SIDE FACTS ONLY.** Per `CLAUDE.md`'s LOKI-14 rule, platform-downloaded
replays carry `BotOutput` events with an EMPTY `stdout` field — **30,664 of
30,664**. Every quantity below is read from positions, entity BUILD/DEATH events
and HP deltas on the wire. **No arm tag, dose counter or flag state is read out
of our own printed output; a leg that plans to is planning on an instrument that
does not exist.**

---

## REGISTRATION BLOCK

**STATUS:** DRAFT — written BEFORE the lock commit, BEFORE any submit, BEFORE the first challenge, BEFORE any game exists.

**PLANK CLASS:** ⟨RATIFY⟩ **defensive** — `_door_turret_turn` is a HOME-defence verb: it melees an enemy turret standing inside `FS_DOOR_DSQ = 40` of OUR OWN core (`main.py:1708`, `doctrine.py:2654`). *(Consequence, and it is deliberate: `PROGRAMME.md`'s `DEFENCE_ADMISSION_BAR` — `r300_crossing_non_regression` — **BINDS on this leg**, and is registered under `KILL-ROUND NON-REGRESSION:` below. The s31 amendment admits defence only where it does not slow the kill; this plank spends builder-rounds and 2 Ti/peck at home, which is exactly the budget the assault competes for.)*

**SURFACE:** unrated (platform challenge, `fcode match unrated`).

**CLUSTER UNIT:** match — **within a CELL the opponent AND the opponent version are fixed by the pin, so the OPPONENT cluster cannot hold more than one member and dies; the MATCH cluster survives** (5 games share an opponent, a version and one ladder slice). ⇒ **DEFF 1.434** (unrated, within-opponent) for every registered read, all of which are within-cell. **NO cross-cell pooled bar is registered** — a pooled read over both opponents would revive the opponent cluster (DEFF 1.833) and is explicitly out of scope (§WHAT THIS LEG DOES NOT REGISTER).

**PINNED:** YES, both cells, **every accept, no exceptions** — `fcode match unrated <team_id> --match <source_match_id>`. **PIN TREATMENT LEGS, NEVER PIN CALIBRATION PANELS**; this is a treatment leg on both cells, and both opponents are perishable (§CELL VERSION CHURN).

**CELLS:** two opponents × two treatments = **four cells**.
* **CELL F (farming_200s, pinned v19)** — pin id **(relayed)** `65c3a6eb-af59-410f-b05b-0efdf0e4c74c` (their 12:52:xx match vs us; commission states oppver 19 verified on the corpus row). Sub-cells **F-C** (control) and **F-T** (treatment).
* **CELL A (not adgato, pinned v25)** — pin id `e0e72d55-c9cb-4213-8f5f-f1855cc66c2b` (their 12:01:10Z 5-0 over our v176; `REPLAY-STUDY-notadgato…:22, 484`). Their team id `fb0e7053-f8f3-4cc8-a38f-1856a518c7d2` (`REPLAY-STUDY-notadgato…:489-492`). Sub-cells **A-C** (control) and **A-T** (treatment).
  ⚠ **farming_200s's team id is NOT in any named input** — the builder supplies it at lock (⟨RATIFY⟩, and it is a GAP, §GAPS).

**CELL VERSION CHURN:**
* **CELL A — HIGH, and measured in a named input: THREE distinct versions inside ~6 hours on 2026-08-21** — v23 last seen 09:52Z, v24 at 10:12Z/10:32Z, v25 from 10:52Z (`REPLAY-STUDY-notadgato…:480-484`), and the study's own caveat is that *"v25 may already be superseded by the time this is read"* (`:640`). Per **OBLIGATION 14** a high-churn cell is **REPORTABLE BUT NOT POOLABLE**; the PIN is what makes it readable at all, and this leg's CELL A result may not be pooled with any unpinned not-adgato games.
* **CELL F — NOT COUNTABLE FROM THE NAMED INPUTS. ⟨RATIFY⟩ the builder counts distinct farming_200s versions over the preceding 24 h off `corpus/league_matches.tsv` at lock** (free, per OB14). What the inputs DO establish: their v19 is current, **confirmed 2026-08-21T12:41Z** (`REPLAY-STUDY-farming200s…:51`), they have shipped **12 versions** over their history (`:107-113`), and **v18→v19 was a TOTAL turret-doctrine flip** (v18 = 7.32 gunners/game and 0.28 sentinels; v19 = 0.00 gunners and 1.60 sentinels, `:118-122`). ⇒ **an unpinned leg here would be measuring their release schedule**, which is why the pin is mandatory rather than nice.

**TREATMENT TREE:** **RATIFIED AND BUILT — treatment arm = `bots/_v544doorflip` (commit `4366121aa`), control arm = `bots/_v542wave` (the live holder v177), digests recorded at lock (`md5 -q`, builder's shell):** eco.py `c954b674c566abb5d07fae17554b530d` · main.py `fe037a69dd64ace9db606ab06c1654a8` · raid.py `e0e74d83880f4379115fa5ae2064b2ff` · siege.py `a798cb53522644e3dfcd9a2097409938` — **all four IDENTICAL in both trees**; doctrine.py control `20848e531b50ff5f8b27b75ce2218141` vs treatment `df90e9320233a3a22e1f341132c02f74` — **the ONLY differing module, one flag line + its comment.** Import-confirm run at lock per the clause below: control reads `FS_V515_DOOR_OFF = True`, treatment reads `False`.
⛔ **AND THE FLAG VALUE IS VERIFIED BY IMPORT, NOT BY READING THE ASSIGNMENT.** The tree's own doctrine warns that arm construction **APPENDS overrides to the end of `doctrine.py`**, so a read-the-assignment check is order-dependent (`doctrine.py:3205-3215`, `:6050`; method recorded at `REPLAY-STUDY-farming200s…:370-376`). ⇒ **Both arms' effective flag values are confirmed with `python -c "import doctrine as d; print(d.LOKI_FS_V515, d.FS_V515_DOOR_OFF)"` against each pinned tree, and the two lines are pasted into the lock.** A treatment arm whose import reads `True` is not a treatment arm.

**MECHANISM METRIC READS:** bots/_v544doorflip/doctrine.py:3132 (`FS_V515_DOOR_OFF`) — the gate itself (the treatment tree's changed line), guarding `_door_turret_turn` (`bots/_v544doorflip/main.py:1708`, byte-identical to the control's) at the early return `if LOKI_FS_V515 and FS_V515_DOOR_OFF: return False`.

**TREATMENT DIFF TOUCHES:** bots/_v544doorflip/doctrine.py

**TREATMENT DIFF REFS:** 4366121aa^ 4366121aa

*(RATIFIED: the refs are the commit adding `bots/_v544doorflip`. That commit is a whole-tree add (git has no rename-with-one-line-change view of a new directory), so the DIGEST TABLE above is the binding one-line proof: four modules byte-identical, doctrine.py differing by the flag line + comment. `git diff 4366121aa^ 4366121aa -- bots/_v544doorflip/doctrine.py` contains the changed line and the OB13 metric file is in the diff.)*

**INTERSECTION:** yes — **and the honest shape of it is stated rather than asserted, because this leg has LOKI-18's silhouette and escapes it for a specific reason.** LOKI-18 spent 25 games on a metric that sat behind a guard in a file **byte-identical between arms**. Here `main.py` IS byte-identical between arms. **The escape is that the read path is registered at the FLAG, not at the verb: `doctrine.py:3132` is the changed line, it is the gate, and the verb's behaviour is wholly determined by it.** And the measured quantities are **ENGINE-SIDE outcomes** (enemy turret deaths, our builder attack events on enemy turret tiles, core-death rounds) — not a code predicate that could read the same in both arms. ⇒ **OB13's actual guarantee holds: the measured quantity is downstream of the changed line, and the leg's own DOSE COLUMN is the direct test of that** (control arm must read 0 door pecks by construction; a non-zero there is an instrument alarm, not a finding).

**METRIC WINDOW:** r0-r1000 — the whole game for CELL F (their plants land r2-r242, median r32.5; their sentinel deaths we recorded run r1-r878 before game end, `REPLAY-STUDY-farming200s…:220-226`). **Per-metric reading windows inside it are declared with each metric in §MECHANISM METRICS** (CELL A's primary nest count is read at r70; CELL A's delay metric is censored at plant+150).

**GATING CONSTANTS:** **no ROUND-NUMBER gate sits on the door verb's call path in any named input.** The constants the inputs name are `FS_DOOR_DSQ = 40` (a **squared distance**, `doctrine.py:2654`), `FS_DOOR_TI_FLOOR = 6` (a **titanium** floor), `FS_DOOR_MAX_RNDS = 40` (a **per-target budget in BOT-ROUNDS**, `doctrine.py:2659` — a spend cap, **not** a round-number window), and `FS_DOOR_TYPES = frozenset((GUNNER, SENTINEL, LAUNCHER))` (`doctrine.py:2662`, import-confirmed, **BARRIER excluded** — see §108-EXCLUDED). `LAUNCHER_MIN_RND = 160` (`doctrine.py:1735`) IS round-valued but gates the **home launcher**, a different verb, untouched by this leg's diff. ⟨RATIFY⟩ **the builder greps the verb's call path for any round gate the studies did not surface.**

**MECHANISM CAN OCCUR IN WINDOW:** yes — unconditionally in the treatment arm once a target of a type in `FS_DOOR_TYPES` stands inside `FS_DOOR_DSQ = 40` of our core, which is **96 of 96 of CELL F's sentinels** (median d² = 13, `REPLAY-STUDY-farming200s…:152-156`) and **4 of 4 of CELL A's nest sentinels in 5 of 5 games** (measured d² 2-16 from our footprint, `REPLAY-STUDY-notadgato…:344, 592-593`). **The trigger condition is satisfied in essentially every game of both cells before the treatment is even armed** — which is why the DOSE column, not the trigger, is the question.

**BAR:** ⟨RATIFY⟩ **three bars, each declared with its own kind, per the OB16 corollary that a POINT RULE and an MDE-bearing bar are invisibly different from the number alone.**

1. **GATE 1 — DOSE (per treatment cell, MDE-bearing by construction against a null of ZERO).** ≥ **19 of 25** treatment-arm games carry ≥1 **door-verb peck event** (§DOSE COLUMN). Wilson-95 lower bound at DEFF 1.434 (n_eff ≈ 17.4) then exceeds 50%. **Null = 0.00% by construction** (the verb returns before acting in the control arm), so **clearing this bar excludes 0 and every rate up to 50%** — it is not a point rule.
2. **CELL F PRIMARY — POINT RULE ONLY.** `share of games with ≥1 of their forward sentinels (d²≤32 of our core) killed by us more than 2 rounds before game end`, **treatment minus control**, must have a 95% interval that **EXCLUDES 0**. At n=25 per arm, DEFF 1.434, p̄≈0.40, the two-arm half-width is **±32.5pp** ⇒ the observed difference must be **≥ +32.5pp**. **⛔ THIS BAR LICENSES NO EXCLUSION OF ANY EFFECT SIZE: its MDE is 0.00pp by construction and it may never be quoted as having excluded a modest effect.** §BAR SIZING states what n a harm-exclusion would cost and why it is not bought.
3. **CELL A PRIMARY — DELAY, MDE INSIDE THE BAR (OB16 preferred form).** `plant-to-core-death lag` = rounds from the FIRST enemy home sentinel (d²≤41 of our core) to OUR core's death, **censored at +150** (a game where our core survives that long scores 150). **BAR = 0 (null) + MDE(+5 rounds) + half_width(0.664 × sd_pooled)**, on `treatment mean − control mean`, where `half_width = 1.96 × sd_pooled × sqrt(1.434 × (1/25 + 1/25)) = 0.664 × sd_pooled`. **Clearing the bar means the 95% interval excludes BOTH 0 AND +5 rounds.** Pre-committed bar values so nothing is chosen after the data:

```
sd_pooled =  4 rounds  ->  half_width  2.66  ->  BAR = +7.66 rounds
sd_pooled =  8 rounds  ->  half_width  5.31  ->  BAR = +10.31 rounds
sd_pooled = 12 rounds  ->  half_width  7.97  ->  BAR = +12.97 rounds
sd_pooled = 16 rounds  ->  half_width 10.62  ->  BAR = +15.62 rounds
sd_pooled > 15 rounds  ->  THE BAR IS UNRESOLVED AT THIS n AND DEFAULTS TO THE RESTRICTION
```

**BAR SOURCE:** all three bars are **CONSTRUCTED ON THIS PAGE**, none is copied from a banked number; the constructions are:
1. **GATE 1** — constructed on this page: null 0.00% (a CODE fact: the control arm's verb returns at `main.py`'s `FS_V515_DOOR_OFF` early return) + the 50% majority boundary. The 19/25 and 6/25 thresholds are the **same Wilson/DEFF arithmetic at the same n and the same DEFF** as `PREREG-LEG-KLADDEDOSE-2026-08-21.md:134-136`, reused deliberately so the two legs' gates are comparable. ⟨RATIFY⟩ **the builder recomputes them rather than copying.**
2. **CELL F** — constructed: `control-arm observed share + two-arm half-width`. Not copied from any banked number; the study's 25.0% is the **expected control value** (below), not the bar.
3. **CELL A** — constructed: `0 + MDE(+5) + half_width`. **The MDE of +5 rounds is taken from the study's own registered expectation for a displacement-class intervention** — *"A single successful throw should push the first number by ≥5 rounds"* (`REPLAY-STUDY-notadgato…:559`) — and is adopted here as the smallest delay worth calling a hit. ⟨RATIFY⟩.

**BASE RATE:** all pre-registered from the two banked studies, and **all are EXPECTED CONTROL-ARM VALUES, never the bar's comparator** (§REFERENCE n):
* **CELL F, primary:** **25.0% (15 of 60 games)** — games in which we killed ≥1 of their forward sentinels more than 2 rounds before game end (`REPLAY-STUDY-farming200s…:213-215`).
* **CELL F, mechanism:** **21 of 96 of their forward sentinels killed (21.9%); median lifetime when killed 74 rounds; 75 of 96 (78.1%) still standing at game end; replant failure 14 of 21 (66.7%), median replant latency 34 rounds — over 60 archived v19 games** (`REPLAY-STUDY-farming200s…:198-206`).
* **CELL A, primary (delay):** first enemy home sentinel at **r30 / r40 / r35 / r43 / r43**; our core dead at **r47 / r59 / r55 / r62 / r66** ⇒ **plant-to-death lag 17 / 19 / 20 / 19 / 23, mean 19.6, over 5 games** (`REPLAY-STUDY-notadgato…:76-80, 88-94, 129-133`).
* **CELL A, mechanism:** **enemy home sentinels alive at r70 = 4 / 4 / 4 / 4 / 4 in 5 of 5 games**; enemy home-sentinel deaths by r70 = **1 of 5 games** (and that one at r59, the round our core died); rounds from the first enemy home sentinel to the SECOND one dying = **never, 5 of 5** (`REPLAY-STUDY-notadgato…:501-507, 578`).
* **BOTH CELLS, dose:** historical: **0 door-verb pecks, by construction** — the verb returns before acting in every game we have ever played on this tree. **RATIFIED — DOSE PATH (i) EXECUTED AT LOCK, BOTH VERDICTS DRIVEN (the REACHABILITY-DRIVEN line, adopted this session after the KLADDEDOSE §HP-GATE registration defect): local paired drive vs `bots/_probe_doorlaunch` (a door-planting fixture, launchers ∈ FS_DOOR_TYPES), same map+seed (skald s11): treatment 60 vs control 0** — treatment `bots/_v544doorflip` produced **60 builder-attack events** (the door verb executing against planted door launchers), control `bots/_v542wave` produced **0** on the identical cell; both arms won by core-destroy r142/r143 (the flip did not break the kill on this fixture). Replays: `scratchpad/s53_doorwave/flip_on.replay26` / `flip_off.replay26`, decoded with `tools/corpus/replay_autopsy.py` (driven to both verdicts on this surface earlier today).

**BASE RATE SOURCE:** the two named replay studies, each read at the line ranges quoted above. Populations, stated inline because numbers carry subjects: **CELL F = 60 ARCHIVED rated games against their v19** (the metadata population is 65; five games of the `2026-08-20T18:32:59Z` match have no archived replay, and that match was a WIN for us, so the replay set is very slightly biased against us — 46.7% in replays vs 47.7% in metadata; `REPLAY-STUDY-farming200s…:65-72`). **CELL A = 5 games = ONE match = one opponent, one version, one 20-minute ladder slot** (`REPLAY-STUDY-notadgato…:633-635`), and the study says in terms that *"every '5/5' here is descriptive, not inferential — do not attach a confidence interval to it."* **⇒ CELL A's baselines are pre-registered EXPECTATIONS, not estimates, and the leg's own control arm is what carries the interval.**
⚠ **ONE INTERNAL DISCREPANCY IN A NAMED INPUT, reported rather than silently resolved:** the adgato study gives our core-death rounds as **47 / 59 / 55 / 62 / 66** in its per-game table (`:88-94`) and as **47 / 56 / 59 / 62 / 66** in its leg-design section (`:504`) — one value differs (55 vs 56). **The per-game table is registered as the authority** (⟨RATIFY⟩) because it is the measurement and the other is a summary of it.

**POOL ERA:** 2026-08-13T07:12:59Z..now — ⟨RATIFY⟩ **the builder re-derives the boundaries in the lock shell** with `tools/prereg_check.py`'s own `pool_eras()`; the value here is the later of the two boundaries the KLADDEDOSE lock derived at 13:0xZ today (`PREREG-LEG-KLADDEDOSE…:119`: **2026-08-13T07:12:59Z** and **2026-08-21T07:52:59Z**) and is **(relayed)**.

**SPANS-POOL-CHANGE:** yes, and only on one side, deliberately. **CELL F's pre-registered baselines are drawn from 60 archived v19 games that straddle the 2026-08-21T07:52:59Z boundary**, while this leg's games sit entirely after it. **CELL A's baseline (the 12:01:10Z match of 2026-08-21) sits entirely after it.** ⇒ **This is why the study baselines are registered as FIXTURE CHECKS and the IN-LEG CONTROL ARM is the bar's comparator** — the control arm is drawn from the same map population, the same clock and the same pinned opponent as the treatment arm, so the pool change cannot enter the primary contrast at all.

**ESTIMATOR:** declared here so it cannot be chosen after the data. **Shares: Wilson score interval on the DEFF-adjusted effective n (`n_eff = n / 1.434`)** — no normal approximation, no Agresti-Coull, no exact-binomial substitution at readout. **Two-arm share differences: `1.96 × sqrt(p̄(1-p̄) × (DEFF/n_t + DEFF/n_c))`, the two-fixture form from `CLAUDE.md`.** **The CELL A delay metric: difference of censored means with a pooled-sd normal interval and the same DEFF, `half_width = 1.96 × sd_pooled × sqrt(1.434 × (1/n_t + 1/n_c))`.** **The r300 guard: ITT RMST₃₀₀ mean difference** (§KILL-ROUND NON-REGRESSION). **No estimator swap at readout, in either direction.**

**PLANNED n:** 100 games — **25 games (5 accepted matches) in each of the four cells F-C, F-T, A-C, A-T.**

**BOUNDARY:** 20 accepts = 100 games. Counted over **ACCEPTS, never attempts** (the CAL-8 incident: a boundary counted over attempt lines; `games = 5 × accepts` is the identity that makes a miscount visible). **Per-cell sub-boundary: 5 accepts = 25 games each**, and **a cell that cannot reach 5 accepts is reported short rather than back-filled from another cell.**

**CUT-SHORT:** below **60 games** total (12 accepts), **or** below **15 games in any single cell**, the leg is **DESCRIPTIVE ONLY** — dose counts reported, no bar verdict and no falsifier verdict on the affected cell. Between those floors and the planned n, a bar may be read **only with its own interval printed in the same sentence.** ⛔ **AND AN ASYMMETRY THAT IS SPECIFIC TO THIS DESIGN: a cell whose CONTROL half completed and whose TREATMENT half did not is not a short leg, it is a BROKEN PAIR** — no difference may be computed from it, and the completed control half is banked as a fixture check only.

**REFERENCE n:** **CELL F = 60 games / 96 sentinels; CELL A = 5 games. BOTH ARE FIXED AND CANNOT GROW.** ⛔ **This is CAL-7's failure mode and it is designed out rather than tolerated: those samples are NOT the bar's comparator.** The bar's comparator in every case is **the in-leg control arm**, which grows with the leg and is drawn from the same pool. **The fixed references are used for exactly one thing — the FIXTURE CHECK below — where their inability to grow costs nothing because they are being compared for gross agreement, not for a bar.**

**FIXTURE CHECK (a positive control on the fixture, pre-committed):** the **CONTROL arm** of each cell must reproduce its study baseline in gross terms — **CELL F-C's forward-sentinel kill share within ±20pp of 25.0%**, **CELL A-C's `sentinels alive at r70` reading 4 in the large majority of games and our core dead by r70 in the large majority**. ⟨RATIFY⟩ the tolerances. **If a control arm does NOT reproduce its baseline, the fixture has moved** (their bot, our tree, or the map pool) — **the treatment-vs-control contrast is still read, and the study baseline is withdrawn from the readout rather than quietly reconciled.**

**GATE RESOLUTION:** ⭐ **OBLIGATION 12 — the gate is sized and its branches are pre-committed.** GATE 1 (dose), per treatment cell, n = 25 games, DEFF 1.434 (n_eff ≈ 17.4), Wilson-95:

```
door-verb pecks in  <= 6/25 games (<=24%)  ->  upper bound < 50   =>  DOSE FALSIFIER FIRES (never dosed)
door-verb pecks in  >= 19/25 games (>=76%) ->  lower bound > 50   =>  DOSE BAR MET
door-verb pecks in  7..18/25 games         ->  interval CONTAINS 50  =>  UNRESOLVED
```

**AND THE PRE-COMMITTED DEFAULT: AN UNRESOLVED GATE DEFAULTS TO THE RESTRICTION,
NEVER THE PERMISSION.** An UNRESOLVED dose gate means the door verb is **NOT
credited** with having been delivered; it does not mean "promising", and it does
not license reading the mechanism bars as though the dose had landed. The
permissive branch is granted only by the ≥19/25 cell.
**AND THE CELL F / CELL A PRIMARY BARS CARRY THEIR OWN RESOLUTION STATEMENTS**,
which are the honest half of this design: **CELL F's bar resolves only a
difference of ≥32.5pp** and **CELL A's bar is UNRESOLVED by construction if the
pooled sd exceeds 15 rounds.** Both default to the restriction.

**PRE-STATE (OBLIGATION 7):** ⭐ **THE PREDICTED-CHANGE SET IS VERIFIED NOT TO BE IN THE TARGET STATE ALREADY, and the outcome is stated as change IN OUR FAVOUR, never as a "mix".**
* **The dose pre-state is EXACTLY ZERO and it is a CODE fact, not an estimate:** the verb returns before acting (`main.py`, `if LOKI_FS_V515 and FS_V515_DOOR_OFF: return False`), both flags import-confirmed `True` (`REPLAY-STUDY-farming200s…:378-388`). **There is no live path in the shipped v177 tree that attacks an enemy turret planted next to our own core** (`:440-442`, and the adgato study's independent read at `:402`: *"Against a home nest, our builders can neither shoot it nor peck it."*).
* **The CELL F mechanism pre-state is at the floor and far from the target:** 21.9% of their forward sentinels killed, **75 of 96 alive at game end**.
* **The CELL A mechanism pre-state is a hard 4/4/4/4/4 with our core dead in 5/5.**
⇒ **Every predicted-change cell is measured away from its target state, so this prereg can fail honestly.**

**MAP SEGMENT:** none expected — **and the reason is structural, not an absence of thought** ⟨RATIFY⟩. The verb's trigger is **core-relative, not terrain-relative**: `FS_DOOR_DSQ = 40` is measured from OUR OWN core, and the target has by definition already been planted inside it. Terrain governs **when** a target appears — CELL F's approach window collapses to ~3 rounds on close-core maps and the plant lands r1-r2 on 10×10 `fjordgate` (`REPLAY-STUDY-farming200s…:776-784`) — but **it does not govern whether a peck lands on a 40 HP turret standing three tiles from our core.** The terrain dependence sits **upstream of the verb**, in the denominator, and divides out of a metric conditioned on a target existing.
⛔ **CONSEQUENCE, pre-committed: NO per-map or per-segment read is registered, and a POOLED FAIL MAY NOT BE RESCUED BY A MAP CUT FOUND AFTERWARDS.** At 25 games per cell over a ~15-map pool a per-map cell holds 1-2 games and resolves nothing. **Any map pattern observed is POST-HOC and must be presented as such.** *(Per OB15c a genuine segment re-screen is a NEW leg with its own n — the rows that suggested a segment cannot also confirm it.)*

**DOSE:** share of games carrying ≥1 door-verb peck event — treatment arm REQUIRED ≥19 of 25 games vs flag-off control 0 of 25 games (n=25 per arm per cell; the control value is a CODE fact, not an estimate — the verb returns before acting).

⛔ **AND THE HONEST STATUS OF THAT LINE: THE LIVE DOSE IS THE QUESTION, NOT A PRECONDITION — and this draft states that plainly rather than manufacturing a dose it does not have.** The named inputs contain **NO local paired both-ways dose probe for `_door_turret_turn`**. What they contain is: (a) the CONTROL-arm dose is **0 by construction and by code read** — the verb returns before acting, flags import-confirmed; (b) the TREATMENT-arm dose is **GATE 1 of this leg**; (c) the trigger condition is satisfied in essentially every game of both cells (`MECHANISM CAN OCCUR IN WINDOW`, above); and (d) the verb's own docstring records a fixture-side measurement of the same mechanism — *"a sentinel is 40 HP and a builder peck is 2 damage for 2 titanium — two bodies finish it in ten rounds, and the bank held the titanium every time"* (`main.py:1655-1664`, quoted at `REPLAY-STUDY-farming200s…:1014-1019`) — **which is a FIXTURE claim and is precisely the class `CLAUDE.md` consequence 3 says lies in a known direction.**
⟨RATIFY⟩ **The builder chooses one of two paths and writes the choice into the lock:** (i) run a LOCAL paired dose check (arm-on vs arm-off, door-peck events counted, driven to BOTH verdicts) before the lock, and register its numbers here; or (ii) register explicitly that **GATE 1 IS the dose probe and this is a dose leg** — the KLADDEDOSE precedent, whose own lock says *"the live dose IS the question, not a precondition — which is precisely why this is a dose leg and not a screen"* (`PREREG-LEG-KLADDEDOSE…:165`). **Path (ii) is legitimate and is the drafter's recommendation; path (i) is cheaper insurance. What is NOT legitimate is leaving it unsaid.**

---

## §DOSE COLUMN — ⛔⛔ MANDATORY, PER CELL, AND IT IS WHY A NULL CAN BE READ AT ALL

**THE PRECEDENT THIS DISCHARGES, named because it is one leg old.** KLADDEDOSE
carried a **per-game minimum-enemy-core-HP-while-adjacent column**, and that
column is the only reason its 0/25 could be read as **never-dosed** on the
redirect path instead of as a flat refutation
(`PREREG-LEG-KLADDEDOSE…:192-196`, and the readout at `13c2f5465`:
*"25/25 of the non-converting arriving games are NEVER IN FINISHING RANGE — the
enemy core's minimum HP while any of our builders stood adjacent was 340 across
all 25 games… 'in range, did not fire' = 0/25"*).

**THE ANALOGUE THIS LEG MUST CARRY: DID THE DOOR VERB FIRE?**

> **DOOR-VERB PECK EVENT (engine-side, per game, per arm):** a builder-attack
> event by one of OUR builder bots whose target tile is occupied by an ENEMY
> entity of a type in `FS_DOOR_TYPES` (GUNNER / SENTINEL / LAUNCHER) standing at
> **d² ≤ 40 of OUR OWN core**. Recorded as: **count of such events**, **count of
> distinct enemy turrets so attacked**, **the round of the first one**, and
> **the enemy turret's HP trajectory** (the builder attack is **2 Ti → 2 damage**,
> which distinguishes it from gunner damage 7 and sentinel damage 18).

**⇒ WITHOUT THIS COLUMN A NULL IS UNREADABLE. WITH IT, A NULL DECOMPOSES:**

| dose column reads | the null means | verdict class |
|---|---|---|
| **0 (or ≤6/25) door pecks in the treatment arm** | the verb was armed and **still never fired** — no body free, no body adjacent, the Ti floor or the budget cap bound first | **NEVER DOSED** — inert, closes nothing about the peck mechanism (LOKI-3 template) |
| **≥19/25 games pecked, turret lifetimes/kill share flat** | the verb fired and **did not work** on this opponent | **REFUTED for this application against this opponent** |
| **≥19/25 games pecked, mechanism moved, delay/kill-share bar cleared** | the plank landed | **BAR MET** |

⛔⛔ **AND THE INERT CLAUSE HAS A LIMIT, WHICH IS THE FRESHEST LESSON THIS REPO
HAS AND IT IS EXACTLY ONE LEG OLD.** KLADDEDOSE's v1 verdict read a pooled zero
as UNRESOLVED and was **AMENDED THIRTEEN MINUTES LATER** (`13c2f5465`, builder's
own words: *"my v1 over-applied the inert clause to the pooled claim, an error in
the plank-flattering direction, corrected"*). The correction's rule, transcribed
here as a registration:

> **A ZERO IS ONLY INERT IF THE ZERO IS SHOWN TO BE ATTRIBUTABLE. The inert
> reading requires that the zero be attributable ENTIRELY to the mechanism never
> having had exposure.** In KLADDEDOSE the additive path had **no HP gate,
> 25/25 arrivals and ~3.9k pooled adjacent rounds of exposure** and produced
> **0 of 1,969 builder attacks into a core** — exposure existed, so the pooled
> claim was **REFUTED**, not unresolved.

**APPLIED TO THIS LEG, PRE-COMMITTED:** a treatment-arm zero on the mechanism
metric may be called **NEVER DOSED only if the dose column ALSO reads at or
below the falsifier band (≤6/25) AND the EXPOSURE column shows the verb had no
opportunity.** ⇒ **THE EXPOSURE COLUMN IS THEREFORE ALSO MANDATORY:**

> **EXPOSURE (engine-side, per game):** the number of rounds in which **at least
> one of our living builder bots is orthogonally adjacent to an enemy entity of
> a type in `FS_DOOR_TYPES` standing at d² ≤ 40 of our core**, plus **our global
> titanium at each such round** (against `FS_DOOR_TI_FLOOR = 6`).

**A treatment arm with high EXPOSURE and a zero DOSE is a finding about the verb
(it was armed, it had the opportunity, it did not act) — not an inert fixture.**
A treatment arm with **zero exposure** is an inert fixture and closes nothing.

**⭐ AND THE ANTI-GOODHART CLAUSE:** **peck counts up with turret lifetime flat is
a NULL, not a hit.** The deciding quantities are **enemy turret DEATHS and the
delay**, never the count of attack events. A body pecking a turret that is being
healed or replanted is producing the number without producing the effect. *(The
CELL F baseline already prices the replant channel: they failed to replant
**14 of 21** times we killed one, median latency 34 rounds — so a kill that is
replanted within ~34 rounds is a partial, and the readout reports replants.)*

---

## §MECHANISM METRICS — PER CELL, ENGINE-SIDE ONLY, EACH WITH ITS PRE-REGISTERED CONTROL VALUE

**All quantities below are read from the platform replay's entity BUILD/DEATH
events, positions and HP deltas. None is read from our own stdout** (stripped:
30,664 of 30,664 `BotOutput` events carry an empty `stdout`).

### CELL F — farming_200s v19 (the door-sentinel bot)

| # | metric | window | **pre-registered control value** | source |
|---|---|---|---|---|
| **F1 (PRIMARY)** | share of games with ≥1 of their forward sentinels (d²≤32 of our core) killed by us **>2 rounds before game end** | r0-r1000 | **25.0% (15/60 games)** | `farming200s…:213-215` |
| F2 | share of their forward sentinels killed | r0-r1000 | **21.9% (21/96)** | `:198-206` |
| F3 | median lifetime of a killed forward sentinel | r0-r1000 | **74 rounds** | `:198-206` |
| F4 | share of their forward sentinels alive at game end | r0-r1000 | **78.1% (75/96)** | `:198-206` |
| F5 | replant failure after we kill one | r0-r1000 | **14 of 21 (66.7%)**, median latency 34 rounds when they do | `:198-206` |
| F6 (COST) | **our builder-rounds spent on the door** and **Ti spent at 2/peck** | r0-r1000 | **0 / 0 by construction** | code fact |
| F7 (CONTEXT) | our core deaths, and whether a plant preceded each | r0-r1000 | **32/32 preceded by a plant, 0/32 without** | `:183-188` |

⛔ **THE >2-ROUNDS-BEFORE-END QUALIFIER IN F1 IS NOT COSMETIC AND IT IS THE
STUDY'S OWN ARTEFACT CONTROL:** of the 21 recorded kills, only **1 of 21** landed
within 2 rounds of game end, so the qualifier removes an end-of-game removal
artefact without removing the signal (`farming200s…:220-226`). **It is registered
in both arms identically.**

⚠ **F6 IS THE PRICE #96's OWN OBJECTION DEMANDS, and it must be charged:** a
40 HP sentinel is **20 pecks = 40 Ti** and 20 builder-rounds. The commission
requires the cost side be charged explicitly; **a kill-share gain bought with a
builder-round budget the assault needed is what the r300 guard exists to catch.**

### CELL A — not adgato v25 (the four-sentinel open-loop rush)

| # | metric | window | **pre-registered control value** | source |
|---|---|---|---|---|
| **A1 (PRIMARY)** | **plant-to-core-death lag**, censored at +150 | first home plant → our core death | **17 / 19 / 20 / 19 / 23 rounds, mean 19.6, 5/5 games** | `notadgato…:76-94, 129-133` |
| A2 | **enemy home sentinels (d²≤41 of our core) ALIVE at r70** | r70 | **4 / 4 / 4 / 4 / 4 (5 of 5)** | `:501-503` |
| A3 | enemy home-sentinel deaths by r70 | r0-r70 | **1 of 5 games** (that one at r59, the round our core died) | `:578` |
| A4 | rounds from first enemy home sentinel to the SECOND one dying | r0-r1000 | **never (5/5)**; Focalground's one winning game read **r31→r63 = 32 rounds and that sufficed** | `:506-507` |
| A5 | round of the first enemy home sentinel | r0-r1000 | **r30 / r40 / r35 / r43 / r43** | `:129-133` |
| A6 | our core HP at r70 | r70 | **dead in 5/5** (r47/59/55/62/66) | `:88-94` |
| A7 (COST) | our builder-rounds on the door, Ti at 2/peck | r0-r1000 | **0 / 0 by construction** | code fact |

⛔⛔ **THE HONEST ARITHMETIC FOR CELL A, CARRIED VERBATIM FROM THE STUDY AND
REGISTERED AS THE EXPECTATION RATHER THAN DISCOVERED AFTERWARDS:** *"Peck
arithmetic is poor on its own: 2 dmg/peck, sentinel 40 HP = 20 pecks per
sentinel, **80 bot-rounds for the nest**, against `FS_DOOR_MAX_RNDS = 40`
(`doctrine.py:2659`). **Expect this to delay, not break, the rush.**"*
(`notadgato…:594-596`).
⇒ **THE REGISTERED EXPECTATION FOR CELL A IS DELAY (A1), NOT NEST DESTRUCTION.**
A2 is registered as a **mechanism metric and a partial-credit column** — the cap
permits **at most 2 of the 4 sentinels** to be finished by a single body's
budget, so **A2 falling from 4 to 2 is the mechanistically PREDICTED shape and
A2 falling to 0 would be a surprise.** *(A surprise is the point, not an anomaly
— write it down before explaining it away.)*
**AND THE CLEAN NEGATIVE IS EQUALLY READABLE:** dose ≥19/25 with **A1's interval
containing 0** and **A2 still reading 4** is *"the verb fired, spent the budget,
and bought nothing"* — a real negative on this application, not an inert
fixture. **That is the bar this cell is shaped to make fail honestly.**

⚠ **A1's CENSORING RULE IS PRE-COMMITTED BECAUSE THE TREATMENT COULD CHANGE THE
DENOMINATOR:** the control expects our core to die in ~5/5, but a treatment that
works may produce games where it does not. **A game whose core survives past
plant+150 scores 150 and is COUNTED in the mean; the count of censored games is
reported beside the mean in the same sentence.** Without this rule, a successful
treatment would silently delete its own best games from the metric.

⚠ **A CONFOUND SPECIFIC TO CELL A, NAMED NOW:** the study measures **`LAUNCHER_MIN_RND = 160`** blocking the home launcher while our core dies at r47-r66 (`notadgato…:426-440`), and **`_try_counterbattery` refusing on the cost gate with our bank at 0-16 Ti** (`:350-373`). **This leg does NOT touch either.** ⇒ the door verb is being armed on a chassis whose other two home answers remain unfunded and unreachable, and **a CELL A null is a null about the door verb ON THAT CHASSIS**, not about home defence in general. The study's own ranking puts the funding plank (CAND-A) ABOVE the flag flip and says *"do NOT fire CAND-C as a standalone arm"* (`:625-627`) — **this leg fires it anyway, deliberately, because CELL F is the cell that justifies the window and CELL A rides along on the same treatment tree at the cost of two more accepts.** ⟨RATIFY⟩ **that trade is the builder's to accept or refuse.**

---

## §108-EXCLUDED — THE COLLAR MIRROR IS NOT IN THIS LEG AND ITS CHANNEL STAYS UNCLAIMED

**`#108` (the collar mirror — extending ring clearance to OUR OWN core's heal
seats) is EXPLICITLY OUT OF SCOPE.** Its verb is unbuilt: `FS_DOOR_TYPES =
frozenset((GUNNER, SENTINEL, LAUNCHER))` (`doctrine.py:2662`, **import-confirmed**)
**does not contain BARRIER**, so **even with `FS_V515_DOOR_OFF = False` the door
verb walks past all 314 of their collar barriers**; `FS_CLEAR_RING_ON` is scoped
to THEIR ring (`siege.py:4165`); `_sabotage_prio` is dead under `LOKI_QUIET_ON`
(`main.py:1858`). **There is no path in the shipped tree that removes an enemy
building from our own heal seats** (`farming200s…:968-978`).

**THREE CONSEQUENCES, ALL REGISTERED:**
1. **NOTHING IN THIS LEG'S RESULT — IN EITHER DIRECTION — MAY BE READ AS
   EVIDENCE ABOUT THE COLLAR CHANNEL.** That channel is **UNCLAIMED and stays
   OPEN.** A null here does not close it; a hit here does not confirm it.
2. **THE COLLAR IS AN UNTREATED ACCELERANT SITTING INSIDE BOTH ARMS OF CELL F,
   AND IT IS 100% DETERMINISTIC:** **625 of 625 of their barriers across 130
   games sit at d²≤5 of OUR core — the orthogonal-8 heal ring — and 0 of 625
   near their own** (v19: 314 of 314, median round **r81**, i.e. **after** the
   r32.5 plant) (`farming200s…:853-864, 960-966`). The study's INFERENCE is that
   the collar is what converts a planted sentinel into a dead core (median lag
   146 rounds rather than the ~84 a continuously-firing sentinel needs). ⇒ **the
   door verb is being measured in the presence of a second, untreated half of the
   same kill channel, identically in both arms.** Balanced, therefore not a
   confound on the CONTRAST — **but a hard ceiling on the delay this leg can
   observe, and the readout says so.**
3. **The instrument that established the collar is not an interpretation imposed
   on their data:** the complement-group control reads **370 of 392 (94.4%) of
   OUR OWN barriers at d²≤5 of THEIR core, median r30** — the same signature from
   the same counter, so the column discriminates (`farming200s…:871-876`).

---

## §KILL-ROUND NON-REGRESSION — THE DEFENCE ADMISSION BAR, IN ITS CURRENT FORM

**KILL-ROUND NON-REGRESSION: ITT RMST₃₀₀ — the mean kill round censored at the r300 horizon over ALL games of the arm (a game with no enemy-core kill scores 300) — computed per arm on this leg's own games, treatment minus control, per cell.** **ADMISSIBLE only if the 95% interval on that difference EXCLUDES a rise greater than the registered MDE of ⟨RATIFY⟩ **+15 rounds**.** **This is scored as an EXCLUSION and is stated that way deliberately**: `CLAUDE.md`'s direction clause says a design effect makes an exclusion claim HARDER and a fail-to-exclude claim EASIER, so **a fail-to-exclude phrasing is barred from this readout — the sentence that may be written is "the interval EXCLUDES a rise beyond +15 rounds", and nothing weaker.**
**REPORTED DIAGNOSTICS, never primary:** the **ITT timely-kill rate** (share of ALL games ending in a core-kill by r300 — CELL F control value pre-registered at **31.7% over 60 games**, `farming200s…:235`), the rate factor, and the speed factor. **The kill-win-CONDITIONED share carries a collider and is a DIAGNOSTIC ONLY.**
**AUTHORITY, opened by this drafter rather than quoted from memory — `PROGRAMME.md:621-651`:** *"THE OPERATIONAL ESTIMATOR IS ITT RMST₃₀₀: mean kill time censored at the r300 horizon over ALL games (a non-kill scores 300; the ITT median pins at the horizon, so the MEAN is the estimator). THE BAR: a plank's RMST₃₀₀ must not RISE vs control by more than its prereg's registered MDE, scored as exclusion."* Vintage rule from the same passage: RMST₃₀₀ governs preregs locked from that ruling onward, so it governs this one. **Drift inside r200-300 is REPORTED, not disqualifying.**
⚠ **RESOLUTION STATEMENT FOR THIS GUARD (OB12 applied to the guard, because a guard is a bar):** at 25 games per arm and an RMST₃₀₀ sd of ~100 rounds, the half-width on the difference is `1.96 × 100 × sqrt(1.434 × 2/25)` ≈ **66 rounds**. ⛔ **THIS LEG THEREFORE CANNOT EXCLUDE A +15-ROUND RISE, AND SAYING SO IN ADVANCE IS THE POINT.** ⟨RATIFY⟩ **the builder chooses:** (a) register the guard as **REPORTED-ONLY on this leg**, with the plank's admission deferred to the screen that follows — the honest reading, and OB16's own rule that *"an unresolvable bar is a reason to state what IS resolved, not a licence to spend games until it resolves"*; or (b) buy the n. **The drafter recommends (a) and recommends that the readout state the observed RMST₃₀₀ difference with its ±66-round interval in the same sentence, so no later reader can quote it as a clearance.**

---

## §FIRE PLAN — ⛔⛔ THE EXPOSURE MATH CHANGED AND THIS SECTION IS THE REASON THE LEG IS SHAPED THIS WAY

**THE FACT THAT DRIVES EVERYTHING BELOW: `fcode submit` IS ACTIVATION.** There is
no upload-now-activate-later. The **treatment** arm plays only if the treatment
tree is the ACTIVE submission, which displaces the v177 holder for the duration.
**The CONTROL arm is v177 bytes = the current holder, so it carries ZERO
exposure and can be fired in any window with no submit at all.**

**⛔ THE CADENCE HAS HALVED AGAIN (relayed, and it is load-bearing):** the
commission records rated pairings landing **every ~10 MINUTES at `:x1:10`**
(three-slot confirmed 12:01 / 12:11 / 12:21, with a maiden slot at `:31`).
**Clear air between pairings is therefore ~9 minutes maximum — half the budget
the old 20-minute cadence gave.**

⛔⛔ **WHAT THE RUNNER MUST RE-DERIVE IN THE FIRING SHELL, NOT COPY FROM THIS
PAGE: BOTH THE OFFSET AND THE INTERVAL, FROM FRESH `createdAt` ROWS**
(`fcode match list --mine --type ladder`). `CLAUDE.md` is explicit that **the
offset has shifted at least once inside an 18-hour span**, that the **20-minute
interval was never the robust part** (some gaps are 600 s), and that the sample
is us-only. **The `:x1:10` figure above is a RELAYED observation from ~12:2xZ
and this leg may fire hours later. Hardcoding it is the failure mode.**

### The block plan — 4 blocks, 2 exposure events, rotated starting cell

| block | submit? | cells filled | accepts |
|---|---|---|---|
| 1 | **NO** — v177 holder is the control arm | F-C ×3, A-C ×2 | 5 |
| 2 | **YES** — treatment active, rolled back at end of block | F-T ×3, A-T ×2 | 5 |
| 3 | **NO** | F-C ×2, A-C ×3 | 5 |
| 4 | **YES** — treatment active, rolled back at end of block | F-T ×2, A-T ×3 | 5 |

**Totals: F-C 5, F-T 5, A-C 5, A-T 5 = 20 accepts = 100 games.**
**Blocks are one rate-limit window apart** (5 test/unrated matches per 20
minutes, **shared across `match unrated` AND `match test`, and rejected attempts
appear to count**). **Wall clock ≈ 80 minutes; exposure ≈ two short intervals
inside it.**
⭐ **THE STARTING CELL ROTATES BETWEEN BLOCKS (3/2 then 2/3) ON PURPOSE:** under a
window a runner cannot outwait, a dropped challenge is **systematic and always
lands on the same tail cell**, which starves exactly the cell it was meant to
measure. `fanout.sh`'s `fire()` gives up after three retries and logs
`fired 3/5`; **that behaviour is barred here** — the runner **waits out the
window and retries the SAME cell.**

### Each exposure block, step by step

1. **OBSERVE a pairing.** Read fresh `createdAt` rows; wait until a rated pairing
   is **observed to have been created**, then start. **Do not fire on a predicted
   slot.**
2. **`date -u` → record. SUBMIT the treatment tree** via `tools/submit_clean.py`
   (**never a bare `fcode submit`**, which ships docs to the platform and is a
   drift flag). **`--activate` IS the ship decision and this leg's submit carries
   it only because the leg cannot otherwise run.**
3. **Confirm activation on the LOAD-BEARING FIELD:** `fcode status` must print an
   `Active bot:` line naming the treatment version. **Gate on the presence of
   that line, never on `$?`** — this CLI exits 0 while printing `Error: True`,
   and `fcode match list` exits 1 in the same outage.
4. **Create all 5 challenges for this block, pinned**, and **confirm all 5 have
   COMPLETED** (~15 s each) before step 5.
5. **ROLL BACK**, `tools/submit_clean.py` restoring the v177 holder and
   **confirming the restore against the `Active bot:` line, never the exit code.**
   **`date -u` → record.**
6. **TARGET EXPOSURE ≤ 4 minutes of a ~9-minute gap.** ⟨RATIFY⟩

### Leak accounting — and the instrument that CANNOT answer this question

⛔ **THE MATCH COUNTER IS BLIND TO EXACTLY THIS FAILURE MODE.** A ladder match
created at T completes minutes later and carries the then-active version into the
rated record; `724→724` proves no match **COMPLETED**, and says nothing about
whether one was **PAIRED**. **`elo_history.tsv` cannot answer it either — it tags
rows by the version active at POLL time.**
⇒ **THE VERIFICATION IS PER-MATCH `teamAVersion` AT THE PAIRING BOUNDARY**, read
off `fcode match list --mine --type ladder`: **the last pairing before each
submit and the next pairing after each rollback are recorded verbatim in the
readout, for both exposure blocks.**
**BUDGET: ~−8 Elo per leaked rated match** (`CLAUDE.md`'s measured −24.67 across
3 matches). **REGISTERED STOP ⟨RATIFY⟩: if a rated pairing is created inside an
exposure interval, the remaining treatment block is ABORTED, the leg is read
short under §CUT-SHORT, and the leaked match is reported with its version tag.**
**Two leaked matches ⇒ the leg stops entirely.**

### Fire gates, checked in the same shell, in this order

1. ⛔ **HOLDER READ:** `fcode status` prints `Active bot: v177` before block 1 and
   **after every rollback.** Gate on the `Active bot:` line, never `$?`.
2. **PIN ON EVERY ACCEPT**, both cells, no exceptions.
   ⛔ **OBLIGATION 17 — AND THE CLAUSE THAT CAN STILL SURPRISE IS #3, SO RUN IT
   FIRST.** (1) **NAME THE EXECUTING TOOL** — ⟨RATIFY⟩ the builder names the
   script that will fire this leg. (2) **CONFIRM THE RUNNER EMITS `--match`** —
   not that the CLI supports it. **`--help` proving a flag exists is not proof
   that the script passes it**, and the CAL418 incident is exactly this:
   `tools/unrated_run.sh:~318` fired a **bare** `fcode match unrated "$id" --json`
   with **no pin path**, found by the builder at fire time, after the lock and
   after a two-clock certificate. *(KLADDEDOSE discharged this against installed
   fcode 2.3.6 — `commands/match_group.py:57` declares `--match → source_match`,
   `commands/test.py:13` sets `body["sourceMatchId"]`, and the CLI echoes
   `Opponent version from match: <id>` on accept. **That discharges the CLI, not
   whatever runner fires THIS leg.**) (3) **STATE THE CONSEQUENCE OF SILENT
   NON-EXECUTION** — here it is the quiet case: the leg would play the
   opponents' CURRENT submissions while this page says v19 and v25, with nothing
   in the output to show it, and CELL A's opponent is **known to ship every few
   hours.**
3. **RATE LIMIT:** 5 per 20 min, shared, rejects count. **Wait out the window and
   retry the same cell.** Rotate the starting cell.
4. **TARGET-VALUE BAND: RE-RUN `tools/target_value.py` IN THE LOCK SHELL.**
5. **BOTH TREES' FLAG VALUES CONFIRMED BY IMPORT** (§TREATMENT TREE), both lines
   pasted into the lock.

**TARGET BAND:** ⛔ **ALL FIGURES BELOW ARE CACHED AND KNOWN-STALE — RE-RUN IN THE LOCK SHELL, DO NOT QUOTE THIS LINE.** farming_200s **gap +19** (them 1845 / us 1826), **win pays +16.88**, **reachable YES** *(relayed, 12:50Z boot band)*; not adgato **gap +92**, **win pays +20.16**, **reachable YES** *(study-time read, `notadgato…:462-467`)*. **Both are re-verified at lock; a low-value target can still be the right leg, but the number is written down BEFORE the work rather than defended after.**
⚠ **THREE DISAGREEMENTS AMONG THE NAMED INPUTS, REPORTED RATHER THAN AVERAGED:**
* **farming_200s's gap:** the study writes ***"gap +0 (them 1845 / us 1826)"*** (`farming200s…:625-627`) — **the parenthetical and the gap contradict each other; 1845 − 1826 = +19**, which is the commission's figure. **The commission's +19 is arithmetically consistent and the study's "+0" is registered here as a typo in a named input.**
* **not adgato's rating:** the commission relays **1864 (CACHED)** while the study's own `target_value.py` run reads **theirs 1913 / gap +92** (`notadgato…:462-467`) — **a 49-point disagreement between two inputs written within an hour of each other.** The commission also warns they are **mid-sweep** (five 5-0s since 10:52Z), so **their live rating will have moved again.** ⇒ **CELL A's payoff figure is the least trustworthy number on this page.**
* **our own rating** reads **1826**, **1821** and **1830** across the three inputs. **All are stale; the lock shell is the authority.**

---

## §PIN INSTRUMENT-ALARM CLAUSE — MANDATORY IN THE LOCK, PER CELL

⛔⛔ **CELL F: ANY DECODED `oppver` OTHER THAN 19 IS AN INSTRUMENT ALARM. CELL A:
ANY DECODED `oppver` OTHER THAN 25 IS AN INSTRUMENT ALARM.** Either the pin did
not take, or the decode is wrong. **REPORT IT AND DO NOT READ THAT CELL.**

* The alarm is **per accepted match**, checked at readout against the **decoded**
  opponent version — **not** assumed from the fact that `--match` was typed. *(A
  server-side drop would be invisible to the CLI's own `Opponent version from
  match:` echo.)*
* **A pinned set whose decoded `oppver` values DIFFER among themselves is the
  same alarm**, and it invalidates the **pooling** of those matches, not merely
  one of them.
* ⛔ **A NULL `oppver` IS NOT A PASS.** The field read the literal string `'None'`
  in **4,375 of 4,375 rows** until it was backfilled on 2026-08-13, and **a null
  reads as "no version change" to any cut that trusts it.** An unreadable
  `oppver` is an alarm, not a clean cell.
* ⭐ **AND THE CONTROL-ARM PIN IS EQUALLY LOAD-BEARING.** The pin is what makes
  the control and treatment halves of a cell the SAME experiment. **A cell whose
  control half met v19/v25 and whose treatment half met something else is a
  BROKEN PAIR** (§CUT-SHORT) and no difference may be computed from it.

---

## §FIXTURE-AXIS IMBALANCE — REPORTED UNDER ONE HEADING, PER THE LOKI-19 COMPANION RULE

**The arms are NOT assigned by an explicit balancing rule** — they are assigned
by BLOCK, and blocks are separated in wall-clock time by the exposure schedule.
**Seat and map are drawn by the platform.** Per the obligations doc's Addendum 8
companion (*"two imbalances are one defect and must be named once"*), this
prereg pre-commits to reporting, **under a single heading and for every cell**:

* **SEAT MIX** (a / b) per arm;
* **MAP MIX** per arm;
* **DECODED `oppver` MIX** per arm;
* **WALL-CLOCK SEPARATION** between the control and treatment halves of each cell.

**DISCLOSING RATHER THAN CORRECTING IS THE REGISTERED CHOICE.** A matched
estimator chosen after the data is the fault this clause exists to catch; **no
post-hoc balancing, matching or reweighting is registered, and none may be
introduced at readout.**

---

## PREDICTIONS

**P1 (PRIMARY, CELL F).** With the door verb armed, the share of games in which we
kill ≥1 of their forward sentinels **>2 rounds before game end** rises against
the in-leg control arm by more than the registered half-width. **Mechanism:
their sentinel is 40 HP; a builder peck is 2 damage for 2 Ti; the object sits at
median d²=13 from our core for a median 74 rounds** and we currently have **no
live verb that touches it.**

**P2 (PRIMARY, CELL A).** With the door verb armed, the **plant-to-core-death lag**
rises by more than +5 rounds plus the half-width. **The registered expectation is
DELAY, not destruction** — `FS_DOOR_MAX_RNDS = 40` against 80 bot-rounds for the
four-sentinel nest permits at most half the nest to be finished on one body's
budget.

**P3 (DOSE, GATE 1, BOTH CELLS).** The door verb fires in ≥19 of 25 treatment
games per cell, and in **0 of 25** control games. *(The control zero is a
positive control on the instrument, not a prediction: a non-zero there means the
decoder is misattributing some other builder attack and the leg's numerator is
wrong.)*

**P4 (DESCRIPTIVE ONLY).** Game share per cell, **printed with its ±23.5pp
half-width in the same sentence. No bar, no pass/fail, and it may not appear in
any verdict sentence.**

---

## FALSIFIER

**⛔ THE FALSIFIER, STATED SO IT CAN FIRE — AND IT HAS TWO INDEPENDENT LIMBS,
BECAUSE THE TWO CELLS ASK DIFFERENT QUESTIONS OF THE SAME FLAG.**

**LIMB 1 (DOSE).** **If the door verb fires in ≤6 of 25 treatment games in a cell
— Wilson-95 upper bound below 50% — the flag flip is NOT DELIVERED in that cell.**
Per the §DOSE COLUMN rule this reads **NEVER DOSED** *only if* the EXPOSURE
column shows the verb had no opportunity; **if exposure was present and the verb
still did not act, that is a finding about the verb and is reported as such, not
as an inert fixture.**

**LIMB 2 (MECHANISM).** **With the dose delivered (≥19/25), if CELL F's
kill-share difference has a 95% interval containing 0 AND CELL A's delay
difference has a 95% interval containing 0, the `#96` flag flip is REFUTED AS AN
ANSWER TO THIS TURRET CLASS against these two opponents.**

**What that refutation means, and what it does not:**
* **It closes the claim that the door verb is the missing counter to a forward
  turret plant** — for THIS application, against THESE two pinned opponents, on
  THIS chassis. **The readout says all three qualifiers in the same sentence.**
* **It does NOT re-confirm the off-switch.** The off-switch's +36pp/+18.9pp
  remain measurements against **our own bots**; a null here leaves that road
  where `CLAUDE.md` point 6 puts it — a hypothesis with a live-game null beside
  it, not a closure.
* **It does NOT close the collar channel (`#108`), which is untreated in both
  arms and stays unclaimed** (§108-EXCLUDED).
* **It does NOT close home defence for CELL A**, whose other two answers
  (`LAUNCHER_MIN_RND = 160`, the unfunded counterbattery) are untouched here.
* **A road closes only on live-game evidence and this leg IS live games** — but
  100 of them against two pinned opponents. **Scope travels with the verdict.**

**⭐ AND THE ANTI-GOODHART CLAUSE, RESTATED AT THE FALSIFIER SO IT CANNOT BE
DETACHED:** **dose up with turret lifetime and delay flat is a NULL, not a hit.**
**And the cost side is part of the verdict, not a footnote:** a mechanism gain
bought with builder-rounds and Ti that the assault needed is what the r300 guard
exists to catch, and **F6/A7 are reported in every verdict sentence that claims
a hit.**

---

## §WHAT THIS LEG DOES NOT REGISTER (enumerated, so nothing is smuggled in later)

| not registered | why |
|---|---|
| any game-share verdict | 25 games/cell, ±23.5pp — cannot resolve; this is a mechanism/dose leg |
| any Elo or ladder claim | unrated surface; the only rated quantity is the LEAK, reported as a cost |
| a cross-cell pooled bar | pooling the two opponents revives the OPPONENT cluster (DEFF 1.833) and mixes two different mechanism metrics |
| pooling with any unpinned games of either opponent | high-churn cells: **reportable, not poolable** |
| a per-map or per-segment read | `MAP SEGMENT: none expected`; 1-2 games per map cell |
| any claim about the collar channel `#108` | untreated in both arms; **unclaimed and open** |
| any claim about `LAUNCHER_MIN_RND` or counterbattery funding | untouched by this diff; CELL A's null is scoped to the door verb on this chassis |
| a re-confirmation or refutation of the off-switch's +36pp/+18.9pp | those were measured on our own bots on a different chassis pair; this leg does not reproduce that fixture |
| an admission decision on the r300 guard | the guard is **REPORTED-ONLY at this n** (⟨RATIFY⟩), because it cannot exclude +15 rounds here |
| a `titanium_collected` or tiebreak claim | `R1000_IS_DEFEAT`; economy is instrumental and does not score |

---

## §GAPS — WHAT THE OBLIGATIONS DOC REQUIRES THAT THIS DRAFT COULD NOT SATISFY FROM ITS NAMED INPUTS

**Named, not guessed. Each is a builder action before the lock.**

1. **OB13's COMPUTED HALF — `TREATMENT DIFF REFS` cannot be supplied.** The
   treatment tree does not exist yet, so there is no diff for
   `prereg_check.py` to intersect. **The builder creates the arm, records both
   digests, and re-runs the checker against a real diff.**
2. **OB14 for CELL F — the 24 h distinct-version count is not in any named
   input.** Free off `corpus/league_matches.tsv`; the drafter had no corpus read
   in scope. **CELL A's count IS in an input (3 versions in ~6 h ⇒ HIGH).**
3. **farming_200s's TEAM ID is in no named input** (only the pin's match id).
   Without it `fcode match unrated <team_id> --match <id>` cannot be typed.
4. **A LINE-NUMBER CONTRADICTION BETWEEN TWO NAMED INPUTS, ON THE VERY LINE THIS
   LEG FLIPS.** The farming study places the off-switch early return at
   **`main.py:1734-1735`** (`:399-405`) and the adgato study places it at
   **`main.py:1729-1730`** (`:392-394`) — **both read the same tree at the same
   git rev `07735243f`.** One of them is wrong. **The builder opens the file and
   records the true line before the lock**; a prereg that cites the wrong line
   for its own treatment is an OB13 defect wearing a typo's clothes.
5. **NO LOCAL BOTH-WAYS DOSE PROBE EXISTS IN THE NAMED INPUTS** for
   `_door_turret_turn` (§DOSE). Builder picks path (i) or (ii) explicitly.
6. **THE `~10-MINUTE / `:x1:10`` CADENCE IS RELAYED, NOT MEASURED BY THIS
   DRAFTER**, and `CLAUDE.md` says the offset has already shifted once and the
   interval is the non-robust part. **Re-derive both in the firing shell.**
7. **THE RATINGS AND TARGET BANDS ARE ALL CACHED AND MUTUALLY INCONSISTENT**
   (§TARGET BAND). **Re-run `tools/target_value.py` at lock.**
8. **THE EXECUTING RUNNER IS NOT NAMED** (OB17 clause 1) and **its `--match`
   emission is NOT verified** (clause 2) — the drafter cannot verify a script it
   was not told will run. **`tools/unrated_run.sh` is the known offender from
   CAL418 and must be greped, not assumed.**
9. **A GENUINE COORDINATION FLAG FROM A NAMED INPUT, carried because it changes
   who owns the subsystem:** the farming study records `bots/_probe_peck_a/` and
   `_probe_peck_b/` created **during that study** and `bots/_probe_doorlaunch/`
   documenting a *"door-launcher kill … TARGETING rule"* keyed to
   `V530_DOOR_DSQ` (`farming200s…:1077-1088`). **Another lane is in this exact
   subsystem. The builder reconciles before locking** — a treatment tree built
   on top of an unreconciled probe change is not the one-boolean arm this page
   registers.

---

## §CHECKER RUN — WHAT `prereg_check.py` SAID ABOUT THIS DRAFT, INCLUDING WHAT IT COULD NOT SAY

`.venv/bin/python tools/prereg_check.py docs/prereg/DRAFT-PREREG-LEG-DOORWAVE-2026-08-21.md`
run against this uncommitted file by the drafter → **last line `PREREG_CHECK: OK`**
(26 presence rules: 24 ok, 2 n/a; every recomputed quantity closes:
`BOUNDARY_UNITS` 20 accepts = 100 games, `BOUNDARY_VS_N` ok, `CUT_SHORT_FLOOR`
60 ≤ 100, `POOL_ERA_SPAN_OK` ok against boundaries re-derived live at
**2026-08-13T07:12:59Z** and **2026-08-21T07:52:59Z** over 7,370 rated rows).
⛔ **A GREEN RUN IS A FLOOR, NOT A BLESSING — the tool's own words — and FIVE
things it printed are NOT clearances:**

1. ⛔ **`OB13_INTERSECTION: CANNOT-COMPUTE`** — *"no diff exists to intersect
   (arm not yet built). This is NOT 'checked and clean' — the check did not
   run."* **The builder MUST re-run with `--fire` once the treatment tree
   exists**, where the same condition FAILs. This is §GAPS item 1 and it is the
   one machine-checkable obligation this draft genuinely cannot discharge.
2. ⛔ **`DOSE_BOTH_VERDICTS: ok treatment 1.0 vs control 0.0`** — **the tool
   parsed `≥1 door-verb peck event` as the treatment value, not the registered
   gate of `≥19 of 25`.** The check passed on the wrong number. **The registered
   dose gate is ≥19/25 vs 0/25 (§GATE RESOLUTION), and the checker has NOT
   validated it.** Recorded here so no reader mistakes the green line for
   arithmetic that was actually done.
3. **`BAR_RESOLVABLE: not computed`** — this page's BAR is prose across three
   differently-shaped bars, so the tool's single-bar resolvability arithmetic did
   not run. **The resolution statements are hand-computed in §BAR and
   §GATE RESOLUTION and the builder re-derives them.**
4. **Four `OBLIGATION 17, PARTIAL WINDOW` warnings** on `FS_DOOR_DSQ=40`,
   `FS_DOOR_TI_FLOOR=6`, `FS_DOOR_MAX_RNDS=40` and `LAUNCHER_MIN_RND=160`. **All
   four are the checker reading a non-round constant as a round gate** — a
   squared distance, a titanium floor, a bot-round budget, and a gate on a
   different verb (§GATING CONSTANTS). **The warnings are correctly emitted and
   incorrectly interpreted if taken at face value; the builder confirms this
   reading by opening the four anchors rather than accepting this paragraph.**
5. **`ESTIMATOR` and `PRE-STATE` passed as `ok~` (attested)** — presence-only.
   Their content is judgement and was not checked.

---

## AUTHORITY

**Drafted by:** a fresh agent with **no inherited session context**, from the
five `PROVENANCE` inputs.
**Ratification, the lock commit, the fire and the verdict sentence:** the
**BUILDER** lane.
**Verdicts here:** none. **This is a DRAFT and it is not locked.**

---

## §RATIFICATION — BUILDER s53, all remaining ⟨RATIFY⟩ items, each verified in this lane's own shell

1. **PLANK CLASS defensive: KEPT.** The DEFENCE_ADMISSION_BAR binds; the kill-round non-regression guard below is registered exactly as the draft frames it.
2. **farming_200s team id: `25288fdb-f3a0-4ae2-964f-2fd7eb9b11d7`** — read via `fcode team search --json` at lock (rating 1834.8 live). not-adgato confirmed `fb0e7053-f8f3-4cc8-a38f-1856a518c7d2` (1939.1 live). Both match research's fills; independently read.
3. **CELL F churn: LOW, RE-COUNTED at lock off `corpus/league_matches.tsv` — exactly ONE farming_200s version in the preceding 24 h (v19, 77 matches).** Poolable per OB14; the pin still mandatory (v18→v19 was a turret-doctrine flip).
4. **Round-gate grep on the verb's call path: RUN at lock** — `main.py:1625→1708-1771` carries FS_DOOR_TYPES/FS_DOOR_DSQ/FS_DOOR_TI_FLOOR/FS_DOOR_MAX_RNDS only (the :1754 budget is bot-rounds, as the draft says); no round-number gate found. `LOKI2_RUSH_RND` (:1355) is a different verb's gate, untouched.
5. **GATE 1 thresholds RECOMPUTED, not copied: falsifier ≤6/25, bar ≥19/25** (Wilson/DEFF 1.434 at n=25) — matches the KLADDEDOSE arithmetic digit-for-digit, third independent reproduction today.
6. **CELL A MDE +5 rounds: ACCEPTED** (the study's own registered displacement expectation).
7. **The 55-vs-56 discrepancy: per-game table is the authority, ACCEPTED as drafted.**
8. **POOL ERA: re-derived by the checker in this lock run** (see PREREG_CHECK output committed with this lock; boundaries as the KLADDEDOSE lock derived them).
9. **FIXTURE-CHECK tolerances: ACCEPTED as drafted** (CELL F-C forward-sentinel kill share ±20pp of 25.0%; CELL A-C sentinels-alive-4 + core-dead-by-r70 in the large majority).
10. **MAP SEGMENT none: ACCEPTED** (core-relative trigger; terrain sits upstream in the denominator).
11. **THE CELL A RIDER TRADE: ACCEPTED.** CELL F justifies the activation window; CELL A rides the same treatment tree for two more accepts. The null-scope caveat is registered verbatim: a CELL A null is about the door verb ON THIS CHASSIS (home launcher still round-gated, counterbattery still unfunded — untouched by this diff), and the study's do-not-fire-standalone ranking is honoured by the rider structure.
12. **KILL-ROUND NON-REGRESSION guard: OPTION (a) — REPORTED-ONLY on this leg.** At 25 games/arm the ±66-round resolution cannot exclude a +15-round rise; the readout MUST print the observed RMST₃₀₀ difference with its ±66 interval in the same sentence, and no clearance sentence may be written. The plank's admission decision waits for the screen that follows.
13. **FIRE PLAN: exposure ≤4 min of a gap ACCEPTED; the registered stop ACCEPTED** (a rated pairing created inside the treatment window = STOP, rollback first, that window's cells discarded for the exposure ledger). **EXECUTING TOOL NAMED (OB17): the manual fcode CLI compound in ONE shell, the KLADDEDOSE pattern** — same-shell `fcode status` Active-bot gate before each phase; `--match` pin path source-verified this session (`match_group.py:57 → source_match → body["sourceMatchId"] → /api/matches/unrated`, non-silent echo); `tools/submit_clean.py` for the activation and the rollback (holder-restore confirmed against the Active-bot line). The fire transcript is pasted into the fire note.
14. **SEQUENCING (this lane's own constraint): the treatment-arm activation window is a SEPARATE, EXPLICIT window call** — control cells may fire on lock (v177 is already active; zero exposure); treatment cells fire only after a fresh createdAt-derived gap read under the ~10-min grid, announced in the tail before the submit. Nothing in this lock authorizes the window; the window call does.
