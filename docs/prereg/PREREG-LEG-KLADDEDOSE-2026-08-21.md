# PREREG — `KLADDEDOSE`: does v177's v541 finisher-peck convert RAIDER ARRIVALS into ENEMY-CORE DAMAGE? — an unrated MECHANISM/DOSE leg vs **kladde chatte tville, PINNED to their v173**

## ⭐ RATIFIED — LOCKED BY THE BUILDER (s53, 2026-08-21)

**Drafted as `DRAFT-PREREG-LEG-KLADDEDOSE-2026-08-21.md` by a fresh opus agent;
every ⟨RATIFY⟩ line below now carries the builder's decision inline, marked
`RATIFIED:`. The lock commit for this file is clock 1; the first accepted
match's platform `createdAt` is clock 2. All (relayed) facts re-verified in the
builder's own shell at lock — each carries its re-verification inline.**

* **Drafted by a FRESH agent with NO inherited session context** beyond the four
  files named under `PROVENANCE`. This agent read no result tape, no
  `coordination.md`, no `HANDOVER.md`, and no prior s53 session message. Where a
  number below came from the commission rather than from this agent's own shell,
  it is labelled **(relayed)** and the builder must re-verify it at lock.
* **THE BUILDER RATIFIES + LOCKS.** Every line marked **⟨RATIFY⟩** is the
  builder's to set, change or delete. The drafter proposes; the lane decides.
* **What this agent wrote:** this file, and nothing else. No commit, no bot
  edit, no worklist row, no `BARS.tsv` row, no challenge, no match.
* **What this agent DID run, disclosed rather than left to the boilerplate:**
  read-only shell commands against the repo — `git log`/`git diff --name-only`
  on `28fde0637`, `grep`/`sed` over `bots/_v542wave/*.py` and
  `docs/research/BUILD-REPORT-v542wave-2026-08-21.md`, `date -u`, and
  `tools/prereg_check.py`'s own `pool_eras()` against `corpus/ladder_games.tsv`.
  **No platform command, no `fcode` invocation, no game.**

**PROVENANCE:** `scratchpad/s53_kladde_prereg_commission.md` ·
`docs/research/PREREG-amendments-and-lock-obligations-2026-08-09.md` ·
`docs/prereg/PREREG-NEWPOOL-BASELINE-2026-08-21.md` ·
`docs/research/PREREG-LEG-EVICT58-2026-08-14.md`

**STATUS: DRAFT, written BEFORE the lock commit exists, BEFORE any challenge is
issued, and BEFORE any game of this leg is created.** Drafting-shell wall clock
**`2026-08-21T13:05:25Z`** (`date -u`, same shell call); repo HEAD at draft
**`07735243f`** (2026-08-21 15:02:07 +0200).
**LOCK, clock 2 (platform leg):** per the obligations doc's 16:3x addendum, the
second clock is the **first accepted match's platform `createdAt`**, quoted
verbatim beside the lock commit's git author time. *(The `# FIXTURE … start=`
local-shard form of the 2026-08-17T07:24:55Z addendum does NOT apply — this leg
produces no shard tape.)*

---

## ⛔ WHAT THIS LEG CAN AND CANNOT BUY — STATED FIRST, BECAUSE IT SETS THE DESIGN

**THIS IS A MECHANISM/DOSE LEG. IT IS NOT A CURRENCY READ AND NO SENTENCE IN ITS
READOUT MAY BE DENOMINATED IN GAME SHARE.** A 25-game window at the unrated
within-opponent DEFF of 1.434 carries a ±23.5pp half-width on any share — so a
game-share number from this leg cannot separate a rout from a rout in the other
direction. `CLAUDE.md` states the rule directly: *a 25-game window is a DOSE AND
MECHANISM probe; a currency read requires pooling windows.*

⇒ **What this leg buys is COUNTABLE, not estimated:** against a real opponent
that our line is currently losing to, **does the v541 peck path put damage on
the enemy core in games where our raider is standing next to it** — the exact
event the misdirected-verb history says has never happened.

⚠ **THERE IS NO MATCHED CONTROL ARM AND THE DRAFT NAMES THAT RATHER THAN
DISGUISING IT.** The comparator is a HISTORICAL base rate measured on **v174**
(`bots/_v537socket`), a different build against the same opponent, partly on the
previous rated map pool (§POOL ERA). A flag-off arm would need a second
activation and a second window. ⇒ **No comparative currency claim is available
and none is registered.**

⚠ **MECHANISM METRIC READS ENGINE-SIDE FACTS ONLY.** Per `CLAUDE.md`'s LOKI-14
rule — **platform-downloaded replays carry `BotOutput` events with an EMPTY
`stdout` field, 30,664 of 30,664** — every quantity below is read from
positions, entity events and HP deltas on the wire. **`FS_V541_LOG` ships
`False` (`bots/_v542wave/doctrine.py:5971`) and would be unreadable even if it
did not: a leg that plans to read its own tag out of a live replay is planning
on an instrument that does not exist.**

---

## REGISTRATION BLOCK

**STATUS:** DRAFT — written BEFORE the lock commit, BEFORE the first challenge, BEFORE any game exists.

**PLANK CLASS:** offensive — a kill-channel finisher clause.

*(Consequence: `PROGRAMME.md`'s `DEFENCE_ADMISSION_BAR` — `r300_crossing_non_regression` — does not bind on this leg. The plank buys nothing on the survival channel: it changes the TARGET of an action a raider was already taking, and adds an action only on a round the parent spent idle. **RATIFIED: class stands as OFFENSIVE, no reclassification — the r300 read does not bind.**)*

**SURFACE:** unrated (platform challenge, `fcode match unrated`).

**CLUSTER UNIT:** match — ONE pinned opponent, so the OPPONENT cluster cannot hold more than one member and dies; the MATCH cluster survives (5 games share an opponent version and one 20-minute ladder slice). ⇒ **DEFF 1.434** (unrated, within-opponent).

**PINNED:** YES — `fcode match unrated <kladde_team_id> --match 6ce2d9b6-cdba-462e-89bc-ee169a62b0f8`, their **v173**, every accept, no exceptions. **PIN TREATMENT LEGS, NEVER PIN CALIBRATION PANELS** — this is a treatment leg.

**CELLS:** one — `kladde chatte tville`, pinned to v173.

**CELL VERSION CHURN:** **HIGH — RE-COUNTED BY THE BUILDER AT LOCK, own shell, off `corpus/league_matches.tsv`: FIFTEEN distinct kladde versions in the preceding 24 h** (136, 141-147, 150, 166-168, 171-173), **INTERLEAVED** (v136 appears at 20T21:12 inside v142's 19:52-22:52 span; v150 at 21T04:32 inside v147's 00:12-05:52 run — consistent with concurrent A/B shipping). v173 current since 11:32Z, 7 rated appearances through 12:41Z. Per OBLIGATION 14 a high-churn cell is **REPORTABLE BUT NOT POOLABLE**; the PIN is what makes this cell readable at all, and the leg's result may not be pooled with any unpinned kladde games. **RATIFIED: churn=HIGH stands on the builder's own count; the per-accept oppver alarm clause is load-bearing.**

**TREATMENT TREE:** `bots/_v542wave` = the LIVE submission **v177 "Baltsars banditer v2"** (`corpus/version_trees.tsv`, activated `2026-08-21T12:26:40Z`).

**MECHANISM METRIC READS:** bots/_v542wave/siege.py:4479 — `_v541_finishable`, the finisher condition on the REDIRECT path (`_v541_corefirst` at `siege.py:4429` → `_v541_fire` at `siege.py:4408`). The second live damage path is `_v541_core_attack` at `siege.py:4542`, reached from the sealer rung `siege.py:3711` and the support rung `siege.py:3786`.

**TREATMENT DIFF TOUCHES:** bots/_v542wave/siege.py bots/_v542wave/raid.py bots/_v542wave/doctrine.py bots/_v542wave/main.py bots/_v542wave/eco.py

**TREATMENT DIFF REFS:** 28fde0637^ 28fde0637

**INTERSECTION:** yes — `siege.py` carries the entire `_v541_*` family and is in the diff that introduced the plank (`28fde0637`, *"the v541 core-peck finisher, each behind its own flag"*). ⚠ **AND THE HONEST SHAPE OF THIS INTERSECTION:** there is no treatment/control arm pair in this leg — the "treatment" is a whole shipped bot. The intersection is asserted against **the commit that introduced the measured mechanism**, not against a two-arm diff, and OB13's actual guarantee is what matters: *the metric is not downstream of code that is identical in both arms*, because the metric's read path IS the plank.

**METRIC WINDOW:** r0-r1000 — the whole game. The peck path carries no round floor of any kind.

**GATING CONSTANTS:** none round-valued on the peck path — the masters are booleans (`FS_V541_COREPECK` / `FS_V541_COREFIRST` / `FS_V541_FINISH_ON`, all True in the shipped tree; `FS_V541_IDLEPECK` False; `doctrine.py:5923/5931/5950/5937`). The five round gates grepped out of `siege.py` (`FS_V519_GF_MIN_RND`, `FS_V519_GF_MAX_RND`, `FS_V520_SPLIT_MAX_RND`, `FS_V521_BUYIN_MAX_RND`, `FS_V539_MIN_RND`) govern ferry-plant, split-throw, buy-in and famine — **none of them is on the peck's call path.**

**MECHANISM CAN OCCUR IN WINDOW:** yes — for the ADDITIVE path unconditionally, and for the REDIRECT path **only under an HP gate that is NOT a round gate and is registered in §HP-GATE below.**

**BAR:** ≥ 50.00% — the CONVERSION SHARE (arriving games with ≥1 builder-peck core-damage event, over arriving games) must be a **strict majority**. **POINT RULE ONLY (OBLIGATION 16):** the bar licenses no exclusion of any effect SIZE; its MDE is 0.00pp by construction because 50.00 is a boundary named by the falsifier, not a null plus a detectable increment. It answers one question — *do converting games outnumber non-converting ones among arriving games* — and it may not be quoted as having excluded anything else.

**BAR SOURCE:** constructed on this page from the falsifier's own wording in the commission (*"if that is the majority of arriving games, the finisher is refuted as shipped"*). The 50.00 boundary is the falsifier's complement and nothing else; it is not copied from any banked number.

**BASE RATE:** 0.00% conversion — **ZERO** builder attacks into an enemy core across the v174 field debut, against 419 attacks made *while standing next to an enemy core*.

**BASE RATE SOURCE:** **(relayed)** — the commission's *"arrival 25/25, damage 0"* history, sourced there to `AUTOPSY-v174-losses-2026-08-21.md` and the s52 coordination tail. The same figures are quoted inside the treatment tree itself, read by this drafter: `bots/_v542wave/siege.py:4432-4448` — *"556 builder attacks across ten match games. 517 into enemy CONVEYORS, 12 into gunners, 27 onto tiles whose target was already dead, and ZERO into an enemy CORE. 419 of those attacks were made while standing next to an enemy core"* — and `siege.py:3781` — *"25 of 25 games, dealing 0 HP."* ⛔ **RATIFIED — THE AMBIGUITY IS RESOLVED BY PINNING TWO DIFFERENT QUANTITIES TO TWO DIFFERENT POPULATIONS, because they ARE different quantities:** (1) **the registered CONVERSION base rate (0.00%) refers to the 10-game v174 loss-autopsy set** — 556 builder attacks, 419 made core-adjacent, 0 into an enemy core (`AUTOPSY-v174-losses-2026-08-21.md:41,101,161`); its **kladde subset (356 attacks, likewise 0 into core) is the leg-matched slice** and is the comparator named in any readout sentence about THIS opponent. (2) **The "25 of 25" figure is the ARRIVAL history from the 25-game v174 field-debut set** (`AUTOPSY…:438`) and sources the arrival-rate expectation in §n-PLAN ONLY — it is never the conversion denominator. A readout comparing conversion against the field-debut population is comparing against the wrong fixture and is a registration breach.

**POOL ERA:** 2026-08-21T05:26:38Z..now — from v174's activation (the base rate's population) to this leg's games.

**SPANS-POOL-CHANGE:** yes, and deliberately — the derived rated-pool boundaries are **2026-08-13T07:12:59Z** and **2026-08-21T07:52:59Z** (derived at draft with `tools/prereg_check.py`'s own `pool_eras()` over 7,355 rated rows / 53 maps; rule ≥3 new maps within 24 h AND ≥500 prior games). **v174 was activated at 05:26:38Z, i.e. BEFORE the 07:52:59Z boundary, so the BASE RATE's population straddles it while this leg's games sit entirely after it.** ⇒ **The base rate and the leg are not on the same map population, and the base rate is used ONLY as the direction-setting comparator for a count-shaped mechanism claim — never as a share to subtract.**

**ESTIMATOR:** Wilson score interval on the conversion share, computed on the **DEFF-adjusted effective n** (`n_eff = n_arriving / 1.434`). Declared here so it cannot be chosen after the data: **no normal approximation, no Agresti-Coull, no exact-binomial substitution at readout.**

**PLANNED n:** 25 games (5 accepted unrated matches × 5 games) — **RATIFIED, WITH A PRE-COMMITTED EXTENSION: if the n=25 read lands in the UNRESOLVED band (7-18 of 25 converting), a SECOND window of 5 accepts fires and the pooled n=50 is read under the n=50 gate registered in §n-PLAN (falsifier ≤16/50, bar ≥34/50, unresolved 17-33; thresholds recomputed by the builder at lock, same Wilson/DEFF form — the n=25 thresholds independently reproduced 6/19 digit-for-digit first). The pooled read is labelled "pooled after a boundary trigger, extension pre-committed" (the s52 rule). A falsifier-fire or bar-met at n=25 is FINAL — the extension exists to rescue resolution, never to re-roll a resolved verdict.**

**BOUNDARY:** 5 accepts = 25 games. Counted over **ACCEPTS, never attempts** (the CAL-8 incident: a boundary counted over attempt lines; `games = 5 × accepts` is the identity that makes a miscount visible).

**CUT-SHORT:** below 15 games (3 accepts) **and** below 10 ARRIVING games, the leg is **DESCRIPTIVE ONLY** — dose counts reported, no bar verdict, no falsifier verdict. Between those floors and the planned n, the bar may be read only with its own interval printed in the same sentence.

**REFERENCE n:** none — this is a one-sample read on the leg's own games. No fixed reference sample contributes a floor to the variance.

**GATE RESOLUTION:** ⭐ **OBLIGATION 12 — the gate is sized, and its branches are pre-committed.** At n = 25 arriving games, DEFF 1.434 (n_eff ≈ 17.4), Wilson-95:

```
observed conversion   <= 6/25  (<=24%)  ->  upper bound < 50   =>  FALSIFIER FIRES
observed conversion   >= 19/25 (>=76%)  ->  lower bound > 50   =>  BAR MET
observed conversion   7..18/25          ->  interval CONTAINS 50  =>  UNRESOLVED
```

**AND THE PRE-COMMITTED DEFAULT: AN UNRESOLVED GATE DEFAULTS TO THE
RESTRICTION, NEVER THE PERMISSION.** An **UNRESOLVED** band here means the
finisher is **NOT credited** as converting arrivals into core damage; it does
NOT mean "promising". The permissive branch is granted only by the ≥19/25 cell.

**PRE-STATE (OBLIGATION 7):** ⭐ **THE PREDICTED-CHANGE SET IS VERIFIED NOT TO BE
IN THE TARGET STATE ALREADY.** The outcome is *core damage IN OUR FAVOUR from a
builder peck*, and its pre-state is **ZERO** — the base rate is 0 core attacks
out of 556 builder attacks, and the tree's own comment reads *"25 of 25 games,
dealing 0 HP"*. **This prereg predicts change on a cell measured at the floor, so
it can fail honestly.** *(The obligations doc's own template: the outcome is
stated as damage in OUR favour, not as a "mix" of damage events.)*

**MAP SEGMENT:** none expected — **and the reason is structural, not an absence
of thought.** The v541 dose in the local battery landed on `midgard` seat B and
nowhere else (12 of 12 midgard/B cells, 0 elsewhere over 180 cells), which
*looks* map-conditional. **It is an ARRIVAL fact, not a peck fact:** that is the
cell where our raider reaches the enemy collar against that particular control.
**This leg's primary metric is CONDITIONAL ON ARRIVAL — the arrival's terrain
dependence sits in the denominator and divides out.** ⇒ **No per-map read is
registered. Any map pattern observed is POST-HOC and must be presented as such**;
at 25 games over a 15-map pool a per-map cell holds 1–2 games and resolves
nothing. **RATIFIED: no primary segment is named — the drafter's structural
argument (arrival dependence divides out of a conditional-on-arrival metric)
stands; any map pattern is POST-HOC.**

**DOSE:** engine-side paired cells in which the peck acted — treatment 12 of 180 cells vs flag-off control 0 of 180 cells (n=180 local paired cells, `_v542wave` against `_v542wave[FS_V541_COREPECK=False]`, `docs/research/BUILD-REPORT-v542wave-2026-08-21.md` §2.2). The single-call harness carries the same two verdicts: `FS_V541_COREPECK=True → fired=True core_hp=-2 ti=-2 pecks=1` vs `FS_V541_COREPECK=False → fired=False core_hp=+0 ti=+0 pecks=0` (§3.3). ⚠ **THE BUILD REPORT ITSELF CALLS THIS DOSE THIN — "1 cell of 180" at the n=180 battery against the baseline opponent, and a ZERO finisher dose on the mjolnir cell.** ⇒ **The live dose IS the question, not a precondition** — which is precisely why this is a dose leg and not a screen.

---

## §HP-GATE — ⛔⛔ THE INERTNESS CONDITION THIS PAGE EXISTS TO REGISTER, AND IT IS NOT A ROUND GATE

**OBLIGATION 17's incident class is "the metric is observed where the mechanism
is gated off in both arms". Here the gate is not a round constant, so no round
window can assert against it — and this page asserts against it by hand.**

Read off the shipped tree at draft, both paths traced:

| path | entry | HP gate on the enemy core |
|---|---|---|
| **REDIRECT** (raid layer, body on-seat) | `raid.py:421` → `_v541_corefirst` (`siege.py:4429`) | ⛔ **YES** — `_v541_finishable` (`siege.py:4479`) requires `core HP ≤ min(FS_V541_FINISH_HP, 2 × (FS_V541_MAX_PECKS − pecks_used))` = **≤ 120 HP** for a fresh body (`doctrine.py:5963`, `:6059`) |
| **ADDITIVE / IDLE** (siege sealer + support rungs) | `siege.py:3711`, `siege.py:3786` → `_v541_core_attack` (`siege.py:4542`) | **NO HP gate** — but gated on the idle predicate, the collar/sentinel build reserve, and the **ammunition clause** (`FS_V541_AMMO_AWARE`, `siege.py:4360`: refuse while a live sentinel's magazine is below `FS_V541_AMMO_MIN`) |
| RAID-layer additive | `raid.py:478` | **OFF in the shipped tree** — `FS_V541_IDLEPECK = False` (`doctrine.py:5937`) |

⇒ ⛔⛔ **THE CONSEQUENCE, REGISTERED BEFORE THE FIRE: A ZERO RESULT HAS TWO
COMPLETELY DIFFERENT CAUSES AND THE READOUT MUST SEPARATE THEM.**
1. **"Never in finishing range"** — the enemy core's minimum HP over the game
   never fell to ≤120, so the redirect path **could not fire in either arm**.
   That is an INERT observation about the fixture, not a refutation of the
   plank.
2. **"In range and did not fire"** — the core entered ≤120 HP with our body
   adjacent and no peck damage followed. **That is a real refutation.**

**⇒ MANDATORY DECOMPOSITION COLUMN: per game, the MINIMUM enemy-core HP observed
while any of our builder bots was orthogonally adjacent to the enemy core.** A
readout that reports the conversion share without this column has not
distinguished a refutation from an inert fixture, and per the pre-committed
default it must then read **UNRESOLVED**, not "refuted".

⚠ **AND THE HYPOTHESIS IS NARROWER THAN THE HEADLINE BECAUSE OF THIS.** "The
finisher converts arrivals into core damage" is only fully testable where the
sentinel line has already brought the core into finishing range. **The ADDITIVE
path is the only one that can open damage on a full-HP core, and it is the path
carrying the ammunition clause** — so a null could equally be the ammunition
clause winning the 2 Ti. **RATIFIED: (a) the pooled conversion share is the
PRIMARY as registered; (b) the finishing-range-restricted redirect share is the
pre-declared DECOMPOSITION — reported always, primary never. The mandatory
min-enemy-core-HP-while-adjacent column is what keeps (b) honest.**

---

## §MECHANISM METRIC — ENGINE-SIDE ONLY, AND HOW EACH QUANTITY IS OBTAINED

All four are read from the platform replay's entity/position events. **None is
read from our own stdout** (stripped: 30,664 of 30,664 `BotOutput` events carry
an empty `stdout`).

1. **ARRIVAL (the denominator).** Per game: ≥1 round in which one of our builder
   bots occupies a tile orthogonally adjacent to the enemy core's 2×2 footprint.
   A game with no arrival is **excluded from the conversion denominator and
   reported separately** — an unarrived game says nothing about a finisher.
2. **PECK EVENTS (the numerator's source).** Per game: count of our builder
   attack events whose target tile is part of the enemy core footprint.
3. **CORE HP DELTA ATTRIBUTABLE TO PECKS.** Enemy-core HP decrements of **2**
   coincident with (2) — the builder attack is 2 Ti → 2 damage, which
   distinguishes it from gunner (7) and sentinel (18) damage.
4. **CORE-HP-ADJACENT MINIMUM** — the decomposition column from §HP-GATE.

**CONVERSION SHARE = |{arriving games with ≥1 event of type (3)}| / |{arriving
games}|.**

⚠ **INSTRUMENT NOTE, and it is a real risk this draft cannot close from its named
inputs:** the drafter did **not** verify that a decoder for (2)/(3) exists and
has been driven to both verdicts on this replay surface. **RATIFIED — OB17
DISCHARGED BY THE BUILDER AT LOCK, BOTH HALVES EXECUTED, NOT ASSERTED:**
* **EXECUTING TOOL: `tools/corpus/replay_autopsy.py`** (self-checking core-damage
  ledger built on `tools/replay_census.py`; its attribution rule for
  `BuilderAttack {id, target}` into the enemy core footprint is exactly metric
  (2)/(3), and its per-round HP deltas carry the 2-HP granularity).
* **DRIVEN TO BOTH VERDICTS ON THIS SURFACE, TODAY:** its `enemy_core` column
  read a CONSTANT 0 across the entire autopsy population — a column never seen
  to read the other verdict validates nothing — so the builder built a positive
  fixture (`bots/_probe_peck_a/b`, builders walk to and peck the ENEMY core) and
  ran it: `scratchpad/s53_peckdrive/peck_skald2.replay26` reads **attributed
  `{'builder_attack': 500}` on the enemy core, total=500 MATCH, per-round 2-HP
  deltas r16-102, per-bot hit counts (85/86/79)**. Negative control: the same
  session's zero-attack game (`peck.replay26`) reads damage=0 MATCH. **The
  decoder sees pecks when they exist and zeros when they do not.**
* **CONSEQUENCE OF SILENT NON-EXECUTION, now closed:** a decoder blind to pecks
  would have read the 0% base rate forever and manufactured a false refutation —
  the positive control is what makes a 0 in this leg a fact about the BOT.

---

## §n-PLAN — HOW MANY 5-GAME MATCHES, AND WHY THAT n RESOLVES THE FALSIFIER

**The mechanism is per-game COUNT data, and the falsifier is a MAJORITY test on
a binomial share.** That is what sizes the leg — not a game-share MDE, which
this leg does not carry.

**PROPOSED: 5 accepted matches = 25 games = ONE rate-limit window** (the
platform allows 5 test/unrated matches per 20 minutes; the window IS the
cadence).

**WHY 25 RESOLVES THE FALSIFIER — and this is the reasoning, not an assertion.**
The registered base rate is **0%**. A result anywhere near it is *far* from the
50% boundary, so the falsifier direction is cheap to resolve:

```
Wilson-95 upper bound, DEFF 1.434, n_arriving = 25  (n_eff ~ 17.4)
  0 of 25 converting  ->  upper ~ 18.1%   -> excludes 50 by ~32pp   FALSIFIER FIRES
  6 of 25 converting  ->  upper ~ 47.5%   -> still excludes 50      FALSIFIER FIRES
  7 of 25 converting  ->  upper ~ 51.5%   -> contains 50            UNRESOLVED
```

⇒ **25 games buys the falsifier outright IF the plank has not moved the
mechanism** — which is the outcome the history predicts and therefore the one
the leg must be powered against. **It buys a CONFIRMATION only at ≥19/25
(≥76%).** The middle band 7–18 is **UNRESOLVED** and defaults to the restriction.

**WHAT 50 GAMES (2 windows, 10 accepts) WOULD ADDITIONALLY BUY:** the
confirmation threshold falls from ~76% to ~67% (half-width at p=0.5 goes
±23.5pp → ±16.6pp). **It buys nothing at all on the falsifier side**, which is
already resolved at 25.
**RATIFIED — 25 PRIMARY, EXTENSION PRE-COMMITTED (the registration-block form
governs):** the second window fires **only** on an n=25 UNRESOLVED (7-18
converting); the pooled n=50 gate, computed by the builder at lock with the
registered Wilson/DEFF form (the n=25 thresholds reproduced 6/19
digit-for-digit first as the positive control on the arithmetic):

```
pooled n=50, DEFF 1.434 (n_eff ~ 34.9), Wilson-95
  <= 16/50 converting  ->  upper < 50   =>  FALSIFIER FIRES
  >= 34/50 converting  ->  lower > 50   =>  BAR MET
  17..33/50            ->  contains 50  =>  UNRESOLVED, FINAL (no third window)
```

⚠ **THE DENOMINATOR IS ARRIVING GAMES, NOT GAMES.** The registered resolvability
above assumes arrival in all 25. The history is *arrival 25/25* **(relayed)**,
which is why 25 was proposed — **but if arrivals come in below ~20, the gate's
branches widen and the CUT-SHORT floor (≥10 arriving games) becomes the binding
clause.** Report arrivals/25 first, before any conversion number.

---

## §FIRE GATES — CHECKED IN THE SAME SHELL, IN THIS ORDER

1. ⛔ **HOLDER READ, SAME SHELL, LOAD-BEARING FIELD:** `fcode status` must print
   **`Active bot: v177`**. **Gate on the presence of the `Active bot:` line,
   never on `$?`** — this CLI exits 0 while printing `Error: True`. **(relayed)
   v177 is already our active submission, so THIS LEG NEEDS NO SUBMIT: there is
   no prototype activation, no holder displacement and no rated-Elo exposure.**
   ⇒ The `EVICT58`-style submit-hold procedure does **not** apply here, and the
   −8 Elo/leaked-match budget is **zero for this leg** — *provided* gate 1 reads
   v177. **If it reads anything else, the leg does not fire until it does.**
2. **PIN ON EVERY ACCEPT:** `--match 6ce2d9b6-cdba-462e-89bc-ee169a62b0f8`.
   ⛔ **OBLIGATION 17 — DISCHARGED AT LOCK, runner source grepped (installed
   fcode 2.3.6), not `--help`:** `commands/match_group.py:57` declares
   `--match → source_match`; `unrated()` passes it to `_show_unrated`
   (`commands/test.py:13`), which sets `body["sourceMatchId"] = source_match`
   and posts `/api/matches/unrated`; on accept the CLI echoes
   `Opponent version from match: <id>`. **The path exists end-to-end and is
   NOT silent** — and the per-accept decoded-oppver alarm clause below still
   binds, because a server-side drop would be invisible to the CLI echo.
3. **RATE LIMIT:** 5 test/unrated matches per 20 minutes, shared across
   `match unrated` and `match test`; **rejected attempts appear to count.** A
   runner must **wait out the window and retry the same cell** rather than
   logging `fired 3/5` and moving on.
4. **TARGET-VALUE BAND, re-verified in the lock shell** — the declaration is
   immediately below; re-run `tools/target_value.py --band` in the same shell
   rather than quoting the cached row.

**TARGET BAND:** re-verified in the builder's lock shell (2026-08-21 ~13:3xZ) — kladde chatte tville **1942.79 LIVE** (`fcode team search --json`, teamId `c7571c87-f960-4c88-a13d-14340bb3200f` — matches the pin's target), our rating 1830, **gap +113**; exact logistic (K=32): **a 5-0 pays +21.0, a 0-5 costs −11.0, reachable YES**. *(The 12:50Z cached read said 1927 — 16 points stale in 45 min, on-brand for this riser; the drafter's re-verify demand was correct.)* ⚠ *This leg is unrated, so the band does not price THIS leg's games; it prices the ladder value of the matchup the leg is buying information about — kladde being the largest current drain against our line.*

---

## §PIN INSTRUMENT-ALARM CLAUSE — MANDATORY IN THE LOCK

⛔⛔ **kladde shipped v171 → v173 within ~a day (relayed). ANY DECODED `oppver`
OTHER THAN 173 IN THIS LEG IS AN INSTRUMENT ALARM: either the pin did not take,
or the decode is wrong. REPORT IT AND DO NOT READ THAT CELL.**

* The alarm is **per accepted match**, checked at readout against the decoded
  opponent version — **not** assumed from the fact that `--match` was typed.
* **A pinned set whose decoded `oppver` values DIFFER among themselves is the
  same alarm**, and it invalidates the pooling of those matches, not merely one
  of them.
* **A NULL `oppver` IS NOT A PASS.** The field read the literal string `'None'`
  in 4,375 of 4,375 rows until it was backfilled on 2026-08-13; **a null reads
  as "no version change" to any cut that trusts it.** An unreadable `oppver` is
  an alarm, not a clean cell.

---

## PREDICTIONS

**P1 (PRIMARY — the bar).** Among arriving games, the conversion share is a
**strict majority (≥50%)**: the v541 finisher puts ≥1 peck of core damage into
the enemy core in most games where our raider stands next to it.

**P2 (DECOMPOSITION, pre-declared so it cannot be selected after the data).**
Restricted to games where the enemy core entered finishing range (≤120 HP) with
one of our builders adjacent, the redirect fires — i.e. `_v541_corefirst`
converts an action the parent was already spending on a 20 HP conveyor.
⚠ **P2 is a SMALLER denominator and is expected to be thin; it is registered as
a decomposition of P1, never as a rescue for it.**

**P3 (DESCRIPTIVE ONLY).** Game share against pinned kladde v173, **printed with
its ±23.5pp half-width in the same sentence. No bar, no pass/fail, and it may
not appear in any verdict sentence.**

---

## FALSIFIER

**⛔ THE FALSIFIER, STATED SO IT CAN FIRE: if games with ≥1 core-adjacent arrival
that show ZERO builder-peck core damage are the MAJORITY of arriving games —
operationally, conversion share ≤ 6 of 25 with a Wilson-95 upper bound below
50% — THE v541 FINISHER IS REFUTED AS SHIPPED.**

**What that refutation means, and what it does not:**

* **It closes the claim that v541 fixes the misdirected verb** — the plank
  shipped to convert *arrival* into *core damage*, and the arrival is not in
  doubt; only the conversion is.
* **It does NOT close core-pecking as a mechanism.** Per the §HP-GATE
  decomposition, a refutation attributable entirely to *"the core never entered
  finishing range"* is an **INERT FIXTURE**, reads **UNRESOLVED**, and closes
  nothing — the honest status is then *"never dosed"*, the LOKI-3 template.
* **It does not close the road on live-game evidence grounds either way:** a
  road closes only on live games (`CLAUDE.md` point 6), and this leg is live
  games — but 25 of them, against one pinned opponent. **A refutation here is a
  refutation OF THIS APPLICATION against THIS opponent, and the readout says so
  in the same sentence.**

**⭐ AND THE ANTI-GOODHART CLAUSE:** **peck counts up with core-HP delta flat is
a NULL, not a hit.** The metric that decides is (3) — HP actually removed from
the enemy core — never (2), the count of attack events. A body that pecks a core
being healed at +4/round is producing the number without producing the effect.

---

## §WHAT THIS LEG DOES NOT REGISTER (enumerated, so nothing is smuggled in later)

| not registered | why |
|---|---|
| any game-share verdict | 25 games, ±23.5pp — cannot resolve, and this is a mechanism leg |
| any Elo or ladder claim | unrated surface, zero rated exposure by construction (fire gate 1) |
| a matched flag-off arm | needs a second activation and a second window; **no comparative claim is available** |
| a per-map segment | conditional-on-arrival metric; 1–2 games/cell |
| a kill-round non-regression read | `PLANK CLASS: offensive` — the r300 admission bar does not bind |
| pooling with any unpinned kladde games | high-churn cell: **reportable, not poolable** |
| a `titanium_collected` or tiebreak claim | `R1000_IS_DEFEAT`; economy is instrumental and does not score |

---

## AUTHORITY

**Drafted by:** a fresh agent with no inherited session context, from the four
`PROVENANCE` inputs.
**Ratification, the lock commit, the fire and the verdict sentence:** the
**BUILDER** lane.
**Verdicts here:** none. This is a draft.
