# SCREEN PREREG — v9-FIX: COMBO minus DIGOUT (the suppression discriminator)

**Committed before the shard's first heartbeat (two-clock).** Builder s37.
Basis: COMBO final 52.30 OUTSIDE-ABOVE with TWORAID's mechanism SUPPRESSED
inside it (research's bar-2 read: two-at-once 7/25 vs control 11/25, zero in
the Leviathan cell); doctrine diff narrows the suspect to LOKI_DIGOUT_ON or
UNDERECO's code path. `bots/_v213ecoraid` = `_v205combo` with
`LOKI_DIGOUT_ON=False` — one flag.

## Twin questions, one shard
1. **CURRENCY:** does minus-DIGOUT beat the full combo's 52.30? (DIGOUT's
   own single read no-information with kills traded away — plausibly dead
   weight or mild poison.)
2. **MECHANISM (the discriminator):** does the second sentinel return?
   Read at the LIVE leg (wire, matched cells) if the screen escalates — the
   local screen cannot show it (self-play doesn't camp), so the screen's
   role here is currency + harm only. If a leg later shows two-at-once back
   at control levels, DIGOUT was the poison; if still suppressed, it's the
   eco code path and the fix is harder.

## Shard
`ECORAID` vs `_v197mapcode`, n=5400, seed base 238000. Futility gates per
RULE-futility-gates. D26: replicated iff |share−50| ≥ 2.0pp, second 239000,
scored alone, same-side pooling. Kill-round paired-seed reported (family
context: COMBO +5.0, UNDERECO +11.0).

## Not licensed
No ship packet regardless of reading (Magnus's hold governs); an
outside-above result assembles into the fixed-v9 evidence base beside
APPRLAUNCH's final and the rc8.4 read.
