# SCREEN PREREG — SEAL139: SEALFLOOR0 re-based on the v139 chassis

**Committed BEFORE the shard's first heartbeat.** Builder s38, 2026-08-14.
Context: Magnus released MAPSEAL's slot ("I'm not sure MAPSEAL tells us
anything more than SEALFLOOR0 does now that we shipped the MAPFIX") —
correct: post-ship, MAPSEAL's v125 control makes its number ≈ mapfix
effect + seal effect, both measured elsewhere; cancelled at n≈1064, rows
kept (its final would have added only the small mapfix×seal interaction).

## Arm and purpose
Treatment `bots/_v221mapseal` — **byte-wise = the v139 tree +
`LOKI_SEAL_TI_FLOOR 12→0`** (verified at build: 6 lines vs v125 = v139's
2 strings + the floor; i.e. 2 lines vs v139). Control `bots/_v218mapfix`
(v139). **This is the seal knob's ship-shaped single AND the attribution
partner for SEALREPAIR:** if SEALREPAIR finals strong, SEAL139 decomposes
it (combo − seal-alone = repair's marginal + interaction).

## Design
`SEAL139` vs `_v218mapfix`, full 15-map POST-patch pool
(`overnight_pool26.sh` + pool26 dir), n=5400, seed_lo 270000, futility
gates per RULE-futility-gates, OB-F band 48.67–51.33. D26: replicate iff
|final−50| ≥ 2.0pp (seed 271000). Kill-round paired-seed rides. Coupling:
validated-asset tuning (s30 ablation), screen-trustworthy; ship path
through the live surface, SHIP_SIT governs (v139 sitting).

## Target-value line
Local screen, zero live exposure ⇒ payout gate N/A.
