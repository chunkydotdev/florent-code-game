# SCREEN PREREG — #53 SEAL SWEEP, first three arms (one knob each)

**Committed BEFORE any arm's first heartbeat (two-clock vs first TSV row).**
Builder s38, 2026-08-14. Queue #53 (research-stocked, deepening lane): the
barrier-seal is a validated −10pp asset (s30 ablation 399/1024) shipping at
birth constants never swept. Coupling class per research's row metadata:
self-knowledge/field-universal ⇒ screen-trustworthy at full weight.

## Arms (all vs `bots/_v197mapcode`, standard 15-map pool, n=5400 each,
## futility gates per RULE-futility-gates)
* `bots/_v219sealfloor0` — `LOKI_SEAL_TI_FLOOR 12 → 0` (seal whenever the
  barrier itself is affordable; earlier/more seals).
* `bots/_v219sealfloor24` — `12 → 24` (seal only when rich; fewer, later).
* `bots/_v219sealfirst` — priority swap in `_raid_act`: SEAL outranks PECK
  (incumbent pecks first; a raider on a seat spends its action pecking while
  free seats stay open). Verified by line order (seal @256 < peck @268;
  incumbent 256/270). Comment labels inside the swapped region are now
  stale prose — code semantics are the swap, cosmetic drift accepted in a
  screen arm, never in a ship tree.

## Dose evidence
Code-level: single-occurrence asserted constant swaps; order swap verified
by grep line numbers both trees. Behavioral dose (seal counts/timing per
game) is NOT pre-verified — if any arm reads outside its band, a kept-replay
seal-count spot check is owed before the verdict sentence cites mechanism.

## Read rules
OB-F final band 48.67–51.33 at n=5400; gates at 1000 (<48 drops) and 2700
(≤50.5 drops). D26: any final |share−50| ≥ 2.0pp replicates (seeds +1000,
scored alone, same-side pooling). Kill-round paired-seed non-regression
rides (a seal that slows our own kill violates the defence bar). Maps note:
the pool's valkyrie file is PRE-patch (frozen mid-shard era, same fixture
as every concurrent shard — consistent internally; era-labeled at read).

## Not licensed
No combo claims; no ship implication (hold + SHIP_SIT govern); #52 collar
medic is NOT this screen (its self-play read is harm-only and it stays
queued for the fixture library).

## Target-value line
Local screens, zero live exposure ⇒ payout gate N/A.
