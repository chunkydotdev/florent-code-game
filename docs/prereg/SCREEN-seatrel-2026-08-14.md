# SCREEN PREREG — #8 seat-relative scan order (+ the coupling test's decoupled probe)

**Committed before the shard's first heartbeat (two-clock).** Builder s37,
2026-08-14. `bots/_v216seatrel` = incumbent + the `_v151seatrel` plank ported
verbatim (orient CARDINALS toward the enemy once per core turn — 20+
first-match-wins scans become seat-canonical; selftest
tests/test_seat_relative.py drives both verdicts: 11/11 maps canonicalise
under treatment, 0 under the shipped order). Both-seat smoke clean.

## Dual role
1. **#8 on its own merits:** seat A/B splits of 54/47-ish appear in every
   shard's seat line; a seat-canonical scan should narrow them. Read the
   per-seat split beside the share.
2. **THE COUPLING TEST'S DECOUPLED PROBE (TEST-coupling-hypothesis):** pure
   geometry, opponent-invariant by the classification rule. **Registered
   prediction: if this screen finals ≥51.33, its live pooled read transfers
   within ±2pp of the screen** — the contrast case to GBNS/L4REPAIR's
   predicted ≥5pp flattery gaps.

## Shard
`SEATREL` vs `_v197mapcode`, n=5400, seed base 244000, futility gates, D26
(replicate iff |share−50| ≥ 2.0pp, seed 245000, scored alone, same-side
pooling). Kill-round paired-seed reported.
