# SCREEN PREREG — V140VS142: our v140 vs x3r0's v142, the slot decision screen

**Committed BEFORE the shard's first row** (two-clock; side lane certifies
against the first remote heartbeat). **Authorization: Magnus, verbatim, this
session:** *"X3r0 put up a bot, analyse if we beat it at n=1000 and put ours
up if we do"* — combined with his rule set minutes earlier: *"at n=1000
anything below 51 is considered bad now."*

**Contrast:** TREATMENT `bots/_v223sealrepair` (v140 "Loki v10", ours, the
displaced holder — era closed at k=8, RULE=held, net +31.8) vs CONTROL
`bots/_x3r0v142` (staged verbatim from platform artifact v142.zip, downloaded
16:49Z, compile-checked; "Counter Router v3" — a two-policy router keyed on an
FNV-1a terrain fingerprint, reads no opponent identity per its README).

**Fixture:** work-server-1 remote (NULLHOST-certified 49.00), WORKERS=40 on
48 vCPU — no oversubscription, so the wall-clock-TLE load bias documented
today does not arise the way it would on the loaded local box; noted because
v142 is a DOUBLE-SIZE bot (two full policy sets) and a contested box would
bias against the compute-heavier side. Full 15-map post-patch pool, seed
partition +32M. ⚠ Engine pin 2.3.6 while the platform offers 2.3.8 — same
fixture as the V141VS140 benchmark; a rules delta between engine versions is
a shared caveat on both benchmarks, not a differential one.

**n = 1000. DECISION RULE, registered now:**
* our share **≥ 51.0 at n=1000** → **activate v140** (existing platform
  version — a re-activation, not a submit; executed inside a safe window just
  after an observed pairing per the house activation discipline).
* our share **< 51.0** → v142 stays; result reported to Magnus, no action.
* Futility/catastrophe gates apply on the way (400-CATA <40; this shard's own
  read is the n=1000 look — no 2700/final exists at target 1000).

**No pooling with any other surface; this shard alone decides.** Descriptive
per-map splits owed at wrap-grade leisure either way.

## Target-value line
Local/remote screen, zero live exposure. The activation decision it feeds is
itself Magnus-authorized; the payout question is his call, made above.

## AMENDMENT A1 — ADD-ONLY, BLIND TO DATA (side-lane pre-row audit; committed
## while the shard fills, zero rows read)
* **Operating characteristic of the bar, named:** at true equality v140
  reclaims only ~26% of the time (1−Φ(0.63) at ±1.58pp) — **ties go to the
  sitting v142 by ~3:1.** This is read as the intent of "put ours up if we
  BEAT it" and is accepted; a 50.x read is not relitigated later.
* **Engine caveat corrected:** 2.3.6-vs-2.3.8 exposure is shared but the
  EFFECT can be differential (a rules delta interacts with a terrain-
  fingerprint router differently than with our chassis). **Transfer of this
  local answer to the platform is UNVERIFIED** pending the 2.3.6→2.3.8
  changelog check (s38's upgrade checklist names --mark and a valkyrie map
  sync — suggestive of CLI+assets, not rules, but unverified is the word).

## A1 CAVEAT CLOSURE (post-decision; does not alter the decision)
ENGINE-238-DELTA-2026-08-14.md: 2.3.6→2.3.8 is RULES-IDENTICAL at the
Python/SBOM level (GameConstants + Controller byte-identical; rules crate
version unchanged; only a --mark CLI flag + zerocopy 0.8.55→0.8.56).
Transfer caveat moves UNVERIFIED → verified-at-source-level; residual: no
.so symbol diff was attempted (would make it airtight).
