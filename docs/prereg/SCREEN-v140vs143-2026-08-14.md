# SCREEN PREREG — V140VS143: our v140 vs x3r0's v143, the slot-pattern screen

**Committed BEFORE the shard's first row.** Authorization: Magnus's standing
pattern, given 14:4xZ for v142 and applied here on its plain reading — *"X3r0
put up a bot, analyse if we beat it at n=1000 and put ours up if we do"* —
with the application to v143 ANNOUNCED to Magnus in-session at ~15:47Z before
this screen fires; he can countermand at any point before the activation
branch executes. The slot-war context (third x3r0 displacement today, two
panels dead to holder churn, fire order suspended) is escalated to him
alongside, per the humans-resolve norm.

**Contrast:** TREATMENT `bots/_v223sealrepair` (v140, ours; beat v142 56.80
[53.7,59.9] at n=1000 this afternoon) vs CONTROL `bots/_x3r0v143` (staged
from platform artifact v143.zip 15:47Z, compile-checked; per its README a
two-policy terrain-fingerprint router, n_*/o_* module sets, "Artifact
Counter" naming — no opponent-identity reads claimed).

**Fixture:** work-server-1 remote, WORKERS=40/48 (no oversubscription), full
15-map pool, seed partition +32M, NULLHOST-certified host. Engine 2.3.6:
RULES-IDENTICAL to platform 2.3.8 at source level (ENGINE-238-DELTA, so the
v142 screen's transfer caveat does NOT carry here; residual .so-symbol-diff
caveat only).

**n = 1000. DECISION RULE (same as v142's, registered now):** our share
>= 51.0 at n=1000 → reactivate v140 in a safe window after an observed
pairing, verify on the Active bot: line; < 51.0 → v143 stays, report to
Magnus. Ties go to the sitter ~3:1 at true equality (the v142 A1 OC note,
carried). 400-CATA gate applies. No pooling; per-map splits descriptive.

## Target-value line
Local/remote screen, zero live exposure; the activation branch executes
Magnus's standing rule unless he countermands in-session.
