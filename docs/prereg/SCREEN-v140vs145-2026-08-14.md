# SCREEN PREREG — V140VS145: our v140 vs x3r0's v145, the slot screen

**Committed BEFORE the shard's first row** (two-clock: this file's git author
time vs the first `V140VS145` row's timestamp in `scratchpad/corefill.log` /
the first remote heartbeat; the side lane certifies the pair). No row of this
shard exists at commit time and none may be read before it does.

**Authorization: `PROGRAMME.md:28` `X3R0_SLOT_RULE: screen_n1000_reactivate_on_51`
— CODIFIED POLICY, not a per-instance announcement.** Magnus, verbatim,
2026-08-14, in-session: *"Whenever he outs one up, run n=1000 against it and
put ours back if we win."* This is the rule's **second codified firing**
(v143 was the first; v142 predates the codification and is its precedent).
**Consequence for this document: no fresh announcement to Magnus is owed
before the fire, and no counter-roll is taken without the number — the screen
IS the resolution mechanism** (`PROGRAMME.md:39`). He can countermand at any
point before the activation branch executes.

**Trigger:** x3r0 uploaded v145 "Top Team Router v3" at **2026-08-14T19:08:37Z**
(`isActive=true`), displacing our v140 at **k=22 rated matches, net_act=+69.8,
RULE=held** — i.e. displaced ABOVE `SHIP_SIT_MIN_K: 8` with the gate armed and
holding, so nothing about the displacement is a stop-loss event on our side.

**Contrast:** TREATMENT `bots/_v223sealrepair` (= v140, ours, the displaced
holder; beat v142 **56.80 [53.7, 59.9] n=1000** and v143 **57.06 [54.0, 60.1]
n=999**, both 2026-08-14) vs CONTROL `bots/_x3r0v145` (staged verbatim from
the platform artifact via `fcode submission download 145`, **106 `.py` files,
ALL compile-checked OK, 2026-08-14T19:14:53Z**; per its README a three-level
terrain-fingerprint router — `main.py` `WEAK_EXPERTS` over 9 named maps →
`base_router.py` over the full pool → 7 `p{NN}router` salt routers — with
**no opponent-identity or match-outcome inspection anywhere**).

**Fixture:** work-server-1 remote, WORKERS=40 on 48 vCPU (no oversubscription
— material because v145 is a 110k-line, 106-file bot and a contested box would
bias against the compute-heavier side), full 15-map pool
(`antler archipelago auroraveil drakkarfjord drumlin fjordgate frostgate
glacierkeep icefloe midgard nordkap ragnarok royale valkyrie yulerune`, parsed
from `tools/overnight.sh`'s single `MAPS=` line by `tools/vps/orchestrate.sh`),
seed partition per `tools/vps/orchestrate.sh` — offset = crc32(hostkey) % 50 ×
1e6, **work-server-1 = +32,000,000**, sidecar-recorded — NULLHOST-certified
host. Worklist row:
`V140VS145 bots/_v223sealrepair bots/_x3r0v145 1000 304000`
(⇒ effective seeds 32,304,000+, disjoint from the v142 and v143 shards).
Engine 2.3.6, **RULES-IDENTICAL to platform 2.3.8** (ENGINE-238-DELTA-2026-08-14:
GameConstants + Controller byte-identical, rules crate unchanged); residual
caveat is only that no `.so` symbol diff was attempted.

## n and decision rule

**n = 1000 games. DECISION RULE — the same registered rule as v142's and
v143's, restated here rather than incorporated by reference:**

* our game share **≥ 51.0% at n = 1000 — equivalently ≥ 510 of 1000 games** →
  **reactivate v140** (an existing platform version: a re-activation, not a
  submit) inside a safe window **just after an observed pairing**, verified on
  the **`Active bot:` line** of `fcode status`, never on an exit code.
* our game share **< 51.0% — equivalently ≤ 509 of 1000 games** → **v145
  stays**; result reported to Magnus, no action taken.
* **400-CATA gate applies** on the way (abort if share < 40% at n = 400).
  This shard's own read is the n=1000 look; no 2700/final band exists at
  target 1000.
* **No pooling with any other surface — this shard alone decides.** Per-map
  splits are DESCRIPTIVE except for the one primary segment declared below.

**Ties-to-sitter OC note (carried verbatim in effect from the v142 A1
amendment):** at true equality v140 reclaims only **~26%** of the time
(1 − Φ(0.63), 1 SE = 1.58pp at n=1000). **Ties go to the sitting v145 by
~3:1.** This is read as the intent of *"put ours back if we win"* and is
accepted in advance; **a 50.x read is not relitigated after the fact.**

### Gate resolution statement (obligation 12)

**The bar is a measurement and is sized here, before the fire.** At n = 1000
and p̄ ≈ 0.5 the naive 1 SE is **1.58pp** and the 95% half-width is **±3.10pp**.
⇒ **The 51.0 boundary CANNOT discriminate "true 51" from "true 50" at this n**
— the gate resolves large effects (the v142/v143 reads at 56.8/57.1 cleared it
by >3 SE) and does not resolve a genuinely marginal one.
**PRE-COMMITTED BRANCH FOR THE UNRESOLVED CASE: an unresolved gate defaults to
the RESTRICTION, never the permission — i.e. the sitter keeps the slot.** That
is already exactly what the `< 51.0` branch does, so no extra machinery is
needed; it is stated so the default is not mistaken for a judgement made at
read-out.

### Cluster enumeration and DEFF (CLAUDE.md scope procedure, performed in writing)

1. **MATCH cluster — DIES.** A local shard has no 5-game match wrapper; every
   row is an independently seeded single game. No stratum can hold two games
   from one match because no match object exists.
2. **OPPONENT cluster — degenerate, and measured.** Exactly one opponent
   (`_x3r0v145`) for all 1000 rows, and no between-opponent contrast is drawn,
   so there is no multi-member opponent stratum to inflate. Measured directly
   on this fixture class: **local pair-weighted DEFF = 0.98 (ρ = −0.020, 124
   shards, s39 audit)**.

⇒ **Applicable DEFF = 1.00 (measured 0.98; naive intervals are correct and
marginally conservative). The platform constants 1.529 / 1.833 are NOT
imported** — doing so would widen local intervals 24–35% for correlation that
is not there.
⚠ **The one place this could bite:** the s39 audit found local outlier arms
with strong map interaction reaching DEFF ≈ 1.25, and v145 is explicitly
map-conditional. **The segment split below is therefore read as indicative,
and a segment claim is banked only via the 15c re-screen, never off these
rows.**

## MAP DEPENDENCE (obligation 15a/15b) — the primary segment

**`MAP SEGMENT: the 9 v145 WEAK_EXPERTS maps — antler, drakkarfjord, drumlin,
frostgate, midgard, nordkap, ragnarok, royale, yulerune — versus the 6
fallback maps — archipelago, auroraveil, fjordgate, glacierkeep, icefloe,
valkyrie. MECHANISM: v145's top-level `main.py` router assigns a dedicated,
separately-tuned policy module per (map, side) on exactly those 9 maps and
falls through to `base_router.py` on everything else, so his tuning effort is
concentrated on a set we can name in advance from his own artifact.
EXPECTED DIRECTION: our share is LOWER on the 9 expert maps than on the 6
fallback maps.`**

**Verified at draft time, not assumed:** all 9 of his named expert maps are in
our 15-map pool, and the complement is exactly 6 maps — so both arms of the
segment are populated, ~600 vs ~400 games at even map allocation.

**EXACTLY ONE PRIMARY SEGMENT.** Every other per-map, per-side or per-size cut
is **descriptive** and may not be used to rescue a pooled fail. Named
explicitly as descriptive-only so they cannot be promoted later: per-map
splits; the A/B side split; the `base_router` `OPENINGS`-coverage split (14
named maps in his `OPENINGS`, 10 in `SIGNATURES` + 4 via `COLLISION_GRIDS`, so
at least one pool map may be unfingerprintable to him and land on a blind
fallback — interesting, not primary).

**15c applies:** if the pooled read fails < 51.0 **and** the 9-expert-map
segment clears 51.0 in the predicted direction, that is a candidate for a
**NEW screen with its own n** on that segment — never a re-read of these rows.
The pooled number still decides the slot today.

## Structural note — this screen is partly v140 against our own history

**v145's policy modules are, by his own docstrings, OUR archived versions.**
`crb/crg/fc/gu/la/t130` = `"LOKI-1 (v105)"`; `crc/lc` = `"LOKI-CHAMPION (d3)"`;
`d6` = `"LOKI-D6"`; `se` = `"LOKI-SEAL2"`; all 14 `p{NN}h` modules are
LOKI-1 (v105) and all 14 `p{NN}i` are LOKI-CHAMPION (d3). **So the control arm
is largely a per-map router over LOKI policy families we shipped, and the
screen is in substantial part v140 vs a map-conditional ensemble of our own
lineage.**

**Interpretive caveats this earns, stated before the fire:**
1. **A win here is NOT evidence that v140 beats a foreign strategy.** It is
   evidence that v140 beats a per-map mixture of earlier LOKI policies. The
   currency question ("who holds the slot") is unaffected — the slot is decided
   by the number, not by the provenance of the opponent's code — but any
   downstream generalisation to the ladder field must carry this sentence.
2. **A loss here is a live claim that a map-conditional mixture of our own
   history beats our current line**, which is a far stronger statement than a
   loss to an unrelated bot and would immediately promote the map-conditional
   road (`LEG-mapconditional-test-2026-08-14.md`) rather than closing it.
3. **His README's "official WR ~53.7% vs baseline loki (n=600)" is HIS number,
   measured on HIS fixture, and this screen DOES NOT CONSUME IT.** It is not a
   prior, not a bar, and not a comparator. It appears here once so that a later
   reader cannot mistake its absence for an oversight.
4. **No opponent-identity read is claimed or alleged.** Per the README, every
   v145 routing decision reads only own entity type/position, nearby buildings,
   the comms-store enemy-core position, and public map dimensions/terrain.
   Nothing in this screen is a norms question.

## Target-value line

**TARGET BAND: n/a — local/remote screen, ZERO live rated exposure; the
activation branch executes codified policy (`X3R0_SLOT_RULE`) rather than
buying rating, so `tools/target_value.py`'s reachable-band question does not
bind.** The rated value of the branch is the value of holding the slot with
the version measured better, which Magnus has already priced by codifying the
rule. **The one live cost this leg can incur is the reactivation itself**, and
it is a re-activation of an existing version inside an observed-pairing window
— no submit, no prototype exposure, no ~−8 Elo/leaked-match budget line.

## Obligations register (`docs/research/PREREG-amendments-and-lock-obligations-2026-08-09.md`)

* **Ob. 7 (outcome stated as MIX vs IN-OUR-FAVOUR):** satisfied — the outcome
  is **game share IN OUR FAVOUR**, not a win-condition mix. Not already in the
  target state at lock: the current state is v145 holding the slot, i.e. the
  predicted-change set is by construction not pre-satisfied.
* **Ob. 8 (denominator rule):** single opponent, single fixture, single shard;
  the denominator is 1000 games from one worklist row and is not pooled with
  any other surface.
* **Ob. 12 (gate carries its resolution statement):** satisfied above,
  including the pre-committed unresolved-case branch.
* **Ob. 13 (`MECHANISM METRIC READS: file:line`):** **N/A by shape, stated
  rather than skipped.** This is a whole-bot screen with no treatment diff and
  no mechanism metric — the treatment tree `bots/_v223sealrepair` is unchanged
  from the version that played the v142 and v143 screens, and the outcome
  statistic is the shard's own game share. There is no diff for a read path to
  intersect. **If any edit lands in `bots/_v223sealrepair` before the shard
  completes, this screen is VOID and re-preregs.**
* **Ob. 14 (opponent version stability):** satisfied by construction and
  better than the obligation asks — the control is a **pinned local artifact**,
  byte-frozen at 19:14:53Z. x3r0's platform churn cannot reach this shard. (The
  churn is real and is exactly why the screen is local: v145 is his third
  displacement of our slot today, after v142 and v143.)
* **Ob. 15 (map dependence):** satisfied above with a signed direction and a
  single primary segment. **Noted for the record: this is the FIRST prereg in
  `docs/prereg/` to carry a `MAP SEGMENT` line** — the obligation was minted
  s40 ~15:0xZ and the v142/v143 screens predate or missed it.
* **Ob. 1–4, 6, 9–11:** Ouroboros/CAD-leg-specific or mechanism-leg-specific;
  do not instantiate on a whole-bot screen.

## PROVENANCE

**This prereg was drafted by a FRESH agent with no inherited session context,
per the standing rule (Magnus, 2026-08-14, s40: one fresh opus agent per
prereg). Its ENTIRE input set was:**

```
PROVENANCE: docs/prereg/SCREEN-v140vs143-2026-08-14.md
            PROGRAMME.md:28-45 (X3R0_SLOT_RULE block)
            bots/_x3r0v145/README.md
            docs/research/PREREG-amendments-and-lock-obligations-2026-08-09.md
```

Plus what those four reference and the builder's handed facts (v145 upload
metadata, staging result, the v142/v143 screen numbers, the fixture and
worklist row). Files opened for verification of a stated fact only, all
read-only: `docs/prereg/SCREEN-v140vs142-2026-08-14.md` (the A1 OC note that
input 1 incorporates by reference), `tools/overnight.sh` `MAPS=` (the 15-map
pool, so the segment membership claim is verified not asserted),
`tools/vps/orchestrate.sh` + `scratchpad/vps/worker@work-server-1/SEED_OFFSET`
(the seed-partition figure), `ls docs/prereg/` and a `MAP SEGMENT` grep (the
first-prereg-to-carry-it claim). **No shard row of any kind was read; no game
was run by this agent.**

**Timestamp:** drafted 2026-08-14T19:16:39Z (`date -u`, same shell call).

## A1 — ADD-ONLY AMENDMENT (2026-08-14T19:22:31Z, BEFORE any shard row exists; zero rows fired)

**THE SLOT RESOLVED ITSELF BEFORE THIS SCREEN COULD: v145 was DEACTIVATED BY
ITS OWNER at 19:17:14Z** (holder_watch transcript: `HOLDER CHANGE
2026-08-14T19:17:14Z: v145 -> v140`; `fcode submission list` reads v145
isActive=False; `Active bot: v140`). **NOT by this lane** — no activation,
submit, or submit_clean invocation was run by the builder between v145's
19:08:37Z upload and this amendment; the lane's only platform interactions were
reads plus the read-only `submission download 145`. The side lane's
discriminating question (their 19:2xZ flag) is answered: branch (b), x3r0's own
rollback. This deactivation PRECEDED the lock commit (8a30265, 19:21:21Z) and
was discovered after it — stated plainly rather than reordered.

**Measured cost of the v145 window (side-lane derivation, consumed):** one
rated pairing at 19:12:59Z played as ourver=145, seat B, vs Erebus v103 —
**−10.0 Elo (1794 → 1784), matches 984 → 985.**

**RESTATED DECISION RULE, fixed before the first row:**
* The screen fires unchanged (same fixture, same n, same bar) and its number is
  **BANKED as the standing resolution for v145-on-the-slot**: if v145 (the same
  platform version, byte-frozen here as `bots/_x3r0v145`) holds the slot at
  any future time, **≥ 51.0 → reactivate v140 immediately per X3R0_SLOT_RULE
  with NO new screen; < 51.0 → no counter-roll, v145 sits** (until its own
  stop-loss per slot-rule machinery).
* **< 51.0 does NOT oblige handing the slot to v145 now.** The rule governs OUR
  counter-rolls; it has never obliged us to install his bot, and v140's
  presence on the slot at read-out is his doing, not step (3) executing early.
  This is the pre-commitment of the uncomfortable branch in its correct form:
  the number binds our NEXT counter-roll decision, in both directions.
* If v145 never returns, the number is descriptive (and caveat 2 of the
  structural note still promotes the map-conditional road on a < 51.0 read).

## A2 — ADD-ONLY CORRECTION + DIRECTIVE (2026-08-14T19:22:56Z, still zero rows fired)

**A1's attribution was WRONG: the 19:17:14Z rollback was MAGNUS'S, not x3r0's.**
Magnus, in-session, verbatim: *"I rolled back until you can decide if it's
better or not."* A1's mechanism claims (not-this-lane, holder_watch transcript,
−10.0 Elo cost row) all stand; only the actor changes.

**AND THE DIRECTIVE MAKES THE RULE SYMMETRIC — the screen decides the slot in
BOTH directions, superseding A1's no-counter-roll reading:**
* our share **≥ 51.0 (≥510/1000) → v140 STAYS** (already live; no action).
* our share **< 51.0 (≤509/1000) → REACTIVATE v145** in a safe window after an
  observed pairing, verified on the `Active bot:` line — his bot measured
  better and the team fields the better bot.
* Ties-to-sitter now favours **v140** (the current sitter); the ~3:1 OC note
  carries with the sitter identity swapped. A 50.x read leaves v140 up and is
  not relitigated.
**This is the uncomfortable-branch pre-commitment in full: the number installs
his bot over ours if we lose.** Typed before the first row.

## A3 — ADD-ONLY (2026-08-14T19:24:48Z, pre-first-row-read; research's non-transitivity objection consumed)

**LIMIT OF THE INSTRUMENT, on the record before any read: this is a HEAD-TO-HEAD
mirror, and the slot pays on FIELD earnings.** The two quantities come apart
under non-transitivity, and v145 is a per-map router — a shape built to be
non-transitive (research s42 objection, accepted). The mirror still decides
TODAY'S slot per Magnus's directive (A2), because the field read on v145 is
unavailable while v140 holds (`match unrated` plays the active submission).
**PRE-COMMITTED CHECK: if the < 51.0 branch fires and v145 is reactivated, its
tenure reopens the field window — research is commissioned AT THAT MOMENT to
prereg (fresh-opus) a matched six-cell field panel (the CAL-8 opponent ids —
NEW panel, CAL-8's own rows stay spent) run under v145. That panel's registered
bar, not this mirror, governs the NEXT slot decision.** A ≥ 51.0 read closes
the question with v140 holding and no field panel owed.

## A4 — ADD-ONLY FIXTURE CORRECTION + SHARD RE-FIRE (2026-08-14T19:36:06Z; no outcome column of any V140VS145 row has been read)

**THE FIXTURE LINE WAS FACTUALLY WRONG AND THE ERROR BIASES IN OUR FAVOUR.**
Magnus, in-session: work-server-1 is **16 cores and we borrow 10** — confirmed
on the worker's own startup line (`WORKER up. host=junghard-server ncpu=16
workers=40`, 19:23:31Z). The prereg's "WORKERS=40 on 48 vCPU (no
oversubscription)" was inherited from the repo record and is false: the shard
ran **2.5× oversubscribed with `--tle 10` WALL-CLOCK enforcement** (worker.sh:71,
271 — "NOT OPTIONAL"), which kills turns under contention and hits the
compute-heavier side (v145, 106 files) hardest — the exact bias this prereg's
own fixture sentence warned against.
* **DECISION: every row fired under WORKERS=40 is VOID for the decision rule**
  (kept on disk as a labelled artifact, never read into the verdict — voiding
  the flattering-direction error, not the inconvenient one). Worker stopped
  19:35:46Z at a batch boundary.
* **RE-FIRE: shard `V140VS145B`, same trees, same n=1000, same decision rule
  and segment, seedbase 306000, WORKERS=10** (the borrowed allotment; wall≈cpu
  at ≤ncpu so TLE parity holds). Clock 2 for the two-clock cert re-points to
  V140VS145B's first row; this amendment precedes it.
* **RETROACTIVE FLAG (research's lane, not resolved here): the v142 (56.80) and
  v143 (57.06) screens ran the SAME 40-on-16 fixture**, as did every remote
  shard since the WORKERS=40 bump (13:47:33Z on 2026-08-14). Their margins were
  >3 SE, but the shared-fixture bias is unquantified and is now an open
  research question — named here so the record cannot silently keep the
  numbers without the caveat.

## A5 — ADD-ONLY RESOLUTION PRE-COMMITMENT (2026-08-14T19:40:00Z; V140VS145B at <60/1000 rows, no outcome column read by any lane — side-lane look-discipline columns on record)

**Research's bar-resolution objection (SCREEN-PREDICTIVE-VALIDITY doc) is
accepted as arithmetic: at n=1000 the 95% half-width is ±3.07pp, so a point
estimate in (46.9, 53.1) does not exclude 50.0 and the 51.0 boundary cannot
resolve itself.** The registered rule is Magnus's codified directive and is NOT
changed. Instead, pre-committed blind:
* If the n=1000 point estimate lands OUTSIDE (46.9, 53.1): the registered rule
  executes as written, CI quoted in the verdict sentence.
* If it lands INSIDE (46.9, 53.1): **the shard EXTENDS ONCE to n=3000** (same
  trees, same rule, same fixture, seeds continuing the same partition), and the
  registered 51.0 rule then executes UNCONDITIONALLY at n=3000 (±1.77pp), CI
  quoted, no further extension. The extension is a resolution purchase, not a
  bar change; both prior x3r0 screens (56.80, 57.06) would have executed at
  n=1000 under this clause.
* Any verdict sentence from this screen carries the CI beside the point
  estimate — a bare share is not a compliant read-out.

## A6 — ADD-ONLY (2026-08-14T19:42:06Z; still blind, side-lane OC recomputation consumed)

**The A5 extension buys precision, not the decision** (side lane, independently
confirming research's arithmetic): at n=3000 the half-width is ±1.77pp and the
51.0 bar still sits inside the zone that cannot exclude 50.0; the n at which
this gate discriminates its branches is ≈9,412 games, which will not be run.
**Pre-committed verdict sentence for that case: a reading inside (48.23, 51.77)
at n=3000 is UNRESOLVED and resolves by the registered DEFAULT — the sitter
(v140) keeps the slot — and the verdict must say "v140 stays because the gate
could not separate the bots", never "v140 measured better."** A default is not
a measurement; the design's most probable terminal state hands the slot to our
own bot on a default, and that is stated here before the read so it cannot be
dressed as a result after it. (The unresolved zone is symmetric about 50.0, not
about the bar — readings on BOTH sides of 51.0 extend alike; certified branch-
symmetric by the side lane.)

## READ-1 — THE REGISTERED n=1000 LOOK (2026-08-14T20:02:55Z, taken by the builder off V140VS145B.tsv, side-lane cert plan 970320c/B11-B14 in force)

**n=1000 (B-shard rows ONLY, zero no-winner rows; the 480 voided WORKERS=40 rows
are NOT in this tally): T(v140) 492, C(v145) 508 — POOLED SHARE 49.20%,
CI [46.13, 52.27] (±3.07pp, DEFF 0.98).**
**49.20 lies INSIDE (46.9, 53.1) ⇒ the A5 pre-committed extension FIRES: the
shard extends ONCE to n=3000, same trees, same rule, same fixture (WORKERS=10),
seeds continuing the same partition. No slot branch executes at this read;
v140 remains the sitter through the extension per the registered default.**
* Treatment md5 at read time: c4e563af4730b4c1595c679fc25098e7 — UNCHANGED,
  VOID clause clean.
* 400-CATA gate: never evaluated in flight (no interim look was taken by any
  lane — side-lane look-discipline columns on record); moot at completion.
* **PRIMARY SEGMENT — PREDICTION FAILED, direction REVERSED:** predicted our
  share LOWER on his 9 WEAK_EXPERTS maps; observed expert-9 **54.2%** (n=600)
  vs fallback-6 **41.8%** (n=400). Descriptive per A1's DEFF caveat and 15c —
  and the honest confound is stated with it: the fallback-6 set contains our
  independently-known worst maps (archipelago 21%, glacierkeep 30%, fjordgate
  32% in this shard), so "his fallback is strong" and "we are weak there
  against anyone" fit these rows equally. A segment claim, if ever wanted,
  buys its own screen.
* **CARRIED CAVEAT (side lane, pre-declared blind at 240/1000, quoted
  verbatim):** *"this is a LOCAL-fixture result on a 23× compute asymmetry
  (4,757 vs 110,184 lines) with a wall-clock turn budget; local and platform
  TLE behaviour are not calibrated against each other."* Declared blind so it
  constrains both branches equally; it is external validity, not bias — the
  A4 contention defect stays closed.

## A7 — ADD-ONLY REPORTING OBLIGATIONS FOR READ-2 (2026-08-14T20:07:37Z; extension at <1100/3000, read-2 blind; research's pre-read note consumed and one assumption of it corrected)

**THE ZONE MAP AT n=3000, in one place so no lane re-derives it differently
(A2 registered rule + A6 unresolved default, which GOVERNS the middle zone —
research's pre-read table assumed A2 alone and is corrected here):**
* **read-2 ≥ 51.77:** v140 stays — outside the zone, rule and resolution agree.
* **read-2 inside (48.23, 51.77):** **v140 stays BY DEFAULT** (A6 verbatim:
  "could not separate", never "measured better"). This includes 49.20 holding.
* **read-2 ≤ 48.23:** v145 is reactivated — the < 51.0 branch, now also
  CI-resolved against 50.0 at the boundary. This is the ONLY branch that
  installs v145.
**MANDATORY SENTENCES, whichever branch executes:**
1. The verdict quotes BOTH exclusions: whether the CI excludes 50.0 and whether
   it excludes 51.0 — they come apart in the middle zone and the write-up says
   which question remained open (research: resolving 49.2-vs-50.0 needs
   ~n=14,800; this design cannot buy that).
2. **If read-2 < 51.0 in ANY zone** (including a stays-by-default outcome), the
   write-up carries the two-legs-pointing-the-same-way sentence: v142 (56.80)
   and v143 (57.06) installed v140 over x3r0 routers on a fixture the TLE retro
   showed FLATTERED the lighter bot, with the router as the heavy arm both
   times — a sub-51 clean-fixture read against the third router of that family
   is independent evidence in the same direction, and **"we may have twice held
   the slot against equal-or-better routers" is recorded as an open question
   with a named mechanism, not dismissed.** (The A3 field-panel commission
   still fires only on an actual v145 installation, because `match unrated`
   plays the active submission and no pinning can field-read a bot that is not
   on the slot.)
3. The side-lane 23× external-validity caveat (READ-1) carries to read-2
   unchanged.

## A8 — ADD-ONLY SUPERSESSION OF A STALE IDENTITY (2026-08-14T20:09:25Z; still blind to all rows past 1000; side-lane contradiction flag consumed — their analysis arrived after A7 but found the root cause A7 left standing)

**The Ob-12 sentence "That is already exactly what the `< 51.0` branch does, so
no extra machinery is needed" IS STRUCK AS OF A2 AND MUST NOT BE READ.** It was
true only while v145 sat (restriction branch = `< 51.0` branch by coincidence
of the holder); A2 inverted the sitter and made `< 51.0` the ACTION branch. The
RESTRICTION is defined as **"nobody moves — the sitter keeps the slot"**, never
as a numeric branch. A7's zone map is the sole operative reading: **A6 governs
(48.23, 51.77); A5's "executes unconditionally" applies OUTSIDE that zone;
v145 installs only at read-2 ≤ 48.23.**
**The choice this encodes — reading (i), restriction-over-permission — was
committed blind at A7 and its cost is stated rather than enjoyed: it is the
status-quo direction and it can keep OUR bot on the slot on a default. The
countervailing cost of reading (ii) is installing a competitor's bot on a
number that cannot exclude a coin flip, at a measured swap cost (−10.0 Elo on
the last holder change's first pairing). Magnus's codified directive is silent
on this state; his countermand window is open until the n=3000 read (~23:0xZ)
and this amendment is surfaced to him in-session.**
