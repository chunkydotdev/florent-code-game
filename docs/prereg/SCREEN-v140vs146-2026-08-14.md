# SCREEN PREREG — V140VS146: our v140 against x3r0's v146, the slot screen

**Drafted by a FRESH opus subagent with no inherited session context** (standing
rule, Magnus, 2026-08-14: the agent prepares, the lane ratifies and commits).
Drafting shell `date -u` at completion: **2026-08-14T21:37:44Z** (opened
21:31:16Z, same shell, both from `date -u` and neither hand-written); repo HEAD
when this agent opened the repo **`1ff0036f`**, and **`52616930`** at completion
(the lane committed during drafting; no file this agent read was among the
changes it relies on).

**STATUS: committed BEFORE the `V140VS146` shard exists — before its worklist row
is stocked, before its `# FIXTURE … start=` header is written, and therefore
before its first game row.** Two-clock standard: clock 1 is this file's git
author time; clock 2 is the shard's own START stamp (`tools/overnight.sh:95`,
written *before* the first game rather than after it, so the certification can
say PREDATES-LEG-CREATION rather than predates-first-row). No row of this shard
exists at drafting time and none may be read before the lock commit lands.

**Authorization: `PROGRAMME.md:28` — `X3R0_SLOT_RULE: screen_n1000_reactivate_on_51`.
CODIFIED POLICY, not a per-instance announcement.** Magnus, verbatim,
2026-08-14, in-session: *"Whenever he outs one up, run n=1000 against it and put
ours back if we win."* `PROGRAMME.md:34-39` fixes the procedure: (1) stage his
artifact, (2) commit a `SCREEN-v14XvsY` prereg pre-row on the v142/v143 template,
(3) execute the branch the number picks. **No fresh announcement is owed before
the fire and no counter-roll is taken without the number — the screen IS the
resolution mechanism.** Magnus can countermand at any point before the
activation branch executes.

**Trigger:** x3r0 uploaded **v146 "Top3 Novel Router v1"** at ~**2026-08-14T21:23Z**
and it is **ACTIVE NOW** (read off the `Active bot:` line, never an exit code —
`fcode status` exits 0 while printing `Error: True` in a degraded response),
displacing our **v140 "Loki v10"**. Our rating moved **1775 → 1759** across the
displacement window (`elo_history.tsv`); ⚠ **that tape tags rows by the version
active AT POLL TIME, so those 16 points are CONTEXT, not attribution** — the
ground truth for per-match credit is `ourver` in `ladder_games.tsv` and this
screen does not read it.

---

## 0. REGISTRATION BLOCK

**TARGET BAND: N/A — LOCAL corefill screen with ZERO rated exposure. No submit,
no unrated challenge, no platform opponent, no prototype on the ladder, so
`tools/target_value.py` has no input to take and its reachable-band question does
not bind. The gate is ANSWERED by the absence of a target, not skipped: nothing
in these 1000 games can pay or cost a rating point. The one live action this
document authorises is a RE-activation of an existing platform version (§9),
which is not a prototype exposure.**
**PINNED: N/A — local screen; there is no platform opponent to pin. The local
analogue is honoured and is stronger than a pin: the control is a byte-frozen
artifact on disk (`bots/_x3r0v146`, md5 below), so x3r0's platform churn — three
displacements yesterday, a fourth today — cannot reach a single row of this
shard.**
**SURFACE: local**
**CLUSTER UNIT: none — enumeration performed in writing in §5; the MATCH cluster
dies (a local shard has no 5-game match wrapper) and the OPPONENT cluster is
degenerate (exactly one opponent for all 1000 rows, no between-opponent contrast
drawn). Applicable DEFF = 0.98, the measured local pair-weighted constant
(ρ = −0.020, 124 shards, s39 audit). ⛔ The platform constants 1.529 / 1.833 are
NOT imported — that would widen these intervals 24–35% for correlation that is
not there.**
**ESTIMATOR: pooled game share = treatment wins / (rows − NOWINNER rows), read off
`V140VS146.tsv` rows only, unweighted, one row = one game. No map weighting, no
seat weighting, no pooling with any other shard or surface.**
**DOSE: 56.80% pooled share against `bots/_x3r0v142` vs 49.20% against `bots/_x3r0v145` (n = 1000 each, same treatment tree, 2026-08-14) — ⛔ NOT a mechanism dose; a whole-bot screen has no mechanism to dose (§10, Ob. 13). What this line supplies is the both-ways instrument check the DOSE clause exists to demand: this exact screen design, same trees on our side, has returned two DIFFERENT verdicts against two different x3r0 artifacts, so it is not a constant column that validates whatever it is pointed at.**
**PLANNED n: 1000 games**
**BOUNDARY: 1000 shard rows = 1000 games (LOCAL surface: one `fcode run` is one
row and one game; the platform `games = 5 × accepts` identity has no accepts to
close on here and none is fabricated)**
**CUT-SHORT: below 700 games this screen publishes descriptive tallies only,
takes NO slot decision, and the sitter (v146) keeps the slot by default; the
runner keeps its rows on cancel, so a short shard is reported with its n and its
share and nothing is inferred from it**
**BAR: 53.1% pooled game share — the SUPERIORITY bar, i.e. the smallest reading
at n = 1000 whose 95% interval EXCLUDES parity (50.0). ⛔ THIS IS NOT THE DECISION
RULE AND MUST NOT BE READ AS ONE. The decision rule is Magnus's policy threshold
of 51.0% (§3), which is unresolvable at the mandated n and fires anyway because
it is a policy, not a significance bar. The two objects are kept apart on purpose
and the arithmetic checker consumes the only one it can close (§4).**
**BASE RATE: 50.0% — parity, the null of exchangeability between two bots in a
seat-balanced head-to-head mirror. It is the comparator by construction, not an
estimate.**
**BAR SOURCE: computed in this document, §4 — the boundary at which
`1.96·√(p(1−p)·DEFF/n)` no longer reaches 50.0 at n = 1000 (53.06 at the measured
DEFF 0.98; 53.10 at the conservative DEFF 1.00, which is the figure registered).**
**BASE RATE SOURCE: structural, not sampled — `tools/overnight.sh` plays every
(map, seed) cell in BOTH seat orders (`:135-136`), so under exchangeability the
expected share is exactly 50.0 with no tape to cite. Reference points from the
same fixture family, quoted for calibration and NOT used as the comparator:
56.80% [53.7, 59.9] against v142 and 57.06% [54.0, 60.1] against v143 (both
n≈1000, but on the WORKERS=40-on-16-core fixture the A4 retro showed FLATTERED
the lighter bot), and 49.20% [46.13, 52.27] against v145 (n=1000, clean
WORKERS=10 fixture, 2026-08-14).**
**REFERENCE n: none**
**POOL ERA: post-2026-08-13-rotation** *(the lane normalises the spelling
`POOL_ERA: post-2026-08-13-rotation`)*
**SPANS-POOL-CHANGE: no — the map pool here is the LOCAL 15-map list frozen at
`tools/overnight.sh:68` (antler archipelago auroraveil drakkarfjord drumlin
fjordgate frostgate glacierkeep icefloe midgard nordkap ragnarok royale valkyrie
yulerune), not the platform rotation; it does not move during the shard.**
**TREATMENT TREE: `bots/_v223sealrepair` (= v140 "Loki v10", 4 `.py` files,
4,757 lines, md5 of the concatenated sources `c4e563af4730b4c1595c679fc25098e7`).
CONTROL TREE: `bots/_x3r0v146` (= his v146, 85 `.py` files, 87,463 lines, md5
`ce20c8392520ab26736268c67ce0058f`). Both are as-built; NEITHER may be edited
while the shard fills.**
**MECHANISM METRIC READS: N/A by shape, stated rather than skipped — this is a
WHOLE-BOT screen with no treatment diff. Nothing is toggled, no constant is
swept, and the treatment tree is byte-identical to the one that played the v142,
v143 and v145 screens (md5 above). There is no diff for a read path to intersect,
so naming a `file:line` would be theatre. The outcome statistic is the shard's own
game share and nothing else. TREATMENT DIFF TOUCHES: NONE — zero files change in
either arm. INTERSECTION: N/A by construction — the LOKI-18 failure mode (a metric
that reads identically in both arms because the diff is elsewhere) cannot arise
where there is no diff and no mechanism metric; the arms are two DIFFERENT BOTS,
which is the arm label itself. ⛔ VOID CLAUSE: if any byte of either tree changes
before the shard completes, this screen is VOID and re-preregs — verify with the
two md5s above at read time.**
**GATE RESOLUTION: the 51.0% policy gate CANNOT resolve itself at the mandated
n = 1000 and this is stated before the fire, not discovered after it. The 95%
half-width is ±3.10pp (DEFF 1.00) / ±3.07pp (DEFF 0.98), so a reading of exactly
51.0 does not exclude 50.0. **The n at which a reading of exactly 51.0 would stop
touching 50.0 is ≈9,400 games** (half-width = 1.0pp), **and ≈19,200 games** for
80% power to detect a true 1pp edge; neither will be run. ⇒ EVERY reading inside
(46.9, 53.1) is UNRESOLVED as a measurement. **The unresolved default binds the
CLAIM, never the ACTION:** the policy branch in §3 executes exactly as Magnus
wrote it, because a codified threshold is an instruction and not an inference;
what defaults to the RESTRICTION is what we may SAY — an UNRESOLVED reading
grants no superiority claim in either direction, and the write-up must say "the
gate could not separate these bots" rather than "v140 measured better" or "v146
measured better". The RESTRICTION is defined as *nobody claims a bot is better*,
never as a numeric branch.**
**PRE-STATE: the predicted-change set is NOT already in the target state at lock,
and this is verified rather than asserted — `bots/_x3r0v146` was staged today and
has never been played by any shard: `grep -c V140VS146 scratchpad/corefill.log`
= 0, no `V140VS146` row exists in `scratchpad/corefill_work.txt`, and no
`V140VS146*.tsv` exists on disk. The slot state at lock is v146 ACTIVE and v140
DISPLACED, i.e. the outcome this screen can trigger (v140 reactivated) is by
construction not pre-satisfied.**
**MAP SEGMENT: the 12 (map, seat) cells x3r0's `main.py` routes to a hand-picked
NOVEL policy — `COUNTER_ROUTES` {(antler,B), (archipelago,B), (fjordgate,B),
(icefloe,A), (nordkap,B)}, `DIRTY_ROUTES` {(drumlin,B), (glacierkeep,B),
(nordkap,A)}, `RUSH_ROUTES` {(drakkarfjord,A), (drakkarfjord,B), (ragnarok,A)},
`J3_ROUTES` {(yulerune,A)} — versus the other 18 cells, which fall through to a
`p{NN}` salt router that (by his own module header) differs from its siblings
"only in spawn-order salt". MECHANISM: his README says the bot is a "map/side
mixture … selected from a 504-game tournament against three top-team replay
surrogates", and those 12 cells are exactly where the selection put a
qualitatively different policy rather than a salt permutation, so his tuning
effort is concentrated on a set nameable in advance from his own artifact
(`bots/_x3r0v146/main.py:71-84`).**
**EXPECTED DIRECTION: our share is LOWER on the 12 novel-policy cells than on the
18 salt-router cells.**
**SEGMENT VALUE CEILING: 40.0% x 6.0pp on-segment = 2.40pp pooled** — 12 of 30
(map, seat) cells is 40% of a seat-balanced shard, so even a 6pp differential on
the segment moves the pooled share by 2.4pp, **below the ±3.10pp half-width.**
⇒ **the segment can neither rescue nor explain a pooled miss, and no slot branch
may cite it.**
**CELL VERSION CHURN: N/A — local fixture, no panel cells. The control is a
byte-frozen tree in this repo (md5 above); it cannot ship a new version mid-leg,
so Obligation 14's 24-hour distinct-version count has no local analogue and no
substitute is faked.**

---

## 1. HYPOTHESIS — one sentence, and it can lose

**Our v140 (`bots/_v223sealrepair`) takes at least 51.0% of 1000 seat-balanced
local games against x3r0's v146 (`bots/_x3r0v146`) on the 15-map pool — i.e. it
is the version the team should have on the slot under `X3R0_SLOT_RULE`.**

---

## 2. THE ARMS

| arm | tree | version | files | lines | md5 (concatenated `*.py`) |
|---|---|---|---|---|---|
| **TREATMENT** | `bots/_v223sealrepair` | v140 "Loki v10" (ours, displaced) | 4 | 4,757 | `c4e563af4730b4c1595c679fc25098e7` |
| **CONTROL** | `bots/_x3r0v146` | v146 "Top3 Novel Router v1" (his, sitting) | 85 | 87,463 | `ce20c8392520ab26736268c67ce0058f` |

**BASENAME COLLISION — CHECKED, NOT ASSUMED.** `tools/overnight.sh:78` scores by
**substring** match on the tree basename and REFUSES a colliding pair up front
(`corefill.sh` guard 4's worked example: *"`_v150cb` vs `_v150cbturret` reads
~100% for the control"*). Checked both directions: `_v223sealrepair` is not a
substring of `_x3r0v146` and `_x3r0v146` is not a substring of `_v223sealrepair`;
they share no prefix. **The pair is scorable.**

**WHAT HIS BOT IS, read off his own artifact and not inferred:** `main.py`
fingerprints the map from public terrain (dimensions + the two core positions,
falling back to a decoded grid for four colliding signatures), derives its own
side, and dispatches once at first `run()` to one of four hand-picked policies on
12 named (map, seat) cells or to one of seven `p{NN}` salt routers otherwise.
**`SIGNATURES`/`OPENINGS`/`A_CORES` name 14 maps; `auroraveil` is in our pool and
in none of his tables**, so on auroraveil his label resolves to `None`, his side
resolves to "B" unconditionally, and he falls through to the `"92"` default
router — a blind fallback on 1 of 15 maps. **Descriptive only** (~67 rows,
half-width ≈ ±12pp: it cannot carry a claim), named here so its later appearance
is not mistaken for a discovery. **Per his `main.py` docstring the router "never
reads opponent identity", and nothing in this screen alleges otherwise — this is
not a norms question.**

**⚠ HIS README'S NUMBERS ARE HIS, ON HIS FIXTURE, AND THIS SCREEN DOES NOT
CONSUME THEM.** "354/420 (84.29%)" held out against three replay-derived
surrogates is not a prior, not a bar, and not a comparator here; it is quoted
once so its absence downstream is not read as an oversight.

---

## 3. DECISION RULE — registered now, before the first row

**The registered rule is `X3R0_SLOT_RULE` in the v142/v143/v145 form, restated in
full rather than incorporated by reference:**

* our pooled game share **≥ 51.0% at n = 1000 — equivalently ≥ 510 of 1000
  scored games** → **REACTIVATE v140.** This is an existing platform version: a
  re-activation (`fcode submission activate`), **not** a submit. ⛔ `fcode submit`
  AUTO-ACTIVATES and would re-upload; that is not this action. Executed inside a
  safe window **just after an observed ladder pairing** and verified on the
  **`Active bot:` line** of `fcode status`, never on `$?` (§9).
* our pooled game share **< 51.0% — equivalently ≤ 509 of 1000** → **v146 stays.**
  Result reported to Magnus, no platform action taken.
* **The RESTRICTION — the branch an unresolved gate defaults to — is "nobody
  moves: the sitter keeps the slot".** In this instance the sitter is v146, so
  the restriction and the `< 51.0` branch coincide. **That is a definition, not a
  numeric coincidence, and it is written this way deliberately:** the v145 screen
  had to strike its own Ob-12 sentence mid-leg when the sitter changed and
  "restriction = the `< 51.0` branch" silently inverted.
* **400-CATA futility gate applies on the way** (abort if our share < 40% at
  n = 400). This shard's own read is the n = 1000 look; there is no 2700 or final
  band at target 1000, and **no extension is registered** — if the lane later
  wants one it is a new, separately-locked document.
* **No pooling with any other surface or shard. This shard alone decides.**

**⛔ WHAT "< 51.0" DOES NOT MEAN.** A reading below 51.0 does **NOT** establish
that his bot is better than ours. At n = 1000 every reading in (46.9, 53.1) is
statistically indistinguishable from parity (§4), and the rule's threshold sits
inside that band. **A sub-51 result means only: "our bot did not clear the
policy threshold that would have bought back the slot."** It is not evidence of
v146's superiority, it may not be quoted as such, and it does not by itself
oblige us to keep his bot up — the rule governs OUR counter-roll, and any
decision to leave v146 sitting is Magnus's, not this screen's.

**Ties-to-sitter, and it is registered in advance so a 50.x read is not
relitigated:** at true equality v140 reclaims the slot only ~26% of the time
(1 − Φ(0.63); 1 SE = 1.57pp at n = 1000). **Ties go to the sitting v146 by
about 3:1.** That is the plain reading of *"put ours back if we win"* and is
accepted before the fire.

---

## 4. n, THE HALF-WIDTH, AND THE TENSION STATED PLAINLY

**n = 1000 games. 95% half-width at p ≈ 0.5: ±3.07pp at the measured local
DEFF 0.98, ±3.10pp at DEFF 1.00.** (For scale, and NOT registered: n = 2700 →
±1.89pp; n = 5400 → ±1.33pp.)

**⚠ THE DECISION RULE IS A 51.0% THRESHOLD AND THE HALF-WIDTH IS ±3.10pp, SO A
READING OF 51.0 DOES NOT EXCLUDE 50.0.** The gate cannot discriminate its own
branches near the boundary; a coin flip lands above 51.0 about 26% of the time.
**This is disclosed, not discovered later.**

**AND IT IS NOT A DEFECT TO BE FIXED HERE.** Magnus fixed both the n and the
threshold in `X3R0_SLOT_RULE`. **This is a policy-mandated decision threshold,
not a significance bar**, and the screen's job is to execute it, not to
relitigate it. What the document does instead is register, in advance, exactly
what each reading is entitled to claim:

| reading | slot action (policy) | claim entitlement (measurement) |
|---|---|---|
| **≥ 53.1** | reactivate v140 | v140 is better than v146 on this fixture; CI excludes parity |
| **51.0 – 53.1** | reactivate v140 | **NONE.** "cleared the policy threshold"; the gate could not separate the bots |
| **46.9 – 51.0** | v146 stays | **NONE.** "did not clear the policy threshold"; the gate could not separate the bots |
| **≤ 46.9** | v146 stays | v146 is better than v140 on this fixture; CI excludes parity |

**Every verdict sentence from this screen carries the CI beside the point
estimate — a bare share is not a compliant read-out — and states whether the
interval excludes 50.0 and whether it excludes 51.0.** Those two exclusions come
apart in the middle two rows, which is the whole reason the table exists.

---

## 5. CLUSTER ENUMERATION AND DEFF (CLAUDE.md scope procedure, performed in writing)

1. **MATCH cluster — DIES.** A local shard has no 5-game match wrapper; every row
   is an independently seeded single game written by `tools/overnight.sh:155`. No
   stratum can hold two games from one match because no match object exists.
2. **OPPONENT cluster — degenerate.** Exactly one opponent (`_x3r0v146`) for all
   1000 rows and no between-opponent contrast is drawn, so there is no
   multi-member opponent stratum to inflate.

⇒ **Applicable DEFF = 0.98** (measured pair-weighted on this exact fixture class:
ρ = −0.020 across 124 shards, s39 audit) — naive intervals are correct and
marginally conservative. **The registered half-width uses DEFF 1.00, the more
conservative of the two.**

⚠ **Where this could bite, stated rather than buried:** the s39 audit found local
arms with strong map interaction reaching DEFF ≈ 1.25, and v146 is explicitly
map-and-side-conditional. That is a reason to read the §0 segment as
**indicative**, and it is why the segment can never rescue a pooled read (its
ceiling is 2.40pp against a ±3.10pp interval).

---

## 6. R1000 IS A DEFEAT — the turns read, registered

`PROGRAMME.md`: `R1000_IS_DEFEAT: yes`, unconditional. **A round-1000 game is a
defeat even when we win it.**

* **The slot decision counts a tiebreak win as a WIN**, because the ladder pays
  game share and a tiebreak win is a won game there. Excluding them would measure
  something other than the quantity the slot decision is about.
* **But no tiebreak win may ever be banked or reported as a kill.** The screen
  therefore reports, alongside the share, **the count of rows with `turns == 1000`
  for each side** — ⛔ **read off the `turns` column, NEVER off the `cond` string**
  (`tools/overnight.sh:155` writes both; `cond` reports a tiebreak key, not the
  round, and a `core_destroyed` at r1000 is not what the field says).
* Median kill round per arm is **descriptive** here. No kill-round bar is
  registered and **no `DEFENCE_ADMISSION_BAR` rider applies**: nothing in this
  screen is a plank proposed for the tree — both arms are as-built bots.

---

## 7. FIXTURE, WORKLIST ROW, SEEDS

**LOCAL corefill on this box** (`sysctl -n hw.ncpu` = **10**), engine
`.venv/bin/fcode`, `--tle 10`, full 15-map pool from `tools/overnight.sh:68`,
both seat orders on every (map, seed) cell (`:135-136`) — **seat-balanced by
construction**, which is what makes 50.0 the structural base rate.

**Proposed worklist row** (the lane stocks it; this agent does not touch
`scratchpad/corefill_work.txt`):

```
V140VS146   bots/_v223sealrepair   bots/_x3r0v146   1000 310000
```

**SEED-BASE COLLISION CHECK, performed rather than assumed.** `overnight.sh:121`
advances the seed once per 16 games, so a 1000-game shard consumes **seeds
310000–310062** and the largest live base in `scratchpad/corefill_work.txt` is
**308000** (SEALFLOOR6, 5400 games ⇒ 308000–308338). **310000 is clear of every
stocked row** (the series runs on a 2000 stride: …296000 V140VS142, 298000
V140VS143, 300000, 302000, 304000/306000 V140VS145/B, 308000 SEALFLOOR6) **and
clear of the CRASH* block at 93000–93300.**

⚠ **Two fixture facts that belong on the page rather than in a reader's head:**
* **1000 is not a multiple of 30** (15 maps × 2 seats), so the final partial cycle
  over-weights the first ~5 maps in `MAPS` order by one game per seat — **≤1% of
  rows**, reported with the per-map split and not corrected for.
* **`overnight.sh:121`'s resume arithmetic is `SEEDLO + n/16` while a full
  map×seat pass is now 30 games**, so a RESUMED shard advances its seed faster
  than it consumes cells. It stays comfortably inside the 2000-wide block (62
  seeds at most), so there is **no collision risk**; it is noted because "seeds
  continuing the same partition" is a sentence this project writes often and here
  it is only true up to that stride.

---

## 8. INTERACTION WITH THE LIVE LOCAL FIXTURE — THIS SCREEN SHARES CORES

**In flight on the same box at lock:** `SEALFLOOR6` (`bots/_v238sealfloor6` vs
`bots/_v223sealrepair`, ~1000 of 5400 rows, mid-leg) and the four `CRASHP` /
`CRASHG` / `CRASHZ` / `CRASHS` cells (60 rows each, SCREEN-crashdrive).

**WHAT IT DOES NOT AFFECT:** no shared row, no shared shard id, no shared seed
block, no pooling. `bots/_v223sealrepair` appears in three of these legs — as
SEALFLOOR6's CONTROL, as CRASHS's thrower, and as this screen's TREATMENT — and
**no row generated by any of them may be read into any other's tally.** This
shard's denominator is its own 1000 rows and nothing else.

**WHAT IT DOES AFFECT, and it is the one that has already voided a leg:** CPU
contention. `--tle 10` is **WALL-CLOCK** enforced, so a contended box kills turns
and the damage lands on the **compute-heavier** arm. Here the heavier arm is the
**CONTROL** by **18.4×** (87,463 lines vs 4,757), **so any oversubscription biases
this screen IN OUR FAVOUR — toward reactivating our own bot.** That is the
flattering direction, which is exactly why the V140VS145 leg voided 480 rows when
it discovered it had run 40 workers on 16 cores (its A4 amendment).

⇒ **REGISTERED FIXTURE CONDITION, pre-committed:** the shard must fill while the
concurrent shard count on this box stays **≤ ncpu = 10** (`corefill.sh` launches
one worker per shard and defaults `MAX_SHARDS=8`, so the default satisfies this;
the count includes SEALFLOOR6 and the CRASH cells). **The shard's own
`# FIXTURE … workers=` header records the number.** If the header or the box
state shows the shard filled oversubscribed, **those rows are VOID for the
decision** — kept on disk as a labelled artifact, never read into the tally.
Voiding in the flattering direction is the whole point of writing this down
before the fire.

⚠ **EXTERNAL VALIDITY, carried verbatim in effect from the V140VS145 screen and
declared blind here so it constrains BOTH branches equally:** this is a
**LOCAL-fixture head-to-head on an 18.4× compute asymmetry with a wall-clock turn
budget**, and local and platform TLE behaviour are not calibrated against each
other. It is external validity, not bias. **And the mirror is not the ladder:**
the slot pays on FIELD earnings, the two come apart under non-transitivity, and a
per-(map, side) router is a shape built to be non-transitive. The mirror decides
today's slot because Magnus codified it as the resolution mechanism, not because
head-to-head and field are the same quantity.

---

## 9. THE REACTIVATION, IF THE ≥ 51.0 BRANCH FIRES — a platform action with a verification step

1. **Observe a ladder pairing** on `fcode match list --mine --type ladder`, then
   act **just after** it. Pairings cluster on a clock (historically minute ≡ 12
   mod 20 at second `:59`), **but the offset has shifted at least once — re-derive
   it from recent rows, never hardcode it.**
2. **`fcode submission activate` on the existing v140 submission.** ⛔ **NOT
   `fcode submit`** — submitting auto-activates a fresh upload and is a different,
   heavier action. No new artifact is uploaded by this branch.
3. **VERIFY on the `Active bot:` line** of `fcode status` (equivalently the
   presence of `active_submission` in the JSON body). ⛔ **Never gate on `$?`:**
   this CLI exits 0 while printing `Error: True` and exits 1 on `match list` in
   the same outage. **The match counter cannot verify this either** — it is blind
   to a match PAIRED but not yet completed.
4. **Report the verified holder line and the shard's number to Magnus.**

If the `< 51.0` branch fires, **no platform action is taken at all** and the
report is the number, its CI, and the two exclusion statements from §4.

---

## 10. OBLIGATIONS REGISTER (`docs/research/PREREG-amendments-and-lock-obligations-2026-08-09.md`)

* **Ob. 7 (outcome as IN-OUR-FAVOUR, and not pre-satisfied):** satisfied — the
  outcome is **game share in our favour**, not a win-condition mix; §0 `PRE-STATE`
  verifies the target state is not already held (v146 sits, v140 is displaced,
  zero rows of this pairing exist).
* **Ob. 8 (denominator rule):** one opponent, one fixture, one shard; the
  denominator is 1000 local rows and is pooled with nothing. `meta_join` and
  `ladder_games.tsv` are not consulted for any number on this page.
* **Ob. 12 (a gate is a bar and must be sized like one):** satisfied in §0
  `GATE RESOLUTION` and §4, including the pre-committed treatment of the
  UNRESOLVED case and the definition of the restriction as a state ("nobody
  moves"), not a numeric branch.
* **Ob. 13 (`MECHANISM METRIC READS: file:line`):** **N/A by shape, declared
  rather than skipped** — whole-bot screen, zero-byte diff in both arms, no
  mechanism metric exists. The VOID clause in §0 is what replaces it: the arms'
  md5s are recorded and re-checked at read time.
* **Ob. 14 (opponent version stability):** satisfied better than the obligation
  asks — the control is a **byte-frozen local artifact**, not a live opponent.
  x3r0's churn (v142, v143, v145, now v146) is real and is precisely why the
  screen is local.
* **Ob. 15 (map dependence):** satisfied in §0 with **exactly one** primary
  segment, a mechanism read off his own `main.py`, a signed expected direction,
  and a value ceiling that closes arithmetically. **Every other cut — per-map,
  per-seat, per-size-class, the auroraveil blind-fallback cell — is DESCRIPTIVE
  and may not be used to rescue a pooled fail.** ⭐ **15c applies:** if the pooled
  read misses and the segment clears in the predicted direction, that is a
  candidate for a NEW screen with its own n, never a re-read of these rows.
  ⚠ **And the confound is named in advance, because the V140VS145 screen's
  segment prediction FAILED and REVERSED:** our own weakest maps (archipelago,
  glacierkeep, fjordgate read 21%/30%/32% in that shard) are not evenly spread
  across his route sets, so "his novel policy is strong here" and "we are weak
  here against anyone" fit the same rows. **A segment claim buys its own screen.**
* **Ob. 1–4, 6, 9–11:** Ouroboros- or mechanism-leg-specific; they do not
  instantiate on a whole-bot screen.
* **Hot-turn rider: N/A — no code change.** Both trees are as-built and neither
  gains a loop, a map scan or a per-tile operation from this document.

---

## FALSIFIER

**The hypothesis is falsified by a pooled share below 51.0% — ≤ 509 of 1000
scored `V140VS146.tsv` rows — at the registered n, on a fixture whose worker
count satisfied §8.** That closes the counter-roll: v146 keeps the slot, no
activation is attempted, and the number is reported to Magnus with its CI.

**It is falsified HARD, with a claim attached, at ≤ 46.9%:** there the interval
excludes parity and the honest sentence becomes *"a per-(map, side) router built
on replay surrogates beats our current line head-to-head on our own fixture"* —
which would immediately promote the map-conditional road
(`LEG-mapconditional-test-2026-08-14.md`) rather than close it.

**What would NOT falsify it, stated so it is not offered later:** a segment
result in the predicted direction with a pooled miss (ceiling 2.40pp, §0); a
per-map or per-seat cut; a favourable kill-round median; anything read off a
voided (oversubscribed) row set; or his README's own 84.29%.

**And the symmetric trap, registered because the design's most probable terminal
state is a reading inside (46.9, 53.1): a pooled number that clears 51.0 without
excluding 50.0 buys the SLOT and buys NO CLAIM.** The write-up says *"v140
cleared the policy threshold; the gate could not separate these bots"* — never
*"v140 measured better"*.

---

## PROVENANCE

**Drafted by a fresh opus subagent with NO inherited session context. Its ENTIRE
input set was the lane's task brief plus the files below, all read-only. No
outcome column of any shard, live or historical, was read by this agent, and no
game was run by it.**

```
PROVENANCE: PROGRAMME.md (X3R0_SLOT_RULE block, :28-44)
            docs/prereg/SCREEN-v140vs142-2026-08-14.md
            docs/prereg/SCREEN-v140vs143-2026-08-14.md
            docs/prereg/SCREEN-v140vs145-2026-08-14.md
            docs/prereg/SCREEN-crashdrive-2026-08-14.md (house style + interaction facts)
            docs/prereg/SCREEN-seatscan-2026-08-14.md (registration-block style)
            docs/research/PREREG-amendments-and-lock-obligations-2026-08-09.md
            tools/prereg_check.py
            tools/overnight.sh
            tools/corefill.sh
            bots/_x3r0v146/README.md
            bots/_x3r0v146/main.py
            scratchpad/corefill_work.txt (seed bases only)
            scratchpad/vps/worker@work-server-1/worklist.txt
            CLAUDE.md
```

Plus three read-only shell facts taken in the drafting shell: `date -u`
(2026-08-14T21:31:16Z .. 21:37:44Z), `sysctl -n hw.ncpu` (10), and `md5` over each arm's
concatenated `*.py` (both recorded in §2). **No mutating `fcode` command was run
by this agent — no submit, no activate, no match.**
