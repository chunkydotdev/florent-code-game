# SCREEN PREREG — V141VS140: x3r0's Counter Router vs our live v140 (benchmark)

**Committed BEFORE the shard's first heartbeat.** Builder s38, 2026-08-14,
answering x3r0's claim (relayed by Magnus): v141 beats an "old" bot at
81.7%. That figure = v141's README confirmation run, 196-44 vs his local
`multisalt_v2` — echo-loop fixture class, missing the current-bot control.
This shard IS that control: `bots/_x3r0v141` (staged verbatim from the
platform artifact) vs `bots/_v223sealrepair` (v140, live), n=2700
(benchmark sizing, band ±1.88), full 15-map POST-patch pool (pool26
runner), seed_lo 278000, futility gates apply. Per-map splits reported
(v141 routes by terrain: weak maps = its v134 chassis; also its stale
valkyrie tables vs our corrected ones should show there). Descriptive
benchmark; no ship inference either direction; slot calls remain
Magnus's/x3r0's. MAPSALT (his salt idea on OUR chassis) remains the
honest test of the reusable ingredient.

## Target-value line
Local screen, zero live exposure ⇒ payout gate N/A.
