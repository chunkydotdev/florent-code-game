# SCREEN PREREG — x3r0's v134 retune at proper n (courtesy battery)

**Committed before the shard's first heartbeat.** Builder s37, 2026-08-14.
Context: x3r0's README claims 53.7% at n=600 vs baseline (p≈0.07, cannot
exclude a coin flip; Magnus: "that's a lot less than we play"). His tree
(v125 + 17 constants, chassis byte-identical) gets the standard battery
free: `X3R0V134` = `bots/_x3r0_v134` (platform artifact, CRLF-normalized)
vs `_v197mapcode`, n=5400, seed base 246000, futility gates, D26 declared
(replicate iff |share−50| ≥ 2.0pp, 247000, scored alone). Kill-round
paired-seed reported.

Coupling note: the retune is population/scale economics on an identical
chassis — the self-play mirror prices exactly the cost-scale arithmetic the
v134 analysis flags (the −13pt precedent was measured in this same fixture
class), so this screen is evidentially FAIR for the economics question. The
analysis's nine wire predictions ride separately on his rated games.
Readout goes to x3r0 + Magnus as information; the slot remains their call.
