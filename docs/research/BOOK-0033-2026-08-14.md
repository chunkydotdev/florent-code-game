# 0033 — OPPONENT BOOK (research s39, 2026-08-14 ~12:4xZ)

Magnus-flagged ("they seem to beat us a lot"). Opus agent, zero replay
downloads (archive held 2,307 games), ~40 scripts/outputs in the session
scratchpad (prof.py, era2.py, nest.py, determinism outputs, reecon.py).
h2h corrections verified by research at ladder_games.tsv before banking.

## The headline
**This is a MATCHUP problem, not a strength problem.** They are Elo-fair vs
the field (+2.2pp over expectation, v56+57, 55 matches) and **−24pp against
US** (our share 20.0% vs 44.0% expected, v56+57 rated). We beat their v42
at +22pp OVER expectation. Their 1850 is a steady climb, not a Bisons-style
understatement. **Corrected h2h: v43+ era = 24/80 = 30.0% us** (an earlier
in-session relay said 24/95 — arithmetic error, retracted).

## (a) What v43 changed (shipped 2026-08-08T01:32-01:42Z)
Neither side changed globally (us vs field −0.4pp; them vs field +0.2pp).
What changed, length-controlled at r≤50 vs us:
- **HEAL ×21** (5.16 → 110.45 heals/100 rounds; median 2 → 224/game) — the
  heal wall. Our era win mechanism was insertion melee: **v42, raider
  reaches their core → 12/12 wins; v43 → 4/10** despite our core damage
  RISING. INFERENCE: +4 HP/1 Ti vs our 2 dmg/2 Ti is a 4:1 exchange.
- Forward turrets CUT (1.60 → 0.24/game by r50) — they pulled back and
  built economy (harvesters 2.4→3.7, conveyors 15→22) + barriers.
- **The maturing plank: a CORE-RING NEST** — structures inside d²≤8 of OUR
  core: 0/20 games (v42) → 93-97% of games (v54+), 4.3 barriers + ~1
  gunner per game, first structure median r43 (p10 r13). Geometry: 93.7%
  forward, median d²=4 to our core. Opponent-blind (72-100% of games vs
  every field team).
- **Their core-kill is 100% turret fire** — batk_core = 0 in 246/246
  archived vs-us games, every era (our control: 36-56, 12/10 nonzero).

## (b) Current shape (v56/v57)
- **Gunner creep, not sentinel mass**: gunners planted to d²6-16 of our
  core r50-150 behind the barrier screen; sentinels mainly the r0-50
  opener at d²≈31. Kill at **median r154** (vs Bisons r71).
- **Strongest opening determinism measured in the league**: 100% identical
  first 4 builds, 99% first 8, 87% first 14 across up to 14 opponents per
  (map, core) cell (368 games; opponents' own control 47%). Nothing we do
  before ~r8-10 changes their script. ⚠ The RING is reactive (39% modal
  tiles) — pre-emption by hardcoded tile will not work.
- Cadence ~15-27h/version; they ROLL BACK (v30→29, v47→45→47) — they
  iterate with controls, like us.
- **Field per-map**: holes at auroraveil 11%, icefloe 15%, nordkap 28%,
  drumlin 30%; strengths fjordgate 73%, drakkarfjord 64%, glacierkeep 62%.
  **Inversion vs us: they beat US 100% on royale/fjordgate/auroraveil/
  frostgate/ragnarok — including auroraveil, the map the field beats them
  on 89%** (per-cell n=2-12; the PATTERN is the finding, INFERENCE).
- Crash-induction: 0/371 candidates (instrument live: field opponents 249
  in same files). CPU: true cpu_max 6-8.4ms of 10 — no TLE lever either.

## (c) Pairing book
- Losses (81 archived, mixed-fixture caveat): they out-collect us from r50
  (280 vs 200; r100 790 vs 415), shoot our BELT apart (7 conveyor deaths
  by r150 vs 3.5 in wins), our ammo conversion halves, they out-shoot 79
  to 33 and take the core r154. Our builder production identical in wins
  and losses — belt attrition + economy race, NOT spawn starvation.
- **The removal counter REPLICATES**: their fwd-turret removal 51% in
  field games they LOSE vs our 14% in our losses (field opponents: ph 67%,
  Flotte 68%, Bean counters 55% vs our 16%). **But the #3 arm must widen:
  priority set needs (i) short-range gunners inside our ring, (ii) the
  barrier screen (we remove 8-14% of it) — not only the sentinel band.**
- **Out-racing is LIVE vs 0033** (unlike Bisons): r154 clock; field
  winners are simply ahead on Ti at r50 and out-shooting 3:1 by r100.
- **Kidnap uncontested: 0 launchers in 2,307 archived games** (opponents
  built 751 in the same files). Their damage requires an unescorted
  melee-less builder standing in our spawn ring from median r43 (p10 13),
  on a script knowable to build #14. → #45's second named customer.

## ⛔ INSTRUMENT FINDING (outranks the book): econ.tsv silently corrupt
30,085/225,200 rows (5,455 recently-decoded files): turns_run=0,
cpu_sum=0, `ti_collected_end` WRONG (738/742 probed cells; corpus 10 vs
true 240), cpu_max understated, and ~~**`tled` is fiction — the wire flag
does not exist in platform replays** (0 of thousands of botOutput events)~~.
⛔⛔ **RETRACTED 2026-08-14 (research s40). THE `tled` CLAUSE IS FALSE AND IT
RETIRED A LIVE INSTRUMENT.** What is corrupt is `econ.tsv`'s tled COLUMN on
recently-decoded files — the same corruption that zeroes `turns_run` and
`cpu_sum` — **not the wire field.** Falsified three independent ways:
(1) `tools/corpus/replay_econ.py:126` reads tled from **botOutput field 4**, a
varint DISTINCT from field 3 (`execTimeUs`), so the two are separate wire
fields; (2) a prior research census consumed it at scale — **"Ouroboros
discards 26,356 unit-turns across 85 games"** (coordination.md:10630), the
measurement that settled the `official-docs.md:1091` "disqualified" vs
`CLAUDE.md` "interrupted" contradiction, and 26,356 events cannot come from a
field that does not exist; (3) the Juusto book's independent re-read finds
**446 tled turns against 449 independently-computed `execTimeUs > 10,000`** —
agreement to 0.7% between two separate fields, which is not a coincidence.
**Reading zeros in a corrupt COLUMN and concluding the WIRE FIELD is absent is
the same class of error as this book's own econ finding, committed in the
sentence that reports it.** ⇒ `tled` is usable; read it from the replay, not
from `econ.tsv`.
≤v54-era files clean; build_agg.tsv unaffected (741/742 verified).
Re-decoder with 30/30 positive controls: scratchpad reecon.py. **Routed to
the builder (decoder owner). Until fixed: no economy read from econ.tsv on
recent files; any recent read denominated in it carries a caveat.**

## Routing
#3 (arm widened: ring gunners + barrier screen in the priority set) ·
#45 (0033 as second named kidnap customer) · econ trap → builder + tape ·
h2h correction retracted on tape · Juusto book (Magnus, next session)
should test whether the three-shape family (Bisons sentinel-mass /
Leviathan sentinel-forward / 0033 gunner-creep-plus-heal) covers them too.
