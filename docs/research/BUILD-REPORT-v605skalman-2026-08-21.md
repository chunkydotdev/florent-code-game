# BUILD REPORT + READ — `bots/_v605skalman` (the exit-guard release)

**Builder s54, 2026-08-21 ~22:0xZ (corrected: the draft header hand-wrote a rolled-over date; date -u at commit read 2026-08-21T22:08Z — the timestamps rule's 5th recorded instance, self-caught).** Copy of `_v604skalman` + the v605 queue; fresh opus
agent; artifacts `scratchpad/s54_v605/`. Outcome column builder-verified on the shipped
K=6 tape (12 wins = 11 core-kills + 1 tiebreak; by-r300 = 10; median kill r209 — matches).
Fixture = the authored NOISE_OFF benchmark copy; no game-share claim, no submit.

## THE HEADLINE: one line carried the release
**SK_NEST_EXIT** — `_prep_barrier`'s self-trap guard reserved an exit against the barrier
being placed but NOT against the nest site (an orthogonal neighbour by construction,
filled by the sentinel one round later). In 9/30 games the engineer walled itself in from
S1 until death (1,059 idle rounds; those 9 = 9 of the 13 no-second-sentinel games). The
widened guard (`free_neighbours(exclude=(q, site))`):
**kills 9→11 · by-r300 6→10 · median kill r275→r208 · ≥2-sentinel games 17→24 · our
deaths 20→18.** Single-source proven: the nestexit-off tape is byte-identical to v604's
in all 30 replays.

## Diagnose-first items — both paid
* **Second-sentinel decomposition (n=30, bank ledger reconciled to 0 residual):** born r3
  → band-adjacent r21 (travel) → prep → **first plant r31.5, funding wait ZERO in 29/30**
  → S2 median gap 74 rounds (the walled-in class). ⛔ TWO PREMISE CORRECTIONS: we beat
  the benchmark-donor on the FIRST sentinel (our r32 vs their r37 median — "r21-58" was
  their spread); their real edge is the SECOND gun (r63 vs our r95), from NOT TRAVELLING.
* **M1/belt-death regression (v604's): CAUSE = SK_DANGER_COST grows the belt INTO the
  enemy turret band** — 9 pieces at d²≤32 of their core where v603 lays zero, 6/9
  destroyed (marginal death rate 32.4% vs 8.9%); carried by 2/30 games; candidates (b)
  and (c) falsified with numbers. NOT FIXED (the cure is the flag that unsticks the
  throat; K does not separate). v605 partially recovers M1 anyway: 34.9/27.3.

## Nulls and negatives, all on the record
* **SK_PATH_ARBITER: EXACT NULL that REFUTES v604's self-blockade finding** — the gate
  ran 250×, refused 0; helheim's core→ring distance is 13 before AND after the entire
  nest cluster (the southern ore corridor is open all game). **The real class-A
  mechanism: the flood has NO BUILDING MEMORY — a barrier visible at d²16 vanishes at
  d²25, so two floods one step apart run on different maps** (155+154 rounds oscillating
  on adjacent tiles). Code stays in-tree, OFF, scanned.
* **SK_BLOCK_MEMO (the fix for that): built, mechanism confirmed, outcome inverted —
  shipped OFF** (9/7/224/76-deaths vs 11/10/208/51). Fourth
  mechanism-works-outcome-loses case in this line; hypothesis filed, not claimed.
* **SK_DANGER_K sweep: K=6 wins outright on by-r300** (10 vs 8 vs 8); the earlier sweep
  on the memo chassis tied — the winner is chassis-dependent, re-run not reused.

## Verification
Static 7/7 + 10/10 dirty controls (3 new) · aliveness 12 games 0 tracebacks/0
exception-removals with firing control · cells: midgard class-B 2r PASS, class-C PASS,
fimbulwinter flat-to-better, helheim_A cell stays 1/12 (open) but the GAME is won r189 ·
fidelity per-seat (seats split into manifests — a both-seat dir under one --side
mis-attributes): lattice 100/100, M4 band 96.4/96.8, point-blank 0/0, M1 34.9/27.3 ·
instruments reproduce v603/v604's quoted numbers exactly · CPU wall-clock flat; platform
match test STILL OWED. Disclosed: an early ablation race produced a corrupt 26/30 tape —
killed, re-run single-writer; all tables from clean runs.

## v606 queue
1. Builder-death jump v603→v604 (17→50, flat at 51) — UNEXPLAINED, diagnose. 2. The
belt-into-turret-band growth (2/30 games). 3. The block-memo hypothesis (a remembered
blocker is a commitment — dead-building expiry?). 4. paths_seatA walking livelock. 5.
fimbulwinter long orbits (pre-date v605). 6. ALLOCATION DOCTRINE (flagged for the
builder, not a constant): the second gun costs ~80 Ti at 268% cost scale largely
self-inflicted (4 builders +80pts, 2 door gunners +40) — whether Skalman buys fewer
door gunners or accepts the scale is a design call. Standing: platform CPU test owed.

## Progression on the fixture (identical opponent, 30 games)
kills 0→0→6→8→9→**11** · by-r300 —→—→5→6→6→**10** · median kill —→—→198→256→275→**208**.
FIRST-CONTACT GATE: **NOT MET** (11/30 does not beat the screen), but by-r300 is now the
closing axis: r180 target vs r208 median, 28 rounds.
