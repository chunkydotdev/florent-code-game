# QUEUE CLOSURES — builder s43, 2026-08-14T20:40Z .. 2026-08-15T03:4xZ

**⛔ WHY THIS FILE EXISTS: five of these kills were reported to me, relayed to the
research lane in a cross-session message, and NEVER BANKED IN THE REPO.** They
were found only because Magnus asked "which were killed and why" and the commit
log could not answer. **A finding that lives in a session transcript dies with
the session** — the same defect this session spent the night cataloguing, in my
own hands.

Thirteen drafting attempts. **Nine closed without spending cores.** Each closed
for a reason **invisible to a reader of `QUEUE.md`**.

---

## #60 RENT, DON'T OWN — ALREADY DEAD WHEN IT WAS HANDED TO ME
**The queue owner killed it with their own arithmetic ~10 minutes after writing
the row, and the row was never annotated.** Launcher = **2.0% of 308.2%** of our
scale contribution at **8.2% coverage** ⇒ **0.65% of the tax we levy on
ourselves**; converted to a registration rider, whose named first customer
(EVICT58) is itself refuted.
**Its registered mechanism metric was INERT BY CONSTRUCTION:** it reads
`get_scale_percent()` at r50/r100/r150 while `LAUNCHER_MIN_RND = 160` means no
launcher exists at any of those rounds **in either arm**.
**GREP: ABSENT as behaviour, and NOT the spec-not-wired shape** — `ct.destroy(`,
`can_destroy`, `self_destruct`, `get_scale_percent` all **0** occurrences; all 11
"destroy" hits are comments and there is no constant, flag or dead branch.

## #67 WIRE `_hunt_turret` — A DESIGN ROW WEARING A WIRING ROW'S CLOTHES
`git log -S` verdict: **NEVER WIRED, not removed on evidence.** `_v105loki1` was a
from-scratch rewrite; its own `DESIGN.md` says `doctrine.py` was **"copied
whole"** while home defence was **"rebuilt"** — that is what orphaned it.
**The finding that falsifies the row's premise: 43 of 168 constants in
`doctrine.py` (25.6%) are read ZERO times.** The "safety scaffolding" the book
says already exists (`_healer_floor`, `_duel_safe`, `_intercept`,
`DUEL_DISCIPLINE_ON`) **is itself spec-only.** ⇒ ~120 new lines and five unmade
design decisions, not a wiring job. Its own metric (**living entities at r75**) is
inert against `HUNT_MIN_RND = 120`.

## #54 NAV LIMIT CYCLE — ROAD-CLOSED, BUILT TWICE, SCREENED TWICE
`OSCLOCK` (K=8) **48.53%** @1867 · `OSCLOCK2` (K=4+bans) **46.49%** @1809.
Source fact behind it: `self.stuck` increments at exactly one site, reachable only
when all four moves fail — **a 2-cycle is made of SUCCESSFUL moves, so
`stuck>=5` can never fire.**
**It also killed the queue owner's own re-screen recommendation** ("+2-4pp on the
900-area segment"): both dead arms read **47.50** and **45.68** on exactly that
segment, and OSCLOCK2's CI there **excludes 50 from above**.
⚠ It surfaced that **`nav_limit_cycle_census.py` was LOST — twice** — the dose
instrument for #54/#63/#64/RETIRE60. **Rebuilt and committed this session** as
`tools/nav_lock_census.py`.

## #51 AIM THE THROW LOOP — ⛔ RE-LABELLED: **A BROKEN LOOP, NOT A DEAD IDEA**
**⛔ THE ORIGINAL GROUND WAS FALSE AND IS RETRACTED HERE, AT THE PROVENANCE
RECORD.** This section read: *"ZERO exile throws across 115 RATED ladder games.
All 180 throws on record are unrated."* **That number is wrong.** It was a relay
requiring a `file → version` join (`throws.tsv` has NO version column) that nobody
had performed. Joined against `meta_join`:

| v140, archived | games | our EXILE throws |
|---|---|---|
| **rated (ladder)** | **200** | **193** |
| unrated | 483 | 180 |

The unrated half reproduces the relay exactly; **the rated half is a sign flip.**

**⭐ AND THE CORRECTED DATA DESCRIBES A DEFECTIVE IMPLEMENTATION, NOT A WORTHLESS
IDEA.** The 193 rated throws occur in **3 of 200 games (1.5%)**, and **ONE game
carries 176 of them (91%)** — the same signature as the 152-throw game vs 0033
and the 548-throw game at v105. **A launcher that latches onto a single victim
and cycles it 176 times is a broken target selector**, which is the thing this row
proposed to fix.
⇒ **The written ground "aiming a loop that never runs" is withdrawn.** The honest
statement is *"our loop fires in 1.5% of rated games and 91% of its output is one
pathological game."*

**WHAT STILL HOLDS, and it is why the row stays LOW PRIORITY rather than
reopening:** launcher coverage **16.2%**, median build round **264** — **past
`KILL_WINDOW_RND: 250`**, so the launcher barely exists inside the window that
scores. That leg is independent of the retracted number. And `AIMTHROW2` screened
a *target-priority* change at n=4,005 → **50.11% [48.58, 51.65]**, deferred.
⚠ **But note what AIMTHROW2 did NOT test: aiming at BORDER tiles.** The shipped
selector sorts destinations by distance from OUR core (`raid.py:925`), and in the
`#17` drive **81.5% of throws landed on interior tiles (565 of 693) at zero
lethality, against 128 border landings at 1.00.** Border-first aiming is a
different change from the one already screened.
⇒ **STATUS: low priority, pending the EXILE-victim decoder read.** If real
opponents die on border landings, this row and `#17`/`#38`/`#60` collapse into one
launcher plank with two named fixes (forward SITING and border-first AIMING). If
they do not, better aiming delivers more of something worth nothing.

## #22 TURRET CUTOFF AFTER r150 — THE GATE DOES NOT EXIST
All 27 `get_current_round()` sites enumerated; both turret build paths read in
full. **No r150 gate anywhere.** The only round-conditioned turret constant is
`LOKI2_RUSH_RND = 60`, an **opening relaxation**, not a late cutoff.
**Every lever that would produce the row's effect is already screened NEGATIVE:**
`MINHARV1` 47.24% · `FWDFLOOR8` 45.88% · `CAP6B` 49.00% · `CAP12B` 48.93%.
⭐ **And the brief's premise was FALSE in the row's favour** — only **20.8%** of
games end before r150 (pooled n=16,216), so the late window is well occupied. It
is the absence of a LEVER, not of opportunity, that kills it.

## #21 THE GUNNER COUNT · #2 OFF-AXIS SENTINEL
Full write-up: **`docs/research/CLOSURE-queue-21-2-2026-08-14.md`.** In one line
each — **#21 closes at the RULES LEVEL** (the gunner branch's admissible set is a
strict subset of the sentinel's, so it can only change a turret's TYPE, never add
one; `GUNADD`'s rate exists on disk at exactly **50.00%**, 194/194); **#2** is a
duplicate of the locked `sentthreat` arm plus a premise-thin hunt half.

## `wirehold` — MINE, AND I WAS WRONG ABOUT ITS SEGMENT
Built off a sound source trace (`_wire_tick` overwrites `link_queue` mid-lay after
`SIPHON_WIRE_RNDS = 12`, ~6 tiles, while 14/16 midgard chains exceed that).
**Then the dose was measured: the branch fires 5 times in 120 games, and ZERO
times on midgard AND ragnarok — the exact segment I reasoned from.** The sign is
not even positive everywhere (vs `_v178salt`: control 5, **treatment 0**) because
the constant is also the escape hatch from a wedged queue. **My corroboration was
wrong too** — `_l4_repair`'s "dead heads" docstring describes a *different*
abandonment path.

## `finishhp` (#2 re-scoped) — ITS OWN DOSE GATE SAID DO NOT FIRE
Floor **0.50** trigger rounds/game fixed **before** any number existed; measured
**0.370** ungated / 0.217 gated, 11.4% of games, both upper bounds. **The floor
was not moved.**

## `BELTBLIND` — KILLED BY THE #63 PROBE, AND IT WOULD HAVE SHIPPED A REGRESSION
CONVEYOR/SPLITTER account for **14 of 61,346 refusals (0.02%)**. Belts do not
block builder movement, so adding them to `_bfs_direction`'s blocked tuple would
block **passable** tiles. It was live in the queue as a coin-flip sibling of
BODYAWARE; the probe picked between them.

---

## THE PATTERN, which is the finding
**The four-part admission checks that a row HAS a mechanism, a metric and a
fixture. Nothing checks that the row is STILL TRUE.** A GREP stamp is taken once,
at stocking, and never re-taken — so a row closed in `coordination.md` keeps
recruiting screens indefinitely. **#60 is the canonical instance: killed by its
own owner's arithmetic ten minutes after being written, and still recruiting
seven hours later.**
