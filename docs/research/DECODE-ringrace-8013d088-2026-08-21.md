# DECODE — the RING RACE in match `8013d088`, v177 vs Erebus v178

**Decoded 2026-08-21T13:18:35Z** (`date -u`, same shell). Rated ladder match
`8013d088-6a5d-416f-aa07-90477cee7818`, created `2026-08-21T12:31:10.314Z`,
completed `12:34:04.020Z`. **`teamAName` = OpenSverige `teamAVersion` 177**
(= `bots/_v542wave`), **`teamBName` = Erebus `teamBVersion` 178**. scoreA 4,
scoreB 1, `eloDeltaA` +14.02. **Team-side verified from the wire, not from the
brief**: the replay `winner` field is `0` in games 1-4 and `1` in game 5, which
is the 4-1 the meta reports, so **team A = team 0 = us** in all five.

Magnus's question, verbatim: *"this game shows they are late to raid — have we
made sure our raiders are launching first before the conveyor ring around the
core is built?"*

**Short answer, and it splits in two because two different races are running:**
* **The BUILDER raid: we are early and they are late.** Our raider is at their
  core at r8-r19 (median **r12**); theirs reaches ours at r28-r87 or never.
* **Their belt beats us to the sockets anyway, and it is not close.** Erebus
  puts **3 conveyors on its own core-adjacent sockets by r3-r5** — before our
  first throw (r6) and 5-14 rounds before our first raider lands. **We have
  never once been first to a socket.** But that belt is not a ring: it is 3-5
  of 8, it is never closed, and the 3 early tiles are the only ones we lose.
* **The race that decides the game is neither of those. It is the FORWARD
  TURRET, and there we are 50-80 rounds LATE in 5 of 5 games.** Our forward
  sentinel lands r100-r128 (never, in game 5); their forward gunner/sentinel
  lands r21-r58. **First damage to their core is the forward-sentinel build
  round + 1, in 4 of 4 games where it exists.**

---

## 0. Instruments — reused, and what changed

* **`scratchpad/s52_launchtime/launchtime.py`** — run **unmodified** for the
  raid clock (launcher build, first own throw, arrivals). Its assumptions hold
  on these files: 5/5 parsed, no stderr, both team-sides populated.
* **`scratchpad/s52_diffstudy/ringtime.py`** — its ring-socket definition
  (in-bounds, non-wall tiles orthogonally adjacent to the 2x2 Core footprint)
  is carried over verbatim into the new file.
* **`scratchpad/s53_ringrace/ringrace.py`** (new, this session) — adds the
  three channels neither instrument carries: per-round ring OCCUPANCY, building
  GEOMETRY by min d² to own core, and per-round DAMAGE off update field 5
  (`UpdateHp`, folded via `replay_census.parse_update_hp`).

⚠ **INSTRUMENT TRAP CAUGHT MID-RUN, flagged because a first cut published the
wrong number.** `placeEntity` **re-fires on gunner rotation**
(`tools/replay_census.py` docstring). A raw event count read **"Erebus built 273
gunners"** in game 1; deduped on entity id it is **3 gunners and 270
rotations**. Every turret count in this document is DISTINCT ENTITY IDS. Any
count of gunners taken off raw place events in this repo is inflated by
rotations.

⭐ **CONSTANT-COLUMN GUARD, DRIVEN TO THE OTHER VERDICT.** `ring_close_r` reads
**-1 in all 10 team-cells of this match**, which is exactly the shape of a
column that validates anything. Run over a **150-replay control** from
`replay_archive/` (`scratchpad/s53_ringrace/control_120.tsv`), the same code
returns a **real closure in 24 of 300 team-cells** (12 A-side, 16 B-side, e.g.
`52a698e3` closes A at r30 and B at r67; `d56cd5c1` closes A at **r7**),
`ring_max` spans the full **0-8**, and the wall exclusion produces denominators
of **4, 6 and 8** rather than a constant 8. **The predicate can fire, and on
this match it does not. The -1s are a finding, not a dead column** — and the
r7-r18 closures in the control show an 8/8 ring is something teams on this
ladder do build, so Erebus's 3-5/8 is a choice rather than a rule of the game.

**Definitions** — every number below carries one:
| term | definition |
|---|---|
| `ring socket` | in-bounds, **non-wall** tile orthogonally adjacent to a Core footprint tile. **Denominator = 8 on all five maps** (walls excluded: 0; ore on a socket: 0). |
| `occ(r)` | ring sockets holding a building of the socket's OWNER at end of round r |
| `ring_close_r` | first r with `occ(r) == 8`. **-1 = never.** |
| `arr1` / `arr2` | first / second **distinct** own builder bot at d² ≤ 2 of the ENEMY core footprint — `launchtime.py`'s `arr2_r`/`arr2_r2`, so commensurable with the banked r12 anchor |
| `fwd sentinel` | our sentinel built at d² ≤ 13 of the enemy core (turret attack range class), distinct ids |
| `dmg_core_r` | first round with a **negative** `UpdateHp` on the enemy Core id |
| `orth_open` | of the enemy's 8 sockets, how many are NOT denied by a building or a body of ours — the quantity `siege.py:5751 _fs_salt_ok` gates on |
Rounds are **0-indexed engine rounds** (`turns[i]` IS round i).

---

## 1. Per-game race table — MEASURED

| # | map | dims | `MAP_CODES`? | result | our launcher / throw | **arr1** | arr2 | **their 3rd socket** | our 1st socket plug | their ring closed? | their `occ` max | **our fwd sentinel** | **1st dmg to their core** | our core 1st dmg |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | fimbulwinter | 20x20 | **no** | WIN r545 | r5 / r6 | **r19** | never | **r5** | r13 | **never** (3/8) | 3 | **r113** | **r114** | **never** |
| 2 | longhouse | 28x18 | **no** | WIN r193 | r5 / r6 | **r17** | r18 | **r4** | r28 | **never** (5/8) | 5 | **r128** | **r129** | r53 |
| 3 | helheim | 18x18 | **no** | WIN r212 | r5 / r6 | **r8** | r9 | **r3** | r21 | **never** (4/8) | 4 | **r116** | **r117** | r32 |
| 4 | paths | 24x24 | **no** | WIN r167 | r5 / r6 | **r10** | r15 | **r5** | r11 | **never** (4/8) | 4 | **r100** | **r101** | r44 |
| 5 | glacierkeep | 30x30 | **YES** | **LOSS r107** | r5 / r6 | **r12** | r13 | **r4** | r14 | **never** (4/8) | 4 | **NEVER** | **never** | **r22** |

**Race verdict** (`arr1` − their-3rd-socket round; positive = they were there first):

| # | arr1 − 3rd socket | arr1 − ring closure | arr1 → 1st core damage (the conversion cost) |
|---|---|---|---|
| 1 | **+14** | undefined (never closes) | **95 rounds** |
| 2 | **+13** | undefined | **112 rounds** |
| 3 | **+5** | undefined | **109 rounds** |
| 4 | **+5** | undefined | **91 rounds** |
| 5 | **+8** | undefined | **∞ (core never damaged; we died at r107)** |

**Against the banked lineage constant** (`DECODE-launch-timing-v174-2026-08-21.md`:
launcher r5 / throw r6 / arrival r12): **launcher r5 in 5/5, first own throw r6
in 5/5** — the ferry clock is a hard constant, not variable. **arr1 median r12**,
inside the banked r9-16 anchor in **3 of 5** (r8, r10, r12; misses r17, r19).
**The raid clock is exactly on its published spec. Nothing here is a launch-time
regression.**

---

## 2. SURPRISE 1 — there is no ring. Neither team ever closes one.

`ring_close_r = -1` for **both teams in all five games**. Erebus's own-socket
occupancy peaks at **3, 5, 4, 4, 4 of 8**; ours peaks at **3, 2, 4, 4, 3 of 8**.
**Magnus's "conveyor ring around the core" is not a ring — it is a 3-conveyor
delivery stub, laid on the sockets nearest their harvest side, at r2, r3 and
r4-r5.** After that the socket count on their side does not move for the rest of
the game except when *we* change it.

I looked for a perimeter built farther out before saying so. Erebus's building
geometry by min d² to its own core (game 2, r80) is
`d²=1:5 d²=2:3 d²=5:3 d²=10:3 d²=16:1 d²=17:3 d²=25:1 d²=26:2 d²=36:1 d²=49:1` —
a **thinning eco fan**, not a shell: no d² band is ever saturated, and the
counts fall monotonically outward. Same shape on all five maps. **There is no
outer ring to measure instead.**

**Per-socket race, first builder to each of Erebus's 8 sockets** (full listing in
`scratchpad/s53_ringrace/report.txt` + the `socket_report` output):

| # | owner-first | **us-first** | never built on | their first three sockets |
|---|---|---|---|---|
| 1 | 3/8 | 3/8 | 2/8 | r2, r4, r5 (conveyors) |
| 2 | 5/8 | 3/8 | 0/8 | r2, r3, r4 |
| 3 | 4/8 | 3/8 | 1/8 | r2, r3, r4 |
| 4 | 4/8 | 4/8 | 0/8 | r2, r3, r5 |
| 5 | 4/8 | 3/8 | 1/8 | r2, r3, r4 |

⇒ **To Magnus's question as asked: no, we are not first. Their first three
sockets are taken at r2-r5 and our first throw is not until r6.** No arrival
clock we can reach makes us first to those tiles — **a builder cannot be at the
enemy core before r6 by any route in this chassis.** What we *can* do (and do)
is take the OTHER five: our barriers land on 3-4 sockets per game, and in games
2, 3 and 4 we later **peck their conveyor out and put a barrier on the cleared
tile** — `(24,7)` r91 removed → r92 our barrier; `(23,8)` r93 → r94; `(25,7)`
r122 → r123 (game 2); r38→39, r41→42, r44→45 (game 3); r23→24, r96→97 (game 4).
**That conversion works. It costs 20-90 rounds per tile.**

---

## 3. SURPRISE 2 — the binding clock is the FORWARD SENTINEL, and it is ~100 rounds after arrival

**Every point of damage we ever did to the Erebus core in this match was a
sentinel shot.** Delta histogram on their core id: `{-18: 216}` (g1), `{-18: 28}`
(g2), `{-18: 48}` (g3), `{-18: 28}` (g4), `{}` (g5). **Zero `-2` builder attacks
on their core in all five games** — `LOKI_QUIET_ON = True` (`doctrine.py:1687`)
silences builder melee, by design.

So the kill is gated entirely on when a sentinel gets built inside range of
their core, and the two clocks are the same clock:

| # | fwd sentinel build | 1st core damage | gap |
|---|---|---|---|
| 1 | r113 @ d²=5 | r114 | **1** |
| 2 | r128 @ d²=5 | r129 | **1** |
| 3 | r116 @ d²=5 | r117 | **1** |
| 4 | r100 @ d²=5 | r101 | **1** |
| 5 | **never** | never | — |

**And the mirror control says this is not a rule of the game — it is a property
of our build.** Erebus's forward turret (d² ≤ 13 of OUR core): gunner r46 (g1),
gunner r52 + sentinel r58 (g2), gunner r22 + sentinel r33 (g3), gunner r30 (g4),
**gunner r21 + sentinel r24 (g5)**. Their first damage to our core follows the
same +1 pattern (r53, r32, r44, r22; never in g1). **They put a turret on our
core 50-80 rounds before we put one on theirs, in 5 of 5 games.** Magnus's read
that "they are late to raid" is true of their BUILDER bots and **false of their
turrets** — on the clock that actually removes a core, they are the fast side
and we are the slow one. We won 4-1 anyway because their forward gunners did
almost nothing (0 damage to our core across 545 rounds in game 1) while our
sentinel, once up, converts.

### What binds — located

`siege.py:5792 _fs_sentinel_ok` is the purchase gate. Under
`LOKI_FS_V515 and FS_V515_GATE_OR` (both True) it is a **disjunction**
(`siege.py:5797-5839`):

```
salt   = _fs_salt_ok(...)        # siege.py:5751 — orth_open == 0, i.e. ALL EIGHT
                                 # enemy sockets denied by a building OR a body of ours
eco    = _fs_eco_gate_ok(...) and rnd >= FS_SENT_RND_FLOOR   # doctrine.py:3165 == 60
```

**Measured off the wire, `orth_open` never reaches 0 in 4 of 5 games** — minima
**5** (g1, from r20 onward and never lower for 525 rounds), **1** (g2, r132),
**2** (g3, r45), **4** (g5, r90) — and in game 4 it hits 0 at **r102**, which is
*after* the sentinel was already bought at r100. ⇒ **The salt disjunct fired
zero times in this match. Every forward sentinel we bought came through the eco
disjunct, whose floor is r60**, and then took a further **40-68 rounds** past
that floor to actually land.

The residual 40-68 rounds is the eco half of the disjunct (`_fs_eco_gate_ok`,
`siege.py:401` — reads `FS_ECO_BIT_CONN` / `FS_ECO_BIT_DELIV` out of the comms
store). **The store is not in the replay, so I cannot read the bit; the
engine-side facts it is supposed to track are:**

| # | our 1st core delivery | rounds with **2 live harvesters** | our fwd sentinel |
|---|---|---|---|
| 1 | r10 | from r15 | r113 |
| 2 | **r31** | **never** (max 1 harvester all game) | r128 |
| 3 | r9 | from r5 | r116 |
| 4 | r14 | from r8 | r100 |
| 5 | **r33** | **never** (max 1 harvester all game) | **never** |

**The two games where we never fielded a second live harvester are the two
slowest turret games in the set — r128 and never.** That is consistent with the
eco disjunct binding and is **not proof of it**: the bit is a store read, the
harvester count is an engine fact, and one is a proxy for the other. Stated as
a proxy, deliberately.

⇒ **The binder is NOT the muster tax and NOT the MAP_CODES fallback.**
`siege.py:1252-1266` (the `_fs_turn` no-route watchdog / appointment guard) does
not fire on the observed clocks — our raiders arrive at r8-r19 and stay alive;
the plank never degrades. And the **MAP_CODES-less** hypothesis points the wrong
way: **4 of 5 maps have no `MAP_CODES` entry and we won all four; the single map
that HAS one (glacierkeep, key `(30,30,14,2,14,26)`) is the one we lost.**
Arrival on the coded map (r12) is not better than on the uncoded ones (r8, r10).

---

## 4. Game 1's long middle (545 rounds) — the raiders parked, and never died

Both raiders survive the entire game (**0 builder-bot deaths on our side, all
545 rounds**) and sit locked at d²=2 and d²=4 of the Erebus core from r21 to
r544, oscillating one tile. **Everything we ever placed near their core:**

```
r 11  barrier  (15,16) d²=2        r 15  launcher (15,15) d²=5
r 13  barrier  (15,17) d²=1        r 20  barrier  (16,19) d²=1
r 15  barrier  (15,18) d²=1        r113  sentinel (14,19) d²=5
```

**Between r20 and r113 the raid places nothing and does nothing measurable** —
0 attacks (quiet mode), 0 heals given to the sentinel (it received **zero** HP
events all game: never damaged, never healed), 0 further barriers. They are not
dead and not replaced; they are **parked**, holding 3 sockets, waiting on a gate
that could not open (`orth_open` stuck at 5 of 8 — the two sockets nobody ever
built on and their three original conveyors, which we never cleared in this
game).

From r114 the single sentinel grinds: **216 shots, 3,888 damage, against 3,388
healed back by Erebus** — a net ~500 over **432 rounds**. That is the whole
second half of the game. **A r545 game we won is a `R1000_IS_DEFEAT`-adjacent
outcome by the programme's own logic, and the 432-round tail is one sentinel
against one healer.**

---

## 5. Game 5 (glacierkeep) — the loss, in full

**We won the raid race and lost the turret race, on the one map the bot has
terrain for.**

| clock | us | Erebus |
|---|---|---|
| launcher / first throw | r5 / r6 | none (0 launchers all game) |
| ferry hops | r5, r7, r9, r11, r13 (5 launchers) | — |
| first raider at enemy core | **r12** | r31 |
| first socket plug | r14 `(15,25)` | — |
| forward turret | **NEVER** (only turret: 1 gunner, r57, d²=485 — at home) | **gunner r21 d²=9, sentinels r24 d²=5 and r25 d²=13** |
| first damage to enemy core | **never** | **r22** |
| core damage taken | **726** (38 sentinel + 6 gunner hits), healed +226 | 0 |
| 1st core delivery / harvesters | r33 / **max 1 alive** | r63 / 2 |

Sequence: ferry chain lands our raider at d²=1 by **r12** — the fastest arrival
of the five games. We plug one socket at r14, then the raid stalls for 36 rounds
(next barriers r50, r51; a fourth at r91). Erebus, which never builds a launcher
and whose own raider does not reach us until r31, simply **walks two sentinels
to d²=5/13 of our core by r25** and starts hitting it at **r22**. Our core
absorbs 726 damage against 226 healing and dies at **r107**. Our raiders are
still alive and still parked next to the Erebus core when our own core falls —
`orth_open` on their side never dropped below **4**, and with the salt disjunct
shut and only one harvester alive, **the forward-sentinel gate never opened at
all**.

---

## 6. Answering Magnus directly

> *have we made sure our raiders are launching first before the conveyor ring
> around the core is built?*

**No — and the target as stated is unreachable.** Erebus's first three
core-adjacent conveyors go down at **r2, r3, r4-r5**; our launcher is built at
r5 and our first throw is at r6. **No arrival clock beats r2.** But the premise
is worth correcting rather than optimising: **there is no ring.** They cover 3-5
of 8 sockets, never 8, and we take 3-4 of the rest — the sockets we lose are the
3 they laid before we could physically be there, and in 3 of 5 games we **clear
and convert** even those, 20-90 rounds later.

**And "they are late to raid" holds only for their builder bots.** Their
FORWARD TURRET is up at r21-r58 in every game while ours is r100-r128 (never, in
the one we lost), and first core damage is the forward-turret build round + 1 on
both sides. **The 90-112 round gap between our raider arriving (r8-r19) and our
core damage starting (r100-r129) is where the game is spent, and it is gated by
`_fs_sentinel_ok`** — the salt disjunct (`orth_open == 0`) fired **0 times in 5
games** because their pre-laid conveyors and two never-built sockets keep
`orth_open` at 2-5, so every sentinel came through the eco disjunct with its
`FS_SENT_RND_FLOOR = 60` floor, plus 40-68 further rounds.

**Nothing in this match is a launch-timing defect. The ferry clock is r5/r6 in
5/5 and the arrival median is r12, both exactly on the banked lineage
constant.**

---

## 7. Open items (not resolved here, not verdicts)

1. `orth_open == 0` is unreachable when the defender pre-lays sockets **and**
   some sockets are never buildable — g1 had 2 sockets **nobody ever built on**
   and `orth_open` still floored at 5, meaning bodies were not covering them
   either. Whether the salt predicate should count "unreachable" seats as
   denied is a design question with a measurable fixture.
2. The eco disjunct's proxy relationship (store bit vs. live harvester count)
   is unverified from the wire. It needs a local run with `FS_GATE_LOG`
   (`doctrine.py:3166`) to read `GATE515` on stderr — the platform strips
   stdout, so this is a LOCAL-only instrument.
3. Game 1's 432-round sentinel-vs-healer grind converted at net ~1.2 HP/round.
   Two aligned sentinels are permitted (`FS_SENTINEL_MAX = 2`,
   `doctrine.py:2513`) and only one was ever bought.

**Files:** `scratchpad/s53_ringrace/ringrace.py` (instrument),
`ringrace.tsv`, `launchtime.tsv`, `report.txt`, `socket_race.txt` (per-socket
first-builder listing, both team-sides, all 5 games),
`control_120.tsv` (the 150-replay constant-column control).
