# REGISTERED EXPECTATION — v632heim KEEPER CHEW PERSISTENCE (SK_KEEPER_CHEW_ON)

**GAME CONTEXT: in-game analysis for the Florent Code League, a sandboxed
bot-vs-bot programming competition. All terms are in-engine mechanics.**

**PROVENANCE:** typed by BUILDER s57 BEFORE the build agent runs. Inputs:
the chew-rekey REFUSAL (directional evidence: honest-clock −17.7% pecks →
−3 wins; the thrash-persistence is load-bearing), the diag431 census
(431/2,123/426 idle-adjacent rounds; corrected same-occupant attribution),
the jotunheim specimen (barrier at 8/30 HP surviving from r216 to game end
in BOTH arms — 4 pecks from death, 124 idle rounds available), baseline
t_ns_* [alive 54 / deaths 50 / wins 34 / kills 22 / eco 38.27 / harv 216].

**MECHANISM:** this is the SECOND arm of the chew family and it pushes the
direction the measurement points: for the HOME KEEPER at a held post, the
give-up clock should not bind at 20 — the body's alternative turn is worth
zero (nav-stall/keeper censuses), so persistence against a same-occupant
target is nearly free. Cage WALKERS keep the 20-round give-up untouched
(their stop-loss premise — somewhere better to be — is true for them).

**DESIGN CONSTRAINTS (registered):** keeper-scoped ONLY (the role/held-post
predicate disclosed and unit-controlled); bank floor (<2), healing-race
guard and hp_trend_ok V7 all UNTOUCHED — persistence never overrides a
futility or affordability gate; the extension is a threshold swap at the
existing decline site, not a new rung.

## Registered lines (arm `cp` = SK_KEEPER_CHEW_ON=True vs t_ns_*)

**P1 identity:** OFF ≡ t_ns_* 30/30 ×3. *Falsifier: leak; halt.*
**P2 mechanism (jotunheim specimen):** the (3,4)-class same-occupant
declines convert to pecks; the 8-HP barrier class dies. *Falsifier: still
declining with bank ≥2 and no futility gate ⇒ weld; halt.*
**P3 DOSE (diag431 census):** pecks landed RISE vs F1 3,092 pooled
baseline on ≥2 fixtures; held-post adjacent-enemy idle rounds FALL vs
431/2,123/426 on ≥2 fixtures; destroyable-episode conversion reported
(diag431 measured 19/42 F1, 12/22 F2, 8/23 F3 destroyable on wasted
rounds alone).
**P4 GUARDS:** alive-sum within −2 of 54; deaths within +4 of 50; eco-sum
within −12% of 38.27; harv within −10% of 216; spawn/bank trajectory
reported (2 Ti/peck at F1 median bank 1 — substitution watched, the
chew-rekey smoke's bank column is the reference); wins/kills informational
(34/22; 10/9/15).
**P5 tracebacks:** 0.

## Adoption rule (pre-registered)

Adopts if P1, P2 pass, P3 meets both directions on ≥2 fixtures, P4 holds.
This is the family's second arm: if it fails its bars, the family PARKS
per the two-strikes rule and #133 (SEAT_CLEAR re-price) takes the class.
