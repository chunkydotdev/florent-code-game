# REGISTERED EXPECTATION — PLANK 10 (battery survival) composite screen

**GAME CONTEXT: in-game analysis for the Florent Code League, a sandboxed
bot-vs-bot programming competition.**

**PROVENANCE:** BUILDER s58, committed before any tape. Arm = SK_ROTATE +
SK_ROTATE_FUND + SK_ROTATE_GUARD (the full rotation stack; the parked
FUND arm's banked numbers are the comparison [wins 34, coverage 0.718,
lives 20/27/5, concurrency 4/2/3]). Attacks the wins-34 freeze via battery
lethality.

**PRE-TAPE AMENDMENT (blind held; the build smoke's structural finding):**
the heal/hold band widens from the body's own plant ledger to **any of OUR
standing forward sentinels this body can see** (vision-safe read, both-
verdict-controlled) — the ledger form is dead for replacement raiders,
which are the post-flip norm (8-body churn measured); shipping it would be
the fourth known-dead mechanism. The seen-firing rule (fund F4) applies:
**G4 requires post-flip tube-heals > 0 observable on the tapes.**

## Registered lines
**G1 identity:** flags-off ≡ t_so 30/30 ×3.
**G2 dose:** (a) post-flip sentinel life, touchable-cell median per fixture,
≥ FUND's [20/27/5] on ≥2 fixtures with none falling >20%; (b) max
concurrent battery ≥ 4 in ≥1 cell per fixture (the §8b threshold).
**G3 the win column:** wins-sum ≥ **37** (FUND 34; the freeze must break
by more than the null win-spread) with per-fixture wins vs victory bars.
**G4 mechanism:** post-flip heals landing on standing battery tubes > 0
per fixture (the seen-firing rule); front-seat occupancy rounds reported.
**G5 guards:** alive-sum −2 of 53; deaths +4 of 54; eco −12%; harv −10%.
**G6 attrition dial (the build's hypothesis, not gated):** rotation-body
lives and replacement counts vs FUND — the body-as-screen's price,
reported for the verdict's honesty either way.

## Pre-registered decision
G1-G5 → ADOPT the full stack (rotation+funding+guard — the win engine
complete). G3 <37 with G2/G4 passing → the lethality thesis fails at the
last step: PARK the stack and the campaign's remaining lever is phase-1
survival alone (the arithmetic note); the builder then takes the
walk-terminal/keeper queue as the next family. G4 =0 → build defect, halt
(the amendment exists precisely to prevent it).

**G4 AMENDMENT (pre-tape, blind held; the opportunity census is the
provenance):** the unit controls prove the widened rung fires end-to-end
(empty-ledger body + seen tube + damaged neighbour → heal lands); the smoke
zeros are OPPORTUNITY nulls (0 rounds of adjacent-body-with-damaged-tube on
3 cells — tubes are either unshot or removed without a graded window). G4
becomes conditional: **heals > 0 REQUIRED only in cells where the
opportunity state occurs** (our builder cardinally adjacent to a live
damaged post-flip battery tube with heal affordable), and the OPPORTUNITY
COUNT itself is a registered column — a zero with zero opportunities is
vacuous-with-disclosure, a zero WITH opportunities is the halt-grade
defect. The seen-firing rule survives in its correct form: seen firing
when firing is possible.
