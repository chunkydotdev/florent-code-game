# REGISTERED EXPECTATION — v632heim PLANK 2 (demolition sweep) screens

**GAME CONTEXT: in-game analysis for the Florent Code League, a sandboxed
bot-vs-bot programming competition.**

**PROVENANCE:** BUILDER s57, committed while the plank-2 build agent is still
running — blind to its code and all tapes. Baseline = the ADOPTED-leash tree
(t_leash_* tapes are the identity and guard reference; original t_ctrl_*
constants quoted where the doctrine bar is absolute). Every bar carries a
measured control value (banked p4/p11 readouts + the prediction study).

**PLANK:** SK_DEMOLISH — the enemy-structure sweep in our half (denier
primary, keeper below its heal rungs), feeding _clear_tile, priority
launcher > sentinel > gunner > seat-barrier > belt > barrier. The prediction
study's ground: planted structures carry 100% of first core damage (64/64)
and 82-92% of belt damage lands within cheb 2 of our core.

## Registered lines

**Z1 identity:** flags-off tree ≡ t_leash 30/30 ×3. *Falsifier: any
divergence ⇒ leak; halt.*

**Z2 liveness:** ON (SK_DEMOLISH=True) diverges from t_leash in ≥8/30 per
fixture (enemy structures stand in our half in every control cell:
368/369/297 per fixture — the trigger is never absent).

**Z3 DOSE (the plank's whole job), vs the ORIGINAL ctrl constants:**
destroyed-share of enemy structures in our half — control measured
**0.277 F1 / 0.184 F2 / 0.091 F3** (leash reference −0.006/−0.020/−0.027
deltas) — bar: **≥ +0.05 absolute on ≥2 of 3 fixtures AND three-fixture sum
of deltas ≥ +0.10** (a dedicated demolisher must beat plank 1's accidental
+0.037 best single-fixture delta). Cell-matched enemy-structure dwell
(destroyed-only, uncensored form) non-increasing on the fixtures where the
share bar passes.

**Z4 GUARDS, vs the LEASH baseline (the tree this plank stands on):**
(a) core-fp heals/game raw within −10% of leash **[14.17 / 11.03 / 13.43]**;
(b) our-core-death cells within +1 of leash **[16 / 20 / 18]**;
(c) P(alive@r300) within −1/fixture of leash **[16 / 13 / 17]**, sum ≥ 45;
(d) eco builds common-horizon within −10% of leash **[10.07 / 11.00 / 11.57]**
    (the sweep spends builder-turns on chewing; the fence is that it spends
    the denier's, not the economy's);
(e) by-r300 kills + wins reported, no gate **[leash: 10/4/7; 14/9/10]**.

**Z5 tracebacks:** concurrent observation; expected 0.

## Pre-registered decision rule

Z1+Z2+Z3 pass and Z4 holds → ADOPT (SK_DEMOLISH default ON) and plank 3 (the
turret ring, with the ammo-by-r5 registered clock) builds on top. Z3 fails →
the sweep returns to design ONCE with the readout's cell evidence; a second
miss parks it (the two-strikes rule, now standing policy). Z4(a/b/c) breach
on F1 through the five named cells → that is the #129 systematic, not this
plank; the readout must separate the five cells before any verdict line is
typed (pre-registered so the separation is not post-hoc).
