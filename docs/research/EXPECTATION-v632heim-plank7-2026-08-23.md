# REGISTERED EXPECTATION — v632heim PLANK 7 (core-apron mesh) screens

**GAME CONTEXT: in-game analysis for the Florent Code League, a sandboxed
bot-vs-bot programming competition.**

**PROVENANCE:** BUILDER s58, committed while the build agent runs. Baseline
= t_so [alive 53 / deaths 54 / wins 31 / kills 14 / eco 35.47 / harv 208].
The plank is Magnus's own field observation (Bean counters' conveyor
curtain) built per study §4d; the prediction invariants it attacks: 100% of
first core damage is turret fire from plants, and the plant band is cheb
1-3 of our core.

## Registered lines

**M1 identity:** flags-off ≡ t_so 30/30 ×3.
**M2 liveness:** ON diverges ≥8/30 per fixture.
**M3 DOSE, two channels, either adopts (registered so a mechanism-true
defence plank is not killed by survival noise, nor a survival gain required
to also show its mechanism):**
(a) **SURVIVAL:** alive-sum ≥ **55** (from 53; the n=2 null envelope tops
at +1, so +2 is outside it) — the touchable-ceiling lever the rotation
campaign needs;
(b) **PLANT DENIAL (mechanism):** enemy turret plants at d²≤4 of our core
footprint, three-fixture sum, ≤ **−25%** vs baseline (measured
same-readout; denominator floor: bar binds only if baseline sum ≥ 15) AND
alive-sum ≥ 53 (not worse).
**M4 GUARDS:** deaths within +4 of 54; eco within −12% of 35.47 (the mesh
conveyors are +1% scale each, capped at 8 — the scale delta reported);
harvesters within −10% of 208; wins/kills informational with null floors.
**M5 mesh mechanics:** mesh tiles planned in ≥ 24/30 ON cells per fixture,
built (not just planned) in ≥ 15/30; positions cheb ≤ 2 of the footprint.
**M6 tracebacks:** concurrent observation, 0.

## Pre-registered decision

M1+M2+M5 + either M3 channel + M4 → ADOPT. M3 both-miss with M5 clean →
ONE siting/cap redesign then park (standing two-strikes). M5 fail → build
defect, halt.
