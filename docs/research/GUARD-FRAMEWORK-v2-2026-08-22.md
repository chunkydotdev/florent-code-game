# GUARD FRAMEWORK v2 — corrected comparator for Heimdall screens

**GAME CONTEXT: in-game analysis for the Florent Code League, a sandboxed
bot-vs-bot programming competition.**

**PROVENANCE:** BUILDER s57, from the null-semantics discriminator (eighth
readout commission, banked verbatim in the tail note of this timestamp;
scripts e46f1_*; side-lane-designed control). Registered BEFORE any re-scoring
of tonight's arms — the corrections below are typed from the null envelope,
then the re-scores follow them.

## The measured facts that retire the old guards

1. **Core-footprint heals is a SYMPTOM metric, not a driver** — 0/398 heals
   precede first core damage; heal HP = 14.7% of damage taken; the median
   damaged round removes 18 HP vs a 4 HP heal (99.3% of damaged rounds exceed
   one heal); cores died in 2 of 6 high-heal cells. RETIRED as a guard
   everywhere; stays as a descriptive diagnostic only.
2. **Per-fixture F1 guard columns sit inside a NULL ENVELOPE**: the
   zero-content arm moved F1 heals −76.2%, death cells +3, wins −3, by-r300
   kills −4. Any per-fixture F1 delta inside those magnitudes is
   uninformative about plank content. F2/F3 per-fixture columns are noisy at
   ±2-4 quanta too (F2 null wins −2; F3 null deaths −3, wins +4).
3. **Three-fixture SUMS are null-stable at ±1**: alive@300 sum 46→47 (null),
   death-cell sum 54→55, win sum 33→32. The sum is the guard-grade surface.

## The v2 guards (for every subsequent Heimdall screen)

- **G1 SURVIVAL:** three-fixture alive@300 SUM within −2 of baseline sum
  (null envelope ±1, doubled for margin). Per-fixture columns reported,
  never gated.
- **G2 CORE DEATHS:** three-fixture death-cell SUM within +2.
- **G3 ECON:** three-fixture eco-builds SUM at common horizon within −12%.
- **G4 WINS (doctrine-informational):** three-fixture win SUM reported with
  the null reference (32 vs leash 33).
- **DOSE bars stay per the plank's own registration** (they were never
  F1-heal-denominated) but adopt fixture-SUM forms where singles were used.

## Re-scores of tonight's dispositions under v2 (ruler correction, not strike forgiveness)

- **Plank 1 (citadel dispatch):** strike 1's E4 was spec-defective (disclosed
  same-day) and its E6 breaches were F1-per-fixture (inside the null
  envelope) → VOID. Strike 2's Y2 F1 breach → VOID; **its Y3 aggregate dose
  fail (−0.012 vs ≥+0.02) STANDS — a valid strike.** Disposition: **UNPARKED
  to one-valid-strike; one redesign remains** (the reachability/dose problem,
  same class as plank 2's).
- **Plank 2 (demolition sweep):** Z3 dose fail (sum −0.005) is
  divergence-independent → **STANDS; one redesign remains** (reachability-
  aware pick). Its Z4 F1 breaches are inside the null envelope → noted void,
  changes nothing (Z3 already failed it).
- **Plank 4 (leash): adoption STANDS** — it passed the old bars, and under
  v2 its sums read 46/54/33 vs control 45/55/32, all inside guards.
- The five-cell #129 attribution and research's per-cell mechanisms remain
  valid MECHANISM findings (the freeze-class code defect is real and its
  fix, SK_ORE_STEPOFF, is built and screens under v2); what is retired is
  only their use as per-fixture guard evidence.

## What this bought

Four "failed" arms and one null control converged on: a corrected measurement
system (sums not per-fixture singles; symptom metrics retired), one adopted
plank (leash), one real dose defect shared by both dispatch planks
(reachability), one latent base defect found and fixed-pending-screen
(ore step-off), and the fixture knife-edge documented so no future lane
burns strikes on it.
