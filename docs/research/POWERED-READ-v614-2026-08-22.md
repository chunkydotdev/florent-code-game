# POWERED READ — V614POWER: the head + the founding tree vs the NOISY benchmark, n=900/arm

**Builder s54, 2026-08-22 ~05:3xZ.** The first properly powered read of the Skalman line:
ws1+ws2 remote battery (runid bb_20260822T052758Z_72330), arms `_v614skalman` (ship head)
and `_v600skalman1` (founding tree) vs the CANONICAL `_v542wave` (NOISE_ON — its noise is
what makes n>30 informative), 15 pool maps × seeds 900000-900029 × both seats = 900
rows/arm, 1,800 games, delivered COMPLETE in ~8 minutes, **0 tracebacks in 1,800 games**
(chassis robustness at n). Gate CLEARED (pooled-not-paired + self-play escapes typed;
control-equivalence structurally skipped vs a reseeding bot, disclosed by the gate).
Naive intervals at local DEFF 0.98.

## The result

| arm | win share [95% CI] | core-kills | by-r300 | median kill | r1000 games |
|---|---|---|---|---|---|
| **v614 (head)** | **35.78% [32.68, 38.88]** | 297 (33.0%) | 235 (26.1%) | **r201** | 38 (4.2%) |
| v600 (founding) | 2.78% [1.71, 3.84] | 8 (0.9%) | 2 | r613 | 50 |

## The two headlines

1. **⛔ THE DETERMINISTIC SCREEN'S LEVEL IS INFLATED ~11pp.** The NOISE_OFF 30-cell
   fixture reads the head at 14/30 ≈ 47%; against the same bot WITH ITS NOISE the
   powered read is 35.8 ± 3.1. **"Two games from beating the banditer" — this lane's own
   phrase earlier tonight — is corrected: the real gap to majority is ~14pp with the CI
   upper bound at 38.9.** The deterministic screen remains the fast ITERATION instrument
   (its per-cell attribution power is why the line moved 0→14 in a day); its LEVEL is
   not currency and never was — this read is the level.
2. **The founding-day progress is real and enormous at power: 2.78% → 35.78%**, both CIs
   tight, same cells, same opponent — the fourteen iterations bought ~33pp of win share
   against the retiring line's peak tree.

## Consequences
* FIRST-CONTACT GATE: stays NOT MET, now with a real number — the screen-clear
  declaration will be made against POWERED reads, not deterministic counts, from here on.
* The kill clock holds at power (median r201, by-r300 26.1% vs the field-era reference
  ~21.5%) and the r1000 tail is small (4.2%).
* The iteration mill continues on the deterministic screen for ATTRIBUTION; every few
  versions, a V-POWER re-read prices the LEVEL (ws1/ws2 deliver 1,800 games in 8
  minutes — this should have been running all along, and now it will be).
