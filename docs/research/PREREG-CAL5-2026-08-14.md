# PREREG — CAL-5 calibration panel on v139 (research s39, 2026-08-14)

**STATUS: committed BEFORE any CAL-5 leg creation** (two-clock standard; side
lane certifies vs platform `createdAt`). Research designs; the BUILDER
executes every match.

## Subject and purpose
**v139 "Loki v9" (`bots/_v218mapfix`), OUR OWN fresh ship** — holder verified
live by this lane earlier this session. Purpose: the standard calibration
read on the fresh ship (rated k accrues at only ~3 pairings/hour; this panel
feeds the k≥8 ship-gate CONTEXT — it does not replace the rated gate, and
every verdict sentence is the builder's).

## Design — V134-CHAR's amended design (A1-A4) INHERITED BY REFERENCE
Identical to `PREREG-V134CHAR-2026-08-14.md` as amended through A4, with:
- **Subject/EXPECT: v139.** Runner `INCUMBENT=139`.
- Same six CAL-4 cells (C1 team lazy · C2 Juusto · C3 Leviathan [D13
  reference cell, deltas not readable] · C4 HTTP 418 · C5 0033 · C6
  farming_200s), UNPINNED (calibration panels never pin), rotation, 5
  accepts/cell cap (**all n's count GAMES, 5 per accepted match: 25
  games/cell, 150 total**), fire-log-id membership (A3), ob-14 per-cell
  opponent distinct-version count at every read (A4).
- **Stop conditions:** holder change (ends at the last fully-under-v139
  window), or the builder needing the unrated budget for a higher-priority
  fire-order item (this panel yields; partial cells stay descriptive).
- **Instrument alarm:** any panel game decoding `ourver` ≠ 139 voids its
  window.

## Look discipline (declared before data)
1. Descriptive per-cell tallies at any n.
2. **ONE comparative look, declared now: at panel completion (n=150 games)
   or at the panel's stop if earlier with n≥50 — question fixed: v139 pooled
   game share on these six cells vs v125's CAL-4 equilibrium share on the
   same cells**, era caveats attached (CAL-4 measured under v125, both
   surfaces unpinned, opponent drift contributes; ob-14 numbers reported
   beside). No interim comparative reads.
3. CAL-2/3/4's own spent looks stay spent; `panel_read` always with explicit
   `--panel`.

## Target-value line
Unrated, zero rated exposure, zero submits (v139 already holds) ⇒ payout
gate N/A. Band re-read before any payoff sentence.
