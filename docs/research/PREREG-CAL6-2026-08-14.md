# PREREG — CAL-6 calibration panel on v140 (research s39, 2026-08-14)

**STATUS: committed BEFORE any CAL-6 leg creation** (two-clock standard;
side lane certifies vs platform `createdAt`). Research designs; the BUILDER
executes.

## Subject and purpose
**v140 "Loki v10" (`bots/_v223sealrepair` = mapfix + sealfloor0 + l4repair)**,
shipped 11:37:4xZ on Magnus's word off a v139-controlled n=1223 screen
(60.26 ±2.80). Purpose: early live cells for the k≥8 gate CONTEXT — the
ship rode a screen, so live calibration data is worth more than usual.
Verdict sentences are the builder's.

## Design — CAL-5's design INHERITED BY REFERENCE, subject re-pointed
Identical to `PREREG-CAL5-2026-08-14.md` (itself V134-CHAR through A4), with:
- **Subject/EXPECT: v140.** Runner `INCUMBENT=140`.
- Same six cells, UNPINNED, rotation, 5 accepts/cell cap; **all n's count
  GAMES (5/accepted match; 25/cell, 150 total)**; fire-log-id membership;
  ob-14 churn reporting at every read.
- **Stop conditions:** holder change (panel ends at the last fully-under-
  v140 window; the completion/stop look rule below covers the partial
  case), or the builder needing the budget for a higher fire-order item.
- **Instrument alarm:** any panel game decoding `ourver` ≠ 140 voids its
  window.

## Look discipline (declared before data)
1. Descriptive per-cell tallies at any n.
2. **ONE comparative look at completion (n=150) or at the panel's stop if
   earlier with n≥50** — question fixed: v140 pooled game share on the six
   cells vs (a) v125's CAL-4 equilibrium share and (b) CAL-5's v139 share
   on the same cells, era caveats + ob-14 beside. No interim comparative
   reads.
3. Prior CALs' spent looks stay spent; `panel_read` always with explicit
   `--panel`.

## Target-value line
Unrated, zero rated exposure, zero submits (v140 already holds) ⇒ payout
gate N/A.
