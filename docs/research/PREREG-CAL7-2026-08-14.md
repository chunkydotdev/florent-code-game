# PREREG — CAL-7, **THE DIVERGENCE PANEL** (research s40, 2026-08-14)

**STATUS: committed BEFORE any CAL-7 leg creation** (two-clock standard; the side
lane certifies the first leg's platform `createdAt` against this file's commit
time). Research designs and schedules; **the BUILDER executes every match**;
verdict sentences are the builder's.

**Subject / EXPECT: v140 "Loki v10" (`bots/_v223sealrepair`).** Live-verified
`Active bot: v140 (Loki v10)`, rating 1741, 966 matches, at
**2026-08-14T13:04:29Z** off `fcode status` — read, not asserted. Runner
`INCUMBENT=140`. Stability gate (R3) MET: **continuous tenure from ~11:59Z**
(first activation 11:37Z, then a ~1-minute v141 displacement caught by CAL-6's
own holder guard at 11:58:27Z; `ship_watch` reads v140 again at 11:59:21Z) ⇒
**65+ minutes at the holder read, past the 40-minute bar** — and the tree is
identified. *(Corrected by A2.5; the original text said "holder since 11:37Z",
which this lane's own CAL-6 read contradicts.)*

---

## ⛔ WHY THIS IS NOT CAL-6 REPEATED

`docs/research/CAL6-READ-2026-08-14.md` (the declared CAL-6 look, spent at its
stop, n=75) reports **three consecutive panels reading 30.0% (v125) → 31.0%
(v139) → 29.3% (v140)** on the identical six cells — and the 95% interval on the
CAL-4-vs-CAL-6 difference is roughly **±13pp**. A ship worth +3–5pp of game share
**cannot be seen by this design.** Three flat reads are three nulls the design
guaranteed. Firing the same six cells a fourth time buys a fourth guaranteed null
at the cost of four hours of the only live fixture we have.

Two further facts re-point the panel:

1. **The old cells are not where the rating goes.** Over the CURRENT era
   (`ourver` ≥ 125, i.e. since 2026-08-13 ~10:16Z; 79 matches / 395 games,
   52.4% share, **net +84.5 Elo**), the six CAL-4/5/6 cells contain one real
   leak (0033 **−39.4**), one mild one (HTTP 418 −11.7) and two net-POSITIVE
   cells (team lazy **+66.5**, farming_200s). **C3 Leviathan has read 1/25, 2/25,
   0/15 — 3/65 across three panels.** A cell pinned at a floor carries no
   information about our ship and is dropped (routed instead to a book
   commission; they are 1997.8 and out-developing us).
2. ⭐ **The panel and the ladder disagree on the same opponents.** Juusto: panel
   **20.0%**, current-era rated **52.5%** (8 matches, +4.2 Elo). team lazy: panel
   **53.3%**, rated **74.3%**. **If the panel reads systematically low, every CAL
   number this project has published needs a correction factor** — a larger fact
   than any single ship, and one the unrated budget can actually resolve.
   The cheap explanation is already closed with its control: **seat is balanced
   in both fixtures** (rated A 52.3% / B 48.5%; unrated A 49.3% / B 46.2%, over
   8,338 archived games of ours) and a ~3pp seat effect cannot make 32pp.

## THE QUESTION CAL-7 ASKS

**Does the unrated fixture — our declared `FIXTURE_OF_RECORD` — measure the same
quantity the rated ladder pays for, on the same opponents in the same era?**

## Design

Inherits `PREREG-CAL6-2026-08-14.md` (itself CAL-5 → V134-CHAR as amended
through A4) for all mechanics: **UNPINNED** (a calibration panel measures
relevance; pinning reintroduces the staleness the panel exists to detect),
5 accepts/cell/cycle cap, **rotating start cell**, rate-limit rejections wait out
the window and retry the same cell, holder asserted before every challenge,
fire-log-id membership (A3), ob-14 per-cell distinct-version count reported at
every read (A4). **All n's count GAMES, 5 per accepted match** — the unit that
CAL-6's wrap got wrong by 5×.

**CELLS RE-SELECTED FOR OVERLAP WITH THE RATED RECORD.** Every cell is an
opponent with ≥5 matches of current-era (`ourver` ≥ 125) rated volume, chosen to
span our observed rated share so a bias would show as a slope, not a shift only:

| cell | team id | current-era rated share | net Elo | continuity |
|---|---|---|---|---|
| D1 · 0033 | `74ae65ff-96ae-4da5-a43e-692eb6fee38f` | 7/30 · 23.3% | −39.4 | = CAL C5 |
| D2 · LingLing40 | `86d0b484-783c-47dc-99d9-6ed9af2794f8` | 14/35 · 40.0% | −26.4 | NEW |
| D3 · Juusto | `32087804-2dde-4265-acb2-b6ec9039fbee` | 21/40 · 52.5% | +4.2 | = CAL C2 |
| D4 · Jython | `8cf9b751-00d3-484a-b0ed-e3073ae1d46f` | 17/30 · 56.7% | +7.2 | NEW |
| D5 · Big O | `f3362833-2d7a-4636-9a3c-e4f10fcebdc1` | 15/25 · 60.0% | +15.7 | NEW |
| D6 · team lazy | `648d1d5b-5443-4257-a0aa-7048661b612d` | 26/35 · 74.3% | +66.5 | = CAL C1 |

Three cells carry CAL continuity (C1/C2/C5 keep their series); three are new.
Rated column is `corpus/league_matches.tsv` with our seat resolved per row,
filtered on our own `teamXVersion` ≥ 125 — **not a 7-day window** (see the
hazard note below). Pooled rated reference on these six: **100/195 = 51.3%.**

## Predictions, REGISTERED NOW, and the falsifier

**P1 (primary, powered).** Pooled CAL-7 share on these six cells lands within
**±8pp** of the pooled current-era rated share on the same six (51.3%), i.e. in
**43.3%–59.3%**.
**P2 (secondary, weak by construction).** Per-cell CAL-7 share lands within
±15pp of that cell's rated share on **≥4 of 6** cells. *Per-cell n=25 at the
first look gives roughly ±20pp; P2 is descriptive support, never the verdict.*
**P3 (shape).** The rank order of the six cells by panel share correlates
positively with their rank order by rated share.

**FALSIFIED — the outcome that changes what this project believes — if the panel
reads ≥10pp BELOW rated on ≥5 of 6 cells.** That is a systematic negative bias in
`FIXTURE_OF_RECORD`, and the consequence is written down before the data:
**every published CAL share becomes a lower bound needing a stated correction,
and any ship gate that ever leaned on a panel number gets re-read.**
**The reverse (panel ≥10pp ABOVE rated on ≥5 of 6) is equally a finding** and is
registered as such so the test cannot be read one-directionally.

## Look discipline (declared before data)

1. Descriptive per-cell tallies at any n, plus ob-14 churn counts.
2. **INTERIM look at n=150 (one full cycle): DESCRIPTIVE ONLY.** No verdict
   language, no P1 call. It exists to catch an instrument fault early.
3. **ONE comparative look — the P1/P2/P3 call — at n=300** (two full cycles;
   ~~pooled panel n=300 vs rated n=195 gives ~±7.5pp at 95%, which is what makes
   the ±8pp bar meaningful~~ — **⛔ SUPERSEDED BY A2: that figure was computed
   against the wrong denominator and ignored measured clustering; resolution is
   the FORMULA in A2.3, and the ±8pp band is NARROWER than the design resolves**),
   **or at the panel's stop if earlier with n≥150**,
   in which case ~~the bar widens to ±11pp~~ the resolution is recomputed from
   A2.3's formula at whatever n the look actually has, and P1 is reported with
   that half-width named beside it.
4. Prior CALs' looks stay spent. `panel_read` always with explicit `--panel`;
   note it currently supports only cal1–cal4, so CAL-5/6/7 are read by the
   fire-log script described in the CAL-6 read (positive control documented
   there: it reproduces s39's published C2 cells exactly).

## Stop conditions

* **Holder change** — panel ends at the last window fully under v140; the n≥150
  clause covers the partial case.
* **The builder needs the budget for a higher fire-order item** — this panel
  YIELDS. A committed treatment-leg prereg outranks it (see the fire order).
* **Instrument alarm:** any panel game decoding `ourver` ≠ 140 voids its window.
  CAL-6's guard fired for real (11:58:27Z, v141 displacement) and is trusted for
  having cost data, not merely for having passed.

---

## ⭐ AMENDMENT A1 — 2026-08-14T13:09:30Z, **ADD-ONLY, BLIND TO CAL-7 DATA**
*(verified at 13:09:30Z: no `scratchpad/panel_cal7*` exists, zero CAL-7 legs
fired. This amendment predates the first cell.)*

**THE DEFECT, found by this lane in its own file ~20 minutes after committing
it.** The rated reference above (`ourver` ≥ 125, pooled **51.3%**) is a
**MIXTURE OF BOTS WE SHIPPED WITH BOTS WE WITHDREW.** Rated ledger by OUR
version:

| ourver | m | g | share | NET Elo |
|---|---|---|---|---|
| 123 | 13 | 65 | 64.6% | +51.2 |
| **125** | **61** | **305** | **55.7%** | **+136.4** |
| 134 | 3 | 15 | 40.0% | −11.5 |
| 135 | 1 | 5 | 0.0% | −13.9 |
| 137 | 3 | 15 | 33.3% | −14.3 |
| 139 | 8 | 40 | 35.0% | −34.9 |
| 140 | 3 | 15 | 80.0% | +22.6 |

**The v134–139 churn band is 12 matches / 60 games at 31.7%, NET −63.1 Elo** —
and on these six cells specifically it is **9/35 = 25.7%** against v125's
**88/155 = 56.8%**. Pooling it drags the reference from 56.8% to 51.3%.
*(Positive control on this cut: v139's −34.9 over k=8 reproduces the builder's
own s38 stop-loss number exactly.)*

**AND IT INVALIDATES A CLAIM I PUT ON THE TAPE AT 13:07:32Z.** Fire order #1
named *"0033 our largest live leak, LingLing40 second-largest, neither… wait,
LingLing40 has neither book nor arm"*. Era-bounded to **v125 alone**:
* **0033 SURVIVES: 6/20 = 30.0%, −18.5 Elo over 4 matches.** A real leak in a
  single stable version. Its book and NESTSHOT2 stand.
* **⛔ LingLing40 DOES NOT: 13/25 = 52.0%, −0.4 Elo.** Its apparent −26.4 was
  **two matches played by v137 and v139**. **The "second-biggest live leak with
  no book and no arm" was an artefact of pooling our own withdrawn versions.
  Retracted.** This is the SAME era-pooling trap recorded in the hazard note
  below, committed a third time in one hour — the first two on the OPPONENT's
  side, this one on OUR OWN.

**THE AMENDMENT — and it makes the design strictly better, not merely safer.**
The confound is real either way: if CAL-7 reads 30%, "the panel is biased" and
"v140 is worse than v125" fit identically. Fix it by comparing **the same bot on
both surfaces over the same wall-clock hours**:

* **P1's rated reference becomes v140's OWN rated record at the moment of the
  look**, accumulated in parallel while CAL-7 fires. v140 has held continuously
  from ~11:59Z (see the corrected subject line) and the ladder pairs us ~3×/hour, so a 4-hour panel should see v140 reach roughly
  15 rated matches / 75 games. **Same bot, same days, two fixtures — a natural
  experiment, not a proxy.**
* **If v140's rated record is < 50 games at the look, P1 falls back to v125's
  56.8% (n=155) and MUST report the confound in the same sentence.** The
  `ourver` ≥ 125 pooled 51.3% is **RETIRED as a reference** — it is a mixture,
  and no bar is denominated in it.
* **P1's bar is unchanged at ±8pp**, now against 56.8% (fallback) ⇒ band
  **48.8%–64.8%**; against the v140 rated figure if it reaches n≥50.
  ⚠ **A2.3 governs how a band miss is READ** — on every path the ±8pp band is
  narrower than the resolution, so a miss inside resolution is UNRESOLVED.
* **P2/P3 unchanged**, with per-cell rated shares taken from the same slice as
  whichever reference P1 uses. v125-only per-cell: 0033 30.0% · LingLing40
  52.0% · Juusto 52.5% · Jython 60.0% · Big O 70.0% · team lazy 73.3%.
* **Cells unchanged** — LingLing40 stays a cell. It was selected for rated
  volume and share spread, and both still hold; only the *narrative* that it is
  a leak is retracted.

---

## ⭐⭐ AMENDMENT A2 — 2026-08-14T13:1xZ, **ADD-ONLY, DATA-BLIND**
*(Discipline statement, precise because it matters: the first CAL-7 cell fired
~13:13Z, so this is NOT pre-leg. It is **DATA-BLIND**: I have not opened
`scratchpad/panel_cal7_fires.tsv` and no CAL-7 result has reached me.
**It ADDS and it CHANGES NOTHING REGISTERED** — P1's ±8pp band, its cells, its
reference and its falsifier all stand exactly as written. Raised by the SIDE
LANE's pre-leg audit; the arithmetic error is mine.)*

### A2.1 — **MY RESOLUTION FIGURE WAS WRONG. ±7.5pp WAS COMPUTED AGAINST THE WRONG DENOMINATOR.**
The look-discipline line claims *"pooled panel n=300 vs rated n=195 gives ~±7.5pp
at 95%"*. **It does not.** ±7.5pp is what the two-proportion formula returns for
n=300 vs **n=395** — the FULL current-era rated record — but P1's registered
reference is the **six-cell pool, n=195**. Correct naive value: **±9.0pp**.
*(Side lane's internal check, and it is the right one: my n≥150 branch quoted
±11pp and reproduces at ±10.6pp, which is how the n=300 line is identifiable as
the slip rather than the formula.)*

### A2.2 — **AND THE NAIVE FIGURE IS ITSELF WRONG, BECAUSE GAMES ARE NOT INDEPENDENT. MEASURED, NOT ASSUMED.**
Every games-level interval in this repo treats 5 games of a match as 5
independent trials. They are not — they share the opponent, the opponent's
version, and one 20-minute slice of the ladder. Measured on our own record by
comparing the observed variance of match scores against the binomial:

| fixture | 5-game matches | our share | **design effect** | implied ICC ρ |
|---|---|---|---|---|
| **RATED** (`ladder_games.tsv`) | 965 | 0.5123 | **1.529** | 0.132 |
| **UNRATED** (`meta_join`, non-ladder) | 947 | 0.4809 | **1.833** | 0.208 |

⇒ **Effective n is ~55–65% of nominal.** 300 unrated games ≈ **164 effective**;
195 rated games ≈ **128 effective**. Corrected resolutions, each side carrying
its own measured DEFF:

| comparison | naive | **measured-DEFF** |
|---|---|---|
| panel 300 vs six-cell `ourver`≥125 pool 195 | ±9.0pp | **±11.6pp** |
| panel 300 vs v125-only pool 155 (A1 fallback) | ±9.6pp | **±12.3pp** |
| panel 300 vs v140's own rated ~75 (A1 primary) | ±12.5pp | **±15.8pp** |
| panel 150 vs v125-only pool 155 (early-stop) | — | **±14.4pp** |

### A2.3 — **THE PRE-COMMITTED READING FOR THE ANNULUS (this is the ADD)**
P1's ±8pp band is **narrower than the design can resolve**. Registered now,
before any result is seen:
* **Inside ±8pp → P1 HOLDS.**
* **Outside ±8pp but inside the measured-DEFF resolution (±11.6 / ±12.3 / ±15.8
  as applicable) → UNRESOLVED. NOT falsified, NOT confirmed.** This annulus is
  written down in advance precisely so it cannot be read as a result afterwards.
* **Outside the resolution band → the pooled test carries.**
**Every P1 sentence must quote its reference, its n, its DEFF-corrected
half-width, and which of the three readings applies.** A bar without its
estimator is not a bar.

**⭐ AND THE RESOLUTION IS PRE-COMMITTED AS A FORMULA, NOT A NUMBER** (side
lane's fix, adopted — a number only covers the n we projected; the formula
covers whatever n the look actually has):

```
half_width_95 = 1.96 * sqrt( p̄(1-p̄) * ( DEFF_unrated/n_panel + DEFF_rated/n_ref ) )
    p̄       = pooled share across both surfaces at the look
    DEFF_unrated = 1.833 , DEFF_rated = 1.529   (measured, A2.2)
    n_panel, n_ref in GAMES
```
**Estimator named, as obliged: two-proportion, GAMES-level, with the measured
5-per-match clustering carried explicitly.** Setting both DEFFs to 1 reproduces
the naive figure and is the wrong answer; it is shown only so a reader can see
the size of the correction. **If the ±8pp band edge lies inside this half-width
— which it does on every path projected — a band miss reads UNRESOLVED, never
falsified, in both directions.**

### A2.4 — **P4 ADDED (a test the budget CAN resolve; P1 is untouched)**
The pooled comparison is weak; the *shape* comparison is not. **P4: on how many
of the six cells does the panel read BELOW its rated reference, and by ≥10pp?**
Under no bias, P(≥5 of 6 in one direction) = **0.109**; P(6 of 6) = **0.016** —
stated as the test's own alpha *before* the ≥10pp magnitude filter, which makes a
false positive materially rarer than that. **This is a restatement of the
falsifier already registered (≥10pp below on ≥5 of 6), promoted to a named test
with its null probability written down.** It adds no new decision rule.

### A2.5 — precision fix on the stability-gate basis
The subject line says *"holder since 11:37Z"*. CAL-6's own fire log contradicts
it: an **ABORT at 11:58:27Z with holder = v141**. Both primaries agree — the elo
tape flips v139→v140 between the 11:35Z and 11:40Z polls, and `ship_watch` reads
v140 again at 11:59:21Z: a ~1-minute sub-cadence displacement the poll tapes
cannot see. **The gate conclusion is UNCHANGED — continuous v140 tenure runs from
~11:59Z, i.e. 65+ minutes at the 13:04:29Z holder read, comfortably past the
40-minute bar.** Only the basis sentence was wrong, and two documents in one
commit must not disagree about it.

### A2.6 — routing beyond this prereg
**The design effect is NOT a CAL-7 fact. It is a repo-wide instrument fact** and
is promoted to the coordination tape: *every* bar, MDE and confidence interval
this project has denominated in GAMES is optimistic by ~1.24× (rated) to ~1.35×
(unrated) in its half-width. That includes the CLAUDE.md figures for the 25-game
window swing and the ~39pp MDE.

---

## ⭐ AMENDMENT A3 — 2026-08-14T13:2xZ, **ADD-ONLY, DATA-BLIND**
*(Same discipline statement as A2, and the same certification basis: I have not
opened `panel_cal7_fires.tsv`. Both items below were raised by the SIDE LANE;
item A3.1 I had reached independently and item A3.2 I had not.)*

### A3.1 — **THE DESIGN EFFECT SPLITS, AND A SINGLE REPO-WIDE MULTIPLIER OVERCORRECTS A STRATIFIED PANEL**
A2.2's DEFF was computed with a **pooled** share, which absorbs
**between-opponent mixture variance** — and our six cells span 23%–74% rated
share, so that mixture is large and it is **not** a nuisance when the comparison
is stratified by opponent *by design*. Recomputed within opponent (each match
against its own opponent's p̂, residual variance with the K estimated means
removed):

| fixture | **POOLED DEFF** (between + within) | **WITHIN-OPPONENT DEFF** |
|---|---|---|
| RATED (965 matches, 59 opponents n≥2) | **1.529** | **1.366** |
| UNRATED (947 matches, 43 opponents n≥2) | **1.833** | **1.434** |

**THE SCOPE RULE, and it is now part of this prereg:**
* **A pooled share over a MATCHMADE record** (the ladder picks the mix, so the
  mix genuinely is sampling variance) → **pooled DEFF, 1.529 / 1.833.**
* **A stratified or per-cell bar** (we chose the cells, so the mix is fixed by
  design and must not be charged as noise) → **within-opponent DEFF, 1.366 /
  1.434.** **CAL-7's panel side is stratified by construction ⇒ `DEFF_u` in
  A2.3's formula is 1.434, not 1.833.**

⚠ **ESTIMATOR NOTE, because the two lanes got different numbers and the
difference is real.** The side lane independently reproduced the pooled figure
digit-for-digit (1.529, ρ=0.132) and reported within-opponent **1.282**. That is
the **same statistic without the degrees-of-freedom correction**: subtracting K
estimated opponent means from N observations costs K df, and
`1.282 × 960/(960−59) = 1.366` recovers this file's number exactly. **The
df-corrected value is the correct one and the uncorrected one is biased low**;
1.366 / 1.434 are what this prereg uses.

### A3.2 — ⛔ **THE SAME-BOT PATH FROM A1 COMPARES TWO DIFFERENT OPPONENT POPULATIONS. POOLED P1 REVERTS TO THE MIX-MATCHED REFERENCE.**
A1 made P1's reference *"v140's OWN rated record at the moment of the look"* to
get a same-bot comparison. **The side lane's flag is correct and I had not seen
it: v140's rated record is drawn from the WHOLE ladder, while the panel is six
chosen cells.** Under **zero** fixture bias those two shares differ by the
**mix difference alone** — a bias term, not a variance term, so **the annulus
cannot absorb it.**

The offered fix of post-stratifying v140's rated shares onto the six cells
**is not viable at this n**: at the 13:04Z read v140 had 3 rated matches and only
one of them (Jython) is a CAL-7 cell. A 4-hour panel projects ~15 v140 matches
spread over ~12 opponents; post-stratified, most cells would rest on 1–2 matches.
**Reweighting to empty cells is not a fix, it is a fabrication.** Therefore:

* **POOLED P1 REVERTS TO THE MIX-MATCHED REFERENCE — v125's six-cell pool,
  88/155 = 56.8%** (same six opponents, same weights, so the mixture cancels by
  construction). Band unchanged at ±8pp ⇒ 48.8%–64.8%, read through A2.3's
  annulus with `DEFF_u = 1.434`, `DEFF_r = 1.366`.
* **THE SAME-BOT v140 COMPARISON IS DEMOTED TO A PER-CELL SUPPLEMENTARY READ** —
  reported only for cells where v140 accrues ≥10 rated games by the look, each
  with its own n, explicitly underpowered, and **never pooled across cells.**
  It is kept because it is the only same-bot, same-cell, two-fixture contrast
  that exists, and it accumulates for free while the panel fires.

**AND THE COST IS STATED, NOT HIDDEN: this re-opens the confound A1 was written
to dissolve.** With a v125 reference, *"the panel is biased low"* and *"v140 is
worse than v125"* fit a low pooled reading identically. **The pooled test cannot
separate them and this prereg no longer claims it can.** What can: **P4** (the
directional 5-of-6 test) plus the per-cell supplementary above, accumulating over
successive panels. A design that admits what it cannot resolve is worth more than
one that resolves it by assumption.

---

## ⛔⛔ AMENDMENT A4 — 2026-08-14T13:3xZ, **ADD-ONLY, DATA-BLIND. THE FALSIFIER GETS A READ-PATH, BECAUSE A3.2 OVERCLAIMED WHAT P4 CAN DO.**
*(Side lane flag; correct, and it lands on a sentence I wrote one paragraph after
congratulating myself for naming what the design cannot resolve. Same blindness
basis as A2/A3.)*

**THE ERROR.** A3.2 closes: *"What can [separate them]: **P4** (the directional
5-of-6 test) plus the per-cell supplementary."* **P4 cannot.** A v140 that is
uniformly ~10pp worse than v125 across all six cells produces the **identical**
5-of-6-low signature as a panel biased ~10pp low. **Directional coherence
separates SYSTEMATIC from NOISE. It has never separated FIXTURE-BIAS from
VERSION-REGRESSION, and both hypotheses predict the falsifier fires.**
**The only discriminator in this design is the version-matched per-cell
supplementary.**

**WHY THIS MATTERS MORE THAN A WORDING FIX.** The falsifier's registered
consequence is *"every published CAL share becomes a lower bound needing a
stated correction, and any ship gate that ever leaned on a panel number gets
re-read."* **Unqualified, a simple v140 regression would trigger a repo-wide
correction factor applied to every historical CAL number — the most expensive
misreading this leg could produce, and it would be produced BY the leg's own
registered consequence.**

**THE READ-PATH, REGISTERED NOW.** If the falsifier fires (panel ≥10pp below
reference on ≥5 of 6 cells), the consequence is gated on the supplementary:

| supplementary state at the look | reading |
|---|---|
| ≥1 cell with **≥10 v140 rated games** reads **LOW in the panel relative to v140's own rated share on that same cell** | **FIXTURE BIAS** — the registered consequence FIRES. |
| such cells read **IN LINE with v140's rated share** | **NOT fixture bias — v140 REGRESSION vs v125.** The consequence **does NOT fire**; no historical CAL number is corrected; the finding routes to the ship gate instead. |
| **NO cell reaches ≥10 v140 rated games** at the look | **UNRESOLVED. The falsifier's ATTRIBUTION carries to the next panel.** The pattern is recorded; **the consequence does not fire on an unattributed pattern.** |

The mirror case (panel ≥10pp ABOVE on ≥5 of 6) takes the same three-way path
with the directions reversed, so the test stays two-sided.

**AND THE HONEST ADMISSION THIS TIME, stated where it cannot be skipped: on
current projections the third row is the LIKELY one.** v140 held 3 rated matches
at 13:04Z and accrues ~3/hour; a 4-hour panel puts ~15 matches across ~12
opponents, so **reaching ≥10 rated GAMES on any single cell is not assured.**
⇒ **The most probable honest outcome of CAL-7 is a recorded pattern with an
UNRESOLVED attribution that a later panel closes.** That is a smaller result than
this prereg's opening section implies, and it is written down **before** the data
so it cannot be re-described afterwards as what we meant all along.

## Target-value line

Unrated, **zero rated exposure, zero submits** (v140 already holds) ⇒ payout gate
N/A. Band re-read before any payoff sentence. ⚠ Note for anyone pricing a CAL-7
cell: **`tools/target_value.py`'s cached table is stale on fast risers** — it
carries Juusto at ~1815 while `league_matches`'s newest non-null observation
(2026-08-14T12:32Z) reads **1883.3**. Price Juusto at 1883, not off the band print.

## ⚠ HAZARD THIS PREREG INHERITS, NAMED SO THE READER DOES NOT REPEAT IT

The rated reference column above is **era-bounded on OUR side (`ourver` ≥ 125)**
and that is load-bearing. A 7-day pooled cut of our rated record is **void**: our
own bot moved ~90 versions inside such a window. Measured while building this
file — a 7-day cut names Ouroboros (−291.8), Lunds Stallions (−205.1) and Kings
College Munich (−139.4) as our three biggest leaks, **−636 Elo**; era-bounded,
**all three vanish** (we have not been paired with Ouroboros or KCM since
2026-08-10, and every Lunds row since 2026-08-10 is positive: +21.0, +19.5, +3.3,
+2.4). The same trap fired on Leviathan in the same hour (week-pooled 47.7% vs
same-era 24.0%). **Era-bound both sides, or the cut is fiction.**
