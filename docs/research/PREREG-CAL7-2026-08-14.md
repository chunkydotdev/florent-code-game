# PREREG — CAL-7, **THE DIVERGENCE PANEL** (research s40, 2026-08-14)

**STATUS: committed BEFORE any CAL-7 leg creation** (two-clock standard; the side
lane certifies the first leg's platform `createdAt` against this file's commit
time). Research designs and schedules; **the BUILDER executes every match**;
verdict sentences are the builder's.

**Subject / EXPECT: v140 "Loki v10" (`bots/_v223sealrepair`).** Live-verified
`Active bot: v140 (Loki v10)`, rating 1741, 966 matches, at
**2026-08-14T13:04:29Z** off `fcode status` — read, not asserted. Runner
`INCUMBENT=140`. Stability gate (R3) MET: holder since 11:37Z (>40 min, two
pairing cycles) and the tree is identified.

---

## ⛔ WHY THIS IS NOT CAL-6 REPEATED

`docs/research/CAL6-READ-2026-08-14.md` (the declared CAL-6 look, spent at its
stop, n=75) reports **three consecutive panels reading 30.0% (v125) → 31.0%
(v139) → 29.3% (v140)** on the identical six cells — and the 95% interval on the
CAL-4-vs-CAL-6 difference is roughly **±13pp**. A ship worth +3–5pp of game share
**cannot be seen by this design.** Three flat reads are three nulls the design
guaranteed. Firing the same six cells a fourth time buys a fourth guaranteed null
at the cost of four hours of the only live fixture we have.

Two further facts re-point the panel:

1. **The old cells are not where the rating goes.** Over the CURRENT era
   (`ourver` ≥ 125, i.e. since 2026-08-13 ~10:16Z; 79 matches / 395 games,
   52.4% share, **net +84.5 Elo**), the six CAL-4/5/6 cells contain one real
   leak (0033 **−39.4**), one mild one (HTTP 418 −11.7) and two net-POSITIVE
   cells (team lazy **+66.5**, farming_200s). **C3 Leviathan has read 1/25, 2/25,
   0/15 — 3/65 across three panels.** A cell pinned at a floor carries no
   information about our ship and is dropped (routed instead to a book
   commission; they are 1997.8 and out-developing us).
2. ⭐ **The panel and the ladder disagree on the same opponents.** Juusto: panel
   **20.0%**, current-era rated **52.5%** (8 matches, +4.2 Elo). team lazy: panel
   **53.3%**, rated **74.3%**. **If the panel reads systematically low, every CAL
   number this project has published needs a correction factor** — a larger fact
   than any single ship, and one the unrated budget can actually resolve.
   The cheap explanation is already closed with its control: **seat is balanced
   in both fixtures** (rated A 52.3% / B 48.5%; unrated A 49.3% / B 46.2%, over
   8,338 archived games of ours) and a ~3pp seat effect cannot make 32pp.

## THE QUESTION CAL-7 ASKS

**Does the unrated fixture — our declared `FIXTURE_OF_RECORD` — measure the same
quantity the rated ladder pays for, on the same opponents in the same era?**

## Design

Inherits `PREREG-CAL6-2026-08-14.md` (itself CAL-5 → V134-CHAR as amended
through A4) for all mechanics: **UNPINNED** (a calibration panel measures
relevance; pinning reintroduces the staleness the panel exists to detect),
5 accepts/cell/cycle cap, **rotating start cell**, rate-limit rejections wait out
the window and retry the same cell, holder asserted before every challenge,
fire-log-id membership (A3), ob-14 per-cell distinct-version count reported at
every read (A4). **All n's count GAMES, 5 per accepted match** — the unit that
CAL-6's wrap got wrong by 5×.

**CELLS RE-SELECTED FOR OVERLAP WITH THE RATED RECORD.** Every cell is an
opponent with ≥5 matches of current-era (`ourver` ≥ 125) rated volume, chosen to
span our observed rated share so a bias would show as a slope, not a shift only:

| cell | team id | current-era rated share | net Elo | continuity |
|---|---|---|---|---|
| D1 · 0033 | `74ae65ff-96ae-4da5-a43e-692eb6fee38f` | 7/30 · 23.3% | −39.4 | = CAL C5 |
| D2 · LingLing40 | `86d0b484-783c-47dc-99d9-6ed9af2794f8` | 14/35 · 40.0% | −26.4 | NEW |
| D3 · Juusto | `32087804-2dde-4265-acb2-b6ec9039fbee` | 21/40 · 52.5% | +4.2 | = CAL C2 |
| D4 · Jython | `8cf9b751-00d3-484a-b0ed-e3073ae1d46f` | 17/30 · 56.7% | +7.2 | NEW |
| D5 · Big O | `f3362833-2d7a-4636-9a3c-e4f10fcebdc1` | 15/25 · 60.0% | +15.7 | NEW |
| D6 · team lazy | `648d1d5b-5443-4257-a0aa-7048661b612d` | 26/35 · 74.3% | +66.5 | = CAL C1 |

Three cells carry CAL continuity (C1/C2/C5 keep their series); three are new.
Rated column is `corpus/league_matches.tsv` with our seat resolved per row,
filtered on our own `teamXVersion` ≥ 125 — **not a 7-day window** (see the
hazard note below). Pooled rated reference on these six: **100/195 = 51.3%.**

## Predictions, REGISTERED NOW, and the falsifier

**P1 (primary, powered).** Pooled CAL-7 share on these six cells lands within
**±8pp** of the pooled current-era rated share on the same six (51.3%), i.e. in
**43.3%–59.3%**.
**P2 (secondary, weak by construction).** Per-cell CAL-7 share lands within
±15pp of that cell's rated share on **≥4 of 6** cells. *Per-cell n=25 at the
first look gives roughly ±20pp; P2 is descriptive support, never the verdict.*
**P3 (shape).** The rank order of the six cells by panel share correlates
positively with their rank order by rated share.

**FALSIFIED — the outcome that changes what this project believes — if the panel
reads ≥10pp BELOW rated on ≥5 of 6 cells.** That is a systematic negative bias in
`FIXTURE_OF_RECORD`, and the consequence is written down before the data:
**every published CAL share becomes a lower bound needing a stated correction,
and any ship gate that ever leaned on a panel number gets re-read.**
**The reverse (panel ≥10pp ABOVE rated on ≥5 of 6) is equally a finding** and is
registered as such so the test cannot be read one-directionally.

## Look discipline (declared before data)

1. Descriptive per-cell tallies at any n, plus ob-14 churn counts.
2. **INTERIM look at n=150 (one full cycle): DESCRIPTIVE ONLY.** No verdict
   language, no P1 call. It exists to catch an instrument fault early.
3. **ONE comparative look — the P1/P2/P3 call — at n=300** (two full cycles;
   pooled panel n=300 vs rated n=195 gives ~±7.5pp at 95%, which is what makes
   the ±8pp bar meaningful), **or at the panel's stop if earlier with n≥150**,
   in which case the bar widens to ±11pp and P1 is reported with that bar named.
4. Prior CALs' looks stay spent. `panel_read` always with explicit `--panel`;
   note it currently supports only cal1–cal4, so CAL-5/6/7 are read by the
   fire-log script described in the CAL-6 read (positive control documented
   there: it reproduces s39's published C2 cells exactly).

## Stop conditions

* **Holder change** — panel ends at the last window fully under v140; the n≥150
  clause covers the partial case.
* **The builder needs the budget for a higher fire-order item** — this panel
  YIELDS. A committed treatment-leg prereg outranks it (see the fire order).
* **Instrument alarm:** any panel game decoding `ourver` ≠ 140 voids its window.
  CAL-6's guard fired for real (11:58:27Z, v141 displacement) and is trusted for
  having cost data, not merely for having passed.

## Target-value line

Unrated, **zero rated exposure, zero submits** (v140 already holds) ⇒ payout gate
N/A. Band re-read before any payoff sentence. ⚠ Note for anyone pricing a CAL-7
cell: **`tools/target_value.py`'s cached table is stale on fast risers** — it
carries Juusto at ~1815 while `league_matches`'s newest non-null observation
(2026-08-14T12:32Z) reads **1883.3**. Price Juusto at 1883, not off the band print.

## ⚠ HAZARD THIS PREREG INHERITS, NAMED SO THE READER DOES NOT REPEAT IT

The rated reference column above is **era-bounded on OUR side (`ourver` ≥ 125)**
and that is load-bearing. A 7-day pooled cut of our rated record is **void**: our
own bot moved ~90 versions inside such a window. Measured while building this
file — a 7-day cut names Ouroboros (−291.8), Lunds Stallions (−205.1) and Kings
College Munich (−139.4) as our three biggest leaks, **−636 Elo**; era-bounded,
**all three vanish** (we have not been paired with Ouroboros or KCM since
2026-08-10, and every Lunds row since 2026-08-10 is positive: +21.0, +19.5, +3.3,
+2.4). The same trap fired on Leviathan in the same hour (week-pooled 47.7% vs
same-era 24.0%). **Era-bound both sides, or the cut is fiction.**
