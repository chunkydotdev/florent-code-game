# PREREG — **EVICT58 LIVE LEG** vs 0033: a DOSE-AND-MECHANISM probe, and it is registered as one (research s40, 2026-08-14)

**STATUS: committed BEFORE any challenge is issued** (two-clock standard; side
lane certifies the first match's platform `createdAt` against this commit).
Research designs and schedules; **the BUILDER executes every match**; the verdict
sentence is theirs.

**Trigger met:** `EVICT58`'s harm gate is **CLEAN at 49.20 [46.1, 52.3]** on its
1,000 target — flat, exactly the harm-only reading its registration specified.
**Fire order #1 item 1 unblocks.**

---

## ⛔ WHAT THIS LEG CAN AND CANNOT BUY — STATED FIRST, BECAUSE IT DETERMINES THE WHOLE DESIGN

**A 5-match window is 25 games. At the unrated within-opponent DEFF of 1.434, a
share read on 25 games carries a 95% half-width of ±23.5pp.** ⇒ **THIS LEG
CANNOT DELIVER A WIN-RATE VERDICT AND WILL NOT CLAIM ONE.** `CLAUDE.md` says it
directly: *"A 25-game window is a DOSE AND MECHANISM probe. A currency read
requires pooling windows."*

**⇒ REGISTERED AS A DOSE-AND-MECHANISM PROBE.** What it buys is countable rather
than estimated: **does the eviction mechanism fire against a real heal-heavy
opponent, and does it interrupt what it is aimed at.** Those are counts off the
wire, not rates needing power.

⚠ **AND THERE IS NO MATCHED CONTROL ARM, WHICH I AM NAMING RATHER THAN
DISGUISING.** A proper matched pair fires both arms in the same window against
the same pinned opponents — **that doubles the rated exposure and needs a second
activation.** Our existing v140-vs-0033 games (CAL-7's D1 cell, 20 games) were
**UNPINNED**, so they are not a matched control. ⇒ **no comparative currency
claim is available from this leg, and none is registered. The comparative read
is a LATER, SEPARATE leg.**

## SUBJECT AND FIXTURE
* **Arm: `EVICT58`** — the forward eviction launcher (`#58`), conditional plant
  gated on a live forward sentinel, cap 1, Ti floor 40, forward-mode throw key.
* **Opponent: `0033`** — the named customer from `BOOK-0033-2026-08-14.md` (their
  v43 flip was a **21× heal increase**; heal-staffing is this mechanism's
  trigger) and **our worst matchup in the opponent-segment map (23.3% @ 30).**
* **PINNED** (`fcode match unrated <team> --match <past_match_id>`) — **treatment
  legs pin; panels never do.** ⛔ **A pinned triple whose decoded `oppver` values
  DIFFER is an INSTRUMENT ALARM: report it and do not read that cell.**
* **5 matches = 25 games. ONE rate-limit window.**

## ⛔ THE EXPOSURE RULE — THIS IS A SUBMIT-HOLD LEG
`fcode match unrated` plays our **ACTIVE submission**, so this leg requires
activating the EVICT58 tree. **`fcode submit` AUTO-ACTIVATES.** ⇒
1. **The submit happens INSIDE the firing window, never before it** — an upload
   "to be ready" is the mistake that put a prototype on the rated ladder in s29.
2. **Fire just AFTER an observed pairing**, re-deriving the cadence from recent
   rows rather than assuming the offset — it has shifted at least once.
3. **Roll back on the fifth accepted challenge and VERIFY the holder on the
   `Active bot:` line**, never on an exit code.
4. **Budget the leg at roughly −8 Elo per leaked rated match, not at zero.**
   One window is the minimum exposure that buys the read; **it is the reason the
   leg is 5 matches and not 10.**

## METRICS
1. **PRIMARY — DOSE, off the wire: evictions per game** (`throws.tsv`, EXILE
   kind, thrown bot theirs). ⛔ **The self-play dose was THIN because their
   heal-staffing is the trigger and our own control does not staff — so the live
   dose IS the question, not a precondition.** **If evictions/game ≈ 0 against a
   heal-heavy opponent, the mechanism does not fire where it was aimed and `#58`
   is refuted as designed.**
2. **MECHANISM — feed interruption:** their heal actions within d²≤8 of their own
   core during our siege window, and rebuild latency of their ring pieces after
   we kill one. *(The `#47`-family metric; `BOOK-0033` supplies the baseline —
   their heals ran 110.45/100 rounds at the v43 flip.)*
3. **DESCRIPTIVE ONLY — game share, printed WITH its ±23.5pp half-width in the
   same sentence.** **No bar. No pass/fail. It cannot resolve and will not be
   quoted as though it could.**

## PREDICTIONS
**P1 (primary):** evictions/game **> 1.0** against 0033 — the mechanism fires
where its trigger exists.
**P2 (mechanism):** their core-adjacent heal actions during our siege fall
relative to `BOOK-0033`'s baseline. ⚠ **P2 is weaker than P1 by construction: the
baseline is from a different era and fixture, so a miss is uninformative while a
hit is suggestive.** Stated so the asymmetry is not read the wrong way round.

## FALSIFIER
**P1 ≈ 0 refutes `#58` as designed** — a forward eviction launcher that does not
evict against the heal-heaviest opponent in our band has no customer. **The row
would close, and the field evidence that three opponents run this mechanism
defensively would stand as proof of the MECHANISM but not of this APPLICATION** —
which is the caveat `#58` was admitted with.

## MAP-SEGMENT LINE (OBLIGATION 15a)
**MAP SEGMENT: none expected.** The trigger is opponent heal-staffing, not
terrain; no mechanism predicts a map dependence. **Maps are not pinned and not
read as a segment.** ⚠ *If the dose varies wildly by map, that is a post-hoc
observation needing its own leg — declared here so it cannot later be presented
as pre-registered.*

## TARGET-VALUE LINE
**0033 at the last band read: rating ~1837, gap +81 on our 1765, a 5-0 pays
≈ +19.7 and a 0-5 costs ≈ −12.3.** **Reachable, top-half of the band.**
⚠ **Band re-read at fire time — the cached table was ~65 points stale on a fast
riser this morning.**
