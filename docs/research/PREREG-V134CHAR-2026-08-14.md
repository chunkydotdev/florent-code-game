# PREREG — V134-CHAR characterization panel (research s39, 2026-08-14)

**STATUS: committed BEFORE any leg creation (two-clock standard; side lane
certifies against platform `createdAt`).** Research designs; the BUILDER
executes every match (research hard limit: never fires).

## What this is
A **descriptive characterization panel** of the CURRENT HOLDER **v134**
(x3r0's authorized run, live since 2026-08-14T06:24:45Z), filling
otherwise-idle 20-minute unrated windows while MB/MC/MD are held on their
holder gates. It feeds the analysis task Magnus armed at 05:54:23Z (report
v134 behaviour + performance vs v125's equilibrium baseline).

**It requires ZERO submits and ZERO activations** — v134 already holds the
slot, so there is no leak surface and no displacement of a teammate's run.
Rated cost: 0 by construction.

## Design
- ⛔ **AMENDMENT A2 (2026-08-14 ~07:1xZ, BEFORE any window fired — builder
  caught a factual error, verified by me at the primary before amending):**
  the original text below named lazy/Leviathan/LingLing40/Juusto/Coreflood as
  "the CAL-4 cell set" with "n=40/cell". **Both claims were WRONG** — that
  five is the rc8.x pinned LEG family; LingLing40 and Coreflood were CAL-3
  cells, and n=40/cell is a CAL-3 figure. The true CAL-4 set, verified at
  `tools/panel_cal1.sh:28-36` and `scratchpad/panel_cal4_fires.tsv`, is SIX
  cells at 5 accepts/cell (25 games/cell). **The panel runs on the TRUE CAL-4
  six so the comparability rationale actually holds:**
- **Cells (6):** team lazy · Juusto · Leviathan · HTTP 418 · 0033 ·
  farming_200s — the CAL-4 cell set (C1-C6, including C3 Leviathan as the
  D13 floor-pinned REFERENCE cell whose deltas are not readable), chosen for
  comparability with v125's CAL-4 equilibrium surface (25 games/cell, 5
  accepts × 5).
- **Cap:** 5 accepted matches per cell (25 games/cell, 150 games total) —
  CAL-4 symmetry; the stop conditions below can end it earlier.
- **UNPINNED**, per the pinning spec (`SPEC-opponent-pinning-2026-08-13.md`):
  this is a panel, not a treatment leg — opponent churn is signal. Decoded
  `oppver` is recorded per game as always.
- **Cadence:** 5 games (one full cell rotation) per 20-minute window, rotating
  the starting cell each window so retry-exhaustion drops cannot bias one
  opponent. Fires only in windows the builder is not using for held legs.
- **Stop condition:** v125's return to the slot (the panel measures the
  holder; a holder change ends it — final window is the last one fully under
  v134), or Magnus/x3r0 asking for the slot's data flow to stop.

## Read rules (declared now, before data)
1. **Descriptive at any n.** Per-cell tallies and behaviour decode (spawn
   counts, scale trajectory, harvester timing, launcher rounds, forward
   sentinels — the builder's nine registered wire predictions are graded by
   the BUILDER's task; this panel only supplies games).
2. **No comparative sentence against v125's CAL-4 figures below pooled
   n≥50**, and any such sentence carries the era caveat (CAL-4 cells are
   n=40/cell, measured under v125, opponents unpinned in both surfaces —
   version drift on their side can contribute).
   **AMENDMENT A1 (ADD-only, 2026-08-14 ~07:0xZ, before any window fired, on
   the side lane's audit flag):** a comparative read of V134-CHAR vs CAL-4 is
   a LOOK and needs its **own pre-committed look declaration** — n and the
   question fixed in a dated commit BEFORE the read. Rule 1's descriptive
   tallies never license the comparison; without a declared look the
   comparison does not happen, however interesting the numbers get.
3. **CAL-4's own comparative look stays SPENT** — this panel never licenses a
   re-read of CAL-4's prereg question. `panel_read` is always called with an
   explicit `--panel` argument for this panel.
4. **No ship/slot verdict language from this lane.** The slot call is
   Magnus's/x3r0's; the deliverable is the report.

**AMENDMENT A3 (ADD-only, committed 2026-08-14T07:10:01Z per its commit
clock — corrected from a hand-approximated "~07:2xZ" stamp on the side
lane's flag; before window 1's decode):**
the panel decode keys **STRICTLY on the runner's own fire-log match ids**
(its fires.tsv), never on "unrated matches with `ourver=134` in the window".
Reason, verified at `fcode match list --mine`: four unrated v134 matches
(34e21276 · b23326e1 · 1acd4931 · a8ca2878, platform `createdAt`
06:26:58-06:27:27Z; the CLI list's displayed "06:28/06:29" is a different,
later clock, same four ids either way; initiator
unobservable — plausibly x3r0's own test burst) already exist, **two of them
vs panel cells (Juusto 0-5, 0033 1-4)** under the A2-corrected set. Those
are **NOT panel games** and must not pool into any V134-CHAR cell —
membership is by match id from the runner's fire log, never by
(opponent, date). This is the s36 panel-contamination incident class,
closed at the design layer this time.

**AMENDMENT A4 (ADD-only, same commit — side-lane ob-14):** at every read,
report per-cell the opponent's **distinct-version count over the preceding
24h** (free off `league_matches.tsv`). A high-churn cell is **reportable,
not poolable**, and the ob-14 number is what makes that call at read time
rather than by feel.

## Falsifier / what would make this panel worthless
If v125 returns before one full window completes, the panel produced nothing
— acceptable, cost 0. If decoded `ourver` in any panel game ≠ 134, that is an
INSTRUMENT ALARM (holder changed mid-window): report, do not read that
window.

## Target-value line (template requirement)
Unrated panel, zero rated exposure ⇒ `target_value` payout gate N/A. Band at
write time (07:03Z read): 15 admissible at our 1790; panel cells team lazy
(+4), Juusto (+25), HTTP 418 (−5), 0033 (+26), farming_200s (+66) sit
in-band; Leviathan is out-of-band but kept as the CAL-4 D13 reference cell.
*(A2 updated this list with the corrected cell set.)*
