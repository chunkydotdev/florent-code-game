# PREREG — V135-CHAR characterization panel (research s39, 2026-08-14)

**STATUS: committed BEFORE any V135-CHAR leg creation** (two-clock standard;
side lane certifies against platform `createdAt`). Research designs; the
BUILDER executes every match.

**This is a NEW prereg, not an amendment** — V134-CHAR's holder-change stop
condition FIRED at x3r0's v135 ship (holder verified live by this lane at
`fcode status`: `Active bot: v135`, 1786, 949 matches). Per the retarget
rule a new subject gets a new prereg; window 1 (from createdAt 07:08:52Z) is
V134-CHAR's final and only v134 data, reportable descriptively under its own
prereg.

## Design — V134-CHAR's design INHERITED BY REFERENCE, subject re-pointed
Identical to `PREREG-V134CHAR-2026-08-14.md` as amended through A4, with:
- **Subject: v135** (x3r0's follow-up, authorized run). Runner `INCUMBENT=135`.
- Same six CAL-4 cells, same 5-accepts/cell cap (25 games/cell, 150 total),
  same rotation, same unpinned-panel rule, same fire-log-id membership (A3),
  same ob-14 churn reporting (A4), same look discipline (A1: any comparative
  read vs CAL-4 — or vs V134-CHAR's window 1 — needs its own pre-committed
  look declaration).
- **Stop conditions:** any holder change (a further x3r0 version, or v125's
  return) ends it at the last fully-under-v135 window; each successor
  version, if a panel is still wanted, gets its own prereg instantiated from
  the V134-CHAR template — versions are never pooled without a declared look.
- **Instrument alarm:** any panel game decoding `ourver` ≠ 135 voids its
  window (report, do not read).

## Read rules
As V134-CHAR rules 1–4 (descriptive at any n; comparative sentences need
pooled n≥50 + era caveats + their own look per A1; CAL-4's spent look stays
spent; no ship/slot verdict language — the slot call is Magnus's/x3r0's).

## Target-value line
Unrated panel, zero rated exposure, zero submits (v135 already holds) ⇒
payout gate N/A. Band as of the 07:03Z boot read stands; re-read before any
payoff sentence is written.
