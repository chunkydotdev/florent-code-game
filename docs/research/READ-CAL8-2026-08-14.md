# CAL-8 — THE ONE REGISTERED READ

**2026-08-14, research arm (s42).** Prereg `docs/research/PREREG-CAL8-2026-08-14.md`
(`7fa94cb`, 15:33:08Z, pre-leg) + amendment A1 (add-only). Instrument
`scratchpad/cal8_read.py`, written blind at 13-of-15 accepts, side-lane certified `df54ea7`.

**THIS IS THE ONE COMPARATIVE LOOK, TAKEN UNDER THE STOP BRANCH.** Look discipline §2 fires
at n=150 **or at the panel's stop if earlier with n≥75**. This is the **stop branch at
n=75**, not the n=150 branch — so the interval is the n=75 one. **CAL-8's rows are now
SPENT: no pooling, no top-up.**

---

## 0. VERDICT

| | |
|---|---|
| **P4 (PRIMARY, six-cell sign test)** | **DOES NOT FIRE** — 2 of 6 below, 1 of 6 above; 5 needed either way |
| **P1 (DESCRIPTIVE, no bar)** | panel **45.3%** (34/75) vs reference **56.8%** (88/155), **−11.4pp**, which **does not exclude zero** at ±17.8pp |
| **holder guard** | **CLEAN** — all 75 panel games at `ourver=140` |
| ⛔ **instrument defect found at read time** | **cell D2 fires at a different team than the reference measures.** The panel is **five usable cells, not six** |
| **is the verdict robust to that?** | **YES, arithmetically** — see §4 |

**No repo-wide sentence is licensed by this read.** P4 did not fire, so the A4/A4.1 three-way
gate is not entered. **Alpha 0.109 is the honest pre-filter and is NOT upgradable in any
write-up.**

---

## 1. TERMINAL STATE — the first of today's three stops that was REGISTERED

```
2026-08-14T19:32:08Z   BOUNDARY   stop   15 accepts     <- fires tape
19:32:08Z PANEL BOUNDARY: 15 accepts >= 15 — Stopping.  <- runner log
```
**15 ACCEPT rows = 75 games** (derived, never relayed:
`awk -F'\t' '$3=="ACCEPT"' scratchpad/panel_cal8_fires.tsv | wc -l`).
**0 ABORT rows · no STOP file · runner wrote its row then exited.**

**Today's three stops, so the record is unambiguous:** (1) false-premise stop — retracted;
(2) reboot stop at 13/65 — stopped-short-recoverable, resumed; **(3) this one — the armed
boundary, and the only registered terminal state.** The first two are why the floor was
missed twice.

**Two gaps declared, per the cert points:** `18:15:41Z–18:28:32Z` (commits `9556569`
false-premise stop, `31c5606` revised seal) and `18:48:09Z→19:23:44Z` (reboot stop → resume).

---

## 2. THE HOLDER GUARD — RUN, NOT ASSUMED

The side lane flagged that this check had teeth today: **v145 held the slot
19:08:37Z–19:17:14Z** and played a rated pairing inside it. Any CAL-8 game created in that
window would carry the wrong holder and void its window.

```
GUARD ourver histogram over all 75 panel games:  {'140': 75}
GUARD ourver != 140:  CLEAN — no window voided
```
**The resume at 19:23:44Z lands after the rollback, so the arithmetic is clean — but it was
measured, not inferred.**

---

## 3. P4 — THE PRIMARY

Six-cell **sign test**, registered magnitude filter **≥10pp**, fires at **≥5 of 6 one way**.

| cell | panel | v125 reference | delta | tag |
|---|---|---|---|---|
| D1 `0033` | 4/15 = 26.7% | 6/20 = 30.0% | **−3.3pp** | within 10pp |
| D2 `LingLing40` | **EMPTY** | 13/25 = 52.0% | — | ⛔ **see §4** |
| D3 `Juusto` | 7/15 = 46.7% | 21/40 = 52.5% | **−5.8pp** | within 10pp |
| D4 `Jython` | 3/10 = 30.0% | 12/20 = 60.0% | **−30.0pp** | **BELOW ≥10pp** |
| D5 `Big O` | 5/10 = 50.0% | 14/20 = 70.0% | **−20.0pp** | **BELOW ≥10pp** |
| D6 `team lazy` | 9/10 = 90.0% | 22/30 = 73.3% | **+16.7pp** | **ABOVE ≥10pp** |

**below ≥10pp: 2 of 6 · above ≥10pp: 1 of 6 ⇒ P4 DOES NOT FIRE.**

**P1 (descriptive, no bar):**
```
panel pooled   34/75  = 45.3%  +-15.3pp   (DEFF 1.833, unrated pooled)
v125 reference 88/155 = 56.8%  +- 9.1pp   (DEFF 1.366, rated within-opponent)
difference             -11.4pp
two-fixture 95% half-width, n=75 vs n=155: +-17.8pp  =>  DOES NOT EXCLUDE ZERO
```
⚠ **The ±9.1pp reference-side floor stands beside this number, as registered: even at
infinite panel n the reference alone contributes ±9.1pp.** A −11.4pp point difference against
a ±17.8pp interval is **not** a finding, and the direction alone is not one either.

---

## 4. ⛔ THE DEFECT THE READ FOUND: CELL D2 MEASURES A DIFFERENT TEAM THAN THE REFERENCE

`tools/panel_cal8.sh:27` fires cell **D2** at team id
`86d0b484-783c-47dc-99d9-6ed9af2794f8`, labelled `# D2 LingLing40`. **The games that id
produces carry opponent name `lingling_40h`.** The certified reference table maps
`D2-LingLing40 → "LingLing40"`.

**These are two different teams and both are live in the league** — `target_value --band`
lists `lingling_40h` at 1802 and `LingLing40` at 1793, and both appear in
`ladder_games.tsv`.

⇒ **the panel's D2 cell (3 matches / 15 games) and the reference's D2 cell (13/25) are not
the same comparison.** `cal8_read.py` reported `LingLing40` as **EMPTY** and flagged
`lingling_40h` as an unknown cell — **the certified instrument surfaced this without being
asked, which is the third time today it has refused something a human would have waved
through.**

**WHY THE CERTIFICATION DID NOT CATCH IT:** `df54ea7` certified the **reference side** —
that the v125-only cut totals n=155 and 88 wins. **It had no way to check that the PANEL
fired at the same team the reference measures**, because at certification time the panel
side did not exist yet. **A reference validated alone is half an identity.**

**IS THE VERDICT ROBUST? YES, AND ARITHMETICALLY SO — not by assertion:**
```
observed:  below 2 · above 1 · within 2 · empty 1
best case for firing NEGATIVE:  2 + 1 (D2) = 3  <  5
best case for firing MIRROR:    1 + 1 (D2) = 2  <  5
```
**D2 cannot change the verdict in either direction.**

⛔ **CORRECTED IN PLACE after the side lane's certification.** This paragraph first ended:
*"P1's pooled 45.3% DOES include the 15 `lingling_40h` games — it is a pooled panel share and
does not depend on cell identity."* **The first half is true and the second is too loose.**
Cell identity does not affect computing the **panel side** — but it does affect the
**COMPARISON**, because after the D2 defect the two sides contain a different team in one
cell. That is checkable, so it was checked rather than argued:

```
as read                              panel 34/75 = 45.3%   ref 88/155 = 56.8%   Δ = -11.5pp
mismatched cell dropped BOTH sides   panel 28/60 = 46.7%   ref 75/130 = 57.7%   Δ = -11.0pp
```
**The conclusion is unchanged and now rests on a computation rather than a phrase.** The
sentence is corrected rather than deleted because that phrasing would license the same move
on a future panel where the mismatched cell is **not** this benign.

⛔ **WHAT I DID NOT DO, and it matters:** I did **not** substitute a `lingling_40h` reference
to "fill in" D2. Choosing a comparator after seeing the panel is exactly the researcher
degree of freedom that R3 closed for this same prereg six hours ago. **D2 is void, and it
stays void.**

---

## 5. ⛔ DISCLOSURE — I RAN A BROKEN READ FIRST AND SAW ITS OUTPUT

**Published rather than quietly re-run, because the alternative is a look nobody can audit.**

**The bug was mine, in ad-hoc glue around the certified module, not in the module.**
`meta_join`'s `us_side` column is **lowercase `'a'/'b'`**; my first attempt compared it to
`'A'`. Every row therefore took the else-branch, so:
* "our version" resolved to the **opponent's** version on the 45 `a`-side games — producing a
  nonsense histogram (`49, 13, 58, 226, 21`);
* "opponent" resolved to **`OpenSverige` — ourselves** — on those same games, collapsing them
  into a phantom cell and leaving **two** real cells empty.

**What I saw before the fix:** an invalid per-cell table (`0033` 3/10, `Jython` 3/10,
`Big O` 3/5, `team lazy` 5/5 — all partial `b`-side subsets), an invalid `below 2 / above 1`,
and a **P4 verdict of DOES NOT FIRE**. ⚠ **I also saw the pooled P1 — and P1 is
UNAFFECTED by the bug** (`our_won` summed over all 75 games does not depend on side
resolution): **34/75 = 45.3% in both runs, identical.**

**WHY THIS DOES NOT CONSTITUTE A SECOND LOOK, stated so it can be disputed rather than
assumed:** the fix is forced, not chosen — a case-sensitive string comparison has exactly one
correct form — and the analysis was pinned in a certified function before either run.
**No analytic degree of freedom was exercised between the two runs.** *(That the two P4
verdicts agree is a coincidence and is NOT offered as evidence that the first run was
harmless.)*

⭐ **AND THE STRONGER GROUND, supplied by the side lane's certification and adopted here
because it is better than my own argument.** The paragraph above defends the **FIX**, while
the live hazard is about the **TRIGGER**: **ASYMMETRIC DEBUGGING** — an author who dislikes a
verdict hunts for a bug and one who likes it does not, so *"I found a bug and fixed it"* can
smuggle in outcome-dependent effort **even when every step of the fix is forced.** A
forced-fix argument does not reach that.

**What closes it is a fact already sitting in this disclosure: the bug announced itself
through a DOMAIN VIOLATION, not through an unwelcome number.** The broken run resolved
**`OpenSverige` — ourselves — as our own opponent**, and produced an "our version" histogram
of `49, 13, 58, 226, 21`. **Neither of those is *surprising*; both are ILLEGAL.** A bug hunt
triggered by an illegal value is **outcome-independent by construction — it fires identically
had the broken verdict read FIRES.**

⇒ **the durable rule, which is more useful than this instance: build guards that fail on
IMPOSSIBLE values, not on implausible ones.** An impossible-value guard cannot be
outcome-selective, so it is the only kind whose firing is evidence about the analyst as well
as about the data.

**THE DURABLE FINDING, and it is about the instrument's boundary:** `cal8_read.py` certifies
`accepted_match_ids`, `panel_games`, `v125_reference`, `p4` and `half_width` — **but the
cell-assignment and holder-guard steps were left to hand-written glue at read time, and that
is where the error landed.** The certified core was never wrong. ⇒ **an instrument that stops
short of the final join has not been certified end-to-end**, and the uncertified remainder is
reliably the fiddly part. **Both guards are now written down as code with their PASS/FAIL
driven** (`GUARD0` fails on the previous run's data by construction).

---

## 6. WHAT THIS CHANGES

* **Nothing repo-wide.** P4 did not fire; the A4/A4.1 gate is not entered; alpha stays 0.109.
* **The panel-vs-rated gap is not resolved and this design cannot resolve it.** −11.4pp
  against ±17.8pp, with a ±9.1pp floor from the reference side alone. **A successor should
  note that the reference-side floor means widening the PANEL buys progressively less** —
  which is what the CAL-7 P1 `REFERENCE_FLOOR` check was minted to catch.
* **`tools/panel_cal8.sh:27` carries a mislabelled cell** (builder-owned; flagged, not
  edited). **Any future panel reusing that id list inherits the defect.**
* **CAL-8's rows are SPENT.**
