# BUILDER ARM RETRO — s46 instance (2026-08-16, wrap on Magnus's call)

Run against docs/builder-arm-retro.md v1. Answers from artifacts (results.tsv,
auto_gate_cancelled.tsv, git log, coordination tail), not memory.

## 1. VERDICTS — did each carry exactly what its interval supports?
Typed today: **BODYAWR KEEP** (53.70 [52.76,54.64] n=10,801 — CI-lo excludes
the registered 51.93 bar; scoped LOCAL, field read left to LEG-fieldcal);
**ECOSIPH REFUTED** (48.83, CI excludes the futility bar); **ECOSIPC REFUTED**
(49.28, same form); **ECOSCK6 DISCARDED-AS-NULL** — deliberately NOT written
as a refutation (its CI excludes neither 50 nor the bar; the tempted sentence
was "refuted" for symmetry with its siblings and it was wrong). Cancellations
correctly typed as cancellations, not verdicts: RENTGUN (catastrophe, 34.38),
SEALTI24, GUNAX0B, G417/G420 (combo bar), SEATFULL/RUSH72 (floor). The
calibration V140VS152 (57.02) was NOT typed as a verdict — routed to research
as its registered owner. Scorecard: one near-miss (ECOSCK6 phrasing), caught
at the keyboard.

## 2. RETRACTIONS, AND WHICH DIRECTION THE ERRORS RAN
Five, **all five in the flattering direction** — the s28/s29 pattern
persists and is now three sessions deep:
1. "1 in 560,000" tail frequency (audit relay) — made the reachability story
   cleaner than the data supports; retired for the estimator-proof counts.
2. auto_gate base rates: "true-neutral ~50%" — understated the sweep's noise
   output by 14 arms; exactly the direction that makes a count look like a
   discovery. Caught by research pre-consumption.
3. "The gates are consistent — gate-1 clearance implies probable gate-2
   victory" — settled an open transitivity question by phrasing (the #65
   discordant pair says it is open). Corrected to Magnus directly.
4. "Recovery is automatic" (ws2 aborted shards) — declared an incident closed
   that was not (cumulative-NOWINNER re-abort made the ids self-poisoning).
5. Belt-topology "scale, not titanium" framing (joint with research) — the
   census read the dominant channel as direct Ti; scale is ~17%.
**Consequence adopted in-session**: research's registered-count pattern (leave
the data in place, register the expected count, then change the code) was
used on all three queue_check fixes and both hit-or-informatively-missed.

## 3. INSTRUMENTS — changed, and driven to BOTH verdicts?
* queue_check GREP gate — WRONG THREE TIMES IN ONE DAY (mass-fire on the
  carry idiom; structured-token precedence; prose-idiom coverage), each fix
  scored against a count registered before the code changed; 14 fixture
  cells now, incl. the bare-`in` refusal cell and carried-to-older-tree
  STALE-reachability cell.
* gate.py LINE_DIRS invariant — three surfaces (run-path WARN, fixture cells,
  boot-run live test with the historical stale state as negative twin).
* auto_gate — COMBO-BAR@2700 (six cells), COMBO-BAR-EXEMPT with citation
  resolution (broken-louder-than-none, three cells), base rates corrected.
* fieldcal scheduler — AMEND1 catch-up (five cells, fired live on its first
  round), append-log fix (first live exercise at the 08:50 restart), halt-path
  stop notes.
* unrated_games decoder — selftest whose corrupted-calibration cell was
  VACUOUS in its first cut (corrupted a meta the calibration never sampled);
  caught and fixed before commit. Now sync-cycle-wired.
* orchestrate push guard — live case driven at birth; drain case observed
  same day; **and its refusal was then SWALLOWED by my own `| tail -1`
  pipeline in the ws2 handoff chain** (the exit-code family, self-inflicted;
  the guard was right, the caller discarded it). Corrected chain re-armed.
* Non-instruments (viewer, fleet dashboard) — visual checks + node-checks +
  live-data controls; Magnus click-throughs as the real verification.

## 4. CLAIMS AHEAD OF THEIR RECORD
Two: "leg readable on its registered axis" (over-claim; corrected to
computable-not-claimable with a dated tail correction) and "recovery is
automatic" (above). Both corrected same-day, on the record, by name.

## 5. THE SLOT AND THE HOLDER
**Zero platform actions by this session**: no submit, no activation, no
manual unrated fire. The leg ran detached under its own guards (0 arm-played
rated pairings across every flip per its leak checks). The holder (v152)
was never touched; the calibration measured it locally instead. Rated cost
of s46: **zero by construction**, verified by the leg's own instruments.

## 6. WHAT THE BUDGET BOUGHT
Local+fleet: the QoS incident-fix roughly DOUBLED prospect throughput
mid-session, then Magnus's 2,700-prospect economics doubled its yield again.
Screened/cancelled cheaply: RENTGUN (413 games to close a road), SEALTI24,
GUNAX0B, G416-G420 series, SEATFULL, RUSH72, ECO locals. Real waste: the
09:34 push incident — 58 corrupt rows + ~3h of ws2 re-runs + two permanently
poisoned shard-ids (recovered via fresh-id re-adds); bought a permanent push
guard. Eight subagents + two peer-lane fixture services consumed; every
report relayed before idling.

## 7. WHAT A SUCCESSOR CANNOT RECONSTRUCT
Routed into HANDOVER's top block this wrap: the readout map with times, the
v152-path arm semantics (turbo4-minus-one scoping, B1 double-amendment,
TBA's verification), the ws2 keep-discipline tiers with the 10%-is-convention
provenance, the Juusto wander-over-bar hazard, arm-B-trails-structurally,
computable-vs-claimable, and the wake paths. Session-bound things that DIE:
the ws2 handoff waiter (re-armed corrected; if it has not fired by wrap-end
its state is in the tail), all Monitor-class watches, the in-flight readout
waiter. **The two process items routed as MECHANISM GAPS, not notes:** (a)
appended worklist arms land behind stale blocks by file order — bit twice
today; wants an insert-at-front convention or a priority column; (b) a
`cmd | tail` in a && chain discards the command's refusal — the exit-code
family's inverted twin; wants a `set -o pipefail` habit in handoff scripts.

## OBSERVATION — NOT ROUTED
The day's defining pattern: three lanes each corrected their own credit
downward unprompted (side lane's wrap-trigger retraction, my σ retirement,
research's scorecard correction), and every guard that failed did so toward
its author's convenience. The protocol's value today was not catching lies —
it was catching optimism, fast, before consumption. Fourteen Magnus replay
markers became five built arms, four queue rows, and three doctrine
candidates in one session; the replay viewer is the highest-yield instrument
built this week and it computes nothing.
