# BUILDER ARM RETRO — s47, 2026-08-16 (~13:47Z → ~20:0xZ) — answered under v1

FIRINGS: 7 (Q2 direction finding, Q3 partial, the one-question answer, and
four routed deltas below). Session: 40+ commits, 17 verdict/cancellation rows,
7 arms built, 1 ship.

## 1. VERDICTS
Seventeen rows typed (results.tsv, all with bars/CIs inline). Scoping I am
prepared to defend: G411g4 typed as "statistically indistinguishable from its
own component" when the tempted sentence was "the combo works"; KLADTURBO's
every mid-read carried a regression warning until the replication existed;
COLLARF2 typed real-but-small with the attribution inconclusive rather than
"the surge works"; SENTBAN's verdict NAMES the registered mechanism metrics
it could not compute. The milestone verdict (KLADTURBOR) claims exactly the
rule's own three terms and nothing beyond them.

## 2. RETRACTIONS, WITH DIRECTION
Five: (a) "P2b satisfied by the retirement" — FLATTERING (eased a resume),
refuted by research from the holder tape, retracted with the direction named;
(b) KILLTILE amendment labeled "pre-start" when the shard had started —
FLATTERING (stronger claim), self-caught same commit-cycle; (c) stock/flow
example misattributed to research — sloppy, corrected on their refusal of
credit; (d) KLADTKILL diagnosis first blamed the known counter bug —
TOWARD-THE-FAMILIAR (my own boot rules warn of exactly this), corrected by
the build agent's pushback; (e) the structural-null tally 4→3 — accepted
research's withdrawal of their own catch. Two of five flattering, both caught
within the hour by the protocol. Better distribution than s29's all-flattering,
still not zero.

## 3. INSTRUMENTS
Eight changed/built, every one driven to both verdicts before trust (slot_rule
refusal test-first + side-lane backtest 0-lost; retirement switch both states;
TREND_FLOOR boundary fixtures; move_miner FIRE/QUIET/BLIND; two help guards;
SHIP_NAME_RE 5/5; KLADTK2 v3 demoed on its own failure maps). ⚠ PARTIAL HIT:
my hand-rolled replay decoders ran ~4 times without positive controls until
the merge agent rebuilt one WITH its control (reproducing 10/13 exactly) —
the delegation slip Magnus called out, fixed mid-session.

## 4. CLAIMS AHEAD OF RECORD
claim_check: clean at wrap (24 files, every claim points at a record naming
its own file).

## 5. THE SLOT
One activation: Sleipnir v1 (v155) at 19:38:40Z, submit_clean --activate,
verified on the Active-bot line, fired ~6 min into a derived 20-min clear-air
window. Zero rollbacks. Zero rated leaks (no prototype exposure all day —
GRIND held until the word). The displaced holder was a teammate's v152;
Magnus's direct order is the authorization of record, and the step-2 H2H
waiver is EXPLICIT in the pre-fire ship record, not implicit.

## 6. THE BUDGET
Zero platform challenges (GRIND). Local+remote utilization near-max all day.
The waste ledger both ways: ~7,950 remote games found sunk past decided
stops (the auto-gate's remote blindness), and against it five structural
nulls killed BEFORE their shards (COLLARF's dead floor, the ladder re-rank,
the builder-attack re-rank, the drainclose relay-artifact [withdrawn], the
DRAINTURBO burn-cap catch) plus two demo-caught wiring failures (KLADKILL
funding, KLADTKILL serialization). The precondition-and-demo discipline paid
its rent several times over.

## 7. WHAT A SUCCESSOR CANNOT RECONSTRUCT
Routed into HANDOVER at this wrap: the overnight readout list and who types
it, the SLEIPH2H interpretation rule, the control-question handoff to
research, the instrument-agent report relay. Monitors that survive vs die
are named there.

## THE ONE QUESTION — did I decide something the machinery could not catch?
YES, twice, same shape: (1) KLADTKILL's v2 "safe direction" reasoning was
wrong because safety depends on what is DOWNSTREAM of the failure (the ladder
gated on the cap), and no instrument owns that reasoning — the shard caught
it at 436 games only because the demo map set was too narrow to; (2) the demo
map set was too narrow because I reused the same three maps every build —
a decision-quality habit no check flagged. The routed fix: demo maps rotate
onto suspected failure geometry (now in KLADTK2's registration precedent);
"safe failure direction" claims must name what is downstream (added to the
deltas). The ship itself is NOT an instance: the machinery (60±2 rule,
replication, window derivation, pre-fire record) covered it, and the one
uncovered judgment (the step-2 waiver) was surfaced to the decider rather
than made silently.
