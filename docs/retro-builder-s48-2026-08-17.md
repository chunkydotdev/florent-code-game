# BUILDER ARM RETRO — s48, 2026-08-17 (~04:17Z → ~07:2xZ wrap-call)

Ran FIRST per charter, before the PROCESS DELTAS. Instrument: builder-arm-retro
questions applied to THIS session's decisions.

## FIRINGS OF STANDING RULES THIS SESSION

1. **Demo-before-register (D2): FIRED ON EVERY BUILD** — 6 trees built
   (kladladder, kladladder2/3, sealsent×4→pair, beltbreak+late twin,
   sealpierce, ecommit2 + the eco trio), every one demoed on rotating maps
   incl. failure geometry before registration. Zero wiring nulls escaped to
   shards (contrast: s47's 436-game escape).
2. **FIRINGS-BEFORE-PRIMARY: INVERTED ON ITS FIRST FIRING — BY ME.** Typed
   KLADLADDER's Band-4 primary before its registered dose battery ran, under
   time pressure, with the rule one day old. Repaired by the amendment chain
   (pending→read→final) and the attribution ultimately survived — but the
   rule failed exactly as the side lane predicted attention-held rules fail.
   Routed: the batch preregs now carry it as a HARD ordering clause naming
   today's inversion; the durable fix is a tool (wrap-fix scope).
3. **Iterate-mill discipline: HELD.** Every arm one mechanism, every prereg
   fresh-opus, every null banked as an iteration (KLADLADDER's three corners
   closed a funding axis in one day; ECOMMIT's catastrophe executed its own
   pre-committed branch same-session).
4. **Self-play-blindness rule: did not fire** (no plank tested against a
   behaviour the control lacks — all screens vs the live incumbent).
5. **Slot rules: HELD absolutely.** Zero submits, zero activations, the flap
   made submit_clean untouchable and the fail-closed fix landed BEFORE any
   need. The one teammate ship/rollback (Odin, 68 min) was watched, staged,
   analysed — never acted on.

## DECISION AUDIT (the question the retro exists for: were the calls sound?)

* **KLADLADDER ws1 routing** — MISS, owned in-session: routed remote under a
  pre-strict-rule rationale and not revisited after Magnus's ruling; cost
  ~2,100 games past the mark; produced the routing rule + (via Magnus) the
  remote-stop automation. The miss's full chain is on the tape.
* **Magnus-requested interim look** — disclosed on the record before the
  stop; the verdict carried it. Sound.
* **SEALSENTA retirement** — the pre-registered clause fired and was honored
  against the temptation to re-primary; the automation's relaunch race is
  disclosed and debt-listed. Sound, with a tool gap named.
* **SEALPIERCE hold→screen** — held on family evidence, released under
  Magnus's cores directive with honest priors on the page. Sound sequence.
* **Policy-vs-data stop distinction** — adopted mid-session and applied to
  every subsequent row; the regime's record reads honestly (see below).
* **⛔ MY 2+2 TALLY LACKED ITS DENOMINATOR** (side lane catch, end of
  session): the true record at the 1000-look is 3 stopped OF 5 LOOKED
  (+1 catastrophe at 400) — ROUTESCORE and BELTBREAK-EARLY PASSED a 52.0
  floor against the champion control. The regime discriminates; my failure
  was reporting only what the failure-watching instrument showed.

## ERROR-DIRECTION CHECK (s29 rule: which way did my errors run?)

Today's published errors by me: "fleet unblocked" (false when written —
flattering), "three surfaces agree" (evidence inflation — flattering),
"Sleipnir ahead of Odin" (point-without-interval — flattering to narrative),
n=3405 header-as-game (neutral), FIRINGS inversion (haste), 2+2 without
denominator (unflattering to the regime — the FIRST counter-direction error
in two sessions, caused by a failures-only instrument, not by interest).
**Mean direction still flattering; the correction loop caught every one
in-session — median time-to-correction under an hour, all by another lane.**

## OBSERVATION — NOT ROUTED

The day's deepest pattern: SEVEN population/confound errors across two lanes
converged into three tools (cluster_ci, population_diff, scale_trace) and a
taxonomy. The error rate did not fall during the session; the CATCH rate rose
and the time-to-catch fell (last instance: pre-send, 4 minutes). If the next
session's confound errors arrive pre-caught by the tools, the day's thesis
(mechanise, don't resolve) is confirmed.

## OPEN ITEMS CARRIED
* inside-band ⇒ NO SHIP needs the NEG cell check — unchanged, binds on any
  no-ship sentence.
* Self-play blindness rule — keep; will bind again the day a plank exploits
  field behaviour.
* FIRINGS-BEFORE-PRIMARY wants its tool form (wrap-fix).

FIRINGS THIS SESSION: 3 (D2 every build; the mill's null-banking twice; the
policy/data distinction adopted and applied 3×).
