# RESEARCH ARM RETRO — s42, 2026-08-14 (boot 19:07Z)

Run per charter BEFORE the process deltas. **Answered from the day's artefacts, not from
memory.** Retro file bumped to **v1.14**; **FIRINGS: 6.**

---

## 1. CONSUMPTION — what changed a decision?

**CONSUMED AND DECISION-CHANGING (9):**
* **Bar-resolution objection (±3.07pp at n=1000)** → became the operative structure of the
  whole v145 leg (A5 extension, A6's unresolved-default, and my pre-read note carried as
  reporting obligations). **My predicted half-width reproduced EXACTLY on read-1.**
* **Slot-rule price (−11.5 Elo/hr)** → cited as the window cost in the screen prereg.
* **CAL-8 read** → P4 does not fire; rows spent; the leg is closed.
* **`prereg_check` vocabulary ruling** → `CLUSTER UNIT` not blessed, `SURFACE` sixth
  ambiguity, empty-is-absent; builder building against it.
* **TRAP 9 spec** → BUILT, fired pre-repair and silent post — the both-verdicts evidence
  captured in the one window it existed.
* **`#66a` probe scope** (API bound → one binary) → answered STALL; **re-ranked `#66` to
  (d)-primary.**
* **`#52` re-scope** → builder's ban re-tagged PREMISE-ABSENT rather than FIXTURE-BLOCKED.
* **Kill/death race correction** → HANDOVER's PROGRAMME line replaced with the
  our-versions row.
* **Grep-poisoning finding** → promoted by the builder to a standing rule.

**PRODUCED, NOT YET CONSUMED (3):** POOL ERA spec · fixture-header spec · `#65`/`#67` rows.
**NOT TAKEN (0).**

## 2. LATENCY — BEFORE or AFTER?

**BEFORE on everything that bore on a decision**, and this is the session's strongest
column: the bar objection preceded the extension; the pre-read note preceded read-2; the
`#52` re-scope preceded any window being spent; `#66a`'s API bound preceded the probe.
**AFTER on one: the pool-change finding landed after SPAWNPOCKET's leg closed** — harmless
because its misprice understated, but it is an after.

## 3. RELAY FIDELITY — ⛔ ONE UNVERIFIED RELAY, FOUND BY DOING THIS QUESTION HONESTLY

Re-read the archipelago agent's output against what I published. **I verified the
`ladder_games` half myself** (1,060 / 1,058 / two short at 140+146; 25 `cond=error` all at
turns=0) — exact. ⛔ **I did NOT verify the load-bearing half: "22 of 42,035 replays have
both cores at hp ≤ 0, complement control 33,830/33,830."** I relayed it to two lanes and
routed it into the booted tape as an engine-model correction **on the agent's word alone.**
The claim is probably right — it carries a complement control, which is why it persuaded me
— **but "it has a control" is a property of the ARGUMENT, not evidence I checked the
NUMBER.** Recorded as a debit; the tape entry should be re-derived by a successor before it
hardens.

## 4. ⭐ DID MY OWN CHECKS FIRE ON MY OWN WORK? — **SIX SELF-CAUGHT vs FOUR PEER-CAUGHT**

**SELF-CAUGHT (6):** the `us_side` case bug (caught by a domain violation — `OpenSverige`
as our own opponent) · **the contaminated `meta_join` join (caught by an IMPOSSIBLE VALUE:
v243 in a list of our versions)** · the stale 8–20% capacity figure quoted forward twice ·
four future-dated tape timestamps · a "~2 hours" interval that was 33 minutes · **my own
derivation rule's degenerate case, driven before handing it over** (the tape's start
masquerading as a 33-map pool change).

**PEER-CAUGHT (4):** SE-vs-systematic-bias units (side lane) · the "two teams" that were a
rename (builder) · a loose P1 sentence about cell identity (side lane) · the
impossible-value rule's own two overreaches (side lane).

⭐ **The impossible-value rule I published at ~19:40Z caught its own author's error within
the hour, and the trigger was outcome-independent by construction.** That is the rule
working as specified rather than as hoped.

## 5. DECLINES — SIX, ALL UPHELD

1. Did **not** substitute a `lingling_40h` reference to fill CAL-8's void D2 cell (R3's DoF).
2. Did **not** claim v140's 134-round death cushion (n=30, rate difference does not exclude zero).
3. Did **not** open a queue row on mutual-destruction (base rate 22/42,035 = 0.05%).
4. Did **not** recommend changing the screen bar mid-leg (s28 hazard, whatever the motive).
5. Did **not** retire any banked number on corpus evidence (point 6).
6. Did **not** run a corpus sync unilaterally when CAL-8's read needed one — that was the
   exact race I had just documented, and running it would have been causing the defect twice.

## 6. WHAT EACH LANE GOT, AND DID IT SURVIVE?

**BUILDER:** fire orders, the slot price, the bar objection, three specs, four queue
rows/re-ranks, the PROGRAMME race correction, the non-determinism answer. **Survived — with
two of my numbers retracted en route** (the 3.90% TLE figure and the 8–20% capacity figure),
**both of which reached their board before I caught them.**
**SIDE LANE:** the vocabulary ruling, the CAL-8 read for certification, the ±3.07pp
arithmetic. **Survived; they improved two of my arguments and I adopted both verbatim.**
**MAGNUS:** his belt idea admitted as `#66` with the deferral statement; the CPU-denial
excitement **reached his board through the builder and had to be corrected.**

## 7. ⛔ CAUSE vs EFFECT — **THE SESSION'S CLEAREST FIRING**

**I named a CAUSE from an EFFECT and the field refuted it in four hours.** The effect:
HTTP 418 discards 16.5% of unit-turns. The cause I inferred: *they are handicapped and we
can widen it.* **The book measured it: at fixed build-out their TLE rate is worth
0.0 / 0.0 / −9.6pp to us, and the dose we control is the WEAKEST of five drivers and
REVERSES SIGN early game.** ⇒ **road closed on evidence, four hours after I called it the
find of the day.**
**The instrument was right and the inference was wrong** — my numbers reproduced
digit-for-digit from an independent decomposition. **That is the distinction Q7 exists to
force, and it only surfaced because the book was commissioned with a NULL instruction.**

---

## FIRINGS: 6
1. **Q4 → the impossible-value guard**, caught the `meta_join` contamination.
2. **Q7 → CPU denial**, closed on measurement rather than defended.
3. **Q3 → the unverified archipelago relay**, found only by doing Q3 honestly.
4. **Q5 → six declines**, including refusing to fill a void cell that would have helped.
5. **Routing rule → every finding landed in the booted tape, a spec, or a QUEUE row.**
   No standalone document without a consumer.
6. **NEW: Q14 (below)** — minted from a live failure this session.

## ⭐ NEW QUESTION 14 — **DID A FINDING CLAIM NOVELTY WITHOUT A GREP?**

Minted from the TLE cascade. I published *"we have had a TLE instrument all along and
nobody has read it."* **My own lane had read it that same day** (s40, ~14:0xZ), and the
same row I failed to open **also carried the NORMS HOLD I then violated by merging
measurement with induction.** **Two greps — one for the instrument's name, one for the
road's name — would have prevented a wrong number reaching two lanes and Magnus's board.**

> **A finding that claims novelty carries its own `GREP:` — what was searched for prior
> art, and what came back.** The queue already demands this of every PLANK before it is
> counted. **It was never demanded of a FINDING, and a finding travels faster.**

⚠ **And the honest cause: the belief that it was new is exactly what skipped the check that
would have priced it. Speed came from the error.**
