# RESEARCH ARM RETRO — INSTANCE s48, 2026-08-17T07:14:10Z

**Run BEFORE the process deltas, per the charter. Answered from the day's artefacts
(`docs/coordination.md` tail, `QUEUE.md`, `git log`, the three tools, the banked
studies) — not from memory.** Session 04:16:52Z → wrap. **~100 commits, all pushed.**

**FIRINGS: 4.** Q4 fired hardest and produced a mid-session behaviour change; Q7
fired once and cost a retraction; Q2's discipline produced three interceptions; Q5
produced seven recorded declines. **The sunset clause stays disarmed.**

---

## 1. CONSUMPTION — 19 deliverables, 18 CONSUMED, 1 PENDING, 0 UNREAD

**CONSUMED (another lane acted):** the same-day drawdown decode (builder dropped
rollback from every window) · the 74/74 re-admission sweep (4 ALREADY-SHIPPED, 7
PREMISE-DEAD — at least four legs saved) · `#54`'s re-spec (reached FREEROUND
MID-BUILD; the agent generalised `_t4_chase_ok` instead of inventing a parallel
detector) · the gunner census + annulus (BELTBREAK's premise) · the rotation
synthesis (v2's axis re-specified, the dead form pre-named as GUNPIN) ·
`cluster_ci` (adopted into the verdict path) · `scale_trace` (prereg boilerplate) ·
`population_diff` (adopted with the outcome-vs-behaviour rule) · the `lingling_40h`
study (`#93`, `#94`) · Sleipnir's closed tenure (ship record) · the Odin alert
(`version_trees` UNKNOWN row + the control question to Magnus) · the
`league_matches` partial-tail alarm (wrap debt 12) · both `move_miner` defects
(wrap debt 15) · the connect-rate mediator check (ECOMMIT re-denominated) · `#62`'s
direction (registration conditions) · `#37`'s pricing (no arm built) · the splitter
DEAD entry · the annulus within-game control (EARLY's readout-language rule).
**PENDING:** the OPENFAST re-point (first-conveyor, not first-harvester) — sent at
the wrap-ping, so its landing is the successor's to verify.
**UNREAD: none I can identify.** Six subagents spawned, six completed, six relayed.

## 2. LATENCY — the number that matters, and it was the best of the day

* **`#50`-is-shipped → the eco study agent MID-RUN.** Intercepted a prereg that would
  have rebuilt the incumbent.
* **The v125 harvester phantom → the archaeology agent KILLED IN ORIENTATION.**
  **19 minutes from publication to retraction; nothing written, no artefact.**
* **The leak-slope retraction → the batch-prereg agent MID-DRAFT.** 10 minutes.
  Coefficient struck before it reached a locked page.
* ⛔ **ONE AFTER: my Sleipnir closing sentence ("did not demonstrably out-share its
  predecessor") had already gone to Magnus in the builder's message before
  `SLEIPH2H` was checked.** Corrected in their next message — **archaeology by one
  hop, and the only deliverable today that reached Magnus wrong.**

## 3. RELAY FIDELITY — one relay diffed against source, clean

Re-read my builder relay of the `lingling_40h` study's PIECE 1 against the agent's
text. **The agent wrote "14,518 damage = 21.6% of everything that has ever hit our
core IN THIS CELL". I carried "in this cell" in both relays.** The scoping qualifier
survived compression, which is the exact failure this question exists for (s28: an
agent's "flat-to-slightly-up" over 0.150/0.175/0.100/**0.314** relayed as "flat").
⚠ **But the compression failure DID occur elsewhere in a different form: I relayed
the study's `14,518 damage` and its `2,074 shots` as two corroborating facts. They
are one fact — 2,074 × 7 = 14,518 exactly. The agent never claimed independence; I
read independence into it.** ⇒ **the fidelity risk is not only DROPPING a qualifier,
it is ADDING a relationship the source did not assert.**

## 4. ⭐⭐ OWN CHECKS ON OWN WORK — ~10 SELF vs ~13 PEER, AND TWO WERE PRE-EMPTIVE

**SELF-CAUGHT:** the v125 map-rotation phantom · the leak slope (within-game control)
· `scale_trace`'s total-vs-marginal defect (found by DRIVING it on its motivating
case) · `cluster_ci`'s injection-point defect (found by DOCUMENTING it for an
auditor) · the splitter percentile I estimated instead of running · BUILD/DEATH
pooling in the first gunner extraction · three interpolated timestamps · "no second
surface exists" for the scale verification.
⭐⭐ **AND TWO OF A NEW KIND — CONTROLS RUN *BEFORE* PUBLISHING: the annulus
within-game control (changed the justification) and the opening-allocation
within-game control (CHANGED A BUILD'S TARGET).** **Every previous self-catch this
session was a retraction; these two were not.**
**PEER-CAUGHT (side lane unless noted):** the argmax-window interval · the −13.1
half-width where a lower bound belonged · case B's lost discriminating power · the
round-number `MIN_CLUSTERS` · the certifying clean-run summary · union-vs-simultaneity
on rotation · the 92.5% near-tautology · home-vs-forward co-location · "dead weight"
unestablished · the `SLEIPH2H` supersession · overlap-is-not-leverage · **the `#44`
stale header (builder)** · **ph-has-5.48-builders-too (builder)**.
⇒ **Near parity, against s28's zero — and the shape changed mid-session: the last
two self-catches were PRE-EMPTIVE, which is the first time this lane has produced
that.**

## 5. DECLINES — SEVEN, ALL UPHELD

Refused to publish a cut-round number (two designs, opposite signs — three
instruments, three refusals) · struck the argmax-window interval rather than hedging
it · refused to quote any v155 slope (22 clusters) · recorded the splitter as DEAD
rather than as a row · refused to propose the opposite `#62` arm from a
difference-in-differences · refused any Odin share statement before ~30 clusters
(and named the date: ~16:2xZ) · **narrowed the builder's own offer of an
interval-pass on EVERY screen to PLATFORM surfaces only, on the grounds that a
precondition firing on everything becomes ceremony.**

## 6. PER LANE

**BUILDER — everything survived.** At least four legs saved (`#50` rebuild-the-
incumbent, `#9` arm-is-the-control, `#24`'s collapsed 2×2, `#44`'s void explanation),
one agent killed in orientation, one build re-pointed (OPENFAST: first-conveyor not
first-harvester), one iteration axis re-specified (BELTBREAK v2's rebuild-aware
rotation), one prereg's metric re-denominated (ECOMMIT: win share primary, leak as a
non-mediator mechanism metric), six rows stocked (`#90`-`#95`), one DEAD entry.
**SIDE LANE — they gave me far more than I gave them.** Mine to them: the
absence-claims formulation, D21(d), the mirror that became D21(f), the symmetry-tell
detector. Theirs to me: eleven catches, two of which reversed a row.
⛔ **RETRACTIONS THAT REACHED A LANE: SEVEN** — the −1.5pp timely-kill, "did not
out-share its predecessor", the v125 regression, the leak slope, the "3.7% best leak",
the splitter percentile, and the first-harvester-deficit framing. **That is the
highest count this file has recorded, and it is the direct cost of the generation
rate.**

## 7. ⛔ CAUSE vs EFFECT — ONE FIRING, AND IT COST A RETRACTION

**Effects measured: ~15. Causal labels attached: 2.**
* ✅ **Cut-round: I measured the effect and REFUSED the label**, explicitly, in the
  message that carried it.
* ⛔ **The leak: I wrote "THE LEAK *IS* THE UNCONNECTED-HARVESTER MECHANISM" — a
  causal sentence — with the control NAMED AS UNRUN in the same note, and told the
  builder to put it in a drafting prereg.** The within-game control read 0.4509
  [0.4014, 0.5000] ten minutes later. ⇒ **the mechanical check ("a causal sentence
  ships with INFERENCE next to it, or it does not ship") did not fire, and naming the
  control as unrun felt like the same protection. It is not.**
⇒ **DERIVED, and it is this session's contribution to the question: IF I CAN NAME A
CONTROL IN THE SAME BREATH AS A CLAIM, I RUN IT BEFORE SENDING THE CLAIM.** Adopted
mid-session and it fired twice afterwards (Q4's two pre-emptive catches).

---

## ROUTING — every finding routed at write time

| finding | route |
|---|---|
| run-the-control-before-sending | **behaviour → coordination tail + this retro's Q7; carried to the builder** |
| outcome-vs-behaviour comparison rule | **behaviour → builder's readout practice, adopted** |
| `cluster_ci` / `scale_trace` / `population_diff` | **instrument → three tools, selftests green, help contracts clean** |
| freshness ≠ completeness (`league_matches`) | **script → builder wrap debt 12 + dated spec in the tail** |
| `move_miner` defects ×2 | **script → builder wrap debt 15 + fix spec** |
| D21(d) and D21(f)'s mirror | **rule → side lane's drift-watch, credited** |
| symmetry-is-a-rules-tell | **behaviour → builder playbook** |
| a self-row inside a league table is a self-claim | **behaviour → coordination tail** |
| the confound taxonomy (7 instances) | **OBSERVATION — ROUTED to D21 by the side lane, not left here** |

## THE ONE METRIC: SELF-CAUGHT vs PEER-CAUGHT
`s28: 0 vs ~5 · s29: ~2 vs ~4 · s30: more self than peer · s31: doubled with the
generation rate · s45: 5 self · s47: ~8 self vs 4 peer · **s48: ~10 self vs ~13
peer, with the first two PRE-EMPTIVE catches this lane has recorded**`
⇒ **the raw ratio worsened because the generation rate roughly doubled; the
PRE-EMPTIVE count went from zero to two, which is the number I would watch next.**
