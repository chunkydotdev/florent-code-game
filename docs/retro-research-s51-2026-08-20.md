# RESEARCH ARM RETRO — s51 RUN (2026-08-18 03:07Z → 2026-08-20 08:46Z, with a Magnus-ordered stand-down gap 08-18 17:54 → 08-20 04:08)

Instance of `docs/research-arm-retro.md` v1.20 → **v1.21**. Answered from the day's artifacts (the coordination tail 03:13Z 08-18 onward, seven banked study docs, the cross-lane message log, ~30 research commits all pushed), not from memory.

## Q1 — CONSUMPTION: everything CONSUMED, receipts on the record; zero UNREAD
The builder's own wrap message: *"Your outputs all consumed with receipts; nothing of yours is dangling in my ledger."* Load-bearing chain: corner-barrier finding → **build mandate** (v530-class) · crater autopsy → **anti-ring spec item 1 rewritten** (belt-finish) · archipelago frame → **delta hunt killed pre-spend** · conveyor reconciliation → **RING blocker removed + a false retraction to Magnus prevented** · gsxWins study → **#21/#22/#86 re-price, #89 fork resolved** · 0033 study → **#96 stocked (side-lane-audited) + siting discriminator into v514 context** · two-body prior → **CREWCONV screen design + fund-AND-fast chain** · same-day decodes → Magnus briefs (0033 cost model, tenure closes). Queue 78 → 79 with one re-price block.

## Q2 — LATENCY: everything before its decision except ONE self-inflicted archaeology
The autopsy gated the spec and landed before it; the reconciliation beat the v530 correction; the frame beat the hunt. **The exception is mine: the 08-18 03:16 gsxWins "field improvement" amendment was consumed by the builder and stood ~2 days until my own commissioned study refuted it.** Archaeology cost: one booted-record correction (Q9 below).

## Q3 — RELAY FIDELITY (diffed: the ring-engagement relay vs its agent's result): CLEAN
Numbers, implications and the confound statement all carried. **Q3b partial instance:** I relayed "royale 92% vs yulerune 18% at identical board specs" — true for size+core-separation, readable as identical MAPS; the autopsy then showed layout is exactly what differs. Same-session self-repair, but the compression existed.
**Q3b′ (anchors) — FIRED WELL, repeatedly:** every anchor relayed this session was opened first (doctrine.py:1687 / main.py:624-631 / raid.py:731-738 / main.py:737-756 / doctrine.py:1231-1233 / doctrine.py:1866-1870 / PAVE_TRAIL_ON:528). Zero RELAYED-UNVERIFIED anchors shipped.
**Q3c (premises into briefs) — FIRED, good outcome from a bad premise:** my gsxWins brief carried two wrong premises (boundary at v54→v58; "field share rose") — but phrased as checkable context, and the agent refuted both. The rule held because the brief licensed refutation; the premises were still wrong at write time.

## Q4 — OWN CHECKS: ~4 self-caught vs ~2 peer/agent-caught — self-majority
Self: the gsxWins early-sample artifact (03:16, though see Q9) · the #96 missing `GREP:` token (row invisible to its own counter) · the v160 tenure undercount in the s50 wrap (5→6 matches) · the untracked-watcher chaining. Peer/agent: side lane's D21f caution on the bytes identity (I then verified it in version_trees.tsv) · the study correcting my amendment (the s47 delegation mechanism, again).
⛔ **THE RECURRENCE THAT MATTERS: the watcher-chaining error fired THREE times, and the mechanical rule I wrote after #2 did not stop #3.** The rule as rewritten: **`rated_watch.sh` never appears inside ANY compound command — it is always the sole content of its own run_in_background call.** A rule that names the failure ("never chained") failed; a rule that names the only allowed form might not.

## Q5 — DECLINES: four, all upheld
(1) Bulk re-grep of 78 stale queue rows at boot — declined under the audit_trigger fire; rows get re-grepped at consumption. (2) The v161/v162 tree imports — declined as bots/ writes per charter precedent; builder did each in ~2 min. (3) The live-probe half of the conveyor reconciliation — declined under the no-arena limit; the builder's half landed decisively. (4) The probe-defect code-read — dropped the moment the builder's v530.1 agent took it (no duplication).

## Q6 — PER LANE · RETRACTIONS REACHING A LANE: 2
**Builder:** numbers before decisions throughout; one false retraction to Magnus prevented (reconciliation); one delta hunt killed pre-spend; every correction taken without defence. **Side lane (while present):** D21f answered with verification not assertion; the #96 visibility catch logged symmetrically. **Retractions: 2** — the gsxWins field-improvement amendment (consumed before its refutation) and the counter-tuning inference (withdrawn same-hour, never acted on). Both corrected at their original sites. Down from s50's counts.

## Q7/Q9 — CAUSE vs EFFECT · CORRECTIONS NEEDING CORRECTION: ONE, and it is the run's lesson
Inference markers travelled correctly (counter-tuning, CB-off convergence, fund-then-crew, kladde-restore — all marked inline). **Q9 fired once: my 03:16 correction was itself corrected by the study** — a one-day two-version cut published as a trend correction. The general form: **a correction derived from the same under-powered surface as the claim it corrects inherits the defect.** The honest move at 03:16 was to state both branches with n and wait for the study the 0-4 already justified.

## A — What I would not produce again
The 03:16 amendment as written (above). Also the third watcher chaining — the first two were errors; the third was a process failure.

## B — What a peer caught that my process should have
The bytes-identity assertion (side lane, D21f): I carried an s50 record claim as fact for ~20 minutes before verifying. The check that should fire: **a claim about CURRENT state inherited from a predecessor's record is verified before it enters any of my own sentences, not after a peer asks.**

## FIRINGS: 5 (Q3b′ serial · Q3c · Q4-mechanical-rule rewrite · Q5 ×4 upheld · Q9 recorded at write time). Sunset clause NOT armed.
