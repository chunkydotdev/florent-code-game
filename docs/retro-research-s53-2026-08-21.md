# RESEARCH ARM RETRO — s53 instance — 2026-08-21 (boot 12:49:57Z → wrap ~16:3xZ)

Run against retro instrument v1.22 → **v1.23** (bump recorded in `docs/research-arm-retro.md`).
Answered from the day's artefacts: 20+ research commits, the coordination tail 12:49→16:28,
the cross-lane message log, ten agent deliverables, `QUEUE.md` diff 87→99.

## Q1 — CONSUMPTION: 12 deliverables, 12 CONSUMED, 0 UNREAD
adgato rush decode → #105-107 + the builder's wave context · farming v19 study → #108 +
amendments + the DOORWAVE farming cell · KLADDEDOSE draft → LOCKED + FIRED same session ·
DOORWAVE draft → LOCKED + FIRED 20/20 · #109 trace → builder-verified, verdict v3.1, and the
NEXT lock (DOORWAVE) adopted its reachability-driven dose drive same day · anchor → #110/#111 +
kill-clock fold · 0-5 famine decode → class attribution into #110/RD3 · Bean study → Magnus
answer + #112/#113 · O(1) study → Magnus answer + #114 · playbook parts + merged book → the
principal's commissioned deliverable · ed8172e7 probe → side-lane alarm closed · re-scope
classification → ratified block in QUEUE.md. **Nothing produced today lacks a named consumer.**

## Q2 — LATENCY: pre-decision on everything load-bearing
The audit_trigger subject check landed before anyone acted on the trip; both prereg drafts beat
their locks; the DOORWAVE commission's stale KLADDEDOSE premise was fixed BEFORE the drafter
read it (pre-emptive, see FIRING 1); the probe verdict landed 4 minutes after routing (side
lane: fastest flag-to-closure recorded). Archaeology cost: none material.

## Q3 / Q3c — RELAY FIDELITY: THE FAILURE MOVED INTO MY BRIEFS, TWICE — and both were agent-caught
1. The farming_200s commission carried a WRONG tape summary ("3-2/2-3/2-3 vs v176/v177"; corpus:
   six matches, 13-17, no v177 cell at study time). Agent caught it against the corpus.
2. The 13:19 tail note's probe-overlap caveat (_probe_peck_a/b "touch the same subsystem") was
   pattern-matched, not verified — builder corrected (standalone decoder fixtures).
⇒ Q3c's mechanical form (open every premise handed down) FAILED on a premise I generated myself
from memory of my own reads. **The compression risk includes MY OWN session memory as a source.**

## Q4 — OWN CHECKS ON OWN WORK: ~5 self-caught vs ~5 peer/agent-caught
Self: the hand-estimated timestamp (caught minutes later, re-anchored to the commit clock) · two
successive decode-key bugs in the probe cut (caught both by implausibility of the output — the
"pairs" artifact) · the 29-0 verification before it reached Magnus · PROGRAMME-field verification
before the re-scope acted on a relayed directive.
Peer/agent: the two brief errors (agents) · RMST estimator drift + timestamps nit + missing-prereg
flag (side lane) · the kladde drafter's honest gap list. Delegation-with-a-real-question caught my
briefs twice — the v1.17 mechanism, working as designed.

## Q6 — PER LANE, AND RETRACTIONS
BUILDER: two of their own framings corrected pre-consumption on verification (v68 "line under
test" → INCUMBENT 29-0, digit-for-digit; O(1) "fast riser" → fallen-bot recovering) — both taken
without defence; every draft/fact-fill consumed. SIDE LANE: probe answered in 4 minutes; every
flag they raised (D5 missing prereg, RMST₃₀₀, timestamps) accepted and fixed at the source.
**Retractions reaching a lane: 2** (probe-overlap caveat; fire-order leg 1's "until the prereg's
n" referencing a lock that did not exist) — both corrected in-record within the hour. *(s52: 2.)*

## Q7 — CAUSE vs EFFECT + THE DAY'S CLASS LESSON
**A gate inferred from an absence, without a reachability check, attributes the zero to a clause
that never ran.** Fired on my own #109 GREP text (which quoted IDLEPECK=False and then described
the path as live) AND on the builder's readout in the same hour. Routed: coordination tail,
DOORWAVE lock (reachability-driven dose drive), obligations-doc wrap debt.

## STANDING LINE A — what I would not produce again
The fire-order leg 1 phrasing "fill windows until the prereg's n" — it referenced a lock as if it
existed. The side lane caught it before the first fire; the cost was zero only because the
protocol worked.

## STANDING LINE B — what a peer caught that my process should have
The RMST₃₀₀ estimator drift in #105's guard line: I assembled the guard from memory of the
old form. The mechanical fix already exists (open the PROGRAMME anchor when writing any guard
line) and was applied to every later row.

## THE FIRINGS — FIVE
1. ⭐ **PRE-SPAWN PREMISE FIX (new, the day's best process moment):** the KLADDEDOSE verdict was
   amended AFTER my DOORWAVE commission file cited its v1 — the fix landed in the commission
   BEFORE the drafter read it. The running-consumer rule applied to a BRIEF: the cheapest Q3c
   form is fixing the input file pre-spawn, cost ~1 minute, prevented an encoded stale premise.
2. **Q3b′ verify-before-relay caught a commissioning lane's framing error twice** (v68 incumbent;
   fallen-bot) — see the v1.23 bump: the receiving lane verifies the COMMISSIONER'S premises too.
3. **Q4's delegation mechanism caught my own briefs twice** — agents briefed with refutable
   premises refuted them (v1.17's procedure, revalidated).
4. **Routing rule held at 100%:** every deliverable landed in QUEUE.md, the ledger, a locked
   prereg, or a banked+consumed study — zero orphan documents.
5. **The reachability class lesson routed to three surfaces same-day** (tail, the next lock's
   dose drive, obligations wrap debt) — a finding that changed another lane's artefact within
   the hour is the consumption standard this file exists to enforce.

## SUNSET CLAUSE: not armed (five firings, one instrument bump).
