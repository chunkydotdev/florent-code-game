# RESEARCH ARM RETRO — s54 instance (2026-08-21T16:41Z → 2026-08-22 ~10:2xZ, overnight session)
Run at wrap per charter (trigger: builder wrap-ping on Magnus's call). Answered from the day's
artefacts: the coordination tail (my ~20 notes), git log, the three agent returns, the monitor event log.

## 1. CONSUMPTION — everything reached a consumer; zero UNREAD named
CONSUMED (decision changed, with the consumer): #116 cut → playbook T24 rider + builder's fidelity
columns + the SITING constraint in the first-contact plan · kladde v173 study → fire-order cell-3 bar
amended BEFORE the cell fired + builder's v619 burst-plank convergence + "highest-recoverable-rating"
pricing · pooled first-contact table → builder's verdict frame (their receipt names it) · b9bb73e2
attribution table → closed on the builder's fire log; leak channel into window pricing permanently ·
amendment v1→v2→v3 → LANDED verbatim in CLAUDE.md:640-655 (site 1 verified in-session) · rate-window
refutation → builder's constant-edit debt discharge · monitor intel (ship-wave, x3r0 v179 round-trip,
Jacobs fixture) → day-read + #118. PENDING (dies-with-session risk, disclosed in the state note):
the first-contact MECHANISM DECODE (agent live at wrap-time; brief + replays banked, re-run is cheap).

## 2. LATENCY — the plan-before-the-event pattern carried the session
Fire order pre-built ~19:4x, DAYS before any screen-clear could fire it; the stealth gate added
BEFORE any fire; the kladde bar amendment reached the builder's fire-order copy BEFORE window 4;
amendment text verified against primaries BEFORE each fold; the monitor armed BEFORE the night.
Archaeology: none banked. ⚠ The one late item: the reach/first-damage decode is landing AFTER the
builder's wrap — the successor consumes it, which is latency cost paid to the session boundary,
not to a decision (the verdict surface transfers per the builder's HANDOVER).

## 3. RELAY FIDELITY — held on verification, failed once on an ABSENCE
Verified-before-use, each against a primary: ebd62b932 (seed-inert), 35981aedc (DEFF 4.57 etc.),
the kladde 13-match count (re-derived on BOTH surfaces), the pin anchor (cbe260a0 resolved to a
Pivot-vs-BC match — which also corrected the side lane's "paste slip" inference). ⛔ THE MISS:
my "unattributed fires" list included 93629c58 — the builder's DISCLOSED probe, on the tail at
08:36, which I did not grep before publishing an attribution claim. **An absence claim
("unattributed") is an anchor-class claim: grep the tail for the id before publishing it.**
(Q3-family, new face: closing-line B's "before asserting an absence, grep" applied to ATTRIBUTION.)

## 4. OWN CHECKS ON OWN WORK — heavy self-catch session, and the guard-fired list is the story
Self-caught (9): DOORWAVE "not archived" (minutes) · boot-note unexpanded timestamp (the ^--- 2026
guard FIRED as designed — its first live catch) · "99→100" queue arithmetic · farming-v20
"ship-and-improve" (corrected on next data) · gsxWins-v88 "softened" (withdrawn) · "seven sweeps"
undercount (via my own commissioned agent) · "kladde top-tier" framing (same agent) · the
GREP-token invisibility of my own #117/#118 (self-found, fixed to the tool's contract, matcher
untouched) · monitor v1 over-firing (retuned same hour). Peer-caught (3): stealth-gate omission
(side lane — the fire order carried a latent PROGRAMME collision), 93629c58 (builder), the
side-lane's routing flag on the third cluster (a finding of mine parked where no procedure-follower
would meet it). Delegation-mechanism catches: 2 of the 9 (the kladde agent refuting its own brief).

## 5. DECLINES — four, all upheld
Declined to verdict the pooled table, the v179 tape (n=4, owner pulled it), and the v180 cells
(mechanism bars registered; game share context only). Declined the move-miner's top-one (lingling)
WITH the deviation reasoned on the record — a decline of the tool's ranking, not of the mandate.
Declined to patch queue_check's near-miss gap (owner's call; my rows fixed to the contract instead).
Declined to edit CLAUDE.md directly on the side lane's suggestion (routed through the builder's
batch — boundary held under a peer request).

## 6. PER LANE
BUILDER: consumed everything sent, corrected me once, converged independently on the burst-counter
shape (study ↔ v619 plank — the strongest cross-validation of the day). SIDE LANE: three flags on
me (stealth gate, routing, the v620 instance), all accepted and executed; my pin verification
corrected THEIR paste-slip inference in return — symmetric catch traffic, the protocol working.
RETRACTIONS REACHING A LANE: 4 (v88-softening, ship-and-improve, seven-sweeps, kladde-top-tier)
— all corrected in-record within the session, two by my own agents. REACHING MAGNUS: the
kladde-top-tier framing appeared in a chat summary before the study corrected it (~2h standing).

## 7. CAUSE vs EFFECT — the session's named failure shape: N=1 DIRECTION WORDS AT MONITOR CADENCE
Two instances, same shape, same night: "ship-and-improve" (farming v20, n=1 match) and "threat
softened" (gsxWins v88, n=1). Both were direction-labels on single-match tape reads, published at
the monitor's decode cadence, both refuted by the next data point. **The monitor's speed is the
hazard: a decode-within-a-minute invites a verdict-within-a-minute.** Mechanical form adopted for
tape notes: a single-match read carries its n and either NO direction word or "one-match read"
in the same clause. (This is Q9's under-powered-correction lesson arriving on the GENERATOR side.)

## Standing lines
A. WOULD NOT PRODUCE AGAIN: the two n=1 direction-labels; and the monitor v1 symmetric filter
   (its retune cost an hour of noise the asymmetric design would have avoided at arming time —
   the act-on class was derivable BEFORE arming: wins on a teammate's bot were never actionable).
B. PEER CATCH → CHECK: the stealth-gate omission converts to: **a fire plan is checked against
   PROGRAMME.md's parsed fields (grep the flag names) before it is published** — the collision was
   one grep away and the side lane found it by running exactly that grep.

## FIRINGS: 6
(1) The ^--- 2026 timestamp guard's first live catch (pre-commit). (2) Q3b'/anchors held all
session EXCEPT the disclosed 93629c58 miss — which fired closing-line B's grep rule in a new class
(absence-of-attribution). (3) Q5 declines ×4 incl. the reasoned ranker deviation. (4) Q4's
delegation mechanism produced the two highest-value corrections (agent refuting its brief). (5)
The routing rule ran at 100%: every finding landed in a booted file, the queue, a rider on the
deliverable it corrected, or the builder's batch — zero standalone orphan docs. (6) Q7 fired
post-hoc on the two n=1 labels and produced the mechanical tape-note form above.

Sunset clause: NOT armed.
