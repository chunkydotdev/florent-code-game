# RESEARCH ARM RETRO — instance s55, 2026-08-22 (~09:47Z → ~11:1xZ, wrap on the builder's ping)

Run against instrument **v1.24 → bumped to v1.25 this instance**. Answers from the day's
artefacts (coordination tail notes 09:5x–10:4x, QUEUE.md #119 diff, the two banked agent
deliverables, scratchpad/s55_skalman_dose.py), not from memory. **FIRINGS: FOUR.**

## Was this lane USEFUL (the premise question)? — YES, with receipts, and one costly defect
Consumed by another lane this session:
* **DECODE-firstcontact-v180** (commissioned, relayed) → builder typed three cell verdicts
  on it (tail 10:18:50Z); side lane certified; its crash-cascade lead became the
  CRASHREP-BC prereg (locked b878ecf0d) — a whole leg generated from one decode finding.
* **REPLAY-STUDY-gsxwins-v88** (commissioned, relayed) → consumed into the v623 design
  frame; its launcher-family refutation retires a stale threat model.
* **The dose measurement** (this lane's own instrument, driven both ways) → v623 brief,
  top of the successor's queue per the builder's wrap-ping.
* Smaller: the WIN_RATE_IS_VERDICT divergence flag (builder batch), the same-map
  executability check (pre-lock), the rate-constant verification, two opponent bumps.
Cost side: **#119 lived 40 minutes as an admitted queue row on a wrong premise and the
builder began a design frame on it.** Caught by the side lane + the decode's own arc;
withdrawn by this lane in-record. The catch chain worked; the admission gate did not.

## FIRINGS (each names its question)
1. **Relay-hierarchy compression** (the relay-fidelity discipline): my decode relay listed
   "65/65 turns+seat" flattening the doc's own §0.2 grading (seat = weak). Side-lane
   catch; amended in-record same hour. *(Existing class; recorded.)*
2. **SUBJECT ERROR — the charter's named characteristic error, landed again:** #119's
   evidence (0 core attacks in 30/30) was MJOLNIR's games; the study said so at its own
   :610/:645 ("why Mjolnir's builder never targets an adjacent core") and my row+relay
   compressed "Mjolnir" to "our builders" and aimed the CHANGE at the Skalman head.
   *(Q3-family; the s43 wrong-subject class, now with a queue-row instance.)*
3. **⭐ NEW CLASS, the bump's incident — TOKEN-GREP ENUMERATION (grep-from-history):**
   #119's GREP cell enumerated `_peck_priority` call sites by grepping the `skip_core`
   TOKEN — a search that can ONLY return callers that pass it, i.e. only evidence for the
   claim. The full enumeration (by function name) is NINE sites, three of them leaving the
   core live, plus a dedicated `_attack_enemy_core` the token search structurally could
   not find. **The side lane's tell was exact: the row named precisely the sites the v602
   fix touched — an enumeration inherited from the fix's history.** Compounding instance:
   the builder's independent "verification" re-ran the same grep and reproduced the blind
   spot (their headline class this session, routed by them). *(→ v1.25 rule below.)*
4. **Q9 — a correction needing correction:** my withdrawal note's "8 sites" was off-by-one
   against its own breakdown (3+1+5=9); the side lane's "7" was a pipe truncation. Both
   corrected in-record within the hour.

## Positive yield (Q4, delegation)
Both fresh-opus commissions refuted their own briefs' premises before those premises could
misprice anything (decode: effective n 58, jitter-side inversion, the crash re-attribution;
study: launcher-family dead, "not strong", my stale rating figures) — third consecutive
session the fresh-context design catches the commissioner. The dose instrument was
validated per the standing bar (known cell exact + mutation control both ways) BEFORE its
numbers were relayed — the discipline holding where #119's grep did not.

## ROUTING (at write time)
* **v1.25 rule (behaviour + instrument bump): A GREP CELL NAMES ITS SEARCH PATTERN, and
  the pattern must be able to RETURN THE DISCONFIRMING CASE** — enumerate call sites by
  the FUNCTION's name, never by the token whose presence the claim asserts; a search that
  can only find confirmations is the constant-column alarm wearing a grep's clothes.
  → bumped into `docs/research-arm-retro.md` v1.25; ALSO a PROCESS DELTA (booted file).
* **Evidence rows carry the SUBJECT TREE of the measured games** (which bot played them)
  next to the number — the Mjolnir/Skalman compression is the incident. → PROCESS DELTA;
  Q3-family extension recorded in v1.25's changelog.
* Firings 1 and 4: existing classes, recorded here, no new routing.
* Observation (builder's non-independent verification): theirs, routed by them in their
  own deltas. `OBSERVATION — NOT ROUTED` from this lane's side.

Sunset clause: NOT armed (four firings, one new rule).
