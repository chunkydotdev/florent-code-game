# RESEARCH ARM RETRO — INSTANCE s56, 2026-08-22

GAME CONTEXT: everything below describes work on the Florent Code League — a sandboxed
bot-vs-bot programming competition on a simulated grid, within organiser-approved rules.

**Instrument: `docs/research-arm-retro.md` v1.25 → v1.26. FIRINGS: FIVE.**
Session ~11:12Z → ~18:1xZ. Wrap fired by the BUILDER'S WRAP-PING on Magnus's call
(standing rule, its Nth consecutive firing). Answered from the day's artefacts — the
coordination tail, `git log`, `QUEUE.md`, the two banked reports — **not from memory**.

---

## Q1 — CONSUMPTION. Of what I produced, what changed a decision?

**CONSUMED (another lane acted):**
* **Same-day rated decode** → produced #120 and the TRRR matchup read; builder consumed
  the v48 shift ("every TRRR number stale for the live pairing").
* **QUEUE #120** → **RESOLVED same day**; its registered study-first gate held and the
  decode's own recommendation (**build nothing TRRR-specific**) was consumed verbatim by
  the builder — *a row that correctly spent zero build time is this lane's cheapest win.*
* **QUEUE #121/#122/#123** → admitted, side-lane certified, **entered the v624/v626 design
  set**; #123's n=1-opponent caveat became a **binding both-ways fixture precondition** in
  the builder's plan.
* **QUEUE #124** → admitted → re-anchored → design-constrained → re-costed → **composed
  into the ADOPTED v628 head as the STAGE variant, same afternoon.**
* **§4.3 rider** (wrong-subject causal glue withdrawn) → closed the builder's ask.
* **M7 cause-filter rider + SPEC** → the instrument defect is now bounded in the doc that
  carries the freshest load-bearing M7 table, with every affected doc named.
* **FIRE ORDER** → staged behind Magnus's Q1/Q2 at the builder's own statement.
* **Opponent-iteration read** (Ouroboros +346/28h) → labelled observation; side lane
  attached a prospective re-derivation toll; **not yet consumed by a decision, correctly.**

**PENDING:** the v631 flow-watchdog seed and the v629 minus-one decomposition (builder
handed both for admission judgement — inherited, not dropped: named in the state note).
**UNREAD: none I can name.** Every artefact was relayed and acknowledged by a named lane.

## Q2 — LATENCY. Before or after the decision it bore on?

**BEFORE on everything that mattered.** #124's costing amendment beat the build (the
builder caught the weld *while I was still writing the row*, and the fix landed before
`_v627latency` was screened). #123's caveat beat the v624 design set. The M7 rider beat
v629's readout. The fire order beat the panel pricing.
⚠ **AFTER, and it is the honest one: the two banked docs' game-context riders.** Magnus's
directive landed 11:16Z; I banked two docs without it and added riders at ~12:1xZ. **The
commissioning briefs predated the directive; the BANKING did not, and banking is when the
directive binds.**

## Q3 — RELAY FIDELITY. One relay diffed against its source.

Re-read the **lingling v86 relay** against the banked report: the 54.4% share, the 18
clusters, the below-floor statement, the R1–R7 refutations and the "no incumbent claim
made" scope all carried intact, **including** the caveat I was most tempted to compress
(the scale estimator is *not validated*, so no absolute scale figure is quotable).
**Q3b (payload right, summary statistic wrong): did not fire.** ✅
⭐ **Q3-family extension EARNED ITS PLACE AGAIN:** every evidence row and relay this
session named the SUBJECT TREE (Mjolnir vs Skalman) unprompted — the s55 defect that
produced #119's withdrawal did **not** recur, across four rows and two studies.

## Q4 — DID MY OWN CHECKS FIRE ON MY OWN WORK?

**SELF-CAUGHT: 2. PEER-CAUGHT: 6. The ratio went backwards, and the composition is worse
than the count.**
* Self-caught: the **RELIGHT flag-off** defect (found while re-anchoring #124 — i.e. found
  by doing a peer's correction thoroughly, not by an independent check); the **rider that
  missed its commit** on an anchor mismatch while its tail note went out (caught on the
  read-back, fixed in minutes, owned in-record).
* Peer-caught (side lane 4, builder 2): the stale `_v622nestfall` grep pointer · the stale
  `_v626nestclear` "LIVE head" label · the CUT-120 context/vocabulary gap · the CUT-120
  header stamp · the §4.3 wrong-subject "because" · the #124 weld costing.
⛔ **THE HONEST READING: I published six load-bearing artefacts and my own first pass
caught almost none of their defects.** The generator-vs-auditor asymmetry (v1.4) explains
part of it and excuses none of it — **two of the six were the SAME class twice in one row.**

## Q5 — WHAT I DECLINED, AND WAS IT RIGHT?

* **Declined to spawn a third audit session** on the 2/6 `audit_trigger` fire — both peers
  independently diagnosed the tripped cells as known-broken instruments; a third session
  would have manufactured the volume the trigger fires on. **Right.**
* **Declined to commission a TRRR v48 study** at n=5 — volume has not cleared the
  move-miner bar, and a study on one match is the n=1 class. **Right; staged instead.**
* **Declined to fold the second immobility-premise site into its own row** (#121 carries
  it) — one premise, one row. **Right.**
* **Declined to re-verify the Ouroboros climb figure** for the observation note, and
  accepted the side lane's prospective toll instead. **Right, and cheaper than either
  alternative.**

## Q6 — PER LANE, AND RETRACTIONS.

* **BUILDER:** got numbers before decisions four times (#120's build-nothing, #123's
  precondition, #124's constraint, the fire order's cost plank) and **two rows that became
  build items the same day.** Everything survived.
* **SIDE LANE:** got re-derivable primaries every time; they re-derived every load-bearing
  count digit-for-digit and **caught four of my six defects.** I caught none of theirs —
  and unlike s31 I am not going to call that symmetric: **it was not.**
* **RETRACTIONS REACHING A LANE: 2** (the `_v626nestclear` head label; the "ledger SHIPS"
  claim). Both were withdrawn before any build consumed them. **Down from s55's higher
  count, but see Q4 — the fall is because peers caught them faster, not because I published
  fewer.**

## Q7 — CAUSE WHEN I HAD ONLY AN EFFECT?

**One firing, inherited rather than committed: the §4.3 "because" in the s55 decode** — a
causal weld between BC's *offensive* siting and its *defensive* clearance metric, unmarked
as inference. **I verified it on the primary before amending**, which is the rule working.
No new causal labels were shipped unmarked this session; the Ouroboros/I Stone velocity
contrast was published as `OBSERVATION` with the causal half explicitly *unattempted*.

## Q8 — DID I READ THE COLUMNS I ADDED?

Yes, and it is the day's near-miss: I **wrote** an M7 spec and **read** `now.py`'s freshness
lines at boot and wrap. ⚠ But `SK_TUBE_RELIGHT`'s *value* is exactly the column I did not
read while citing its gate — Q8's defect wearing a code-anchor's clothes.

## Q15 — DID A CORRECT FINDING CARRY AN UNCHECKED ONE?

**Yes, twice, both in #124:** a correct latency thesis carried an unchecked head label; a
correct pre-prep anchor carried an unchecked ledger-liveness claim. **The correct half lent
its credibility to the unchecked half both times** — Q15's exact shape, and the reason the
new class below is worth a rule rather than a resolution.

---

## ⭐⭐ NEW Q17 — **LIVE-FROM-CODE-PRESENCE: DID I CLAIM CODE *RUNS* FROM SEEING IT EXIST?**

**Incident (s56, twice in one day across two lanes):** I wrote *"the down-clock ledger
SHIPS"* from the presence of `if SK_TUBE_RELIGHT:` in `sk_roles.py` **without reading the
flag's value** — `SK_TUBE_RELIGHT = False`, built, dosed, measured and shipped OFF, with a
recorded postmortem that then became the row's binding design constraint. The side lane
recorded the class firing independently the same day.

**THE MECHANICAL FORM: a claim that code RUNS cites the FLAG'S VALUE and the CALL PATH,
never the gate's existence. Grep the flag's ASSIGNMENT in the same breath as its gate.**

⭐ **AND IT IS ONE FAMILY WITH s55's TOKEN-GREP RULE, so they are stated together:**
*a search that can only return confirmations is not a check.* Token-grep finds only callers
that pass the token; gate-grep finds only that the branch was written. **Both feel like
verification and neither can produce the disconfirming case.**
**FAMILY RULE: name what result would DISCONFIRM the claim, and confirm the search could
return it, BEFORE running the search.**

## ⭐ NEW Q18 — **DID I STAMP A POINTER FROM TRAFFIC INSTEAD OF FROM AN ADOPTION CLAUSE?**

**Incident (s56, three firings in one day, the third running FORWARD):** GREP cells stamped
`_v622nestfall` after v623 adoption, then `_v626nestclear` labelled "the LIVE head" while
v626 was an **unadopted candidate under a running grid** — a promotion invented by reading
the newest version number in the coordination traffic.
**MECHANICAL FORM: the line head comes from the last "remains line head" ADOPTION clause,
never from the newest version number in the traffic, and never from directory mtimes.**
*(Anchors are tree-specific — the same flag sat at :3124 and :3148 in two trees — so a stale
pointer is a material defect, not a cosmetic one.)*

---

## Closing line A — WHAT WOULD I NOT PRODUCE AGAIN?

**#124's first stamp.** The row's thesis was right and survived every challenge, but I
published its GREP cell with a head label taken from traffic and a liveness claim taken
from a gate — **two unchecked clauses riding one correct finding**, inside the single
artefact type (a GREP cell) whose entire purpose is to be the checked part of a queue row.
The row is now the best-formed latency row the queue carries; it took three peer catches to
get there, and none of the three needed to be a peer's.

## Closing line B — WHAT DID A PEER CATCH THAT MY PROCESS SHOULD HAVE?

**The side lane's stale-pointer catch, twice.** Both were one `grep` of the tail's adoption
clause away — the s28 lesson (*before asserting, grep*) in a new costume. **Converted into
Q18 rather than a resolution**, because "I will check the head next time" is exactly the
comfortable non-routing this file exists to catch.

## FIRINGS (5) — sunset clause NOT armed

1. **Q5 produced four recorded declines, all upheld** — including refusing to spawn the
   audit session the trigger invited and refusing an n=5 study.
2. **The Q3-family subject-tree rule (s55's scar) held across four rows and two studies** —
   the defect that withdrew #119 did not recur.
3. **The routing rule put every finding in a booted file or a queue row** — zero standalone
   unrouted documents; the M7 finding became a rider **plus** a dated spec.
4. **Q17 born with two same-day incidents and stated as a FAMILY with the s55 rule** — the
   first time this file has generalised two scars into one mechanical check.
5. **Q18 born from three firings in one day, one of them forward-running** — and the
   corrected keying (adoption clause, not traffic) was adopted mid-session and held on its
   next use (#124's re-anchor, verified before stamping).

## The one metric — decisions changed ÷ subagent invocations

**2 agents commissioned; both banked; both refuted premises of their own briefs**
(lingling: the launcher-family label and the kidnap-risk premise; TRRR: the wall/heal shape
the row anticipated). **Decisions changed: ≥6** (a build cancelled, three rows into design
sets, one row composed into the adopted head, one instrument bounded). **Ratio ≥3.0.**
⭐ **The fresh-commission design paid for the fourth consecutive session, and both agents
again refuted the framing they were handed — that is the mechanism, not the mood.**
