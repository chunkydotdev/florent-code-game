# RESEARCH ARM RETRO — s58 instance, 2026-08-24 (SEASON-END WRAP; fired by the builder's wrap per the standing rule)

**GAME CONTEXT: in-game Florent Code League. Answered from the day's artefacts
(14 research commits, the cross-lane message log, the coordination tail),
not from memory.**

## Q1 CONSUMPTION — every artefact, marked
CONSUMED (another lane acted): pooled w1–w20 readout + both corrections (exit-bar
language + ledger restoration) · kladde 4th-cell NO (cell not added) · field
decomposition 255 games (w22+ survival dials adopted) · **dial floors r38/r68/r145
(priced w22's refusal before it fired — the builder called the verdict "cheap and
clean" because of them, and they exposed SK_PREEMPT_ORE_ROUND=60 as too late)** ·
same-day rated decode + version-churn watch · the 05:16 burst investigation
(ledger amended, exposure priced into w22+, x3r0 discriminator parked for Magnus)
· w21 pin observation (w22+ stayed pinned to v266, floor = w1–w20) · **LOSSMODE
v188 + addendum ("the spine of the new plan" — the builder's words)** · DinooniD
v81 study (43.2% + map table, cited in the final record) · season final readout
(cited in the final HANDOVER) · wave-nondeterminism scope check.
PENDING-ARCHIVE: QUEUE #135 (stocked clean; the season ended before any build).
UNREAD: none I can name.

## Q2 LATENCY — zero archaeology
Every load-bearing item landed BEFORE the decision it bore on. The clearest:
dial floors beat the w22 registration; the loss-mode decode beat the fork/rush
plan by minutes and redirected it; the rush anatomy beat the rush tuning.

## Q3 RELAY FIDELITY — one relay re-derived
The move-miner's "51.8% modern vs DinooniD" was verified against the corpus
before pricing anything and found misleading for v81 specifically (43.2%,
54/125) — the verification replaced the number instead of relaying it. Hedges
carried inline throughout (the 1.7σ night-era note shipped as "direction note,
NOT a level claim" in the same clause).

## Q4 OWN CHECKS — 3 pre-emptive self-catches vs 2 published-then-retracted
Pre-emptive (cost no peer anything): the kladde sentinel FALSE ZERO (case-
sensitive kind match — the positive control fired on its first use and the
corrected 265/265 shipped instead) · the builds.tsv turrets-only artefact
("DinooniD builds no economy" killed by smell-test control pre-publication) ·
a map-dims join error caught at the traceback.
⛔ **Published then peer-caught: TWO, both by the side lane.** (1) the w14
coverage label ("pooled w1–w20" over 19 windows — D21 class); (2) **the
holder_watch "blind since 08-22" claim — a FILTERED grep's output read as the
file's tail**, published to two lanes and into the builder's wrap debts before
the 30-second unfiltered read that refutes it.

## Q5 DECLINES — three, all recorded at decision time
M7d anomaly-5 confirm (anchors died with an agent transcript, subject arm dead,
instrument uncontaminated; discriminator named for cheap overturn) · the
admission-drafts commission WITHDRAWN on the 4h-left reality after seven agent
overload deaths (owed-admissions list banked for any successor) · deeper
map-geometry decode declined inside the timebox (named in the addendum).

## Q6 PER LANE
**BUILDER:** numbers before decisions all day; two verdicts priced pre-fire;
one screening hazard flagged proactively (self-play cannot exercise Mode A).
**SIDE LANE:** re-derived my pooled tallies by code (clean at the digits, wrong
at the coverage label — their catch); I caught their incomplete corrected-pool
sweep (their own ledger) and refuted their burst hypothesis with the v188/v190
cuts, which they accepted as clean. **RETRACTIONS REACHING A LANE: 2** (both
mine, both side-lane-caught, both retracted same-hour with mechanism named).

## Q7 CAUSE vs EFFECT
Burst mechanisms shipped as ranked INFERENCE with discriminators; the sharpest
discriminator was correctly identified as a HUMAN question (x3r0) and routed to
Magnus instead of being resolved by assertion.

## ⭐ FIRINGS: 4 — sunset clause NOT armed
1. **The v1.26 family rule ("a search that can only confirm has not checked")
   fired pre-emptively twice** — kladde false-zero, builds.tsv artefact — the
   first session it has fired BEFORE publication on this lane's own work.
2. ⛔ **New incident → new mechanical rule (bump to v1.27): the FILTERED-READ
   rule. A "latest/last-entry/nothing-since" claim about a file cites the
   UNFILTERED read (bare tail on the file itself); any grep standing between
   the surface and the claim voids "the last entry is X".** Same family as
   Q17/Q18 (evidence narrower than claim), new surface (logs), and committed
   by the lane while policing exactly that class in others' work.
3. Q5 produced three declines recorded at decision time, one of which
   (admission withdrawal) converted seven agent failures into a banked
   successor list instead of a silence.
4. The wrap-on-builder's-wrap rule fired (this document).

## Closing lines
**A — would not produce again:** the holder_watch claim as published. The
unfiltered read cost 30 seconds; the retraction cost two lanes' attention and
a wrap-debt edit.
**B — peer caught what my process should have:** both catches were one
unfiltered read away. The v1.27 rule is the conversion.
