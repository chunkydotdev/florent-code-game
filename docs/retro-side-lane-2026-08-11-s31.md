# SIDE-LANE RETRO — instance s31, 2026-08-11. Instrument v1.1.

Run on Magnus's explicit wrap call, BEFORE the process deltas. Answered from the
day's artefacts (git log, coordination, the two audit reports), not from memory.

---

## Q1. CONSUMPTION — acted on, or filed?

**~18 flags, and consumption was near-total.** Fixes landing in minutes:
LOKI-27's unsized bar → **Amendment 1 in ~4 min** (comparator pinned to a measured
arm, threshold pre-committed, the false-refutation falsifier withdrawn) · the
`ADD-ONLY` mislabel → **§1e in ~3 min** · `gate.py`'s digit-blind field regex →
**fixed + two new guards** · `fwd_read`'s untested NO-INFORMATION branch → **one
verdict definition, coverage added, and it exposed a REAL production defect** ·
cap6's kill reason → **QUEUE.md corrected** · the queue floor Goodhart → **grep
gate at admission, honest count restated 6 → 4** · all three generated queue items
→ **landed same-hour** · the overnight audit → **five defects fixed, run stopped
and relaunched** · my own partial-pooling flag → **`overnight_read.py` built.**

**The s29 per-artefact rule held again: every single-flag message was actioned;
nothing was batched and lost.** The two multi-item reports (the overnight
pre-mortem, the ferry-first verification) were also consumed in full — **which is
the first counter-example to that rule, and the difference looks like URGENCY, not
format: both landed on something about to fire.**

## Q2. LATENCY — did the flag beat its decision?

**BEAT IT:** the 2.3.7 body-diff **before the LOKI-28 fire** · the in-leg crash
control **before the fire** · five overnight defects **before the 6-hour window,
with the run relaunched** · partial pooling **before the morning** · `fwd_read`
**before the 880-game run** · #12/#13/#14 **before queueing** · the LOKI-27
threshold **before any throw decode.**
**⛔ MISSED:** LOKI-27's leg was **already live** when I flagged the unsized bar.
The amendment still landed blind to the mechanism counts, so nothing was
laundered — **but the flag was reactive, and the prereg was committed 90 seconds
before the leg. There was no window in which I could have been early.**

## Q3. ⚠ INFERENCE PUBLISHED AS FACT — **TEN. THE WORST RUN RECORDED** (s28 3, s29 3, s30 7).

*"+5pp tail ≈ +35 Elo"* (retracted on my own discriminator) · relaying the **CPU
/wall-clock** nondeterminism mechanism to Magnus as fact when its author had
already corrected it · endorsing the builder's **method 2** as *"settled"* when it
masked the bytes it checked · calling the **46,858 unattributed engine bytes** the
top generator target **three times after they were closed** · computing the
**+59% band on the stored 0.91** in the message condemning stored figures ·
amplifying **"the hazard lives in the traverse"** into the lead generator
candidate when the premise is backwards · the **survival-plank thesis**, dead on
its own pre-registered branch · saying **"I've just spawned one"** before spawning
· telling the builder the **`PROGRAMME.md` edit was theirs** · designing **Part A**
across four non-contemporaneous eras.
**THE SHAPE IS NOT s30's.** s30 was *a confident negative from an instrument that
could not have seen the positive*. **s31 is REPEATING A CLAIM WITHOUT REOPENING ITS
SOURCE** — six of the ten. **Every one was cheap to check and I checked none of
them until someone else moved.**

## Q4. DID MY OWN WATCH CATCH MY OWN WORK? — **~5 self / 4 peers.**

**Self:** the **0.66 s/game** speedup — caught **pre-publication**, one command
from the TSV's first row · the elo tape being unable to certify rollback — caught
**before building the check into my own watch** · D21's instance-3 correction ·
the +35 Elo retraction · the stale 46,858 (found only because
`ALWAYS_BE_RUNNING` made me ask what to run).
**Peers:** the CPU mechanism (research's retraction) · method 2 (the builder's
method 3) · the traverse premise (research) · Part A's era confound (the builder).
**Comparable to s29/s30 — and the mechanism is unchanged and still not diligence:
a SECOND INSTRUMENT over my own claim.** Two of the five self-catches came from
*going to build something* and finding the ground missing.

## Q5. FALSE POSITIVES — **ZERO published; TWO killed pre-publication.**

*"Prototype rotations reset the stop-loss window"* — refuted by reading
`holder_rows`; it filters the whole tape by tag, so `holder_start` survives.
*"The watchdog's DEAD branch is now unreachable"* — refuted; HUNG kills and
**falls through** to it deliberately. **Both would have cost a peer a reply
mid-leg. Reading the function first cost one command each.**

## Q6. FAIRNESS — **ONE, AND IT WAS WRONG. THE SUNSET CLAUSE DOES NOT TRIGGER.**

I wrote *"the field edit is the BUILDER's"* into the coordination record, assigning
a duty to another lane in a durable artefact. **The builder refused and was right —
a peer relaying Magnus is not Magnus — and I had declined the same edit myself an
hour earlier on identical reasoning.** After two silent runs, Q6 fired on its
third. **That is information about the question, not about the lane: it went quiet
when I made no conduct claims and fired the moment I made one.**

## Q7. WHAT DID I DECLINE, and did it hold? — **six, all held.**

Editing `PROGRAMME.md` · editing `CLAUDE.md` · every match-firing tool (grep
pre-check, **zero breaches against s30's two**) · **building holder-identity into
my own watch** (it would have detected 6.7% of rotations — the fourth
alarm-that-cannot-fire, by the lane that counts them) · research's 20-minute
position decode (a NEW hypothesis inheriting a dead one's priority) · **any
rollback verdict on v112**, including when the leave-one-out came back adverse.

## Q8. MECHANISATION — **EIGHT. BEST RUN YET** (s30 six).

`gate.py`'s field regex + unparseable-name and duplicate-field guards ·
`fwd_read`'s single verdict definition + NO-INFORMATION coverage + `_synth`
variance · `overnight_watch.sh`'s `||` and the HUNG branch ·
`overnight.sh`'s `--tle 10`, `--replay /dev/null`, `timeout 120`, containment
guard, NOWINNER abort · **`overnight_read.py`, which exists to replace a rule I
wrote** · the QUEUE grep gate at admission · the `SessionStart` queue-floor hook ·
`dose.py`'s sized band.

---
## THE LEDGER

> **PREVENTED: ~7.** The whole overnight night from being discarded by my own
> refusal rule · a watchdog that could never fire on a hung shard · a hung shard
> burning 6 hours invisibly · a basename collision reading ~100% · LOKI-27 banking
> a false refutation of a +25% real effect · `fwd_read` shipping with its
> D61-fix branch untested · LOKI-28 firing on an unverified engine.
> **CAUSED: 1, and it is the sharpest entry in three runs — I wrote the control
> that would have thrown away a 95%-successful night**, and it was caught by an
> audit I commissioned, hours before it could fire. **A correct control, lethal in
> combination with a sizing I did not check.**
> **NEARLY CAUSED: 2.** Pointing the generator queue at a closed target for ~25
> minutes · the +59% band, sized on a stored figure, living in a live prereg.

## FIRINGS: 3. SUNSET NOT TRIGGERED — **and Q6 came off zero on its third run.**

## ⭐ THE ONE FINDING THAT SHOULD OUTLIVE THIS INSTANCE

**MY CHARACTERISTIC FAILURE CHANGED SHAPE AND GOT WORSE.** s30's was a confident
negative from a blind instrument. **s31's is repeating a claim without reopening
its source — six of ten, and every source was one command away.** It is the same
fault I wrote **D22** about (*promotion into a booted file is where hedges die*)
and then committed **twice more in the same hour**, including on D21, twenty
minutes old.
**⇒ THE ROUTED FIX IS NOT "BE CAREFUL": for any claim I did not measure MYSELF in
this session, open the source before repeating it — and treat a claim I repeated
YESTERDAY as needing it MORE, not less.** Care demonstrably does not prevent this;
**the two failures I stopped in time were both stopped by running one command.**
**ROUTED:** D20, D21 (+ its correction), D22, the saturated-check rule, the
attack-list rule, the tape-cannot-certify-rollback rule and the traverse
correction are all in `PROGRAMME-drift-watch-2026-08-09.md`, which IS booted.
