# SIDE-LANE RETRO — INSTANCE s32, 2026-08-11

**Instrument: `docs/side-lane-retro.md` v1.1.** Run at Magnus's explicit wrap
call, **before** the process deltas, per the lane's protocol. **Answered from the
session's artefacts** — 27 side-lane commits of 75 in the window, the
coordination notes, and the peer message log — **not from memory.**

---

## Q1. CONSUMPTION — were flags ACTED ON, or filed?

**~25 distinct flags raised. Effectively all consumed; ~15 changed an outcome.**
Named, because the question requires naming what each changed:

| flag | what it changed |
|---|---|
| Part A ladder signs inverted | **research corrected `cf843c3`; the Part A verdict reversed from INVERTED to AGREE** |
| kidnap closure selected on its own outcome | **retracted `ff26614`; sign REVERSED −0.080pp → +0.265pp; a closed road reopened** |
| LOKI-29 had no contemporaneous null | **`SRNULL0` added to the spec 6 minutes before launch** |
| LOKI-30's primary bar could not fail | **Amendment 1: bar replaced, n fixed at 12 matches, leakage repriced** |
| the wire carries `tled` explicitly | **Amendment 2 — and it became the v114 SHIP GATE** |
| spawn-sort is a second absolute-geometry site | **Amendment 3; arm 2 `_v152seatrel2` built** |
| partition components ≈ their own error bars | **Amendment 4; claims narrowed; negative-share pre-declared** |
| §7.2 vs Amendment 3 contradiction; obligation 12's missing branch | **Amendment 6, three rulings** |
| gate escape's docstring claimed a feature the code lacked | **reason-string requirement, refuses without one** |
| monotonicity guard cannot fire on a first poll | **startup refusal shipped, exit 3** |
| `FX` fixture wrote into the live run | **`OUT=${OUT:-…}` fixed in BOTH runners** |
| D14: QUEUE #8 vs PLAN control 3 on `NOISE_ON` | **QUEUE rider became a POINTER** |
| 63 trees carry `NOISE_ON=False` under a degenerate-map prescription | **QUEUE #15 created** |
| band ≠ MDE; multiplicity is 5 arms not 7 | **taken into the read-out; CAP12's escalate did not survive** |
| CI built from marginals at their extremes | **read-out corrected to "two signed cells, one marginal"** |
| 1650 floor in zero enforcing surfaces | **wired into `target_value.py` AND `PROGRAMME.md:25`** |
| half-idle machine, stale `EXPECTED_GAMES` | **9→5, `corefill.sh` started, 3 shards added** |

**Two consumed as SAVINGS rather than changes:** `--replay /dev/null` (research's
planned corpus cross-check was impossible — a cut saved) and the LOKI-29 early
integrity check (clean; the run was left alone).

## Q2. LATENCY — did the flag beat the decision it bore on?

**Yes on every load-bearing one, and two were measured in minutes:**
* Part A signs — **~20 min before the read-out**, which is the decision they would
  have inverted.
* contemporaneous null — flagged 18:31, **`SRNULL0` in the spec 18:33:31Z,
  launch 18:39:20Z. Six minutes.**
* LOKI-30's dead bar and unfixed n — **before leg creation.**
* `tled` — before the leg read, and early enough to become a ship gate.
* the 1650 floor — **before the next prereg ran the gate it would have
  contradicted.**

**Two landed after their immediate decision and before the downstream one:** the
spawn-hash site (after §4 read out, before §5 was designed) and the kidnap
closure (after publication, before it reached a build).

## Q3. ⚠ INFERENCE PUBLISHED AS FACT — this lane's characteristic failure

**FOUR PUBLISHED. s28: 3 · s29: 3 · s32: 4 — the worst recorded, and the SHAPE is
now singular where it used to be varied.**

**All four are the same fault: I inferred from an ARTEFACT instead of opening the
PRIMARY.**
1. **Cleared `c541aae` on a `--stat`** — reported "audited, on-programme, no flag"
   having read the subject line and the file list.
2. **`cores_idle` "trips only on TOTAL idleness"** — FALSE; the code is
   `if n < EXPECTED`. I read the docstring and `PROGRAMME.md`, **both stale
   relative to a fix my own lane shipped one session earlier.**
3. **`FX` called "channel pollution"** — `OUT` was hardcoded; the fixture ran
   against the LIVE run and **launched a stray shard.**
4. **Urgent pairing flag without reading `Active bot:`** — sent off the existence
   of an arm file. **The fault was inside the sentence recommending that line.**

**⭐ AND THE DIRECTION IS NOT RANDOM: all four ran toward the COMFORTABLE
reading** — a clean clearance, a defect that was someone else's, a smaller
severity, a danger that had passed. **s28's ran toward the dramatic; mine run
toward the reassuring.** Both are removed by the same act.

## Q4. DID MY OWN WATCH CATCH MY OWN WORK?

**4 of 5 self-caught** (s28: 0 of 8 · s29: 4 of 5). **Holding, not improving.**
* self: the `cores_idle` code read · the `FX` diff · `fcode status` one minute
  after the pairing flag · **the z-denominator objection killed BEFORE sending**
* external: the `--stat` clearance — surfaced only because **its own author
  questioned the commit I had waved through.**

**The mechanism is unchanged from s29 and is not diligence: running a SECOND
INSTRUMENT over my own claim.** The z-denominator was killed by re-deriving from
the builder's own reported z-values; the pairing flag by running one command.
**Both first passes were careful and wrong; both second passes were mechanical
and right.**

## Q5. FALSE POSITIVES — what did flagging cheaply cost?

**TWO wrong, neither caused harm, each cost one correction.** `cores_idle` (the
defect did not exist) and the pairing urgency (the risk had already passed).
**One NEARLY caused harm and was stopped pre-send:** the z-denominator
"correction" would have moved atoll from −2.16 to −1.51, removed the only
significantly-below-50 map, and **destroyed the builder's turn-order kill on my
arithmetic error.**

**⚠ INSTRUMENT OBSERVATION: Q5 and Q3 measured the same thing this session.**
Both false positives are Q3 instances. **A future bump should ask whether Q5 is
still discriminating or has become Q3's tail.**

## Q6. FAIRNESS — conduct claims about another lane?

**ZERO.** Characterisations offered were labelled as readings (*"I read it as you
driving the DEAD branch"*) and one was understated rather than unfair. **No claim
about what another lane DID was published unverified.**
**⚠ Q6 has now produced ~nothing across three runs. The sunset clause named it as
a likely candidate and this is the third quiet run.**

## Q7. WHAT DID I DECLINE, and was declining right?

**Four, all held:**
1. **Withheld the LOKI-29 effect direction at 8% of n** — on the leg's own
   prereg (*"NO INTERIM PEEK MOVES ANYTHING"*), with the numbers offered to the
   leg's owner if they ruled otherwise. **A new kind of decline for this lane:
   withholding a computed number from the person entitled to it.**
2. **Did not kill the zombie shards** — flagged instead; resolved in ~3 minutes
   by their owner, who then documented a kill-order trap (by PID, never by name,
   because `SRNULL0` and s31's `NULL` are the same bot pair) that a blind kill
   would have walked into.
3. **Did not drive the watchdog's POSITIVE branch** — it would have launched real
   shards. **The decline produced a finding: the start path has no safe test.**
4. **Did not veto the v114 ship.** Audited, reported clean, left clause (b) to
   its owners.

## Q8. MECHANISATION — did any flag become a SCRIPT?

**SIX became enforced code — the strongest Q8 recorded (s28: three).**
`gate.py`'s reason-string requirement · `overnight_watch.sh`'s **startup refusal**
(exit 3) · its **monotonicity guard** · `OUT=${OUT:-…}` in **both** runners ·
**`TARGET_RATING_FLOOR: 1650`** in `PROGRAMME.md` + `target_value.py` · and the
**`tled` bar**, which became a **ship gate** rather than a leg statistic.

**⭐ THE MECHANISM BEHIND THE JUMP, and it is a practice worth keeping: I stopped
flagging defects and started flagging defects WITH THEIR FIX.** The startup
refusal was my option 2. The `tled` bar named the field, the file, the live
column and its positive control. **A flag that arrives with a buildable
replacement gets built; a flag that arrives as a criticism gets acknowledged.**

---

## THE LEDGER

> **Prevented: 6. Caused: 0. Nearly caused: 1.**

**Prevented** — a pre-registered *"stop screening for the week"* firing on a
falsification that never happened · a road closed on an estimator selected on its
own outcome · a partition whose baseline would have carried a contention change
into the CARDINALS share · a ship gated on a bar that could not fail · a prereg
fired with no fixed n · a read-out quoting a stale MDE and an over-counted
multiplicity.
**Caused: 0** — both wrong flags were corrected before anyone acted.
**Nearly caused: 1** — the z-denominator correction, stopped pre-send.

---

## ROUTING — every finding, at write time

| finding | route | landed |
|---|---|---|
| **Q3: the characteristic failure is now singular — INFERRED FROM AN ARTEFACT, 4 of 4, all toward the comfortable reading** | **behaviour change → promote to `PROGRAMME-drift-watch-2026-08-09.md`** (booted) | ✅ this session |
| **Q8: flag WITH THE FIX — the practice behind 6 mechanisations** | **behaviour change → promote to the drift-watch** | ✅ this session |
| **Q5 has become Q3's tail; Q6 quiet for a third run** | **instrument change → v1.2 bump of `side-lane-retro.md`** | ✅ this session |
| **Q7's new decline type (withholding a number from its owner)** | **instrument change → named in the v1.2 bump so it is countable next run** | ✅ this session |
| the banner-concatenated CLI JSON (`…fcode{"matchId":…}`) | **flag to the builder; `tools/` is theirs** | raised, **NOT YET ACTIONED** |
| `cores_idle.py:31` docstring and `PROGRAMME.md:60-62` are stale against the shipped `n < EXPECTED` | **flag to the builder** — the stale text is what produced my own Q3 #2 | raised |
| the 1650 floor's **rating-dependence** (39 → 16 → 0 points of room as we fall) | **OBSERVATION — ROUTED to Magnus in-session**, since the field is his | reported |
| LOKI-29's remaining reads (partition, §5, §6) | **OBSERVATION — NOT ROUTED.** The leg lands ~23:00Z and belongs to its owner | — |
