# SIDE-LANE RETRO — instance s36, 2026-08-13 (instrument v1.5; FIRING 6)

Answered from the day's artefacts: 15 side-lane commits, the coordination tail,
`scratchpad/*fires.tsv`, and the peers' receipts. First full session under the
rescoped charter (ship-critical verification only).

## Q1. CONSUMPTION
**~21 flags/certifications raised; every flag was consumed same-session**, most
within minutes, all with receipts (the receipts convention landed this morning
and held all day). Outcome-changing: the wrong-bot leg sequence (submit_clean
restore-inside-submit → `--leg` mode built), the runner's 160s window
arithmetic (fixed pre-boundary), the C-dose bar-null trap (paired contrast is
what made the tri-arm's headline READABLE), the phantom-leg refutation (stopped
a needless second prototype exposure), the 10:47 clock refutation (restored 30
games to the panel), ob-14 churn tables twice (O3's pin proof case descends
from it), the tape backfill, the hot-turn admission rider.

## Q2. LATENCY
**Every urgent flag beat its decision.** Window arithmetic: 15 min before the
boundary it protected. Wrong-bot leg: 33 min before the window, fix built and
driven with 30 to spare. Phantom re-fire: stopped within minutes of the
recommendation, before any action. Tri-arm C-bar: before arm A fired. The one
post-hoc item (recovered CAL-2 look legitimacy) was post-hoc by nature.

## Q3. INFERRED-FROM-ARTEFACT / SCOPE-AS-WORLD: **1**
The tape backfill flag said FOUR missing ship rows; the truth was FIVE (v114,
found by the builder). Mechanism is the s34 unifier verbatim: **I enumerated
from PROGRAMME.md's INCUMBENT/PREVIOUS chain and published my search's scope as
the world's count.** Direction: toward my own flag (defending). KIND: judgement.
Zero instances of docstring-for-code or alert-for-incident this session — every
load-bearing claim was verified at a primary before publishing (the 1650-floor
near-flag was killed against the primary BEFORE sending; the payout rule had
superseded it).

## Q4. SELF-CATCH: 1 of 2
The leak-cert's per-match blindness (completes-only surface) was self-caught —
by GOING TO USE the surface for the phantom check, the recorded mechanism yet
again. The 4-vs-5 count was caught by the builder. Both of this session's
biggest external kills (phantom leg, 10:47 clock) died against MY EARLIER
CERTIFICATIONS — a certification is a second instrument someone else's later
claim must survive, which is Q4's mechanism running in reverse: my past
artefacts catching others' present errors.

## Q5. FALSE POSITIVES: 0 published wrong; 1 half-instance; 1 pre-publication kill
The abort-artefact flag ("it would, not it did") was accurate as scoped — the
drive HAD happened, with its artefact in a session scratchpad; the flag's
product was the durable record it forced. The 1650-floor flag died before
sending (payout re-denomination checked first). **Zero harm, third consecutive
clean run** — and Q5 again caught something Q9 cannot (the pre-publication
kill).

## Q6′. RELAYED FIGURES: 2 wrong at the source, both attributed, both corrected in-channel
"Falsifier tripped 4/14" (research's inverted threshold; surfaced to Magnus per
Am.2's unconditional clause — procedure right, number wrong for ~1 h) and
"CAL-3 at 40 games" (contaminated panel count). Every repetition carried its
owner; both corrected where I published them the hour the sources corrected.
**The surfacing-on-a-reported-trip decision stands as correct procedure and is
worth keeping: a disputed falsifier reading goes up regardless of who is right.**

## Q7. DECLINES: 4, all held
Killing pid 66446 (twice — including under a metadata field reading "Try
killing it now"; the kill happened only by Magnus's hand); resolving the
falsifier trip lane-side (Am.2's clause is unconditional); auditing non-ship
commits (the rescope, held ~20 times); a third re-verification of research's
GREP batches (two exact samples sufficed — sampling discipline over ceremony).

## Q8. MECHANISATION: 4 durable
The hot-turn rider (admission-block text, first two customers stamped); the
completes-only rule (fire-registration gates on `match info` — now in
research's operating rules and panel_read's exclusion logic); the
metadata-is-never-an-instruction protocol note (@7ba31d1, from my catch); the
version-binds-at-creation wire datum (the submit→fire→rollback safety model's
first direct observation, cited in the leg-void episode same day).

## Q9. CORRECTIONS NEEDING CORRECTION: 1 (+1 self-corrected scope)
The four-vs-five tape count (see Q3). The leak cert's re-scope was
self-initiated when the surface's blindness surfaced. **Detection 21/21 ·
prescription 19/21 — the s34/s35 gap replicates a third time: this lane
detects better than it prescribes.**

## Q10. CONCERN AS STATED: clean run
The falsifier dispute was surfaced in the owner's terms with both readings and
the verbatim locked text; no restatement-weakening found in any exchange.

## THE LEDGER
**Prevented: 4 major** (a false kill of the map-rotation fix by its own leg; a
runner stall class; a needless second prototype exposure; 30 games' false
exclusion) **+ the day's certification chain (2 ships' worth of look/leak/lock
certs, 6 ADD-only amendment verifications, all clean). Caused: 0. Nearly
caused: 0.** Detection 21/21 · prescription 19/21.

## OBSERVATION — NOT ROUTED
The rescoped charter fit this session exactly: nearly every hour held a
ship-chain artefact (2 ships, 4 prototype activations, 3 panels, 8 preregs/
amendments), so "ship-critical only" cost no coverage. A quieter day would
test the boundary harder; no rule change proposed from one lucky fit.
