# SIDE-LANE RETRO — instance s37, 2026-08-14 (instrument v1.7)

**Session span: 2026-08-13T17:03Z → 2026-08-14T06:54Z (overnight). Wrap called
by Magnus. Answered from the day's artefacts: the coordination tail, my cert
notes (d063650…fd186f3), the flag/consumption receipts in both peers' commits,
and the drift monitor's event stream.**

## Q1. CONSUMPTION — ~16 flags/commissions; consumption near-total, most within minutes
Outcome-changing (each with its consuming artefact): **PAVEFIRST burning cores
on a withdrawn arm** (cancel marker `7b07861`, ~4,100 games refunded) · **bar-3
absent from the feeder45 readout** (D6 → NOTE 2 `0045e4f`, off-branch run with
exposure denominator) · **EVICT45 legacy-throw stratification** (adopted
`91a9e66`; became the BUILT-IN guard by iteration 4 — the class died by
pre-closure) · **GUNAXIS0's missing D26 replication line** (Amendment 1
`b8cf127`; declared-at-queueing became the default on every later screen) ·
**UNDERECO paired-seed estimator** (run same hour `b6239d4`, adopted as the
admission-bar instrument, and it CUT AGAINST the arm its consumer wanted to
escalate) · **wake-gap sweep** (Magnus's question → CAL-3 boundary found
ALREADY PASSED at n=250; consolidated watcher + runner-side stop `0b93794`
landed same hour) · **map-conditional 5th-maps + Obligation-12 flags**
(Amendment 1 `7810c50`, stricter branch, one minute) · **D13 resolvability
rider** (CAL-4 Amendment 1 `6820e60`, then applied at rotation) · **ourver
{131,132} filter note** (ahead of the wire read) · **coupling-row commission**
(TWORAID upheld with the interpretation pinned `002e92d`; QUIET0 disagreement
recorded unresolved+moot).

## Q2. LATENCY — pre-fire audits beat every fire; two races lost, disclosed
All four live-leg audits (rc8.3/8.4/8.5×2, map-conditional) landed BEFORE their
fires. PAVEFIRST caught ~72 min into a ~5h burn. Lost races: EVICT45 flag
(runs started first; vacuous against the 0), coupling interpretation line (gap
commit in flight; research disclosed the race in Amendment A rather than
papering it). One flag was simply wrong after the fact (Q3).

## Q3. THE CHARACTERISTIC FAILURE — 2, and the mechanism is one thing
1. **LAUNCH0 arm identity**: receipted acf4849's D26 line without opening the
   arm behind the figure (it was the NO-launcher arm). Caught by research's Q10.
   `KIND judgement · STATE auditing · toward my own receipt`.
2. **GATE45 bar-4 flag**: pattern-matched print-line numbers to v200's
   structure instead of reading the guard condition eight lines up in a file I
   had open. Caught by the builder's anchor (guard existed, built in response
   to my own iter-2 flag). `KIND judgement · STATE auditing · toward my flag`.
**Same mechanism twice: inferred-from-artefact while auditing — specifically,
reading a PRIOR VERSION's structure as the current tree's.** Count 2 (s35: 5,
s34: 4). Both cost one reply each.

## Q4. SELF-CATCH — 0 of 2 on the wrong flags; the stored-cert mechanism fired positively
Both wrong flags were caught by peers. But v1.6's new mechanism (certifications
as standing instruments) did the biggest single catch of the session on the
POSITIVE side: the live-verified CAL-3 table (20:26Z) changed Magnus's answer
(C6 edge→OUT), and the single-build-site verification (main.py:658/:613) was
consumed as the load-bearing premise of three later preregs without re-derivation.
Every count I certified was re-derived from primaries: 10/25 · 14/25 · 9/25 ·
23/50 · 205/0-at-r≥160 · paired 1151/+11.0/537/610/z2.16 — **all exact, six for six.**

## Q5. FALSE POSITIVES — 2, zero harm, fourth consecutive zero-harm run
Cost: two one-line replies. Both flags carried their fix and their anchor,
which is why the cost stayed at the designed level.

## Q6′. CLAIMS ABOUT PEERS — zero wrong relays
Every relayed figure verified against a primary before repetition (rated-leak
51 matches, Jython v33→v119, the paired decomposition, every leg count). The
one attribution correction owed (research's Q10 catch superseding my receipt)
was owned in-channel within minutes.

## Q7. DECLINES — three, all held
Declined a redundant fallback Monitor (named-handover convention instead —
mooted hours later by the durable runner-side fix, the better outcome) ·
declined to derive CAL-3's n myself (the authority was research's fire log; a
naive count would have hit the R5 trap — and did not, because it was never
taken) · declined verdict language throughout (counts receipted, never judged).

## Q8. MECHANISATION — ~7 flags became code or standing text
Stratified tag scoring (two preregs, then built-in) · paired-seed estimator
(admission-bar instrument + method doc) · D26 replication-at-queueing (default
practice) · D13 resolvability (CAL-4 selection rule + panel_read label) ·
git-time section clocks (universal after one nit) · boundary stop in the
runner (precipitated by the wake sweep; research's ask; builder's code) ·
completes-only rule (verified into the protocol doc, then used to keep the
Leviathan cell's absence from being misread — twice).

## Q9. CORRECTIONS NEEDING CORRECTING — 0, and the LEDGER SPLIT INVERTED
**DETECTION 16/18 · PRESCRIPTION 14/14.** Three prior runs measured perfect
detection with lossy prescription; this run took both losses on DETECTION
(two phantom defects — defects that did not exist) while every consumed
prescription survived contact. **The lane's leading failure is now the
phantom-defect flag, mechanism: reading a prior version's structure as the
current tree's, while auditing.** v1.7 sharpens Q3 accordingly.

## THE LEDGER
**Prevented: 4 major** (PAVEFIRST's remaining ~4,100-game burn · the
map-conditional rule breaking on its first application · the wake sweep
surfacing an already-passed boundary · the paired-seed instrument feeding the
no-ship on the swing leg — shared credit with the builder's pooled rule) **·
caused 0 · nearly caused 0 · wrong flags 2 (cost: two replies).**

## THE ONE QUESTION — did I decide something the machinery could not have caught?
Yes: **declining to count CAL-3's n myself.** No instrument forced that; if I
had computed a naive n from the corpus it would have included rc8.3's 20 leg
games (the R5 trap), read ≥170, and the boundary conversation would have
started from a wrong number in a channel where my counts had been exact all
session — exactly the credibility that makes a wrong one dangerous. The
protection was knowing which lane owns the denominator.
