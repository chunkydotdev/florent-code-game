# SIDE-LANE RETRO — instance s38, 2026-08-14 (~07:00–12:33Z). **Instrument v1.8** (v1.7 at run time, bumped in the same action). FIRING 8.

Answered from the day's committed artefacts (my ~30 tape/cert commits), not memory.
Session shape for context: two ships certified (v139 mapfix, v140 sealrepair),
four x3r0 slot events, the map-conditional test completed negative end-to-end,
the VPS fleet's first box admitted, two Magnus dashboard commissions executed,
~14 preregs/legs/panels certified.

## Q1 CONSUMPTION — effectively total, and fast
~15 substantive flags raised; **every one consumed same-session, most within
minutes**, several upgraded beyond their ask: V134-CHAR spec flag → A1;
units flag → institutionalized in the CAL-5 base; tape-before-action →
adopted as a standing rule same hour; chassis-era ADD → upgraded to a CLASS
rule by the builder; L4REPAIR collision → uncovered a phantom probe (the
new port had NEVER RUN — a stale launch-once marker consumed by a DONE-looking
row); runner-pattern flag → tools/lib hoist with a live kill-path selftest,
catching a 4th unflagged instance. The dashboard BLIND diagnosis (measured:
73 s tool vs 120 s budget) → 20 s fix within minutes.

## Q2 LATENCY — every outcome-changing flag beat its decision
Burst-or-skip arithmetic landed pre-pairing; tape-before-action landed before
MD and governed it; the chassis-era rule landed before any ship case read;
the v137 push beat the pairing it warned about by ~8 minutes and Magnus's
restore followed. Zero post-mortem flags.

## Q3 — TWO published errors, ONE mechanism, and it is NEW to the list
1. **The seat-inverted MD gloss** — I read the wire's scoreA-scoreB without
   normalizing by our seat and told Magnus MD "looks like it clears with
   room"; the decode read net 0. Tagged at occurrence: KIND judgement ·
   STATE surveying · toward the exciting.
2. **The limit-3 era count** ("two more unrated matches") — scope-as-world,
   corrected twice over (research's meta_join "three" was also wrong; the
   wide live-CLI read found four).
**⇒ THE MECHANISM BOTH SHARE, and the sharpening this instance buys: A WIRE
FIELD IS IN THE PLATFORM'S FRAME, NOT OURS.** scoreA/B is seat-framed;
`match list` is window-framed; git dates are zone-framed (the TZ near-miss,
below). **Normalize the frame — seat, clock, units, window — before any
number leaves the lane.** This is the s30 winner_seat lesson, committed by
its own auditor, twice, plus one near-miss.

## Q4 — the self-catch mechanism this session was the DOMAIN CHECK, 2-for-2
Two frame errors were caught pre-publish by illegal values, not diligence:
the TZ trap (a prereg postdating its own fires — impossible, so re-derived
with TZ=UTC) and the false-DIFFER (da39a3ee… = the empty-string sha).
The two that had no illegal value (seat gloss, limit-3 count) were published
and caught externally. **Domain violations announce themselves; plausible
frames do not — D31 measured on the auditor again.** External catches on my
work: 3 (cell-set miss by the builder; both Q3 errors). Stored-cert
mechanism: constructive this session — my minted MB anchors became MD's
cert basis; my NULLHOST re-derivation co-certified fleet admission.

## Q5 — ZERO wrong flags, fifth consecutive zero-harm run
The S3 practice (read the guard's actual condition line) prevented at least
two would-be phantom flags: the map-conditional "STOP both" ambiguity
(answered by the prereg's own text) and the A2 supersession question.
s37's leading failure class produced nothing this session.

## Q6′ — relayed figures clean
The Bisons first-contact figure was relayed WITH its owner and died cleanly
with research's own retraction. The era-count chain kept every number's
owner and window attached through three corrections. My one wrong figure
was mine, not a relay (counted in Q3).

## Q7 — declines, all held
Declined to adjudicate or counter-roll any x3r0 slot event (recorded "not
rankable by any lane" — the principals' channel governed, and Magnus's
restores confirmed the routing); declined duplicate pushes when the builder
had Magnus in-session; declined to edit the builder's worklist on the
L4REPAIR collision (flagged, they fixed); declined to re-litigate the
D26 threshold-pooling nuance against my own sealsweep cert mid-use (noted
for the method file instead — consistency over cleverness).

## Q8 — the compounding ran at its fastest recorded
Flag→rule same-hour, repeatedly: tape-before-action · the pre-v139 class
rule · units into the CAL-5 base · A3 fire-log-id membership · the
runner-pattern lib + selftest · zsh -n at write time (builder's, from the
burst-failure chain) · the shard_descriptions maintenance practice.
The panel template needed FOUR amendments at its first instantiation and
ZERO by CAL-5/CAL-6 — the template absorbed its own review history inside
one session.

## Q9 — one correction needed correcting
My MD boundary criterion ("must carry v125") was overtaken by the v137
re-activation and restated (correctly) as "not v138". The correction of my
own cert note was itself right; count 1. My correction of research's
era-count correction was final and right.

## Q10 — clean; asks answered as stated (the 40 s MC boundary ask answered
with the stronger form it wanted).

## THE LEDGER
**Detection ~19/22** (the cell-set cert miss + two Q3 errors against ~19
verified certs/findings) · **Prescription: all consumed; one (burst) rightly
declined on operational grounds I had underweighted** · **Prevented-major 4**
(phantom probe; the v137/pairing chain; the leg/rollback collision; the
panel-contamination path) · **Caused 0 · Nearly caused 1** (the "clears with
room" gloss reached Magnus; the extraction preceded any decision on it).

## OBSERVATION — NOT ROUTED
The three-lane-plus-Magnus system corrected itself four times today faster
than any single lane could have: my flag wrong→builder catch; research's
correction wrong→my wider read; builder's gloss→their own agent; my
gloss→their extraction. No participant was the reliable one; the STRUCTURE
was.
