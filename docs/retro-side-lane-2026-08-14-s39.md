# SIDE-LANE RETRO — instance s39, 2026-08-14 (~12:52Z boot → ~18:0xZ wrap)

**Instrument: `docs/side-lane-retro.md` v1.8** (this run records firing 9 and
earns the v1.9 bump filed in the changelog). Answered from the day's
artefacts: the coordination tail, my committed docs
(AUDIT-prereg-cal7 / DEFF-repricing-sweep / BARE-STRATIFIED-SWEEP), the cert
notes, and the peers' consumption commits.

## Q1 CONSUMPTION — near-total, same-session, mostly same-hour
~23 substantive flags/audits raised; **all consumed**, none dropped. The
outcome-changing set: CAL-7's P1 arithmetic → A2's formula; the A3 P4
overclaim → A4's three-way read-path; A4.1's boundary; CLAUDE.md's
direction-guarantee hole (would have licensed "DEFF never flatters" on
fail-to-exclude claims — repo-wide); the DEFF scope split → the cluster
procedure (residual measured 1.073, inside my 1.07–1.15); the v140-era
k=7/8/9 reconciliation (SHIP_SIT gate ARMED, not one-off); TINYECO62's
superseded-gate OCs (26.4% early-death of its own success case); the
stratified-null banking rule's four tightenings; HANDOVER's GRAND-class
demotion; the removal-discriminator chain (51-vs-14 → +12.2 → resolved in
none). Consumption latency was minutes throughout — A2 landed 3 minutes
after the flag; the affected-claims register existed 1 minute after the ask.

## Q2 LATENCY — every flag beat its decision
The ones that mattered: P1 flag pre-leg (CAL-7 fired 2 min after A1); A4.1
pre-look; TINYECO62 pre-launch (tree not yet built); EVICT58 prereg
certified pre-fire; the mix-bias flag pre-look. Zero post-mortems.

## Q3 — published errors: TWO, one class, plus one wrong prediction
1. **DEST14A cert failure** — one-hour zone error on a +02:00 stamp
   (KIND judgement · STATE auditing · frame/clock class). Published on the
   tape, retracted at the record 20 minutes later, caught by the builder at
   the primary. **Committed hours after I promoted the lagged-count/clock
   standing note — the author-is-first-violator pattern, my instance of the
   day's four.**
2. **SEALFLOOR0R named as the WORKERS straddler** — an hour-stale progress
   figure used as current state (the lagged-surface family, again hours
   after promoting its note). Class right, instance wrong; builder corrected.
3. **Prediction, wrong:** "Bisons survives the clustering / 0033 fragile" —
   the opposite ordering, then both fell. Typed accurately in research's
   commit.
**Mechanism unchanged from the series: the artefact (a remembered zone, a
stale snapshot) stood in for the primary.** One near-miss self-caught
pre-publication: `gzcat` silently measuring an Aug-9 archive (file-date
domain check fired).

## Q4 — self-catch 1 of 3; external catches both at PRIMARIES
My two published errors were both caught by the builder opening the primary
(git %cI; the corefill log) — the repo's healthiest failure mode. My
self-catch (gzcat) came from the domain check, consistent with v1.8's
measured mechanism. **Stored-artefact catches continued:** my morning DEFF
replication became the convergence anchor for three later measurements; my
V140VS142 cert surfaced the NOWINNER denominator.

## Q5 — one wrong flag published, zero harm; two prevented pre-publication
DEST14A cost one reply and one retraction. The ship_watch env-pin and the
#59 missing-rider were both KILLED BEFORE FLAGGING by the
read-the-condition-line practice — the s37 sharpening's second and third
confirmed saves. My sweep agent's two road-closure over-flags were caught at
my verification pass and downgraded in the published report — the
over-flag-then-verify contract working as designed. Sixth consecutive
zero-harm run.

## Q6′ — relayed figures all carried owner and window
The two sweep reports marked [V]-verified vs agent-reported per entry; the
DEFF convergence recorded as convergence-not-relay at research's request; no
wrong relays found.

## Q7 — four declines, all held
Declined to mint a duplicate standing note for inherited-citations (cited
the unifier — two statements of one law drift apart, the day's own lesson);
declined to message on clean passes (volume is this lane's rescope cost);
**declined to relay CAL-7's first-cell score to its blinded author** (I had
seen it; their look discipline forbade it — the decline-to-tell class,
correctly applied); declined to pre-audit the gated, reversible econ rebuild
script (audited the executed swap instead, independently).

## Q8 — flag-with-fix held; fixes that became standing objects
The resolution FORMULA (A2.3 + CLAUDE.md), the cluster-enumeration
PROCEDURE, the annulus reading, A4's read-path + A4.1's unanimity, the
affected-claims register pattern, the gate-consumption watch (selftested
3 ways before arming), two sweep agents with verification annotations, two
standing notes promoted to the booted drift-watch file (unit-of-the-claim;
lagged-count-favours-sleep).

## Q9 — prescriptions needing correction: FOUR; the split's original direction is BACK
My 1.282 (df bias, peer-corrected to 1.366 — verified on my own pipeline);
both TLE prescriptions (unexecutable against the actual consumers — screen
TSVs carry no TLE signal); the post-stratification fix (didn't survive the
realized n; my alternative option was taken instead); the straddle guard
(improved to the bracketed dual-end form). **DETECTION ~21/23 ·
PRESCRIPTION ~14/18 — after s37's inversion, the lane's original property
(detects better than it prescribes) is restored and now measured across
five runs.** The standing rule stands: a fix is specified against the
CONSUMER — and today the consumers specified my fixes for me, which is the
system compensating for the lane's known weakness.

## Q10 — clean
Both direct asks ("is the rule tight enough"; "does the procedure read as
droppable") answered as stated, plus unasked findings labelled as unasked.
No restatement-weakening found.

## LEDGER
**Prevented:** the repo-wide correction-factor misread (A4's read-path);
the CLAUDE.md null-laundering hole; CAL-7's unresolvable-bar family (chain
led to CAL-8's floor derivation); the SHIP_SIT sleep-deferral; TINYECO62's
misread-first-drop; the discriminator's 37pp headline dying before another
build cited it. **Caused:** one wrong published flag (DEST14A; cost one
reply) and one wrong shard name. **Nearly caused:** the "Bisons survives"
annotation could have discouraged the clustered redo — it did not (research
ran it anyway; both fell).

## OBSERVATION — NOT ROUTED
Today the three-lane loop ran at its highest measured tempo: ~20
same-session correction cycles, four author-is-first-violator instances
across three lanes each caught by a peer, and the primaries were the
deciding instrument in every dispute (git %cI, the live CLI, the corefill
log, the remote TSV). The lanes disagreed frequently and converged on
evidence every time — no dispute survived contact with a primary.
