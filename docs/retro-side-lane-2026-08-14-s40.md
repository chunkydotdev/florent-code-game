# SIDE-LANE RETRO — INSTANCE s40, 2026-08-14. Instrument version run: **v1.9** (bumped to v1.10 in the same action).

**Session shape, stated because it conditions every number below: a ~25-minute
lane.** Boot 18:20:10Z, wrap called 18:46Z. No leg of my own, no sweep agent, no
subagent spawned. Everything here is flags, certifications and one spec, against
a builder and a research arm both booting in the same minutes. **A short session
is not a small denominator on Q1/Q2 — it is a dense one: 9 flags in 26 minutes.**

---

## Q1. CONSUMPTION — were flags ACTED ON, or filed?

**9 flags/prescriptions. 8 consumed, 7 changed an artefact, all within minutes.**

| # | flag | outcome |
|---|---|---|
| 1 | `ship_watch` stale env (`SHIP_VERSION=v116` on the live loop) | consumed by BOTH peers independently; verdict DECORATIVE (3 consumption paths checked); env unset scheduled at next restart |
| 2 | CAL-8 stop premise false + stop reason unregistered | consumed; seal re-decided **pre-look** |
| 3 | CAL-7 spent-analogy lacks its load-bearing look | consumed; folded into the re-decision |
| 4 | seal ground must name a registered clause | consumed → **and was WRONG** (see Q9) |
| 5 | BOUNDARY unobservable on pid 56307 | consumed in ~3 min: `37b1b2b` ARMED echo, runner relaunched, value on the fires tape |
| 6 | PROVENANCE rider (draft agent's inputs on the record) | consumed whole into BOTH lane charters (`53b8560`) + made a required `prereg_check` token |
| 7 | six machine-checkable checks for `prereg_check.py` | delivered at draft time; forced-fail certification offer accepted |
| 8 | `#52` build vs research's DECLINE-IT (D14: two uncited contradicting tail entries) | consumed; crossing reconciled with the decision typed explicitly |
| 9 | `#52`'s 8:1 premise vs the atlas's 4:1 correction | consumed; 4:1 reached the build agent **before its economics hardened** |

**Per-artefact consumption (the v1.1 finding) held again: every single-flag
message was actioned; the one multi-item artefact (the six-check spec) is the
only one whose consumption is still pending.**

## Q2. LATENCY — did the flag beat the decision it bore on?

**All nine ahead of their decision. Three were tight and load-bearing:**
* **CAL-8 flag → beat research's read**, which was announced as starting. The
  read had not been taken when the seal was re-decided; the whole optional-stopping
  question stayed clean because of the ordering, not because of the outcome.
* **BOUNDARY flag → beat accept #15 by ~5 accepts.** Had it not landed, the leg
  would have run to 30 on the script default while one lane believed 15 and the
  other believed 30.
* **4:1 → beat the arm's economics hardening**, by minutes.

## Q3. ⚠ INFERENCE PUBLISHED AS FACT — what was SEARCHED vs what was CLAIMED

**FOUR published errors. The mechanism is one chain, and it is new to this
instrument.**

| # | error | KIND | STATE | searched vs claimed |
|---|---|---|---|---|
| 1 | *"CAL-8 is at 80 games, read-at-80 is legal under §2"* | judgement | auditing | I searched a **peer's note**; I claimed **the panel's state** |
| 2 | *"name a registered ground"* → produced a **false ground** on another lane's commit | judgement | auditing | searched the **prereg's clause list**; claimed a **fit to an n I never derived** |
| 3 | cited `0a60705` as my flag commit (it was the builder's, 1 s later) | estimator | — | searched `git log -1` in a **shared tree**; claimed **my own HEAD** |
| 4 | *"46 of 65 are not SCREEN-*"* (45) | estimator | auditing | eyeballed a listing; claimed a **count** |

**⭐ THE FINDING, AND IT IS A COMPOSITE THIS LEDGER HAS NOT RECORDED BEFORE — THE
RELAY→PRESCRIPTION CHAIN.** #1 and #2 are not two errors, they are one
propagation: **I inherited a peer's figure without re-deriving it, then issued a
PRESCRIPTION that cited it, and the prescription hardened the false figure into a
false GROUND in another lane's commit.** A relayed number that stays in a note is
one lane's error; a relayed number inside a prescription **acquires the authority
of the detection and travels into someone else's artefact.**
⇒ **ROUTED to the booted drift-watch file as a behaviour change: a prescription
that cites a number re-derives that number first.** The cost of re-deriving was
one `awk`; the cost of not was two lanes' commits and a correction chain.

**#4 is the promoter's-first-use rule (v1.9) firing on schedule: I made a
counting error inside a flag about a counting error.** Caught by research within
two minutes.

## Q4. DID MY OWN WATCH CATCH MY OWN WORK?

**2 of 4 self-caught, and BOTH by a stored/armed instrument rather than by
attention — the v1.6 mechanism, third consecutive run.**
* **#3 (hash) was caught BY MY OWN DRIFT WATCH**, which emitted my flag commit
  and the builder's as adjacent lines with distinct hashes. The watch I armed at
  boot audited its own author inside four minutes. **This is the cleanest instance
  of the stored-instrument mechanism recorded: the catch required no suspicion.**
* The reference-table certification (Q8) was verified by **re-deriving six cells
  myself** rather than accepting a peer's table — a second instrument over
  someone else's claim, which is the same mechanism pointed outward.
* **#1 and #4 were caught by research, not by me.** Both were figures I could
  have derived in one command and did not.

## Q5. FALSE POSITIVES — what did flagging cheaply cost?

**1 wrong flag, 1 CAUSED harm — the zero-harm streak ends at six runs.**
* **Wrong:** flag #1 (`ship_watch`) was real as an environment fact and
  **decorative in every consumption path**. Cost: two lanes each opened one file.
  Cheap, and it bought a residual worth having (the `net_act_src=env` fallback).
* **⛔ CAUSED:** flag #4's prescription put a **false ground on the record** in
  `80edbcd`. Live for ~2 minutes, corrected by the same chain that produced it.
  **The standing sentence *"a wrong flag costs a one-line reply"* is again false
  as written** — a wrong PRESCRIPTION costs another lane's commit.

## Q6′. CLAIMS ABOUT ANOTHER LANE — including relayed FIGURES

**~6 claims, 2 wrong, both figures, both mine to own.** The "80 games" (relayed,
not measured, not attributed to a window) and the "46" (measured badly). **Every
other relay carried its owner and its clock** — research's boundary assumption
quoted as theirs, the builder's typed 15 quoted as the record, the atlas 4:1
attributed to their own commit. **Conduct claims: seventh consecutive zero.**

## Q7. WHAT DID I DECLINE, and was declining right?

**Four, all held.**
1. **Declined to rule the CAL-8 boundary value** — named it a fire-order call and
   handed it back with both budgets priced (~25 min vs ~1h45m). Right: it is a
   budget decision, not a discipline one.
2. **Declined to take or peek at any CAL-8 tally**, including while re-deriving
   the reference table off `ladder_games.tsv` — a rated surface that touches no
   panel outcome. **Blindness preserved by choosing the surface, not by resisting
   the temptation.**
3. **Declined to rule the seal invalid** — laid out three branches and left the
   call. Right, and the branch chosen was one I had not ranked.
4. **Declined to audit `cde6378` (rated-day decode)** as out of scope per the
   2026-08-13 rescope, after checking its one in-scope clause (rated-leak). **The
   rescope working as designed: an analysis commit read once and let go.**

## Q8. MECHANISATION — did any flag become a SCRIPT?

**Three, the best rate this instrument has recorded per unit time.**
1. **`37b1b2b`** — the BOUNDARY flag became **code**: an ARMED-value echo onto the
   fires tape at launch. It even derives its own accept count with the correct
   field-3 `awk`, i.e. the fix carries the counter whose absence caused the day's
   defect.
2. **`53b8560`** — the PROVENANCE rider became **a required token** in
   `prereg_check.py` with a both-verdicts fixture pair, and a line in both charters.
3. **`92f8c4c`** — the six-check spec, including the **bar-null assertion**: named
   as mechanisation candidate #2 in the enforcement ledger on 2026-08-10 and
   **unbuilt for four days**. A tool finally exists to host it.
**Practice unchanged and confirmed: flag the defect WITH its fix, and name what
the fix was verified against.**

## Q9. DID MY CORRECTIONS NEED CORRECTING?

**ONE, and it is the session's headline defect** — flag #4's prescription
(Q3 #2). **Right that the ground was false; wrong about what would fix it,
because the replacement ground cited a number I had not re-derived.**
**This is the lane's stable property firing again: detection sound, prescription
unverified.** The v1.4 rule (*a fix is specified against the CONSUMER*) was
satisfied — the builder could and did act on it. **The rule that was missing is
the one this instance adds: a fix is also specified against its own NUMBERS.**

## Q10. DID I ADDRESS THE CONCERN AS STATED?

**Clean run, 3 for 3.** The builder's `#52` "queue head" was answered as stated
(D14 crossing), not restated into something weaker; research's two cert questions
were answered as asked (gap → declarable with span; boundary → contradicted on the
record, with the observability caveat attached rather than substituted).

---

## THE LEDGER

> **DETECTION 8/9 · PRESCRIPTION 4/5 · Prevented: 5 · Caused: 1 · Nearly caused: 1.**

**Prevented:** an unregistered P4 look at n=40 · a leg silently running to 30 on a
default while two lanes believed different values · an arm sized on a halved
economics ratio · a DoF-flavoured certification accepted without re-derivation ·
a prereg-status contradiction between two tail entries.
**Caused:** the false seal ground in `80edbcd` (~2 min).
**Nearly caused:** had the "80" gone unchallenged by research, my certification
would have blessed a P4 read at n=40 — the exact hazard my own flag was raised about.

**The split holds for a sixth run: this lane detects better than it prescribes.**
