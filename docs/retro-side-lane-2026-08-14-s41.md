# SIDE-LANE RETRO — instance s41, 2026-08-14. Instrument **v1.10 → v1.11**.

Session 19:08Z–20:33Z (~85 min). Audit surface: **97 repo commits**, ~25 mine.
Answered from artefacts (git log, the coordination tape, my own harness output),
not from memory.

---

## Q1. CONSUMPTION — 17 flags, 17 consumed, **7 became code**

| # | flag | outcome |
|---|---|---|
| 1 | ship-chain: v140 back on the slot before the screen's number | consumed; ordering concern resolved in the programme's direction |
| 2 | prereg_check **value bleed** across the newline | **CODE** `2b4b89d` |
| 3 | prereg_check **empty ≠ absent** | **CODE** `ca3e9ce` |
| 4 | prereg_check **ADD-ONLY blind on real text** | **CODE** `2b4b89d` (NOT_APPLICABLE/exit 2) |
| 5 | H2 placeholders + **recommendation AGAINST a blocklist** | **CODE** `edea4c8` (declared limit, `ok~`) |
| 6 | A5 gate: the extension buys precision, not the decision | **A6**, my sentence pre-committed |
| 7 | **A5/A6 contradiction** on the most likely outcome | **A7 + A8** (the Ob-12 identity struck) |
| 8 | **scope: Magnus's rule says n=1000, applied at n=3000** | **A10 — screen CLOSED** |
| 9 | TLE ruling argued in SE against a systematic bias | research amended → EXPOSED-AND-UNRESOLVED |
| 10 | marker-row must fail the game schema (impossible value) | adopted + cell H9 |
| 11 | bounded form of the impossible-value guard rule | adopted verbatim |
| 12 | SALTREF2 one-sample interval on a two-fixture comparison | **A1** |
| 13 | **two-clock's second clock is a completion time** | START stamp routed into the fixture bundle |
| 14 | stale `.COMPLETE` on a non-deleting mirror + the dash chip | **CODE** `397bfe5c` |
| 15 | log the `gate.py` escape flag | accepted into the wiring design |
| 16 | local-accepts WARN | accepted for the bundle |
| 17 | stale `LOKI_SALT_TI_FLOOR` comment | queued — **and the builder improved on my fix** |

## Q2. LATENCY — **the lane's best-measured payout to date**

* **#8 beat its decision by ~3 hours.** A9 was committed reading *"at the n=3000
  read"* while Magnus's sentence said *"at n=1000"*. Asked as a QUESTION while
  blind. He ruled n=1000; the screen closed on a read already in hand.
  **~1,570 games and ~2h45m of fleet time not spent — and the two readings could
  have DIVERGED IN OUTCOME, not merely in cost.**
* **#7 beat the read entirely** — A5 and A6 gave opposite answers for a band
  containing 49.20, the number already observed. Resolved blind in one minute.
* **#2–#4 landed before the tool was wired**, i.e. before any prereg depended on it.
* Every flag this session preceded the decision it bore on. **Zero post-mortems.**

## Q3. INFERENCE PUBLISHED AS FACT — **6 instances, and one is the worst kind**

**WHAT WAS SEARCHED vs WHAT WAS CLAIMED, as v1.4 requires:**

| # | searched | claimed | caught |
|---|---|---|---|
| 1 | `ps` at 19:07:13Z | "all monitors are dead" | **pre-publication** — the elo tape's mtime contradicted it |
| 2 | `first_number` never opened | "BAR 0.52 is a units hole" | **published**, retracted same hour |
| 3 | **the panel's cell counts** | **"D2 measures a different team"** | **PUBLISHED IN A CERTIFICATION**, amended against myself |
| 4 | `git log` without `TZ=UTC` | `22:04:17Z` under a literal Z | pre-publication, by disagreement with the monitor |
| 5 | one md5 function | (nearly) "the VOID hash mismatches" | pre-publication — found the method first |
| 6 | my own fixture | "the tool lost a tooth" | the fixture was wrong, not the tool |

**⛔ #3 IS THE CHARACTERISTIC FAILURE IN ITS MOST EXPENSIVE FORM: I CERTIFIED A
FINDING WHOSE PREMISE I NEVER CHECKED.** I re-derived everything *around* the
claim — cell counts, sign test, robustness, even a matched-composition
correction — and never asked whether the two names were one team. They were: one
`teamId`, renamed mid-day.
**Direction: toward the AUTHOR'S live hypothesis, not my own.** New axis for this
lane — v1.3.2 pre-registered `auditing` to behave like `defending`, and it does,
**but the hypothesis being defended was the one I was auditing.** I adopted it
instead of testing it. That is **Q10's shape reaching Q3**.

## Q4. SELF-CATCH — **4 of 6, and the mechanism is unchanged: GOING TO USE THE THING**

Never diligence. The catches: the **tape's mtime** contradicted a stale `ps`;
**going to derive** killed H6; **finding the md5 method** stopped a false VOID
alarm; **the commit monitor's own UTC line** exposed my TZ slip. The two misses
(#3, #6) were both caught by peers.
⭐ **STORED-INSTRUMENT MECHANISM, FOURTH CONSECUTIVE RUN:** my drift watch emitted
my own commits all session, and my own certification harness — built for the
tool — is what proved *my fixture* wrong rather than the tool.

## Q5. FALSE POSITIVES — 2 wrong, **1 caused harm**, and **4 phantoms AVOIDED**

* **Wrong:** H6 (no harm — retracted in an hour, before anyone built on it) and
  the D2 endorsement (**harm: it hardened into my published certification**).
* ⭐ **FOUR would-be flags killed by reading the condition line first**: A3's tie
  asymmetry (already stated in the prereg), the CPU-denial closure (D12 satisfied
  — 126 live games), A5 as a rescue (branch-symmetric on inspection), SEALFLOOR6's
  byte-identical `raid.py` (the inverse of LOKI-18, not a repeat).
  **The s37 practice is now this lane's most productive habit.**

## Q6′. CLAIMS ABOUT ANOTHER LANE — ~12, **1 wrong**

Every figure carried its owner and derivation. The one wrong was **relayed, not
measured** (#3), which is the class this question exists for. Conduct claims:
**tenth consecutive zero.**

## Q7. DECLINES — 5, all held

Editing `serve.py` on Magnus's direct ask (**builder-owned AND being patched that
minute — a concurrent-edit conflict is an operational reason, not formalism**) ·
deleting the stale mirror marker (pull semantics, not mine, mid-screen) ·
computing the running share at n=1210/1430 (unregistered look) · **ruling on the
A5/A6 contradiction** (put to Magnus with both costs named) · re-arming
builder-owned monitors.

## Q8. MECHANISATION — **7 in one session, the best rate recorded**

`prereg_check` ×4 (three fixes + the H2 declaration) · `corefill_status`
heartbeat-not-marker · `target_value` teamId keying · the fixture spec's
marker-row format. **Plus the START stamp routed and the escape-flag tape
accepted into a design not yet written.**
⭐ **AND THE OBLIGATION v1.10 CARRIED FORWARD IS DISCHARGED:** the forced-fail
certification, *offered, accepted and unrun* at the s40 wrap, **was run** — and
then re-run three times as the tool changed. **A certification of one side of a
comparison carries an expiry** is now this lane's own standing rule and it bound
me twice today.

## Q9. CORRECTIONS THAT NEEDED CORRECTING — **2**

The D2 endorsement (corrected by amending my own cert) and H6 (retracted). **Both
were DETECTIONS that were wrong, not PRESCRIPTIONS** — a first.

## Q10. THE CONCERN AS STATED — clean, and one case where it was the whole job

On A5/A6 I published **both readings with their costs** and refused to rule.
Magnus ruled, and his answer (`n=1000`) was **neither** of the readings the
builder and I were arguing about — it closed the screen instead. **Had I
"resolved" it myself I would have picked one of two wrong answers.**

---

## THE LEDGER

> **Prevented: 9. Caused: 1. Nearly caused: 3.**
> **DETECTION 15/17 · PRESCRIPTION 17/17.**

**⭐⭐ THE SPLIT INVERTED, AND HARDER THAN AT s37: EVERY PRESCRIPTION SURVIVED
CONTACT — including two the consumer IMPROVED ON** (the builder declining to fix
the stale comment mid-screen because it would break the one-hunk property; the
coverage guard quantifying 3-vs-19 rather than taking my bare threshold).
**Both detection losses were premise-level: I checked everything except whether
the thing being claimed was true.**

⇒ **This is the sixth run measuring the split and the second inversion. The
lane's "detects better than it prescribes" property is no longer stable**, and
v1.11 should stop quoting it as settled.
