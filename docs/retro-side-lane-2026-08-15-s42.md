# SIDE-LANE RETRO — INSTANCE s42, 2026-08-15

**Instrument version run: v1.11.1.** Session 2026-08-14T20:41Z → 2026-08-15T05:39Z
(~9 h). Wrap called by Magnus (*"Wrap when builder wraps"*). **Answered from
artefacts — 78 session commits, the coordination tape, and the peer channel — not
from memory.**

---

## Q1. CONSUMPTION — were flags ACTED ON, or filed?

**~18 flags changed an outcome. Consumption was effectively total; nothing of mine
sat unconsumed at wrap.** Named, because the question requires it:

| flag | outcome it changed |
|---|---|
| wiring-bundle item (a) **withdrawn by me mid-build** | the escape tape was rebuilt to tape EVERY invocation; the agent was building my wrong spec |
| all four `gate.py` escapes, not one | the ≥20-char reason promoted to `--off-programme`/`--skip-tle`/`--allow-self-play` |
| OB13 **silent-pass** on an unrelated dirty tree | scoping fix + `CANNOT-COMPUTE` kept distinct from clean |
| OB13 **committed-arm blindness** | treatment-vs-control diff adopted as the real basis |
| SEALFLOOR6 blinding leak | both gates read **mechanically**; disclosure written; tool fix deferred correctly |
| gate anchoring | superseded by **reconstruction**, now the standard for every future gate |
| v140 stop-loss fired 68 s before x3r0 | builder verified; Magnus ruled the branch |
| juustopin target one point inside `BAND_HI` | **exit rule registered** in the re-fire amendment |
| `target_value` silent cache fallback | `QUEUE #56` widened, credited |
| research's `2.33pp ± 0.76` unenumerated cluster | **estimator replaced**; conclusion strengthened (p 0.002 → 0.00017) |
| `throws.tsv` has no version column | caused the join that found **193 rated throws, not zero** |
| `#51` dismissed on a bad build | row **re-labelled** *"a broken loop, not a dead idea"* |
| OB16 (bars carry an MDE) | built as a **`prereg_check` row**, not prose |
| worklist starvation | `fixture_starvation.py` |
| heartbeat says RUNNING forever | `worker.sh` stamps `CURFEW`; detector reads **freshness** |
| cross-host dispersion vs `CLAUDE.md` | routed to Magnus; rider added where bars are certified |

## Q2. LATENCY — did the flag beat the decision it bore on?

**Mostly ahead, and one badly late.**
* **AHEAD:** the wiring-bundle (a) withdrawal landed **while the agent was building
  the wrong thing** (*"your 60-second check saved a wasted diff"*). GATE-1000 was
  flagged ~15 min before it armed, and the builder's own words are that **the flag
  is why the read was mechanical.** The juustopin band flag preceded the re-fire.
* **⛔ LATE:** the **stop-loss un-fire**. It fired 02:22–03:12Z and cleared at
  03:22Z; **I found it at 03:47Z, thirty-five minutes after the state was gone.**
  Nobody was awake for the fifty minutes it was on. **The flag did not beat that
  decision — there was no decision, which is the defect.**

## Q3. ⚠ INFERENCE PUBLISHED AS FACT — the characteristic failure

**EIGHT published errors. The count is the worst recorded; the MECHANISM is the
finding.**

1. *"`--fire` already turns the OB13 WARN into a FAIL"* — **a phantom FEATURE**,
   asserted from a commit that did something adjacent.
2. `target_value` prices off a cached rating — **a defect my own lane had already
   got fixed**, with the credit in the comment above the code.
3. **SALTREF2 "IS DEAD"** — it was **asleep**. Three confirmations all confirmed
   the *instrument* was silent; I read that as the *leg* being dead.
4. Pooled point estimates quoted as levels (ladder **and** unrated).
5. The unrated drop attributed to **prototype pooling**; the operative mechanism
   was **opponent composition**, measured at −11.06pp of −15.22pp.
6. **An ILLEGAL confidence interval, `[-0.5%, 10.5%]`, published to another lane** —
   a proportion cannot be negative.
7. Audit §2: *"three inert metrics, **all three for the same reason**"* — #54 is
   counter reachability, a different mechanism.
8. *"the fifteen **NEVER CERTIFIED**"* — true of my harness, **false of the system**.

**⭐ THE MECHANISM, and it unifies six of the eight: I CHECKED THE SURFACE ADJACENT
TO THE ONE THAT MATTERED.** A commit that did something adjacent (1). A comment
instead of the code's history (2). The pulled **mirror** instead of the box, and
never the **schedule** (3). My harness's **coverage line** instead of the probe's
**assertion expressions** (8). Three symptoms read as one mechanism (7).
**Not "I did not check" — I checked, one surface over.**

**⛔⛔ AND THE DIRECTION FINDING INVERTS THIS INSTRUMENT'S OWN PRE-REGISTRATION.**
v1.3.2 pre-registered **`auditing` to behave like `defending`** — *"a lane-specific
hazard for THIS lane specifically, because auditing is what it does all day."*
**Measured this session: every alarming over-claim came while SURVEYING with no
hypothesis — 4 for 4** (the 53-shard false stall, SALTREF2-dead, *"never
certified"*, and the audit's over-unified §2). **My auditing errors were
premise-misses, not over-claims.** ⇒ **surveying, not auditing, is this lane's
high-risk state, and the tell is that nobody asked me to look.**

## Q4. DID MY OWN WATCH CATCH MY OWN WORK?

**⛔ THE WORST RECORDED: roughly 2 self-caught of 8. Six were caught by the
builder.** (`--fire`, SALTREF2, the illegal CI, audit §2, the harness defect, and
the `throws.tsv` relay.) Self-caught: the pooled-estimate levels and the
prototype-vs-composition misattribution — **both by running the peer's diagnosis on
my own cut**, which is the recorded mechanism, not diligence.

**⭐ AND THE MECHANISM THAT ACTUALLY WORKED IS SHARPER THIS RUN: A SECOND
INSTRUMENT IN THE SAME OUTPUT.** The `pgrep -fl` listing I printed **for display**
sat beside a count of `0` and contradicted it — **an ILLEGAL PAIR**. That is what
caught three D33 instances I committed inside four minutes while verifying D33 on
myself. **Not suspicion; adjacency.** ⇒ **print the EVIDENCE beside the VERDICT and
check they agree.**

## Q5. FALSE POSITIVES — and what flagging cheaply cost

**TWELVE flags withheld before publication** — the highest recorded — including
**#54's closure re-litigated and found sound**, and **GATE-2700 nearly flagged
under my own OBLIGATION 12 for lacking a resolution statement its cited standing
rule supplies.**
**Wrong flags that reached a peer: two.** The `target_value` phantom (cost: one
reply) and **SALTREF2-dead (cost: real investigation)**. ⚠ **But SALTREF2 was
NET-POSITIVE: the instrument finding was correct and produced two fixes** —
`worker.sh` now stamps `CURFEW`, and the detector reads freshness rather than
content. **A wrong conclusion pointing at a real gap is not the same object as a
phantom.**

## Q6′. CLAIMS ABOUT ANOTHER LANE

**One wrong relay, and it is the session's cleanest instance of the class.** I
repeated *"v140 makes ZERO exile throws across 115 rated games"* back to its
originator **without re-deriving it**. It was false (193 across 200). **I judged it
"low harm because I relayed it to its own originator" — that was LUCK, not
reasoning: the same figure was already a headline to Magnus and load-bearing in the
`#38` brief.** ⇒ **a relayed figure's harm is not bounded by who you said it to.**

## Q7. WHAT DID I DECLINE?

**Five, all held.**
1. **Reading SEALFLOOR6's share at either gate** — I flagged the leak, so my own
   alarm printed the row count and clock and never the statistic.
2. **Litigating the arming-order dispute** with the builder — unresolvable from
   artefacts I hold, changes nothing, and `auditing`-errs-toward-its-own-conclusion
   is on this lane's record.
3. **Credit the builder offered** on the prototype-pooling reason — my own numbers
   contradicted it.
4. **Auditing analysis commits**, per the 2026-08-13 rescope — repeatedly.
5. **A PushNotification on the stop-loss** — a drawdown building for hours is not
   break-glass, and the builder was active and owned the verdict.

## Q8. MECHANISATION — did a flag become a SCRIPT?

**SEVEN became code**, the joint-best rate recorded: `fixture_starvation.py` ·
`worker.sh` CURFEW stamp + freshness detector · **OB17 as a `prereg_check` row**
(the builder chose the enforceable form **using my own framing back at me** —
*"the second is enforceable and the first is attention"*) · the escape tape rebuilt
to my corrected spec · OB13's scoping fix · **reconstruction superseding armed
watchers** as the gate standard · my own harness denominator.

## Q9. DID MY CORRECTIONS NEED CORRECTING?

**TWO, and both were corrections OF MY OWN CORRECTIONS** — §2's over-unified
heading, and *"the fifteen never certified."* **Both ran ALARMING and both were
milder in truth.** ⇒ **a correction published while surveying inherits the
surveying direction**, which is the same finding as Q3 arriving one level up.

## Q10. DID I ADDRESS THE CONCERN AS STATED?

**Yes, on the run that mattered.** Magnus asked whether any dismissed prereg died
because *we built a bad version of it*. **I answered that question** — one row
(`#51`), a near-miss pattern of three, **and four rows explicitly CLEARED** rather
than swept into the finding. **The temptation was to return a bigger number.**

---

## Q0 — PROPOSED AT v1.11.1, ANSWERED HERE FOR THE FIRST TIME

> **What did not happen because this lane was running?**

**Withheld before publication: 12 · corrected before consumption: 1 · phantoms
killed: included in the 12.** Against **8 published errors**.
**⇒ The proposed anchoring rule held: every withheld flag above left an artefact**
— a command run, a file opened, a source read. **None is self-reported.**

---

## THE LEDGER

> **Prevented: ~18 · Caused: 0 lasting · Nearly caused: 1**

**Nearly caused:** *"the fifteen never certified"* would have sent the builder to
write nine redundant corruption cells had they acted before my amendment landed.

**DETECTION ~18/26 · PRESCRIPTION 7/7 consumed, THREE IMPROVED BY THEIR
CONSUMERS.** The improvements: **MDE *inside* the bar** rather than beside it;
**OB17 as an enforceable check** rather than a numbered obligation; **reconstruction
by arithmetic** rather than anchoring by convention. ⇒ **v1.11's retirement of
*"this lane detects better than it prescribes"* holds — the split inverted again,
and prescription is now the stronger half two runs running.**
