# SIDE-LANE RETRO INSTANCE — s45, 2026-08-16

**Instrument version run: `docs/side-lane-retro.md` v1.14 (bumping to v1.15 in this same action).**
**Session 07:51:00Z → 13:2xZ (~5.5 h). Trigger: THE BUILDER'S WRAP-PING — the standing rule Magnus
set today, second firing, first for a full session.**
**Answered from the day's artefacts — `git log`, the coordination tail, the certificates — not memory.**

---

## Q0. WHAT DID NOT HAPPEN BECAUSE THIS LANE WAS RUNNING?

**KILLED PRE-PUBLICATION — 3**, each with the check that killed it:
* *"the peck fires on the arrival turn"* — killed by the act/move exclusivity rule, **in the same
  reasoning pass, before the commit.** The only same-shape catch today that cost nothing.
* *"CAL418's two-clock lock statement was never signed"* (subagent) — killed by reading past the
  first `§14` to the second at `:883`.
* *"`PROGRAMME.md` declares a retired estimator to a machine"* (subagent) — killed by checking that
  `gate.py` reads that field zero times.

**DECISIONS CHANGED BEFORE THEY WERE TAKEN — 6:** B1 registered UNATTRIBUTED-BY-DESIGN pre-start;
#77's withdrawal condition registered before `#76` reads; the fieldcal reader's refusal moved from
brief into code; the `GREP-TREE` structured-token recommendation over a fifth prose widening; the
`LINE_DIRS` invariant sited in a test that boots rather than a selftest that is invoked; #78
registered UNCONVERTED at the artefact.

⇒ **9 items that never became findings, against 10 published errors.**

## Q1. CONSUMPTION — acted on, or filed?

**~30 flags/certifications/rulings to two lanes across 5.5 h. Every one consumed the same session.**
**11 became CODE or a registered artefact:** the `LINE_DIRS` invariant (3 surfaces), the filter-proof
twin name, `B1 UNATTRIBUTED-BY-DESIGN`, #77's withdrawal condition, #78's UNCONVERTED registration,
the fieldcal reader's inline refusal, the leg-disclosure badge, the cooldown derivation in-tree,
the stop-note on three halt paths, the AMEND1 seam field, the `sed '1,/^-->/d'` guard form.
⭐ **Three consumers went PAST the ask** — the disclosure badge injected at serve time (closing a
cache-staleness hole I never raised), the stop-note sited at the halt rather than the reader, and
the cooldown answered as an engine-measured derivation rather than a stated value.

## Q2. LATENCY — did the flag beat its decision?

**Ahead in every case that mattered.** The B1 flag, the SEALPECK question, the #77 annotations, the
#78 registration and the AGENTS.md guard correction were all PRE-START or PRE-LOCK. **Behind once,
and it is the session's worst:** the transitivity flag on V140VS152 landed **11 seconds after**
research published the headline — commits crossed, so it corrected rather than prevented.

## Q3. ⚠ INFERENCE PUBLISHED AS FACT — **TEN, AND THE COUNT ITSELF IS THE FINDING**

| # | claim | caught by |
|---|---|---|
| 1 | regenerating `AGENTS.md` would carry `RMST₃₀₀` | research |
| 2 | the builder's commit destroyed my uncommitted note | builder |
| 3 | evidence table published with every tree name empty (heredoc) | self |
| 4 | the carry idiom is a genuine assertion, not a silencer | research |
| 5 | the verb-list failure mode "does NOT occur" — tested on ONE phrasing | research |
| 6 | two criteria "converge to two decimal places" — a GRID ARTEFACT used as evidence | research |
| 7 | "E3 expires tonight when the shards resume" — they cannot resume | builder |
| 8 | D36's own citation destroyed (heredoc) — **REPEAT of #3, 2h later** | self |
| 9 | "the consequence neither lane has named" — Magnus named it first | self |
| 10 | "drop B1 from one arm and the contrast falls out free" — it does not attribute | builder |

**⛔⛔ AND THE HEADLINE IS THE ENUMERATION, NOT THE TALLY.** I tried to count these MECHANICALLY from
commit subjects and got **9** — **wrong in both directions**: it caught two commits that were not
errors (a pre-commitment registration, an amendment on new evidence) and MISSED three that were
corrected inside other commits or in messages. ⇒ **My own error count is not mechanically derivable
from my own artefacts.** ⭐ **This is research's D36 instance-2 landing on me within the hour I wrote
it up about them: *a count is only as wide as its enumeration rule.*** **I applied the rule, the rule
under-specified, and only READING gave the honest number.**

**SHAPE: 7 of 10 are one thing — A CORRECT ANSWER TO A QUESTION SMALLER THAN THE ONE ON THE TABLE**
(#1, #4, #5, #7, #9, #10, and #6's inference half). **Two are rendering failures of the same
mechanism, the second a repeat.** **One (#2) is a conduct-adjacent attribution on an artefact clock.**

## Q4. DID MY OWN WATCH CATCH MY OWN WORK?

**4 of 10 self-caught; 6 needed a peer.** ⛔ **And the self-catches are the LOW-VALUE ones**: two
rendering failures (visible as shell errors) and one same-hour retraction. **Every SUBSTANTIVE error
— scope, attribution, inference — was caught by the other lane.**
⭐ **The mechanism that worked, three times: WRITING THE EXPECTED VALUE BEFORE READING THE OUTPUT.**
It converted a plausible number into an illegal one on the AGENTS.md cell. ⚠ **And its limit, which
I published and then proved: it bites on CHECKS, not on CHARACTERISATIONS** — #1, #4 and #9 had no
pre-declarable expectation because I was describing, not measuring.

## Q5. FALSE POSITIVES — what did flagging cheaply cost?

**3 killed pre-publication · 10 published-and-wrong · ZERO caused harm.** ⚠ **One nearly did: #2
named a colleague's commit as having destroyed my work, on an mtime and a clock.** **It cost nothing
only because the builder answered with disk-state reasoning instead of deferring** — which is this
lane's own standing finding recurring with me on the receiving end.

## Q6′. CLAIMS ABOUT ANOTHER LANE

**~12, all measured with their surface named. ONE WRONG (#2), and it was the conduct-adjacent one.**
⭐ **Three times I corrected a claim in the UNFLATTERING direction for me** — the option-1
prescription, the re-ranking of D36's instances (research argued MINE was the harder failure and I
adopted it), and "I contributed nothing here that was not already in motion."
⚠ **Research corrected credit in the unflattering direction for THEMSELVES three times.** **Worth
recording as a property of the exchange rather than of either lane.**

## Q7. WHAT DID I DECLINE?

**Six, all held:** editing `AGENTS.md`, `PROGRAMME.md`, `HANDOVER.md` or `QUEUE.md` (not my files,
every time routed instead) · ruling on the bar's rightness when I held new information about it ·
recommending what to do about a teammate's patch · **writing two checklist rows on volume grounds**
· asserting a second clobber mechanism after the first failed · **and the hardest: declining to bank
"the +6.28pp seat asymmetry is the biggest lever" after research retired their own framing.**

## Q8. MECHANISATION — did any flag become a SCRIPT?

**SEVEN became code**, equalling the record: the `LINE_DIRS` invariant on three surfaces (including
a boot-run test), the filter-proof twin name, the fieldcal reader's inline refusal, the
leg-disclosure badge, the stop-note on all three halt paths, the AMEND1 seam field, the in-tree
cooldown derivation. **Plus four registered-artefact guards that are not code but cannot be
forgotten:** B1's non-attribution, #77's withdrawal condition, #78's UNCONVERTED stamp, the
observation certificate's prospective condition.

## Q9. DID MY CORRECTIONS NEED CORRECTING?

**TWICE.** #6's correction was accepted and then research corrected my *ranking* of it (they argued
my instance was the HARDER of the two and were right). And #2's retraction correctly refused to name
a replacement mechanism — **which research separately confirmed was the right resting state.**
⭐ **New this run: a correction that was itself a repeat.** #8 is #3, two hours later, with the
correct form applied five lines away in the same shell call. ⇒ ***I fixed the instance and not the
practice*** — **the sharpest formulation of D36 produced today, and it is about me.**

## Q10. DID I ADDRESS THE CONCERN AS STATED?

**Clean.** Research asked me to check whether they had OVER-withdrawn and I confirmed the survivor
stood rather than ratifying the retraction. They asked me to check the conservative-bound step and I
found two conditions, one breaking a branch they had registered. **Both were asked as questions and
answered as questions.**

---

## THE LEDGER

> **Prevented ~11 · Caused 0 · Nearly caused 1** (#2, the attribution).
> **DETECTION ~30/40 · PRESCRIPTION 11 consumed, 3 improved by consumers, 1 REJECTED AND RIGHTLY**
> (option 1 on B1 — it would not have attributed).

**⛔ PRESCRIPTION IS THE WEAKER HALF AGAIN, AND THIS RUN IT IS MEASURED RATHER THAN ASSERTED:** one
prescription rejected outright with a better reason than mine, three improved past the ask, and one
(#10) that I sold as free and that would not have answered the question I raised it for.
**Detection remains this lane's product; prescription remains the thing consumers fix.**
