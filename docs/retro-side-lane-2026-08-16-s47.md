# SIDE-LANE RETRO — INSTANCE s47, 2026-08-16

**Instrument version at run: `docs/side-lane-retro.md` v1.15 → bumped to v1.16 in this same action.**
**Session 13:46:50Z → 19:43:03Z (~5.9 h). 24 side-lane commits. Trigger: the BUILDER'S WRAP-PING**
(Magnus's standing rule — third firing, second over a full session).

---

## Q1. CONSUMPTION — acted on, or filed?

**~10 outcome-changing flags. Every one consumed the same session; zero filed-and-ignored.**

| flag | outcome it changed |
|---|---|
| stop-loss widened window (`f5d9b0c6`) | **became code** (`12f62f5d`), test-first |
| *"there is no builder session"* | research **retracted to Magnus**; both lanes kept working |
| three-valued OWNER predicate | **adopted unchanged** by research |
| remote gap = every case, not tail case | builder's **G413g4 tickler** |
| G413g4 remaining-under-stop = 4,120 | builder's kill/no-kill call became a **comparison** |
| `#79` would rebuild shipped code | row **re-priced** by the queue owner |
| `#43` staleness | research escalated it to **leg-invalidating** |
| regression on floor-stopped arms | how the cancelled set is **read for combinations** |
| retirement reversibility drive | **certification the builder said he was glad existed independently** |
| ship certification (Sleipnir v1) | transitivity assumption named **before `SLEIPH2H` reads** |

## Q2. LATENCY — did the flag beat the decision?

**All ahead, and two by a wide margin.** The *"no builder"* correction went out **inside two minutes**
of a false absence-claim reaching Magnus. The stop-loss flag preceded its own predicted firing by
**~40 minutes** — `m=1115` at rating 1735 against a predicted fire condition of `<1763`. The ship
certification landed **~4 minutes** after activation, before `SLEIPH2H` produced a number anyone
could argue about.

## Q3. INFERENCE PUBLISHED AS FACT — **8**

**ENUMERATION RULE, stated because v1.15 requires it and grep is not enough:** a *published error* is
**a factual claim committed to a durable artefact or sent to a peer, later shown false and requiring
correction.** Excludes claims killed pre-publication (→ Q5) and matters of framing. **Derived by
reading the session, not by grepping commit subjects.**

| # | what was SEARCHED | what was CLAIMED | whose work | caught by |
|---|---|---|---|---|
| 1 | my lane's own last session number | *"this session is s46"* | mine | **me** |
| 2 | `grep -c` exit status | *"the watch returns rc=1"* | mine | **me** |
| 3 | s45's carry-forward list | *"these five are open"* — 2 of 5 were not | a predecessor's | **me** |
| 4 | ONE comment block | *"`doctrine.py:122` says the band beyond d²≤36 is unowned"* | a peer's row | research |
| 5 | a freshness claim, true when made | *"per-match attribution needs a platform call"* | mine | research |
| 6 | research's relayed figure | *"our own ship decisions are TWO, not five"* — it is ZERO | research's | research |
| 7 | a cumulative games-past-mark total | *"≈7,950 games of fleet time"*, `NOW`-tagged | mine | builder |
| 8 | a `--dry-run`'s CONTINUE lines | *"no live arm is affected; lowest of the four is 55.28%"* | mine | **me** |

⭐ **THE UNIFIER HOLDS A THIRD RUN — 6 of 8 are ONE MECHANISM: I reported a property of a SET or
SYSTEM from a PARTIAL VIEW of it.** `#8` is its purest form yet: **an ABSENCE in a list I never
enumerated** (`SEALPECK` had been floor-stopped 38 minutes earlier, and its name still appears in the
tool's output — inside a 238-entry registry line — so a grep returns 1 either way).

⛔ **AND ONE IS A NEW MECHANISM, WORTH THE BUMP ON ITS OWN — `#7` IS NOT A SCOPE ERROR, IT IS A TENSE
ERROR.** The number was correct; it described a **STOCK** (games already played) and I reported it in
the present, implying a **FLOW** (games recoverable). **Every word around it implied action; none was
available.** ⇒ **Q3 has recorded scope errors for three runs and this is the first tense error.**

**Target (v1.13's axis): 5 of 8 concern MY OWN work, 2 a peer's, 1 a predecessor's.** ⇒ **the drift
back toward my own output is real** — s43 ran 3-of-5 about colleagues.

## Q4. DID MY OWN WATCH CATCH MY OWN WORK? — **4 of 8; 2 of 6 SUBSTANTIVE**

**Scored on v1.15's sharpened form (*how many of the ones that MATTERED*), not the raw rate.**
Substantive = `#3 #4 #5 #6 #7 #8`. **Self-caught among them: `#3` and `#8`.**
⇒ **2 of 6, against s45's 0 of 6.** The two cheap ones (`#1`, `#2`) were shell-visible.

⭐ **MECHANISM CONFIRMED AND UNCHANGED FOR A FOURTH RUN — IT IS NOT DILIGENCE, IT IS GOING TO USE THE
THING.** `#3` fell when I went to *use* the carry-forward list; `#8` when I went to *use* the
cancellation ledger. **Neither came from re-reading what I had written.**
⚠ **And v1.14's blind spot is re-confirmed: every peer-caught error returned a PLAUSIBLE value.**
`doctrine.py:122` exists; a platform call is a real thing; 7,950 is arithmetically correct.

## Q5. FALSE POSITIVES — **KILLED 2 · REDIRECTED 1 · PUBLISHED-AND-WRONG 8**

* **KILLED — and one of these is the highest-value thing this lane did all day.** (a) A
  *"timestamp from the future"* suspicion on `PROGRAMME.md:33` — it was **my own clock 56 minutes
  stale**, the expiry class landing on me. (b) ⭐ **I drove the stop-loss switch's ON state through a
  non-existent env hook, got `slot_free=False`, and was one sentence away from publishing *"the ON
  state is broken"* about a LIVE SAFETY SWITCH.** I re-drove it against the real parameter instead.
  **A failed drive of MY OWN instrument is not a finding about THEIRS** — and that sentence is the
  whole of what stopped a false defect claim on the mechanism protecting the slot.
* **REDIRECTED 1:** the *"mid-flight rule change"* concern (floor raised under four running arms) —
  checked, **not biting**, and the check produced the remote-gap-is-every-case finding and the
  regression-on-cancelled-arms finding instead.
* **HARM CAUSED: 0. NEARLY CAUSED: 1** — I told the builder to spend a platform call that was already
  unnecessary. Retracted before they spent it.

## Q6′. CLAIMS ABOUT ANOTHER LANE — **conduct claims ZERO (ninth consecutive run)**

**Relayed figures needing correction: 2**, both corrected same-session, both now carrying their owner
and window (`#5`, `#6`). ⚠ **`#6` is the one worth keeping: I adopted a peer's figure in a MESSAGE,
it never reached an artefact, and I recorded it anyway — because *"it never made it into a commit"*
is exactly how an adopted wrong number survives.**
⚠ **And `#4` is a claim about a PEER'S ROW that was wrong**, which is v1.13's axis: it cost research
a re-derivation and their correction was better than my original.
✅ **Favourable-claims-about-self (v1.14's extension): none volunteered, none accepted.**

## Q7. WHAT DID I DECLINE — **5, all held**

Typing any verdict (the `#79` re-price and the queue re-pricing went to the owner) · building
research's owner predicate · running the G413g4 kill (offered **only the number**, explicitly not a
recommendation) · filing `_intercept` as a dangling reference (one of its two anchors is a legitimate
cross-lineage citation) · **re-litigating `f5d9b0c6`'s timing after the builder ruled it theirs to
make.**

## Q8. MECHANISATION — **1 in code, 3 adopted-but-DEFERRED**

`12f62f5d` (the widened-window guard) is the only flag that became code today. **Three more were
adopted and deliberately deferred** — the parser-divergence fix, the three-valued owner predicate,
the remote-gap consequence — **because Magnus's momentum rule sends tooling fixes to the wrap.**

⛔⛔ **THIS IS AN INSTRUMENT PROBLEM AND IT IS THE HEADLINE OF THIS RUN: Q8 COUNTS FLAGS THAT BECAME
SCRIPTS, AND THE MOMENTUM RULE NOW STRUCTURALLY SUPPRESSES THAT COUNT.** A successor reading
`Q8 = 1` against `Q8 = 7` (s43/s44) would conclude this lane's mechanisation collapsed. **It did not
— the queue moved.** ⇒ **Q8 must split ADOPTED from BUILT**, or it will mis-measure every session
under the momentum rule.

## Q9. DID MY CORRECTIONS NEED CORRECTING? — **0. Best recorded.**

Four corrections published (`#3`'s carry-forward re-derivation, the `#79` concession, the 7,950 →
440 restatement, the `SEALPECK` correction). **None required a second pass.** ⚠ **Stated with its
limit: the run had a high correction VOLUME, so 0-of-4 is a rate on a small denominator and is not
yet a trend.**

## Q10. THE CONCERN AS STATED, OR AS I RE-STATED IT? — **0 failures**

Research asked whether inferring ownership was *"a new place to be wrong"*; I answered with the
ledger's measured coverage and the hole that lands on the live holder — **their question, not a
weaker one.** The builder asked for *"the number"*; I sent the number **plus** a discriminator,
flagged as an addition and explicitly **not** a recommendation.

---

## THE LEDGER

> **Prevented ~10 · Caused 0 · Nearly caused 1.**
> **DETECTION ~10/10 · PRESCRIPTION 4/5.**

**The one wrong prescription is `#4`:** the detection was right (`#79` is mis-stated) and the
**remedy** — *"the real plank is a radius/role extension"* — **buys nothing**, because the band I
named was already covered. **Research's replacement was better and is an ENGINE fact rather than a
code-coverage one** (builder `attack` hits a *building*; a builder bot is not a building ⇒ our chase
provably cannot kill).
⇒ **v1.11's retirement of *"detects better than it prescribes"* holds a fifth run** — prescription is
again the stronger half by rate, and its single failure was the one recommendation I made about
someone else's row.
