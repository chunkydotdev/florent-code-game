# SIDE-LANE RETRO — instance s50, 2026-08-17 (~17:19Z → 20:46Z). Instrument version: **v1.18** (bumped in the same action). FIRING 18.

Answered from the day's artefacts (coordination tail f973d4a7..179518c9, my 11 commits, peer messages where a claim was only ever a message — labelled as such).

## Q1 CONSUMPTION — effectively total, mostly within minutes
~10 flag/cert artefacts raised; every one acted on same-session. F1 (uncommitted load-bearing work) consumed in **<2 minutes** with all four items committed. F2 (Erebus-anomaly-is-v143) consumed by BOTH lanes and promoted by research into standing panel practice (**cells split by oppver**). Four number flags on the v159 decode accepted with in-place amendments. Two lock certificates (SALTRAY, RINGLADDER) consumed; runner cert consumed before its first automated fire. The heartbeat-frozen-at-RUNNING gap and the append-check fix shape both FILED on the wrap list (verbatim, per the builder).

## Q2 LATENCY — every outcome-changing flag beat its decision
The v142-"twice" correction beat the builder's consumption line into the record. F2's oppver split beat the pooled-cell readout. The runner aim/guard cert beat cell 2's fire by ~10 minutes. RINGLADDER's cert landed before any gate look (~14 rows on the tape). The lock-in sweep surfaced 605de1bc within one cycle of the directive.

## Q3 INFERENCE PUBLISHED AS FACT — **2 published (best on record; prior runs 5-10), ~4 killed pre-publication**
Enumeration rule: read every commit and message of mine, not grep (v1.15 rule). Published:
1. **"`_v490rush2` is STAGED (index A); a bare `git commit` sweeps it"** — FALSE (intent-to-add; `git commit` does not include ita entries). KIND: judgement · one surface (a porcelain status letter read as index state). Peer-caught (builder, with an empirical drive); retracted at the durable record same hour.
2. **"BARS+worklist `bb2dbf4b` — no exemption-less window"** — true at COMMIT level, incomplete at FILE level (a silent awk empty-append had inverted the file ordering for ~2 min). D21 shape: the claim was about TIME, the evidence was about COMMITS — and the failed append left no git trace, so the gap was unverifiable from my surface. Peer-disclosed; scoped by addendum, not silently ratified.
Killed pre-publication, all by OPENING THE PRIMARY: the SALTRAY "stall" (corefill log: CANCEL), the "cancel-for-capacity" misattribution (typed row: TREND-FLOOR by rule), the CONTROL_PIN-mismatch worry (cat: already moved), the lingling id "absence" (pagination). ⇒ **the mechanism that works is unchanged for a sixth run: going to the primary before typing the sentence.**
⚠ **v1.14's blind spot confirmed a THIRD time: both published errors returned PLAUSIBLE values; every pre-publication kill came from a primary that returned something ILLEGAL for my hypothesis.**

## Q4 SELF-CATCH — substantive kills 4, published escapes 0 self-caught (2/2 needed peers)
The redirect case is the run's best: the "stall" flag died AND redirected to a better finding (**a cancelled shard's heartbeat stays RUNNING forever** — filed as wrap debt with an accepted fix shape). Q5's REDIRECTED bucket earned its keep again.

## Q5 — killed ~4 · redirected 1 · published-and-wrong 2 · harm 0
The wrong sweep-hazard clause cost one reply — and only because the reader audited it empirically ("a wrong flag costs a one-line reply" held, conditionally as v1.14 noted).

## Q6′ — conduct claims ZERO (11th consecutive run); every relayed figure carried owner+window
Verified-before-flagged throughout (all four decode flags were re-derived on the platform before publishing). The runner byte-identity claim was explicitly carried as RELAYED in both certs. One favourable claim risk ("most complete prereg I've certified") — checkable, and the builder correctly re-routed the credit to the fresh-drafter RULE; logged as the confirmed-approach observation (durable in the tail at 20:5xZ... committed in the c15e1a20 note).

## Q7 DECLINES — four, all held
Refused to attribute c3d122b7 (both branches carried; resolved field-probe on a three-way denial — the refusal was vindicated). Refused to read/publish the SALTRAY interim share (the gates are the registered readers). Declined to deep-audit analysis docs per the rescoped charter (Jython, 0033, census — stated on the record each time). Declined to fix the heartbeat gap inline (Magnus's wrap rule).

## Q8 (FIRST RUN OF THE v1.17 BUCKETS: ADOPTED / DEFERRED-WITH-A-HOME / CORRECTED-IN-RECORD)
**ADOPTED+DEFERRED-WITH-A-HOME: 4** — heartbeat terminal-state (wrap list, fix shape accepted), append-check bind-to-mutation (wrap list, verbatim), mid multi-UUID grep (wrap list), the 20-row match-list pagination trap (recorded in a committed note as an instrument warning). **CORRECTED-IN-RECORD: 2** — the F1 retraction and the cert addendum, both changing what a successor reads. **BUILT: 0, correctly** — the buckets did their job: a zero BUILT no longer reads as a mechanisation collapse.
(Session-scoped, not counted: the selective ship-chain watch with batching — my own wake instrument, dies with me by design.)

## Q9 — corrections needing correction: **0** (F1 retraction and cert addendum both held). Prescriptions: 2/2 accepted verbatim (heartbeat, append-check) — the first run in three where none was improved on delivery; n=2, not a trend.

## Q10 — concerns addressed as stated: the builder's disclosure was amended at the NAMED sentence of my cert, not restated weaker.

## THE LEDGER
> **Prevented ~3 (double-weighted v142 sample pre-commit; stale pooled-cell population; the F1 closure itself) · Caused 0 · Nearly caused 0.**
> **DETECTION: strong (every phantom died on a primary). PRESCRIPTION: 2/2 consumed verbatim.**

## OBSERVATION — NOT ROUTED
The day's two best catches (F2's v143 re-scoping; the heartbeat gap) both came from the same move: **reading the platform/logs DIRECTLY at a moment when every lane already had a story that fit.** The story was never wrong enough to flag — it was incomplete in a way only the primary showed.
