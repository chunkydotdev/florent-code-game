# SIDE-LANE RETRO — instance s51 (2026-08-18 boot 03:07Z → 2026-08-20 wrap ~08:5xZ, incl. a ~36 h parked window under Magnus's token directive). Instrument: v1.18 → v1.19 (bumped in this same action). FIRING 19.

Answered from the session's artifacts (coordination notes, results.tsv rows, commit log, peer messages), not memory. Trigger: the builder's wrap-ping relaying Magnus's call — the standing rule's SIXTH firing.

## Q1 CONSUMPTION — effectively total, with verified closures
Every flag raised this session was consumed by its owner same-session, and every closure was verified by this lane on a primary before being credited: the HANDOVER v159 record (fixed, tape-adjudicated, arithmetic confirmed exact both ways); v513 D-pass F1/F2/F3 (Magnus list item 6 / RMST₃₀₀ registered in the SIEGECREW prereg / evidence base banked in-repo — F4-F12 consumed into the prereg's own text, several verbatim); wrap-fix re-drive D1-D4 (D2 fixed as record correction incl. the provenance seam; D1/D3/D4 routed to committed wrap debts with behaviour-now lines carried verbatim); SHIP_BAR encoding flag (PROGRAMME field added, parse-clean); siegecrew-final two record flags (correction row, tape-recomputed); head-vs-holder final-row nudge (row banked with the decision-time read bound). The builder's closing characterisation ("flags caught real money all the way through") is THEIR judgement, logged as theirs per Q6′.

## Q2 LATENCY — every flag beat its decision
F1 beat the SIEGECREW lock (its state became ratification blocker B5); the SHIP_BAR encoding flag landed the same hour as the bar's first use; the siegecrew-final flags landed minutes after the row; three lock certificates were issued at lock time with measured pre-start margins (86 s, 86 s, 22 s); the bar-arithmetic confirmation reached the Magnus+x3r0 conversation before it happened.

## Q3 PUBLISHED ERRORS — **2**, and both are a NEW COSTUME of the old mechanism: WRONG CLEARANCES
Enumeration rule: read every published verdict/flag of mine in the tail and messages; count those later shown wrong. Grep would return 0 — both errors were the word "clean."
1. **"Clean on every check" on the SHIP_BAR PROGRAMME edit** while the edit orphaned KILL_TARGET from the parsed block — my read grepped the diff's `+` lines only, so deletions were invisible BY MY OWN FILTER (D21c: a bounded read claimed as the file). Caught by the builder's own restoration commit.
2. **"CLEAN" on QUEUE #96's admission** after verifying all three anchors — without asking whether the CONSUMER (queue_check, which requires the literal `GREP:` token) could read the row. Caught by research. (Authorship of the defect was theirs; the audit miss was mine; both logged with that split, mutually agreed.)
**Both clearances returned PLAUSIBLE values — the v1.14 blind spot's FOURTH confirmation.** The v1.19 carry question ("could my surface have shown the refutation?") is DISCHARGED with a clear answer: **yes, both times** — the full diff and a queue_check run were each one command away. These are available-surface misses, not relay cases; no RELAYED-UNVERIFIED label was owed. The carry DISCRIMINATES and the question is kept.
⇒ **THE INSTRUMENT CHANGE THIS EARNS (v1.19): a clearance names its checks.** "Clean" is a claim about the audit's whole domain; what I ran was a checklist. The publishable form is "clean on: [enumerated checks]" — which makes the two misses impossible to write, because "clean on the + lines" and "clean on anchor verification" would each have exposed their own bound.

## Q4 SELF-CATCH — 0 of 2 on the ones that mattered; the mechanism held on the ones that didn't
Both substantive misses needed a peer. Self-catches (lower stakes): the v528 "defence bar rises" estimator misread (killed pre-publication by opening the report — pattern-matched "rises" to RMST where the quoted metric was the timely share); the FLIPPOOL interval-convention wobble (caught by cross-row comparison). Mechanism unchanged for a sixth run: a second instrument / going to use the thing — never re-reading.

## Q5 — killed ~4 pre-publication, 1 redirect, 0 published-and-wrong flags
Killed: the v528 misread; the PINCERPOOL 67.05 input (chased to its ratio derivation instead of flagged); the remote_battery cross-host pooling concern (dissolved by reading the control block, which was exemplary); the v530 "engine conflict" non-flag (correctly routed by its owners before I could mis-enter it). REDIRECTED: the head-vs-holder D-check redirected from "audit the framing" to "the final row does not exist" — the more consequential finding, third session running that the redirect bucket pays.

## Q6′ — conduct claims ZERO (twelfth consecutive run); relayed figures carried owners throughout
One explicit non-ratification on the record: research's "control same bytes as v159" was declined as a checked fact and labeled as the s50 record's claim at their decode's foundation.

## Q7 DECLINES — four, all held
Declined to re-escalate the x3r0 lock-in scope question (already on Magnus's record); declined to flag the builder's "alarm stands unaffected" refinement (13.5pp margin vs sub-pp selection effect — noise); accepted the builder's scoped decline of the truncated-normal sizing (conditional obligation, correctly priced); declined to audit research's analysis commits throughout (the rescoped charter held under ~40 monitor events).

## Q8 — ADOPTED 3 / CORRECTED-IN-RECORD 6 / BUILT 0
ADOPTED: gate.py field-completeness manifest (wrap debt 7, my framing verbatim); per-pair det.py controls (conditional, priced); D1's fix shape (baseline tagged with the number's provenance) carried into the debt list verbatim. CORRECTED-IN-RECORD (changed what a successor reads): HANDOVER v159 record; siegecrew-final correction row; the atlas §9.3 inversion chain + provenance seam; build-report median 640→565; PROGRAMME SHIP_BAR field; KILL_TARGET restoration (builder's, from my miss). BUILT: 0 — the wrap rule working as designed, second consecutive session.

## Q9 — 0 corrections of mine needed re-correction
Every number this lane published (tape adjudications, recompositions, bar arithmetic, prior derivations) stood; the two failures were clearances, not corrections, and are Q3's.

## Q10 — no restatement instances found
The builder's median question was answered on its own file-mapping terms; the mapping settled it.

## THE LEDGER
**Prevented ~9** (the reaped-evidence base; the unrecorded door plank reaching a lock; the two-files ship bar; the ship_watch laundering path priced with a behaviour-now line; the dangling 46.6 decision citation; the siegecrew firing-statistic/selection-caveat pair; the SIEGECREW/PINCERPOOL/FLIPPOOL certs' correction-forcing function — priors and conventions fixed pre-lock twice). **Caused 0. Nearly caused 0** (the v528 misread died in-check). **DETECTION**: every number verified this session reproduced or was adjudicated (v159 record, +32.14, 565, 63.91 cell-exact, both finals' recompositions). **PRESCRIPTION**: all consumed; the manifest and the D1 fix shape accepted verbatim; zero prescriptions needed correcting — but note both Q3 misses were VERDICTS, this lane's third product class, and that class now carries the enumeration rule.

## OBSERVATION — NOT ROUTED
The three-lane loop ran near its ceiling this session: five preregs/shards certified or closed with every stop called in advance by the pricing instrument (5-for-5), two engine-fact conflicts resolved with retract-at-source, and both of this lane's misses were caught by the lane whose defect class they matched (the builder caught the file-scope miss; research caught the consumer-visibility miss). The audit net catches the auditor — which is the structural answer to Q4's oldest worry.
