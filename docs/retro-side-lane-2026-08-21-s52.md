# SIDE-LANE RETRO — instance s52, 2026-08-21 (instrument v1.19; FIRING 20)

Session span 2026-08-20T08:52Z → 2026-08-21T12:3xZ. The session: stealth grind → v174 ship
(waiver) → map rotation + baseline move → v177 ship (Baltsars banditer v2). Two ships, five
lock certificates, one voided screen, one hung-wake escalation, ~12 flags.

## Q1 CONSUMPTION — all flags consumed same-session; 7 changed outcomes
Outcome-changing, each verified on a primary: (1) **the HOMEPOOL r300-deferral flag** → the
registered read was computed and BOTH BARS FAILED with the fall confirmed — a measured
DEFENCE_ADMISSION violation that deferral would have left reading as "never violated";
(2) **the v169-screen provenance question** → 150/201 rows were zip-era shells; screen VOIDED
pre-bank (release-bar evidence was 75% garbage); (3) **the v176 objection at the v2 window
call** → rollback target + disclosure corrected pre-fire; (4) **the hung-wake escalation** →
93 stalled minutes ended minutes after the ping+push; deadman-check wrap debt filed;
(5) **the GENREF cross-battery flag** → the overfit gap became a within-fixture read;
(6) **the D21d called-vs-copied question** → a real 8-line re-derivation found and refactored
away; (7) **the powered-n omission on bar (1)** → the bar gained its fixture class.
Notes adopted without exception (registered-prior, exclusion-restatement, stale-anchor
labels, candidate-not-proxy, twin-arms control, pathspec rule, running-consumer rule).

## Q2 LATENCY — flags beat their decisions in every case but one
Pre-readout, pre-fire, pre-lock throughout. THE EXCEPTION, recorded against this lane: the
hung wake was detected 93 minutes AFTER the stall began — from research's cadence note
cross-checked against the tape, not from any watch of mine. My ship-watch covered the
boundary AFTER a fire; nothing watched for a fire that failed to happen. **The gap class:
I certify fires; nothing certifies non-fires.** (Routed: reboot state names the successor
item — a ship-sequence deadman is the builder's wrap debt; my checklist gains "arm a
completion-deadline check at every window call".)

## Q3 INFERENCE PUBLISHED AS FACT — 3, all corrected, with the mechanism named
1. **"§6 manifest complete" (v535)** — claimed domain: artifact existence; actual domain:
   table shape. Peer-caught (the V535POOL drafter's B3). The falsifying check was one `ls`
   away. → the EXISTENCE-CHECK RULE (a manifest row's path gets a sampled existence check
   before the word "complete"), applied in every subsequent report audit.
2. **The "~4pp/day" repetition** — a relayed screen-note figure echoed into release framing;
   research's paired McNemar refuted it (p=0.14). Corrected in-channel where I amplified it.
   Sub-question (v1.18 carry): could my surface have shown it? NO — the refutation needed
   the paired analysis; the correct label at repetition time was relayed-unverified.
3. **"the socket mechanism's fourth confirmation" folding in off-prediction cells** —
   builder-corrected: the confirmation is the predicted cells; off-prediction wins are
   labelled, not banked (D6's own rule, applied to my summary wording).
**v1.19 CLEARANCE-RULE FULL-SESSION TEST: PASSED-WITH-TEETH.** Every clearance named its
checks; instance 1 shows the residual failure mode — a WORD ("complete") exceeding the
enumeration — and produced the existence-check sub-rule. The enumeration bounded the blast
radius exactly as designed.

## Q4 SELF-CATCH — 2 of 5 substantive
Self-caught: the near-ratification log (v535 clearance amended unprompted when the B3 catch
landed), the pathspec self-audit (5 commits verified 1-file). Peer-caught: the three Q3
instances. The catching mechanism remains GOING TO USE THE THING / a second instrument.

## Q5 FALSE POSITIVES — 0 wrong flags, 1 redirected
The v536 tree-only-commit flag resolved as agent-mid-flight (not wrong: the no-fire-before-
report constraint it asked for was adopted and mattered). PICKUP-TIMEOUT fired once on a
real blocker (control_pin). No flag cost more than a reply.

## Q6′ CLAIMS ABOUT LANES — 1 firing (the 4pp repetition), corrected; conduct claims 0 (13th consecutive)

## Q7 DECLINED — 3, all held
(1) Every bar-meaning adjudication routed to Magnus (carve-out, #98 class, bar anchor,
"beat" reading) — all four returned rulings within hours; (2) the 02:33 bar-2-met push
declined (no action needed of him) — correct, it kept for morning; (3) no engine games run
all session (charter hard limit; the reconcile pattern's probe halves went to the builder).

## Q8 — ADOPTED 9 / CORRECTED-IN-RECORD 4 / BUILT 0
Adopted into tools/briefs/practice by name: gate-receipt design · report_lint spec (named
wrap debt at the 3-failure threshold) · flag-effective-state lint (RD2) · existence-check
rule · running-consumer rule (3 firings in-session, each catching something) · pathspec
rule (3 lanes) · twin-arms control · deadman-check class · fire-conditioned-on-status.
BUILT 0 = the wrap rule working as designed (all buildable shapes are wrap debts with
behaviour-now interims).

## Q9 CORRECTIONS NEEDING CORRECTION — 0
The v176 objection, the 4pp correction, and both PROGRAMME-slug flags all stood as issued.

## LEDGER
**Prevented: 7 (Q1's list). Caused: 0. Nearly caused: 0.** Detection and prescription both
clean this session; the prescription discipline (fix named against the consumer, verified
per class) held — report_lint and the deadman item are specs with named positive controls.

## OBSERVATION — NOT ROUTED
The session's audit volume ran far above the s51 rescope's intent (ship-chain-only) because
the ship chain itself ran continuously — two ships, five locks, a rotation, a baseline move.
The rescope's boundary held: zero analysis-doc audits were performed; everything audited was
ship-chain or a build report feeding one. Volume tracked the chain, not drift.
