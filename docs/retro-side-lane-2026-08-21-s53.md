# SIDE-LANE RETRO — instance s53, 2026-08-21 (instrument v1.20; FIRING 21)

Session: boot 12:49:56Z → wrap ~16:4xZ. Answered from the day's committed artifacts
(coordination tail 9ed2399b4..7f9314f35), not memory. Enumeration rule for every count:
derived by reading the tail's side-lane blocks and peer consumption notes end to end;
greps used only to locate, never to count.

## Q1 CONSUMPTION — ~12 flags raised, consumption effectively total, most same-hour
kladde D5 prereg gate (leg held → band-lined → locked a525fcf5c) · cadence-cell subject
check (RD-s53-1 spec fa6682bff) · #105 estimator (fixed at source 614075601) ·
KLADDEDOSE verdict pre-flag (the v1→v3.1 chain) · reachability ratify line (ADOPTED BY
BOTH LANES; executed in V543POOL and DOORWAVE locks) · bar-2 stale-constant rider
(folded into audit-R2) · unrated subject discipline (builder wrap batch) · Torsko
attribution question (answered: x3r0) · ed8172e7 instrument alarm (answered in 4 min —
fastest flag-to-closure on record) · R1000_IS_DEFEAT collision (MAGNUS RULED same hour:
"We play to destroy their cores") · escalation-path check (answered: in-thread) ·
orphan-monitor cleanup (builder killed + ps-verified at boot).

## Q2 LATENCY — mostly ahead; one scope miss; the v1.20 non-fire sub-rule PASSED
Ahead of the decision: R1000 flag beat the new-line encoding (~15 min); estimator flag
beat the prereg draft; D5 flag beat the leg fire; KLADDEDOSE pre-flag beat the verdict
typing. THE MISS: block composition — the builder's deviation disclosure beat my fire
certificates, which had never checked the mixed-block clause (owned in-record b18da57bf;
checklist amended). Non-fire deadmen: FOUR armed (readout, shard launch, flip window,
two pairing polls), zero invisible windows — the v1.20 sub-rule's first full-session
test PASSED. The existence-check sub-rule also passed-with-use (RATIFY-F verified
executed; digests re-derived 5/5 at two locks).

## Q3 PUBLISHED ERRORS — 5, all corrected in-record; ~3 killed pre-publication
1. "explained-not-actionable" on the cadence trip — read the value, inferred the guard's
   DIRECTION without opening audit_trigger.py:516 (peer-caught; corrected cbb2fa9e2).
2. Certified the builder's v1 KLADDEDOSE verdict against my own already-published
   pre-flag — STATE: CONCEDING (see conjecture below; corrected 8a5a7b610).
3. Correction #2's vocabulary — "verdict sentence survives" where the registered word
   for an undosed mechanism is UNRESOLVED (v3.1 exposed it; owned in the v3 cert).
4. The intermediate-176-restore model (message-level, falsified by the builder's
   same-shell primaries within minutes; retracted 202eba007).
5. The premature push all-clear — one success read as steady state during a flap
   (self-caught within ~1 min; the v1.16 sawtooth error, named at correction time).
Killed pre-publication: the 5th-kladde-accept "drop" (my own filter artifact, killed by
re-driving on the full list) · a Torsko D5 accusation (converted to an attribution
question by the morning's own subject-check lesson) · my deadman's fetch/merge
shared-tree hazard (caught re-reading my own loop, re-armed clean).

## Q4 — substantive self-catch 2 of 5; mechanism unchanged (GOING TO USE THE THING)
Self: the sawtooth all-clear (next push in my own shell failed); the deadman defect
(re-read while arming). Peer: direction error (audit agent), conceding error (builder's
v2), vocabulary (v3.1), restore model (builder's shell reads). The plausible-value blind
spot: confirmed a FIFTH time — every peer-caught error returned a plausible value.

## Q5 — 0 harm; 1 near-miss; the KILLED bucket paid three times
Near-miss: the v1-endorsing certificate could have anchored the wrong verdict had the
builder not already amended. The pre-flag itself resists single classification: wrong in
registered-rule terms at v2 time, vindicated in substance by v3.1's claims-split — the
chain is recorded as a chain, not scored as one flag.

## Q6′ — attribution-by-elimination handled clean; conduct claims ZERO (14th run)
2f254fee attributed to shared-account x3r0 BY ELIMINATION, labelled as such, verified
against the builder's enumerated fire ledger, never asserted about x3r0 directly. The
shared-slot incident record kept strictly to verified acts.

## Q7 DECLINES — three, all held
Duplicate-push to Magnus during the escalation (builder's channel; vindicated — he was
active in-thread) · verdicting the 0-5 anomaly (research's; resolved same hour as famine
class) · working around the orphan-kill permission block (routed to builder instead).

## Q8 — ADOPTED 6 / CORRECTED-IN-RECORD 5 / BUILT 0 (wrap rule holding, third run)
Adopted: reachability ratify line (both lanes, executed twice same-session) · HEAD-check
certificate template (own) · block-composition clause (own) · unrated subject discipline
(builder batch) · stale-constant rider on the ship gate (folded to R2) · behavioral
version-probe method (research executed it).

## Q9 — 2 corrections needed correcting, both same-hour, both vocabulary/scope
Correction #2 needed v3.1; the all-clear needed the flap caveat. Same shape as s34's
"right that something was wrong, wrong about the fix" — here: right substance, wrong
word.

## THE CONJECTURE — FIRST CONFIRMED FIRING OF THE CONCEDING PRE-REGISTRATION
v1.3.3 pre-registered: "conceding is pre-registered to err TOWARD THE CRITIC; if
concession errors run toward the author's original position instead, the model is
STRUCK." Error #2 ran exactly toward the critic (I flipped to the builder's v1 reading
against my own published analysis without re-deriving the deciding test). The model
survives its registered test — first confirmation since the pre-registration, n=1,
recorded without promotion to a prior.

## LEDGER
**Prevented ~8 · caused 0 · nearly caused 1.** Detection strong all session; the
prescription record is the KLADDEDOSE chain — right substance twice with the wrong
verdict word, resolved only by the builder's v3.1 claims-split. Certificates issued: 11
(2 lock, 5 fire/cell, 1 stop, 1 anchor readout, 2 verdict) — three were stale-at-
publication against a moving subject before the HEAD-check template landed; zero after.
