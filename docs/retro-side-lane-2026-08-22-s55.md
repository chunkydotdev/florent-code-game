# SIDE-LANE RETRO — instance s55, 2026-08-22 (instrument v1.23; FIRING 23)

Run at wrap on the builder's-wrap trigger (standing rule, tenth firing of the trigger).
Answers from the day's artifacts: the coordination tail 09:46Z–10:59Z, nine committed
side-lane notes/certificates, the git record.

## Q1 CONSUMPTION
~9 flags raised; consumption effectively TOTAL, every one acted on by its owner:
site-2 DEFF closure (consumed by research, nothing owed) · decode-absent (re-spawned) ·
decode spawn collision (both lanes stood to the resolution pre-fire — zero clobber) ·
weld-anchor 5948-not-5722 (DIAG corrected + v622 edited at the right site) · era
name-collision _v62Xh4 (builder checks per version) · #119 enumeration incomplete (row
WITHDRAWN by its author, and extended past my flag) · WIN_RATE stale-citation
verification (rides the next CLAUDE.md batch) · fable-agent fate gap question (produced
the builder's correction — two artifacts fixed, one false finding withdrawn) ·
"equally blind" overcorrection (corrected ledger line on the record).

## Q2 LATENCY — every flag beat its decision
The two that mattered most: the 5948 anchor correction landed BEFORE the v622 un-weld
commit (the edit went to the right line); the collision flag reached the builder BEFORE
their agent spawned (their words: "flag arrived before any spawn"). The fable gap
question came after the disclosure but before any downstream consumer built on the false
audit_trigger finding — the instrument-debt entry was withdrawn unconsumed.
Non-fire deadman (v1.20 sub-rule): no window-call GO occurred (CRASHREP held), so the
deadman had nothing to arm — not a gap, an empty precondition.

## Q3 INFERENCE PUBLISHED AS FACT — ONE, corrected in-record within ~4 minutes
**"7 call sites exist, not 5" — published from a grep piped through `head -8`.** The
true count is 9; my own pipe truncated :5161/:5200 out of view. D21(c) verbatim — a
bounded read published as a complete enumeration — committed WHILE flagging someone
else's incomplete enumeration. TAG: KIND judgement · STATE auditing · HYPOTHESIS mine
(the flag's correctness). Caught via research's fuller (also wrong: 8) count; settled by
my unfiltered re-derive. **Sub-rule earned, applied twice the same hour and adopted by
the builder's retro independently: ENUMERATE WITH THE UNFILTERED INSTRUMENT — never
publish a count read through head/tail/first-alternative without re-running unbounded,
and when re-deriving a claim about a pattern, EXTRACT THE PATTERN FROM THE SOURCE FILE
(applied: the audit_trigger regex driven from an ast.literal_eval of the file, which is
what caught the "equally blind" overcorrection).**

## Q4 SELF-CATCH
1 substantive miss (the count), externally prompted (research's extension), self-completed
(the unfiltered re-derive that corrected BOTH lanes' counts). The session's four
verification-shares-the-blind-spot instances (my skip_core-style bound, the agent's
first-alternative quote, the builder's truncated re-test, the "equally blind" clause)
were caught 2-by-me / 2-by-builder — the class is lane-symmetric, per the builder's
retro the same hour.

## Q5 WRONG FLAGS — ZERO
Every flag was substantively correct; the one with a wrong NUMBER inside it (#119's "7")
is Q3's instance, and the flag's direction and mechanism ("enumeration from the fix's
history") were both right and were what triggered the withdrawal.

## Q6′ CLAIMS ABOUT PEERS
All relayed figures carried owner + window; the decode relay's "65/65 turns+seat"
compression was flagged WITH the doc's own hierarchy cited (research amended in-record).
Conduct claims: ZERO, sixteenth consecutive run. The self-disclosure verification stayed
strictly on artifacts (files, commits, riders) — the error-direction analysis in it is
the builder's own finding, quoted as theirs.

## Q7 DECLINES
One: declined to re-derive the decode's per-game §3 table and crash-census selftest,
LABELLED as not-re-derived in the certificate (the doc's own controls cover them). The
label is the decline done right — no unlabelled inheritance.

## Q8 — ADOPTED 3 / CORRECTED-IN-RECORD 3 / BUILT 0
ADOPTED: the source-extraction verification method (same hour, both lanes) · the
five-item CRASHREP pre-lock checklist (items 4/5 forwarded to the running drafter — the
mid-flight brief channel) · the window-hold hazard framing (orphaned-restore) into the
builder's decision. CORRECTED-IN-RECORD: my count · the withdrawal's 8 · the "equally
blind" ledger line. BUILT 0 — the wrap rule working; no scripts owed.

## Q9 CORRECTIONS NEEDING CORRECTION — ZERO
My correction of the two counts (7→9, 8→9) stands; the certificate re-derivations stood
un-amended all session.

## LEDGER
**Prevented ~5** (double-spawn clobber · wrong-line v622 edit · a #119 build on a false
premise · consumption of the false audit_trigger finding · the overcorrected blindness
claim entering the ledger) **· caused 0 · nearly caused 0.**

## v1.22 CARRIES, DISCHARGED OR ROLLED
* **The re-derive rule's first full-session test: PASSED WITH TEETH.** Every certificate
  re-derived what it asserted (exclusion arithmetic, dup-table sums, checker re-run,
  two-clock reads, pattern drives); the one number I published WITHOUT a full
  re-derivation is exactly the Q3 instance — the rule's counterexample proves its scope.
  RATIFIED as permanent, with the source-extraction sub-rule added (see Q3).
* **Q1's window-cert-template survival question: NOT TESTED** — no live window fired
  (CRASHREP correctly held pending the model decision). ROLLED to v1.24.

## OBSERVATION — NOT ROUTED
The session's audit volume was certification-heavy and flag-light relative to s54
(9 flags vs ~14) with zero wrong flags and zero caused harm — consistent with the s54
observation that this lane's value in a clean-wave session is keeping corrections honest
rather than flag volume. Second consecutive such reading; if a third occurs, the retro
should ask whether the lane's cadence targets are calibrated to flag-rich days.
