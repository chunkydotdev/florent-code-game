# AUDIT — 2026-08-14 s39, spawned on audit_trigger 2/6 (opus, no queue stake)

Builder's summary of the audit session's report; verdicts are the auditor's,
consumption receipts are on the coordination tape at ~15:0xZ. Prior art:
docs/workflow-analysis/ 2026-08-08 (19%-power find).

**Headline: both tripped rows were instrument defects, not signals — the FIRE
was spurious. With both repaired the day reads 0/6. But the sweep found one
decision resting on a reading its own data no longer supports, and answered
the local-DEFF question with a new measurement.**

1. **cross-lane analysis TRIP — UNSUPPORTED.** Numerator counts 8 PREREG-*
   files living in docs/research/ (directory-prefix exclusion misses them);
   denominator starved because the BUILDER typed zero of today's ~10 decisions
   onto results.tsv (22.1h stale, no freshness refusal). Repairs: filename-
   based exclusion, freshness gate, and the builder types its gates (BACKFILLED
   same hour, results.tsv rows sealrepair-gate2700 .. v140-era-close).
2. **ship cadence TRIP — UNSUPPORTED.** Verdict flips with window choice
   (0.562@18h .. 0.429@24h); the s30 ±4h stability rule was applied to the
   sibling row and never this one; counts rollback transitions as ships (the
   row reads healthier when churn is worse). Repair: stability rule + net-new
   versions.
3. **NESTSHOT2 harm claim — AT-RISK, AMENDED at the record.** Registered-n drop
   45.75@1012 stands as ALLOCATION; zombie overrun rows read 52.66@621, pooled
   48.38 [45.95,50.80] — harm UNRESOLVED, mechanism story demoted to
   hypothesis. Control: V141VS140's overrun CONFIRMS its drop (pooled 45.78).
4. **Local screens need NO DEFF correction — CONFIRMED, newly measured.**
   Pair-weighted local DEFF = 0.98 (rho -0.020) across 124 shards; balanced-by-
   construction fixture. Naive bars correct, marginally conservative. Hazard
   runs the other way: #55 as worded would widen local bars 24-35% for
   correlation that is not there. Outliers: strong map-interaction arms up to
   ~1.25 — still leaves SEALFLOOR0 untouched.
5. **AIMTHROW2's 2700 pass was 0.05 sigma — not a decision.** Now 50.06@3933.
   Rule repair owed: pre-committed indifference band + tie-break at gates; also
   write down that GATE-1000 <48 discards a true-50 arm 10.3% of the time
   (deliberate, previously unwritten).
6. **Catastrophe gate — CONFIRMED**; alpha restated 1.0e-4 (continuous
   boundary), power 100%@true-30 / 4%@true-45. Wake<42 costs 0.13%.
7. **Retro-silencers sound today, keyed on the wrong variable** — :400 tokens
   have no expiry; repair: condition on tsv_age or drop dead shards from the
   worklist.
8. **D26 pool label — AT-RISK, RELABELED.** Host delta 1.61pp p=0.093; stated
   ±0.94 is finer than the exchangeability test behind it (spread-inflated
   ±1.24). Verdict robust per-host. D26 standing rule needs the
   compute-neutrality clause (TLE bias) written in.
9. **Velocity rigor — CONFIRMED.** Four pre-screen kills each by denominator or
   both-ways dose probe, ~10 games total; CONTINUE verdicts at 9.8/4.4 sigma;
   nothing resembles the 2026-08-08 failure except #5's coin-flip.
