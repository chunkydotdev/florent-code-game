# COREFILL WORKLIST — everything is measured against the NEW incumbent v114
# (_v146gunaxis), because PROGRAMME.md:169 says every control moves with the ship.
# Targets are multiples of 16 (8 maps x 2 seats) so seat/map balance is exact;
# seat is worth ~6.8pp on byte-identical arms, so an unbalanced cut measures seat.
# shard     treatment              control              target seed_lo
#
# 1-3 FIRST AND THEY ARE NOT NEGOTIABLE: two calibration cells + the top plank.
#     Without NULL and NEG the other verdicts cannot distinguish "no effect"
#     from "no power", which is what made tonight's read-out mean anything.
GUNFERRY    bots/_v154gunferry     bots/_v146gunaxis     5408  30000
NULL114     bots/_v146null         bots/_v146gunaxis     5408  31000
NEG114      bots/_v140noseal       bots/_v146gunaxis     5408  32000
# 4-7 re-baselined against v114. All four were screened last night against v112
#     AND ALL FOUR LACK FERRY-FIRST, so their old numbers were (X - ferry_first).
#     Against v114 -- which also lacks it -- the contrast is finally clean.
GUNBLANK    bots/_v147gunblank     bots/_v146gunaxis     5408  33000
CAP12B      bots/_v144cap12        bots/_v146gunaxis     5408  34000
CAP6B       bots/_v143cap6         bots/_v146gunaxis     5408  35000
BESTFITB    bots/_v145bestfit      bots/_v146gunaxis     5408  36000
# --- appended 21:1xZ, all measured against the LIVE incumbent v114 ---
# DOSE SWEEP on the shipped knob. v114 went live with LOKI_GUNAXIS_PENALTY = 8
# and NOBODY EVER SWEPT IT. Monotone in the penalty => VOLUME (a bigger push off
# gunner rays pulls stations further back); interior optimum => SITING. This
# answers the GUNAXTB question a second, independent way, on one constant.
GUNPEN4     bots/_v155gunpen4      bots/_v146gunaxis     5408  40000
GUNPEN16    bots/_v155gunpen16     bots/_v146gunaxis     5408  41000
# SEAT-RELATIVE ON WHAT IS ACTUALLY LIVE. LOKI-29 tests seat-relative on the v112
# chassis; if it works we would have to re-test it on v114 anyway. This is that
# test, and it does not wait on LOKI-29 to read out.
GUNSEAT     bots/_v156gunseat      bots/_v146gunaxis     5408  42000
# --- appended s33 2026-08-12T04:1xZ (builder) ---
# GUNBLANK read 52.11% (n=5408) OUTSIDE-ABOVE against the LIVE incumbent v114.
# That is a SHIP CANDIDATE and this repo replicates before it ships: GUNAXIS was
# replicated by GUNAXREP (51.94% -> 52.31%) before v114 went live. Fresh seed
# block (50000) so this is an independent draw, not a longer run of the same one.
GUNBLANKREP bots/_v147gunblank     bots/_v146gunaxis     5408  50000
# --- appended s33 2026-08-12T04:4xZ (builder) — LOKI-33 BORDER-FIRST EXILE ---
# The crash mechanism is MEASURED (tools/crash_cells.py, 72 games): EVERY border
# arrival killed the unguarded probe (13/13, 16/16) and the GUARDED probe took 16
# and lost nothing (0/16). DOSE MEASURED ON THIS TREE, 48 games vs the same probe:
#   GUNBORDER  border=28 interior=42 (40.0%)  crashes=28
#   V114       border= 7 interior=21 (25.0%)  crashes= 7
# 4x the border dose and 4x the kills, from a SORT-KEY REORDER -- no new unit, no
# new cost, no filtering, so it cannot lose an exile the incumbent would have made.
# This is the currency read. Mechanism confirmed != currency plank.
GUNBORDER   bots/_v157gunborder    bots/_v146gunaxis     5408  60000
# STACK: LOKI-26 gunblank (52.11% vs v114) + LOKI-33 border-first exile.
# Independent mechanisms -- a barrier on an enemy GUNNER's firing axis vs the
# ORDER we throw exiled enemies in -- so they should not interact. If GUNBLANKREP
# confirms and GUNBORDER lands, this is the ship candidate; if it reads BELOW
# both singles, they interact and that is worth knowing before shipping either.
BLANKBORDER bots/_v158blankborder   bots/_v146gunaxis     5408  70000
# --- LOKI-34 NON-STRIKE SURCHARGE (QUEUE #16) — dose swept, knob pre-committed ---
# One condition in eco.py `_eco_spendable`, the single chokepoint for non-strike
# spend (conveyor eco.py:543,774 · harvester :944 · launcher main.py:608). While
# a raider heartbeat is fresh on SLOT_RAID_LIVE and rnd < 250, eco spend must
# clear cost + surcharge. ONE new constant, ZERO new store slots (all 16 bound),
# ZERO new units, ZERO scale delta. At surcharge 0 it is byte-identical.
# This is DEFENCE_ADMISSION_BAR enforced as a RUNTIME GATE rather than a bar a
# reviewer checks after the leg. READ MEDIAN KILL ROUND, not just game share.
SURCH30     bots/_v159surch30      bots/_v146gunaxis     5408  80000
SURCH90     bots/_v159surch90      bots/_v146gunaxis     5408  90000
# ⛔⛔ GUNBORDER RELABELLED s33 — IT IS A COST SCREEN, NOT A BENEFIT TEST.
# Flagged by the side lane, verified independently by research, confirmed by me
# against the tree: `_v146gunaxis/main.py:120` wraps `_dispatch` in a blanket
# `try/except Exception`. THE CONTROL IS A GUARDED BOT, so the crash channel --
# the entire mechanism this plank exists for -- IS OFF BY CONSTRUCTION. And the
# shard schema (ts shard game map seed seat winner cond turns) has no crash or
# unit-loss column, so it could not record it even if it fired.
# The only surviving channel is displacement, at +0.265pp PER EVENT (17 and 13
# events, z=2.25) -- which is not a win-rate delta, and converting it needs two
# factors nobody has computed. Even assuming 1:1, detecting 0.265pp needs
# ~279,103 games; this shard resolves ~2pp. It is ~52x short.
# ⇒ GUNBORDER ANSWERS EXACTLY ONE QUESTION: "is the sort-key reorder FREE?"
# A clean ~50% is NON-INFERIORITY and is the precondition that licenses spending
# a live-unrated window on the benefit -- which is where FIXTURE_OF_RECORD says
# the benefit must be measured anyway. A ~50% read is NOT evidence of no benefit.
# KEPT RUNNING. The cost question is real and answerable at this n.
# ⚠ If an unguarded arm is ever added, report it as a CEILING and NEVER pool it
# with the guarded arm: two fixtures we wrote, bracketing the field from both
# ends, averaged together produce a number about neither.
# ⛔ BLANKBORDER IS UNINTERPRETABLE WITHOUT ITS BASE. It is the STACK of LOKI-26
# gunblank + LOKI-33 border-first exile, and the base (GUNBLANK) is currently
# FAILING to replicate: first draw 52.11% (n=5408, OUTSIDE-ABOVE), replication
# reading well below at partial n. If GUNBLANKREP nulls, a BLANKBORDER read
# CANNOT be attributed -- any effect could be either plank or their interaction,
# and the stack was queued to test interaction ON TOP of a confirmed base.
# ⇒ Read GUNBLANKREP FIRST. If it nulls, BLANKBORDER answers nothing about
# gunblank and only weakly about border-first, which GUNBORDER measures cleanly.
# ⛔⛔ LOKI-34 NON-STRIKE SURCHARGE — REFUTED, and refuted with a CURVE rather
# than a shrug. Both arms stopped early at decisive n; rows kept.
#   SURCH30  38.10%  (n=693, band +-3.72)  CI excludes 50 by ~8pp
#   SURCH90  26.62%  (n=725, band +-3.64)  CI excludes 50 by ~20pp
#   incumbent (surcharge 0) = 50% by construction
# MONOTONE IN THE DOSE: 0 -> 50%, 30 -> 38%, 90 -> 27%. Every increment of the
# tax makes us worse, with no interior optimum. This is the strongest possible
# form of a negative: the knob HAS a large live effect (unlike GUNPEN4/GUNPEN16,
# flat both sides = dead knob) and its effect runs entirely the wrong way.
# ⇒ THE FIELD TACTIC DOES NOT TRANSFER. `confused`/BC2020's "rush cost" -- tax
# everything non-rush until the rush ends -- is a REAL mechanism here and a
# LOSING one. Starving eco to protect the strike costs more than the strike
# gains, at every dose we tested. Do not re-queue without a new mechanism.
# NOTE THE SHAPE: this is what the surcharge plank was FOR -- making defence
# self-limiting by arithmetic. It turns out the thing that needed limiting was
# not our eco spend.
# RESUMED s33: SURCH30 re-listed to finish its KILL-ROUND cell only. The plank is
# DEAD on game share (38.05%, z=-6.4) and nothing will revive it. What is still
# unresolved is the kill-round CI [-2,+41], and the pair 30->+17 / 90->+58 is the
# first direct price anyone has put on DEFENCE_ADMISSION_BAR: how many rounds of
# kill speed a unit of defence spending costs. The LOW-dose end is where an
# admission bar actually gets set, so that is the cell worth closing. Resumes
# from the 728 rows already on disk. SURCH90 stays cancelled: resolved on BOTH
# axes (26.42% win share AND +58 [+35,+90] kill round, failing the bar outright).
# ⛔ RESUME DID NOT TAKE: corefill's supervisor holds SURCH30 in CANCELLED
#   state and LAUNCHES ONCE -- re-listing the shard does not restart it, it
#   only duplicates the status row. Recoverable on the NEXT corefill launch
#   (overnight.sh resumes from the 728 rows on disk). Not worth a manual
#   launch racing the supervisor for one doctrine number on a dead plank.
# --- QUEUE #24 THE LAUNCHER SINGULARITY — price what nobody else pays for ---
# Launchers/game: sporks 0.00 · Lorem Ipsum 0.00 · Pivot 0.00 · O(1) 0.00 ·
# Clankers 0.09 · ph 0.87 · US 1.17. Five of the top six build essentially NONE
# and we build the most, at 20 Ti and +10% global scale each. Today's archive
# work put the CRASH channel at 1 event on 110 border landings (<=4.2% upper
# bound), so we pay a premium nobody else pays for a channel measured as weak.
# NOT a recommendation to drop them -- the launcher IS the kidnap weapon and
# LOKI is the programme. This PRICES it for zero rated exposure.
# POSITIVE CONTROL RUN BEFORE QUEUEING: LAUNCH0 produces 0 throws in 12 games
# against V114's 68. The ablation actually ablates -- verified by outcome, not
# by reading the branch. (An explicit `if LAUNCHER_CAP <= 0: return False` was
# REQUIRED: at cap 0 the cap test is always true, the release branch zeroes the
# slot and execution falls THROUGH to the build. Without the gate this arm
# would have built launchers and read as a null.)
LAUNCH0     bots/_v161launch0      bots/_v146gunaxis     5408  95000
# --- QUEUE #24b LOKI-37 EXILE0 — the arm that ATTRIBUTES what LAUNCH0 prices ---
# The launcher does TWO OPPOSITE JOBS (its own docstring: "exile intruders
# first, then ferry raiders forward"). EXILE (raid.py:679, sites sorted farthest
# from our core) is the kidnap weapon; FERRY/INSERT (raid.py:703, iterating
# friendly_bots toward dest) is how the assault reaches their core inside
# KILL_WINDOW_RND -- and uncapping it IS LOKI-27, SHIPPED as v112.
# LAUNCHER_CAP=0 removes BOTH, so a positive LAUNCH0 has two OPPOSITE readings:
# "retire the kidnap line" or "retire our fastest route to their core".
# LAUNCH0 PRICES the premium; EXILE0 ATTRIBUTES it. Run the pair, read together.
# CONTROL, outcome-verified: EXILE0 delivers 0 enemy throws in 12 games against
# V114's 33. ⚠ NOT VERIFIED BY THIS CONTROL: that FERRY still fires. Ferry moves
# OUR OWN bots and the probe logs only its own units, so the ferry half rests on
# the gate being branch-local, which is a CODE READ, not an outcome. Suggestive
# only: EXILE0's exposure is 8574 vs V114's 8124 (5.5% skew) while LAUNCH0's is
# 6065 (25% below) -- consistent with EXILE0 keeping a working launcher. Treat
# as suggestive; a successor wanting proof must log our own side.
EXILE0      bots/_v162exile0       bots/_v146gunaxis     5408  96000
# --- QUEUE #21(b) LOKI-39 GUNNER-FIRST — the tuple order WAS the constraint ---
# `main.py:_try_counterbattery` tries SENTINEL then GUNNER. A sentinel's line
# IGNORES obstacles at r^2=32; a gunner's is BLOCKED at r^2=13 -- so
# `can_fire_from` is TRUE for a sentinel EVERYWHERE it is true for a gunner and
# strictly more often. SENTINEL DOMINATES GUNNER ON THE EXACT PREDICATE THE LOOP
# SELECTS ON, so tried first it wins every contested tile and the gunner branch
# is reachable ONLY when we cannot AFFORD a sentinel (20 <= Ti < 30).
# Nobody chose that: it is tuple order meeting a dominance relation.
# DOSE, OUTCOME-VERIFIED off `tools/corpus/replay_events.py`, 6 paired games:
#   gunner-first  19 gunner / 20 sentinel  (3.17 gun/game)  39 turrets
#   incumbent      4 gunner / 26 sentinel  (0.67 gun/game)  30 turrets
# 4.75x the gunners AND 30% more turrets for the same titanium.
# ⚠ THE STATS DO NOT OBVIOUSLY FAVOUR THE GUNNER, WHICH IS WHY THIS IS A TEST:
# per Ti sentinel is 0.200 dmg/rnd vs gunner 0.175; per ammo 1.8 vs 1.75; and
# sentinel has 40 HP to 25 and 2.5x the range. On paper the incumbent's order is
# DEFENSIBLE. What contradicts it is the FIELD -- Pivot 10.77 gunners/game,
# O(1) 7.02, ph 5.79, US 1.86. One of those is wrong; the battery says which.
GUNFIRST    bots/_v164gunfirst     bots/_v146gunaxis     5408  97000
# ============================================================================
# s33 EXPERIMENTATION LOAD (Magnus: "Today is experimentation day, we focus on
# building bots and load them"). EVERY ARM BUILT TODAY IS QUEUED.
# ⛔ AND THE REASON THE PREVIOUSLY-REJECTED ONES ARE BACK: I rejected three arms
# on LOCAL DOSE CONTROLS AT n=6-32, and those controls are NOT RELIABLE. Our bot
# reseeds an UNSEEDED RNG every game (main.py:276, NOISE_ON=True), so --seed
# never controlled it. MEASURED TODAY: the SAME incumbent, SAME 6 seeds, SAME
# maps produced 4 gunners in one run and 17 in another. A gate whose control
# swings 4x cannot reject anything.
# ⇒ The n=5,408 shard IS the instrument. It reads game share off the engine's
# own winner field and never touches a counter I wrote. Let the battery decide.
GUNEARLY150 bots/_v166gunearly150 bots/_v146gunaxis     5408  98000
GUNEARLY60  bots/_v166gunearly60  bots/_v146gunaxis     5408  99000
GUNSUB      bots/_v165gunsub      bots/_v146gunaxis     5408 100000
GUNADD      bots/_v163gunadd      bots/_v146gunaxis     5408 101000
# ============================================================================
# THE LAUNCHER 2x2 — closing a design I had accidentally built three corners of.
# ⛔ MY "FERRY IS BAD" READING WAS WRONG AND THIS IS WHY. LAUNCH0 (+2.64pp) and
# EXILE0 (-4.68pp) look like they bracket a strongly negative ferry -- but they
# differ in MORE than the mechanism: LAUNCH0 NEVER BUILDS A LAUNCHER, so it also
# saves 20 Ti and +10% GLOBAL SCALE per launcher (~1.17/game). EXILE0 still PAYS
# for the launcher and merely stops exiling with it. So LAUNCH0's gain may be
# entirely the ECONOMY SAVING -- which is literally what QUEUE #24 exists to
# price -- and I conflated the launcher's PRICE with the ferry's VALUE.
#   cell       launcher  exile  ferry   arm
#   incumbent  yes       ON     ON      _v146gunaxis     50% by construction
#   EXILE0     yes       off    ON      _v162exile0      45.32% running
#   FERRY0     yes       ON     off     _v167ferry0      <- NEW, isolates FERRY
#   BOTH0      yes       off    off     _v168both0       <- NEW
#   LAUNCH0    NO        off    off     _v161launch0     52.64% running
# ⭐ BOTH0 vs LAUNCH0 is the pair that matters: identical MECHANISM (neither
# exiles nor ferries), differing ONLY in whether the launcher is BUILT AND PAID
# FOR. That difference IS the launcher premium, measured directly instead of
# inferred from a between-shard contrast.
# ⚠ Both live readings are interim (~30% of target) and are SEPARATE SHARDS with
# different seeds, so the 7.3pp gap between them is between-shard, not paired.
# ============================================================================
# ⭐ REORDERED s33: the LAUNCHER DECOMPOSITION runs NEXT, ahead of everything
# else queued. Magnus is holding a doctrine decision on it -- whether to drop
# the kidnap line -- and that decision needs FERRY0 and BOTH0, not another
# gunner arm. FERRY0 isolates the ferry at constant launcher cost; BOTH0 vs
# LAUNCH0 isolates the LAUNCHER PREMIUM at identical mechanism. LAUNCH2/3 then
# complete the dose curve 0/1/2/3 on launcher count.
# The four displaced arms (GUNSUB, GUNADD and the gunner family) are NOT
# cancelled -- they keep their place after these and lose nothing but order.
# ============================================================================
FERRY0      bots/_v167ferry0       bots/_v146gunaxis     5408 104000
BOTH0       bots/_v168both0        bots/_v146gunaxis     5408 105000
LAUNCH2     bots/_v160launch2     bots/_v146gunaxis     5408 102000
LAUNCH3     bots/_v160launch3     bots/_v146gunaxis     5408 103000
# --- LOKI-42 LAUNCHER DEFERRAL — the likely SHIPPABLE form of LAUNCH0 ---
# Built BEFORE the decomposition lands because it is the one arm that helps
# under the good outcome and is INERT under the bad one.
# The cost we pay is not the 20 Ti: each launcher adds +10% to the ONE GLOBAL
# scale factor, inflating EVERY subsequent build of EVERY type for the rest of
# the match. Bought at r10 that surcharge is levied on the WHOLE game; bought
# at r150 only on the tail. We build ~1.17/game against five of the six teams
# above us at essentially ZERO.
# ⇒ If LAUNCH0's gain is the PREMIUM, deferral captures most of it while KEEPING
# the exile mechanism EXILE0 says is worth ~+3pp. If LAUNCH0's gain is the FERRY,
# this reads null — a clean answer, not a wasted arm.
# ⚠ Not free: deferring also delays the ferry, and early exile is our cheapest
# home defence. Dose SWEPT because I do not know where that trade turns over.
# GUNADD and GUNSUB CANCELLED s33 to free two slots for FERRY0/BOTH0 — rows KEPT.
# Not a verdict on either: GUNADD was at 6% and GUNSUB at 12%, both far from any
# readable n. They are the two LOWEST-PRIOR arms on the board (GUNADD's site was
# refuted by grep — no build_gunner exists in raid.py at all; GUNSUB failed to
# hold count, which was its entire purpose) and Magnus is holding a DOCTRINE
# decision — whether to drop the kidnap line — on FERRY0/BOTH0 specifically.
# Cores spent on a 6%-complete low-prior arm while the decision arm sits queued
# is the wrong allocation, and the queue order was set before that decision existed.
# ============================================================================
# ⭐⭐ THE HEADROOM ARMS. The 2x2 prices the launcher at ~7.0pp TO OWN (BOTH0
# 45.79% vs LAUNCH0 52.77%, identical mechanism, differing ONLY in whether it
# is built and paid for) against mechanisms returning ~4.2pp. Deleting it wins
# +2.77pp -- but a launcher with FULL mechanism and ZERO ownership cost would
# read ~57%, so there is ~4pp of headroom ABOVE LAUNCH0 that DELETION CANNOT
# REACH. These three arms reach for it, two different ways:
#   LAUNCHLATE  -- DEFER the purchase, so the +10% scale is levied on the tail
#                  of the game instead of all of it.
#   LAUNCHRENT  -- REFUND it. CLAUDE.md:23: destruction REMOVES the scale
#                  contribution, so a launcher that throws and then self-
#                  destructs costs 20 Ti ONCE instead of +10% on every
#                  subsequent build of every type, forever.
# DOSE VERIFIED BY OUTCOME before queueing: the rent arm records 0.62 launcher
# DEATHS/game against the control's 0.00, so the self-destruct fires and
# LOKI6_LAUNCHER_RELEASE rebuilds -- the rent cycle runs with no new machinery.
# ⚠ Scale effect NOT measurable at n=8: the incumbent's own two sides read 397%
# and 354% on identical code, 43pp apart. That is the documented per-game
# reseeding, and it is why the battery decides this and not a local check.
# ⚠ Rent is not free -- 20 Ti re-paid per throw, ferry lost between cycles, a
# builder action per rebuild. Ferry measured at ~0 (FERRY0 49.84%), which is
# what makes the trade plausibly worth taking.
# ============================================================================
LAUNCHLATE80  bots/_v169launchlate80  bots/_v146gunaxis   5408 106000
LAUNCHLATE160 bots/_v169launchlate160 bots/_v146gunaxis   5408 107000
# ⛔⛔ LAUNCHRENT (LOKI-43) WITHDRAWN BEFORE FIRING — the trade is ~BREAK-EVEN,
# and a break-even plank is not worth 5,408 games. Research flagged it; I
# attacked the quantity they named and BOTH OF US WERE WRONG, IN OPPOSITE
# DIRECTIONS, and the answer landed in the middle:
#   their 'base cost after the first throw' = 169 Ti   -> mine measures 260 Ti
#         (so the SAVING is 26 Ti, not 17 -- 54% BETTER for the plank)
#   their rent term assumed n=5 cycles/game            -> I MEASURED 0.62
#         (the self-destruct fires only on the EXILE branch and the rebuild
#          needs the Core to lose sight of a launcher, so cycles are bounded
#          by geometry, not by throw count -- exactly the side lane's caveat)
#   => at the measured 0.62 cycles: rent 25-50 Ti against a 26 Ti saving,
#      NET +1 to -24 Ti depending on scale. NOT their 14x shortfall -- but not
#      a gain either, and ~24 Ti on a ~568 Ti build-out is well under what a
#      +-1.33pp band can resolve.
# THE MECHANISM IS REAL (0.62 launcher deaths/game vs the control's 0.00, so it
# fires) AND THE MAGNITUDE IS ~ZERO. Same family as E-22.3, which died on the
# same shape: mechanism confirmed, magnitude absent. The two now cite each other.
# ⭐ AND THE REASON IT IS NEAR-ZERO IS THE REASON IT LOOKED GOOD: the rent cycle
# fires rarely, which caps the cost -- and caps the benefit with it.
# ============================================================================
# ⭐⭐ X3R0 SHIPPED v115 AT 08:57Z AND IT HOLDS THE SLOT. Magnus: 'run out v114
# against it and see if we beat it.' Downloaded off the platform and diffed:
# v115 = v114 + SIX LINES in main.py -- an opening ammo pre-buy, converting Ti
# to ammo up to 17 at rnd 0. Ammo starts at 0 with NO passive income and the
# ONLY source is convert_ammo, so this is 'the first turret we build can
# actually fire'. doctrine/eco/raid are byte-identical to v114.
# ⛔ AND IT MOVES EVERY CONTROL WE ARE RUNNING. PROGRAMME.md: 'when a ship
# lands, every control moves with it -- a stale control measures the wrong
# contrast and still reads as a valid result.' All 16 arms today are measured
# against bots/_v146gunaxis = v114 = the PREVIOUS incumbent. They are still
# internally valid (they answer 'better than v114?') but they no longer answer
# 'better than what is LIVE?' until this shard says how big the v114->v115 step is.
# THIS SHARD IS THE BRIDGE: it prices the gap so every other arm can be read
# against the live holder without re-running any of them.
AMMO115     bots/_x3r0_v115        bots/_v146gunaxis     5408 110000
# --- THE STACKS (Magnus): our launcher findings + x3r0's ammo pre-buy ---
# CONTROL IS bots/_x3r0_v115, NOT v114 -- deliberately. Both stacks carry the
# ammo pre-buy verbatim, so they differ from the control in the LAUNCHER
# TREATMENT AND NOTHING ELSE. Verified by diff: the only non-comment deltas are
# the launcher gates. That makes these the first arms today whose control is a
# bot that has actually held the ladder slot.
# WHAT EACH ANSWERS, given the launcher decomposition at n=5,408 vs v114:
#   LAUNCH0 52.77% · LAUNCHLATE160 51.40% · ferry -0.15pp · exile +2.98pp ·
#   launcher premium +6.34pp to own · dose curve 0>1>2>3 monotone.
# ZEROAMMO  = does DELETING the launcher still pay once ammo is held constant?
# LATE160AMMO = does DEFERRING it still pay on the same footing?
# ⭐ AND TOGETHER THEY ANSWER THE SHIP QUESTION DIRECTLY: v116 (live) IS
# LAUNCHLATE160 WITHOUT the ammo pre-buy, so LATE160AMMO is v116 + the thing
# v116 accidentally reverted. If it beats the control, the fix is additive.
ZEROAMMO    bots/_v171launch0ammo  bots/_x3r0_v115       5408 111000
LATE160AMMO bots/_v171late160ammo  bots/_x3r0_v115       5408 112000
# --- DELETE vs DEFER, HEAD TO HEAD. The decision arm. ---
# We currently have LAUNCH0 52.77% and LAUNCHLATE160 51.40%, each measured
# against v114 in SEPARATE shards with different seed blocks. The 1.37pp gap
# between them therefore carries BOTH shards' noise: SE ~0.96pp, z ~1.43, NOT
# significant. Inferring "delete beats defer" from two independent contrasts is
# exactly the between-shard comparison the GUNBORDER/EXILE0 caveat warned about.
# A DIRECT head-to-head puts both arms in the SAME games and resolves the
# contrast itself at +-1.33pp.
# ⭐ RUN ON THE AMMO STACKS, NOT THE BARE ARMS: both carry x3r0's pre-buy, so
# ammo is held constant and the ONLY difference is delete-vs-defer. That also
# makes the winner directly shippable -- it already contains the change v116
# accidentally reverted.
# READ: >51.33% means DELETE beats DEFER; <48.67% means DEFER beats DELETE;
# inside the band means they are indistinguishable and the choice falls to
# DOCTRINE rather than measurement -- which is Magnus's call, since deletion
# removes the kidnap weapon and deferral keeps it.
DELVSDEF    bots/_v171launch0ammo  bots/_v171late160ammo  5408 113000
# ⛔⛔ DELVSDEF IS ASYMMETRIC AND THE READ MUST SAY SO BEFORE THE DATA.
# Flagged by the side lane, measured by research, REPRODUCED BY ME to the digit
# (n=3,913 inserts over 1,915 attributable games): OUR ferry delivers EARLY --
# median INSERT r82, median FIRST insert r27, and 74.6% of all inserts land
# BEFORE r160.
# ⇒ LAUNCH0AMMO HAS NO LAUNCHER, SO IT HAS NO FERRY, SO IT ATTACKS LATER than
# our already-late chassis. And deferral's ENTIRE exposure is "undefended until
# r160 against an opponent that attacks EARLY". THE H2H FIXTURE CANNOT EXERCISE
# DEFERRAL'S FAILURE MODE -- it removes exactly the traffic that would arrive
# inside the exposure window.
# THEREFORE THE THREE OUTCOMES ARE NOT EQUALLY STRONG:
#   >51.33% DELETE WINS  -> STRONG. It won DESPITE a fixture tilted toward its
#                           opponent. Take it.
#   <48.67% DEFER WINS   -> WEAK. Won on the one fixture that cannot exercise
#                           its failure mode. NOT evidence that the undefended
#                           opening survives real early aggression -- that is
#                           what the live leg vs The Bisons was for, and that
#                           leg passed (48.0% share, 40.0% early deaths vs a
#                           44.8% baseline), which is separate evidence.
#   inside band          -> indistinguishable ON A FIXTURE FAVOURABLE TO DEFER,
#                           which is strictly weaker than plain indistinguishable.
# GENERAL FORM: our own trees are the wrong fixture for a mechanism whose
# trigger is OPPONENT BEHAVIOUR WE DO NOT EXHIBIT -- the same reason
# _v146gunaxis could not measure the crash channel. The H2H answers "which beats
# which on OUR chassis"; the ship question is "which does better against the
# FIELD", and those coincide only if the ordering is transitive across opponent
# types. For a knob whose value depends on ENEMY TIMING, that is exactly what is
# not guaranteed.
# --- LOKI-44 MAGAZINE BURST: same titanium, spent when the gun exists ---
# FOUND BY DECODING 25 LIVE GAMES OF v116, not by reading code:
#   our opening is r0-4 builders · r5 harvester · r7 conveyor · r8 2nd harvester
#   (opens the ammo gate) · r13 FIRST SENTINEL, FORWARD in 14/25 · r18 barriers ·
#   r39 gunner in only 9/25 · r246 launcher in only 2/25 · splitters NEVER.
# ⇒ THE FIRST THING THAT CAN FIRE IS A FORWARD SENTINEL AT r13, AND A SENTINEL
#   IS 10 AMMO A SHOT. x3r0's r0 pre-buy of 17 is AMMO_FLOOR+1 -- the same 16 the
#   harv>=2 gate gives us at median r8, moved earlier, plus one. It funds ONE
#   VOLLEY and strands 7.
# The incumbent ALREADY knows the right magazine: `if fwd_guns: ammo_target =
# max(..., min(120, 40+20*fwd_guns))` = 60 for one forward gun. What it lacks is
# FILL RATE: `amt = min(16, ...)` dribbles 16/turn, and fwd_guns is a STORE read
# buffered one round -- sentinel lands r13, seen r14, magazine full ~r17. FOUR
# ROUNDS of a forward sentinel in the enemy half holding one shot.
# THE CHANGE: raise the per-turn cap ONLY when a forward gun exists. No earlier
# spend, no larger target, and it cannot exceed what ammo_target already asks
# for. Dose swept 32/64 because I do not know where the ti_floor trade bites.
# CONTROL is _v171late160ammo -- v116 + the ammo pre-buy it reverted -- so this
# measures the TIMING fix alone, on the chassis closest to what is live.
BURST64     bots/_v172burst64      bots/_v171late160ammo  5408 114000
BURST32     bots/_v172burst32      bots/_v171late160ammo  5408 115000
# ⛔⛔ LOKI-44 BURST64/BURST32 CANCELLED BEFORE READING — D27: the expected effect
# is below the band, so a null would be uninformative BY CONSTRUCTION.
# Research raised it from the archive (1.7% of enemy builder kills fall in r13-20
# over 5,878 games; 96.3% of games contain NO such kill). I verified on my own
# 50 decoded LIVE games: ONE enemy builder died in r13-20, out of 28 total
# enemy builder kills = 3.6%. Same order, both tiny.
# ⇒ The plank buys ~2 extra sentinel volleys (~36 damage) into a window that,
# across 50 live games, contained ONE killable target. Expected effect well
# under 0.1pp against a +-1.33pp band at n=5,408.
# ⭐ AND I CHECKED THE ONE WAY IT COULD HAVE BEEN BIGGER, BECAUSE BOTH OF US HAD
# NARROWED IT: the cap change applies whenever fwd_guns exists, not only at the
# first fill -- so does it also speed REFILLS all game? No. A sentinel burns
# ~3.3 ammo/round in steady state (10 per shot, reload 2) against a 16/turn cap,
# so the cap NEVER binds after the initial 0->60 fill. The effect really is
# confined to r13-17.
# THE MECHANISM IS SOUND AND THE WINDOW IS EMPTY. Third plank today killed by
# arithmetic before spending games (LOKI-43 rent, the launcher-cap arms, this).
# --- LOKI-44 RE-QUEUED (Magnus: "uncancel them, we can cancel them later") ---
# Fresh shard names because corefill holds a CANCELLED shard cancelled and
# launches once; removing the marker alone does not restart it (same as SURCH30).
# ⛔ AND THE REASON I CANCELLED IT WAS WRONG TWICE, WHICH IS WHY IT IS BACK:
#  (1) I first killed it on "the r13-20 window has nothing to hit", measured as
#      ENEMY BUILDER DEATHS. Wrong outcome entirely: `raid.py:415 tiles =
#      core_tiles(E)` -- the forward sentinel is aimed at the CORE, and core
#      damage produces no death event until the core falls. Magnus caught it.
#  (2) I then killed it on "their core heals at ~16 HP/round against our
#      sentinel's 6, so the damage is refunded in 2.2 rounds". Also wrong, and
#      Magnus caught this one too: A BUILDER GETS ONE ACTION PER TURN AND ACTING
#      IS MUTUALLY EXCLUSIVE WITH MOVING. So a builder that heals cannot build,
#      attack, or move. Their 4 healers cost them 4 Ti AND 4 BUILDER-ACTIONS per
#      round, and they are pinned in place. We spend ~5 Ti/round of ammo to deny
#      that. THE ABSORPTION IS THE WEAPON, not the mechanism failing.
# ⇒ If the sentinel's job is PINNING rather than killing, filling its magazine
#   sooner means pinning sooner, and the value is NOT bounded by whether 36
#   damage survives the heal line.
# ⚠ STILL NOT THE RIGHT INSTRUMENT, AND SAYING SO BEFORE THE DATA: game share at
#   n=5,408 resolves 1.33pp. The metric that would actually test this is ENEMY
#   BUILDER-ACTIONS DENIED per round while our forward sentinel is firing, which
#   the replays can answer and a win-rate shard cannot. Read a null here as
#   "under 1.33pp", never as "the pin does not work".
BURST64B    bots/_v172burst64      bots/_v171late160ammo  5408 116000
BURST32B    bots/_v172burst32      bots/_v171late160ammo  5408 117000
# --- TWO ARMS FROM THE LIVE REPLAY WORK. Magnus: "don't assume anything, measure" ---
# HEALERFIRST (LOKI-46): a FORWARD turret shoots enemy BUILDERS before the CORE.
#   MEASURED, 75 live games: their core carries a median 4 builders (up to
#   16 HP/round repair) against our sentinel's 6 HP/round -- so core fire is net
#   negative. Those same builders do 2 dmg each to our 40 HP sentinel; 41% of
#   our sentinels DIE, median life 12 rounds. Their builder replacement latency
#   after a death is a median 12 ROUNDS, capped at 1 spawn per core turn.
#   DOSE VERIFIED, 12 paired games: 30 enemy builders killed vs the control's 19.
#   ⚠ AND THE THING I PREDICTED THAT DID NOT HAPPEN: our own sentinel losses went
#   UP, 18 vs 13. I expected killing the attackers to protect the gun. At n=12
#   that is noise-or-real and the battery decides -- recorded because predicting
#   it and then finding the opposite is exactly what should not be quietly dropped.
#   ⚠ NOT ATTRITION: destruction REFUNDS their scale, so a kill makes their next
#   builder cheaper. This buys a repeatable ~12-round tempo window, not a grind.
# NOAPPROACH (LOKI-47): delete the approach sentinel entirely.
#   The same arithmetic says a gun firing before the collar is sealed makes ZERO
#   net progress, at 30 Ti and +20% global scale. Rather than assert that, this
#   measures it. If the approach sentinel is worth ~0, deleting it should be
#   neutral-to-positive; if it is worth something, this reads clearly negative.
# ⛔ BOTH REST ON AN UNVERIFIED PREMISE AND IT IS NAMED HERE: I have never
#   OBSERVED the enemy healing. events.tsv carries BUILD and DEATH only -- there
#   is no heal verb -- so "16 HP/round" is CAPACITY (4 builders x 4 HP), not
#   behaviour. The builder-damage half (2 dmg/round each vs our sentinel) IS
#   directly observable and does not depend on it.
HEALERFIRST bots/_v174healerfirst   bots/_v171late160ammo  5408 118000
NOAPPROACH  bots/_v175noapproach    bots/_v171late160ammo  5408 119000
# --- LOKI-48 IDLE CULL: self-destruct a builder idle 12 consecutive rounds ---
# MEASURED, our scale over 75 live games: we run at ~377% and BUILDER BOTS ARE
# 44% OF IT (6.2/game x +20pp = 123pp, more than every turret combined).
# Destruction REFUNDS the contribution, and 25.76% of our builder-rounds are
# idle with both cooldowns at zero -- the same share in WINS, so it is what our
# bot always does, not a losing-position artefact.
# DOSE VERIFIED, 8 paired games: 6.8 builders culled/game vs the control's 1.6,
# and END SCALE 222% vs 318% -- a 96pp reduction, every later build ~25% cheaper.
# ⚠ AND IT REBUILDS: 8.4 builders built vs 6.9, so the cull is partly a
#   treadmill and the titanium cost is real. That is what the battery prices.
# ⛔ NOBODY IN THE FIELD DOES THIS: 25 live games carry 86 undamaged GUNNER
#   removals (turret walking, routine) against TWO undamaged builder_bot
#   removals. Untested by anyone -- novel lever, or an idea already rejected.
IDLECULL    bots/_v176idlecull     bots/_v171late160ammo  5408 120000
# ⛔⛔ DELVSDEF CARRIES TWO INDEPENDENT BIASES AND THEY NOW POINT THE SAME WAY.
# Recorded BEFORE the shard reads out (it is at 83% as this is written).
# BIAS 1 — FIXTURE: _v171launch0ammo has NO LAUNCHER, so no ferry, so it attacks
#   LATER than our already-late chassis. 74.6% of our ferry INSERTs land before
#   r160 (n=3,913 over 1,915 games, first at median r27 -- reproduced by me to
#   the digit). Deferral's ENTIRE exposure is "undefended until r160 against an
#   opponent that attacks EARLY". THE FIXTURE CANNOT EXERCISE IT.
# BIAS 2 — STRUCTURE, and it is new: **v116 IS THE DEFERRAL ARM.** So this is not
#   challenger-vs-challenger any more, it is CHALLENGER vs THE SHIPPED BOT.
#   DELETE-wins implies a SHIP; DEFER-wins implies INACTION.
# ⇒ THE COMPOUNDING IS THE POINT: a DEFER win would be simultaneously the
#   WEAKER-EVIDENCED branch AND the STATUS-QUO-PRESERVING one -- the combination
#   that attracts the least scrutiny and deserves the most. The two biases were
#   independent while both arms were challengers. They are not now.
# THE READ, asymmetric, declared before the data:
#   >51.33% DELETE WINS -> STRONG. Won DESPITE a fixture tilted toward its
#           opponent, and it licenses replacing the holder.
#   <48.67% DEFER WINS  -> WEAK TWICE OVER. Won on the one fixture that cannot
#           exercise its failure mode, AND it is the incumbent defending. NOT
#           evidence the undefended opening survives early aggression -- the
#           live Bisons leg is the separate evidence there (48.0% share, 40.0%
#           early deaths vs a 44.8% baseline).
#   inside band -> indistinguishable ON A FIXTURE FAVOURABLE TO DEFER, against
#           an arm that is already live. Weaker than plain indistinguishable.
# ⭐ AND THE REPLAYS PREDICT INSIDE-BAND: v116 built a launcher in 2 of 50 live
#   games, so deferral IS deletion in practice and the two arms are near-identical
#   bots. An inside-band read is therefore the EXPECTED result confirming a
#   mechanism, not an inconclusive one.
# ============================================================================
# --- appended s34 BOOT — THE SHIP GATE THAT DOES NOT EXIST YET ---
# ⛔ THE s33 SUCCESSOR WARNING, ITEM 1, VERBATIM: "No shard is controlled against
#   the live holder. 29 vs v114, 8 vs our stack, 2 vs v115, ZERO vs v116."
# LATE160AMMO reads 53.27% and ZEROAMMO 52.54% -- BOTH AGAINST v115, x3r0's bot.
# v116 IS WHAT WE WOULD BE REPLACING and it has never been the control for either.
# The transitive chain (cand > v115 ~ v114 < v116) is THREE hops on three
# different fixtures; a ship decision does not get to be transitive.
# READ, declared before the data:
#   >51.33%  candidate beats the live holder -> the ammo repair is ADDITIVE and
#            v116's silent revert of x3r0's pre-buy is a real regression to undo.
#   <48.67%  the holder wins -> the v115-based numbers are a BASELINE artefact,
#            not a candidate effect, and NOTHING ships on them.
#   inside   indistinguishable from the bot already live => NO SHIP. An arm that
#            cannot separate from the holder has no claim on the slot, whatever
#            it did against a bot nobody is running.
SHIPGATE160 bots/_v171late160ammo  bots/_v169launchlate160  5408 121000
SHIPGATE0   bots/_v171launch0ammo  bots/_v169launchlate160  5408 122000
# ⛔ THE GATE ABOVE WAS APPENDED WITHOUT ITS CALIBRATION CELL, WHICH IS THIS
# FILE'S OWN NON-NEGOTIABLE (:8-10): "Without NULL and NEG the other verdicts
# cannot distinguish 'no effect' from 'no power'." Flagged by the side lane; the
# omission is mine. NULL114/NEG114 are both controlled on `_v146gunaxis`, so the
# harness that adjudicates the SHIP is calibrated on a DIFFERENT PAIR OF TREES
# than the one it adjudicates -- and the gate shards ADD A THIRD control tree
# while the calibration stays on the first.
# WHY IT BITES SPECIFICALLY: `overnight_read.py:340` assigns `centre` only inside
# the `abs(z) > 3` branch, and the per-shard verdict block PRINTS BEFORE the
# calibration block runs. So the re-centring defect's dormancy is CONDITIONAL on
# NULL114's z staying inside +-3 -- and NULL114 cannot observe the v116-contrast
# fixture at all. A clean bill from a control that cannot see the fixture.
# `_v169null` is a byte-identical copy of `_v169launchlate160`, md5-verified on
# all four files, `diff -rq` empty -- the same construction as `_v146null`.
SHIPGATENULL bots/_v169null       bots/_v169launchlate160  5408 123000
# --- NEG169: the gate's NEGATIVE control, and it does a SECOND job ---
# Side lane, correctly: all three uncalibrated contrasts lack a NEG cell, and
# NEG is what licenses reading NO-INFORMATION as "no effect" rather than "no
# power" -- this file's own stated reason the calibration cells are
# non-negotiable.
# ⭐ AND IT REPAIRS A CLAIM I MADE TO MAGNUS TODAY. I nearly told him the collar
# is worth 13.7pp, citing NEG114 (`_v140noseal`, 36.32%). It is NOT a clean
# single-flag ablation: `_v140noseal` ALSO lacks LOKI_GUNAXIS_PENALTY -- the
# LOKI-25 plank v114 shipped on -- differing by 7 doctrine lines AND 26 in
# raid.py. So 13.7pp was an UPPER BOUND on the collar, never its value, and the
# repo has never had a clean measurement of the single most load-bearing thing
# in the bot.
# `_v169noseal` is `_v169launchlate160` (THE LIVE HOLDER) with ONE line changed:
# `LOKI_BARRIER_SEAL_ON = True -> False`. Verified: `diff -rq` reports one file,
# and the diff is one line. Clean attribution, on the chassis we actually ship.
# ⇒ ONE SHARD, TWO JOBS: the NEG cell the gate contrast lacks, and the first
# honest number for what the collar is worth.
# PRE-DECLARED: this MUST read well below 50% or the screen has lost its power
# on this contrast -- and if it reads NEAR 50%, that is not "the collar is free",
# it is "this fixture cannot detect a known-bad arm", which invalidates every
# NO-INFORMATION verdict on the same contrast INCLUDING the ship gate's.
NEG169      bots/_v169noseal       bots/_v169launchlate160  5408 124000
# --- LOKI-49 L4 TRUNK REPAIR --- Magnus's replay observation, s34 ---
# MEASURED: we repair 6.8% of our cut conveyors against a FIELD 40.5% and our own
# OPPONENTS 50.3% (n=885 eligible deaths, field control n=27,871; matcher driven
# to the other verdict four ways -- CTRL-A 0.00%/885, CTRL-B 0.79% on a shuffled
# game, positive control verified on raw events rows).
# ⛔ AND THE ARM'S OWN BUILD FOUND THE DEFECT IS WORSE THAN THAT: `PAVE_TRAIL_ON
# = False` (doctrine.py:528, turned off by LOKI-13), so the "incidental repair"
# the doctrine describes DOES NOT EXIST IN THIS TREE. Repair here is structurally
# zero, not merely low.
# ⭐⭐ AND THE BIGGEST SURPRISE, FOUND BY COUNTING NOT BY READING: only 1 of the
# first 55 repaired tiles had EVER held a conveyor. The belt half is mostly
# repairing OUR OWN DEAD HEADS -- chains `_build_next_link` abandoned mid-walk
# and never revisits, because it pops its queue as it lays it. ⇒ THE L4 DEFECT
# HAS TWO HALVES AND ONE IS SELF-INFLICTED. This arm fixes both and cannot
# attribute between them; that is a known limit, declared before it runs.
# DOSE VERIFIED: treatment 40 repairs / 40 games (24 of 40 games >=1); control
# 0 / 40. Flag-off reproduces the parent 20/20 on gate.py's five keys with
# NOISE_ON disabled, and the mutation control differs in 2/5 with the flag ON.
# 0 tracebacks in 80 games, mutation-tested with a canary raise (5 tracebacks).
# ⚠ CPU HEADROOM IS UNVERIFIED: `get_cpu_time_elapsed()` returned 0 on all 6,515
# local calls and the replay `tled` flag is 0 even at --tle 1, so the local CPU
# instrument is DEAD. Analytic bound ~<=40 extra getters/turn, eco path only.
# ⇒ IF THIS ARM IS EVER SHIPPED IT NEEDS A PLATFORM CHASSIS GATE FIRST.
L4REPAIR    bots/_v177l4repair     bots/_v169launchlate160  5408 125000
# --- GUNAX0: DOES THE PLANK WE SHIPPED v114 ON ACTUALLY DO ANYTHING? ---
# ⛔ THE JUSTIFICATION IN THE SHIPPED TREE IS STALE. `raid.py:508-517` says "92%
# of our FORWARD builder deaths are enemy gunners... we are dying almost entirely
# to the AVOIDABLE one." Research reproduced 92.0% ALL-TIME to the decimal -- and
# on the SHIPPED trees it is 64.8% any-gunner / 31.2% gunner-ONLY, against 68.4%
# any-SENTINEL. In the bot on the ladder, SENTINELS COVER MORE FORWARD DEATHS
# THAN GUNNERS DO.
# ⭐ I VERIFIED THE ERA SHIFT INDEPENDENTLY, by a different measurement (enemy
# TURRET MIX, not death coverage): enemy sentinel share of their turret builds
# went 21.4% (2,540 older games) -> 48.8% (325 shipped games), and OUR forward
# builder deaths fell 2.17/game -> 0.73/game. Two instruments, same direction.
# ⚠ NOT A REFUTATION OF LOKI-25 -- suppressing gunner deaths mechanically RAISES
# the sentinel share, so this is equally consistent with the plank WORKING. The
# archive cannot attribute it: the adjacent contrast runs BACKWARDS (v112 0.43
# gunner-covered fwd deaths/game vs v114 0.59) and v114 differs from v112 by more
# than this flag.
# ⇒ ONLY AN ABLATION ANSWERS IT. `_v183gunaxis0` is the LIVE HOLDER with ONE line
# changed: LOKI_GUNAXIS_PENALTY 8 -> 0. Verified: diff -rq names one file, the
# diff is one line. At 0 the gun_axis penalty term is dead and station scoring
# reverts to pre-LOKI-25 behaviour.
# PRE-DECLARED, before the shard runs:
#   <48.67%  the flag is LOAD-BEARING -- removing it costs us, v114's promotion
#            plank is doing work, and the gunner term stays.
#   >51.33%  removing it HELPS -- we shipped a plank that hurts, and the whole
#            gun_axis machinery should be re-pointed (QUEUE #30, sentinels).
#   inside   the flag is worth <1.33pp either way ON THIS FIXTURE. NOT "it does
#            nothing" -- our own control builds few forward gunners, so the local
#            fixture UNDER-REPRESENTS the rays the penalty exists to dodge.
GUNAX0      bots/_v183gunaxis0     bots/_v169launchlate160  5408 126000
# ============================================================================
# --- OVERNIGHT BLOCK, appended s34 ~20:0xZ. Magnus: run until ~08:00, no end
#     time, keep building. SHIPGATE160/SHIPGATE0 CANCELLED at 56% -- 49.44% and
#     49.19% at +-1.78, inside the pre-registered band, which IS the verdict
#     (NO SHIP). NOAPPROACH (18.6%) and IDLECULL (33.5%) cancelled as decided
#     negatives. Rows kept for all four.
# ⛔ EVERY ARM BELOW IS CONTROLLED ON `_v169launchlate160`, WHICH IS THE HOLDER
#     AGAIN AFTER MAGNUS ROLLED BACK v120 AT ~19:40Z. SHIPGATENULL (already
#     running) is the null cell for this contrast; NEG169 is its negative.
#     Do not read an inside-band result as "no effect" until NEG169 has n.
# ============================================================================
# SCREEN -- the approach doctrine we have ZERO of and ph/not adgato run at
# d^2 17-25. Dosed 2.57/game, >=1 in 92/96 games, did NOT cannibalise the collar
# (6.01 vs 5.20 pooled) or the forward sentinel (1.67 vs 1.66).
# READ: >51.33% the doctrine transfers · <48.67% approach barriers cost us and
# the collar-only doctrine is CORRECT, which is worth knowing · inside = under
# 1.33pp on this fixture, and NOTE the fixture is our own bot, which also has no
# approach screen to be obstructed by.
SCREEN      bots/_v182screen       bots/_v169launchlate160  5408 130000
# GUNBLOCK -- barrier on a visible enemy gunner's ray. Doses only 0.20/game
# LOCALLY and that is a FLOOR, not an estimate: we build 1.26 gunners/game
# against Leviathan 13.86 / Erebus 8.35 / Lunds Stallions 10.56, so a self-play
# fixture under-represents the rays this arm exists to block by 4-11x.
# ⛔ AN INSIDE-BAND READ HERE IS NEARLY UNINFORMATIVE BY CONSTRUCTION. It is
# queued to price the COST (does carrying the logic hurt?), not the benefit.
GUNBLOCK    bots/_v181gunblock     bots/_v169launchlate160  5408 131000
# MINHARV1 -- the CLEAN forward-timing arm. LOKI_FWD_MIN_HARV 2 -> 1, one line.
# Dose: first forward sentinel r27 -> r22 (p=0.097, inside the same-bot band, so
# a WEAK dose). Field deficit is r33 vs r25, so this is the right direction.
# ⭐ PREFERRED OVER _v179rushon, which is a RE-RUN tested twice with OPPOSITE
# signs AND has a second consumer at main.py:324 (sends seats (0,1) on the raid),
# making any delta unattributable between the gate and the extra body.
MINHARV1    bots/_v180minharv1     bots/_v169launchlate160  5408 132000
# L4 TRUNK REPAIR is already running (13%). GUNAX0 already queued.
# --- OVERNIGHT, second block ~20:4xZ. Two arms that have been stable for hours.
# SALT vs THE HOLDER. The live leg (25 games) killed it on kill round -- 13 kills
# at median r179 vs a pooled baseline of 129, Mann-Whitney p=0.008. That is a
# STRONG signal on a SMALL n, and the local A/A floor measured tonight is +-25
# rounds at n<=96. A 5,408-game battery is the only thing that turns "probably
# ~50 rounds" into a number. ⭐ AND IT IS THE CONTROL FOR `_v187saltidle`: if the
# idle-gate arm is to be read as "same denial, less tempo cost", the ungated
# arm's cost must be measured on the SAME fixture, not carried over from a
# 25-game live leg.
# READ: this arm is EXPECTED to lose. A clean OUTSIDE-BELOW at n=5408 with a
# measured kill-round delta is the deliverable, not a surprise.
SALT        bots/_v178salt         bots/_v169launchlate160  5408 133000
# RUSHON -- LOKI2_RUSH_ON False->True. ⛔ QUEUED WITH A KNOWN DEFECT, STATED:
# (a) it is a RE-RUN -- the same one-line flip was tested twice with OPPOSITE
#     signs (LOKI-4 arena -15.6/-18.9pp on orizon/cad, the fixtures CLAUDE.md
#     names as lying; LOKI-11 live-unrated +16.0pp core-kill share);
# (b) the flag has a SECOND consumer at main.py:324 (sends seats (0,1) on the
#     raid, not just seat 0), so ANY delta is unattributable between "the
#     harvester/bank gate lifted" and "a second body left home".
# ⇒ IT IS QUEUED FOR THE DOSE AND THE COST, NOT FOR ATTRIBUTION. `MINHARV1` is
# the clean single-mechanism arm on the same axis and is already running.
# A result here NEVER licenses a sentence about the rush gate on its own.
RUSHON      bots/_v179rushon       bots/_v169launchlate160  5408 134000
# ============================================================================
# --- OVERNIGHT, third block ~20:4xZ. FIVE MORE. ---
# ⛔ THE `_f` SUFFIX IS A FROZEN SNAPSHOT, NOT A DIFFERENT ARM. The build agent
# was still live when these were queued (last edit 6 min prior), so each tree was
# copied and md5-verified identical at freeze time. A late edit to the original
# therefore cannot contaminate 5,408 games. The agent's report will name the
# UNSUFFIXED trees; they are the same code.
#   _v184screen4_f        0858bb44e97cfdd0bb358553d63acd57
#   _v185collarvol_f      8f2a92dd8a53aabe055e2d4b1766a3c9
#   _v186gunblocknoshield_f 5d690fe431c6cb6a94a148eee053d41f
#   _v187saltidle_f       945a6e91c3941d85f4d29c03acd410b1
# ============================================================================
# SCREEN4 -- the approach doctrine at DOUBLE dose (cap 2->4, gap 10->5 rounds).
# SCREEN itself is already running, so this is a two-point DOSE CURVE on the one
# barrier doctrine we have zero of and ph/not adgato run. Monotone in the dose is
# the strongest shape either verdict can take: if 2.57/g and ~5/g both help, the
# doctrine transfers; if both hurt, collar-only is CORRECT and that is worth as
# much. ⚠ Read ONLY beside SCREEN -- alone it is one point.
SCREEN4     bots/_v184screen4_f    bots/_v169launchlate160  5408 135000
# COLLARVOL -- collar volume, same placement, lower bank floor. We spend 34 Ti/game
# on barriers; Clankers spends 110 on the SAME median titanium (880) at the same
# 7-9 Ti unit cost. This asks whether our under-spend is a DEFECT or a CHOICE.
# ⚠ AND VOLUME IS NOT OBVIOUSLY GOOD: sporks tops the ladder at 3.02 barriers/game.
# Every barrier is +1% scale forever, and scale is what prices our launcher out.
COLLARVOL   bots/_v185collarvol_f  bots/_v169launchlate160  5408 136000
# GBNOSHIELD -- gunblock WITHOUT the shield clause. 4.2x the local dose (0.20 ->
# 0.83/game) at the cost of sometimes placing cover in front of nothing.
# ⭐ THIS IS THE ARM THAT MATTERS OF THE PAIR: the live v121 leg placed ZERO ray
# barriers in 25 games against 4.68 enemy gunners/game, so the shield clause is
# the prime suspect. If the cheap high-dose version measures the same or better,
# the clever logic is unnecessary complexity.
GBNOSHIELD  bots/_v186gunblocknoshield_f bots/_v169launchlate160 5408 137000
# SALTIDLE -- salt gated on the raider having NO better action. The revival test
# for a plank killed tonight on TEMPO, not on mechanism (live: 13 kills at median
# r179 vs pooled 129, p=0.008; mechanism 20/20 corpse-salts at median latency 1).
# ⭐ READ IT BESIDE `SALT`, WHICH IS QUEUED ON THE SAME FIXTURE FOR EXACTLY THIS:
#   SALTIDLE kill round ~= parent AND salt events > 0  -> REVIVED
#   SALTIDLE kill round ~= SALT (regressed)            -> the idle-gate hypothesis
#                                                         is WRONG and salt is dead
#   SALTIDLE salt events ~= 0                          -> the gate never opens;
#                                                         says nothing about salt
SALTIDLE    bots/_v187saltidle_f   bots/_v169launchlate160  5408 138000
# ⭐ V120 -- x3r0's ship, PROPERLY. My panel read 12% (3/25) against four
# baselines at 40-45% on identical maps (p~0.012) -- but that is n=25 and its
# RATED record was 5/10 over two matches. A teammate's bot deserves better than a
# 25-game verdict, and the tree is already local. This is the cheapest honest
# answer available and it costs nothing but cores.
# READ: <48.67% confirms the panel and the rollback · >51.33% my panel was wrong
# and v120 was rolled back on noise · inside = the 12% was a small-n artefact.
V120        bots/_x3r0_v120        bots/_v169launchlate160  5408 139000
# ============================================================================
# --- OVERNIGHT, fourth block ~20:5xZ. Research's ranked list, acted on. ---
# CUT `RUSHON` -- a second consumer at main.py:324 makes it unreadable AT ANY n.
#   ⭐ CONFOUNDING IS NOT A POWER PROBLEM, so no shard size fixes it. Right call.
# CUT `GUNBLOCK` at 793 rows (kept) -- `GBNOSHIELD` stays. The live v121 leg
#   placed ZERO ray barriers in 25 games against 4.68 enemy gunners/game, so the
#   SHIELD CLAUSE is the prime suspect and the no-shield arm is the informative
#   one. Running a plank twice ALONGSIDE its own precondition (`GUNAX0`, same
#   batch) is the expensive ordering.
# NOT QUEUED, on research's preconditions:
#   `#30` station-scorer sentinel term -- separable from `#32` but its population
#     is 7x SMALLER (sentinel-only builder deaths 0.32/game vs our sentinel deaths
#     2.25/game, same 30 Ti and +20% scale, so directly comparable). Follow-up.
#   `#21` gunner count -- a 0.2 gap (sporks 0.6 vs our 0.4) is not a consensus,
#     and the deficit is FORWARD, which `MINHARV1` already tests.
# ============================================================================
# LAUNCHER_RESERVE DOSE PAIR -- best effort-to-finding ratio on the board, and
# unqueued until now. ONE CONSTANT each, verified one changed line.
# TWO INDEPENDENT SURFACES CONVERGE: the gate is
# `_eco_spendable(get_launcher_cost() + LAUNCHER_RESERVE)` = floor(scale*20)+80
# ~= 120-160 Ti, our bank at the end of the r150-200 band is MEDIAN 54 Ti and
# clears 120 in only 12.4% of games -- against MY live decode of 3 of 55 games
# building a launcher (5.5%). ⇒ THE LAUNCHER IS PRICED OUT, NOT DEFERRED, and
# `LAUNCHER_MIN_RND` (v116's whole plank) is close to a no-op.
# ⭐ RESOLUTION DECLARED BEFORE THE SHARDS, per research's row: at ~1.17
# launchers/game a dose that does NOT move launchers-per-game by >=1 is
# UNINTERPRETABLE -- read the DOSE column first and refuse the win rate if the
# dose did not land. Two points (20 and 0) make it a curve, not a point.
LAUNCHRES20 bots/_v189launchres20  bots/_v169launchlate160  5408 140000
LAUNCHRES0  bots/_v189launchres0   bots/_v169launchlate160  5408 141000
# --- SENTSAFE — the `#32` arm, the largest measured gap on the board ---
# Penalise forward-sentinel SITES inside visible enemy turret coverage, reusing
# the `get_attackable_tiles_from` machinery at raid.py:500-527. SOFT PENALTIES
# (gunner 8, sentinel 3, gunner-ROTATION 4) rather than a hard veto, so it
# degrades gracefully when every site is covered — the idiomatic form here.
# VERIFIED BY ME, not taken from the agent (its report had not landed):
#   * frozen FIRST then verified — md5 94fe67dd3091e033bfa7db682f55f5f5.
#     ⛔ THIS IS THE FIX FOR TONIGHT'S OWN DEFECT: I froze COLLARVOL/SALTIDLE and
#     TRUSTED them, and BOTH froze stale. Freeze-then-verify, never freeze-then-trust.
#   * all 4 files compile; one master flag `LOKI_SENTSAFE_ON`.
#   * ⭐ FLAG-OFF EQUIVALENCE 6/6 IDENTICAL vs the parent, `NOISE_ON = False` in
#     both trees, 3 maps x 2 seeds vs `_det_opp_v56`. This is `gate.py:226-261`'s
#     control-equivalence check ACTUALLY RUN — impossible before tonight, because
#     the engine was wrongly believed nondeterministic.
#   * DOSE: flag ON vs OFF changes the game in 2/8 deterministic cells. Sparse,
#     and EXPECTED to be — our own bot builds 1.26 gunners/game against the
#     field's 8-14, so the fixture under-represents the coverage this arm dodges.
# ⛔ READ, DECLARED BEFORE THE SHARD: the headline metric is NOT win rate. It is
# SENTINEL MEDIAN LIFETIME and DESTROYED-SHARE, ours vs parent, BUILD/DEATH paired
# on tile — the same instrument that produced 71%/12-rounds vs their 34%/34.
# ⛔ AND A FILTER THAT PLANTS FEWER SENTINELS HAS CURED THE SYMPTOM BY REMOVING
# THE PATIENT: check forward-sentinels/game against the parent FIRST. Materially
# fewer = FAILURE, not a trade.
SENTSAFE    bots/_v188sentsafe_f   bots/_v169launchlate160  5408 142000
# ⛔⛔ SENTSAFE RE-QUEUED ON `_g`, NOT `_f`. THE `_f` SNAPSHOT WAS STALE.
# I froze `_v188sentsafe_f` at 94fe67dd… and the build agent then finished,
# leaving the real tree at cac092d5…. Caught at 0 rows, so nothing was wasted —
# **and it is the THIRD stale freeze of the session, in the same hour I wrote the
# delta about the first two.** ⇒ B2 is upgraded: a snapshot is only valid once
# its BUILD AGENT HAS REPORTED. Freezing to defeat an edit race and then queueing
# before the agent finishes just moves the race earlier.
# RE-VERIFIED ON `_g` (md5 cac092d5de2c524b35238edcce7eae65): 4/4 compile ·
# master flag present · **FLAG-OFF 8/8 IDENTICAL to the parent** (deterministic,
# 4 maps x 2 seeds vs `_det_opp_v56`).
# ⭐ AND THE AGENT'S MEASUREMENT INVERTED MY BRIEF, which is why the arm is worth
# running: I warned that SENTINEL coverage might blanket every site and starve
# the plant, and suggested gating on GUNNERS only if so. **All 14 redirects were
# driven by SENTINEL coverage and ZERO by gunner coverage — gunner-only gating
# would have dosed exactly nothing**, because when a site sat on a gunner ray so
# did its alternatives. Forced-to-build-anyway is only 12.5%.
# REQUIREMENT 2 HOLDS BY CONSTRUCTION, not by luck: 497 forward sentinels vs the
# parent's 498 (-0.2%). The arm re-CHOOSES among admissible sites; it never
# refuses one. Median kill round 171 both. 0 crashes in 192 games, positive
# control fired 8 tracebacks.
# ⚠ DOSE IS SPARSE AND STRUCTURALLY CAPPED: 3.4% of builds redirect, and
# 92.7% of games are byte-identical, so the whole effect lives in 14 of 192.
# ⛔ THE CEILING IS PHYSICS, NOT TUNING: a builder sees r^2=20 and a sentinel
# reaches r^2=32, so the coverage set is a LOWER BOUND — the arm cannot dodge a
# sentinel that outranges the unit choosing the site. An inside-band read is
# therefore NOT "coverage does not matter"; it is "we could only see part of it".
# ⚠ ROTATION TERM MEASURED AND REJECTED: 3.4% -> 4.4% redirects for 8x the
# coverage calls, and it RAISED forced-builds 12.5% -> 17.4%. Left in, flagged off.
SENTSAFE2   bots/_v188sentsafe_g   bots/_v169launchlate160  5408 143000
# ⛔ GUNAX0 — AMENDMENT TO THE READ, ADDED ~21:2xZ BEFORE READ-OUT (side lane).
# ⚠ DISCLOSURE FIRST, because this is NOT a blind amendment: I have seen the
# partial (46.66%, n=703, band +-3.70) on the status board. **The branch this
# amendment governs is the NULL branch, and the partial does not currently point
# there** — it trends OUTSIDE-BELOW, i.e. toward "the flag is load-bearing".
# Recording that so the amendment cannot later be read as data-driven.
#
# THE PROBLEM: a null on GUNAX0 has TWO readings with OPPOSITE actions.
#   "NO EFFECT"     -> the avoidance mechanism is worthless, and #30's sentinel
#                      term is the SAME mechanism pointed at another turret, so
#                      #30 dies with it.
#   "NO POPULATION" -> the mechanism is untested because its TARGET MOVED, and
#                      #30 becomes MORE urgent, not less.
# ⭐ AND THREE INDEPENDENT SURFACES NOW SAY THE TARGET HAS MOVED:
#   (a) research's forward-death cut on shipped trees: 68.4% sentinel-covered vs
#       64.8% gunner-covered, against an all-time 92% gunner;
#   (b) my enemy-turret-mix shift: their sentinel share 21.4% -> 48.8%;
#   (c) ⭐ the SENTSAFE build, a MECHANISM channel rather than a death
#       attribution: **all 14 site redirects were driven by SENTINEL coverage and
#       ZERO by gunner coverage.**
# ⇒ REQUIRED COLUMN BEFORE ANY GUNAX0 VERDICT: **gunner-covered forward deaths
# per game IN THE CONTROL ARM.** Baseline 0.60/game, movement bar >=0.15/game.
#   * control-arm gunner-covered rate NEAR ZERO -> **NO POPULATION.** The leg
#     cannot resolve the flag at ANY n. It says nothing about avoidance and must
#     NOT be quoted against #30.
#   * control-arm rate at ~0.60 and the arms still tie -> **NO EFFECT**, and that
#     verdict does reach #30.
# WITHOUT THIS LINE A NULL DEFAULTS TO THE FIRST READING AND TAKES #30 WITH IT.
# ============================================================================
# --- s35 BOOT BATCH, 2026-08-13 ~04:4xZ. THE SALT SURPRISE. ---
# ⭐⭐ SALT READ 61.00% ON n=5408 AND ITS OWN PREREG SAID "EXPECTED TO LOSE".
# Calibrated on the SAME contrast the same night: SHIPGATENULL 49.56%,
# NEG169 40.53%. z=+16.2 -- the multiplicity warning (62 arms at 5%) does not
# reach a z of 16. Kills 3116 vs 1936 of 5408 (57.6% vs 35.8% core-kill share),
# which is the PRIMARY currency direction under R1000_IS_DEFEAT.
# ⛔ AND IT CONTRADICTS THE LIVE 25-GAME LEG THAT KILLED IT (13 kills at median
# r179 vs pooled 129, p=0.008). One of those two is wrong. Local n=5408 with a
# both-ways-calibrated fixture beats live n=25 against a POOLED baseline on
# precision -- but live is FIXTURE_OF_RECORD, so neither settles it alone.
# ⇒ THIS BLOCK BUYS: (1) an independent replication on FRESH SEEDS, and
#   (2) the two single-mechanism ablations, because a 61% we cannot attribute
#   is a 61% we cannot iterate on.
# SALTREP -- SAME PAIR, DISJOINT SEED RANGE (133000..133337 -> 200000..200337).
# The one check that separates "real" from "seed artefact" without new code.
SALTREP     bots/_v178salt         bots/_v169launchlate160  5408 200000
# SALTIDLE2 -- _v187saltidle_f NEVER GOT A ROW (cancelled at 0 as non-dosing on
# a stale freeze). It is the gated variant and is now the RIGHT-HAND point of a
# dose curve rather than a revival test, because the ungated arm did not lose.
SALTIDLE2   bots/_v187saltidle_f   bots/_v169launchlate160  5408 201000
# ============================================================================
# --- s35, ~05:0xZ. SALT ATTRIBUTION. Three ONE-LINE ablations of _v178salt. ---
# ⭐ WHY: SALT shipped to the live slot at 61.00% and WE CANNOT SAY WHICH HALF
# DID IT. The design doc asserts the two behaviours "only pay together" and
# "neither half is worth shipping without the other" -- that is a CLAIM, it was
# never measured, and it is exactly the kind of claim a one-line flag can settle.
# A 61% we cannot attribute is a 61% we cannot iterate on.
# ⛔ ALL THREE ARE VERIFIED SINGLE-LINE DIFFS FROM _v178salt (+1/-1 each), so
# any delta is attributable to that line and nothing else. Same contrast as SALT
# (_v169launchlate160), so SHIPGATENULL 49.56% / NEG169 40.53% both apply and no
# band is borrowed.
#
# READ THE THREE TOGETHER:
#   CUTONLY ~= SALT           -> the barrier is decoration; the CUT is the plank
#   BARONLY ~= SALT           -> the cut is decoration; the BARRIER is the plank
#   both << SALT              -> the design doc's "only pay together" is CORRECT
#                                and this is a genuine interaction, not a sum
#   NOBLOCK ~= SALT           -> the speculative pre-emptive barrier is free to cut
#   NOBLOCK  < SALT           -> denying the REBUILD SEAT is carrying the result,
#                                which would be the most surprising outcome here
# CUTONLY -- LOKI_SALT_MAX_PER_UNIT 4->0. Kills BOTH barrier paths (each needs
# can_pay_barrier, raid.py:414). Leaves the belt-cutting melee alone.
SALTCUTONLY bots/_v190saltcutonly bots/_v169launchlate160 5408 202000
# BARONLY -- LOKI_SALT_CUT_MAX 40->0. Kills the melee. Leaves both barrier paths.
# ⚠ NOTE ITS DOSE IS EXPECTED TO FALL: path (1) salts a tile where a belt piece
# DIED, and with our own cut disabled it only fires on belts that die to other
# causes. A low reading here is partly a DOSE story, not purely a mechanism one.
SALTBARONLY bots/_v191saltbaronly bots/_v169launchlate160 5408 203000
# NOBLOCK -- LOKI_SALT_BLOCK_ON True->False. Removes ONLY the speculative third
# path (barrier ground beside a LIVE belt to deny the repair bot its approach).
# The cleanest of the three: it subtracts one behaviour and touches no budget.
SALTNOBLOCK bots/_v192saltnoblock bots/_v169launchlate160 5408 204000
# ============================================================================
# --- s35 ~05:3xZ. THE COUNTER PROGRAMME. Magnus's frame and Magnus's bar. ---
# ⭐ FRAME: *"now that we have a salt bot, we can probably pit counters against
# it and see what we can find and then put them together?"*
# ⭐ BAR (his, and it is also PROGRAMME.md's `COMPARE_AGAINST:
# previous_line_iteration`): *"A counter + salt vs just salt should win more
# than it loses"* — i.e. **salt+counter vs salt must clear 50%.** SALT is v122,
# the live incumbent, so it IS the control the next iteration answers to.
#
# ⭐⭐ THIS CONTRAST IS ALREADY HALF-CALIBRATED FOR FREE, WHICH IS WHY ONLY ONE
# CELL IS QUEUED HERE:
#   NEG cell — ALREADY MEASURED. `_v169launchlate160` vs `_v178salt` = 2,109/5,408
#   = **39.00% at n=5,408**. That is the SALT battery read from the other side:
#   a real, powered, known-worse arm on this exact contrast. Nothing to run.
#   NULL cell — THE ONE THING MISSING, queued below. Without it the band applied
#   to every counter arm is BORROWED, which the read-out flags in red.
# SALTNULL -- byte-identical copy of _v178salt against itself. Verified identical
# by `diff -r --exclude=__pycache__` at build time (zero differing files).
# ⛔ IT MUST READ ~50%. If it does not, the fixture is biased on this contrast and
# NO counter verdict may be read at all.
# ⛔ RENAMED FROM `_v178salt_null`, WHICH THE COLLISION GUARD CORRECTLY REFUSED:
# `_v178salt` is a SUBSTRING of `_v178salt_null`, and overnight.sh scores by
# substring match on the basename, so it would have read ~100% for whichever
# side matched first. Guard worked. My name was wrong.
NULLSALT    bots/_v195nullcell     bots/_v178salt         5408 205000
# --- THE BAR ARMS. `salt+counter vs salt` -- Magnus's stated bar, verbatim:
# *"A counter + salt vs just salt should win more than it loses."*
# ⛔ SHIP BAR IS **ABOVE THE BAND (>51.33%)**, NOT ">50" -- a bare "clear 50%" is
# met by 50.01% and cannot fail. Our own SHIPGATE160 read 49.44% and was
# correctly called no-information on this same +/-1.33pp band. Inside band =>
# NO SHIP, pre-committed in docs/prereg/PREREG-anti-salt-counter-2026-08-13.md.
# ⛔ NULLSALT GATES BOTH: if the byte-identical cell does not read ~50%, neither
# of these may be read at all.
# Both are _v178salt + ONE counter block, ported onto salt (eco.py is
# byte-identical between the two parents, md5 3b636ca8..., so the port is a
# swap, not a merge). Diffs vs salt: saltclear +36/-3, saltroute +34/-3 --
# matching the source arms exactly. Smoke-tested 4 games, 0 tracebacks.
SALTCLEAR   bots/_v196saltclear    bots/_v178salt         5408 206000
SALTROUTE   bots/_v197saltroute    bots/_v178salt         5408 207000
# --- THE ATTRIBUTION ARMS. Counter WITHOUT salt, vs salt. These separate
# "the counter defends" from "the counter is worth its price inside a salt bot".
# Their control point is the FREE NEG: _v169launchlate160 vs _v178salt = 39.00%
# at n=5,408. A counter-only arm should sit ABOVE 39.00% if the counter defends
# at all. ⚠ That is a SALT-SIZED reference, not a counter-sized one -- it says
# nothing about resolution near 50 (D13), which is what NULLSALT is for.
CLEARONLY   bots/_v193clearlink    bots/_v178salt         5408 208000
ROUTEONLY   bots/_v194routelink    bots/_v178salt         5408 209000
# --- MAGNUS, 06:1xZ: *"Wait 65% against our current slot?"* NO -- and the
# question exposed that I had stated it loosely. EVERY salt-family arm is
# controlled on `_v169launchlate160` (= v116, the bot SALT REPLACED), NOT on
# `_v178salt` (= v122, the LIVE slot). SALTIDLE and SALT HAVE NEVER PLAYED
# EACH OTHER.
#   SALT     61.00%  n=5,408  vs v116   (band +-1.33)
#   SALTIDLE 65.79%  n=1,289  vs v116   (band +-2.73)
# The 4.8pp gap is an UNPAIRED CROSS-BATTERY inference: combined band
# +-3.04pp, so it is marginally outside -- suggestive, NOT settled, and the
# direct contrast is strictly better AND cheaper (one proportion vs a
# theoretical 50 null, band +-1.33 at full n, no sqrt-2 penalty).
# ⇒ IDLEVSALT IS THE ARM THAT ANSWERS HIS QUESTION. Its control is the LIVE
# SLOT. Bar is the same one his counter bar carries: >51.33% to beat the slot,
# inside band => the slot stays. NULLSALT gates it like everything else on
# this contrast.
# ⚠ AND ITS DOSE IS NOW VERIFIED (196 SALT events + 2,571 funnel events over 8
# games with logging forced on) -- it was written off last night as
# "non-dosing" on a reading taken through a switched-off log.
IDLEVSALT   bots/_v187saltidle_f   bots/_v178salt         5408 210000
