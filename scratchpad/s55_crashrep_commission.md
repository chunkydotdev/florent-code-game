# COMMISSION — PREREG DRAFT: CRASHREP-BC leg (pinned re-fire of the 5ee3afec crash cascade)

Commissioned by: BUILDER s55, 2026-08-22 ~10:2xZ. Fresh opus drafter per the s40 rule
(no inherited session context beyond these named inputs; the builder ratifies the judgment
lines and types the lock commit).

PROVENANCE (inputs — read these, nothing else is assumed):
- docs/research/DECODE-firstcontact-v180-2026-08-22.md §2 (the cascade: BC core removed at
  284/500; ten age<=1 full-HP builder removals, all of the 65-game pool's instances in one
  game; trigger UNIDENTIFIED, ring-barrier candidate REFUTED at 65/65 base rate) and §8
  (the named lead: pinned re-fire, same map+seat question).
- docs/research/PREREG-amendments-and-lock-obligations-2026-08-09.md (the template + lock
  obligations; the PROVENANCE rider requires this commission file named verbatim in the
  prereg's PROVENANCE line).
- CLAUDE.md "WHAT LOKI IS" (crash-induction is the APPROVED class; a new TRIGGER needs no
  new organiser question, only a new MECHANISM would) + the TARGET-BAND gate rule (run
  tools/target_value.py; one line in the prereg: TARGET BAND ...).
- docs/fcode-cli.md §pinning (~line 330): `fcode match unrated <team> --match <past_match_id>`
  plays the opponent's submission from that past match. The source match: 5ee3afec-17b1-46a5-
  ba8d-116a25cceaba (BC v68). Our side plays the ACTIVE submission: v180 "Skalman rc619.1"
  (already uploaded; activation by integer inside a validated window, restore v176 after).
- tools/crash_census.py (the instrument; --selftest exists and passes; it reproduced the
  cascade's 10 candidates on the primary in this session).
- Constraints the prereg MUST encode: 5 unrated fires per 10-min window (re-derive cadence
  from fresh createdAt rows in the firing shell, never hardcode); exposure windows are also
  free looks for opponents (the Jacobs leak channel — priced, not prohibitive); rated-leak
  budget ~-8 Elo per leaked match (the validated read-then-restore-by-integer procedure has
  run 5 windows with zero leak); R1000_IS_DEFEAT and the r300 guard do NOT bind a mechanism
  leg (this is a reproducibility probe, not a strength test — Magnus's rc ruling frame).

## WHAT THE DRAFTER PROPOSES (builder ratifies)
1. HYPOTHESIS + treatment bar: what counts as "the cascade reproduced" (suggested shape:
   >=1 game in the leg showing >=N enemy-builder crash-candidates (age<=1, full-HP, no
   damage event) via crash_census; the drafter picks N and the denominator and defends
   them; note the base rate measured: 10 candidates in 1 of 65 games, 0 in 64).
2. FALSIFIER: what result closes the road (e.g. 0 candidate games in 25 games -> the
   cascade is not reproducible by re-fire; state what that does and does not prove about
   determinism, since match maps/seats vary across unrated games).
3. Dose accounting: 5 matches x 5 games; the map/seat question (the cascade game was
   30x30 our-seat-B; unrated matches draw their own maps — the drafter states how map/seat
   variation limits the claim and whether the leg is a REPRODUCIBILITY probe (any-map) or a
   DETERMINISM probe (same-map only), and scopes the claim accordingly).
4. TARGET BAND line via tools/target_value.py for Bean counters (a mechanism leg may fire
   on a low-payout target — the gate is a written number, not a veto).
5. Instrument lines: crash_census (driven both ways this session), the win/loss and
   core-residual reads; NO game-share verdict language (mechanism leg).
6. Window plan: one 5-fire window, validated procedure (same-shell holder read, CPU test
   NOT needed — v180 already passed it 08:24Z, cite it), restore v176 by integer, side-lane
   cert per the s54 template.

## DELIVERABLE
docs/prereg/DRAFT-PREREG-LEG-CRASHREP-BC-2026-08-22.md — committed as DRAFT by you with
PROVENANCE line naming this file; the builder ratifies, renames per convention, runs
tools/prereg_check.py, and types the lock commit. Do not touch bots/, tools/, QUEUE.md,
or fire anything.
