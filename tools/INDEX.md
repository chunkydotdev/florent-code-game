# tools/ — INDEX

**GENERATED by `tools/gen_index.py`. Do not edit by hand** — regenerate with
`.venv/bin/python tools/gen_index.py`. A hand-edited index is a second source
of truth, and this repo has measured that failure mode more than once
(`docs/tooling.md` named 6 of 86 tools; a boot file bounded a 57k-line file
at "41k lines" for days).

**Every tool honours `--help`**: side-effect-free, prints its docstring, exits 0.
Enforced by `tests/test_instruments.py::TestHelpContract`.

`mutates` = a static scan found a write or a subprocess call. It is deliberately
over-inclusive: **trust `read-only`, and check before running a `mutates` tool
blind** — never the reverse.

⭐ **If you only run one thing: `.venv/bin/python tools/now.py`** — what is live,
what the control is, and how stale every state surface is.

## Live state — what is true right now

Start here. These answer questions about the ladder, the holder and the control. **`now.py` is the single canonical entry point**; the others are narrower reads of the same surfaces.

| tool | | what it answers |
| --- | --- | --- |
| [`control_pin.py`](control_pin.py) | ✏️ | CONTROL-TREE PIN — refuse to launch a shard whose control moved under it. |
| [`delta_status.py`](delta_status.py) | ✏️ | DELTA STATUS — which retro deltas are ENFORCED, and which are only prose? |
| [`era_guard.py`](era_guard.py) | 👁 | ERA GUARD — refuses to return an our-side statistic unless the caller names an era. |
| [`fleet_health.py`](fleet_health.py) | ✏️ | FLEET HEALTH — what is SUPPOSED to be running, and is it? |
| [`freshness.py`](freshness.py) | ✏️ | SHARED FRESHNESS ASSERTION — one helper, because this is one bug in four files. |
| [`now.py`](now.py) | ✏️ | NOW — the one canonical state block. Run it at boot, before any verdict. |
| [`plank_status.py`](plank_status.py) | ✏️ | PLANK STATUS — does HANDOVER still agree with the plank's own artefacts? |
| [`ship_ledger.py`](ship_ledger.py) | ✏️ | ship_ledger.py — cumulative Elo since a holder's activation, and leaked |

## Gates — things that REFUSE

Run before an action, and they exit nonzero to stop it. This repo's measured finding is that a tool that exits 1 binds where a paragraph does not.

| tool | | what it answers |
| --- | --- | --- |
| [`audit_trigger.py`](audit_trigger.py) | ✏️ | Audit trigger — is the project producing analysis instead of decisions? |
| [`auto_gate.py`](auto_gate.py) | ✏️ | auto_gate.py — STOP arithmetically-dead shards without waking a human. |
| [`cite_check.py`](cite_check.py) | 👁 | cite_check.py -- does every URL the tactics library CITES actually exist? |
| [`claim_check.py`](claim_check.py) | 👁 | CLAIM CHECK — every "mutation-tested" claim must point at ITS OWN record. |
| [`cli_capabilities.py`](cli_capabilities.py) | ✏️ | CLI CAPABILITY WATCH — alarm on platform capabilities we have never triaged. |
| [`corpus_sanity.py`](corpus_sanity.py) | 👁 | Fail loudly on corpus columns that are silently dead. |
| [`gate.py`](gate.py) | ✏️ | Pre-flight guard for a plank battery. Refuses to run a battery that cannot |
| [`inert_check.py`](inert_check.py) | ✏️ | INERT CHECK — mechanised form of D42: can anything in the DIFF move the |
| [`name_check.py`](name_check.py) | ✏️ | NAME CHECK — does every identifier a document NAMES actually exist? |
| [`preflight.py`](preflight.py) | 👁 | Pre-ship gate: refuses the "ready to ship" line until the pipeline ran. |
| [`prereg_check.py`](prereg_check.py) | ✏️ | PREREG CHECK — the pre-registration checklist as a tool that exits 1. |
| [`queue_check.py`](queue_check.py) | ✏️ | QUEUE FLOOR ALARM — is there anything the builder can start TODAY? |
| [`rate_budget.py`](rate_budget.py) | ✏️ | RATE BUDGET METER — how much of the 5-per-20-minutes is left, measured. |
| [`slot_rule.py`](slot_rule.py) | 👁 | The GOVERNING slot rule, as ONE importable statement. |
| [`slot_sprt.py`](slot_sprt.py) | 👁 | Ladder-side SPRT for the slot: a stop-loss with a statable error rate. |
| [`sprt.py`](sprt.py) | 👁 | Early-stopping match gate: same matches as arena.py, but stops as soon as the |
| [`target_value.py`](target_value.py) | ✏️ | TARGET VALUE GATE — what does beating this opponent actually PAY? |

## Shipping and the ladder slot

⛔ `fcode submit` AUTO-ACTIVATES. Only ever submit via `submit_clean.py`.

| tool | | what it answers |
| --- | --- | --- |
| [`escape_tape.py`](escape_tape.py) | ✏️ | ONE ROW PER INVOCATION for the tools that carry escape flags. |
| [`ladder_census.py`](ladder_census.py) | ✏️ | *(no docstring)* |
| [`leg_name.py`](leg_name.py) | ✏️ | LEG NAME ALLOCATION — one registry, so two planks cannot share a LOKI number. |
| [`leg_read.py`](leg_read.py) | ✏️ | Read a LIVE-UNRATED LEG in the programme's currency, from the platform. |
| [`leg_record.py`](leg_record.py) | 👁 | LEG RECORD — the fixture fingerprint of a live leg's cells. |
| [`match_ledger.py`](match_ledger.py) | ✏️ | MATCH LEDGER — stamp OUR initiative on every match we create. |
| [`oppver_window.py`](oppver_window.py) | ✏️ | OPPONENT-VERSION WINDOW CHECK — did a cell ship a new bot mid-leg? |
| [`reprice.py`](reprice.py) | 👁 | Re-price a plank battery under BOTH estimators, from its per-game JSON. |
| [`submit_clean.py`](submit_clean.py) | ✏️ | Submit a bot with CODE ONLY — never our documents. |

## Running batteries and the fleet

Local shards and remote workers. The operator interface is: append a line to `scratchpad/corefill_work.txt`.

| tool | | what it answers |
| --- | --- | --- |
| [`arena.py`](arena.py) | ✏️ | Run two bots against each other many times and report who's actually better. |
| [`det.py`](det.py) | ✏️ | Deterministic-paired A/B: run two candidates against a fixed opponent on |
| [`dose.py`](dose.py) | ✏️ | SERIAL DOSE CHECK — does the arm actually DO the thing? Local, minutes. |
| [`fleet_dispatch.py`](fleet_dispatch.py) | ✏️ | FLEET DISPATCH — ONE central queue of shards; work flows to whichever core frees first. |
| [`mech_battery.py`](mech_battery.py) | ✏️ | Paired battery that KEEPS its replays, and reads the mechanism out of them. |
| [`overnight_read.py`](overnight_read.py) | 👁 | OVERNIGHT READ-OUT — pools on the DECLARED denominator, never discards. |
| [`rebase_arm.py`](rebase_arm.py) | ✏️ | REBASE an arm built on one chassis onto a newer chassis, by real 3-way merge. |
| [`stack.py`](stack.py) | ✏️ | STACK — compose N planks into one bot tree, by real merge, not file-picking. |
| [`stub_engine.py`](stub_engine.py) | 👁 | A minimal FAKE ENGINE implementing the Florent Code League Controller API |
| [`treehash.py`](treehash.py) | 👁 | Version-identity digest over an ENTIRE bot directory, not just main.py. |
| [`tune.py`](tune.py) | ✏️ | CEM tuner for bot constants, driven by arena win rate against a fixed opponent. |

## Reading the replay corpus

⛔ Query `corpus/` before writing a new decoder — a question that used to cost a session now costs a `csv.DictReader`.

| tool | | what it answers |
| --- | --- | --- |
| [`bank_trace.py`](bank_trace.py) | 👁 | PER-ROUND BANK TRACE — the instrument arm B's falsifier actually needs. |
| [`border_defect_scan.py`](border_defect_scan.py) | 👁 | border_defect_scan.py -- which teams look like they have the OFF-MAP QUERY defect? |
| [`camp_detect.py`](camp_detect.py) | ✏️ | CHRONIC-CAMP DETECTOR — the PRECONDITION arm B's falsifier is conditional on. |
| [`choke_census.py`](choke_census.py) | 👁 | CHOKE CENSUS — does the map pool ADMIT a blockade at all? |
| [`collar_census.py`](collar_census.py) | 👁 | Does the collar actually seal, and does sealing stop the heal line? |
| [`core_entry.py`](core_entry.py) | 👁 | Core-entry throughput, read straight off `DistributeResources`. |
| [`crash_cells.py`](crash_cells.py) | ✏️ | QUEUE #17 — DOES THE CRASH WEAPON ACTUALLY FIRE?  A LOCAL, BOTH-WAYS DRIVE. |
| [`crash_census.py`](crash_census.py) | ✏️ | Detect units destroyed by an UNCAUGHT EXCEPTION, from replay bytes alone. |
| [`dodge_capture.py`](dodge_capture.py) | 👁 | How much of the available headroom does the shipped DODGE rule capture? |
| [`field_deaths.py`](field_deaths.py) | 👁 | Field mechanism read: our builder deaths per 1k rounds, by OUR version. |
| [`fwd_read.py`](fwd_read.py) | ✏️ | FORWARD EFFICIENCY READ-OUT — the numerator, the denominator, and the guard. |
| [`game_census.py`](game_census.py) | ✏️ | Per-GAME census from the API -- no replay downloads, no parsing. |
| [`kidnap_fate.py`](kidnap_fate.py) | ✏️ | kidnap_fate.py -- what happens to an ENEMY builder after WE throw it? |
| [`loki27_read.py`](loki27_read.py) | ✏️ | LOKI-27 FERRY-FIRST READ-OUT — frozen BEFORE the control arm was fired. |
| [`nav_lock_census.py`](nav_lock_census.py) | ✏️ | NAV LOCK CENSUS — how much of our builder workforce is stuck in a two-tile |
| [`panel_read.py`](panel_read.py) | ✏️ | PANEL-CAL-1 pooled readout (research s36). |
| [`peck_read.py`](peck_read.py) | ✏️ | LOKI-19 bars 5a (DOSE) and 5b (MECHANISM) read: builder attacks on the |
| [`rdiff.py`](rdiff.py) | 👁 | Replay turn-differ: compare two .replay26 files turn-by-turn and report the |
| [`replay_census.py`](replay_census.py) | 👁 | Extract strategy signals from Florent Code League `.replay26` files. |
| [`ring_read.py`](ring_read.py) | 👁 | LOKI-16 MECHANISM read: enemy-spawn-ring occupancy, from archived replays. |
| [`ring_retention.py`](ring_retention.py) | ✏️ | ⛔⛔ RETIRED 2026-08-11. THIS DECODER IS WRONG AND IT REFUSES TO RUN. |
| [`tle_census.py`](tle_census.py) | ✏️ | TLE / CPU-budget census over the replay archive (read-only). |
| [`triarm_read.py`](triarm_read.py) | ✏️ | TRI-ARM READ — decode-side instrument for PREREG-triarm-live-2026-08-13. |
| [`turret_selfkill_census.py`](turret_selfkill_census.py) | ✏️ | Do opponents' GUNNERS/SENTINELS destroy themselves by firing at zero ammo? |
| [`wincond_backfill.py`](wincond_backfill.py) | ✏️ | Backfill `turns` + `wincond` onto EVERY archived replay, from the replay bytes. |

## Statistics and power

Design effects, MDE, effective n. ⛔ Games are NOT independent — see `CLAUDE.md` on DEFF before quoting any interval.

| tool | | what it answers |
| --- | --- | --- |
| [`effective_n.py`](effective_n.py) | 👁 | EFFECTIVE n OF A LOCAL BATTERY — how many DISTINCT games are behind a denominator. |
| [`fixture_starvation.py`](fixture_starvation.py) | ✏️ | FIXTURE STARVATION DETECTOR — is a compute surface idle because nobody stocked it? |
| [`mde.py`](mde.py) | ✏️ | THE BAR: what is the SMALLEST change our current tools can actually detect? |
| [`pair.py`](pair.py) | ✏️ | Interleaved same-batch runner: every candidate plays a fixed opponent on the |
| [`paired_vs_pooled.py`](paired_vs_pooled.py) | ✏️ | Pooled-vs-paired CI comparison for arena batteries -- AND THE REFUTATION IT PRODUCED. |
| [`score.py`](score.py) | 👁 | KILL-SPEED SCORE — one number per game, per `docs/research/SPEC-kill-speed-score-2026-08-10.md`. |
| [`slot_denoms.py`](slot_denoms.py) | 👁 | DENOMINATORS FOR THE SHIP-GATE COLUMNS — `dd_z`, `resolvable_k`, `p_null`, |

## Maps and the engine

Map generation, encoding, and engine-level probes.

| tool | | what it answers |
| --- | --- | --- |
| [`ceiling.py`](ceiling.py) | ✏️ | Price a change in the CEILING METRIC: core-kill rate and time-to-kill. |
| [`cpu_lag_probe.py`](cpu_lag_probe.py) | ✏️ | Per-round CPU trace: does OUR entity count drive the OPPONENT's exec time? |
| [`loki17_mech.py`](loki17_mech.py) | ✏️ | LOKI-17 MECHANISM: shootable-on-build, on retained local replays. |
| [`loki19_5d.py`](loki19_5d.py) | 👁 | LOKI-19 BAR 5d — the falsifier. `hold_pinned` retention, treatment vs control. |
| [`loki9_facing.py`](loki9_facing.py) | 👁 | LOKI-9 DID-IT-FIRE CHECK — are our new home turrets aimed at the enemy core? |
| [`make_map.py`](make_map.py) | ✏️ | Generate .map26 map files without the platform or the browser map editor. |
| [`map_admits.py`](map_admits.py) | 👁 | MAP ADMISSION CHECK — does the fixture's terrain ADMIT the mechanism? |
| [`map_encode.py`](map_encode.py) | 👁 | Encode maps/*.map26 into the bot's MAP_CODES packing — with the control |
| [`rentgun_drive.py`](rentgun_drive.py) | 👁 | SCENARIO DRIVE for LOKI-RENTGUN (bots/_v313rentgun) on the stub engine. |

## Unclassified

⚠ **These matched no group pattern in `gen_index.py`.** That is a prompt, not an error: either the name does not say what the tool does, or `GROUPS` needs a pattern. Listed openly rather than filed somewhere plausible.

| tool | | what it answers |
| --- | --- | --- |
| [`boot.py`](boot.py) | ✏️ | BOOT — the whole boot context for one lane, on one screen, under a token budget. |

---

**87 tools indexed** (👁 read-only, ✏️ writes or shells out). Subdirectories not covered here: `tools/monitors/` (daemons — see `fleet_health.py` for what should be running), `tools/corpus/` (corpus builders), `tools/dash/`, `tools/vps/`.
